---
title: "Emails"
description: "How HouseLoop sends email — one-to-one and bulk campaigns — the AI-assisted template system, recipient lists, and the from-identity routing rules."
---

{/* legacy-banner */}
<Warning>
  **LEGACY — UNVERIFIED.** This page predates the Master QA build and has NOT been verified
  against the product. The canonical docs are being rewritten lane-by-lane as each lane goes
  green — check the lane's tab first.
</Warning>

**Emails** is the email channel: a teammate, the AI agent (Myli), or a bulk campaign sends mail to a lead, and
the reply threads back on the lead. The governing rule is simple — **the *from* identity decides everything**:
the sending alias, the rail (Gmail for humans, Resend for AI/bulk), the signature, and where replies come back.

## What's in here

| Page | What it is |
|---|---|
| **[Campaigns](/crm/email-campaigns)** | Send one email to many contacts — pick a list, compose from a template or custom, choose a warmed-up sending inbox, and send now or schedule. |
| **[Email Templates](/guides/email-templates)** | The AI-assisted template system — describe → generate → preview → lock production HTML emails with merge fields. |
| **[Lists](/crm/email-lists)** | Named audiences of contacts (static, smart-rules, or saved-search) used as campaign recipients. |
| **[Email Settings](/guides/email-settings)** | The from-identity routing rules — the three senders (human/Gmail, Myli/Resend, system/bulk), signatures, and reply routing. |

## How an email flows

1. A send resolves the **from identity** — a teammate (Gmail OAuth), Myli (Resend), or system/bulk (Resend).
2. That identity sets the **alias, signature, and reply-to** — sent from one place, replies can come back to another.
3. Each recipient send is logged (`hl_email_send_log`); campaigns fan out one row per member.
4. Inbound replies thread back on the lead and fire a notification.

For the end-to-end architecture (inbound capture, one-writer logging, and how email fits alongside texts and
calls), see the comms subsystem map: **[CRM Comms — Texts, Calls, iMessage](/guides/crm-comms)**.

## Sending identity — who an email goes out AS (verified 2026-07-16)

A live audit (Caleb) confirmed exactly how each send resolves its **from** identity, the real addresses, and
what works today. The rule holds: **the from-identity decides the rail, the signature, and where replies land.**

| What you send | Goes out AS | Rail | Working today |
|---|---|---|---|
| **Composer** — an agent typing a 1:1 to a lead | the agent's **real Gmail**: `ana@` / `myli@` / `zach@houseloop.ai` | Gmail | ✅ **live-tested** |
| **Workflow "send as Ana / Myli / Zach"** | `ana@` / `myli@` / `zach@houseloop.ai` | Resend (houseloop.ai is a verified domain) | ✅ resolves + Resend live |
| **Workflow with no assigned agent · system alerts** | **`notifications@houseloop.ai`** | Resend | ✅ |
| **Home-Value Report** | `notifications@houseloop.ai` | Resend | ✅ |
| **Campaign / branded / home-search** (the "warmup pool") | `recommendations@` / `my-listing@` / `my-saved-home@` / `instant-updates@` (`@mail.houseloop.ai`) | Gmail pool | ❌ **pool not OAuth-connected — dormant by design** |

**The facts, confirmed live:**
- **The system / notifications sender is `notifications@houseloop.ai`** (SMS system line `913-800-4470`). Resolved
  from `hl_settings.notification_sender.email_from`.
- **Three team mailboxes are connected + signed** — `ana@`, `myli@`, `zach@houseloop.ai` (`hl_oauth_connections`).
  A workflow set to any of them sends as that identity; a 1:1 composer send uses that person's real Gmail.
- **The identity resolver never returns a null sender** — `resolveAgentSendIdentity` (`lib/agent-runtime/resolve-send-identity.ts`)
  resolves the assigned agent, else falls back to the system sender.
- **The warmup pool (`@mail.houseloop.ai` / `@email.houseloop.ai`) is deliberately NOT connected** (all 8 inboxes
  `oauth_connected=false`). Branded / marketing / home-search email does **not** send yet — this is the intended
  "warming up, no mass sends yet" state, **not a bug.** Connecting the pool (or the DWD alternative) is a future
  decision, gated on being ready to send mass email.
- **Reality check:** in the trailing 30 days the only sends were 2 manual composer tests. Automated volume is
  effectively zero today.

The deep how-to (the three-sender model, signatures, reply-routing, the four Settings → Email tabs) lives in
**[Email Settings](/guides/email-settings)**.

## ⭐ Email Engine — the one-sender redesign (Caleb, in progress)

<Info>
**Living status — last updated 2026-07-15.** Texts have one writer (`logMessage`) — one place the rules live.
Email did not: a three-agent code audit found the send path shattered. This section is the single record of the
redesign that makes email as clean as texts, and exactly what has shipped vs. what is still coming.
</Info>

### Why (the audit)

A file-line-grounded audit of every email path found the subsystem fragmented and, in places, broken:

| Finding | Detail |
|---|---|
| **2 transports, 4 senders** | Resend + Gmail, realized as four separate sender implementations. |
| **3 duplicate Gmail stacks** | The RFC-2822 builder + OAuth token refresh + `messages/send` fetch were copy-pasted in three routes. |
| **7 hand-rolled log writes** | Each feature wrote its own `INSERT INTO hl_email_send_log` with a different column set; one logged elsewhere, one logged nothing. |
| **3 merge engines** | `{{token}}` substitution done three different ways (one is inline regex). |
| **Live bugs** | Open/click tracking silently dead for Gmail-composed + template email; the Gmail marketing path skipped unsubscribe/footer; scheduled campaigns never send (no runner); the Home-Value Report was never logged (threw on a NOT NULL, swallowed). |

The strong parts are kept, not replaced: the send-identity resolver (`resolve-send-identity.ts`), the canonical
signature system, the branded shell/footer, and the lead-scoped composer from-picker.

### The model — one of everything

Every email flows through one function — the email analogue of `logMessage`. That function owns transport,
identity, merge, compliance, tracking, and logging, so a rule lives in exactly one place.

**Transport decision (locked, Hybrid B):** system + marketing send via **Resend** (full open/click tracking);
agent 1-to-1 sends via the agent's **real Gmail** mailbox, with our own open-pixel + click-wrapper so tracking
is transport-independent. This kills the "tracking only works on Resend" bug at the root.

### Status board

| Phase | What | Status |
|---|---|---|
| **1a** | ONE `logEmailSend()` — the single writer; all 7 sites delegate to it. Relaxed `lead_id`/`inbox_used`/`subject` NOT NULLs so system/aggregate sends log (fixes the Home-Value Report never-logged bug). | ✅ **SHIPPED** (`041c… → mig 0412`) |
| **1b** | ONE Gmail transport — `gmailRawSend` + `buildRfc2822` + `refreshHlOAuthToken` in `lib/email/gmail-send.ts`; the composer + exp-grid delegate to them (~170 lines of duplication deleted). Token *source* left unchanged for safety. | ✅ **SHIPPED** |
| **1c** | The `sendEmail()` choke point — one door: guard (both transports) → transport routing (Hybrid B) → send → `logEmailSend`. **SHIPPED + LIVE-TESTED (2026-07-16):** the lead-detail composer (`/api/email/send`) + agent alerts route through `sendEmail`; **every** Gmail send is now deliverability-guarded (`sendViaGmail` + `sendEmail`). Live test: a real email sent composer→`sendEmail`→Gmail→log (`gmail_message_id` returned, log row `status=sent`); guard test refused `blocktest@example.com`. | 🟢 **CORE DONE + TESTED** — remaining: consolidate HVR / execute-step / pool senders (campaigns, send-template) onto the door + merge-engine unification (low-risk polish; already logged+guarded) |
| **2** | Provider-id split (`provider` + `provider_message_id`) → fixes open/click tracking on Gmail sends; `conversation_key` on the email tables (email joins the one-conversation model); collapse the 4× engagement storage; drop the 3 dead tables. | 🔲 planned |
| **3** | Compliance unified — one suppression source, `kind`-based enforcement inside the sender (closes the warmup-path unsubscribe hole); bounce → auto-suppress. | 🔲 planned |
| **4** | Enterprise features on the clean base — RFC threading (`In-Reply-To`/`References`), attachments, a real scheduling runner. | 🔲 planned |

### Where the code lives

- **The one writer:** `apps/houseloop-crm/lib/email/log-email-send.ts` (`logEmailSend`).
- **The one Gmail transport:** `apps/houseloop-crm/lib/email/gmail-send.ts` (`sendViaGmail`, `gmailRawSend`, `buildRfc2822`, `refreshHlOAuthToken`).
- **The Resend transport:** `apps/houseloop-crm/lib/email/resend-send.ts` (`sendViaResend` — the only path with the undeliverable-address guard today; 1c extends the guard to Gmail too).
- **Identity (kept):** `apps/houseloop-crm/lib/agent-runtime/resolve-send-identity.ts`.

<Warning>
**1c is load-bearing** — a bug in `sendEmail()` breaks *all* email at once, and it decides the transport-source
question deferred in 1b. It is built + verified as its own focused pass, with a real test send fired through each
migrated caller — never rushed. Until 1c lands, the 8 callers still send directly but all log through the one
`logEmailSend`.
</Warning>
