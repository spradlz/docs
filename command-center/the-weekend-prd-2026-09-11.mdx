---
title: "The Weekend PRD — 9/11"
description: "The weekend PRD"
---

# THE WEEKEND PRD — everything that must get done, and how the desk runs it (Fable desk, Friday 9/11, 6:10 PM EDT)

> **⚠ CLOCK: the desk's stamps in §7.4/§8 ("9:55 PM") were ~5 h fast — the true time was ~3:55 PM EDT Friday; lane Z-stamps are right. READ §7.4 AND §8 FIRST (added ~3:55 PM Friday): tonight's rulings applied and the Monday bar — they override earlier sections where they conflict.**

Written by Lee on Fable for whoever runs the desk from here, on any model. Zach: "come up with a really good PRD document with everything that needs to get done, so if I have to downgrade to Opus for the weekend we have a really good plan to work from." Zach has 8% of Fable left this week (#257.536): the desk may be Opus from here; every lane is Opus or Sonnet. This document is complete on its own. When it and the conversation disagree, this document and the rulings file win.

---

## 0 · HOW THE DESK RUNS (read before spawning anything)

1. **The record is the truth.** Every lane spawn needs a record item: `node tools/master-plan/task.mjs add "<title>" --kind <build|fix|seo|polish|decision|data|docs> --raised-by <zach|desk> --card <slug>` (the TITLE IS POSITIONAL; `--title` silently creates nothing), then `where "<match>"` to read the key back, then `ITEM-KEY: <key>` at the top of the brief. The intake hook refuses a spawn without one. Tick with `tick <key> --receipt <path>`. Retire with `retire <key> --ruling "#xxx" --reason "…" --by "…"`. Known defect: retire/void leave `done:false`, so retired items still count as open in every projection (carded).
2. **Rulings live in** `.claude/reports/myli-os/ZACH-REVIEW-RULINGS-2026-08-27.md`. Today's are #257.500 through #257.536. Append, never edit. Zach's words verbatim, then RULED.
3. **The screenshot is the spec (#257.529).** When Zach gives a screenshot, it beats every wireframe and every paraphrase. A lane building from screenshots produces ONE composite per state (Zach's shot | the reference | ours), reads it by eye, and the DESK reads it again before anything reaches Zach. A BUILT without composites is not BUILT. Same for locked wireframes: the board frame beside our screen, per state.
4. **Browsers open http://localhost:3001; Node/curl/cages use http://127.0.0.1:3001.** 127.0.0.1 never hydrates in Next 16 dev (0 React fibers); localhost does. Two lanes lost an hour to this today. Never restart :3001 under lanes (it tears the Next cache); if you must, use `scratchpad/desk/restart-3001.sh` which waits for the old PID.
5. **A 500 on `/` with tsc clean under many lanes is the Prisma client pool,** not the tree (memory). Read `/private/tmp/hl-web-dev.log` for "timeout exceeded when trying to connect" before chasing a regression.
6. **Git on the shared tree:** commit by pathspec only (`git commit -m "…" -- <paths>`), never `git add`, never a bare `git commit`, never any `git stash` (a bare stash swept 76 files off disk on 9/10; a scoped stash+pop popped another lane's entry today). A cross-file change that breaks in either order (removing an export, a required prop) lands as ONE commit naming every file; a lane broke every page for 3 minutes today by landing an export removal before its importers. Never `git add .claude/state`. Screenshots go to the session scratch dir, never the repo root (the Playwright MCP writes a bare filename to the repo root; use absolute paths).
7. **One push a day.** Tomorrow's (Saturday 9/12) push carries everything marked VERIFIED below. The push runs the battery at the root (`tools/hooks/pre-push.sh`); the SEO cage needs `SEO_CAGE_BASE=http://127.0.0.1:3001`. Before the deploy set Railway env: INDEXNOW_KEY, CRED_DATAFORSEO_EMAIL / CRED_DATAFORSEO_PASSWORD (web AND crm), APIFY_MONTHLY_USD_CEILING=17, APIFY_DAILY_RUN_CAP_SELLER=80, APIFY_DAILY_RUN_CAP_CENSUS=20, and the Myli switch value (below). The Railway CLI is signed out; Zach runs `railway login` in his terminal.
8. **Money rails:** Apify is on the $19 Starter plan; our own guard caps runs per day (80 seller / 20 census, account-wide count) and $17/month; the local caps were lifted for a test today and RESTORED to 80/20 at 14:25 (backup ~/hl-reversals/2026-09-11-apify-caps-before-test.env). DataForSEO: $51 balance, ~$7.60/month at the nightly cadence. Never print a key; load env with `node --env-file=apps/houseloop-web/.env.local`, never by sourcing.
9. **Lanes message the desk only at BUILT or BLOCKED.** The desk never re-reads echoes. Builders on Opus (measure-first work) or Sonnet (specced work). Judges: this week the desk's own eye (Zach's call); when Fable returns, Fable judges.
10. **ChatGPT is the strategy partner** (Zach: "he is your thinking partner for any strategy"). The desk tool: `node scratchpad/desk/desk-chatgpt.mjs health | ask <prompt.txt> | capture <chat url> <out.md>` on the :9222 Chrome bridge; never `goto` a project URL (crashes the tab); close crashed `/project` tabs via `curl :9222/json/close/<id>` first. Every answer is filed as `SECOND-OPINION-<topic>-<date>.md` with Asked / Verdict / Adopted / Departed and the verbatim transcript.
11. **Zach's live boards:** `/boards/the-program-2026-09-11` (the one page; refresh it by editing `.claude/reports/desk-boards/the-program-2026-09-11.html` and `node tools/boards/promote.mjs --slug the-program-2026-09-11 --source <file> --title "The Program" --description "…" --by lee --replace --commit`), `/boards/zach-desk-2026-09-11` (today's decisions + pictures), `/boards/state-of-surfaces-2026-09-11` (measured status per surface), `/boards/morning-brief` (regenerate with `node tools/boards/desk-boards.mjs` then promote). Reports to Zach are plain English, no code in prose, numbers in tables, and every phone state goes to his phone as a screenshot document (SendUserFile) the moment it passes.

---

## 1 · TODAY'S RULINGS, ONE LINE EACH (#257.500–.536)

| # | What Zach decided |
|---|---|
| .500–.517a | Morning: the entity fix set (/about, /contact, Organization JSON-LD, sameAs = HouseLoop-owned only), LinkedIn page live, Bing Webmaster imported + sitemap submitted, DataForSEO activated, standout page like its siblings, Trulia citations (ZIP + neighborhood links). |
| .518 / .518a | The phone map sheet to Zillow: solid white to the bottom, ~20px collapsed strip, one count line, no prose, pills above the sheet, one search bar + one filter on the list; the "Sort · Save Search" row STAYS (Zillow has it); "Save Search" both places. |
| .519 | Every photo from every source kept with its source; 25 homes graded; the monthly bill measured. |
| .520 / .520a | Monthly Redfin AND the first Redfin lookup go through Apify; Camp David is a manual backup only. |
| .521–.521e | The About map: your ChatGPT reference → rounds → FINAL (.521c): plain blue US, electric outline, KC pulse, cities on it, NO backdrop; SIGNED (.521d); labels ≥12px at 390; legend bottom-left (.521e). Intro: "HouseLoop is live in Kansas City and expanding nationwide." |
| .522 / .522a | AI search: HomeLight, Wikipedia, Facebook, Zillow, Realtor.com, FastExpert, Effective Agents, Homes.com, the agent pages, all on the SEO list. |
| .523 / .525 / .525a / .526 | houseloop.ai is HouseLoop only; the person is "a HouseLoop licensed real estate agent"; the eXp footer is compliant; one phone number for now; the co-brand lives on the Spradling side. |
| .527 | Desktop results page: the whole header stays sticky, the search bar beneath it, Myli and the houses scroll. BUILT + verified. |
| .528–.528d | Phone listing page: no menu; two buttons on an active home; on a sold home ONE button "Claim this home" + "to get your home value and monthly demand report" (one line or nothing); multiple people may claim; the sheet drops by the menu's height so photos get the room. |
| .529 | The screenshot is the spec; composites per state, read by the desk. |
| .530 | "Tour this home" everywhere, never "Request a tour"; keep the calendar and the "as early as &lt;day>" subline. |
| .531 | Sold search: the price filter applies to the LIST price (sold prices are not disclosed); the count matches the cards. BUILT + verified. |
| .532 / .532a | The home value master sheet was NOT built (one of four states); Zach was told it was. |
| .533 | THE PROGRAM ORDER (12 items; §2 below). Spradling = the LOCAL PARTNER: an /about paragraph + FAQ backlinks on important KC geo pages (NOT all 11,600 home pages, per ChatGPT, adopted .535). |
| .534 | The boards home: per major build only the PRD+checklist and the most recent LOCKED board; everything else to a collapsed LEGACY section by build; a REVIEW QUEUE at the top. |
| .535 | ChatGPT's 9/11 SEO+Bing plan ADOPTED IN FULL; one locked SEO revamp plan; one canonical checklist on the SEO board. |
| .536 | Only the desk on Fable this week; every lane Opus/Sonnet. |

---

## 2 · THE WORKSTREAMS — spec, state, what is left, in order

Legend: VERIFIED = the desk checked it on the local build · BUILT = a lane says built, unread · OPEN = not built · RED = failed its check. Lane names are the agents running at 6:10 PM; message them by name; a new lane needs a fresh spawn with the ITEM-KEY.

### 2A · THE PHONE, TO ZACH'S ZILLOW SCREENSHOTS
**Spec:** the four Zillow shots Zach gave at 5:39 AM 9/11 (`/Users/zachspradling/.claude/uploads/7b99acaa-dbbb-420b-94d0-2844c9c9a40a/{59dd2f4e,ce90ff39,859aa811,d20ac8f5}-image.png`: list, map halfway, map collapsed, detail) + rulings .518/.518a/.528–.528d/.530/.533B. The 9/10 mobile wireframe (redraw #4) and the morning "listing glass" receipt are SUBORDINATE to the shots where they differ; the signed Apple glass surface itself (the 5/21 glass: transparent surface, white/60 hairline, the signed blur; `RECEIPT-listing-scroll-law-2026-09-11.md`) must be restored exactly.
- VERIFIED: map collapsed, map halfway, list (card 76e5a3ad, `RECEIPT-map-sheet-zillow-2026-09-11.md`, composites in scratchpad/lane-map-sheet-zillow/).
- VERIFIED: desktop sticky header (da77d3af).
- OPEN, lane running: **lane-detail-two-buttons** (card d2822974, round 2). Acceptance, per state at 390 with the Zillow composite: ACTIVE: no menu; "Tour this home" FILLED blue, "as early as &lt;day>" on ONE line (drop the clock time; drop the subline before wrapping); an "Ask a ?" ICON button, outlined, beside it; the signed glass exactly; the sheet resting lower by the menu's former height (print the before/after sheet-top px). SOLD/off-market: ONE full-width "Claim this home" + the one-line prose or none; opens the existing claim road (ClaimHomeCta.tsx:48/:100, /api/home/claim); a second claimant is never blocked (measure the API; card if it blocks). COMING SOON: same as active. 1280 unchanged. nav-anchor test gains the detail-route exception. The old "Is this your home? … Get Report" card copy is retired.
- OPEN after that: "Tour this home" site-wide (card "TOUR THIS HOME everywhere": ~80 occurrences in 20 files; MEASURE which are labels vs Myli lookup keys/route-map tokens before swapping; a vocabulary rename is a routing change).
- OPEN: the mobile-states document: every phone state (map collapsed, map halfway, list, active detail top + scrolled, coming-soon detail, sold detail, the tour modal, the ask modal, the claim flow's first screen) at 390 via localhost:3001, one HTML page with the composites, promoted `--home-section review`, and sent to Zach's phone with SendUserFile. Build it the moment the detail lane passes the desk's read.
- Carded defects found today: 6904 W 79th St renders "For Sale" while status maps to `other` (get.ts:51); the FAQ test's missing ez reference; the phone list's chip-above-sort order; the standout shelf's 10–14 s by-id refetch.

### 2B · THE HOME VALUE FLOW, END TO END, TO THE LOCKED BOARD
**Spec:** `/boards/home-value-master-2026-09-10` (source `docs/wireframes/home-value-MASTER-2026-09-10.html`; rulings .483/.484/round 13). Four states + the round-12 arrival + the print/email view + the node-line stepper. Every other seller drawing is legacy (.483 lists them).
- OPEN, lane running: **lane-master-sheet-gap-3** (card 388ee54a) finishes the gap audit: per state the composite (board frame | ours) and the numbered differences, the record reconciliation, and ONE OPUS BRIEF PER STATE with seams, acceptance, tests, files, hours. Its partial file: `.claude/reports/grinder/home-value-report/GAP-master-sheet-2026-09-11.md`. Board: `/boards/master-sheet-gap-2026-09-11`.
- THEN, in this order, one Opus lane each, the next starting when the previous is VERIFIED by the desk's composite read: STATE 1 the /sell entry ("What's your home worth?", ONE sentence, the address bar, "See my report", "Here's the whole road before you commit to any of it.", the ENTIRE dashboard dimmed beneath; kill the stats strip 2,204/8/102% and the four-line paragraph) → STATE 2 the instant report after an address (Myli's card, What it's worth with the three sources and the HouseLoop Estimate in blue, the range, the 15-year line, Your home story, Search demand, Homes like yours, Your neighborhood, Your home records, Your home equity, Thinking about selling, each section's "Unlock" road) → STATE 3 sign-in inside Myli's chat, the four moments, REGISTRATION (the existing SessionProvider/sign-in road; the board's moments map onto it; never a real lead; qa-bench personas) → STATE 4 the signed-in dashboard (nothing masked; un-landed sections build inline) → the print/email view → the node-line stepper. Round 13's address micro-flow (Is this a unit? / which unit / Mine isn't listed / It's the whole building) belongs to state 2's address step and is REQUIRED because of the unit defect below.
- RED, lane running: **lane-value-photos-25** round 2 (card 9709a4eb; verdict `VERDICT-value-photos-25-2026-09-11.md`). The ladder sent condo addresses WITHOUT the unit and wrote another unit's zpid, photos, baths and a live $320K listing event onto two homes (one a past client's sold unit). Both rows RESTORED from backup at 15:45. Round 2 in order: the unit in every actor query + address verification before ANY write (red-first: HLID100408's APT 1303 record must be REFUSED; HLID447154 still writes) → idempotent paid steps (the paid call is recorded durably before a retry can re-buy; root cause: a pg-pool timeout after purchase, then a step retry) → the MLS sync (cache.ts:427) and the sold flip (archive.ts:65–76) go through mergeCachedRows so kept sources survive → the two ONE VALUE holes (HLID424558, HLID260451: Zestimate present, hlEstimate NULL) → ship or delete the unread source line → re-grade the 25 against the HOME → then re-run ONLY the two repaired condos through the fixed ladder. The same unit bug is LIVE on prod's /sell for condos since the 9/10 push (P0 card); the push must carry the fix or not carry the ladder.
- THEN (card 8965967a): 25 RANDOM Kansas City houses (not the 25 used), minting the HLID for never-in-MLS homes, including past-client homes, through the registered Inngest ladder; a judge grades every field against the HOME and the cost table from Apify's run records; GREEN required.
- THEN the two Apify roads (cards 08c474a8, d509129c): the monthly Redfin leg on the cheap scraper (memo23, $0.0022/home, 25/25 with an estimate; the stored redfinEstimateUrl exists on 2,046 of 2,146 watch-list homes) and the first Redfin lookup by address for the ~92 without a URL (address verification mandatory: 1 of 5 came back the wrong house). Measured bill: ~$6.89/month steady state; $4.28 + $0.83 one-time; ~$1.91 per 100 new homes.
- Carded page defects from the walk: "$0" twice on a minted seller's page (Market Estimate builds from listPrice 0); an unlabeled stale list price ($259,950) as a past client's hero; a blank grey screen for a home with no photos; two Listing rows carry HLID101864; 921 rows off the ONE VALUE formula by exactly −1.

### 2C · SEO, ALL OF IT
**Spec:** `.claude/reports/seo-revamp/SECOND-OPINION-seo-bing-plan-2026-09-11.md` (ADOPTED IN FULL, .535) → being folded into `THE-SEO-REVAMP-PLAN-LOCKED-2026-09-11.md` by **lane-seo-plan-locked-2** (card 6e4d36fb), which also reconciles floor 55's SEO bands into ONE canonical checklist (tick done with receipts, retire duplicates, add the plan's rows) and re-promotes `/boards/master-schedule-seo-revamp` with the PRD card linking the locked plan.
- VERIFIED today: Bing Webmaster (site + sitemap) · DataForSEO first pull (50 keywords; Zillow 44.8% SOV, houseloop.ai 0.07%; the nightly costs ~$7.60/mo) · standout page + article · ZIP/neighborhood links on home pages · canonical home path folded to one · KC weekly report dedupe · 44 tools probing the local site · the sold price filter · IndexNow function (needs INDEXNOW_KEY on Railway) · HouseLoop-only copy on /about, /contact, /myli (9c0f144d) with the live numbers · the Spradling local-partner paragraph on /about + the restored geo-page FAQ backlinks (8c64c71d; the four geo generators persist their payload, so RE-INVOKE geo-city-page, geo-county-page, geo-neighborhood-page, geo-zip-page through Inngest after the push).
- OPEN, lane running: **lane-seo-dashboard-6c** (card fc54b512): the dashboard from the signed wireframe (/boards/seo-dashboard-wireframe-2026-09-10, .439) with panels: rank (DataForSEO ledger) · GBP (+ the fix list computed site-vs-profile) · GA4 · Search Console · IndexNow · inventory · BING (current REST/OAuth API; the SOAP one retired 8/31; the IndexNow key is NOT the Bing API key; empty state naming CRED_BING_WEBMASTER_API_KEY; Bing AI Performance if exposed) · a Clarity placeholder; the TOP STRIP = five KPIs: non-brand SOV (Google+Bing), canonical index coverage, AI visibility (cited pages + citation share), freshness (change→index latency), authority (quality referring domains); a tile without a source says "not measured yet". Any new table → DDL at the desk.
- OPEN, in the locked plan's 30-day order (each already carded on seo-revamp; the locked plan carries the keys): 1 the facet-canonical fix (the filtered /listings URL 301s to the built facet page when equivalent; "price-reduced homes kansas city" ranks #10 via the wrong URL) · 2 the indexable-URL audit · 3 IndexNow create/update/delete pipeline monitored (submitted→crawled→indexed) · 4 the geo→facet→home link graph · 5 address pages as one machine-readable factual block · 6 Microsoft Clarity installed + AI citation measurement · 7 Bing REST API into the dashboard (Zach creates the key) · 8 Bing Places for the Spradling Group (Zach's login) · 9 the controlled HouseLoop↔Spradling links (done: /about + geo FAQs, branded anchor) · 10 clean/expand the local citations: Expertise.com FIRST (it says Keller Williams at 6850 College Blvd), then Realtor.com, Homes.com, HomeLight, FastExpert, Effective Agents, RateMyAgent, Facebook; website = spradlingrealtygroup.com; the co-branded one-liner; the Zillow team name STAYS "The Spradling Group powered by HouseLoop" (Zach) · 11 the DataForSEO set from 50 keywords to intent clusters · 12 the fixed AI prompt benchmark as a nightly Inngest job (20 city + 20 address + 10 market + 10 brand prompts across ChatGPT/Copilot/Perplexity; extends the existing 20-address baseline). Then the 31–90 list (scale winning facets; kill useless facet combinations; city topical clusters; earn KC links with our datasets; the Spradling entity across directories; optimize on Bing grounding queries; per-URL-type SOV; template index coverage; Orlando only after).
- Also carded: the GBP profile update (categories Real estate agency + Real estate agent, the 607-char description in the Spradling-copy opinion §D, services, UTM on the website link, no Products); the GBP address audit (7900 College Blvd must be a real staffed office before any GBP edit); SRG's /house-loop must become a real origin article (today it is byte-identical to the SRG home page); the bot-crawl dashboard; the intermittent prod 500 on /kansas/johnson-county/overland-park; the standout home-page shelf row is DISABLED on prod (isActive=false, backup ~/hl-reversals/2026-09-11-standout-shelf-row-before-disable.json) until the push, then RE-ENABLE it.
- WAITS ON ZACH: the Bing API key (portal click), Bing Places, Expertise.com and the directory logins, the GBP edits, the office-address answer, the DataForSEO auto-refill reminder, the MLS AI-use question, HouseLoop's published address (none, per the record).

### 2D · THE MYLI FREEZE AND THE CUTOVER
**Spec:** `.claude/reports/myli-os/THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md` (v3 §9 supersedes), `.claude/rules/MYLI_AUTHORITY_MAP.md`, `tools/myli/authority-manifest.json`, `THE-MYLI-CONTRACTS-V1-2026-09-10.md`, the board `/boards/myli-ssot-fold`, floor 42 (45 F-items: 25 DONE, 20 OPEN at 16:55). THE MORATORIUM (#257.470) goes verbatim in every Myli brief.
- LIVE on prod since the 9/11 04:48 push: F0 (the map, the cage, the vector floors, the manifest, the contracts) · F1.1 the one write road · F1.2 the CRM projector · F1.3 the backfill · F1.4 identity · F1.6 · F2.1b kill-now-free · F2.2 flow config = capability ids · F3.1 the one context assembler · F3.2 the page snapshot · F3.3 surfaces migrated in order · F3.x the tool boundary · F4.1 one inventory snapshot · F4.2 the status FSM · F5.1 one fact catalog · F6.1 claims are references (+ .1c the dashboard road) · F7.1 projections stay stupid · F8.1 the escape hatches destroyed.
- OPEN, lane running: **lane-cutover-switch** (card 9f38a522): ONE switch `MYLI_RESPONDER` ∈ &#123;ssot_v1_responder, frozen_legacy_response_adapter&#125; at the outermost webchat ingress, replacing the MYLI_NEXT/MYLI_AGENT_VOICE reads there; the legacy adapter runs BEHIND the canonical ledger/TurnContext and through the same renderer; default = what prod serves today; a cage arm: exactly one reader; both values ledger one row per turn. If the legacy road cannot sit behind the boundary without a rewrite, the lane STOPS and the desk decides (plan §9.1: fail closed).
- OPEN, the weekend's realistic set (desk's proposal; Zach's Monday bar is his call): F1.4 four surfaces read the ledger · F2.1 kill the second pill engine (dynamic-pills.ts, lib/myli-legacy/, the MYLI_NEXT + MYLI_AGENT_VOICE roads die with it) · F2.3 pills = registry label + eligibility only · F2.4 likeness.assets one URL each · F4.3 the county walk's four · F4.2b retire tool-status.ts · F1.2b the ON CONFLICT branch test · F1.4b the lead-bearing actor · F9.0 the Friday deploy (= tomorrow's push, with the switch) · F9.1 Sunday: the four journeys pass×2 by judges (the desk on Opus this week: run `node tools/flow-judge/run.mjs <journey>` yourself; the deterministic selftest is in the push battery) · F9.1b the three walls · F9.2 Zach's Monday walk.
- NEXT WEEK: F5.2 the assertion model (evidence_event_id NOT NULL) · F5.3 derived values · F5.4 one extractor · F4.4 market knowledge · F3.5 the dashboard ledger backfill · F6.1b the K adversary journey.
- Carded: the manifest names vs the code's flag (the switch lane reconciles); the buyer dashboard shows "$321,901" in Myli's opener beside "No value for this home yet" (two authorities, one screen); both standing qa-bench personas hold a home claim so the buyer lens is unreachable (add a buyer-only persona); the localhost:3001/myli desk photo never paints (hostname quirk, mirror of the hydration one).

### 2E · THE ABOUT PAGE
- VERIFIED: the map (signed .521d; plain blue US, electric outline, KC pulse, no backdrop; all backdrop code and the dev route deleted; labels 12.31px at 390; legend bottom-left).
- VERIFIED: the copy (HouseLoop-only; the one co-brand sentence; the local-partner paragraph; live numbers).
- OPEN (Zach's word): whether the legend goes back top-left; the exact partner-paragraph wording (DRAFT-FOR-ZACH).

### 2F · THE DESK ITSELF
- OPEN, lane running: **lane-boards-home-legacy** (card 527da29f): per major build only PRD+checklist + the latest LOCKED board; everything else to a collapsed LEGACY section by build (nothing deleted; `legacy_of`); a REVIEW QUEUE section first (`--home-section review`); a `resection-legacy.mjs --dry-run | --apply` script; the manifest test gains the arms.
- OPEN, lane running: **lane-brain-map-v3c** (card 9d50ee05): the 9/07 v2 card is the ONE spec; redrawn with measured data, every ChatGPT quote traced or marked UNVERIFIED, today's architecture and working memory, the F-slices marked; promoted as `brain-map-horizontal-v3-2026-09-11` (v2 untouched).
- VERIFIED: `/boards/state-of-surfaces-2026-09-11` (measured 13:00), `/boards/the-program-2026-09-11`, `/boards/zach-desk-2026-09-11`.
- OPEN: the record-tool defect (retire leaves done:false; ~56 retired items count as open); the surfaces board's fold proposals (525 open → ~380).

---

## 3 · SATURDAY 9/12 — THE PUSH DAY, IN ORDER
1. Morning: read every BUILT that landed overnight against its composites; tick or send back. Regenerate the morning brief and the program board.
2. GATE: the value ladder is GREEN on the re-grade (2B), or the push does NOT carry listing-mint-enrich.ts's ladder changes (revert by pathspec to the 9/10 04:48 state and say so). The unit bug is live either way; the fix ships when it is green.
3. Zach: `railway login`; set the Railway env (§0 item 7) on houseloop-web AND houseloop-crm; MYLI_RESPONDER to the value the switch lane names as "what prod serves today".
4. Run the push launcher at the root (quiet tree → three 200s on / → pre-flight cages → the battery). The battery must be GREEN; a red is fixed at the named source, never by raising a floor.
5. After the deploy: `curl -X PUT https://houseloop.ai/api/inngest` and the CRM's; invoke the four geo generators; re-enable the standout shelf row (hl_home_shelf slug popular-homes isActive=true); re-prove the home value on prod for HLID251739, HLID225202 and one never-in-MLS home; run the 20-address ChatGPT baseline; walk prod on a phone (map, list, detail active/sold, /sell, /about, /myli, /contact) and send Zach the screenshot document.
6. Sunday: F9.1 (the four journeys); the desk decides the switch flip only on green×2.

---

## 4 · WAITS ON ZACH (one line each, all on /boards/the-program-2026-09-11)
Bing API key · Bing Places · Expertise.com + directory logins · GBP edits + the office-address fact · the Monday Myli bar · the "Tour this home" desktop rail (same word both widths?) · the legend position · the partner paragraph wording · the four parked picks (last sold price shown?, the New pill 24h/7d, the standout feature catalog, retire popular-homes-seed) · the MLS AI-use question · the CLAUDE.md docs host line (.app → .io).

---

## 5 · THE TRAPS THAT COST TIME TODAY (so the next desk does not pay again)
localhost vs 127.0.0.1 hydration · the Prisma client pool under 30+ walkers · a half-landed export removal breaking every page · the intake hook refusing a spawn with a DONE item (a "zombie claim"; add a fresh item) · `task.mjs add --title` creating nothing · a lane's stash pop taking another lane's entry · a paraphrased spec (four map rounds; the detail bar built without the Zillow shot; "Request a tour" wired where Zach wanted "Tour this home") · prod walks of surfaces changed today measure yesterday's code · the Fable weekly limit killing three thinking lanes mid-run (write receipts PENDING-first so a kill loses nothing).

---

## 6 · THE OPUS DESK'S STANDING ORDERS (Saturday 9/12 through Sunday 9/13, until Fable resets Monday)

Zach: "have an Opus running the desk… very explicit instructions with pass/fail parameters, and create a working, stamped, logged, verified-by-Opus record for Fable to review." This section is that. Follow it literally; do not improvise past it; when a step's PASS condition cannot be met, write FAIL with the measured reason and move to the next step, never "close enough".

### 6.1 The cycle (repeat every 60 to 90 minutes, and on every lane's BUILT message)
1. `cat .claude/state/lee.lock | head -5` and `ls -t .claude/reports/**/RECEIPT-*2026-09-1[23]*.md 2>/dev/null | head`: what landed since the last cycle.
2. For every BUILT: open the receipt; run the ONE proof command it names yourself (the test, the cage, the curl); open its composites and READ them beside the reference; write a LOG entry (§7) with PASS or FAIL and the measured reason; on PASS `node tools/master-plan/task.mjs tick <key> --receipt <path> --note "opus desk: <what you re-ran>"`; on FAIL send the lane ONE message naming the failing condition and the exact number, and log it.
3. Spawn the next lane in §2's order for every workstream that has capacity (one lane per workstream at a time, up to six lanes total; Opus for measure-first work, Sonnet for specced work; every brief carries ITEM-KEY, MEASURE FIRST with the grep, the acceptance from this document, the rails from §0 item 6, the composite requirement, and "message the desk only at BUILT or BLOCKED").
4. Refresh the program board (§0 item 11) with the new states. Regenerate the morning brief at the start of each day.
5. Every 3 hours, one plain-English note to Zach (three to eight lines): verified since last time, what is running, what waits on him. Nothing else. Send any finished phone-state document to his phone with SendUserFile.

### 6.2 Pass/fail parameters per workstream (the desk does not tick without these)
| Workstream | PASS only if | FAIL if |
|---|---|---|
| 2A detail page | At 390 via localhost:3001: active listing shows no menu; a filled blue "Tour this home" (measure `background-color` = the brand blue token) with a one-line subline or none; an outlined "Ask a ?" icon button beside it; the sheet's top at rest ≥ 60px lower than in `scratchpad/desk/mobile-now/5-detail-scrolled.png` (print both); the glass tokens identical to the signed values (print both sets); sold listing shows exactly one button "Claim this home", prose on one line or absent; tapping each opens the named modal (screenshot); composites read by the desk; tsc 0; nav-anchor + listing-detail tests green; 1280 unchanged (before/after diff). | Any state's composite differs from the Zillow shot in an element Zach named; any label wraps; the menu renders on any /home/ route at 390. |
| 2A mobile-states doc | One HTML page with every phone state (≥9 shots) via localhost:3001, promoted `--home-section review`, and SendUserFile succeeded (the tool result lists the file). | A state missing, a shot taken via 127.0.0.1 (0 fibers), or the page not promoted. |
| 2B each master-sheet state | The composite (board frame \| ours) shows every element the frame has, in the frame's order, with the frame's copy; numbers come from resolvers (print resolver value = rendered value); no element the frame lacks (the stats strip, the old paragraph); tsc 0; the page's tests green; the home-render census green. | Any missing frame element; any typed number; any leftover pre-lock copy ("Is this your home? … Get Report", "What's your home worth right now?"). |
| 2B value ladder (RED → GREEN) | The judge's 8 revised-spec items each PASS with a named reason: the unit is in every actor query (print one query per actor for a condo); a wrong-unit record is REFUSED (the plant with HLID100408's APT 1303 record); a paid step cannot re-buy on retry (the mutation RED); the MLS sync and the sold flip keep non-MLS galleries (a plant survives a simulated sync); hlEstimate = round(zestimate×1.005) on every home with a zestimate (print the count of exceptions = 0); the two repaired condos re-run through the fixed ladder and either fetch the right unit or refuse; the 25-home table re-graded against the HOME with 0 wrong-unit cells. | Any of the 8 unproven; any new Apify run before items 1–2 are proven; any write to a home whose returned address differs from its identityKey. |
| 2B 25 random houses | 25 homes not used before, chosen by a printed SQL with ORDER BY random() and a seed, ≥5 never-in-MLS (HLID minted through the registered mint), ≥5 past-client; every home COMPLETED in the registered Inngest function (run ids listed); the grader's table has a source per cell and 0 wrong-home cells; cost from Apify usageTotalUsd ≤ $1.00 for the batch. | Any hand SQL write; any home graded against the record it came from instead of the home; a run refused by our cap (the caps are 80/20; raise ONLY with Zach's word, and restore). |
| 2C SEO dashboard | Every wireframe panel exists; each panel's rendered value equals one printed SQL/API read; the five KPI tiles render with a named authority or "not measured yet"; the Bing panel shows the empty state with the key's vault name and the exact portal clicks for Zach; tsc 0; eslint 0; the projection-composer cage green; screenshots at 1280 and 390 read by the desk. | A placeholder number; a panel reading a second authority; a new table without desk-signed DDL. |
| 2C locked plan + checklist | `THE-SEO-REVAMP-PLAN-LOCKED-2026-09-11.md` exists with sections a–h; every floor-55 SEO item has a fate in its RECONCILIATION table; the SEO board's PRD card links the plan; the open count per band printed before/after. | A plan row with no record key; a done item without a receipt path; a hand edit of floors.json. |
| 2C each 30-day row | The row's own KPI line in the plan is satisfied and printed (for the facet fix: `curl -sI` of the filtered URL shows a 301 to the facet page; for IndexNow: a submitted→crawled sample; for Clarity: the script in the one loader and a page view in the Clarity project). | Anything asserted from memory. |
| 2D cutover switch | `MYLI_RESPONDER` is read in exactly ONE file (print the grep, count = 1); both values produce one hl_chat_events row per turn (print the ids); the cage arm goes RED on a planted second reader; the myli cages green; tsc 0; the default equals what prod serves today (say which). | A second reader; a turn under either value with no ledger row; any old writer restored. |
| 2D each F-slice | Its YAML receipt per plan §6 with `reachable_after: 0` and the negative control PASS; the cage for that fold green; the deterministic flow-judge selftest green. | A receipt without the YAML; a fold whose old road is still reachable (the census says so). |
| 2D Sunday F9.1 | `node tools/flow-judge/run.mjs <journey>` GREEN twice for each of the four journeys, receipts kept. On Opus this week the judge model is the desk reading the transcript against the rubric; write PASS/FAIL per turn in the LOG. | Any RED, JUDGE_ERROR, MOVE_NOT_REACHED, or TRUTH_UNPROBEABLE. The switch does NOT flip. |
| 2F boards home | `/boards` renders; each major-build block shows exactly one PRD+checklist card first then only LOCKED boards; a collapsed LEGACY section grouped by build with counts; a REVIEW QUEUE section first; the manifest test's new arms green and red-proven; nothing deleted (board count unchanged: 149+). | Any unlocked board left in a major-build block; any board missing from the manifest. |
| 2F brain map v3 | Every number on the v3 card carries a date; every ChatGPT quote carries a trace path or UNVERIFIED; the F-slice table matches floor 42; v2 untouched (its sha unchanged); the board readable at 1280 and 390 (no overlaps). | A number or quote with no source; v2 modified. |
| The push | Battery GREEN at the root; Railway env set (§0.7) with Zach's confirmation; the value ladder GREEN or reverted; after deploy §3 step 5 every line done and logged with the prod URL and the measurement. | Any battery red "fixed" by raising a floor; any post-deploy step skipped. |

### 6.3 What the Opus desk must NOT do
Push more than once a day · run any lane on Fable · change a cap, a floor, or a rule file to get a green · restart :3001 under lanes · report "done" to Zach without the composite read and the LOG entry · start the master-sheet states out of order · flip MYLI_RESPONDER on prod · touch the Spradling site's DNS/GBP/Zillow (Zach's logins) · delete any board or record item (retire, never delete) · walk prod for a surface changed since the last push and present it as the build.

---

## 7 · THE LOG — stamped, verified by Opus, for Fable to review Monday
File: `.claude/reports/desk-boards/OPUS-DESK-LOG-2026-09-12.md` (start it Saturday morning; one file per day). One entry per verification, appended, never edited:

```
### <YYYY-MM-DD HH:MM EDT> · <ITEM-KEY> · <workstream §> · <lane name>
CLAIM:    <what the lane said, one line>
RE-RAN:   <the exact command(s) the desk ran itself>
OUTPUT:   <the decisive line(s) of output, verbatim; numbers, not adjectives>
READ:     <composite/screenshot paths the desk opened, and what it saw in one sentence each>
VERDICT:  PASS | FAIL — <the §6.2 condition met or missed, with the number>
ACTION:   ticked <key> | sent back: "<the one-line message>" | carded: "<title>"
```
Plus, at the end of each day, a ten-line DAY SUMMARY: verified, sent back, open, what waits on Zach, and anything the Opus desk was unsure about (flag it "FOR FABLE"). Fable's Monday review reads the LOG top to bottom, re-runs a sample of the RE-RAN lines, and either confirms the ticks or reopens them.

### 7.1 The first LOG entries are due Saturday morning for whatever landed overnight
At 6:10 PM Friday these lanes are running and will report BUILT into the desk: lane-detail-two-buttons (2A) · lane-value-photos-25 (2B, round 2) · lane-master-sheet-gap-3 (2B audit) · lane-seo-dashboard-6c (2C) · lane-seo-plan-locked-2 (2C) · lane-cutover-switch (2D) · lane-boards-home-legacy (2F) · lane-brain-map-v3c (2F) · lane-spradling-partner (2C, narrowing the FAQ backlink to the county + city pages). Each has a receipt path in its brief; each must be verified by §6.2 before its tick.

### 7.2 · 6:40 PM update to §2 (so the Saturday desk starts from the truth)
VERIFIED since 6:10 PM: the phone detail page (d2822974; two taste calls open for Zach: "Ask a question" text vs a "?" icon; the signed transparent glass vs a solid panel) · the boards home fold (527da29f; 30 keep / 64 legacy / review queue first) · the SEO dashboard's four missing pieces (fc54b512; Zach's Bing API key click is the only blocker for the Bing panel). RUNNING: lane-mobile-states-doc (13 states → /boards/mobile-states-2026-09-11 + a contact sheet for Zach's phone) · lane-cutover-switch on #257.538 (measure prod's responder, then delete the legacy road if prod serves ssot_v1; if not, STOP) · lane-value-photos-25 (items 3–8 now; the 3-fresh-home live proof at 00:05Z after the cap reset; then BUILT → re-grade) · lane-master-sheet-gap-3 · lane-seo-plan-locked-2 · lane-brain-map-v3c · lane-spradling-partner (narrowing to county + city pages). NEW CARDS: the claim guard (one claim per home vs #257.528c) · the refused-run write · the ssot-census cage RED at baseline (42 > 41, geo producers) · the CRM settings shell at 390 · pending homes' bar.

### 7.3 · 7:05 PM: Zach's answers (#257.539, #257.528g) applied to §4
CLOSED: the Bing API key (stored: vault row 204 as CRED_BING_WEBMASTER_API_KEY, the CRM's .env.local; put it on the houseloop-crm Railway service with the push) · the GBP address (7900 College Blvd, Overland Park, KS 66210 is the real office; the SRG site's "Kansas City, MO" structured data is a defect, carded) · the standout feature catalog (approved; carded to lock) · popular-homes-seed (retired; carded to stop) · the last-sold-price pick (NO: sold prices are never shown; the last list price is) · the MLS AI-use question (signed off) · the detail-page glass (stays the signed transparent glass; the map view keeps Zillow's white card). STILL OPEN FOR ZACH: the legend position · a pending home's bar · the 15 unfiled legacy boards · Mission Control's wall lock · the "Tour this home" desktop rail (#257.530 says everywhere; the global-swap card does it). ChatGPT was asked (chat 6aa444f2) for the /about partner paragraph, the county/city FAQ pair, and the access checklist (Bing Places, Expertise.com, the directories, the GBP reviews API, Clarity, the Bing REST calls); file it as SECOND-OPINION-partner-copy-2026-09-11.md and hand the copy to a Sonnet lane.

---

## 7.4 · 9:55 PM FRIDAY — TONIGHT'S RULINGS APPLIED (read this BEFORE §1–§4; where a line here conflicts with an earlier section, THIS section wins)
Rulings #257.537–.546 are in the record (`.claude/reports/myli-os/ZACH-REVIEW-RULINGS-2026-08-27.md`, from line ~16196). One line each, and what it changes here:
- **#257.539/.540/.540c–h — THE NAMING, LANDED.** The entity/NAME field everywhere = "The Spradling Group". "powered by the HouseLoop real estate technology platform" = the TAGLINE (a prose line in bios/descriptions/signatures/the sign's second line; never a fused name, never a logo lockup). Kansas City is the ONLY co-brand; every new market is HouseLoop + "a HouseLoop team agent". Separate records (schema, GBP, domains), unified KC brand — the PLACE model; the SRG site leans IN (name + tagline under the mark, an SRG `/house-loop` page, the two-way schema bridge, card 036dd492). GBP name stays "The Spradling Group" (recommend dropping "- EXP Realty"; awaiting Zach). ZILLOW team name STAYS "The Spradling Group powered by HouseLoop" (#257.545b). Ana-named BUSINESS listings rename after Zach's per-site yes; PERSON profiles keep the person's name (#257.540e). Sold prices never public (#257.540). HouseLoop's own address NEVER appears publicly (#257.546). → §4's "office-address fact" and "partner paragraph wording" are CLOSED.
- **#257.541/.541a — THE TOUR BUTTON = THE CALENDAR; MYLI OUT.** Built + ticked (c02de5ea, receipt RECEIPT-tour-calendar). The 9/08 "tour opens Myli" was actually THE SELLER CONSULTATION FLOW: address confirm (+unit) → upgrades pills → condition 1–10 → loan balance → net sheet, Skip / "I'd rather not say" on each → the EXISTING scheduler books it (card 0f919938, builds after the master-sheet states). "Tour this home" is the label everywhere (20360038, ticked; 87 hits).
- **#257.542 — THE LOCKED MASTER SHEET IS THE MARKUP.** A master-sheet lane LIFTS the board's HTML (phone frame + desktop frame) into JSX with the same classes/tokens/copy/order and binds data into it; nothing re-drawn. BUILT = per-section composites (sheet | ours, 390) the desk reads first. State 2 is BUILT this way (f5706966, ticked; /sell/report/&lt;HLID>); slice 2 = the read-only demand resolver (8404e8b7, lane-ms-state2 running); state 1 (56f98894, lane-ms-state1) and state 4 (a2f30663 + a4639e1a, lane-ms-state4) running under the same rule. → §6.2's "2B each master-sheet state" row now ALSO requires: the JSX carries the sheet's literal class strings; a test that every class and every var() the lift references resolves.
- **#257.543 — PHONE MAP/LIST ROUND 2** (lane-map-sheet-r2, 7755266e): flat handle, no chevron; Chat with Myli + Save Search = Myli's round pill; the "Overland Park, Kansas · 465 Homes for Sale" strip gone; Sort/Save Search lean + black; the sheet floats ABOVE the glass menu, nothing cut off. PASS = composites vs Zach's Zillow shots for collapsed / halfway / list-top + a DOM proof of no overlap with the menu.
- **#257.544 — PHONE DETAIL ROUND 4** (lane-detail-bar-r4, fb9dad79): bar = PRICE · filled "Tour this home / as early as" · "?"; no menu; sheet collapsed; PENDING = one big "Ask a question" + one small "currently pending — accepted offer" line; SOLD = "Claim this home". AND the reporting defect: Zach's page carried stale pictures — lane-one-page-pictures (570371ea) re-shoots §0 from the current build with FIXED / STILL OPEN labels. **Standing order: every picture Zach sees carries its capture time and a FIXED/OPEN tag.**
- **#257.546 — THE FOUR SEO ANSWERS.** FAQ link on 18 pages = 8 metro counties + top-10 cities (lane-spradling-18, d8b7f310, widening the Johnson-County guard) · HouseLoop address never public · 7900 College Blvd staffed, walk-ins yes · legend stays · Mission Control wall lock stays in legacy · brain map v3 already read · Bing AI Performance read WEEKLY from a logged-in Camp David session (070fe9fa) · never-in-MLS homes with no photo → Google Street View (42b70335) · the one-time Redfin URL lookup + one-time Zillow ID pass + Apify monthly refresh: approved · §5b copy approved.
- **THE PROFILE CENSUS is BUILT** (536e7ef0; `/boards/spradling-profiles-2026-09-11`): 21 rows, 0 right · 13 fix · 1 missing · 7 not found; 7 Ana-named listings; nothing edited on any site until Zach's per-site yes.
- **THE LOCKED SEO PLAN is BUILT** (6e4d36fb; `THE-SEO-REVAMP-PLAN-LOCKED-2026-09-11.md`, first link on the SEO board's PRD card; 15 new items; its §H questions 1–4 are now ANSWERED by #257.546 — mark them in the plan on Saturday morning).
- **THE CUTOVER is BUILT** (9f38a522): the legacy chat road DELETED (lib/myli-legacy/ 11 files + 2 legacy components), one responder, prod proven ssot_v1, MYLI_RESPONDER is a RESOLVED single value in the manifest; authority cage green with ARM R; floors re-seeded (myli-authority.floor.json committed). → §2D's "lane-cutover-switch OPEN" and §3 step 3's "MYLI_RESPONDER to the value…" are CLOSED: there is no switch to set. The myli-ssot-census cage is RED at baseline (count_producers 42 > 41, the geo sites — carded) and persist-conversation.ts is dead code (carded): both are Saturday fixes, not floor raises.
- **WAITS ON ZACH, the whole list now:** (1) the HouseLoop phone number (~$1/mo Twilio KC number; a purchase — his explicit yes) · (2) "- EXP Realty" in the GBP name: keep or drop · (3) per-site yes on the profile fixes · (4) the Google credential grant (6106d7f8) for IndexNow's sheet · (5) `railway login` on push day. Nothing else waits on him.

- **⚠ PUSH-DAY GATE THE DESK MEASURED AT 10:35 PM:** the SEO page-structure cage is RED on `standout` — DISCOVERY_FAILED, no `data-seo-fixture="popular-homes"` on `/` — because the desk set `hl_home_shelf` slug `popular-homes` to `isActive=false` on the (shared) prod DB this afternoon (backup `~/hl-reversals/2026-09-11-standout-shelf-row-before-disable.json`). localhost reads the same DB, so the battery WILL go red on that cage until the row is re-enabled. Saturday, IMMEDIATELY before the battery: `UPDATE hl_home_shelf SET "isActive"=true WHERE slug='popular-homes'` (log it), re-run `SEO_CAGE_BASE=http://127.0.0.1:3001 node tools/seo/page-structure-cage.mjs --cage` → GREEN, then launch. Never touch the floor, never edit the census URL to make it pass.
- **THE 18-PAGE FAQ LINK is BUILT** (d8b7f310, ticked; receipt RECEIPT-spradling-18): 8 counties + the top-10 DISTINCT cities (`topDistinctCitiesMetro(10)` — the raw top-15 held Kansas City four times across four counties); the exact 18 slugs are in the receipt; live geo pages show it only after the post-deploy geo regeneration.

- **C2 v1.1 LANDED (#257.551, 5:25 PM EDT, lane-projection-dup, receipt RECEIPT-projection-dup-2026-09-11.md):** every lead-bound webchat turn was filed TWICE into hl_messages (the one projector + turn-writer's array-position road with NULL source_event_id — 50 unkeyed rows/day). The chat-events wire gained optional move fields; the receiver now owns the conversation card, requestMemoryProcess and applyConversationMove; the array-position write is CUT for lead-bound turns (census after the cut: 0). Projector test 32/32 red-first per effect; authority cage + flow-judge selftest GREEN (desk re-ran all three). ⚠ **PUSH-DAY CONSEQUENCE:** the sender half runs on the local tree but the projecting DRAIN runs on PROD (Inngest dispatches to the one registered app), so until Saturday's deploy a lead-bound LOCAL turn gets no run chain / no memory pass. PROD is unaffected today. After the deploy: re-run the census query in the receipt's §5 (array-position 0, projector rising) and confirm `adopted` is moving; that closes #257.551. → §8's "must land before the matrix" list: F1.2b's exactly-once is now the projector test + the census; the NOT NULL on source_event_id next week is safe once the census reads 0 on prod for a day.

- **THE MASTER SHEET AT 5:45 PM EDT, all six lanes BUILT + ticked by the desk (composites read):** state 1 `/sell` (56f98894) · state 2 `/sell/report/<HLID>` + slices 2–4 (f5706966, 8404e8b7, ffa2252d, a2a7d00c, c32985cb, 39fd1136) · state 3 sign-in inside Myli's chat (4a54fb30; M4 proven on a real SMS with the qa-bench past-client persona) · state 4 = the owner lens (a2f30663 closed via #257.547; a4639e1a) · the arrival (d21f31cf, #257.550 enrichment.progress) · the sold entrance (1b53b21d) · Google's return path (0566d413). OPEN under it, carded: the comps/"this month" resolver (b5b4e159) · the third lens, signed-in-non-owner (5aa86e82, lane-ms-state2 slice 5 running) · the arrival's ✓-line composites after the 8 PM cap reset (39fd1136 done; re-shoot only) · a phone-less qa-bench persona (f8aef94f) · Zach taps "Continue with Google" once Monday (cbecdbdd) · the seller consultation flow (0f919938, after everything above). → §6.2's "2B each master-sheet state" row is SATISFIED for six states; the Saturday desk re-reads nothing here unless a lane reports on it.

## 8 · THE MONDAY BAR — MYLI WITH PERFECT MEMORY ON WEBCHAT AND PERFECT STORAGE IN THE CRM (Zach 9/11 9:45 PM: "we need to be fully ready on Monday to start testing Myli to have perfect memory on the website chat and perfect storage in the CRM with facts")
This is the weekend's finish line for 2D. "Ready to test" means the Opus desk has RUN this matrix on prod itself, Sunday night, and every row reads PASS in the LOG with its printed evidence — so Monday Zach walks it and Fable reviews the LOG, nobody discovers it. The matrix is derived from the authority map (`.claude/rules/MYLI_AUTHORITY_MAP.md`) and the contracts; every row names its authority, so a FAIL says which fold is unfinished.

| # | Test (qa-bench persona, phone-width, PROD after Saturday's push) | PASS only if (print the evidence) | Authority / fold |
|---|---|---|---|
| M1 | Open the site as a stranger, chat three turns, close the browser, reopen the same browser next day: Myli continues the SAME conversation and refers to what was said. | One conversation id across both visits (print `hl_chat_events` rows by conversation: sequence_no monotone, no gap, no duplicate); her first line on return references a turn-1 fact verbatim. | `hl_chat_events` via `appendChatEvent()` · resolveActor (F1.1, F1.4) |
| M2 | Every turn is ledgered ONCE. Ten turns → exactly ten user events + ten Myli events. | `SELECT count(*) … GROUP BY role` = 10/10; `hl_turns` envelope count = 10; zero rows written by any file other than chat-ledger.ts (the one-writer cage). | one writer (F1.1) |
| M3 | Every webchat message appears in the CRM ONCE, on the right person. | `hl_messages` rows with `source_event_id` = the ledger's event ids, 1:1 (the partial UNIQUE holds — print a duplicate insert being refused); the lead-detail feed shows all ten in order with the named actor; the activity log rows come from `logActivity` only. | the outbox projector (F1.2, F1.2b) |
| M4 | Sign in mid-conversation (the one flow inside Myli's chat, state 3): the stranger's turns attach to the person. | Before sign-in: actor with no person; after: `linkActorToPerson()` row; the CRM lead shows the PRE-sign-in turns too; no second conversation created. | resolveActor / linkActorToPerson (F1.4b) |
| M5 | Say a fact in turn 2 ("we have two kids and a dog, budget around 450"); ask something that needs it in turn 8. She USES it and never re-asks. | Three `hl_lead_facts` rows via `assertFact()` each with `evidence_event_id` = the turn-2 event; turn 8's answer cites the budget; zero "what's your budget?" after turn 2 (the flow-judge NO_REPEAT arm); the CRM lead detail shows the facts with their evidence link. | assertFact / hl_lead_facts (F5.1, F5.2) |
| M6 | Correct a fact in turn 5 ("actually budget is 500"). | The fact row updates (or supersedes) with the turn-5 evidence; the old value is not asserted again; the CRM shows the current value + history. | F5.2 |
| M7 | She knows the page. Open /kansas/johnson-county/overland-park, tap Chat with Myli. | Her opener names Overland Park and the count the PAGE shows (print PageContextV1's snapshot id + the page's rendered count = equal); never a metro count. | PageContextV1 / InventorySnapshot (F3.2, F4.1) |
| M8 | Status while she works. | Every status line is one of the FSM's enum states rendered by the client; zero model-authored status prose in the ledger (print the grep over ten turns). | the status FSM (F4.2) |
| M9 | Pills. | Every pill's label + eligibility comes from `lib/myli/contract/registry.ts` (print the count of pills rendered vs registry ids; zero from any other file); the undeclared-pill cage green. | the capability registry (F2.1–F2.3) |
| M10 | Nothing else answers. | `node tools/vocab/myli-authority-cage.mjs --cage` GREEN with ARM R; `node tools/vocab/myli-ssot-census.mjs --cage` GREEN (count_producers ≤ 41 — the geo-site producers folded, never the floor raised); `lib/myli-legacy/` absent; persist-conversation.ts deleted; the deterministic flow-judge selftest green. | the map's "Dies" column |
| M11 | The four journeys. | `node tools/flow-judge/run.mjs <journey>` GREEN ×2 for each of the four (receipts in tools/flow-judge/receipts/), the desk reading the transcript against the rubric where the Fable judge is unavailable. | F9.1 |
| M12 | Zach's walk, Monday. | The LOG carries M1–M11 with timestamps, the phone screenshots for M1/M4/M5/M7, and one honest paragraph: what still cannot be promised. | §7 |

**What must land BEFORE the matrix can pass (Saturday → Sunday, in this order):** F9.0 the push (Saturday) · F1.4 the four surfaces read the ledger · F1.2b the ON CONFLICT branch test · F1.4b the lead-bearing actor · F4.2b retire tool-status.ts · **F5.2 the assertion model (evidence_event_id NOT NULL) — pulled forward from "next week" because M5/M6 are Zach's bar** · F2.3 pills = registry label + eligibility · F2.4 likeness.assets · the ssot-census red (42→41) · persist-conversation.ts deleted · F9.1 the journeys ×2 (Sunday) · the matrix run on prod (Sunday night). F4.3, F4.4, F5.3, F5.4, F3.5, F6.1b stay next week. Each lands as a lane with the fold's YAML receipt (§6.2 "2D each F-slice") — the MORATORIUM line verbatim in every brief.

## 9 · THE TWO-DAY RUN ORDER FOR THE OPUS DESK (Saturday 9/12 → Sunday 9/13)
**Saturday morning:** (1) read every overnight BUILT against its composites (lane-map-sheet-r2, lane-detail-bar-r4, lane-ms-state1, lane-ms-state4, lane-ms-state2 slice 2, lane-spradling-18, lane-one-page-pictures, lane-value-photos-25's 00:05Z proof + re-grade) — tick or send back, LOG each · (2) the value-ladder gate (§3 step 2) · (3) mark §H 1–4 answered in the locked SEO plan · (4) regenerate the morning brief + the program board · (5) the push (§3, minus the retired MYLI_RESPONDER step; Zach's `railway login` + the env) · (6) post-deploy §3 step 5 in full, incl. the phone walk document to Zach's phone with capture times and FIXED/OPEN tags.
**Saturday afternoon/evening:** spawn, in parallel, one lane per workstream: 2D F1.4 · 2D F5.2 (Opus, census-first) · 2D F1.2b+F1.4b (Sonnet) · 2B the arrival (lane 3) then state 3 (lane 5) as state 1/4 land · 2B the 25 random houses (8965967a) once the ladder is GREEN on prod · 2C the next 30-day rows from the locked plan (the facet canonical, IndexNow monitoring, the Bing REST panel's remaining groups, the SRG schema bridge 036dd492, Street View 42b70335, the weekly Bing AI read 070fe9fa) · 2A whatever the morning composites sent back.
**Sunday:** F2.3/F2.4, the ssot-census fold, persist-conversation delete · F9.1 the four journeys ×2 · **the §8 matrix on prod, every row, by the desk's own hands** · the seller consultation flow (0f919938) only if 2B's states 1–4 are all ticked · 9 PM: the LOG closed with the Monday paragraph; the program board and Zach's one page refreshed; one plain-English note to Zach: what he will walk Monday, what is honestly not ready.
**The measure of the weekend:** every §8 row PASS on prod, every 2A/2B composite read, the push landed once a day with the battery green, and a LOG Fable can audit line by line Monday. A weekend that ticked more boxes but cannot show M1–M11 on prod has failed the brief.
