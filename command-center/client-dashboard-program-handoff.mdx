---
title: "Client Dashboard Program — Handoff (account swap 2026-08-13)"
description: "The cohesive client-experience build + the saved-search/lists schema signing. Everything the next session needs — sources in the repo, not artifacts."
---

# Client Dashboard Program — Handoff

## 🚀 THE 8/13 SHIP PLAN — Myli MVP for the public + Conner & Jasmeet TODAY (Lee runs point)

**The spec = the wireframe** `docs/wireframes/client-experience.html` (artifact 67fd7a53, v13). Myli's 3 jobs:
create the first search · brief/update the search · flag the standout house. Profile = the 5-step touring
funnel (+20% each, must-haves first, Tour scheduled = the goal; buyer agency deferred). Dashboard = the
first-class LOGGED-IN view (routing rule, marketing home untouched). Own-a-home/valuations DEFERRED.

**Lee's rulings (veto anytime):** D1 Book-a-tour = pick from your homes/matches + add-by-address fallback ·
D2 max 3 homes per tour, over = "we'll check with Zach" · D3 pin title "Your profile · with Myli" ·
D4 provenance on every done row (uploaded/set by Zach vs set by you) · D5 must-haves settable by agent at
search-creation OR by client via Myli, provenance-stamped, client edit notifies agent · D6 couple = linked
via connected-person on the search; both see each other's saves ("Saved by Conner"); Myli greets each by name.

**Workstreams:** WS1 screens (Lee, v13 published — awaiting Zach walk) · WS2 website "Search with Myli"
swap + NLP-page voice (agent web_myli) · WS3 dashboard React build S1/S2 (agent dash_build, behind a flag
next to the live 8/12 surface) · WS4 discovery+plumbing: phone login, profile-facts home (twin-hunt → Lee),
pre-approval doc slot, couple link, tours read, Myli-line data (agent data_plumb) · WS5 ⛔ THE BATCH DEPLOY
(585+ commits — NOTHING ships without Zach's go) · WS6 C&J dry-run (Zach: their phone numbers + the
Fountain letter + his search-create with connected person, then a live walk).

**Zach's human steps:** sign the v14 screens · give the deploy GO · flip the two Searches toggles ·
add Jasmeet's facts · create their search via + Search.

### 8/13 PM — EXECUTED STATE (Lee)

**Shipped + Lee-verified:** "Search with Myli" swap live in code (all 6 AI-button sites + the NLP page in
her voice, one canonical `MyliAvatar`, asset ownership: web_myli writes `public/myli/`, everyone reads) ·
the Myli dashboard React v1 (flag-gated `?myli=1` beside the live surface, S1+S2 walked) · the internal
endpoint `GET /api/internal/client-dashboard-context` (facts + `agentFirstName` + consent-gated
`household.members[{userId,firstName}]`; Conner: 13 facts, "Zach") · **mig 0749 home tours APPLIED**
(`hl_home_tour_stop` = one row per home on a tour, per-home `feedback` lives there — Zach's directive;
`hl_appointments.is_home_tour` checkbox, ORTHOGONAL to type) · secret parity web↔CRM **verified MATCHING
in prod** (Railway GraphQL, the old mismatch memory corrected) · Jasmeet's lead repaired via the API
(first_name "Jasmeet", assigned agent Zach — facts still Zach's step) · phone login empirically proven
safe for hidden leads (the resolver filters deleted-only).

**Zach rulings today:** `hidden` = hidden from the LEADS TABLE ONLY — comms/login/dashboard NEVER
suppressed (audited: the contactable predicate deliberately includes hidden; inbox + notify have zero
status filters; ⚠ one overload: the e-sign broker-fallback skips hidden leads' loops — QA-guard
semantics, flagged) · profile funnel order: timeline → must-haves → search created → pre-approved →
tour scheduled (the goal) · the pre-approval letter is HELD until a logged-in-only private slot exists
(the current R2 bucket is PUBLIC — never upload client financials there).

**Queued signing-desk items (post-launch):** the HOUSEHOLD ROW + connected band (one leads-table row per
household = the primary, partner chip on the band, activity folds in; retires the hidden hack —
`household_primary_id` is the dormant hook) · `hl_lead_document` + private R2 + signed URLs (the letter
slot) · the saved-search **write-validator** (a hand-made searchUrl with wrong param spellings silently
drops filters — an "under $250k" fixture counts 433 homes to $899k; the live CRM writer is correct, the
parser is the gap) · the appointment-composer UI for home tours (checkbox → home slots → buyer invite
template) · web TourRequest → CRM appointment wiring · `home_tour_*` + `home_list_*` catalog kinds ·
wiring `include_in_saved_searches` into the digest send path (the toggle is currently inert).

**C&J launch runbook (the demo order):** flip the two Searches toggles → Zach adds Jasmeet's facts →
create the search via + Search (texts the login links) → each logs in once at houseloop.ai/login
(accounts mint automatically, phone-keyed) → the household lights up. Jasmeet STAYS hidden (ruled
fine). Letter: hand the PDF to Lee AFTER the private slot ships.

Written for an engineer **account swap** mid-build. Artifacts (claude.ai) are NOT viewable on
restart — so every design below points at its **source HTML file in the repo**, which stays readable.
Read those files, not the artifact URLs.

## THE GOAL (Zach, verbatim intent)

ONE **cohesive** client experience that **looks like the houseloop.ai website** — "it can't feel like,
when you go to the dashboard, a completely different software." The **locked seller home-value view is the
anchor**; its navy value modules are **HIGHLIGHTS on the light website page**, not a separate dark app.
**Buyer and Seller are two lenses of the same website-native experience.** Keep every function.

## THE FOUR CLIENT SURFACES → their SOURCE FILES (read these)

| Surface | Source file (readable) | Hosted / artifact | State |
|---|---|---|---|
| **Site menu / nav** | `apps/houseloop-web/components/site/Header.tsx` + `apps/houseloop-web/app/globals.css` | the real site | the design GROUND — pull its tokens/fonts/cards |
| **BUYER = "Welcome back, Timothy"** | `apps/houseloop-crm/boards/your-houseloop-dashboard.html` | `crm.houseloop.ai/boards/your-houseloop-dashboard` | ✅ the correct buyer: search rails, saved searches, favorites, toured, shortlist, Myli ask-bar |
| **SELLER = home-value detail (LOCKED anchor)** | `apps/houseloop-crm/boards/home-value-report-ui.html` (the ".cb Combine") | artifact `b5e8830c`; design study `87543e5b` | ✅ locked 8/11: value band (Low/Est/High), history graph + scrubber, story, Myli dock |
| **Search workspace (SIGNED 8/13)** | `docs/wireframes/saved-search-dashboard-LOCKED-2026-08-13.html` (v10) | artifact `ba95c7d7` | ✅ Zach said "locked" — being ported to React (task #15) |
| **Client-experience (menu + buyer/seller toggle)** | `docs/wireframes/client-experience.html` | artifact `67fd7a53` (republished post-swap; old `16a791ee` is the pre-swap account) | ✅ COHESIVE PASS DONE (Lee walked both lenses 8/13, commit `c7bc2b2b33`) — awaiting Zach's iteration/lock |

## WHERE THE COHESIVE ARTIFACT IS (task #48, workspace_polish)

`docs/wireframes/client-experience.html` → artifact `16a791ee`. It currently has the **correct** two
dashboards (buyer = Welcome-back-Timothy, seller = locked home-value) bolted under the real site menu with
a Buyer⇄Seller toggle. **BUT it is NOT yet the cohesive design** — the buyer is still dark-themed (its
board's own style). **NEXT SESSION:** finish the cohesive pass — ground the whole thing in the real
houseloop.ai website design (`apps/houseloop-web` globals.css/fonts/cards), make the buyer light + website-
native, keep the seller's navy value modules as **highlights** only, unify nav + the Myli dock across both
lenses. Do the user dashboard as the next session's focus (Zach's call).

## THE SEARCH-WORKSPACE REACT BUILD (task #15, workspace_build)

Verbatim React port of the SIGNED v10 wireframe into `components/leads/detail/SearchDashboardModal.tsx`
(mounted via `CenterLane.tsx:64/616/664`). Split: **Slice A = search mode** (real data, building now),
**Slice B = list mode** (waits on the schema below). Rulings already given (D1–D5): D1 extend `IpadModal`
with additive `chrome:'bare'`; D2 the website filter panels via the generated-copy sync
`tools/shared-ui/sync-client-dashboard.mjs` (NEVER cross-app import — disjoint Docker); D3 add
`SectionBand` `size="eyebrow"+flush`; D4 generalize `EditableContactRow` with additive `onSave?` + `tone?`
(guardrails: additive-only, `no-raw-inline-editor` green app-wide + red on a plant, tsc green all
importers); D5 build Slice A now, Lists rail HONEST-EMPTY, schema is a separate lane. Send routes to the
TWO existing intents (`SavedSearchSendModal` for custom-picked; the `sys_saved_search_digest` workflow for
all/since/today) — no new send door, no touching `deliver()`.

## ⭐ THE SAVED-SEARCH / LISTS SCHEMA — Bedrock signing (Zach ENGAGED + RULED)

Proposal: `.claude/reports/schema-lists-proposal.md` (schema_lists, 475 lines). **Zach's mandate:
"the schema has to be correct… no vibe coding… no patching… doing it the correct way and migrating what
needs to be done correctly."** Hold that bar.

**ZACH'S RULINGS (this session):**
- The **CRM saved search is the SINGLE source of truth** — one saved-search database. (Answers proposal Q2:
  the CRM object SUPERSEDES the web `SavedSearch`; the web keeps at most a read since matching runs where
  `Listing` lives.)
- **DELETE the old editorial web table** (not rename — supersedes proposal Q1's rename recommendation).

**VERIFIED FACTS (queried the CORRECT centerbeam web DB `HL_WEB_DATABASE_URL`, 2026-08-13):**
- Editorial `hl_saved_search` (Prisma `HouseloopSavedSearch`, `schema.prisma:987`, `@@map hl_saved_search`):
  **EXISTS on centerbeam · 0 rows · 0 FK dependents · 0 app-code references** (grep excluded node_modules).
  It's a dead admin "featured/curated search" config (`slug·label·criteria·notes`, e.g. "$750k+ Single
  Family in Overland Park") for the site's "Inspiration for your next move" nav — **NOT** user saved
  searches. **Safe to drop** (Zach-authorized; RULE#0 satisfied — named table, web DB, 0 rows/deps/uses).
  The drop MUST also remove the dead Prisma model (`schema.prisma:987–1000`) in the same change.
- Real user saved searches = `"SavedSearch"` (**10 rows**, centerbeam) → these migrate to the CRM.

**THE CORRECT PLAN (schema_lists ready to execute post-signature, in its lane):**
1. Write the 3 `tools/schema/target-verdicts.json` entries (§4.1 drafted) — the framework gate
   (`migration-lint.mjs`) rejects any CREATE TABLE with no verdict, so the migration won't compile without
   them. This IS the signing act — do it deliberately.
2. CRM `hl_saved_search` (person-keyed): **use the SHIPPED actor canon** `by_kind·by_user_id·by_lead_id·
   by_system_id·by_name` + `actor_type` + the two CHECKs — **NEVER the 8/7 `actor_*` spelling** (that mints
   a 7th actor generation — the Bedrock disease). Add `legacy_web_id` (5 orphaned `hl_email_send_log.
   saved_search_id` cuids) + `search_url` (load-bearing querystring). **NO opens/clicks/views columns** —
   those already live in `hl_email_send_log` (which has `saved_search_id`); the left-rail stats are a QUERY.
3. CRM `hl_home_list` + `hl_home_list_item`: `lead_id`→`hl_leads` CASCADE, `hlid text` (no FK, cross-DB,
   the hl_home_claim pattern), `sort_order`. **Watermark = `sent_at` ON THE ITEM** ("only what I added since
   last sent" = `sent_at IS NULL`), NOT a `sentHomes[]` array. Catalog has zero list vocabulary — the
   `home_list_*` kinds are a SEPARATE vocabulary event after the tables land.
4. Migration drafted as **0748** (0747 is max applied), additive, GRAIN lines, twin-check clean.
5. Migrate the 10 web `"SavedSearch"` → CRM (registered **Inngest** backfill with receipts — schema_lists
   owns it; INNGEST_OR_IT_DOESN'T_EXIST).
6. DROP the editorial `hl_saved_search` on centerbeam + remove the dead Prisma model.
7. Repoint the workspace + saved-search reads to the CRM table.

**EXECUTED 2026-08-13 (Lee):** steps 1–4 are DONE — the three verdicts are in `tools/schema/target-verdicts.json`
(the signing act), **migration `0748_saved_search_and_home_lists.sql` is APPLIED to the CRM DB** (runner green,
lead-purge guard 51/51, lint 4/4 green, census 204→207, `model.lock.json` regenerated; commit `a361cce42b`).
Still open, each in its lane: **(5)** the Inngest backfill of the 10 web `"SavedSearch"` rows —
**BUILT + REGISTERED 8/13 (ss_backfill, Lee-verified: 19/19 key-normalizer tests, fn in serve(), registry
slug = fn id, 0 DB writes)**; fires through the Inngest dashboard post-deploy, dry-run first, Zach sign-off
(the FUB tape upsert is a separate later adapter); **(6)** ✅ **EXECUTED 8/13** — Zach typed the drop
same-session; web `hl_saved_search` DROPPED (re-verified 0 rows/0 deps first), Prisma model removed,
prisma-check green; **(7)** repoint workspace reads to the CRM table (Slice B) — still open.

## ⚠ SESSION-END UPDATE — workspace_build Milestone 1 + a CRM-wide bug + 2 open rulings

**Milestone 1 SHIPPED + verified** (commit `1782d48e47`, variants `fbcc855661`):
`apps/houseloop-crm/components/leads/detail/search-workspace/SearchWorkspace.tsx` — shell + navy header +
both rails + search header on REAL data (nav 84 · rails 318 · feed 820). The three approved extensions
landed additively and proven: `IpadModal chrome='bare'`, `SectionBand size='eyebrow' flush`,
`EditableContactRow onSave + tone='on-navy'`. `no-raw-inline-editor` 0 app-wide + red-on-plant; tsc clean;
rename/cadence via the real `PATCH /api/leads/saved-searches/edit`. Lists render honestly empty. 4 first-
render defects fixed (blank tour tiles `start_at`→`starts_at`; activity showed raw kind → `eventLabel()`;
activity dumped whole email bodies → decoded/clamped; 4 dead controls wired or honestly disabled).

**⭐ CRM-WIDE BUG FOUND + FIXED — the lead-detail Visits & Saved tiles had ALWAYS read ZERO.**
`app/api/leads/[id]/activity/route.ts` counted `byKind['property_view']` / `byKind['save_home']` — **neither
is a real kind.** Real: `property_viewed` (21,275 rows) / `property_saved` (3,432). Lucy Hartman showed 0
where the DB has 1,225 views / 155 saves. Fixed by counting catalog FAMILIES via DISTINCT canonical keys
(`PROPERTY_VIEWED_KINDS`/`PROPERTY_SAVED_KINDS`) — matched the DB exactly. ⚠ **FOLLOW-UP ITEM:** the rule
`hl/no-hardcoded-event-kind` is at WARN in that route — 11 MORE hardcoded typed tokens remain there
(`share_home`, `tour_request`, `contact_request`, `home_value_report`, `seller_get_started`), same defect
class, all likely mis-counting. Clear them, then take the rule to ERROR in that file. Own task.
**✅ DONE 8/13 (route_kinds, Lee-verified):** zero literals remain — families come from the ONE resolver
(`lib/activity/catalog`: `HOMES_ENGAGEMENT_KINDS` etc.); rule at ERROR scoped to the route (red-plant proven,
which caught an unescaped-`[id]`-glob trap); endpoint vs DB exact match on Lucy Hartman (visits 1,225 = 1,225).

**RULINGS for the next session's build (Lee, this session):**
- **D2 amendment APPROVED:** sync only the 3 PORTABLE panel files byte-identical (`filter-atoms.tsx`,
  `FiltersPanel.tsx`, `PriceRangePanel.tsx` + leaf deps) via `sync-client-dashboard.mjs`; the CRM owns the
  thin bar (~150 lines: the 4 triggers + input, `URLSearchParams` from the saved-search criteria, `onCommit`
  updates the search — NO route push). Do NOT copy `FilterBar.tsx` — it does `router.push('/listings')` and
  would break in the CRM modal, and editing a synced file defeats `--check`. The wireframe already drops the
  AI pill + Save button per Zach's signed pixels (locked 1507-1509).
- **Search-name geometry:** add an additive `variant='title'` (20px/700) to `EditableContactRow` (same
  additive pattern as `tone`) — UI_ITERATION_HTML_FIRST, the wireframe wins; don't deviate or hand-roll.
- **Slice A remaining:** the search bar (D2 as above) → results toolbar → cards → selection footer → the
  criteria prose line beside the name (locked 1464). Slice B (list mode) still waits on the schema.

## OTHER LIVE LANES

- **catalog_promote (#47 DONE):** the Activity Card Catalog is promoted + renders + on the Command Center
  (old dup `card-catalog` slug retired via `promote.mjs --retire`). ⚠ Follow-up: the board is 188 kinds,
  the live catalog is 189 — `ai_searched` (Website · home_card) needs a hand-authored CONTENT row in
  `content.mjs` then rebuild + re-promote (catalog/card_port lane).
  **✅ DONE 8/13 (catalog_fix, Lee-verified):** content row authored, board rebuilt 189/189, re-promoted
  under the existing slug (commits `150a4e6b0b` + `44b23c5f33`); prod shows it after the batch deploy
  (boards ride the Railway image).
- **⛔ THE BATCH DEPLOY — the #1 pending thing:** the branch is **~499 commits ahead of origin/houseloop-app**;
  NOTHING from the overnight enterprise-QA + open-projects work is live in prod — including the **AI
  containment gate** (built, tested, NOT enforcing on prod — the old `ai_master=false` mute is holding),
  the commission road, the pour-door fix, and the owner dashboard. A push ships ALL of it. Needs Zach's
  go + a 30-sec owner-lens pixel sign-off. The card catalog + everything goes live in that same push.

## THE ARTIFACT URLS (for reference; not readable on restart — read the source files above)

- Search workspace (SIGNED): `claude.ai/code/artifact/ba95c7d7-6786-45ae-b767-763545f5d745`
- Owner/seller home-value: `claude.ai/code/artifact/b5e8830c-06df-42a9-94e1-3519bf02ca08`
- Client-experience (menu + toggle, in progress): `claude.ai/code/artifact/16a791ee-434d-4233-96d8-5a8a23b50bd0`
- Homeowner design study (4 directions + Combine): `claude.ai/code/artifact/87543e5b-a716-45f5-a720-e7e68526d3e8`

## FIRST MOVES NEXT SESSION

1. Read this page + `.claude/state/lee-handoff.md` + `.claude/reports/schema-lists-proposal.md`.
2. Continue the **user dashboard cohesive pass** (Zach's next focus) — website-grounded, dark modules as
   highlights, buyer+seller two lenses. Source: `docs/wireframes/client-experience.html` + the four surface
   files above + `apps/houseloop-web` for the real site design.
3. The schema is Zach-ruled (single source of truth + delete the editorial table) — execute it correctly
   per the plan above when picking it up; schema_lists is ready in its lane.
4. Surface the **batch deploy** decision to Zach.
