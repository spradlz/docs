---
title: "THE MYLI OS — the master PRD"
description: "One build: the Myli MVP, the SSOT Fold and the Architecture Cutover reconciled into one program, with the fold-to-step map."
---

# THE MYLI OS - the one master document

**Ordered by Zach, 2026-09-12 morning:** *"there should be ONE thing called THE MYLI OS, and in there should be
the MYLI MVP, the MYLI SSOT FOLD and the MYLI ARCHITECTURE CUTOVER, all into one build called THE MYLI OS. And
you need to analyse all those documents and make ONE MASTER DOCUMENT, one master PRD document, then make the
separate projects within them. The Myli cutover and the Myli SSOT should be very similar - analyse those
documents and reconcile everything."*

**Written by lane `myli-os-prd`, 2026-09-12. Read-only on both databases. No product code.**
This document does not replace the three source documents. They stay on disk as evidence and are cited by name
throughout. This is the JOIN between them.

---

## 1 · WHAT THE MYLI OS IS

The Myli OS is everything Myli is and does, as one build. A stranger lands on HouseLoop, talks to her, and she
answers from real data with her own words. They register and become a real person in the CRM. Every fact she
heard becomes a row on that person. Weeks later she texts them and remembers all of it. The Myli OS is that
whole loop plus the machinery underneath it: one place that knows what happened in a conversation, one place
that knows who this person is, one place that counts the homes, one place that decides what she is allowed to
offer. Today there are several places for each of those, which is why she contradicts herself and forgets
things. The work is not building more; it is getting down to one of each and deleting the rest.

**The name is not new.** `the-myli-book` has carried a banner since 2026-09-07 (Zach ruling #257.305) saying
*"the law of Myli's program is THE MYLI OS and THE MYLI BRAIN MAP."* The record card `myli-os` already exists
and is titled *"The Myli OS - one brain, the one document, one checklist"*
(`tools/master-schedule/initiatives.json`). What drifted was the BUILD REGISTRY, not the name.

---

## 2 · THE THREE PROJECTS INSIDE IT

### 2.0 First, the registry defect that is the whole reason this reconciliation was needed

`tools/pm/major-builds.json` declares three separate major builds. Two of them are **the same row twice.**

Measured from `.claude/state/pm/projection.json` (generated 2026-09-12T11:41:25Z):

| build slug | record card | total | verified | open | percent |
|---|---|---|---|---|---|
| `myli-mvp` | `myli-os` | 290 | 113 | 138 | 39.0% |
| `myli-architecture-cutover` | `myli-os` | 290 | 113 | 138 | 39.0% |
| `myli-ssot-fold` | `myli-ssot-fold` | 81 | 37 | 44 | 45.7% |

`myli-mvp` and `myli-architecture-cutover` point at the identical record card and therefore produce identical
counts. **Zach's instinct that two of these are one thing is correct at the registry level before a single
document is read.** The PM projection has been reporting one pile of work as two builds.

One more measured fact from the same file: the cutover's declared spec board resolves to
`{"slug": "myli-architecture-cutover", "board": null}` - **the board was declared and never minted.** Its
actual home is a Mintlify page, `docs-site/command-center/myli-architecture-cutover.mdx`, not a CRM board.

### 2.1 THE MYLI MVP - the product loop and, in practice, the program backlog

**What it is.** `apps/houseloop-crm/boards/the-myli-mvp.html` §0: *"ONE AUDITABLE LOOP"* - stranger taps or
types → Myli answers with real tools and real data → the steps she names become working pills → the person
registers as a real CRM lead → every fact files against the activity catalog and renders on the iPad view →
the playbook fires and the stage moves → her next conversation on any channel reads all of it. Seven links
(L1–L7), each with an acceptance and a receipt.

**What it delivers.** The closed loop, proven twice on the bench, before tours, documents, appointments or
texting a showing partner open.

**Measured state.** Record card `myli-os`: **290 items, 115 done, 39 superseded, 136 open.** The board's own
9/09 status column claims L1 LIVE on prod, L3 PROVEN for the bench persona, L2 mechanism shipped but
UNMEASURED, L4 PARTLY, L5 unmeasured end to end, L7 STALE.

⚠ **Zach's framing needs correcting here.** The MVP is not "the product" as a clean slice. Its card holds 290
items spanning voice rules (floor 45), playbook conversion (floor 47), the test cockpit (floor 36), partner
texting (floor 37), the surface map (floor 49) and the CRM dashboard (floor 50). **`myli-os` is the whole Myli
program's backlog, not the MVP's checklist.** And the MVP board's own §2a is an architecture settlement
(the five layers, ruling #257.383) - so the MVP is not architecture-free either.

**Depends on:** nothing above it. Everything else exists to make its seven links true.

### 2.2 THE MYLI SSOT FOLD - the architecture, executed on webchat only

**What it is.** `.claude/reports/myli-os/THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md` (180 lines, v2 + the v3
hardening). One authority per semantic question, installed as constitutional law
(`.claude/rules/MYLI_AUTHORITY_MAP.md`) plus a machine-readable manifest
(`tools/myli/authority-manifest.json`) plus per-fold cages. Ten slices, F0 through F9.

**What it delivers.** Its own §8 test for done: *"For every semantic question Myli can answer on WEBCHAT,
there is exactly one component permitted to answer it, and the old component is physically incapable of doing
so."* Scope is webchat and only webchat, stated out loud in §7.

**Measured state.** Record card `myli-ssot-fold`: **81 items, 37 done, 0 superseded, 44 open.**
The spine landed (F0 all five sub-slices done, F1.1/F1.2/F1.3 done, F3.1/F3.2/F3.3/F3.4 done, F4.1/F4.2 done,
F5.1 done, F6.1 done, F7.1 done, F8.1 done). **The gate did not run: F9.0, F9.1, F9.1b and F9.2 are all
open.** See §4 contradiction C1 - the serving cutover happened anyway.

⚠ **Zach's framing needs correcting here too.** Calling the fold "the plumbing" undersells it. The fold is the
one that wrote the LAW the cutover cites as governing, and it carries per-fold mechanical cages the cutover
only describes in prose. **The fold is the architecture, already executed at one-runtime scope.**

**Depends on:** nothing. It is the floor everything else now stands on - literally: the cutover's Phase 0
Steps 1 and 2 are *"resolve the `actor-binding` RED"* and *"resolve the `myli-ssot-census` `count_producers`
RED"*, both of them fold-built cages.

### 2.3 THE MYLI ARCHITECTURE CUTOVER - the same architecture, widened to four runtimes, plus a retirement discipline

**What it is.** `docs/myli-audit/synthesis/THE-CUTOVER-MASTER.md` and its three synthesis documents:
`current-state.md` (750 lines), `target-architecture-prd.md` (1,632 lines, 101 rules, 52 refusals),
`migration-cutover.md` (45 steps in six phases). Behind them `docs/myli-audit/audit-findings.md` (F1–F79),
fifteen registries, and eight `SECOND-OPINION-*-2026-09-12.md` files.

**What it delivers.** ChatGPT's sign-off sentence, which is the executive metric:
*"We are not finished when the new road exists. We are finished when there is only ONE road."*
It starts at **65 of 90 semantic questions with more than one answerer** and the week is judged by whether
that number falls.

**Measured state.** No record card of its own. Its build row shares `myli-os` (§2.0). Its 45 steps are not in
the record as items. **Its findings ARE in the record - on a fourth card.** 47 audit-night-shaped items,
including every one of the P0s, are filed on card `qa-bench`, which belongs to the `mission-control` major
build, not to any Myli build:

| card key | finding | record card |
|---|---|---|
| `24be990a` | P0 a county cannot stay a county | `qa-bench` |
| `5b2992a1` | P0 a signed token is read around | `qa-bench` |
| `0550e5b2` | P0 a second fact writer | `qa-bench` |
| `ebec1c3d` | P0 there is no single assistant (four runtimes) | `qa-bench` |
| `7c689b24` | P0 the typed context is three empty arrays | `qa-bench` |
| `4ec7e091` | P0 the subject primitive does not exist off-screen | `qa-bench` |
| `fb48f239` | P0 one home, two HouseLoop values | `home-value-report` |
| `e5b7811b` | 198,810 of 200,935 value-history rows are synthesized | `home-value-report` |

The `cutover-order` lane signed its own document as *"ITEM-KEY f9d60768, card `myli-os`."* So the cutover's
work is scattered across three cards while its build row claims one.

**Depends on:** the fold, whether or not it says so. Every contract it builds on is a fold contract (§3).

---

## 3 · ⭐ THE RECONCILIATION - the fold's F0–F9 against the cutover's 45 steps

**The verdict in one line: the cutover ABSORBED the fold almost entirely, and where it did not absorb it, it
stood on it.** The two documents are not rivals at different resolutions and they are not duplicates. **The
fold is the cutover's first phase, already executed at webchat scope.** The cutover is the same architecture
with the scope widened from one runtime to four, plus one thing the fold genuinely lacked: a retirement
discipline with a number attached.

**The hardest evidence for that reading is not my inference. It is the cutover's own PRD.**
`target-architecture-prd.md` §3.9 is titled *"Reconciliation with THE-MYLI-CONTRACTS-V1-2026-09-10.md"* and
rules on all ten frozen fold contracts. **Eight of ten SURVIVE. None is refused.** That section is the join
this lane was asked to build, already half-written by the cutover itself:

| fold contract | cutover disposition (PRD §3.9, verbatim) |
|---|---|
| C1 the ledger (`hl_chat_events`, `appendChatEvent()`) | **SURVIVES UNCHANGED.** "one of the estate's four control rows" |
| C2 the outbox and projector | **SURVIVES.** `source_event_id` becomes the universal lineage key |
| C3 identity (`ActorRef`) | **EXTENDED, desk ask** |
| C4 capabilities registry | **SURVIVES AND BECOMES LOAD-BEARING** |
| C5 `TurnContextV1` | **BECOMES THE INTERACTION FRAME, desk ask** |
| C6 `PageContextV1` | **SURVIVES**, folded into the frame |
| C7 `InventorySnapshotV1` | **SURVIVES UNCHANGED** and is the sole answer to "how many homes" |
| C8 memory | **SURVIVES.** "The healthiest authority in the estate" |
| C9 status | **SURVIVES AND WIDENS** |
| C10 claim refs | **SURVIVES AND GAINS A SIBLING** |

### 3.1 The table Zach asked for, one row per fold

| fold | what it was | cutover step(s) that carry it | verdict |
|---|---|---|---|
| **F0** freeze, authority map, moratorium cage, manifest, frozen contracts, vector floors | law + cages + the contract barrier | Steps 9, 10, 11, 12 (retirement contract · authority ledger · registry-growth refusal · mutator manifest); PRD §3.9 rules the contracts | **PARTLY ABSORBED - and DONE.** All six sub-slices (F0.1, F0.2, F0.2b, F0.3, F0.5, F0.6) verified in the record. The cutover does not redo F0; it opens Phase 0 by clearing two fold cages. **Still distinct:** F0.3's import-GRAPH cage never landed - the shipped cage is a text detector and its own header says so. Step 11 is its nearest heir. |
| **F1** the webchat spine: one write road, outbox + CRM projector, the backfill, identity, the switch | webchat only | Steps 15 (`actor_resolution`), 27 (owner dashboard into the ledger), 28 (`turn_id` everywhere), 39 (web-to-CRM lineage) | **MOSTLY DONE, THEN WIDENED.** F1.1, F1.2, F1.3, F1.4-identity, F1.5, F1.6 all verified. Step 39 measures the projector the fold built at **589 of 23,316 CRM messages (2.5%)** - exactly the webchat-only scope the fold declared in §7. **Still open inside the fold:** F1.4 "four surfaces read the ledger", F1.2b (the ON CONFLICT branch has no test). |
| **F2** capabilities: the registry owns every promise | kill 2 pill engines | **Step 19, the offer object and the one producer** | **ABSORBED AND MADE MUCH BIGGER.** The fold aimed at `dynamic-pills.ts` + `lib/myli-legacy/`. Step 19 measures **13 producers across 5 id vocabularies with the registry consulted by 1 of 13** and kills 12. F2.2 done; **F2.1, F2.3, F2.4 open.** |
| **F3** the one context assembler + the page snapshot | `buildTurnContext` → `TurnContextV1` | **Step 14, the InteractionFrame** - PRD §3.2.1: it IS `TurnContextV1` v2, widened additively | **FULLY ABSORBED AND SUPERSEDED - and the fold already built the thing being widened.** F3.1–F3.4 verified. **Still open:** F3.5 (the dashboard ledger backfill), F3.3's residue. Step 14's own done-when cites `app/page.tsx` still passing `capabilityIds: []` - the cutover's P0 `7c689b24`. |
| **F4.1** one inventory snapshot | one resolver, one fingerprint | **none - C7 SURVIVES UNCHANGED** | **ALREADY DONE AND ADOPTED AS FINAL.** The only fold contract the cutover adopts with no change and no step. Its v1.1 delta becomes rule R15. |
| **F4.2** the status FSM | 8-state enum, no free-text status | **Step 20, the status FSM and liveness** (C9 widens to `interaction_state.liveness`) | **ABSORBED AND WIDENED.** F4.2 verified. Step 20 adds what the fold did not reach: deleting `SearchThinking.tsx:28-37`'s six-string 1,500 ms interval and retiring `lib/search/tool-status.ts` (which is fold item `a90990b0`, still open). |
| **F4.3** product polish (scroll anchor, card slide, coming-soon, question pills) | deferred by the fold to "next week" | **none** | **STILL DISTINCT.** Not in the 45 steps at all. It is product work and it belongs on the MVP, not the cutover. |
| **F4.4** market knowledge (#257.489) | one resolver over what we hold | **Step 36, one metric registry** | **ABSORBED.** Step 36 measures the disease the fold named: 132 of 140 ZIPs disagree across two tables under three geo-key grammars. |
| **F5.1** one fact catalog / slot vocabulary | generated `FactSlotId` | **Step 37, one fact-slot vocabulary** | **PARTLY DONE, RE-CARRIED.** F5.1 verified for the generated id. Step 37 carries the data half the fold deferred: 19 case-variant slot names, 34 leads holding two disagreeing active facts. |
| **F5.2** the assertion model, one evidence pointer | `evidence_event_id NOT NULL` | the P0 `0550e5b2` (a second fact writer at `extract-context.ts:1325`, 15 live sites) | **NOT DONE, AND THE CUTOVER RAISED IT TO P0.** F5.2 open; the sized fold-map job `652b93b9` open. |
| **F5.3** derived values are a species | `hl_derived_value` + inputs | **Step 24, `hl_derived_value` and `hl_derived_value_inputs`** | **NOT DONE, FULLY RE-CARRIED.** See §4 settled contradiction S2 - the table exists nowhere. |
| **F5.4** one extractor | fold two extraction roads | the same P0 `0550e5b2`; Step 12's mutator manifest | **NOT DONE.** Open in the record. |
| **F6** claims are references | 6 ref classes, renderer commits | **Steps 17 and 18** (command envelope, commitment receipt); C10 gains `CommandRef` + `CommitmentRef` | **ABSORBED AND EXTENDED.** F6.1 and F6.1c verified. **Open:** F6.1b (the K adversary journey). The cutover's extension is a real widening: the fold covered FACT claims; the cutover adds ACTION claims and FUTURE OBLIGATION claims, which is what makes "she promised a tour" auditable. |
| **F7** projections stay stupid | two CRM surfaces only | **no named step.** Nearest: Step 9 (the retirement contract) and Phase F (strangle CRM internals) | **STILL DISTINCT.** F7.1 verified for Monday's two surfaces. The general case is Phase F, which never cites F7. This is the clearest place the two plans do not meet. |
| **F8** death by permission and import | imports banned + DB roles refuse | **Step 9 (retirement contract + auto-ratchet), Step 40 (dead-asset quarantine)**, and ChatGPT's whole framing: *"a retirement program, not an implementation roadmap"* | **ABSORBED AND PROMOTED TO THE EXECUTIVE METRIC.** F8.1 verified for the import half. ⚠ **The permission half is impossible as designed** - see §4 settled contradiction S3. |
| **F9** the cutover itself | Fri expand · Sat migrate · Sun cut over | **the entire 45-step order**, at four-runtime scale over weeks instead of one runtime over a weekend | **SUPERSEDED IN SCALE, UNRUN IN FACT.** F9.0, F9.1, F9.1b, F9.2 all open. See §4 contradiction C1. |

### 3.2 What this means for the shape of the one build

- The fold is **not a subset that the cutover swallowed**, and it is **not divergent**. It is the **same
  architecture at 1× runtime scope, executed**, with the cutover at 4× runtime scope, planned.
- **Nothing in the fold is refused by the cutover.** Not one contract, not one cage, not one authority.
- The cutover adds exactly three things the fold does not have: **(a) the other three runtimes** (CRM/SMS,
  Pipecat, Retell - the fold said "webchat only" in plain words); **(b) the retirement discipline with a
  number** (65 competing authorities falling, and "nobody will announce that the migration failed, they will
  call it hybrid"); **(c) the evidence rank** (OBSERVED > DEMONSTRATED > SUSPICIOUS > INFERRED), which is a
  verification discipline the fold never had.
- **Where the fold is better evidenced than the cutover:** the fold's slices carry MECHANICAL CAGES that
  exist and have been red-proven. The cutover's 45 steps carry acceptance criteria in prose. When they
  conflict, the fold's cage is a fact and the cutover's step is an intention.

---

## 4 · THE CONTRADICTIONS

### Settled - where the evidence decides

**S1 · The PM registry counts one pile of work twice.** `myli-mvp` and `myli-architecture-cutover` resolve to
the same record card and the same 290/113/138. Measured in `.claude/state/pm/projection.json`. This is a
registry edit, not a judgement call.

**S2 · `hl_derived_value` does not exist.** `MYLI_AUTHORITY_MAP.md` names it as the sole authority for derived
values. Measured by me, 2026-09-12: `git grep -l hl_derived_value` returns **zero application files** - only
boards, `tools/master-plan/floors.json`, `apps/houseloop-crm/pm/projection.json` and
`tools/myli/authority-manifest.json` (the law naming it). The cutover's PRD says the same (F31, *"exists in
neither database and nowhere in the tree"*). The fold's F5.3 is open. Three independent sources agree.
**The law currently points at a table nobody built, and no lane may cite it as live.**

**S3 · The fold's "death by permission" cannot be done as written.** Fold F8.1 requires DB permissions to
refuse the old write paths. Record item `d619cf7e` (open, card `myli-ssot-fold`) measured the blocker: *"both
databases have exactly ONE login role - postgres, a superuser, the role the app connects as."* There is no
second role to revoke from. The import half of F8 is real and shipped; the permission half is a plan written
against an estate that does not have the shape it assumed. **The cutover's Step 9 retirement contract is the
workable replacement.**

**S4 · The findings count disagrees four ways.** `docs-site/command-center/myli-architecture-cutover.mdx`
frontmatter says *"28 modules, 71 findings."* `THE-CUTOVER-MASTER.md` §5 says 65 findings placed across five
classes. `docs/myli-audit/audit-findings.md` runs to **F79**. The evidence rank ranked **F1 to F65**. The
reconcilable reading: 65 is the SYNTHESISED-AND-RANKED set, 79 is the FILED set, and the gap is findings filed
after the synthesis closed. **71 matches neither and the mdx description is stale.** Fix the description, not
the counts.

**S5 · Three items whose titles begin "ZACH" are invisible to the morning desk.** `d7e311b2` (which email do
you sign in with), `f6628d75` (does an account or a claim unlock sold prices), `a00194aa` (a stranger reads a
sale price off a /sell/report URL) all carry `blocked_by: none`. The projection's `zach_calls` list holds two
entries, neither of them Myli. A decision that needs Zach and carries no `zach:` block does not reach
`/boards/decision-digest`. **This is a record-grammar fix, one line each.**

### Unsettled - both readings, and what each stands on

**C1 · ⛔ Did the fold cut over, or not?** This is the most important unreconciled thing in the estate.

- **Reading A: it cut over.** Fold item `9f38a522` (F1.5, THE ONE CUTOVER SWITCH) is ticked DONE with
  `checked_at: 2026-09-11` and a note reading: *"prod proven ssot_v1 by two ledgered turns; the legacy road
  deleted (11 files); floors re-seeded by the desk."* Receipt on disk at
  `.claude/reports/grinder/myli-ssot-fold/RECEIPT-cutover-switch-2026-09-11.md` (17,940 bytes). I verified the
  deletion myself: `apps/houseloop-web/lib/myli-legacy/` **does not exist**, and no `MYLI_RESPONDER` dispatch
  exists anywhere in `apps/houseloop-web`. There is one responder.
- **Reading B: it did not.** The fold's own release gate is F9.1b THE THREE WALLS and F9.1 the four journeys
  pass×2, and the plan says in plain words *"anything else: do not flip Sunday."* **F9.0, F9.1, F9.1b and
  F9.2 are all open in the record.** The gate that was supposed to authorize the flip never ran.
- **And the consequence nobody has ruled on:** `MYLI_AUTHORITY_MAP.md` THE CUTOVER RULE and
  `migration-cutover.md` §4.1 (*"the one switch that exists"*) both describe a `legacy_responder ↔ ssot_v1`
  switch as the live rollback path. **That switch does not exist.** The serving plane is no longer reversible
  by flipping anything. The fold's v3 §9.1 anticipated this (*"if the fault is in the ledger, identity, memory
  admissibility, PageContext, or the renderer, there is NO rollback - fail closed"*) - so deletion may be
  correct and intended. But a constitutional law and a 45-step plan written a day later both still name it.
  **Someone must rule whether the law is stale or the deletion was early.**

**C2 · Which card owns the cutover?** The build registry says `myli-os`. The 47 audit-night items and all six
runtime P0s are filed on `qa-bench` (a `mission-control` card). Two data P0s are on `home-value-report`. The
`cutover-order` lane signed itself as card `myli-os`. Reading A: findings belong on the bench because the
bench is where they were caught. Reading B: a build whose findings live on another build's card cannot report
its own progress, which is exactly what §2.0 measures. **Desk call.**

**C3 · Fold F1.4 is two different slices wearing one number.** Record holds `4957abf9` *"F1.4 FOUR SURFACES
READ THE LEDGER"* (open) and `95dd6952` *"F1.4 IDENTITY"* (done). Any sentence saying "F1.4 is done" is true
and false at once. **Renumber one of them.**

**C4 · A verdict registry certifies a file that no longer exists.** `tools/vocab/claim-refs-verdicts.json:12`
carries a verdict on `lib/myli-legacy/agent-voice.ts` describing it as *"A REAL SECOND COMMITTER, counted in
the floor."* The file was deleted with the legacy road. Whether the claim-refs floor is now permanently one
count too high, banking headroom, is unproven - I did not run the cage (read-only lane). BEDROCK's third
clause is explicit that this is how a registry rots. **Worth one command at the desk.**

**C5 · `the-myli-book` is registered as a live spec board and declares itself legacy.** Its own first line:
*"⚠ LEGACY since 2026-09-07 (ruling #257.305)."* It is still `spec_boards[1]` of `myli-mvp`. Reading A: keep
it, it is the record of every ruling behind the program. Reading B: a legacy board registered as a spec is
the precise trap of a board saying what someone intended. **Recommend: keep the file, move it from
`spec_boards` to the docs `book` slot it already occupies in the projection.**

---

## 5 · WHAT IS ACTUALLY DONE - measured against the record, not the boards

Source: `tools/pm/record.mjs` over `tools/master-plan/floors.json`, read 2026-09-12.

| | items | done | superseded | open |
|---|---|---|---|---|
| card `myli-os` (MVP + cutover share it) | 290 | 115 | 39 | 136 |
| card `myli-ssot-fold` | 81 | 37 | 0 | 44 |
| **THE MYLI OS, combined** | **371** | **152** | **39** | **180** |

Plus the cutover's findings on other cards: **47 audit-shaped items on `qa-bench`**, 46 of them open, and two
P0s on `home-value-report`, both open.

**Where a board and the record disagree, the record wins. Three places they do:**

1. **The MVP board's L1 column says "LIVE on prod."** The record's floor-42 band shows the turn contract items
   done, so this one holds. But the same column says L2 "UNMEASURED", L4 "PARTLY", L5 "unmeasured end to end",
   L7 "STALE" - and those are dated 2026-09-09. **No board status column has been re-taken in three days.**
2. **The fold board carries no status at all.** `apps/houseloop-crm/boards/myli-ssot-fold.html` is the plan
   rendered, with no ticks. Its ticks live on `/boards/master-schedule-myli-ssot-fold`. So for the fold, "the
   board says what someone intended, the record says what shipped" is literally the file layout.
3. **The cutover's board does not exist** (§2.0). Its status lives only in the mdx's own §4 table, which says
   W1/W3/W4 DELIVERED and W2/W5/W6 QUEUED - and the record confirms: `3f18c12f` (W5), `2b8b2bfd` and
   `25087a1e` (W6), `3bffd522` (W2) are all open on `qa-bench`.

**The honest headline: THE MYLI OS is 41% verified (152 of 371), the fold's spine is built and its gate never
ran, and the cutover has delivered three of six audit workstreams and zero of 45 build steps.**

---

## 6 · THE ONE ORDER

**This is not a new sequence. It is `migration-cutover.md`'s 45 steps, cited by number, with the fold's
outstanding work slotted where §3 puts it.** Where a fold item is named, it is named by its 8-character record
key so it can be ticked.

### Before anything: Phase 0, and it is not a build (Steps 1–6 + probes P1–P3)

Both Phase 0 reds are fold cages, which is the reconciliation made concrete:
- **Step 1** - resolve the `actor-binding` RED at the source (26 against a floor of 25). Related open fold
  item: `c82f6bbe` (the cage's blind spot on `user!.`).
- **Step 2** - resolve `myli-ssot-census` `count_producers` (42 against 41). ⚠ Its own note: never edit the
  floor file to clear this.
- **Steps 3–6** - push the two landed P0 fixes · re-cut the invariant register · re-take the outbox count
  (fold item `dc9dddd9`, the 446 legacy_import events wrongly projected, is the same query) · contain F56 and
  route the permitted-disclosure matrix.
- **Probes P1, P2, P3** - hours each, and P3 (one signed-in qa-bench walk) closes five findings at once. P3 is
  the highest-value hour in the whole order.

### Week one: the three prerequisites, all of which land together (Steps 8, 10, 16)

- **Step 10 - the authority ledger.** PRODUCT records signed by Zach, SYSTEM records by the technical
  authority. Nothing normative converts before this.
- **Step 8 - the verdict algebra and the verdict-inflation sweep.** No floor is read as evidence before this.
  Its evidence is already carded: `1ec03071`, `88abec08`, `68fa88f2`, `f47125fa`.
- **Step 16 - `CustomerValue<T>` and the execution wrapper.** Nothing in Phase D begins before this.

Beside them: **Step 7** (arm the verifier tree-wide - the escalation is carded, `bba87cda`), **Steps 9, 11,
12, 13**.

### Phase E runs from Monday, in parallel, and this is where the fold's unfinished memory work lives

Five of six Phase E steps depend on nothing in Phases A–D. Serialising them behind the kernel wastes the
capacity `PROACTIVE_PM_MODE` exists to use.

| step | carries the fold item |
|---|---|
| **Step 24** `hl_derived_value` + inputs | **F5.3 `785ce897`** - and until it lands the authority map's row is a gap wearing an authority's name |
| **Step 37** one fact-slot vocabulary | **F5.1's data half**, and fold item `652b93b9` (53 rows across 4 casing twins) |
| **Step 36** one metric registry | **F4.4 `d279aaff`** (market knowledge) |
| **Step 35** one value authority | the two `home-value-report` P0s `fb48f239` and `e5b7811b` |
| **Step 38** identity endpoint binding | **F1.4-identity's backfill half**; run its partition query in Phase 0 |
| **Step 39** web-to-CRM lineage | **F1.2's projector at 2.5%**; fold items `b86e0b2e`, `dc9dddd9` |
| **Step 40** dead-asset quarantine | **F8.1's next-week half**; fold item `d619cf7e` supersedes the permission approach |

### Phase B: the tiny shared kernel - and this is where the fold's contracts get widened

- **Step 14 the InteractionFrame** - `TurnContextV1` widened additively. Carries fold F3's residue: `3af4acac`
  (the lead-bearing actor), `5ee9a646` (ActorInput gains personId), `43223bb6` (F3.5 dashboard backfill).
- **Step 15 `actor_resolution`** - ⛔ runs alone, both halves in one pathspec commit. Carries `fc4d181a`.
- **Step 19 the offer object** - carries fold **F2.1 `0a493076`** and **F2.3 `84983b6f`**, which are the same
  work at smaller scope. ⛔ never behind a switch.
- **Step 20 the status FSM** - carries fold `a90990b0` (retire `lib/search/tool-status.ts`).
- **Steps 17, 18** - carry fold **F6.1b `c29ca1cc`** (the K adversary journey).
- **Steps 21, 22, 23** - the state-machine registry, `CanonicalHouseResolver` + the route boundary, the policy
  executor. Step 22 carries the county P0 `24be990a`.

### Phase C: wrap all four runtimes (Steps 25–29) then ⭐ STEP W1, the 390 px walk that gates Phase D

Step 27 (the owner-dashboard road into the ledger) is fold F1 applied to the surface the fold left out;
`41da0dba` (all 29 owner-dashboard turns ran outside the ledger) is its measured brief.

### Phase D: web surfaces, serial (Steps 30–34) · Phase F: the autonomy ladder, serial (Steps 41–45)

### And the fold work that belongs on the MVP, not the cutover

F4.3's product polish (`2d55c382`), the prod defects from the 9/11 walk (`e3fbbb92`, `03b20bf8`, `7a33c3a2`,
`0bc786ae`, `cee78f35`, `88279713`, `27ebd1bd`). These are MVP link-quality items. Putting them in the
cutover's order is how a migration becomes "hybrid".

---

## 7 · WHAT NEEDS ZACH

Seven real decisions. The first three are structural and cost nothing to answer.

1. **⭐ Is the webchat serving plane still supposed to be reversible?** The legacy responder is deleted. The
   law and the 45-step plan both still name the switch as the rollback path. Either the law gets amended to
   say "fail closed, no rollback" (which the fold's own v3 §9.1 already argues), or the switch comes back.
   **Nothing else in this document is as load-bearing as this one.** (§4 · C1)

2. **Does THE MYLI OS become one build row with three projects inside it, and which record cards does it
   own?** Recommendation: one build `myli-os`, three project sections, and the 47 cutover findings move from
   `qa-bench` to a `myli-os` section so the build can report its own number. (§2.0, §4 · C2)

3. **Is the fold's Sunday gate (F9.1b THE THREE WALLS, F9.1 four journeys pass×2) still required, now that the
   flip already happened?** If yes it is the first QA of the week. If no, void the four items with a reason
   rather than leaving them open forever. (§4 · C1)

4. **The permitted-disclosure matrix (O7, O7a–c).** Blocks Steps 6b, 23 and 33. Routes to the compliance
   authority FIRST, then Zach inside the permitted set. Two record items are waiting on it: `f6628d75` (does
   an account or a claim unlock sold prices - #257.540 reads one way, #257.568 the other) and `a00194aa`
   (⛔ P0: a stranger reads any home's sale price off a `/sell/report` URL today).

5. **O10 and O11 - is a five-step flow a conversation, and what is "the Journey"?** Blocks Step 31 and, with
   it, the seller consultation, the must-haves funnel and the stepper all at once.

6. **O12 - how much of "what she can do" belongs on the signed-out page?** Blocks Step 34. ChatGPT asked for a
   pass with dashboard screenshots and nobody has done it.

7. **O19, O20, O21 - the low-confidence value sentence, the sample page, the second claimant.** Blocks Steps
   33 and 35. Sharpened by `e5b7811b`: 198,810 of 200,935 `hl_value_history` rows are `source='backseed'`,
   synthesized rather than observed. **If that chart is shown to a seller as their home's history, it is a
   fabricated series presented as a record.**

⚠ **And a mechanical note that makes all seven visible:** items 4 through 7 are routed to Zach by
`migration-cutover.md` §10 and **none of them is carded with a `zach:` block**, so none reaches
`/boards/decision-digest`. Seven `task.mjs block` lines fixes that. (§4 · S5)

---

## 8 · THE SOURCES, AND WHAT EACH IS GOOD FOR

| document | what it is the authority for |
|---|---|
| `apps/houseloop-crm/boards/the-myli-mvp.html` | the seven links and what PERFECT means per link |
| `apps/houseloop-crm/boards/the-myli-book.html` | ⚠ LEGACY since #257.305. The record of every ruling behind the program |
| `.claude/reports/myli-os/THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md` | the ten slices, the three walls, the eight named refusals |
| `.claude/reports/myli-os/THE-MYLI-CONTRACTS-V1-2026-09-10.md` | C1–C10, frozen. A lane implements behind them; it never redefines one |
| `.claude/rules/MYLI_AUTHORITY_MAP.md` | constitutional. One authority per semantic question ⚠ its `hl_derived_value` row is a gap (§4 · S2) |
| `docs/myli-audit/synthesis/current-state.md` | what the estate IS today, 65 findings in five classes |
| `docs/myli-audit/synthesis/target-architecture-prd.md` | the target. 101 rules, 52 refusals. **§3.9 is the contract-level reconciliation this document builds on** |
| `docs/myli-audit/synthesis/migration-cutover.md` | **the order. 45 steps. Cite steps by number; do not paraphrase** |
| `docs/myli-audit/audit-findings.md` | F1–F79 with evidence |
| `docs/myli-audit/13-evidence-rank.csv` | the rank: OBSERVED 17 · DEMONSTRATED 31 · SUSPICIOUS 14 · INFERRED 2 · REFUTED 1 |
| `tools/master-plan/floors.json` via `tools/pm/record.mjs` | **what actually shipped. The record wins over every board above** |

---

*⚠ One limit of this document, stated rather than hidden: I read the three synthesis documents by section and
by grep, not line by line - `migration-cutover.md` alone is 1,643 lines and `target-architecture-prd.md` is
1,632. Every claim above about a step's content is taken from that step's own text, and every claim about
state is a record read or a `git grep` I ran on 2026-09-12. Where I did not measure something myself I said
whose measurement I am citing.*
