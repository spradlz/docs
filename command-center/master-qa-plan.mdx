---
title: "The Master QA Build — Live Plan"
description: "The week-long end-to-end QA campaign: lanes, coverage, depth charter, build log."
---

# 🔍 THE MASTER QA BUILD — the plan, live

**For Zach · opened 8/11 · this page updates as the loop runs. You are never the bottleneck: I only
ping you for red findings that need your taste, plus a daily summary here.**

## The mission, in your words

> "Before I add more features I need to make sure the existing features work perfectly… every single
> thing tested… verified from end to end… I should not have to be brought in to catch the Conner
> Kelley thing."

## Your four rulings (8/11 interview)

1. **Test identities:** I provision my own (test inbox + a phone number I can read) — deliveries get
   verified without ever pinging you.
2. **Notifications un-mute per lane** — the freeze's lifeline suppression lifts class-by-class, only
   as each lane's walk proves its notifications correct.
3. **Fix as found** — nothing stays red behind the walk; every fix is one line in the build log.
4. **Lane order: Website → CRM → FUB → TC** — start where leads are born, follow the data.

## The machine (what's novel here)

- **The Coverage Ledger** — the checklist is *generated from the code* (every route, nav item,
  button), so nothing is skipped because nobody thought of it. Green/red/unwalked, visible here.
- **The Render Lint** — a nightly sweep that reads every live card like a human and flags the
  "An agent edited a lead" class automatically, forever. Machine-caught, never Zach-caught.
- **Docs = proof** — the ~300 drifted docs move to a legacy shadow set; canonical docs are written
  ONLY as each lane goes green. The documentation becomes incapable of describing the unverified.
- **The Build Log** — every fix made during QA, one line each, running record → the changelog.

## The lanes (board items 60–70 on the Bedrock board)

| # | lane | status |
|---|---|---|
| 60 | Scaffolding: test identities + coverage ledger + this page | ✅ done 8/11 |
| 61 | Website walk — every registration path, view/save/share, saved searches both arms, alert DELIVERY | 🟢 opens next |
| 62 | CRM walk — every left-nav surface, every button, card+notification+runlog per action | queued |
| 63 | FUB replay — test lead + all 26 signals as live payloads, E2E | queued |
| 64 | TC loop walk — the full spec from the signed artifacts | queued |
| 65 | Render lint — nightly card QA, forever | queued |
| 66 | Notification un-mute, per lane | queued |
| 67 | Docs consolidation + build log | queued |
| 68 | Seller engine census → sized follow-on | queued |
| 69 | Rule-0 column drop — `channel` dropped, reversal kept; `direction` awaits its writer migration | ✅ done 8/11 |
| 70 | The 74 "system" sends + fresh-eyes folds | queued |

## The build log (fixes made during QA — newest first)

- **8/11** · Lane 1 · **Tour + share flows walked; a SILENT-DEAD notification class found and
  fixed** — a returning lead's tour/contact request notified NOBODY: the intake spoke pre-rename
  tokens while the wiring spoke canon, and the fail-closed default read the miss as "off." Both
  ends fixed per the rename law's own prescription; tour/contact join the un-mute list with this
  walk as their proof. Bonus: the tour form backfills the nameless lead's name. (5f02600432)
- **8/11** · Lane 1 · **Saved-search LEAD ARM green + the organic automation proof** — the walk's
  registrant saved a search (web row + CRM card verified), and the registration organically fired
  the full automation chain: Myli searched, noted, and sent a genuinely on-voice first-touch text
  within 10 seconds. Also caught + fixed same-hour: cards showing a raw UUID as the actor (the one
  writer took an id as a display name — 12 old rows healed). Alert-delivery + agent-arm legs are
  running with a worker now. (54020829ea)
- **8/11** · #66 E2E · **THE BELL RINGS** — fresh registration → lead minted → a real
  "Registration Completed" notification addressed to Zach at 18:13:58. The per-lane un-mute is
  proven live in production; classes keep joining as their lanes green.
- **8/11** · #81 · **The auditor's finds FIXED same-day** — the showing-confirmation landmine
  root-caused (the code resolved the partner's phone then dropped it before the template could use
  it — now threaded, template rewritten on real fields); two SMS templates tightened; a color-law
  green swapped to brand blue. One "finding" was the grader's own artifact — noted, not fixed. (ce9f06a6f4)
- **8/11** · #80 · **THE COPY-QUALITY AUDITOR is live** — every human-facing message (emails,
  texts, activity sentences) LLM-graded against the voice laws. Its FIRST sweep caught three real
  bugs: a showing-confirmation text where the partner's name silently renders as NOTHING, a
  533-char SMS that splits, and a truncated alert email — all filed as item 81, fixing next.
  Baselines: email 4.65 · text 4.03 · activity 5.0. (72c87be88a)
- **8/11** · Lane 1 · **FRESH REGISTRATION MINT proven E2E** — gate → email → phone step → Finish:
  lead minted, registration_completed card, the triggering save retro-attached AND renders in the
  user's Favorites. Findings: email registrants are never asked their NAME (nameless lead) — queued;
  the registration notification class (lead_created) is now the FIRST per-lane un-mute (item 66
  shipped: proven classes pass the tourniquet, additive + reversible).
- **8/11** · #65 · **THE RENDER LINT is live** — an Inngest sweep that reads every activity card
  like a human and flags 5 defect classes vs a shrink-only baseline (seed: 51+51 historical rows).
  The "An agent edited a lead" class is now machine-caught, never you-caught. (3a75068bb3)
- **8/11** · Lane 1 · **REGISTRATION + EMAIL DELIVERY proven** — full path walked as a real user;
  the welcome email FIRED and DELIVERED, verified with eyes on the inbox via the new inbox-check
  tool (domain-wide-delegation Gmail read — the delivery eyes for all of QA). (8dcec9605e)
- **8/11** · Lane 1 fix #1 · **GHOST SESSIONS DIE** — found on the walk's FIRST CLICK: a login
  naming a deleted user made every website save/like silently 500 (Zach's own session class — his
  saves have been failing). Root: a DB-error catch that swallowed "user absent." Now the ghost
  degrades to signed-out and the normal sign-in prompt takes over. (1f521dc093)
- **8/11** · #71 · **THE TEST TOWN is live** — Quincy + Quinn Towne (spouse pair on real test
  phone lines) + QA Lender/TC/Showing Partner, all seeded through the real product, all parked
  hidden, every creation card properly named. Registry: Command Center → test-town. Also: Jasmeet's
  REAL email found in Conner's own text and added through the UI — the edit card wrote itself
  perfectly ("Zach Spradling — added jasmeetkh17@gmail.com to contact information"). (21b016122f)
- **8/11** · #73 · **The lender-email routing FIXED** — one email now attaches to EVERY lead it
  belongs to (new join table + connected-people matching rung); the Kelley/Kaur pre-approval is on
  BOTH Conner's and Jasmeet's Emails tab. Opens: the All feed still shows it on one lead (a signed
  key decision queued), and Jasmeet's missing email address is a Test-Town data fix. (2232b495d0)
- **8/11** · #72 · **The Jasmeet hidden-leak FIXED** — the leads table's default view never consulted
  `status='hidden'`; now excluded by default, shown by the Hidden filter (bin symmetry). Proof:
  old gate 1 / new gate 0 / hidden view 1. (97fe20eac8)

## Item 60 scaffolding — progress

- ✅ **Test phone lines:** reusing the two isolated Twilio test numbers already in the estate
  (test-lead line + test-agent line, both readable via the Twilio API — SMS delivery provable
  both directions without touching Zach's phone).
- ✅ **Test inbox:** `qa-probe@houseloop.ai` created (real Google Workspace mailbox, IMAP on,
  creds stored). Email arrival now provable with eyes; Resend delivery events back it up.
- ✅ **CRM probe lead:** reusing the harness probe (hidden disposition, never in the real views).
- 🟡 **Coverage-ledger generator:** in build — enumerates every route + nav surface of both apps
  from the code; walk receipts merge in; unwalked is a visible state, never a silence.
- ⚠ Manual for Zach (whenever): the Master Credentials sheet needs the qa-probe row by hand —
  the sheet-sync tool's Google grant is still invalid from the password change.

## The Depth Charter (Zach, 8/11 — the coverage bar, verbatim scope)

**"If it is a feature it has to apply"** — nothing gets waved off as not-applicable without Zach's sign.
The walks run against a persistent **TEST TOWN**: a full cast of test personas with REAL communication
endpoints — test agent (own inbox, receives the real Gmail calendar invite), test showing partner (own
inbox + own Twilio line), test lender, test TC, test connected people, and multiple Twilio numbers so
GROUP TEXTING (Conner + Jasmeet class) is provable. Idle test numbers get reassigned (authorized).
The bar per feature: appointments with invites RECEIVED · tasks through EVERY disposition · emails
full-gamut including CC-a-connected-lead · saved searches with the connected person VERIFIED receiving.

**The second depth pass (Zach 8/11, items 76–79):** the FILTERS CAMPAIGN — advanced behavioral
filters built and walked ("Appt Set AND no text in 7 days" + website behavior), each proven by
seeding Test-Town behavior and checking the filter finds exactly those leads · TRASH/VISIBILITY
semantics as a full matrix (state × surface × filter — the Jasmeet leak is one cell) · EMAIL
ENGAGEMENT telemetry (saved-search opens, house clicks, the green open pill) verified inside
Zach's real Gmail via the confirmed DWD delegation · TC DOCUMENT status events carrying the
document's NAME on every card.

**Two live bugs Zach found, now board items 72–73:** Jasmeet marked hidden still shows on the leads
table (measured: the hide never landed on her lead row — a silent no-op); a lender's pre-approval
email with Conner + Jasmeet attached never reached their feeds. Plus item 74: the inbox
simplification study — a design pass, not a patch.

## The Command Center rule (new today, Zach's directive)

Artifacts are account-locked scratch. **Anything signed gets promoted as a real page** —
boards to `crm.houseloop.ai/boards/…`, documents to the Mintlify Command Center
(`houseloop.mintlify.io/command-center`) — so the next dev on either Claude account reads the
same truth. First promoted: the Gate-2 report + this very plan (re-promoted as it updates).

## The coverage denominator (derived from the code, 8/11)

**179 surfaces**: 143 CRM (78 in the left nav, 24 sections) + 36 website. 0 missing.
5 marked as ComingSoon stubs so walkers don't file false reds. All 179 currently ⬜ unwalked —
this number turns green surface by surface, with a receipt behind every green.

## Daily summary

- **8/11 (day 0):** Gate 2 executed ten-for-ten; freeze lifted on Zach's sign; Conner Kelley card
  class fixed (headline + sublines + 11 writer doors closed); plan drawn, interview held, lanes set.
