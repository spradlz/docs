---
title: "Playbook — Buyer's Agency"
description: "The buyer-representation packet end-to-end: which of the three agreements to send, the Kansas and Missouri broker disclosures that must be signed WITH it by law, the human how-to, the AI-callable path, and the licensing check that decides who may be designated."
---

**In the app:** a loop → **Forms & E-Sign** → send the buyer-representation agreement.

This is the first TC Playbook: the documented process for putting a buyer under representation, written for
both a **human** and the **AI clone** to execute from the same source.

## THE LAW — the packet, not the form

<Warning>
**Ana Spradling, 2026-07-30:** *"I will need you to include with the buyers agency the Kansas brokerage
disclosures and the MO broker disclosure with it. **Per law they have to sign with the agency.**"*

A buyer-representation agreement sent **by itself is non-compliant.** The state broker disclosure is
not optional paperwork and not a follow-up — it is signed **with** the agreement, in the same packet.
</Warning>

| Sent | Also required, same packet | Packet |
|---|---|---|
| **`exclusive_buyer_agency`** — the Buyer Agency Agreement | **Kansas** brokerage relationships disclosure **AND** **Missouri** broker disclosure | **9 pages** (6 + 1 + 2) |
| `single_property_buyer_rep` | — none | **4 pages** |
| `non_exclusive_buyer_rep` | — none | **6 pages** |

Only `exclusive_buyer_agency` carries `attachments: [KS_DISCLOSURE, MO_DISCLOSURE]`. **BOTH** disclosures
ride with it — never one state or the other, because we work both sides of the line.

<Warning>
**⚠ I FLIP-FLOPPED THIS PAGE FOUR TIMES ACROSS TWO DAYS. The record is here so the drift is visible
rather than looking like the page was always right.**

1. The table originally said **"Any"** buyer-representation agreement — my own paraphrase of Ana.
2. Zach: *"I think only the exclusive buyer agency has to carry the disclosure."* → narrowed.
3. Zach: *"always include both Kansas and Missouri disclosures."* → I widened it back to all three
   AND added the attachments to all three forms in code.
4. Zach, 2026-08-07: *"the single property and the non exclusive buyer tenant do not require the KS and
   MO disclosures, only the buyers agency agreement — I thought we had agreed on that in the playbook."*

⭐ **STEP 3 IS THE MISREAD, AND IT IS A PRECISE ONE.** *"Always include BOTH"* means **both DISCLOSURES**
— Kansas AND Missouri, never just one. It does **not** mean "on all three AGREEMENTS." He was answering
*which disclosures*, and I heard *which agreements*. Two different questions, one pronoun.

⭐ **AND ANA'S ORIGINAL SENTENCE SAID IT ALL ALONG:** *"include WITH THE BUYERS AGENCY the Kansas
brokerage disclosures and the MO broker disclosure."* **With the buyers agency.** Not "with any
buyer-representation agreement." My paraphrase in step 1 widened a legal rule, and every later round was
me arguing with my own paraphrase instead of re-reading the source. The quote was never ambiguous —
I stopped reading it.
</Warning>

## WHAT EACH DOCUMENT IN THIS PACKET IS

Three separate legal documents, welded into ONE signing session so the client signs once.

### 1 · Exclusive Buyer Agency Contract — 6 pages
**The representation agreement itself.** It is what makes us the buyer's agent: it names the client, the
brokerage (eXp Realty LLC), the designated agent, the term, the property type and price range, and how we
are compensated. Signed by the **buyer** (and co-buyer) and the **designated agent**; a broker line exists
for Missouri. The buyer initials pages 1-5 and signs page 6.

⚠ **This is the one that has to be signed before showing property.** Everything else in the loop —
offers, disclosures, the transaction — assumes this exists.

### 2 · Kansas Brokerage Relationships Disclosure — 1 page
**A Kansas-required consumer notice**, not an agreement. It explains the relationships a Kansas licensee
can have (seller's agent, buyer's agent, transaction broker) so the client understands which one we are.
The client signs to acknowledge they received it. It is an acknowledgement — it grants nothing and
obligates the client to nothing. **No date line is printed on the form**, so none is placed.

### 3 · Missouri Broker Disclosure — 2 pages
**The Missouri equivalent**, and the reason both ride along: our market crosses the state line and a
client may end up buying on either side. Sending only one state's disclosure means guessing which state
the eventual house is in, at a moment when nobody knows yet. Signing both costs the client one extra
signature and removes the guess. **Also no printed date line.**

<Note>
**Why ONE packet and not three emails.** They merge into a single PDF and a single signing session, so
the client signs once and every page returns together, fully executed. Three separate envelopes means
three chances for one to sit unsigned — and an agency agreement without its disclosure is the
non-compliant state Ana's rule exists to prevent.
</Note>

## THE DATE — a plain field on the form, and the certificate at the back

The KS and MO disclosures have **no printed DATE line** — the field extractor confirms it and correctly
refuses to invent a coordinate for one (`SIGNATURE_ANCHORING.md`: a missing label is a warning, never a
guessed coordinate). The Exclusive agreement's broker line has none either.

⚠ **AND THAT STAMP IS GONE — REMOVED 2026-08-06.** We drew a `HouseLoop verified / date / Signature ID`
block beside every mark for five weeks. It had nowhere to fit on forms with no date column (the KS and MO
disclosures), so it landed on the cursive signature; on the agreement it collided with the printed date
and captions. Zach: *"the signing certificate is legit enough ... we have to have something robust enough
to go through about 58 documents without worrying about placement of every one of the certification seals."*

**What carries the date now:** a plain Documenso DATE field on the form's own printed date line, computed
server-side in Central time — so a signer cannot backdate, typo or skip it. The audit trail is the
**signing certificate** Documenso appends as the packet's last page. We archive its sealed PDF byte-for-byte
and draw nothing on it.

## The three agreements

Which one to send is the first decision:

| Form key | Name | When |
|---|---|---|
| `exclusive_buyer_agency` | **Exclusive Buyer Agency Agreement** | The standard Spradling buyer-rep path |
| `single_property_buyer_rep` | **Single Property Buyer-Broker Representation** | The **showing agreement** — one specific house, before a tour |
| `non_exclusive_buyer_rep` | Non-Exclusive Buyer-Tenant Broker Rep | Exists; not the standard path |

The **Single Property** agreement is the one that goes out before showing a house the buyer hasn't committed
on. The **Exclusive** agreement is the real representation agreement.

## WHO may be designated — the licensing check

Before any of this, the system checks the designated agent against the **property's state**. From the
broker, Chris Cribb (Designated Managing Broker, MO & KS, eXp Realty, 2026-07-29):

> Ana has an active MO and KS license and can sign any forms or be designated for sales in either state.
> Zach has **ONLY a KS license** and can be designated to sign and show property in the state of KS, but
> **can NOT show, write contracts or be designated for properties in MO.**

HouseLoop knows each agent's per-state license (`hl_user_license`) and **warns** — it never silently
blocks — at three points: picking the agent, filling the document, and sending it. Full model:
[Transaction Coordination → Licensing](/guides/transaction-coordination).

## FOR A HUMAN — the steps

1. **Open the lead's loop.** No loop yet? Create one. For a showing agreement, attach the specific house
   first — the property drives `deal.address` on the contract, and without it the Property line fills blank.
2. **Check the designated agent** against the property's state. If the licensing warning appears, change the
   agent — do not send past it.
3. **Add Document** → pick the agreement → it lands as a **draft**.
4. **Fill out** → review every field. The buyer's name, the property, and the term dates pre-fill from the
   lead and the loop; complete anything blank.
5. **Add the state disclosures** to the same packet (see the warning above).
6. **Save & Share** → pick who gets it → send. The buyer gets a signing link by email, and you can text it.
7. The loop shows **Waiting on Signatures** with a reminder toggle until it comes back signed.

## FOR THE AI — one call

`createFillSend(leadId, formKey, opts)` composes the same engines the UI uses — no fork, no separate path.

```ts
import { createFillSend } from '@/lib/esign/create-fill-send';

// Showing agreement for one specific house: create the loop WITH the property first,
// then createFillSend reuses that loop.
await createLoop({ leadId, side: 'purchase', propertyAddress: '14637 Crystal Place, Grandview, MO 64030', assigneeUserId: agentUserId });

const res = await createFillSend(leadId, 'single_property_buyer_rep', {
  agentUserId,                 // the human agent who signs — licensing is checked against the property
  senderUserId: agentUserId,   // whose account it sends AS
  includeBroker: false,        // true = full 3-way (adds the broker of record)
});
// → { loopId, docId, esignDocId, signingUrl, sentTo, sentAs, license }
```

**Check `res.license`.** `ok: false` means the designated agent is not licensed where the property is —
the send still happens (warn, never block), but a mismatch must never be silent.

Then deliver `signingUrl` however the clone communicates.

## What gets signed, and where the date comes from

Each agreement carries a **SIGNATURE** and a **DATE** field per party, extracted from the PDF — never typed.
The signer does not type the date: the signing engine computes it server-side at the moment that party
signs, in **Central** time, formatted `MM/dd/yyyy`. Nobody can backdate or skip it.

The law and the extraction process: `.claude/rules/SIGNATURE_ANCHORING.md` · [Forms & E-Sign](/tc/forms).

## What's missing (open, named)

| Gap | Detail |
|---|---|
| **The two state disclosures** | Kansas brokerage disclosure + Missouri broker disclosure are **not in the catalog**, so today's send is not the compliant packet Ana requires. They need to be pulled from dotloop ([dotloop Integration](/guides/dotloop-integration)) and added as catalog entries with their own field maps. |
| **Packet send** | `createFillSend` sends ONE form. Sending an agreement + its disclosures as one signable packet is a build, not a config change. |
| **Non-Exclusive form dates** | `non_exclusive_buyer_rep` still has signature fields only — no DATE fields. Its text layer is garbled, so it needs the vision path in [Forms & E-Sign](/tc/forms). |

See also: [Playbook — Writing an Offer](/tc/playbook-offer-writing) · [Forms & E-Sign](/tc/forms) ·
[Transaction Coordination](/guides/transaction-coordination)
