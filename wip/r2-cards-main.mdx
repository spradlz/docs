---
title: "R2-cards (r2-cards LANE HANDOFF — LEE-WRITTEN BACKSTOP (2026-08-14 ~21:50Z, pre-account-switch))"
description: "Work-in-progress dossier — R2-cards. r2-cards LANE HANDOFF — LEE-WRITTEN BACKSTOP (2026-08-14 ~21:50Z, pre-account-switch)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/r2-cards.md` on every push. **Progress: checklist pending.**</Info>

**Owner:** R2-cards

## Checklist

# (If r2-cards wrote its own version, that one wins — this is the desk's map of the lane.)

### MISSION — LEDGER 103 (Zach-signed with creative freedom)
Full-payload HERO cards DROP the actor circle (Zach: "Brent Morris liked a property is too wide...
wrapping"); COMPACT cards KEEP it. Desk chose the clean drop (not the stamp-on-picture alternative).
Then REGENERATE the catalog board so the signed board matches the shipped render.

### SHIPPED BEFORE THE SWITCH (from the lane's own report, logged in the run log)
- The rulings-executed round: arrow dead via Forward→Users; mig 0771 provider_thread_id live
  (inbound side needed nothing — their scope correction); they caught the `if (!key) continue`
  guard bug in stampReplies.
- Their earlier work rode train 981974a765 (verify Railway went green).

### STATE AT SWITCH
- Lane was STILL WORKING when the session ended — ledger 103 hero de-avatar was the active task.
- Files they own: components/leads/detail/ActivityHeroCard.tsx, lib/activity/render-activity.ts
  (render-activity.ts had an in-flight buffer that safe-push set aside earlier — check git status).
- NOT YET CONFIRMED DONE: the hero de-avatar itself, the board regenerate/promote.

### NEXT SHIFT
1. `git status` + `git log --oneline -20 -- apps/houseloop-crm/components/leads/detail/ apps/houseloop-crm/lib/activity/` — auto-save commits carry their last edits.
2. Read ActivityHeroCard.tsx: is the actor circle conditional on layout yet?
3. Finish per ledger 103, regenerate the catalog board, promote, re-walk one hero + one compact card.
