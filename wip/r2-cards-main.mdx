---
title: "R2-cards (r2-cards — LANE HANDOFF (written 2026-08-14 ~22:05Z, account switch))"
description: "Work-in-progress dossier — R2-cards. r2-cards — LANE HANDOFF (written 2026-08-14 ~22:05Z, account switch)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/r2-cards.md` on every push. **Progress: checklist pending.**</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 193h ago. If R2-cards has shipped since, this dossier under-reports — refresh `.claude/state/wip/r2-cards.md`.</Warning>

**Owner:** R2-cards

## Checklist

**TREE STATE: CLEAN. NOTHING MID-FLIGHT. NOTHING HALF-WRITTEN. THE TRAIN IS NOT BLOCKED BY ME.**
Every file the area-guard named is committed. There is no in-flight edit to finish and no next step
inside my lane — the queue is empty. Details below so the next shift can verify rather than trust.

---

### 1. THE ONE-LINE ANSWER TO "WHAT IS BLOCKING THE TRAIN?"

Nothing of mine. The three files the guard flagged were committed before this handoff:

| file | state | commit |
|---|---|---|
| `apps/houseloop-crm/components/leads/detail/ActivityHeroCard.tsx` | committed, clean | auto-save (a PARALLEL worker's ledger-103 edit, not mine — see §4) |
| `apps/houseloop-crm/components/leads/detail/hero-avatar.test.tsx` | committed | `0e0473257b` |
| `apps/houseloop-crm/boards/manifest.json` | **restored to HEAD on purpose** — see §5 | unmodified |

Verify in one command: `git status --porcelain apps/houseloop-crm | grep -v '^??'`.

⚠ **IT WILL NOT BE EMPTY — and none of what you see is mine.** At handoff the tree also carried
OTHER lanes' in-flight files. I deliberately did not commit them (per-file auto-save exists precisely
so one lane's commit cannot swallow another's). Each was checked, and the app COMPILES with all of
them present (`tsc` 0):

- `lib/email/gmail-realtime.ts` — **COHERENT and worth knowing about**: the inbound half of the email
  fold (realtime-received replies wrote `hl_inbound_emails` + the activity log + the notification but
  never `hl_messages`, which the Inbox reads — so a reply rang the phone and then did not exist on the
  screen). Complete statement, import present, idempotent by `ON CONFLICT (channel, external_id)`.
  Safe for the train if the desk wants it; the call is the owning lane's, not mine.
- `middleware.ts` · `app/api/team/users/[id]/route.ts` · two agent `page.tsx` — the avatar-one lane.
- `tools/vocab/kind-literal.floor` (**ratcheted 878 → 876 by another lane**) · `platform-zero.floor`.

---

### 2. SHIPPED THIS SHIFT (shas)

| sha | what |
|---|---|
| `d8fff966c0` | **Ledger 65 · 66 · 67 + the dead arrival set.** Optional-clause catalog grammar `[ … ]`; "HouseLoop automation" stamped at the writer + 45 rows backfilled; document_shared names doc + people; the orphan-glyph mechanism; `ARRIVAL_KINDS` → `INBOX_ARRIVAL_KINDS` (it was a DEAD SET — held the alias `'registration'`, so 59 rows were invisible to the Inbox's arrival half). |
| `098375005a` | **Ledger 88 + the 66 writer gap.** `components/ui/EngagementPill.tsx` — ONE pill, both families. Documents = Opened/Signed only. Rollup keyed on the CEREMONY (`esignDocId`), never `docId`. Kind floor ratcheted **882 → 878**. |
| `f85d11ee78` + `428d2a1d93` | **Mig 0771 `hl_email_send_log.provider_thread_id`** (Lee-signed verdict) + schema re-lock. Applied to the DB and confirmed. |
| `0e0473257b` | **Ledger 103 gate** — `npm run test:hero-avatar` + `tsconfig.render-test.json`. |
| (data, no sha) | Catalog UPDATEs: `stage_changed.sentence`, `document_shared.sentence`, `document_shared.icon` `Forward`→`Users`. All idempotent + before/after recorded. Projection regenerated. |

**GATES AT HANDOFF:** tsc **0** · catalog parity **1487** · card + sentence suites green ·
`test:hero-avatar` green (and proven RED on a planted violation) · kind-literal **878** · door-zero **742**.

---

### 3. LEDGER 103 — DONE, NOT MID-FLIGHT (the item the checkpoint order named)

The de-avatar change was **already in the tree when I picked it up**, built by a parallel worker also
stamped `r2-cards`. I verified rather than re-doing it:

- `ActorAvatar` now survives at **exactly two** call sites — `ActivityHeroCard.tsx:138` (compact) and
  `:379` (insight). `home_card` and the payload hero draw none. ArrivalCard untouched.
- `tools/activity-catalog/cards.mjs` matches (its `homeCard` no longer calls `avatar(who, 36)`; the
  ledger-103 rationale + Zach's verbatim quote are in the header).

**What I added is the GATE that was missing**, because this amendment partially reverses a previously
signed ruling (card-r3's universal hero faces) and is therefore the kind of thing the next person
"restores" in good faith. No model test could catch it: `resolveCard` still resolves an avatar for
these rows and SHOULD — the amendment lives only in which BRANCH renders one.

```
cd apps/houseloop-crm && npm run test:hero-avatar
```
6 assertions. **Proven to bite:** it passed first run (worthless as evidence), so I planted the avatar
back on the home_card body column, watched that one assertion go RED while the other five stayed
green, then restored the file byte-identically (`git diff` clean) and re-ran.

⚠ **If you write another component render test, use `npm run test:hero-avatar` as the pattern.** It
needs `tsconfig.render-test.json` because the app sets `jsx:'preserve'` (Next compiles JSX itself), so
tsx falls back to the CLASSIC runtime — under which EVERY component in this app throws
"React is not defined", since none of them import React. This is the first component-render test in
the CRM; the harness is reusable.

---

### 4. THE BOARD — REGENERATED, AND THE RECEIPT IS THE INTERESTING PART

`node tools/activity-catalog/build-catalog.mjs` then `node tools/boards/promote.mjs …`.

**My independent rebuild produced BYTE-IDENTICAL html to what is already promoted and committed** —
`sha256 5dc60271914c84f31cd75d692a6bb4376f4674756f3d66ddeb7e6a8b27b8df85`, 3,315,467 bytes. That is a
reproducibility proof that the served board genuinely matches the factory, which is a stronger claim
than a fresh promote would have been. **No board work is outstanding.**

Two path facts that cost me ten minutes and are worth inheriting:
- The served board is `apps/houseloop-crm/boards/activity-card-catalog.html`, via `boards/manifest.json`;
  `tools/boards/promote.mjs` is the manifest's ONLY writer.
- The 3.3MB `docs/wireframes/activity-card-catalog.html` output is the **sanctioned untracked build
  artifact**. `dfccd453b6` deleted it from git ON PURPOSE. **Deleted-from-git ≠ removed-from-pipeline** —
  the manifest still names it as `source`. Do not "fix" it by re-committing it or by pointing the
  generator at the boards dir (that publishes bytes under a manifest still vouching for the old hash).

---

### 5. ⚠ ONE THING I DID WRONG AND UNDID — DO NOT RE-DO IT

Re-promoting rewrote `boards/manifest.json`'s title + description, replacing a detailed accurate entry
("30 cards changed; nothing else moved", the full ledger-103 rationale) with a generic one-liner —
**while the sha256 was unchanged.** I made a receipt strictly worse for zero new information, because
I published before reading what was already there.

Restored with `git checkout HEAD -- apps/houseloop-crm/boards/manifest.json docs-site/command-center.mdx`
(both contained nothing but my own churn). The earlier entry stands and is correct.

**THE RULE: a promote is a WRITE TO A SYSTEM OF RECORD. Read the existing entry first, and if the hash
has not moved, do not touch the receipt.**

---

### 5b. FINAL SHIFT BLOCK — ICON (b) SHIPPED + THE ONE-PAGER (assigned after the first handoff)

**ICON (b) — DONE on the PRODUCT.** `document_shared.icon` `Users` → **`Share2`** (data; before/after;
idempotent), both app registries regenerated, and proven through the real resolver:
`badgeFor('document_shared').Icon` **is** the `Share2` component. Zach's "random arrow" is gone.

⭐ **THE DEFECT IT EXPOSED, worth more than the glyph:** `tools/activity-catalog/extract-icons.mjs`
carried **25 hand-typed** icon names under a comment claiming they were "the 25 icon names on
hl_activity_catalog" — the catalog has **59**. The board's registry had drifted from the table by more
than half. Symptom: a new catalog glyph updates the generated APP registry and leaves the BOARD without
the geometry, so the signed board draws a **blank** where the product draws an icon. **It now DERIVES
from `catalog.generated.ts`** — 77 → 78 glyphs on first run, and the one it added was `Share2`.

⚠ **THE BOARD STILL CANNOT DRAW IT — a FOURTH source, reported not patched.** `paint.mjs:paintFor()`
reads ONLY `badges.json`, a deliberately FROZEN 72-badge snapshot (`extract-badges.mjs` is retired:
*"do not regenerate it; do not delete it"*). A kind with no badge entry renders **grey with an EMPTY
circle** whatever the table says — the build's own **`133 grey/no-icon`** line, **133 of 189 kinds**.
I rebuilt TWICE; **the sha did not move either time** (`5dc60271914c84f3`), which proves it cannot move
from the catalog side. **So I did NOT re-promote** — hash unmoved, receipt untouched, per the rule.
The freeze and "the board shows the catalog" are in direct conflict; that resolution is a DESK call.

**ONE-PAGER DELIVERED (the `stage_changed` order):** `.claude/reports/card-copy-r2/ONE-PAGER-icons.md`.
Short version: it is **not** a three-way tie — the TABLE's `Settings` is what renders (proven through
the resolver); `content.mjs`'s `Flag` is a proposal and mig 0506's `ArrowRightLeft` is a fossil.
**Recommendation: keep `Settings`, retire the other two sources — zero pixel change.** No pixels were
changed, per the order.

**HELD DELIBERATELY, each with a reason:** `stage_changed` icon (one-pager first) · `property_shared`
(still `Forward`+green — the OTHER half of the share concept, but Zach signed it, never complained, and
it is the most-rendered family in the product; fold it to `Share2` with the board decision) ·
`badges.json` (frozen) · **`document_shared`'s COLOUR** — the instruction to make it green predates the
fact that **ONE kind covers granted AND revoked**, so green would paint *"removed document access"* in
the success colour (10 granted / 1 revoked live). Options are in the one-pager; I did not pick.

⚠ **COMMIT NOTE:** the auto-save hook swept these files into `5be7d290ae` / `719ecd2d70` while my own
`git commit` was racing it (`cannot lock ref 'HEAD'`). **The work IS committed and the tree is clean** —
only my authored commit message was lost, which is why the full rationale is here and in the one-pager.

**A CORRECTION I OWE m10-delta (their F1):** I suspected their "only 29% of engagement rows can ever
show a pill" was overstated, because my rollup keys on the DERIVED `actor_name`, not raw `by_name`.
**I measured, and they are right and I was wrong:** on every anonymous ceremony the derived name
resolves to exactly the same count as the raw column (`by_user_id` is NULL too, so the COALESCE adds
nothing). **Their writer-gap finding stands as written**, and my pills light up the moment the e-sign
writer stamps an actor. (Also verified while checking: `owner` in that COALESCE is the CLONE's human,
keyed off the row's own actor — NOT the lead's owner — so the rollup cannot misattribute a signature.)

---

### 5c. THE THREE DESK RULINGS — EXECUTED (commit `f842d4b712`)

**RULING 1 — THE GREEN: held at grey, my recommendation adopted.** `document_shared` stays
`#64748B` because ONE kind covers `granted` AND `revoked`, and painting *"removed document access"* in
the success colour is a lie. Zach's green-share carve-out lives at the ICON/affordance level, where it
already exists. **No colour change was made.**

**RULING 2 — THE FREEZE: resolved as THAW-AT-SIGNATURE (desk).** `badges.json` stays frozen so the
signed artifact re-renders as signed; at the **G21 re-signature** the snapshot regenerates ONCE and
re-freezes, and the **133-grey line becomes 0 at that same moment**. Until then, `document_shared`
drawing grey on the OLD signed board is HONEST — it is what was signed. `property_shared` → `Share2`
folds into that same re-signature batch. **Nothing for me to do here until the packet.**

**RULING 3 — THE TWO RETIREMENTS: DONE, and they move ZERO pixels.**
- `content.mjs:106` `Flag` → **`Settings`** (aligned, not deleted, so the card face keeps its sub-line).
- mig `0506`'s `ArrowRightLeft` → marked **SUPERSEDED**. ⚠ **The SQL is byte-identical and deliberately
  so** — an applied migration is a RECORD OF WHAT RAN, not a place to edit history, and a fresh-DB
  bootstrap must replay exactly what the estate replayed. **Comment only.**
- **Verified a no-op on glass, not assumed:** the catalog row still reads `Settings`/`#64748B`, the
  board rebuilt, and **the sha did not move** (`5dc60271914c84f3`) → **no re-promote**, per the rule.
  `migration-lint` green on all four gates (numbering · ledger · table cage · framework).

**NEXT FOR THIS LANE — THE G21 RE-SIGNATURE PACKET (APPROVED by the desk, awaiting timing).**
ONE pass, ONE signature, carrying all three together because they only make sense as one:
  **(a)** `badges.json` regenerate + re-freeze · **(b)** `property_shared` `Forward` → `Share2` so the
  share concept has ONE glyph across property and document · **(c)** the re-render that takes the
  **133-grey line to 0**.

⭐ **A HARD REQUIREMENT OF THE PACKET, adopted by the desk from my flag — do not let this be dropped:**
**(b) changes a card Zach ALREADY SIGNED and never complained about, on the most-rendered family in the
product (24k+ property rows). THE PACKET MUST LEAD WITH IT EXPLICITLY. His signature has to SEE that
change, never INHERIT it.** A re-signature that quietly carries a pixel change he did not notice is
not a signature on that change.

**TIMING (not mine to trigger):** assembly begins after tonight's two open verifies land — the current
deploy verifying green, and the janitor's first Inngest run. Until both, this lane has nothing to do.
Until the packet runs, `document_shared` drawing GREY on the old signed board is **honest** — it is
what was signed — so do NOT "fix" the 133 greys mid-cycle.

---

### 6. OPEN — NOT MINE, NOT STARTED, EACH NEEDS AN OWNER

1. **LEDGER 101 — the All feed drops rows.** Measured: the activity API returns **104 rows** for real
   Conner (`601ade98-…`); the DOM renders **58 cards**, and `document_sent` is among the missing at any
   scroll position — on the tab whose law is ALL = EVERYTHING. Assigned to `allfeed-101`. **Scope line
   Lee set and I agree with: feed COMPOSITION is theirs, card INTERNALS are mine.**
2. **Icon option (b)** — `Share2` into BOTH registries via the generators, wearing Zach's signed GREEN
   share carve-out. Queued to the board-refresh family. ⚠ An icon name absent from either registry
   renders **BLANK** — that is why (a) `Users` shipped instead of a drive-by Share2.
3. **`stage_changed` has three icon answers** — DB `Settings` · `content.mjs:106` `Flag` · mig 0506
   `ArrowRightLeft`. One concept, three sources. For the paint stamper; registry item.
4. **`"zach@houseloop.ai sent an email"`** — the sender renders as a bare ADDRESS on the exact card
   Zach quoted. Different mechanism from ledger 87 (sender-identity resolution): `identityForSender`
   returns null for a real user's own mailbox and `parseAddr` falls back to the address. Cheap fix
   available — resolve the FROM through `resolveRecipientActor`, same as `party_name`. Belongs to the
   attribution lane.
5. **`EmailCard` hand-composes its own sentence** (`EmailsStream.tsx`) instead of reading the catalog —
   a SECOND sentence composer. Lee minted this as **ledger 96**. Also: one email renders as an
   EmailCard in some feed rows and an ActivityCard in others.

---

### 7. THINGS THAT WILL BITE THE NEXT SHIFT (hard-won, not in any doc)

- **`tsc` in this shared tree RACES the auto-save hook.** Three times a run reported failures against a
  file state that no longer existed — including one where my own change looked reverted and was not.
  **Re-run once before acting on a red `tsc`.** I nearly re-fixed correct work.
- **The MCP browser is SHARED and my tab was navigated out from under me five times**, then the server
  wedged entirely (60s nav timeout, then a 1800s idle abort). Open your OWN tab, re-select it before
  every call, and collapse navigate→act→assert into ONE `browser_evaluate`.
- **`/api/email/thread` enforces its own session** — the `x-hl-internal-secret` header does NOT get you
  in (it bypasses the middleware redirect, not route auth). Use the browser's `fetch` with
  `credentials:'same-origin'`. `/api/leads/[id]/activity` does not need it.
- **The desktop CRM has no phone breakpoint.** At 390 the shell does not reflow; `/m` renders no card
  sentences at all (its own renderer). A 390 "receipt" for card work is a geometry assertion, not a shot.

---

### 8. THE REPLY-THREADING STATE (mig 0771), SO NOBODY "FIXES" IT WRONG

Reply detection is LIVE and shipped on a **subject-thread** match requiring a real earlier SENT row.
Mig 0771 added `provider_thread_id` as the EXACT key; the reader **prefers it and keeps the subject
match as the fallback**. That fallback is load-bearing, not a nicety: **every send written before
8/14 is NULL** (no backfill is possible — the ids were discarded), so removing it silently stops reply
detection across the entire existing book. Verified after the migration: sends NULL, inbound carrying
real Gmail thread keys, every reply still detected.

⚠ Correction to the earlier map, so it is not re-scoped: **the inbound writers needed NO change.**
`hl_inbound_emails.gmail_thread_id` has existed since the realtime sync was written and is populated on
**3053 of 3053** rows. Only the SENT side was ever blind.

Receipts: `.claude/reports/card-copy-r2/` (shots + `engagement-options.html`, the A/B/C sheet showing
each treatment on an EMAIL card AND a DOCUMENT card — recommendation **A now, C later**).
Scope lock: `.claude/state/session-scope-r2-card-copy.lock`.
