---
title: "R2-cards (r2-cards LANE — LEDGER 103 COMPLETE (2026-08-14, resurrected lane))"
description: "Work-in-progress dossier — R2-cards. r2-cards LANE — LEDGER 103 COMPLETE (2026-08-14, resurrected lane)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/r2-cards.md` on every push. **Progress: checklist pending.**</Info>

**Owner:** R2-cards

## Checklist

### DONE THIS SHIFT
**Ledger 103 shipped to the tree (unpushed).** Full-payload cards drop the actor circle; compact keeps it.

- `d6d194c862` — the code. `ActivityHeroCard.tsx`: the `home_card` branch and the default hero branch
  no longer render `<ActorAvatar>` (home_card's flex wrapper collapsed back to one column div).
  COMPACT (44px) and INSIGHT (40px) untouched; ArrivalCard untouched. The `avatar` prop stays on the
  model — `avatarFor` in render-activity.ts is unchanged, so compact/insight keep one resolver.
  `cards.mjs`: the board's `homeCard` + `heroCard` drop `avatar(who, 36)` in the same pass.
- `1378292d76` — the board regenerated + RE-PROMOTED (`activity-card-catalog`, sha 5dc60271914c…).
  Diff proof: **30 cards lost their 36px face; zero 44px and zero 40px faces moved; no card gained one.**
- `8aef28b63f` — three walked receipts in `.claude/reports/myli-mvp/shots/ledger103-*.png`.

### GATES
- tree `npx tsc --noEmit` → **exit 0**.
- localhost:3020 walk, my own Chrome (own port 9781 + scratch profile): home_card
  ("Kristen Hansan shared a property") and hero ("requested a tour") render `faces=[]`; the same page's
  14 compact rows still hold their 44px face. Every shot read back before it was cited.
- ⚠ **Conner-TEST carries NO full-payload card** (its kinds are stage/message/document/search only, and
  the search family was demoted to compact by Zach's 8/14 ruling), so the hero + home_card receipts are
  shot on a lead that HAS them. The compact receipt IS Conner-TEST.
- eslint on the file reports 9 `@next/next/no-img-element` "rule definition not found" errors — an
  UNTOUCHED sibling (`EmailsStream.tsx`) reports the same class, so it is the repo's lint invocation,
  pre-existing and unrelated.

### FOUND WHILE REGENERATING (worth knowing, fixed)
`build-catalog.mjs` writes `docs/wireframes/activity-card-catalog.html`, which `dfccd453b6` had deleted
from GIT as a duplicate — but the manifest still names it as the `source`, and `promote.mjs` is the
manifest's ONLY writer. I first "fixed" it by pointing the generator straight at
`apps/houseloop-crm/boards/` — that publishes new bytes under a manifest whose sha256 still vouches for
the old ones. Reverted; the two-step publish is now written in the file's header. **Deleted-from-git ≠
removed-from-the-pipeline.**

### NEXT
- Nothing open on ledger 103. UNPUSHED — Lee sequences the train (the board 404s in prod until the
  boards/ + manifest commit ships).
- Queued at the desk from the previous shift, not mine to close: icon option (b) Share2-via-generators,
  and the `stage_changed` three-icon disagreement.
