---
title: "Benji (Trigger Event Reconciliation — the ONE canonical catalog)"
description: "Work-in-progress dossier — Benji · HouseLoop Website. Trigger Event Reconciliation — the ONE canonical catalog."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/benji/trigger-events.md` on every push. **Progress: 0/6 done (0%).** _Updated 2026-07-01._</Info>

**Owner:** Benji — HouseLoop Website

## Checklist

**North star (Zach-signed 2026-07-01):** ONE canonical DB table `hl_trigger_events` is the single source
of truth for every trigger in the system. Everything derives from it — the Lead Sources page, the System
Events page, the workflow builder palette, the Workflows list — no second list, no patchwork. A "Trigger
Event" = a trigger (starts a workflow) that fires on an event (a lead did something); one word everywhere.

**Why:** today the same trigger info is duplicated across 4 code lists (`WF_TRIGGERS`, `CANONICAL_LEAD_EVENTS`,
`EVENT_TITLES`, `EVENT_TRIGGER`) + 3 DB tables, hand-synced — which is why a trigger added under Lead Sources
/ System Events never flowed into the workflow builder. Researched with 3 sub-agents; full map in
`.claude/checkpoints/workflow-trigger-catalog.md`.

**Model:** each trigger event = `type · category (registration/system/follow_up/other) · lead_source_key ·
label · description · icon`. Registration triggers group by Lead Source (HouseLoop.ai · FUB · Manual · future
Google PPC → its own landing-page trigger events). Every trigger event gets a matching workflow ("Coming Soon"
if unbuilt).

### Build order (Zach-approved: P0→P2+P5 first, then P3/P4)
- [ ] P0 — migration: create `hl_trigger_events` + seed from today's triggers (deduped, fix the 2 naming bugs)
- [ ] P1 — repoint the Lead Sources + System Events pages + the builder palette to READ the table
- [ ] P2 — derive `WF_TRIGGERS` / titles / event→trigger / `DOOR_EVENT_KEYS` from the table (delete the hardcoded
      lists; internal controls stay in code, appended)
- [ ] P5 — generic Inngest door so UI-created trigger events fire without a deploy (fast-follow)
- [ ] P3 — Workflows LIST = every trigger event as a row, "Coming Soon" when unbuilt (second pass)
- [ ] P4 — builder Lead-Source ↔ System-Event toggle from `category` (second pass)

**Gate:** every prior trigger still present · no live workflow orphaned by a rename · the 3 saved-search
trigger events show in the builder (System) + fire · tsc + build green both apps · Zach spot-checks the builder.

**Blocks →** [[saved-search-process]] (can't finalize the saved-search origins until the catalog is canonical).

## PRD

`GT/GT - Pending/GT_HOUSELOOP_TRIGGER_EVENTS_CANONICAL_PRD.md`

> GT — TRIGGER EVENTS: THE ONE CANONICAL CATALOG (PRD) · Owner: Benji · Zach-signed 2026-07-01 (terminology + DB-table decision locked) · Status: PENDING → build · THE LAW · There is **ONE canonical table of Trigger Events: `hl_trigger_events`**. Every trigger in the system is a · row here. **Everything derives from it — no second list, no patchwork.** A "Trigger Event" = a trigger · (it starts a workflow) that fires on an event (a lead did something). One word across the whole app. · WHY (the drift this kills) · Today the same trigger info is duplicated across **4 code lists + 3 DB tables**, hand-synced — which is · why adding a trigger under Lead Sources / System Events did NOT flow into the…

## Scope lock

`.claude/state/session-scope-trigger-catalog.lock`

> SCOPE LOCK — UNIFIED TRIGGER CATALOG (Benji, Zach-signed 2026-07-01) · Mode: BUILD · Tree: /Users/zachspradling/Code/BossLoop-Gold · Branch: houseloop-app · Brainstorm checkpoint: .claude/checkpoints/workflow-trigger-catalog.md (full research + synthesis) · NORTH-STAR (Zach signed) · ONE catalog of triggers (registration events + system events). Everything derives from it — the · workflow-builder trigger palette, the event titles, the Inngest door registration. Add a trigger to · the catalog → i…

## Related documentation

Doc sections this work creates or updates:

- [`guides/workflows`](/guides/workflows)
- [`guides/lead-sources`](/guides/lead-sources)
- [`guides/lead-flow`](/guides/lead-flow)
