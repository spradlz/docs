---
title: "Janitor (janitor LANE HANDOFF — LEDGER 104, THE NIGHT JANITOR)"
description: "Work-in-progress dossier — Janitor. janitor LANE HANDOFF — LEDGER 104, THE NIGHT JANITOR."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/janitor.md` on every push. **Progress: checklist pending.**</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 57h ago. If Janitor has shipped since, this dossier under-reports — refresh `.claude/state/wip/janitor.md`.</Warning>

**Owner:** Janitor

## Checklist

# Written by the janitor lane 2026-08-14 ~22:00Z (supersedes Lee's pre-account-switch backstop).

### MISSION (Zach verbatim)
"a nightly audit of what actually got accomplished — not a rewalk... a dedicated cron agent whose
only job is to be the nightly janitor... wolf of wall street style... gets it ready for the next
morning." Two products: THE NIGHTLY SWEEP (doc/report drift, consolidate + archive, NEVER delete)
and THE MORNING TRAY (what actually got accomplished, from evidence).

### ⚑ STATUS IN ONE LINE
**SHIPPED AND ARMED.** Zach signed the supervised run (`01M016C8449D85RDEARMYNDNGP`, verdict
yellow) — "i sign off on whatever you need dude" — and BOTH halves were armed in the same pass.
**ONE THING IS STILL OWED:** the cron rides the next train, and after that deploy goes green
somebody must issue the PUT sync and CONFIRM the trigger list actually shows the cron (see the
event-id lesson below). Until that is confirmed, the schedule is code, not a schedule.

| half | schedule | armed |
|---|---|---|
| LOCAL sweeps | 03:00 local | ✅ `~/Library/LaunchAgents/com.bossloop.night-janitor.plist` — loaded, and PROVEN by `launchctl start` (sweep → tray → 9 receipts `e512e1a254` → heartbeat → notify, exit 0) |
| CLOUD watchdog | 07:00 CT (`0 12 * * *`) | ⏳ in the code (`06863b045e`), NOT yet synced to Inngest — needs deploy + PUT + verify |

⚠ **NEVER ARM ONE WITHOUT THE OTHER.** The watchdog reads the heartbeat the local job writes.
Arm the watchdog alone and it reds every morning for a reason unrelated to a dirty office.

### WHAT EXISTS (all committed on houseloop-app)
| piece | path |
|---|---|
| the four sweeps | `tools/janitor/sweep.mjs` + `tools/janitor/lib/{ledger,trash,drift,claims,evidence,paths}.mjs` |
| the morning tray | `tools/janitor/tray.mjs` |
| **the heartbeat wire** | `tools/janitor/lib/heartbeat.mjs` (NEW 8/14) |
| **the live-catalog reader** | `tools/janitor/lib/livedb.mjs` (NEW 8/14) |
| the docs | `tools/janitor/README.md` (NEW 8/14 — two TS files cited it and it did not exist) |
| the cloud half | `apps/houseloop-crm/lib/janitor/portable-sweep.ts` |
| the Inngest function | `apps/houseloop-crm/lib/inngest/functions/night-janitor.ts` |
| serve() registration | `apps/houseloop-crm/app/api/inngest/route.ts` (line ~58 + the functions array) |
| **Run Log registry row** | `apps/houseloop-crm/lib/run-log/inngest-registry.ts` → `houseloop-crm-night-janitor` (NEW 8/14) |

### THE SPLIT — READ `tools/janitor/README.md` BEFORE QUESTIONING IT
`apps/houseloop-crm/Dockerfile` does `COPY . .` from the app dir, so the production image has no
`tools/`, no `.git`, no `.claude/`, no `docs-site/`. A deployed function CANNOT run the sweeps.
Local = the sweeps (launchd, not yet armed). Cloud = the WATCHDOG on the local half + LIVE-DB
adjudication of doc table-claims. The one wire between them is `hl_settings.janitor_heartbeat`.

### PROOF ON THE RECORD (2026-08-14, live DB, not memory)
- Local sweep `--apply` + tray `--apply --no-notify` ran clean. Tray:
  `.claude/reports/janitor/tray-2026-08-14.md`; record: `sweep-2026-08-14.json`.
- Receipt commit `9e9390e07d`: 242 files, **all** under `.claude/reports`, zero foreign paths,
  zero receipts left uncommitted.
- Heartbeat row written (`mode: apply`, 30 unadjudicated tokens handed up).
- Cloud half run directly against the live CRM: `stale:false · verdict:yellow · 30 tokens checked`.
- **Watchdog RED path proven** (`staleHours:0` → `stale:true · verdict:red`) — the control can fire.
- `tsc --noEmit` exit 0 · door-zero 742 (floor held) · kind-literal-floor 878 (floor held).

### ⚑ WHAT THE FIRST REAL RUN EXPOSED (all fixed in `3f50ecf28e` — do not re-introduce)
1. **Nothing wrote the heartbeat.** The cloud watchdog could only ever say "the local janitor has
   never run", forever, no matter how good the night was. A watchdog on a wire nobody connected.
2. **The lock is not an inventory of the database.** It lists BASE TABLES, so every doc citing a
   live VIEW was called a liar — 9 objects, 33 citations. The live catalog now settles it; with no
   live read the accusations are filed UNCONFIRMED and excluded from the total.
3. **`total` came from display-capped arrays** — a saturated pile read as an exact 300 when the
   truth was 325. A ceiling wearing an exact number's clothes.
4. **`git add -- path` then a BARE `git commit` publishes the WHOLE index.** It swept two files
   another lane had just staged into a `[janitor]` commit that mislabelled them (`9a1aa38079` —
   left on the branch on purpose; never rewrite shared history). The pathspec must ride the COMMIT.
5. **git porcelain quoting.** 6 screenshots containing U+202F came back octal-escaped, matched no
   file, and an unmatched pathspec aborts the WHOLE commit — 241 receipts died with them **while
   the sweep printed "committed 241"**. `-z` everywhere; a commit is now reported only if HEAD
   actually advanced.

### ⚑ THE ONE REMAINING STEP — THE 07:00 CT FIRE
Deploy `03df6345` (train `9d9a16173a`, carrying the cron) is SUCCESS, and the PUT sync was issued
(HTTP 200). **What is NOT yet proven is that Inngest stored the cron** — see the correction below.
**The proof is tomorrow's 07:00 CT run appearing with NOBODY dispatching it.** Whoever is up:
check `hl_run_log` for an `fn='night-janitor'` row around 12:00 UTC. If it is absent, the schedule
did not register and the function needs a dashboard-side look.

### ⚠ CORRECTION TO MY OWN LESSON — `modified: true` PROVES NOTHING
I told the desk that `modified: true` from the PUT was evidence the function list had CHANGED.
**That was wrong.** Three consecutive PUTs, seconds apart, no deploy between them, ALL returned
`modified: true`. It is emitted on every successful registration and cannot distinguish "was
absent, now added" from "nothing changed." Corrected in the memory + the README before it reached
the law. The ORIGINAL lesson survives intact and is still proven — an event id is not proof a
function ran, and the PUT re-registration genuinely fixed the dead dispatch. Only my evidentiary
reading of `modified` was false. **The only proof a schedule is live is the schedule firing.**

### ⭐⚠ THE LESSON THIS LANE PAID FOR — AN EVENT ID IS NOT PROOF A FUNCTION RAN
The first dispatch after a GREEN Railway deploy was ACCEPTED by Inngest — a real event id came
back — and **nothing ran, for two minutes, with no error anywhere.** The function had never been
registered. I ruled out the cheap explanations first (capture-fn was alive and logging other
functions; the code was confirmed on `origin` including the `serve()` entry), which left a
registration gap. The PUT returned `modified: true` — proof the function list CHANGED, i.e. it was
genuinely absent until that moment. Re-dispatch produced a run in under 5 seconds.
**The event key accepts events for functions that do not exist and hands back a cheerful id.**
Confirm THE RUN, never the dispatch. Memory: `feedback_an_event_id_is_not_proof_a_function_ran`.
The desk is amending INNGEST_OR_IT_DOESNT_EXIST with the PUT-sync requirement.

### HARD RAILS (constitutional, unchanged)
- **NEVER DELETES.** Archive or commit only — both reversible, both leave the bytes on disk.
- **NEVER MESSAGES A PERSON.** Only outbound is `tools/notify/notify.sh` (Zach's operator channel).
  No code path to a lead, ever. (The first supervised run used `--no-notify` deliberately: an
  unsigned first tray should not ping Zach at 10pm.)
- RULE #0: the sweeps issue NO SQL; the single settings upsert is the only write in the system.
- Commit BY PATHSPEC with a `[janitor]` tag — never `git add -A`, never a bare commit after adding.
