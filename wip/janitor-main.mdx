---
title: "Janitor (janitor LANE HANDOFF — LEE-WRITTEN BACKSTOP (2026-08-14 ~21:50Z, pre-account-switch))"
description: "Work-in-progress dossier — Janitor. janitor LANE HANDOFF — LEE-WRITTEN BACKSTOP (2026-08-14 ~21:50Z, pre-account-switch)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/janitor.md` on every push. **Progress: checklist pending.**</Info>

**Owner:** Janitor

## Checklist

# (If janitor wrote its own version, that one wins — this is the desk's map of the lane.)

### MISSION — LEDGER 104 (Zach verbatim, the order that spawned the lane)
"a nightly audit of what actually got accomplished — not a rewalk... a dedicated cron agent whose
only job is to be the nightly janitor... wolf of wall street style... gets it ready for the next
morning." Replaces the Opus-era nightly document audit with a better novel solution. Two products:
1. THE NIGHTLY SWEEP — doc/report drift cleanup (consolidate, archive, never delete).
2. THE MORNING TRAY — what actually got accomplished yesterday (from the git record + run log +
   Ledger of Everything deltas), ready for Zach at morning coffee.

### HARD RAILS (restated because they are constitutional)
- THE JANITOR NEVER DELETES. Archive only (git mv to an archive dir / status flips). A janitor
  that deletes is how the record dies.
- INNGEST OR IT DOESN'T EXIST: the nightly run MUST be a registered Inngest function visible in the
  dashboard + Run Log (lib/inngest/functions/, registered in app/api/inngest/route.ts serve(),
  categorized in lib/run-log/inngest-registry.ts). A bash cron or a launchd job does NOT count.
- No auto-schedule before Zach signs a first supervised run (the 7-step Inngest loop).
- DOCS_SECTION_DISCIPLINE governs any doc moves: retire = merge + git rm + redirect, never orphan.

### STATE AT SWITCH
- Lane was STILL DESIGNING/BUILDING when the session ended. Registration state UNKNOWN — assume
  NOT registered until proven in the Inngest dashboard.
- First sweep + morning tray were due "tonight" — that promise does NOT survive unless the next
  shift confirms the Inngest function exists, is synced, and a supervised first run happened.

### NEXT SHIFT
1. Grep for the lane's files: `git log --oneline -15 --all -- '*janitor*'` + ls
   apps/houseloop-crm/lib/inngest/functions/ for anything new.
2. If nothing registered: the design brief above IS the spec — build the function, register it,
   run once via the dashboard Invoke, show Zach, THEN set the cron.
3. The morning tray's data sources: git log since last tray, the qa-gauntlet run log new ledgers,
   tools/master-plan/registry.json proof-bit deltas, DEPLOY_LOG.md.
