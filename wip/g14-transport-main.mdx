---
title: "G14-transport (g14-transport — LANE HELD WARM (2026-08-14, desk-gated))"
description: "Work-in-progress dossier — G14-transport. g14-transport — LANE HELD WARM (2026-08-14, desk-gated)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/g14-transport.md` on every push. **Progress: checklist pending.**</Info>

**Owner:** G14-transport

## Checklist

&gt; ## ⭐ UPDATE 22:33Z — HANDSETS ARRIVED, RUN DONE, NEW BLOCKER. READ `v2/RECEIPT-2-HANDSET-RUN.md`.
&gt; Zach designated **+14074034088** (his personal) + **+19132081633** (Myli's line). The v2 send WORKED
&gt; and the **binding shape is proven live — zero proxy addresses anywhere**. But:
&gt; - **The group did NOT form.** Twilio sent two separate 1-segment **SMS** (`media=0`,
&gt;   `msgSvc=(none)`), not one group MMS. Myli's iPhone: **0 group chats**, our long code appears only
&gt;   as `participants(1)` 1:1. Not FUB parity.
&gt; - **⛔ THE NO-GO TEST WAS NOT RUN, DELIBERATELY.** `Conversations ADDRESS CONFIGURATIONS: NONE` —
&gt;   nothing on this account can route inbound INTO a conversation at all, so the test would PASS
&gt;   VACUOUSLY (private stays private because nothing can reach a conversation, not because the
&gt;   binding is safe). **Do not "just run it" to get a green.** The same config that makes the trap
&gt;   impossible makes the group impossible — one fact, not two.
&gt; - **TWO CONFIG GAPS, desk/Zach call, NOT a lane action:** (1) no Address Configuration for
&gt;   +19132861761; (2) the Conversations default messaging service `MGd7a0b2…` is EMPTY while our long
&gt;   code sits in `MG05ab6…`, so group traffic bypassed the registered service — which is also the
&gt;   **A2P answer to open question 1, and it's bad news**. Pricing still `price=pending`.
&gt; - **Waiting on:** Zach answering *"does your phone show a group thread with Myli's number, or a
&gt;   plain 1:1?"* (5 seconds, most decisive fact left), then the config ruling.
&gt; - **GAP-2 PROPOSAL WRITTEN + HELD:** `v2/CONFIG-PROPOSAL-messaging-service.md`. A2P campaign
&gt;   `CA7VR08` is **VERIFIED on MG05ab6…** (which holds all 12 numbers); `MGd7a0b2…` is an empty
&gt;   unregistered shell. ⛔ Do NOT "just move the number" — a number lives in exactly ONE messaging
&gt;   service, so that would drop the agent line out of the verified campaign. **Recommended = pin the
&gt;   conversation** (`MessagingServiceSid` on create — code, in-lane, no account change), proven
&gt;   available no-send (201, echoed back, both v2 bindings still attach). ⚠ NOT proven this is why the
&gt;   group failed — gap 1 may own it; the two are unseparated, and the confirming test's pass/fail
&gt;   discriminator is written down IN ADVANCE in the proposal.
&gt; - Near-miss logged as **task #60** (purge +14074038088 from pa-test-harness — live Verizon handset).
&gt; - Live artifacts: conversation `CH0da1da2689124e23bd6958bc7147f1f0`, CRM thread `dacb1e77-…`,
&gt;   fixture leads Zach Handset-TEST `67de3518-…` / Myli Handset-TEST `bf896a65-…`. Cage allowlist now
&gt;   the two handsets, **`mode` still `test`**.

**STATUS: build COMPLETE + desk-ratified. The no-go test is UNRUN — now blocked on Twilio ACCOUNT
CONFIG, no longer on handsets.**

Receipt: `.claude/reports/g14-group-text/v2/RECEIPT.md`
Spec: `.claude/reports/g14-group-text/PRD-group-transport-v2.md` · `.claude/rules/SEAT_MODEL_LAW.md`
Ledgers 106 / 109 / 113 in `.claude/skills/qa-gauntlet/runs/myli-mvp-1-fix-round-1.md`.

### WHEN THE HANDSETS LAND — RESUME HERE, SAME SESSION
1. Point the rollout allowlist at the designated handsets (`hl_settings` key `group_sms_rollout`;
   ABSENT row = test mode, which is the fail-closed default — do NOT flip `mode` to `live`).
2. v2 group send → both handsets show ONE group thread.
3. Reply from one handset → rebroadcasts natively to the other.
4. ⭐ THE NO-GO TEST, both directions: a 1:1 from a handset to the long code while the group is
   ACTIVE must arrive PRIVATE (plain sms webhook, only the 1:1 thread). The guard now LOGS instead
   of bailing — grep prod logs for `[g14/no-go]`. A logged line on a private 1:1 is EXPECTED; the
   same body ALSO landing as a group reply means the v1 double-fire is back.
5. Per-participant delivery receipts + the A2P/pricing evidence off the message records (PRD open
   question 1, still open — it needs a real send).
6. Composer 10-cap walk (➕ dead at 9, copy names the carrier) — sequenced with the above.

⚠ The INBOUND half needs PROD: Twilio webhooks hit `crm.houseloop.ai`. The train carrying this
build pushed on 8/14 — verify the binding change is actually live before trusting an inbound result.

### THE BLOCKER, IN ONE LINE
Both old TEST lines (+19139379359, +19137333704) are Twilio numbers WE OWN. Twilio refuses an owned
number as an Address-only group participant (**error 50407**) — so no group can form on them. The v1
PAIR shape accepted them, which is why the rig worked while the design was wrong. Probes:
`.claude/reports/g14-group-text/v2/04-bind-isolation.log`. Candidate handset already in the estate:
913-208-1633 (Myli's iMessage line, NOT on the owned list). Second is Zach's call.

### SHIPPED (tsc 0 · test:outbound 45/45 · cages green)
- `apps/houseloop-crm/lib/phone/send-conversation.ts` — leads `MessagingBinding.Address` ONLY; our
  line as its own participant (`Identity=agent:<userId>` + `ProjectedAddress`). **Projected
  participant MUST be added BEFORE the leads** (leads first → 50407; not in the PRD, now an
  amendment Caleb carries). Orphan teardown on any post-create failure. v1-thread reuse blocked by a
  JOIN condition (agent seat with a non-null participant SID).
- `apps/houseloop-crm/lib/phone/group-limits.ts` (new) — `MAX_GROUP_LEADS = 9`, dependency-free so
  the wire and the `'use client'` composer share ONE constant.
- `apps/houseloop-crm/components/leads/detail/TextComposer.tsx` — ➕ disables at 9, copy names the
  carrier, not us.
- `apps/houseloop-crm/app/api/phone/webhooks/sms/route.ts` — the group-proxy guard LOGS and flows on
  instead of returning. **Desk-ratified**; standing principle: *a visible double-fire beats a silent
  swallow.*

### DELIBERATELY NOT DONE
Seat writes — `TODO(SEAT_MODEL_LAW)` sits on the N-row write block. Awaiting the joint DDL sitting
(this lane's fold proposal + inbox-truth slice-1). **Do NOT invent an interim seat table.**

### STATE OF THE WIRE RIGHT NOW
Fail-CLOSED and safe: no rollout row → test mode → allowlist = the two owned numbers → every group
send refuses at the Twilio bind. Group texting is inert and loudly broken, never quietly wrong.
Twilio account swept to 0 conversations; both v1 threads closed; no orphans; no DB residue.
