---
title: "Lane-client-dashboard-r4 (HANDOFF — lane-client-dashboard-r4 (Opus build lane, night of 2026-09-03/04))"
description: "Work-in-progress dossier — Lane-client-dashboard-r4. HANDOFF — lane-client-dashboard-r4 (Opus build lane, night of 2026-09-03/04)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/lane-client-dashboard-r4.md` on every push. **Progress: checklist pending.**</Info>

**Owner:** Lane-client-dashboard-r4

## Checklist

**Tree:** `/Users/zachspradling/Code/BossLoop-Gold` · branch `houseloop-app` · **NOTHING PUSHED**
(freeze #256.58). Every gate below was re-run at handoff, not quoted from earlier in the night.

### THE COMMITS, in order

| commit | what |
|---|---|
| `91da31e795` | **F2** — the dashboard transcript hydrates from the ONE story thread |
| `cea426783c` | **J3** — the client dashboard's own flow-judge journey |
| `970a6eb85c` | J3 + #256.70 — the new opener as three declared assertions |
| `9b83b23834` | **#256.70 (1/2)** — her opening turn is a CONTRACT TURN ZERO |
| `36e2c4b689` | #256.70 (2/2) — the walk, and the two defects it found in my own build |
| `2eaa4bfec5` | **#256.70 (3/3)** — the chat turns run the contract and file as their own room |
| `a4b60f2ff9` | ruling 2 — the 18 written composers die on the client road (18 → 0) |
| `ce4267b26c` | rulings 1 + 2b + 4 — the escape every turn, MEMORY_NEXT room-aware, one story read |
| `bc2467ac87` · `883c4606e3` | the receipts (§7, §8) |
| `67f31a2ecc` | **the owed walk** — the escape on a real screen, and the two defects it found |

**The receipts:** `.claude/reports/client-dashboard/revival/F2-AND-THE-DASHBOARD-JOURNEY.md` and
`THE-DASHBOARD-CUTOVER-256.70.md` (§0 the ruling, §5 the cutover + the consequence proof, §7 the
escape/room/one-read, §8 the three decisions).

### GATES AT HANDOFF (all re-run)
`npm run test:dashboard-thread` **60/60** · `npm run test:myli-state` **364/364** ·
`npx tsx … lib/ai/website-chat-turns.test.ts --no-model` **25/25** ·
`npx tsc --noEmit` green in BOTH apps · `node tools/shared-ui/sync-client-dashboard.mjs --check`
green · `.env.local` restored to its committed values · every bench PROOF line released.

### ✅ NOTHING IS OWED — the last drive is done (`67f31a2ecc`)
The escape was walked on a real phone once `lane-sold-comps-cma` released the bench, and it found
TWO more defects in this lane's own build, both in the half a suite cannot see:
1. **the client threw away the pills the server sent** — her turn's row was gated on the OPENER's
   retirement rule, which is true the moment the client types, so the offers were retired by the act
   that produced them;
2. **the escape was visible and untappable** — the bounded log put the flex column under shrink
   pressure and the row collapsed to ZERO height while its 44px button overflowed under the
   composer. `elementFromPoint` at the pill's own centre returned `mcInput`. **No rect check finds
   this**; only asking what is ON TOP does.
Both fixed and re-walked: `onTop mpill · hits TRUE`, tap sends nothing, composer focused.

### WHAT THE NEXT LANE SHOULD KNOW BEFORE TOUCHING THIS SURFACE

1. **The dev config cannot walk this work.** `HOUSELOOP_CRM_BASE_URL` points at PROD CRM, which has
   neither M1's thread nor F40's intake under the freeze. Both walks needed `HOUSELOOP_CRM_BASE_URL`
   AND `HL_CRM_BASE_URL` pointed at `http://localhost:3020`, and `.env.local` restored after. The
   first attempt proved it by the intake's own `404`, logged loudly rather than swallowed.
2. **Facts arrive when M3 runs, not per turn.** The web-composed dashboard road files turns; the
   memory comes from `myli-memory-pipeline`'s cursor pass, which is EVENT-ONLY until Zach watches a
   run. Say that plainly rather than implying per-turn extraction.
3. **Three findings are FILED, not fixed** (all in the receipts): the dashboard room's home-scoped
   pill labels are home-blind (two homes → two identical "Tell me more"; `registry.ts` is the CMA
   lane's), the suppressed-chat card keeps the one written line by design (§8a), and the sitting
   clock spans both rooms by decision (§8b).
4. **The `::text` lesson (§8c).** A naive timestamp read through a client that assumes local time is
   a four-hour lie in either direction. I filed a non-bug off one; read it `::text` before believing
   a clock.
5. **Two files in this area belong to other lanes right now:** `lib/search/agent-voice.ts` and
   `lib/myli/contract/prompt.ts` carried uncommitted WIP all night (the `agent-voice.ts` one is a
   real `tsc` error in the working tree — not mine, named in every receipt), and
   `lib/dashboard/sold-nearby.ts` is the CMA lane's, which I cleared them to edit.
