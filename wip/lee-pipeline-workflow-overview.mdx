---
title: "Lee (Pipeline and Workflow Master Overview)"
description: "Work-in-progress dossier — Lee · Infra · Pipeline & Workflow · FUB · Notifications. Pipeline and Workflow Master Overview."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/lee/pipeline-workflow-overview.md` on every push. **Progress: 9/21 done (43%).** _Updated 2026-07-09._</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 55 days ago. If Lee has shipped since, this dossier under-reports — refresh `.claude/state/wip/lee/pipeline-workflow-overview.md`.</Warning>

**Owner:** Lee — Infra · Pipeline & Workflow · FUB · Notifications

## Checklist

**⭐ 2026-07-09 — WORKSTREAM ABSORBED into the reorganized docs.** The old canonical
`guides/pipeline-workflow-overview` was DELETED in the doc consolidation (redirects to /myli-master); the living
story is now the ONE "Pipeline & Workflow" nav group ordered **Lead Integration → Trigger Events → Workflows**
(`guides/web-registration-and-intake` → `guides/intent-workflows` → `guides/workflows`), with the FUB side in
**`fub/trigger-events` ("FUB Trigger Events & Notifications", the 22-event master)** and notifications in
`platform/message-catalog`. The checklist below is the historical build record of the reorg that shipped.

**North star (as-built):** HouseLoop's lead engine to MVP. ONE platform (HouseLoop, never named); everything is a
Lead Source with events; `Workflow = f(source × event)` for the registration family, one intent canvas each.
HouseLoop is the first-class notifier; Realloop retired.

### 🆕 ACTIVE WORKSTREAM (2026-07-03) — Lead-Detail Parity Lanes + Polymorphic Assignment
**PRD/SSOT:** `GT/GT - ACTIVE/GT_HOUSELOOP_LEAD_DETAIL_PARITY_AND_ASSIGNMENT_PRD.md` · **scope:** `session-scope-parity-lanes.lock` · **handoff:** `.claude/state/lee-handoff.md` · **prod baseline:** `98650ba504`
Both lead-detail lanes (LEFT = human + AI clone · RIGHT = Myli) share ONE skeleton: `📋 Tasks & Appointments`
(ALWAYS on) + `🤖 AI Workflow` (gated). Grouped polymorphic `Assign to` (Agents · AI Agents · TCs). Clone
task-level override (clone-assigned task fires without global AI mode).

**SHIPPED (pushed, deploying):** Phase 0 docs/PRD (3 new + fixes) · Phase 2 mig 0306 (clone actor_type) ·
Phase 3 grouped assignee picker · Phase 4 parity lanes (fixed the `aiActive` gate that hid human
tasks/appointments — T&A now always-on both lanes; per-lane scoping + faint/mirror; bold toggle) · v2 Tags
under T&A · Myli 4-row Contact. Awaiting Zach's final live sign-off.

**🎯 THE GOAL — NEXT: Phase 5 (Zach-directed 2026-07-03), build end-to-end THEN test.**
(1) **Clone config UI** — surface `POST /api/team/clone` + `hl_member_role` persona/tools override in Team
settings so a member creates + configures their AI Clone. (2) **Task-level override DISPATCH** — a task
assigned to a clone actually FIRES the clone (eligible types only) without the global AI toggle, via
`hl_scheduled_actions.actor_type='clone'`, clone acting AS its human. (3) Model-row real value + Myli
iMessage-reassign → Team settings. (4) Live end-to-end test → Zach sign-off.

### 🧪 SIGN-OFF MATRIX — every webhook, top to bottom (Zach + Lee, iterate live)
**How we run it:** go row by row. Lee hands the **test-lead payload** for `localhost:3020`, Zach fires it, we **log
every gate**. A row **signs off (✅)** only when: (1) the webhook lands + the lead/activity card renders correctly,
(2) Creates-Lead behaves as set, (3) it's **wired to the CORRECT notification** (notify_class / notify_owner — this is
also checking **Benji's** notification work), and (4) its workflow (if any) fires. `⬜` = not yet verified.
_Source of truth: `hl_fub_webhook_settings` (FUB) · `hl_trigger_events` (HouseLoop, notify_class) — pulled 2026-07-02._

### A. Follow Up Boss webhooks (`hl_fub_webhook_settings`)  — all `notify_owner=realloop`, no workflow bound yet
| # | Webhook | Creates Lead | Notify → | Workflow | Result |
|---|---|---|---|---|---|
| A1 | `property_inquiry` | ✅ | realloop | — | ⬜ |
| A2 | `tour_request` | ✅ | realloop | — | ⬜ |
| A3 | `contact_request` | ✅ | realloop | — | ⬜ |
| A4 | `rental_inquiry` | ✅ | realloop | — | ⬜ |
| A5 | `seller_inquiry` | ✅ | realloop | — | ⬜ |
| A6 | `virtually_toured` | ✗ | realloop | — | ⬜ |
| A7 | `text_message` | ✗ | realloop | — | ⬜ |
| A8 | `in_app_message` | ✗ | off | — | ⬜ |
| A9 | `notification` | ✗ | off | — | ⬜ |
| A10 | `email_received` | ✗ | off | — | ⬜ |
| A11 | `property_viewed` | ✗ (counted signal) | off | — | ⬜ |
| A12 | `property_saved` | ✗ | off | — | ⬜ |
| A13 | `property_shared` | ✗ | off | — | ⬜ |
| A14 | `insight_high_intent_buyer` | ✗ | off | — | ⬜ |
| A15 | `insight_high_intent_seller` | ✗ | off | — | ⬜ |
| A16 | `insight_viewed_10x_7d` | ✗ | off | — | ⬜ |
| A17 | `insight_new_area` | ✗ | off | — | ⬜ |
| — | `_global` (master switch) | n/a | n/a | — | n/a |

### B. Phone-call webhooks (FUB → the Phone Numbers section)
| # | Webhook | Creates Lead | Notify → | Workflow | Result |
|---|---|---|---|---|---|
| B1 | `phone_call` (generic) | ✗ | realloop | — | ⬜ |
| B2 | `phone_call_company` | ✗ | realloop | — | ⬜ |
| B3 | `phone_call_sign` (sign/listing line) | ✗ | realloop | — | ⬜ |
| B4 | `phone_call_past_agent` | ✗ | realloop | — | ⬜ |
| B5 | `phone_call_ana` (Ana's old cell) | ✗ | realloop | — | ⬜ |

### C. HouseLoop webhooks (`hl_trigger_events`, platform `houseloop`)
| # | Webhook / event | Creates Lead | Notify class | Workflow | Result |
|---|---|---|---|---|---|
| C1 | New Lead — General Registration | ✅ | `lead_created` | ✅ | ⬜ |
| C2 | New Lead — Liked Home | ✅ | `home_liked` | ✅ | ⬜ |
| C3 | New Lead — Saved Home | ✅ | `home_saved` | ✅ | ⬜ |
| C4 | New Lead — Saved Search | ✅ | `saved_search` | ✅ | ⬜ |
| C5 | New Lead — Tour Request | ✅ | `tour_request` | ✅ | ⬜ |
| C6 | New Lead — Contact Request | ✅ | `contact_request` | ✅ | ⬜ |
| C7 | New Lead — Seller Get Report | ✅ | `home_value_report` | ✅ | ⬜ |
| C8 | New Lead — Seller Get Started | ✅ | `seller_request` | ✅ | ⬜ |
| C9 | New Lead — Shared Home | ✅ | `home_shared` | ✅ | ⬜ (now in `hl_trigger_events`, mig 0287) |
| C10 | System — Property Viewed | ✗ (counted signal) | `property_viewed` | — | ⬜ |
| C11 | System — Property Liked | ✗ | `home_liked` | — | ⬜ |
| C12 | System — Property Saved | ✗ | `home_saved` | — | ⬜ |
| C13 | System — Property Shared | ✗ | `home_shared` | — | ⬜ |
| C14 | System — Saved Search (client) | ✗ | `saved_search` | — | ⬜ |
| C15 | System — Saved Search · Agent | ✗ | `saved_search` | — | ⬜ |
| C16 | System — Saved Search · System | ✗ | `saved_search` | — | ⬜ |
| C17 | System — Tour Request (existing lead) | ✗ | `tour_request` | — | ⬜ |
| C18 | System — Contact Request (existing lead) | ✗ | `contact_request` | — | ⬜ |
| C19 | System — Seller Get Report (existing lead) | ✗ | `seller_request` | — | ⬜ |

**Gaps flagged from the pull (2026-07-02):** (1) C9 Shared Home missing from `hl_trigger_events`. (2) Every FUB row is
still `notify_owner=realloop` — the HouseLoop-notifier migration (per-row flip) hasn't started; each becomes a matrix
sign-off as it moves to `houseloop`. (3) No FUB row has a `workflow_id` bound yet.

---

### SHIPPED (Phase 0 — foundation)
- [x] Lead Sources = ONE page (`/settings/lead-platform`): source block = header + KPI funnel + events + Configure
- [x] Collapsed Registration Events + Lead Sources + FUB Settings into the one page
- [x] Workflows grouped BY lead source; `Workflow = f(source × event)` (reversed the old "agnostic" model)
- [x] Dynamic Back → "Back to Lead Sources"; canonical `HlBackLink`
- [x] FUB webhook config (per-type toggles: Process / Creates-Lead / Notify / Notify-Via) + 17-event matrix (migs 0274/0275)
- [x] Phone Numbers registry `hl_phone_numbers` (41 FUB lines) + `/settings/phone-numbers`
- [x] Notifications = Everything Phase 1 (catalog-derived notification list from the same event tree)
- [x] R2/R3/R4 shipped (mig 0270 `notify_owner`): HouseLoop native notifier + notify-toggle repoint + per-event Realloop kill-switch

### ARCHITECTURE RAILS (must be green before matrix rows pass)
- [ ] R1 Native FUB ingestion (Realloop mirrors `fub.*` data only, stops being the notifier)
- [ ] R8 Webhook test harness (fire/replay a real FUB webhook + verify every gate)
- [ ] R9 Native dialed-line preserved (Ana 913-954-7123 — never the forward target)

### OPEN BUILDS (the keystones)
- [ ] 1d. FUB SUB-SOURCE dimension — split FUB events by portal so `(FUB × sub-source × event)` binds its own workflow (the keystone for source-specific FUB copy)
- [ ] 1e. Email received (`emailsCreated`) — settings row + native HL notify + matrix row
- [ ] 2. A workflow (text + email + nurture) for EVERY event (add missing text steps; DRAFT→ACTIVE the existing-lead doors)
- [ ] 3. Zillow-inbound render MVP (Zillow logo, "Find on HouseLoop", iMessage-bubble restyle)
- [ ] 4. FUB Phone Call Activity engine — ONE `(FUB · Phone Call)` workflow, Myli reads the dialed line → category → copy; text-back from BlueBubbles 913-208-1633; Ana email bridge; then the Twilio port
- [ ] 5. Past-client reconciliation (single canonical table → listing page → seller report → Ana campaign)
- [ ] 6. Go-live — quarantine legacy FUB Zillow doors, E2E-verify each real inquiry in the Run Log

### SIGN-OFF MATRIX — recent edits
- [x] **Viewed home** added to the HOUSELOOP → Existing-lead matrix as a **COUNTED signal** (Zach 2026-07-01):
  no workflow — increments the lead's Visits/Properties counter + "viewed N×"; can gate a workflow with a count
  condition. Backed by `hl_trigger_events.property_viewed` (system, houseloop, mig 0276).

### NEXT CONCRETE ACTION (Lee)
- [ ] KEEP THE OVERVIEW DOC CURRENT — reconcile it against today's changes: Benji's saved-search 3-trigger split (saved_search=client · saved_search_agent · system) is not reflected in the HouseLoop registration/existing-lead matrix; update it.
- [ ] Then resume the Sign-Off Matrix one row at a time (Zach coming back with direction on lead sources + event triggers).

---

## PRD · `GT/GT - ACTIVE/GT_HOUSELOOP_LEAD_PLATFORM_PRD.md`

#### GT — HOUSELOOP LEAD PLATFORM PRD (Zach + Lee, 2026-06-26) — ✅ SIGNED, BUILD

**Owner:** Lee (end-to-end). **Status:** model locked + signed by Zach; building now.
**Design SSOT:** `.claude/checkpoints/lead-system-MAP-2026-06-26.md`. **Scope lock:**
`.claude/state/session-scope-platform-source.lock`. Supersedes the platform/source UI direction in
`GT_HOUSELOOP_LEAD_FLOW_UI_PRD.md §12` (that doc's attribution columns ship as-is; the NAV/screens here win).

---

##### 1. THE PROBLEM
HouseLoop is the website AND the CRM in one product — and Follow Up Boss is a legacy CRM piping in leads with
history. "Lead source" alone can't express that. We spent a day circling because two things were tangled:
**where a lead came from** vs **what it does on our site**. This PRD untangles it.

##### 2. WHAT THE RESEARCH SAID (5 CRMs — HubSpot, Salesforce, UTM/GA4, FUB/kvCORE/Sierra, Pipedrive/GHL/AC)
Unanimous findings (full briefs in session history):
- **Two fields, never one:** a TRUE ORIGIN (Zillow) kept separate from the CONNECTOR (Follow Up Boss). Origin
  is first-touch + immutable. (HubSpot Original vs Record Source; Salesforce LeadSource vs Source System.)
- **Events ≠ source.** Activity is a separate growing stream; source is a stamp set once. (We already do this:
  `hl_lead_activity`.)
- **Trigger on the EVENT, filter on SOURCE/PLATFORM.** Never one workflow per source. Merge fields fill the
  words. (Every CRM. Sierra/GHL/AC explicit.)
- **No "type" field** for origin — "type" = the event (already our catalog) or the pipeline role (already our
  Buyer/Seller pipelines). Don't add it.
- **Don't prefix `fub-zillow.com`** — keep source = the real origin (Zillow), connector separate (live FUB data
  with 48k people confirmed this). Platform = the connector/world.

##### 3. THE MODEL (LOCKED)

###### 3.1 The keystone — TWO buckets, TWO routing keys
| Bucket | Keyed on | Meaning |
|---|---|---|
| **New Lead Registration** | the lead's **ORIGIN** (source + platform) | "how they came in." realtor.com/Zillow via Follow Up Boss → that source's registration (the reactivation). Origin is **set once, permanent** — a merge field forever, but stops STEERING after registration. |
| **(HouseLoop) System Events** | the **SURFACE** (where the action fired) | "what they do on HouseLoop." ANY lead — regardless of origin — who likes a home / requests a tour on HouseLoop.ai fires the **HouseLoop** workflow. |
| **Follow-Up** | schedule | the nurtures |

**Proof (Zach's, universal):** lead acquired platform=Follow Up Boss, source=Zillow.com → registration runs
**Reactivation** ("you worked with Alicia… homes on HouseLoop.ai"). Origin stays Zillow forever. They then like
a home **on HouseLoop.ai** → SURFACE = HouseLoop → runs the **HouseLoop liked-home workflow**, not the FUB one.

###### 3.2 The cascade (first match wins)
```
A lead's event fires →
  1. SPECIFIC workflow for (this source + this event)?    → run it
  2. else a workflow for (this platform)?                 → run it   (e.g. FUB → Reactivation)
  3. else GENERAL REGISTRATION (the catch-all floor)      → always runs ({{lead_source}} fills the words)
```
Already engine-true: `dispatchDoor` routes existing-lead events by event SURFACE and keeps registration
ORIGIN-scoped (built P3). New sources work instantly off the floor; specials are opt-in.

###### 3.3 Scale = MERGE FIELDS
One event → one workflow → `{{first_name}}` `{{home_address}}` `{{lead_source}}` `{{platform}}` fill the rest.
Never a workflow per source. Adding a source = a new ROW, not a rebuild.

##### 4. NAV (LOCKED) — 3 items (Lead Flow folded away)
```
Pipeline & Workflow
  ├─ Lead Platform     ← master: Platform → Source → registration doors + system events
  ├─ Lead Sources      ← thin list: the 3 sources
  └─ Workflows         ← the categorized workflow library
```

##### 5. LEAD SOURCES — EXACTLY 3 (all else killed)
| Source | Platform | Type | Default Stage |
|---|---|---|---|
| HouseLoop.ai | HouseLoop | web | New Lead |
| Zillow.com | Follow Up Boss | portal (fix from `manual`) | New Lead |
| Manual entry | HouseLoop | manual | New Lead |
KILL (deactivate, reversible): Referral · SRG-Follow Up Boss (`fub_import`) · `__platform_houseloop__` ·
`__platform_fub__` · IDX Sign-up. **Build-order care:** `fub_import` holds 6 FUB leads + their reg doors →
REASSIGN leads + doors onto Zillow.com FIRST, then retire it. Nothing lost.

##### 6. EVENT NAMING — the "New Lead –" prefix IS the registration-vs-system tell
- **Registration doors** (origin): prefixed **"New Lead – …"**. HouseLoop.ai = 8: New Lead – General
  Registration · Liked Home · Saved Home · Saved Search · Tour Request · Contact Request · Seller Get Report ·
  Seller Get Started. (3 need relabel today: liked_home/saved_home/general_registration; other 5 already correct.)
  Zillow.com = 2: New Lead – Tour Request · New Lead – Contact Request (= "property inquiry"). (Move from
  fub_import; rename "FUB Property Tour"→"New Lead – Tour Request"; drop FUB's general/seller/saved-search regs.)
- **System events** (surface): UNPREFIXED: Tour Request · Contact Request · Saved Search · Seller Get Report ·
  Seller Get Started · Property Viewed · Property Liked · Property Saved · Property Shared.

##### 7. THE SCREENS (wireframes — final, real names)

###### 7.1 LEAD PLATFORM  → cascades into Workflows (click an event row → its workflow)
```
╔═ HouseLoop ════════════════════════════════════════════ platform ═╗
  ─ SOURCES (origin → New Lead Registration) ──────────────────────
   🌐 HouseLoop.ai — Organic ················· 1 lead · 8 doors
        New Lead – General Registration  → General Welcome
        New Lead – Liked Home            → Liked Home
        New Lead – Saved Home            → General
        New Lead – Saved Search          → General
        New Lead – Tour Request          → General
        New Lead – Contact Request       → General
        New Lead – Seller Get Report     → General
        New Lead – Seller Get Started    → General
  ─ HOUSELOOP SYSTEM EVENTS (surface → regardless of source) ──────
        Tour Request · Contact Request · Saved Search · Seller Get Report ·
        Seller Get Started · Property Viewed · Property Liked · Property Saved · Property Shared
  ─ FOLLOW-UP ──  (none yet)
╔═ Follow Up Boss ═══════════════════════════════════════ platform ═╗
  ─ SOURCES (origin → New Lead Registration) ──────────────────────
   🌐 Zillow.com ····························· 6 leads · 2 doors
        New Lead – Tour Request          → Reactivation Campaign
        New Lead – Contact Request       → Reactivation Campaign
  ─ SYSTEM EVENTS ──  (none — activity happens on HouseLoop)
```
Layout: blue platform header → source sub-section → bucket sub-headers → event rows (Event · Workflow ·
Active). Brand: white-on-blue header. Tables, not chevron trees.

###### 7.2 LEAD SOURCES  → cascades into Lead Platform (click a source → its block)
```
 SOURCE          PLATFORM          TYPE      DEFAULT STAGE   LEADS   REG DOORS
 HouseLoop.ai    HouseLoop         web       New Lead          1        8
 Zillow.com      Follow Up Boss    portal    New Lead          6        2
 Manual entry    HouseLoop         manual    New Lead          0        0
```

###### 7.3 WORKFLOWS  → cascades into the canvas. The automation LIBRARY (grain = one row per WORKFLOW)
Categorized Registration / System / Follow-Up (NOT "Other"). Columns: Workflow · Trigger · Steps · Completed ·
Status (+ Platform · Source as light context). Steps = counted from the canvas; Completed = `hl_workflow_runs`.
```
═ REGISTRATION ═══════════════════════════════════════════════════════════
 WORKFLOW                         TRIGGER                STEPS  COMPLETED  STATUS
 New Lead – General Registration   New Lead – Gen. Reg.    4    138/142    active
 Reactivation Campaign             New Lead – Tour Req.    6      6/7      active
═ SYSTEM ═════════════════════════════════════════════════════════════════
 Liked Home                        Liked Home             3     12/12     active
═ FOLLOW-UP ══════════════════════════════════════════════════════════════
 90-Day Nurture                    schedule               8      4/20     active
```
NOT a clone of Lead Platform: Lead Platform rows = DOORS (by source); Workflows rows = WORKFLOWS (the canvas +
its health). One workflow can serve many doors.

###### 7.4 LEAD DETAIL (already shipped — confirm labels)
`Source: Zillow` + `Platform: Follow Up Boss` in the Contact card.

###### 7.5 WORKFLOW TRIGGER (the one engine add)
Open a workflow → trigger node: `Event [▾] · Platform [▾ any/HouseLoop/FUB] · Source [▾ any/Zillow/…]`.
This is what makes "Registration WHERE platform = Follow Up Boss → Reactivation" buildable.

##### 8. BUILD PHASES (Lee, all reversible/additive where possible)
- **P1 — Data cleanup migration:** reassign FUB leads+doors → Zillow.com; retire junk sources (deactivate);
  fix Zillow type→portal; relabel the 3 HouseLoop doors + the Zillow doors with "New Lead –".
- **P2 — Lead Platform page:** Platform → Source → (New Lead Registration · System Events · Follow-Up), real
  data, tables, blue headers, workflow-per-row. (Replaces the scrapped tree.)
- **P3 — Lead Sources page:** the thin 3-row table (Source · Platform · Type · Default Stage · Leads · Reg Doors).
- **P4 — Workflows page:** categorize Registration/System/Follow-Up + Steps/Completed columns.
- **P5 — Workflow trigger:** Platform + Source filter dropdowns on the trigger node.
- **P6 — Nav:** Lead Platform · Lead Sources · Workflows (remove Lead Flow).
- **P7 — Verify on prod (Playwright) + handoff/docs.**

##### 9. WHAT'S ALREADY BUILT (don't redo)
Lead `platform`/`channel` cols + backfill (mig 0247) · lead-detail Platform/Source rows · dispatchDoor
origin-vs-surface routing (P3) · `hl_lead_activity` event stream · the workflow canvas · the 3-bucket Lead Flow
grouping (becomes the System Events section). The work is the SCREENS + the data cleanup + the trigger filter.

##### 10. NON-GOALS
No `fub-` prefix. No "type" field. No per-source events (merge fields handle words). No Lead Flow nav page.
No inventing sources beyond the real 3. Grows by ROWS.

---

## Scope lock · `.claude/state/lee.lock`

AGENT: Lee
ROLE: Chief of Staff / Systems Architect / signing desk
LAST_UPDATED: 2026-09-02T15:20Z — Fable 5.1 desk, post session-limit restart (handoff: lee-handoff.md 2026-09-01 ~18:00 CT)

##### ONE-LINE STATE
MC wireframe LOCKED (MC-38c..MC-71) · port on the grinder (20 items, MC-B1 building) · home-value-report r2 judge running ·
~190 unpushed commits incl. MC-65 dedupe (Zach ordered live) → push after lanes settle · every seat on claude-fable-5-1.

##### LANES LIVE (2026-09-02 ~15:20Z — Zach: parallel; cap 3)
- grind-mission-control-port-4 (FABLE 5.1) — THE MISSION CONTROL PAGE: the lock as its own page (spec .claude/reports/mission-control-port/MISSION-CONTROL-PAGE-SPEC.md): shell + left band + seat; compact page untouched (diff vs 79f025fc1d must be empty). Owes: the platform-zero fix (WallLeadColumn.tsx import path) before the push clears.
- grind-pill-fix-1 (FABLE 5.1) — PILL FIX 1: one thread + chat history + the anonymous thread persisted server-side by visitor id and attached at login (desk-briefed; receipt to .claude/reports/grinder/pill-flow-engine/&lt;ts&gt;-receipt-fix1.md; the desk files it). First before/after for the machine.
- build-pill-machine (OPUS) — THE PILL MACHINE lane A (PRD .claude/reports/pill-engine/THE-PILL-MACHINE-PRD.md): journeys derived from route-map, the facts column, diff.mjs (IMPROVED / NO DIFF / PARTIAL / REGRESSED), the improve-or-refuse push cage + three shrink-only floors, the fixed-on-prod board writer.
THE NUMBER: /boards/fixed-on-prod = 0/7 (Zach's walk; only Zach ticks). The push is blocked by platform-zero (337 vs 336) until MC's import fix lands.
QUEUED: fix 2 (one number + must-have in the query) → fix 3 (one question at a time) → fix 4 (browse in my range) → fix 5 (dead ends + the double 'building' line) · lane B of the machine (Fable: the rubric judge + goal scripts) after lane A's schema · the compact AI view (seven hard-fact rows) · housekeeping (bench item 4 · Kirsten off 7847 Juniper · retire Zach Handset-TEST · fix sync-creds) · the desk-board generator.

##### THE NIGHT SHIFT (Zach 2026-09-01 ~18:35 CT, ruling #216: "your call tonight you own the night shift") — CAP 2, ALWAYS QUEUED
THE QUEUE (foreman priority: qa-bench &gt; pill-flow-engine &gt; mission-control-port &gt; home-value-report …):
  0. THE BENCH item 1 r2 BUILT 01:31Z (receipt 1788312644-receipt-r2.md: F1 site-phone census + jordan cured via the profile road; F2 env-reserved lines refused in the allocator) → builder RESUMED for the P0 addendum (conner's site phone = Zach's real cell, jasmeet's = Myli's iMessage line → both to their test lines via the same road) → THEN spawn judge-qa-bench r2 (brief qa-bench-judge-r2-1788312651637.md). CARRIED: sync-creds Sheet reader returns malformed rows.
  1. MC-B1 r2 RED 00:34Z (ratchet 1-8 holds both configs; O10 P1 seat editor collapses paragraphs + O11 P2 label alignment) → r3 brief EMITTED mission-control-port-r3-1788309933387.md → spawn grind-mission-control-port in the NEXT FREE SLOT (after CL-1 or the bench lands). SEQUENCE MC-B18 before this band reaches Zach at 1920.
  2. ✅ HVR gamify GREEN r3 (00:05Z, verdict-r3-1788306147417.md) — TICKED. Carried: a 7-line COPY REDLINE PACKET (DRAFT-FOR-ZACH, owner-profile-steps.ts) → CL-2 copy desk covers it; the fold (taste, Zach); D2 'tap an option' over Back-only (P2, next brief on this surface). HVR card set idle; next HVR item's brief emitted (home-value-report-1788307485096.md), served after the pill card.
  3. CL-1 WALKED 01:13Z — COPY-TABLE.md 114 rows / 128 shots / receipt 1788311064-receipt.md; four P0 classes confirmed on prod (typed question hijacked · re-offer · 6 dead ends · memory-next absent + ZERO website facts) → judge-pill-flow-engine RUNNING 01:14Z (grades the walk's coverage, re-drives the four P0s) → GREEN ticks CL-1 → CL-2 copy desk
  4. then CL-2 copy desk → CL-3 retention judge → CL-4a THE MYLI OS MEMORY → CL-4b composer → CL-5..7
  5. Floor item to file when a slot frees: 154 of 2,105 past-client homes have NO value and NO Zestimate (the one-value-law fallback not reaching them)
  6. THE PUSH once lanes settle (≈200 unpushed incl. MC-65 dedupe Zach ordered live): full battery → git pull --rebase && git push → verify MC-65 on prod by name → tell Zach
FOR ZACH'S MORNING (01:22Z): the standing Kirsten fixture is pinned to a REAL past client's rooftop (HLID100236 claim 15 ncseufert, 7/06) — re-pin the fixture or accept; the one-value law is broken on that home (3 numbers); hl_home_claim is a twin table name across the two DBs.
ZACH ONE-WORDERS QUEUED (+06:22Z): beds vs bedrooms (the copy sheet defaults to beds; touches one PE-7 line — his word overrides).
ZACH ONE-WORDERS QUEUED (desk log 00:35Z): Lead Summary value grain · 'Not on record' wording · green Active dot on dispositions · LEAD SUMMARY vs AI Summary naming twin.
#217.3 SIGNED: regular lead detail = 1920×1440 · Mission Control ultrawide = 5120×1440 · nothing between.
RULES: never a third lane · each landing = run foreman, execute its printed order · hygiene cron 37003dbe :23 · sentinel GREEN 23:01Z · desk log every ruling.

##### 2026-09-02 ~09:40 CT — THE TWO LOCKS (ruling #225)
- WIDE MC = docs/wireframes/mission-control-LOCKED-2026-09-01.html (Zach: "this is locked"). r4 = the hybrid → RED by Zach's walk (verdict-r4 on record). r5 brief = STEP 0 render the lock verbatim at the route, report, THEN bind node by node. Lane: grind-mission-control-port-4 (Fable), headless.
- LAPTOP = the compact page as in .claude/reports/mission-control-port/locks/lead-detail-LAPTOP-LOCK-2026-09-02-0923.png (=79f025fc1d) + ONE change: AI Summary = 2 sentences · hard rows (Location · Price range · Timeline · Pre-approved · Last contact · Last website activity as the ACT · Must-haves · Affordability) · Next Best Action · Where they stand. Same block = Myli's summary on the wide view. Lane: designer-lead-detail-compact (Opus) → PRELOCK for Zach's redline.
- Other lanes live: grind-pill-fix-1 (Fable), build-pill-machine (Opus). Push still blocked on platform-zero (WallLeadColumn.tsx:19) until r5 lands.
- ~09:55 CT: THE PILL MACHINE lane A LANDED (desk re-ran selftest 48 arms GREEN + deterministic cage GREEN; the improve gate is LIVE in pre-push: a pill push needs before/after receipts + IMPROVED). Board writer = tools/flow-judge/write-fixed-on-prod.mjs ONLY (scratch generator retired). Ruling #226 (one question at a time) → fix 3 queued in the fix-1 lane. Lane B (Fable rubric judge + goal scripts) spawns after fix 1's after-receipt exists.
- ~10:00 CT: laptop card PRELOCK v1 landed (designer, Opus) — desk read it: 4 redlines sent (2 sentences · "beds" · +Affordability row · paragraph vs rows contradiction $2.4M/$350–450k). Re-opens for Zach after. DEFECT LOGGED (not fixed, out of scope today): the compact page's six unreached pipeline pips render as GREEN rings — no-green law (UI_DESIGN_DOCTRINE); file on the CRM foolproof card when the compact page next opens for edits.
- ~10:10 CT: MC STEP 0 ACCEPTED (the lock renders verbatim at /mission-control; desk read the 5120 shot; quirks ruling (a) standards mode, lock file unchanged). Lane on STEP 1: left band+seat → Myli band (score top-right, hard-fact Key Insights) → middle. Open Zach call: the wireframe's explainer header/"The screen" band on the product page — keep or drop. platform-zero back to 336. THE PUSH now rides with pill fix 1: the fix-1 lane's autosave commits touch pill files, so the improve gate needs fix 1's after-receipt before anything pushes.
- ~10:25 CT: RULING #227 — laptop iPad middle: the lock's menu cluster (MC parity), collapsible 2–3 sentence summary band (chevron expands hard facts + next action + where they stand), the FULL AI summary lives under the "AI" view (infinite scroll: summary · facts by category · memory). No facts column on laptop. Designer lane → PRELOCK v2 (3 frames). v1 card fixes in flight (beds; UMB not Fountain — desk verified in hl_lead_facts). After Zach locks v2 → a Fable build lane against the locked HTML.
- ~10:35 CT: PILL FIX 1 BUILT (one thread + chat history; anonymous snapshots keyed on the visitor cookie; desk re-ran typed-line + thread arms GREEN). Lane now: attach-at-login proof arm → flow-judge after-receipt + diff (the improve gate) → fix 3 (#226). IMPROVED verdict = the desk pushes (boards + Landing Plan ride along). Fixture defect logged: Conner TEST fact says UMB, document says Fountain Mortgage. TODO desk: MEMORY.md trim (20.2KB &gt; 17.1KB hold).
- ~10:50 CT: RULING #228/#228.1 — test leads polluted (measured); LAYOUT FIRST (locks judged on layout, not fixture truth), THEN the bench rebuilds the cast: real seeded test leads + SHADOW leads (cloned real histories, seats swapped to bench lines), scenario leads retired backup-first. Bench item 4's note carries it. Designer told: values are shape, stop reconciling.
- ~10:55 CT: #228.2/#228.3 — shadow lead = snapshot → play forward → RESET (wipe the soul clean); Zach can take the human seat and role-play; that is WEEK 2. This week = infra only (two layouts, pill fixes, the push, brain wiring).
- ~11:00 CT: RULING #229 THE WEEK'S FOUR: MC surface · AI test cockpit surface · pill engine writes the real lead history · Myli wired to read everything. #225–#229 stamped.
- ~11:10 CT: LAPTOP PRELOCK v2 landed (docs/wireframes/lead-detail-compact-PRELOCK-v2-2026-09-02.html, 3 frames) — desk read frames 01+03: the lock's cluster in the iPad, collapsed purple band, AI view = full summary+facts+memory. One desk ruling: the lock's 13-view row (Starred/FUB, no Docs) wins over my #227 paraphrase; designer restoring, then it is Zach's redline. Plan: after his lock → a Fable build lane against the HTML (compact page files change under UI_ITERATION_HTML_FIRST).
- ~11:20 CT: FIX 1 addition built (anon restore by visitor id + attach-at-login proven, row c37ff571 adopted to jordan). Improve gate = REGRESSED, mechanism = the walker counts restored history as this turn's questions (instrument). Spawned pill-machine-lane-b1 (Fable): newest-assistant-beat counter (red-proven) + fix-1 ratchet beats in the journey + re-measure before(prod)/after(localhost) + diff. fix-1 lane → fix 3. Push waits on an IMPROVED verdict.
- ~11:30 CT: MC STEP 1 band (1) accepted (lock's left band + seat bound to Conner/RaShaund, overlay clean, light DOM with prefix-scoped lock CSS). Desk ruling: seat VALUE GRAIN = one line per row w/ ellipsis, band stays 344 (fixture paragraphs are the cast's defect, #228.1). Lane → Myli's band (score top-right, hard-fact Key Insights) → the middle. Laptop v2: 'ppointment' clip → match the live row's sizing.
- ~11:40 CT: RULING #230 (Zach's v2 redline): middle iPad header line gone, pills at the very top, nothing wraps; mode chip + TEST SCENARIO on Myli's band (mirrors the lead's band); AI Agent section removed; Myli lane = Active Triggers · Current Workflow · Current Playbook · Current Action · Next Action. Designer → PRELOCK v3. 'AI view and All view look good.'
- ~12:05 CT: LAPTOP PRELOCK v3 cut (#230 applied: header gone, pills top, Myli mirrors, five sections). #230.1 (one-line actions) + #230.2 (cluster ≈130–140px) landing in the same file next. Then Zach's 'locked' → Fable build lane.
- ~12:15 CT: RULING #231 LOCKED. Laptop lock pin pending the designer's final pass → LOCKED file. Spawning: laptop build (Fable) + bench cast (Opus). MC r5 continues. Pill: lane-b1 counter fix → diff → push.
- ~12:25 CT: ZACH'S TODAY LIST → lanes: MC r5 (Fable, running) · pill fix 3 (Fable) · pill-machine-lane-b1 counter+diff (Fable) · build-laptop-lead-detail (OPUS, waits for the LOCKED file) · bench-cast-newly-registered (OPUS) · website-facts-to-lead (OPUS) · home-value-test-lead (OPUS, jordan on a non-client rooftop through the real road) · designer pinning the LOCKED file. Budget: 60% session at 2h, 20% Fable/week — no new Fable lanes; desk answers notices without tools.
- ~12:30 CT: RULING #232 — four personas only (anonymous · newly-registered · jordan active buyer · kirsten past client on the borrowed real rooftop — reverses #222 re-pin); retire every other is_test lead TONIGHT after lanes land (backup-first, trash road, harness do-not-touch excluded).
- ~12:35 CT: RULING #233 tier loop (Fable designs → Opus builds → Fable critiques → Opus rebuilds). Brain wiring TODAY: spawning a Fable DESIGN lane (spec only) → Opus build.
- ~12:50 CT: LAPTOP LOCKED pinned (1938940328 197717). MC r5 built (all three bands) — grain fix ordered before the Fable judge. Fix 3 built (9/9). Pill diff waits on lane-b1's counter. Designer done.
- ~13:20 CT: RULING #234 — MC explainer chrome removed; board at the top. Builder + judge told.
- 10:26 local: SESSION CAP hit — 4 lanes killed (laptop build, bench cast, lane-b1, home-value); handoff updated with resume points; brain spec landed. Desk switching accounts.
- back after the cap: RULING #235 (brain = person + world: schools/tax/vision/subdivisions/appt ratios). Resuming the 4 idle lanes by message; brain build (Opus) spawned from spec §8; design lane writes §10 (world half).
- post-cap: #232.1 delete ALL test leads (jordan/kirsten too), four fresh personas via real roads (bench lane: Taylor · Morgan Activebuyer-TEST · Riley Pastclient-TEST shadow rooftop · anonymous); retire script executes AFTER MC judge + website-facts finish. Resumed: laptop build, bench, lane-b1 (diff). Stopped: home-value lane (file only). Spawned: build-myli-brain-reader (Opus, spec §8 → §10). Design lane writing §10 (world story).
- MC r5 judge RED (board 5062 vs lock 1802: unbounded middle tracks; dead Look-back; name wrap). r6 brief → grind-mission-control-port-6 (OPUS) + desk addition: zero Jordan-mock text on a bound lead (bands 2–5 + Myli's bottom four real/honest-empty). Retire of old test leads waits on r6's judge + website-facts.
- website-facts GREEN (saved search + Myli funnel → facts through the one writer; converse banked NOTHING before). #233.2 → crm-facts-plumbing (Opus): affordability route+store+flag, source='website', evidence. Website-facts lane idle until the fresh cast lands (then the Taylor walk + #233.1 sentences).
- #236 ONE READER ONE LENS (funnel matrix generated from the ladder; visibility classes; cages). Design lane → §11/§12. Spawned dashboard-claim-value-infra (Opus): one dashboard both halves · claim-road rule (one claim/rooftop, no past-client homes, test band refused) · value persisted at check time via the monthly writer (Zach: 'the value should absolutely be stored'). Taylor born (c60d4a31); bench lane → Morgan + Riley + pixel purge in reset + retire --only proof. Retire full run after MC r6 judge.
- Desk minted Workspace mailboxes test-active-buyer@ / test-past-client@ (creds in .env). Retire EXECUTING now for all test leads except Conner/RaShaund (held for the MC r6 judge); freed lines → Morgan + Riley; Riley's rooftop HLID100795 approved. Laptop port built → judge-laptop-lead-detail (Fable) running.
- ⛔ INCIDENT (desk-caused): sync-creds --push rewrote .env (1220→672 lines, HL_CRM_DATABASE_URL gone) at 10:44; REPAIRED by merge at ~10:52 (546 keys back, newer 10 kept; broken copy in ~/hl-reversals/env-broken-*). Lesson saved to memory. All DB lanes notified. Bench: purge accepted, --only proof, cull (except Conner/RaShaund), lines, Morgan+Riley birth. Website-facts: saved_search_created moves server-side (API creates were feed-invisible).
- Taylor's first session: 3/5 sentences render (saved a search ✓ · viewed ✓ · saved a home = folded pill → its own card ordered · affordability ×2 blocked → route (plumbing) + amount in the sentence w/ harness proof (plumbing) + Save-to-profile control (website-facts) · tour = unfired by design). #233.3 recorded. Zach copy call pending: 'saved a search' vs 'created a saved search'.
- Laptop port judge r1 RED (5 cheap items: AI ISA chip dead picker · hover clip · value weight/pencil red · badge overhang/Transactions pill · memory row). Round 2 → same Opus lane; desk ruling: pencils hover-reveal per the row law. Asked bench to confirm Conner/RaShaund untouched.
- crm-facts-plumbing DONE (#233.2 ×3: affordability route+store, saves on the reveal turn for signed-in; source='website'; evidence). Items 4–5 (amount in the sentence w/ harness proof; property_saved own card) in flight. Website-facts re-scoped: the anonymous register-then-save road. jordan soft-deleted by the cull at 14:50Z (expected). CRM tsc red at mission-control/page.tsx:87 = the MC r6 lane mid-edit.
- MC r6 BUILT: board 1802.3 exact both leads/widths; all 7 judge orders closed; zero mock text; each column its own scroll port. judge-mission-control-port-6 (Fable) running. Named: conversation bubbles = walls of raw listing-polygon URLs (composer defect → pill copy/fix list). tsc red only in myli-memory.ts (brain lane mid-edit).
- Plumbing items 4–5 DONE (card: 'calculated their affordability with Myli: $315K–$405K'; property_saved stands as its own card). #233.4 rulings → items 7–9 (no FUB band on native rows · 'saved a home' · heroPayload meta.property); migrations desk-signed. Brain lane told to re-point 6 feed-pipeline WIRED checks + fix its tsc red. Laptop r2 re-judge running.
- LAPTOP LEAD DETAIL: judge r2 GREEN (all six verified on RaShaund; left lane diff empty). Awaiting: MC r6 judge · pill diff (the push) · cast · plumbing 7–9 · anonymous save road · brain build · dashboard/claim/value infra.
- Plumbing item 6 DONE (saved-search dedupe keyed on the lead + savedSearchId, no window; the costume cage shaped it). ⚠ COSTUME CAGE 197 vs floor 188 — the brain lane's JSON-keyed reads (myli-memory.ts 14 + a second meta-&gt;&gt;'saved_search_id'); told to re-key onto tables, never raise the floor. CRM tsc EXIT 0 app-wide now.
- THE CAST IS BORN: Taylor c60d4a31 · Morgan e65dfa1c · Riley 017e61d3 (real road, verify ×3); 18 retired/backed up; Conner+RaShaund held. Finishing: Riley's pin via the suggestion tap (the one exception), Morgan's search via the funnel. .env '#' quoting fixed by the lane.
- Infra lane DONE (one dashboard both halves · claim guard w/ named refusals · value persisted at check time = 468832 on Conner's fixture home). Rulings F1 one chrome / F4 fail-open real accounts / F5 named gap; orphan claim 23 released. Judge for the merged dashboard = the pill machine's myli-owner journey run (no separate judge).
- BRAIN round 1 (§8) LANDED: readPersonStory/readVisitorStory; activity route → load-lead-feed.ts; converse reads the story (read NOTHING before). #236.1 rulings: target §11+§12; household scope + ended flag; pixel road (meta.property) reaches her mouth; \D fix; A10 stands; sessionId stamping → website lane; costume ≤188 owed.
- Cast final: Taylor re-minted (lead c7358dd5), Morgan e65dfa1c, Riley 017e61d3 PINNED (claim 28 on HLID100795 via POST /api/home/claim; /sell renders the report but writes no claim). Reset purges pixels (proven). Morgan's search + calculator beats → via Myli's funnel/affordability flow (ordered). Live is_test = 6 (3 personas, harness probe, Conner, RaShaund).
- BRAIN round 2: load-production/load-pulse importable (byte-identical routes); readHomeStory + /api/internal/home-story door (13744 Archer Rd: value, schools, 23 tax rows, 141 vision tags, subdivision, geo). §11+§12 next; server-only package to be installed. Costume ≤188 still owed.
- MC r6 judge RED narrowly (one red: Next Best Actions grain). r7 → Opus MC lane (grain + 'N more'). Re-judge (same judge, tiny budget) after r7. Row pickers = MC-B2 next.
- Infra lane: #237 claim half BUILT (unresolved_home refusal; past-client arm crosses the key); hl_value_history re-key PARKED (signed migration, 94-home collapse). 97 listings truly unresolved.
- Plumbing 7–8 DONE (native rows never FUB-banded; 'saved a home' mig 0969); item 9 withdrawn (LEDGER 310 compact rail is signed — Zach's one-word call: rail vs hero); item 10 'viewed a home' (mig 0970) in flight then the lane stops. Bench: birth step-6 idempotency + the bench-reset.lock coordination rule ordered.
- Plumbing lane CLOSED: items 1–8 + 10 delivered (migs 0968/0969/0970 desk-signed), item 9 withdrawn. Costume cage GREEN again (184, floor 184). Left: property_unsaved 'unsaved a property' → 'home' (next catalog pass); hero rail = Zach's word; bench fragility (birth step 6) with the bench lane; feed-pipeline WIRED ×6 with the brain lane.
- Infra lane CLOSED: F1 one chrome (href discriminator, mirrored via sync-client-dashboard) · F4 fail-open real/closed test, red-proven vs a dead port (residual: a test account named like a person reads real during an outage — bounded, stamped 'self-claim-unverified', reversible; cure = the is_test column) · F5 named gap · orphan claim 23 released via a new internal release arm. Lanes still live: MC r7, lane-b1 (diff), bench (Morgan), website-facts (anon road + sessionId), brain (§11/§12).
- PUSH PRE-FLIGHT GREEN: landing 4/4 · platform-zero 336 · costume 184/184 · flow-judge deterministic GREEN · pill-machine selftest GREEN · tsc clean both apps · 391 unpushed commits. Only the pill improve gate (lane-b1 round-3 diff) stands. Status ping sent.
- MC r7 BUILT (NBA rows one line, ratchet red-proven; 'N of M shown · K more' subset lines). Re-judge (same judge, ≤6 shots) running. Parked until the brain is judged green (#235): the composer writes Next Best Actions the width of a row (343-char actions ellipsise today).
- Website-facts DONE: anonymous save road proven (4/5 sentences). Infra lane CLOSED. Pill lane-b1: cast swap killed the AFTER side; re-running both sides as Morgan (~12 min) → diff → the push. CRM dev :3020 degraded (empty responses) — restart AFTER the MC judge lands.
- Bench DONE: Riley pinned via /api/home/address (the one road: resolve·mint·enrol·claim); Morgan's search corrected (1 search, canonical criteria). Three findings logged (no Save pill after a captured search · 32→37 · owner card $765,000 vs $513,856). Conner/RaShaund still held.
- Website-facts lane CLOSED (sessionId already stamped at both writers; the 94% = 8/26 history + cookie-less server callers; 'unauditable null' → STRUCTURE_GATE interview, parked). Live lanes: MC judge r7 · lane-b1 round 4 (Morgan) · brain §11/§12 · bench (step-6 + reset lock).
- DISK: 11→~22 GB free (dead session scratch 9 GB, npm cache, MCP dumps 2 GB). Pending the pill AFTER walk: restart :3001 with a clean .next (apps/houseloop-web/.next/dev = 32 GB). BossLoop-Gold-adam (6 GB) has 43 uncommitted files — not deleted; Zach's call. Downloads 10 GB = Zach's.
- MISSION CONTROL GREEN (r7) — ticked after 7 rounds; MC-B2 (row pickers) brief emitted. Conner/RaShaund retire ordered. :3020 restarted --clean. Push waits on lane-b1 round 4.
- #238: adam worktree REMOVED (backup ~/hl-reversals/BossLoop-Gold-adam-uncommitted-20260902-1210.tar.gz; HEAD 31cfbeb006 on origin/houseloop-app-adam). Home cards by source + 'created a saved search' → Opus lane.
- #239 (Morgan's page): home-card grammar (photo left · address inline · time far right) · dashboard-channel messages render as CHAT not text (measured: 26 dashboard rows, 0 texts) · the person is the actor of a funnel search. All three → home-cards-by-source lane (Opus) with #238.
- Bench hardening DONE (idempotent birth, reset lock). Taylor re-born as 2f133eb2. NEW PILL DEFECT: dashboard chat runs no affordability flow (fix 6 tomorrow). Awaiting the bench's Conner/RaShaund retire tally.
- PILL DIFF r4: NO DIFF (cage RED, surfaced). Mechanism: reachability not scored per target; two real defects (beds tap after Narrow renders nothing; Narrow double-renders). #240: diff.mjs gains per-target reachability (lane-b1); fix lane fixes both; re-walk AFTER; re-diff. BRAIN round 3 LANDED (§11+§12; 38/0; both cages red-proven; 24 commits). CULL COMPLETE: live is_test = 4.
- #233.7: affordability_saved (new kind) + 'calculated … on the HouseLoop website' + 'updated their must-haves on the HouseLoop dashboard' → home-cards lane items 6–7.
- #233.8: catalog signing from ONE census (Fable lane website-act-catalog-census running; board /boards/website-act-catalog). Home-cards lane keeps 1–5, drops 6–7. Pill: fix lane on the two defects; lane-b1 on per-target reachability; then re-walk AFTER → diff → push.
- #241: 'Open in Mission Control' door on the compact page → laptop lane (Opus). Lanes live: pill fix (2 defects) · lane-b1 (reachability scorer) · brain r4 (X9 lens wiring) · home-cards 1–5 · census · desk boards · laptop door.
- 12:24 local: SECOND CAP IMMINENT — handoff rewritten (.claude/state/lee-handoff.md), checkpoint orders sent to all 7 live lanes. Push still gated on the pill re-diff (fix lane + lane-b1). Zach's open item: 'the top-level dashboard of the lead page to match command center mode' — confirm before building. Census signing pending.
- Desk boards regenerated from the record (tools/boards/desk-boards.mjs; the DAY block is the one hand-dated part). Number board statuses corrected.
- 12:29 local: BACK after cap #3. Resumed: pill fix 1b · lane-b1 (reachability scorer, HOLD the re-walk) · brain r4 (X9) · home-cards 1–5 · census · laptop #241 door. Desk boards lane DONE (generator + 3 boards). Zach's 'header not fixed' = he was on PROD (struck). Push sequence unchanged: fix 1b → scorer → AFTER re-walk → IMPROVED → push → prod verify → two pings.
- #242 (laptop): the AI tab leaves; the summary band expands in place into the full story (hard facts · next action · where they stand · facts by category · memory), LOCKED-v2 pin → laptop lane after #241. #236.2: lens live on every surface (client-dashboard leak closed); cage widened to consumers; brain r5 = the 2-site ratchet.
- FIX 1b GREEN: station-1 pills are quick-reply on every road (the beds tap now answers); the 'double Narrow' was restored history (empty wire). AFTER re-walk GO sent to lane-b1 → diff → verdict → the push.
- #243 (Zach on localhost MC): the middle column's 'Myli's responses' track is a HAND-ROLLED projection (LockMiddleColumn.tsx:201 .mr grammar) — retires; the feed renders through the ONE resolver, filtered by the view row; the curved ear. → MC r8 (Opus). The r7 judge passed it because the LOCK drew that rail as mock — a drawing of a concept is not a licence for a private composer.
- #244: Myli's MODE picker (draft-only · live · off) on both views → MC r8 (weld mode chip) + laptop lane (second-rung slot); bound to the switch the wire reads.
- #245: MC needs a visible EXIT (same-tab 'Open lead page' + Back). In r8 with #243 + #244.
- #246 the welded tab (laptop + MC) → laptop lane (with #244 mode picker) + MC r8. Projection-composer cage lane spawned (Opus) — LockMiddleColumn must red today.
- BRAIN round 5 DONE (cage scope grown to story consumers 196→225 declared, ratcheted to 223; routing moved to read-ai-assignment.ts). Brain lane STOPPED. Flow judge owed post-push. Lesson saved: commit with a pathspec always.
- #243.1: MC right track STAYS as MYLI'S ACTIONS — signed cards, Myli as actor, each beside the left card it answers; no freelance. MC r8 amended.
- CENSUS LANDED: 67 rows (9 exist · 19 wrong sentence · 7 exist-no-writer · 7 signed today · 12 new-proposed · 9 not-an-act · 8 no-control); board /boards/website-act-catalog; 24-item signing list in front of Zach. Mint lane spawns on his answers.
- #243.2: MC split — Myli-actor cards go RIGHT (under the held draft), time-aligned to the left card they answer. → r8.
- Home-cards 1–3 LANDED (photo cards for HouseLoop homes; 'created a saved search' 0971; address inline — 414/428 website home rows had NO address, fixed). #239.2: item 5 actor ruled (person on the inbound road, Myli autonomous; 379-row backfill through the writer); item 4 → census mint. MC r8: BUILD IT (parallel tracks, 'Fact saved to memory' mig 0973). Zach owes: the 24 census answers.
- #246 weld FIXED (a 4px margin from the port). #244.1 STRUCTURE-GATE signed at the desk: hl_leads.myli_autonomy (draft|live|NULL=global) + myliAutonomyFor(channel, leadId) — laptop lane builds migration+read+picker; MC r8 imports theirs. DDL desk-signed under Rule #0 v2.
- #243.4 FINAL SPLIT: center = all communications (Myli's sends included) + lead/system acts; right 25% = Myli's internal work only (fact/memory/tool/playbook/draft), row-aligned. Laptop = one running list. → MC r8.
- Projection-composer cage LIVE (desk re-ran: 95/95 green; red-proof on the recovered #243 code names the sentence). #243.5 recorded.
- #243.7 reference conversation (three texts center; 'Called a tool · Single-Property Showing Agreement' on the third row) → MC r8 third proof pair.
- PILL r5: REGRESSED (correct) — all 5 pill beats CURED; /my-houseloop 500 from the brain lane's savedSearches shape change (gather-state.ts:291). Brain lane reopened for the fix → lane-b1 re-runs the AFTER → push.
- #243.8: laptop = Myli's internal work as a sub-line under the parent card (same discriminator/link as MC). → laptop lane after #244.1.
- Home-cards item 5 LANDED (person actor on the inbound road; 147 backfilled structurally, 232 undecided stay Myli's). 'via Myli' carrier token ruled wanted → same lane, last item. Held-draft Approve/Send: wired r5, never clicked (read-only rails, ai_master OFF) — the first certification run is tomorrow's cockpit work unless Zach pulls it.
- #243.9: one grid, one scroll port (reverses r6's two ports for the tracks); heading MYLI'S ACTIONS. Zach is looking at r7 on localhost; r8 not landed yet.
- #247: MC must fit one 1440 screen incl. the command row (bands 344→~244); LOCKED-v2 pin. → MC r9 item (4). Grinder ticked B1 at r7 → state now MC-B2 r1; the emitted judge brief targets the wrong item — held; the desk judges the split once after r9.
- #247.1 shave prose (mode note, dnote, cmdrow caption, headings) → MC r9. DEFECTS: queue shows trashed leads (r9 item 5); Showing Feedback playbook loop 24×/day on RaShaund (automation card).
- #247 sharpened: side lanes right; only the middle weld runs below the fold → the tracks port absorbs it + prose removals. MC r9 = 6 items (split · exit · picker import · #247 fold · queue excludes trashed · prose).
- P0 FIXED at the door (unwrapLensedStory; the shape change was r4's lens wrapper, not S12). lane-b1 ROUND 6 AFTER walking on the fixed tree. Laptop: #244.1 landed (column + one read + picker, proven); closing items 0973/0974/cage exemption then #243.8. MC r8 landed (resolver identity 16/16; weld ear); r9 = six items. Brain cage 224 vs 223 = the autonomy read → exemption with reason.
- Home-cards lane CLOSED: 'via Myli' carrier token (three provenance fields: portal · leadSource · carrier; null when Myli is the actor; a clone renders 'via Clone'); proven on Morgan with the control on the same page. Item 4 (chat kinds) → the census mint. Still live: MC r9 · laptop closing items · lane-b1 round 6 (the push).
- Bench punch-list item TICKED from the receipt. HYGIENE TOMORROW: master-schedule warns 16 tasks on floor 9 unclaimed by any card; the grinder's pill card (stale 7h — fixes ran outside the grinder) and MC card (ticked at r7 → B2 r1, wrong judge brief) need state reconciliation after the push.
- #248 THE ACT CATALOG SIGNED (Zach's dictation; affordability ≠ budget; the tour family separate from the browse verdicts; one saved_search_edited with a mandatory &#123;&#123;what&#125;&#125;). mint-website-act-catalog (Opus) spawned: migrations from 0975, harness proof per kind, ALL_FEED registration, the grid-save + /updates pixel defects. Live: lane-b1 round 6 (the push) · MC r9 · laptop closing items · the mint.
- Laptop #243.8 LANDED: Myli's internal work folds to a sub-line under its parent (isMyliInternalAct in lib/activity/myli-internal.ts, imported by MC). Ruling: ai_searched folds by ACTOR (person's stay cards; Myli's fold). Awaiting the lane's confirmation of 0973/0974/the cage exemption.
- MC r8-final LANDED (split · one port · exit · weld · picker import). TWIN: two internal-act predicates → fold to isAiInternalAct (ai-internal-act.ts). SIGNED: caused_by_activity_id + trigger_fire_id on hl_activity_log (the reaction→trigger link does not exist today) — build tomorrow. Still owed in MC r9: #247 fold + #247.1 prose + the queue; mig 0973 from the laptop lane.
- #239.4 home card = three hard lines (amends 'address inline') → laptop lane (the shared horizontal card renderer).
- #246.1 view row spreads when wide (laptop + MC) → laptop lane + MC r9.
- ~14:30 CT: Zach switched the desk to FABLE 5 (5.1 eating tokens). Desk continues; judge lanes inherit the desk model from here. Push holds on the two lanes committing their applied migrations (0972–0974, 0975–0991 ledger orphans), then retry from the clean worktree.
- Zach: all Fable seats → claude-fable-5 (not 5.1); config swapped + committed; new lanes inherit it. The desk session itself was switched by Zach's hand.
- Predicate resolved: isAiInternalAct (ai-internal-act.ts) carries the laptop logic; both surfaces import it. MC r9 done except #246.1. Push waits on: the mint lane's migration commits + the laptop lane's 0974 batch + the dog-ear re-verify.
- MINT batch 1 GREEN (7 sentence kinds, migs 0985–0991; a new sentence cage in the battery; contact_requested held → signed the about-clause). Desk deleted the 4 stale ledger rows (pre-image kept). Door-zero sweep in flight (brain lane). Laptop owes: the sentence-test expectation for 0973. Batch 2 = affordability + profile + registration + the verdict pair.

## Related documentation

Doc sections this work creates or updates:

- [`guides/intent-workflows`](/guides/intent-workflows)
- [`guides/workflows`](/guides/workflows)
- [`guides/registration`](/guides/registration)
- [`guides/web-registration-and-intake`](/guides/web-registration-and-intake)
- [`guides/tour-and-contact-requests`](/guides/tour-and-contact-requests)
- [`fub/trigger-events`](/fub/trigger-events)
- [`fub/lead-import`](/fub/lead-import)
- [`platform/message-catalog`](/platform/message-catalog)
