---
title: "Lee (Pipeline and Workflow Master Overview)"
description: "Work-in-progress dossier — Lee · Infra · Pipeline & Workflow · FUB · Notifications. Pipeline and Workflow Master Overview."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/lee/pipeline-workflow-overview.md` on every push. **Progress: 9/21 done (43%).** _Updated 2026-07-09._</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 63 days ago. If Lee has shipped since, this dossier under-reports — refresh `.claude/state/wip/lee/pipeline-workflow-overview.md`.</Warning>

**Owner:** Lee — Infra · Pipeline & Workflow · FUB · Notifications

## Checklist

**⭐ 2026-07-09 — WORKSTREAM ABSORBED into the reorganized docs.** The old canonical
`guides/pipeline-workflow-overview` was DELETED in the doc consolidation (redirects to /myli-master); the living
story is now the ONE "Pipeline & Workflow" nav group ordered **Lead Integration → Trigger Events → Workflows**
(`guides/web-registration-and-intake` → `guides/intent-workflows` → `guides/workflows`), with the FUB side in
**`fub/trigger-events` ("FUB Trigger Events & Notifications", the 22-event master)** and notifications in
`platform/message-catalog`. The checklist below is the historical build record of the reorg that shipped.

**North star (as-built):** HouseLoop's lead engine to MVP. ONE platform (HouseLoop, never named); everything is a
Lead Source with events; `Workflow = f(source × event)` for the registration family, one intent canvas each.
HouseLoop is the first-class notifier; Realloop retired.

### 🆕 ACTIVE WORKSTREAM (2026-07-03) — Lead-Detail Parity Lanes + Polymorphic Assignment
**PRD/SSOT:** `GT/GT - ACTIVE/GT_HOUSELOOP_LEAD_DETAIL_PARITY_AND_ASSIGNMENT_PRD.md` · **scope:** `session-scope-parity-lanes.lock` · **handoff:** `.claude/state/lee-handoff.md` · **prod baseline:** `98650ba504`
Both lead-detail lanes (LEFT = human + AI clone · RIGHT = Myli) share ONE skeleton: `📋 Tasks & Appointments`
(ALWAYS on) + `🤖 AI Workflow` (gated). Grouped polymorphic `Assign to` (Agents · AI Agents · TCs). Clone
task-level override (clone-assigned task fires without global AI mode).

**SHIPPED (pushed, deploying):** Phase 0 docs/PRD (3 new + fixes) · Phase 2 mig 0306 (clone actor_type) ·
Phase 3 grouped assignee picker · Phase 4 parity lanes (fixed the `aiActive` gate that hid human
tasks/appointments — T&A now always-on both lanes; per-lane scoping + faint/mirror; bold toggle) · v2 Tags
under T&A · Myli 4-row Contact. Awaiting Zach's final live sign-off.

**🎯 THE GOAL — NEXT: Phase 5 (Zach-directed 2026-07-03), build end-to-end THEN test.**
(1) **Clone config UI** — surface `POST /api/team/clone` + `hl_member_role` persona/tools override in Team
settings so a member creates + configures their AI Clone. (2) **Task-level override DISPATCH** — a task
assigned to a clone actually FIRES the clone (eligible types only) without the global AI toggle, via
`hl_scheduled_actions.actor_type='clone'`, clone acting AS its human. (3) Model-row real value + Myli
iMessage-reassign → Team settings. (4) Live end-to-end test → Zach sign-off.

### 🧪 SIGN-OFF MATRIX — every webhook, top to bottom (Zach + Lee, iterate live)
**How we run it:** go row by row. Lee hands the **test-lead payload** for `localhost:3020`, Zach fires it, we **log
every gate**. A row **signs off (✅)** only when: (1) the webhook lands + the lead/activity card renders correctly,
(2) Creates-Lead behaves as set, (3) it's **wired to the CORRECT notification** (notify_class / notify_owner — this is
also checking **Benji's** notification work), and (4) its workflow (if any) fires. `⬜` = not yet verified.
_Source of truth: `hl_fub_webhook_settings` (FUB) · `hl_trigger_events` (HouseLoop, notify_class) — pulled 2026-07-02._

### A. Follow Up Boss webhooks (`hl_fub_webhook_settings`)  — all `notify_owner=realloop`, no workflow bound yet
| # | Webhook | Creates Lead | Notify → | Workflow | Result |
|---|---|---|---|---|---|
| A1 | `property_inquiry` | ✅ | realloop | — | ⬜ |
| A2 | `tour_request` | ✅ | realloop | — | ⬜ |
| A3 | `contact_request` | ✅ | realloop | — | ⬜ |
| A4 | `rental_inquiry` | ✅ | realloop | — | ⬜ |
| A5 | `seller_inquiry` | ✅ | realloop | — | ⬜ |
| A6 | `virtually_toured` | ✗ | realloop | — | ⬜ |
| A7 | `text_message` | ✗ | realloop | — | ⬜ |
| A8 | `in_app_message` | ✗ | off | — | ⬜ |
| A9 | `notification` | ✗ | off | — | ⬜ |
| A10 | `email_received` | ✗ | off | — | ⬜ |
| A11 | `property_viewed` | ✗ (counted signal) | off | — | ⬜ |
| A12 | `property_saved` | ✗ | off | — | ⬜ |
| A13 | `property_shared` | ✗ | off | — | ⬜ |
| A14 | `insight_high_intent_buyer` | ✗ | off | — | ⬜ |
| A15 | `insight_high_intent_seller` | ✗ | off | — | ⬜ |
| A16 | `insight_viewed_10x_7d` | ✗ | off | — | ⬜ |
| A17 | `insight_new_area` | ✗ | off | — | ⬜ |
| — | `_global` (master switch) | n/a | n/a | — | n/a |

### B. Phone-call webhooks (FUB → the Phone Numbers section)
| # | Webhook | Creates Lead | Notify → | Workflow | Result |
|---|---|---|---|---|---|
| B1 | `phone_call` (generic) | ✗ | realloop | — | ⬜ |
| B2 | `phone_call_company` | ✗ | realloop | — | ⬜ |
| B3 | `phone_call_sign` (sign/listing line) | ✗ | realloop | — | ⬜ |
| B4 | `phone_call_past_agent` | ✗ | realloop | — | ⬜ |
| B5 | `phone_call_ana` (Ana's old cell) | ✗ | realloop | — | ⬜ |

### C. HouseLoop webhooks (`hl_trigger_events`, platform `houseloop`)
| # | Webhook / event | Creates Lead | Notify class | Workflow | Result |
|---|---|---|---|---|---|
| C1 | New Lead — General Registration | ✅ | `lead_created` | ✅ | ⬜ |
| C2 | New Lead — Liked Home | ✅ | `home_liked` | ✅ | ⬜ |
| C3 | New Lead — Saved Home | ✅ | `home_saved` | ✅ | ⬜ |
| C4 | New Lead — Saved Search | ✅ | `saved_search` | ✅ | ⬜ |
| C5 | New Lead — Tour Request | ✅ | `tour_request` | ✅ | ⬜ |
| C6 | New Lead — Contact Request | ✅ | `contact_request` | ✅ | ⬜ |
| C7 | New Lead — Seller Get Report | ✅ | `home_value_report` | ✅ | ⬜ |
| C8 | New Lead — Seller Get Started | ✅ | `seller_request` | ✅ | ⬜ |
| C9 | New Lead — Shared Home | ✅ | `home_shared` | ✅ | ⬜ (now in `hl_trigger_events`, mig 0287) |
| C10 | System — Property Viewed | ✗ (counted signal) | `property_viewed` | — | ⬜ |
| C11 | System — Property Liked | ✗ | `home_liked` | — | ⬜ |
| C12 | System — Property Saved | ✗ | `home_saved` | — | ⬜ |
| C13 | System — Property Shared | ✗ | `home_shared` | — | ⬜ |
| C14 | System — Saved Search (client) | ✗ | `saved_search` | — | ⬜ |
| C15 | System — Saved Search · Agent | ✗ | `saved_search` | — | ⬜ |
| C16 | System — Saved Search · System | ✗ | `saved_search` | — | ⬜ |
| C17 | System — Tour Request (existing lead) | ✗ | `tour_request` | — | ⬜ |
| C18 | System — Contact Request (existing lead) | ✗ | `contact_request` | — | ⬜ |
| C19 | System — Seller Get Report (existing lead) | ✗ | `seller_request` | — | ⬜ |

**Gaps flagged from the pull (2026-07-02):** (1) C9 Shared Home missing from `hl_trigger_events`. (2) Every FUB row is
still `notify_owner=realloop` — the HouseLoop-notifier migration (per-row flip) hasn't started; each becomes a matrix
sign-off as it moves to `houseloop`. (3) No FUB row has a `workflow_id` bound yet.

---

### SHIPPED (Phase 0 — foundation)
- [x] Lead Sources = ONE page (`/settings/lead-platform`): source block = header + KPI funnel + events + Configure
- [x] Collapsed Registration Events + Lead Sources + FUB Settings into the one page
- [x] Workflows grouped BY lead source; `Workflow = f(source × event)` (reversed the old "agnostic" model)
- [x] Dynamic Back → "Back to Lead Sources"; canonical `HlBackLink`
- [x] FUB webhook config (per-type toggles: Process / Creates-Lead / Notify / Notify-Via) + 17-event matrix (migs 0274/0275)
- [x] Phone Numbers registry `hl_phone_numbers` (41 FUB lines) + `/settings/phone-numbers`
- [x] Notifications = Everything Phase 1 (catalog-derived notification list from the same event tree)
- [x] R2/R3/R4 shipped (mig 0270 `notify_owner`): HouseLoop native notifier + notify-toggle repoint + per-event Realloop kill-switch

### ARCHITECTURE RAILS (must be green before matrix rows pass)
- [ ] R1 Native FUB ingestion (Realloop mirrors `fub.*` data only, stops being the notifier)
- [ ] R8 Webhook test harness (fire/replay a real FUB webhook + verify every gate)
- [ ] R9 Native dialed-line preserved (Ana 913-954-7123 — never the forward target)

### OPEN BUILDS (the keystones)
- [ ] 1d. FUB SUB-SOURCE dimension — split FUB events by portal so `(FUB × sub-source × event)` binds its own workflow (the keystone for source-specific FUB copy)
- [ ] 1e. Email received (`emailsCreated`) — settings row + native HL notify + matrix row
- [ ] 2. A workflow (text + email + nurture) for EVERY event (add missing text steps; DRAFT→ACTIVE the existing-lead doors)
- [ ] 3. Zillow-inbound render MVP (Zillow logo, "Find on HouseLoop", iMessage-bubble restyle)
- [ ] 4. FUB Phone Call Activity engine — ONE `(FUB · Phone Call)` workflow, Myli reads the dialed line → category → copy; text-back from BlueBubbles 913-208-1633; Ana email bridge; then the Twilio port
- [ ] 5. Past-client reconciliation (single canonical table → listing page → seller report → Ana campaign)
- [ ] 6. Go-live — quarantine legacy FUB Zillow doors, E2E-verify each real inquiry in the Run Log

### SIGN-OFF MATRIX — recent edits
- [x] **Viewed home** added to the HOUSELOOP → Existing-lead matrix as a **COUNTED signal** (Zach 2026-07-01):
  no workflow — increments the lead's Visits/Properties counter + "viewed N×"; can gate a workflow with a count
  condition. Backed by `hl_trigger_events.property_viewed` (system, houseloop, mig 0276).

### NEXT CONCRETE ACTION (Lee)
- [ ] KEEP THE OVERVIEW DOC CURRENT — reconcile it against today's changes: Benji's saved-search 3-trigger split (saved_search=client · saved_search_agent · system) is not reflected in the HouseLoop registration/existing-lead matrix; update it.
- [ ] Then resume the Sign-Off Matrix one row at a time (Zach coming back with direction on lead sources + event triggers).

---

## PRD · `GT/GT - ACTIVE/GT_HOUSELOOP_LEAD_PLATFORM_PRD.md`

#### GT — HOUSELOOP LEAD PLATFORM PRD (Zach + Lee, 2026-06-26) — ✅ SIGNED, BUILD

**Owner:** Lee (end-to-end). **Status:** model locked + signed by Zach; building now.
**Design SSOT:** `.claude/checkpoints/lead-system-MAP-2026-06-26.md`. **Scope lock:**
`.claude/state/session-scope-platform-source.lock`. Supersedes the platform/source UI direction in
`GT_HOUSELOOP_LEAD_FLOW_UI_PRD.md §12` (that doc's attribution columns ship as-is; the NAV/screens here win).

---

##### 1. THE PROBLEM
HouseLoop is the website AND the CRM in one product — and Follow Up Boss is a legacy CRM piping in leads with
history. "Lead source" alone can't express that. We spent a day circling because two things were tangled:
**where a lead came from** vs **what it does on our site**. This PRD untangles it.

##### 2. WHAT THE RESEARCH SAID (5 CRMs — HubSpot, Salesforce, UTM/GA4, FUB/kvCORE/Sierra, Pipedrive/GHL/AC)
Unanimous findings (full briefs in session history):
- **Two fields, never one:** a TRUE ORIGIN (Zillow) kept separate from the CONNECTOR (Follow Up Boss). Origin
  is first-touch + immutable. (HubSpot Original vs Record Source; Salesforce LeadSource vs Source System.)
- **Events ≠ source.** Activity is a separate growing stream; source is a stamp set once. (We already do this:
  `hl_lead_activity`.)
- **Trigger on the EVENT, filter on SOURCE/PLATFORM.** Never one workflow per source. Merge fields fill the
  words. (Every CRM. Sierra/GHL/AC explicit.)
- **No "type" field** for origin — "type" = the event (already our catalog) or the pipeline role (already our
  Buyer/Seller pipelines). Don't add it.
- **Don't prefix `fub-zillow.com`** — keep source = the real origin (Zillow), connector separate (live FUB data
  with 48k people confirmed this). Platform = the connector/world.

##### 3. THE MODEL (LOCKED)

###### 3.1 The keystone — TWO buckets, TWO routing keys
| Bucket | Keyed on | Meaning |
|---|---|---|
| **New Lead Registration** | the lead's **ORIGIN** (source + platform) | "how they came in." realtor.com/Zillow via Follow Up Boss → that source's registration (the reactivation). Origin is **set once, permanent** — a merge field forever, but stops STEERING after registration. |
| **(HouseLoop) System Events** | the **SURFACE** (where the action fired) | "what they do on HouseLoop." ANY lead — regardless of origin — who likes a home / requests a tour on HouseLoop.ai fires the **HouseLoop** workflow. |
| **Follow-Up** | schedule | the nurtures |

**Proof (Zach's, universal):** lead acquired platform=Follow Up Boss, source=Zillow.com → registration runs
**Reactivation** ("you worked with Alicia… homes on HouseLoop.ai"). Origin stays Zillow forever. They then like
a home **on HouseLoop.ai** → SURFACE = HouseLoop → runs the **HouseLoop liked-home workflow**, not the FUB one.

###### 3.2 The cascade (first match wins)
```
A lead's event fires →
  1. SPECIFIC workflow for (this source + this event)?    → run it
  2. else a workflow for (this platform)?                 → run it   (e.g. FUB → Reactivation)
  3. else GENERAL REGISTRATION (the catch-all floor)      → always runs ({{lead_source}} fills the words)
```
Already engine-true: `dispatchDoor` routes existing-lead events by event SURFACE and keeps registration
ORIGIN-scoped (built P3). New sources work instantly off the floor; specials are opt-in.

###### 3.3 Scale = MERGE FIELDS
One event → one workflow → `{{first_name}}` `{{home_address}}` `{{lead_source}}` `{{platform}}` fill the rest.
Never a workflow per source. Adding a source = a new ROW, not a rebuild.

##### 4. NAV (LOCKED) — 3 items (Lead Flow folded away)
```
Pipeline & Workflow
  ├─ Lead Platform     ← master: Platform → Source → registration doors + system events
  ├─ Lead Sources      ← thin list: the 3 sources
  └─ Workflows         ← the categorized workflow library
```

##### 5. LEAD SOURCES — EXACTLY 3 (all else killed)
| Source | Platform | Type | Default Stage |
|---|---|---|---|
| HouseLoop.ai | HouseLoop | web | New Lead |
| Zillow.com | Follow Up Boss | portal (fix from `manual`) | New Lead |
| Manual entry | HouseLoop | manual | New Lead |
KILL (deactivate, reversible): Referral · SRG-Follow Up Boss (`fub_import`) · `__platform_houseloop__` ·
`__platform_fub__` · IDX Sign-up. **Build-order care:** `fub_import` holds 6 FUB leads + their reg doors →
REASSIGN leads + doors onto Zillow.com FIRST, then retire it. Nothing lost.

##### 6. EVENT NAMING — the "New Lead –" prefix IS the registration-vs-system tell
- **Registration doors** (origin): prefixed **"New Lead – …"**. HouseLoop.ai = 8: New Lead – General
  Registration · Liked Home · Saved Home · Saved Search · Tour Request · Contact Request · Seller Get Report ·
  Seller Get Started. (3 need relabel today: liked_home/saved_home/general_registration; other 5 already correct.)
  Zillow.com = 2: New Lead – Tour Request · New Lead – Contact Request (= "property inquiry"). (Move from
  fub_import; rename "FUB Property Tour"→"New Lead – Tour Request"; drop FUB's general/seller/saved-search regs.)
- **System events** (surface): UNPREFIXED: Tour Request · Contact Request · Saved Search · Seller Get Report ·
  Seller Get Started · Property Viewed · Property Liked · Property Saved · Property Shared.

##### 7. THE SCREENS (wireframes — final, real names)

###### 7.1 LEAD PLATFORM  → cascades into Workflows (click an event row → its workflow)
```
╔═ HouseLoop ════════════════════════════════════════════ platform ═╗
  ─ SOURCES (origin → New Lead Registration) ──────────────────────
   🌐 HouseLoop.ai — Organic ················· 1 lead · 8 doors
        New Lead – General Registration  → General Welcome
        New Lead – Liked Home            → Liked Home
        New Lead – Saved Home            → General
        New Lead – Saved Search          → General
        New Lead – Tour Request          → General
        New Lead – Contact Request       → General
        New Lead – Seller Get Report     → General
        New Lead – Seller Get Started    → General
  ─ HOUSELOOP SYSTEM EVENTS (surface → regardless of source) ──────
        Tour Request · Contact Request · Saved Search · Seller Get Report ·
        Seller Get Started · Property Viewed · Property Liked · Property Saved · Property Shared
  ─ FOLLOW-UP ──  (none yet)
╔═ Follow Up Boss ═══════════════════════════════════════ platform ═╗
  ─ SOURCES (origin → New Lead Registration) ──────────────────────
   🌐 Zillow.com ····························· 6 leads · 2 doors
        New Lead – Tour Request          → Reactivation Campaign
        New Lead – Contact Request       → Reactivation Campaign
  ─ SYSTEM EVENTS ──  (none — activity happens on HouseLoop)
```
Layout: blue platform header → source sub-section → bucket sub-headers → event rows (Event · Workflow ·
Active). Brand: white-on-blue header. Tables, not chevron trees.

###### 7.2 LEAD SOURCES  → cascades into Lead Platform (click a source → its block)
```
 SOURCE          PLATFORM          TYPE      DEFAULT STAGE   LEADS   REG DOORS
 HouseLoop.ai    HouseLoop         web       New Lead          1        8
 Zillow.com      Follow Up Boss    portal    New Lead          6        2
 Manual entry    HouseLoop         manual    New Lead          0        0
```

###### 7.3 WORKFLOWS  → cascades into the canvas. The automation LIBRARY (grain = one row per WORKFLOW)
Categorized Registration / System / Follow-Up (NOT "Other"). Columns: Workflow · Trigger · Steps · Completed ·
Status (+ Platform · Source as light context). Steps = counted from the canvas; Completed = `hl_workflow_runs`.
```
═ REGISTRATION ═══════════════════════════════════════════════════════════
 WORKFLOW                         TRIGGER                STEPS  COMPLETED  STATUS
 New Lead – General Registration   New Lead – Gen. Reg.    4    138/142    active
 Reactivation Campaign             New Lead – Tour Req.    6      6/7      active
═ SYSTEM ═════════════════════════════════════════════════════════════════
 Liked Home                        Liked Home             3     12/12     active
═ FOLLOW-UP ══════════════════════════════════════════════════════════════
 90-Day Nurture                    schedule               8      4/20     active
```
NOT a clone of Lead Platform: Lead Platform rows = DOORS (by source); Workflows rows = WORKFLOWS (the canvas +
its health). One workflow can serve many doors.

###### 7.4 LEAD DETAIL (already shipped — confirm labels)
`Source: Zillow` + `Platform: Follow Up Boss` in the Contact card.

###### 7.5 WORKFLOW TRIGGER (the one engine add)
Open a workflow → trigger node: `Event [▾] · Platform [▾ any/HouseLoop/FUB] · Source [▾ any/Zillow/…]`.
This is what makes "Registration WHERE platform = Follow Up Boss → Reactivation" buildable.

##### 8. BUILD PHASES (Lee, all reversible/additive where possible)
- **P1 — Data cleanup migration:** reassign FUB leads+doors → Zillow.com; retire junk sources (deactivate);
  fix Zillow type→portal; relabel the 3 HouseLoop doors + the Zillow doors with "New Lead –".
- **P2 — Lead Platform page:** Platform → Source → (New Lead Registration · System Events · Follow-Up), real
  data, tables, blue headers, workflow-per-row. (Replaces the scrapped tree.)
- **P3 — Lead Sources page:** the thin 3-row table (Source · Platform · Type · Default Stage · Leads · Reg Doors).
- **P4 — Workflows page:** categorize Registration/System/Follow-Up + Steps/Completed columns.
- **P5 — Workflow trigger:** Platform + Source filter dropdowns on the trigger node.
- **P6 — Nav:** Lead Platform · Lead Sources · Workflows (remove Lead Flow).
- **P7 — Verify on prod (Playwright) + handoff/docs.**

##### 9. WHAT'S ALREADY BUILT (don't redo)
Lead `platform`/`channel` cols + backfill (mig 0247) · lead-detail Platform/Source rows · dispatchDoor
origin-vs-surface routing (P3) · `hl_lead_activity` event stream · the workflow canvas · the 3-bucket Lead Flow
grouping (becomes the System Events section). The work is the SCREENS + the data cleanup + the trigger filter.

##### 10. NON-GOALS
No `fub-` prefix. No "type" field. No per-source events (merge fields handle words). No Lead Flow nav page.
No inventing sources beyond the real 3. Grows by ROWS.

---

## Scope lock · `.claude/state/lee.lock`

⚠ CLOCK CORRECTION (desk, 05:15 EDT Fri 9/11): the LAST_UPDATED stamps from "12:15 CT / 13:15 EDT" through "17:30 CT / 18:30 EDT" below are WRONG by about +5 h — the desk labelled machine-local times as afternoon/evening; the machine clock is EDT and the wakeups printed 22:24 … 05:14. The push landed ~05:00 EDT Friday (04:00 CT), BEFORE Zach's day, not Friday evening. Every relative order in those entries is right; only the wall-clock labels are off. — LAST_UPDATED: 2026-09-11 08:05 CT / 09:05 EDT — lane-zillow-record-source BUILT + ticked (850d0dd3: the whole Zillow record → photos/schools/price history/tax years/structural through the one writers; Redfin specs/schools/public-records; tests 28→67; proven by replaying the stored dataset on the census row for $0: 11 price events, 3 rated schools, 2,450 sqft render on :3001). Desk: redacted the vendor Street View key in realtor-found.json (not ours; not in .env) by pathspec; carded the two-photo-columns defect, the propertyType vocabulary ruling, the tax-year guard. lane-mobile-schools BUILT (false alarm; test arm kept) · lane-offer-insights-offmarket BUILT (gated on the one basis; taxes/HOA $0 carded). IN FLIGHT: lane-entity-build (mid link census; tsc red on its WhereHouseLoopIsMap.tsx mid-rename). Zach's sitting: full-bleed map (hide the Myli band in map view?), the address one-word, the ChatGPT click, the MLS GRID email, the Apify subscription. — LAST_UPDATED: 2026-09-11 07:45 CT / 08:45 EDT — ⛔ ZACH #257.500d ("figure out what's wrong" — I had hand-waved "Redfin refused by the cap"). MEASURED: the cap is OUR guard (apify.ts DEFAULT_DAILY_RUN_CAP=40, benji 9/10); 32 of 42 runs = the ten-home census at 00:01Z, 6 = the Hadley double-dispatch, 4 = the judge; Redfin/Apify impose no daily cap; the real limit is the free plan $5/mo ($3.47 used, ~$0.21 per seller run). Charlotte has NO photos anywhere (Zillow photos = one Street View URL, Realtor "", Redfin photoCount 0; built 1922, last sale 1980) — the judge picked a never-listed home; the control home returned 25 photos/11 events/3 schools. THE REAL DEFECT: the Zillow detail record carries ~50 fields and apify.ts reads 3 → lane-zillow-record-source (Opus, 850d0dd3) SPAWNED: the whole Zillow + Redfin records through the one writers, proven by replaying the stored dataset against the census row. Told Zach straight. Carded the 904 SW 36th identity triplicate (zip typo). IN FLIGHT: lane-entity-build · lane-mobile-schools · lane-offer-insights-offmarket · lane-zillow-record-source. — LAST_UPDATED: 2026-09-11 07:10 CT / 08:10 EDT — lane-map-view-shelf BUILT + ticked (592fc1b2): ListingBottomSheet reused with six optional props, ListingCard via listingFromPin, /api/listings/map returns inView + pins in ONE response (no second count possible), nav-anchor 23/23, contract test red-proven; four map shots SENT to Zach. Its decision: the map is NOT full-bleed at 390 (y=352 under the Myli band) — carded for Zach (hide the band in map view only, one reversible rule). Census red on app/about/page.tsx = the entity lane's in-progress file (told). IN FLIGHT: lane-entity-build · lane-mobile-schools · lane-offer-insights-offmarket. — LAST_UPDATED: 2026-09-11 07:00 CT / 08:00 EDT — lane-calculator-hero-price BUILT + ticked (4993e8e2; resolveCalculatorPriceBasis over hero-price.ts; "Add a price to run the numbers." on pending). Its twin: OfferInsights.tsx $0 → carded bdb370a0 + lane-offer-insights-offmarket (Sonnet) SPAWNED (the widget does not render off-market). Home-render census RED on app/about/page.tsx (entity lane in progress) — the entity lane told to resolve by fact (verdict registry, surgical insert), never raise the floor. IN FLIGHT: lane-entity-build · lane-map-view-shelf (Opus) · lane-mobile-schools · lane-offer-insights-offmarket. — LAST_UPDATED: 2026-09-11 06:50 CT / 07:50 EDT — lane-value-ladder-rulings BUILT + ticked (928e016a per-road Apify ceilings, no DDL; 8283a76b: the schools rung via enrichOneListingZillow schoolsOnly, Realtor-first photos, no Street View in the gallery, hlEstimateSource stamped — $0 Apify, HLID464069 now hero $304,917 + 3 GreatSchools ratings; Redfin still refused by name at 42/40 — the rail). Its departure: schools DESKTOP-ONLY at 390 (hidden md:block) → carded 19884c62 + lane-mobile-schools (Sonnet) SPAWNED. Carded the Fable prod re-proof of the value ladder after tomorrow's deploy. ChatGPT MCP: measured LIVE on prod (mcp-smoke GREEN 4/4); told Zach the registration is his developer-mode click (or "go" for the desk on his account via the bridge) + the MLS GRID AI Use Addendum email; three cards. IN FLIGHT: lane-entity-build · lane-map-view-shelf (Opus) · lane-calculator-hero-price · lane-mobile-schools. — LAST_UPDATED: 2026-09-11 06:35 CT / 07:35 EDT — lane-value-road-p0 BUILT + ticked (090b92cb, 6dbebf2d): lib/listings/hero-price.ts (the $0 lived only in the mobile peek), the demand-report in-flight guard + AbortController, and `idempotency: "event.data.hlid"` on listing-mint-enrich (NO DDL — corrected my claim-column assumption; UNVERIFIED LIVE). Carded: the mortgage calculator $0 (spawning Sonnet), the idempotency live proof. copy rev 4 BUILT (1a5ccb4c69); /contact one-line swap ordered; lane-entity-build told to build /about from rev 4. ⚠ TOMORROW'S PUSH CHECKLIST ADDS: the Inngest PUT resync on web after deploy (function-config change on listing-mint-enrich) + the concurrent-dispatch proof on the bench. — LAST_UPDATED: 2026-09-11 06:20 CT / 07:20 EDT — ZACH #257.500b (the /about map is a VISUAL image, no links/pages/pin list; three /about themes: vs the portals, KC stats, RELOCATION "a tour guide for the city"; the eXp/legal block already in the footer → the eXp decision + /orlando + the Orlando window are NOT carded) and #257.500c (the glass is SIGNED "perfect"; the map page with the glass shelf + the N cards is next — lane-map-view-shelf told: same glass, screenshots are the deliverable). copy-entity-pages: REV 4 ordered (/about only: map → caption, two new H2s, stats moved up). lane-entity-build told: map = visual (13 pins, inline SVG, no links), reuse Footer.tsx legal block, build /about LAST after re-reading rev 4. ⚠ CAUGHT: my six `task.mjs add --title` cards never existed (positional title; memory written) — re-added the three that survive #257.500b (no-404 cage 05299798, the address decision, the KC duplicate-key fix); floors.json committed by pathspec 685a6de0e9 (387/0, the day's record). — LAST_UPDATED: 2026-09-11 06:05 CT / 07:05 EDT — copy-entity-pages rev 3 BUILT (b955d19f12: no HouseLoop address, the frozen phrase ×12, the eXp sentence, pins only) → lane-entity-build (Sonnet) SPAWNED for ffb63d80/bb75f978/bac0296d/632705e5: /about (+ inline-SVG US map), /contact, /myli, the OnlineBusiness JSON-LD, footer/menu/sitemap, the no-404 cage, the SRG /house-loop page + RealEstateAgent schema in lib/srg/site-html.ts (deploy road to be reported, not driven). lane-citability-lines BUILT + ticked (bdd56d8d; the visible lines already existed — dateModified added to CollectionPage/RealEstateListing JSON-LD); carded the KC weekly duplicate-key error. reconcile GREEN (33 findings, 0 red), boards re-promoted. IN FLIGHT: lane-entity-build · lane-map-view-shelf (Opus) · lane-value-ladder-rulings (Opus) · lane-value-road-p0 (Sonnet). The copy keys tick on the build receipt. — LAST_UPDATED: 2026-09-11 05:50 CT / 06:50 EDT — BUILT + TICKED: lane-houseloop-insights (dbf78534; H2 "Insights on &lt;address&gt;", generator guard + retry, cage no-ai-overview floor 0, stored blurbs 0/10,579 contain the phrase, nothing rewritten) · lane-listing-scroll-law glass restore (19995a5a; byte-for-byte from git, A/B shots SENT to Zach, A ships; 30/30 listing-detail; the audit's keep-list names the retired nav-clearance anchor — flagged, not restored). SPAWNED: lane-map-view-shelf (Opus, 592fc1b2, Zillow's map view + viewport shelf, one resolver for N and the cards). SECOND OPINION #257.500a CAPTURED + judged (SECOND-OPINION-brand-structure-2026-09-11.md, four headings): HouseLoop OnlineBusiness with NO address (overrules my #257.500 (4) call — correction appended to the rulings), SRG RealEstateAgent memberOf eXp on its site, the frozen phrase "The Spradling Group, powered by HouseLoop", new markets plain HouseLoop, pins only (no /orlando today — carded blocked on Zach), the eXp co-branding on houseloop.ai = Zach's decision (carded). copy-entity-pages re-briefed twice (the facts + the map copy; then the amendment) — REVISING; its earlier BUILT superseded, keys bb75f978/ffb63d80/bac0296d/632705e5 NOT ticked. reconcile.mjs running in the background (the morning brief re-promote). NEXT on wake: when copy-entity-pages reports → spawn the SONNET ENTITY BUILD (the /about page with the inline-SVG US map, /contact, the OnlineBusiness JSON-LD + WebSite alternateName, sitemap, sitewide nav/footer links, the no-404 cage, the SRG site's schema + reciprocal sentence in a sibling or the same lane) — then the copy keys tick on the build receipt. Value lanes: lane-value-road-p0 (Sonnet) + lane-value-ladder-rulings (Opus) in flight. — LAST_UPDATED: 2026-09-11 05:30 CT / 06:30 EDT — ZACH RULED #257.500 (entity facts: founder Zach Spradling, foundingDate 2026-01, SRG since 2010, one phone, SRG NAP 7900 College Blvd, HouseLoop = Florida-registered online platform, no GBP; NO 404s behind the home page; /about = the US map: KC active · Orlando next · Dallas/LA/Atlanta/Denver/Seattle/Chicago/St. Louis + NE coming soon; the positioning: nationwide, teams under the HouseLoop brand, the relationship kept lead→closing, unlike Zillow/Redfin) and #257.500a (ask ChatGPT the brand STRUCTURE: SRG powered by HouseLoop today; new markets = HouseLoop/eXp teams under eXp). DONE: both recorded; copy-entity-pages sent the revision order (the facts + the map copy + URLS THE BUILD MUST CREATE); three cards on seo-revamp (the map build, the no-404 cage, Zach's open decisions: release dates per market + one word on the address); the ChatGPT ask q-brand-structure.txt LAUNCHED in the background (receipt → SECOND-OPINION-brand-structure-2026-09-11.md, then the four headings). lane-value-ladder-rulings (Opus) SPAWNED for 928e016a + 8283a76b ($0.30 Apify rail). NEXT on wake: capture the ChatGPT answer; tick copy-entity-pages on BUILT then spawn the Sonnet entity BUILD (/about with the map, /contact, JSON-LD, sitemap, nav links, the no-404 cage); report the value verdict to Zach (sent this turn). — LAST_UPDATED: 2026-09-11 05:15 CT / 06:15 EDT — ZACH ASKED for the fully-auto value road (not built; said so) and whether Apify credit remains: MEASURED on the account — free plan $5/mo, $3.22 used, $1.78 left; a Zillow detail run $0.17–0.20, the other two actors $0.02 + &lt;$0.01 → ~six homes left this month. The desk's own "OFF until the subscription" rule lifted for a PROOF: judge-value-ladder-proof (Fable) runs TWO real off-market KC homes through the real /sell road on :3001 with a $0.60 HARD CAP (start/end usage recorded), grades every rung (Zillow logo + Zestimate, Redfin, the HouseLoop value, schools, history, real photos, the HLID URL), then the owner view per the locked sheet as the bench seller persona; verdict + the two HLIDs to Zach. — ZACH IS AWAKE AND RULING. #257.497 (his 04:40 prod walk vs the Zillow app): (1) the listing-to-listing swipe DIES · (2) the Apple glass shipped 9/10 is WRONG — the signed one (early August: near-transparent middle, thin bezel, slight blur) is restored BYTE-FOR-BYTE from git · (3) Zillow's scroll law (tour + ask + the peeking shelf stay through the photo scroll while the iOS bar hides; the gallery has the menu only) · (4) no activity row while collapsed · (5) Zillow's map view with the viewport pull-up shelf. #257.497a: "Yes that's correct on the listing shelf design build it" — BUILD ORDERS. LANES LIVE: audit-apple-glass-archaeology (Fable, 3c36bcc5 — the commit + the exact CSS, then a Sonnet restore) · lane-listing-swipe-and-row (Sonnet: a4b1ffa2 + 8fa5745e) · lane-listing-scroll-law (Opus, the scroll law) — then the map view on the Opus slot. The county tool scope landed + ticked (c1d5721e: the tool schema had no county parameter — the model wrote the cities; fixed + the place-vs-scope refusal). Nothing pushes today (the day's push landed 09:06Z); tomorrow's push carries the shelf build. — PRIOR: LAST_UPDATED: 2026-09-11 04:25 CT / 05:25 EDT (the real clock) — ⭐ THE PUSH LANDED (run 7, origin 08037a1e82 → 8871d31514, 121 cages green) and BOTH RAILWAY BUILDS ARE LIVE since 09:06:21Z (the watermark tools/vocab/fub-mint-has-signal.watermark stamped at that instant — uncommitted, rides tomorrow's push). POST-DEPLOY SO FAR: PUT /api/inngest both apps ✓ · web functions 59→61 ✓ (chat-outbox-drain, kc-weekly-report) · CRM 174 = correct (no new CRM function since the base; the old "174→175" line was wrong) · the drain's first prod runs (09:07Z, 09:08Z): pending 71 anonymous/bench, projected 0, no legacy row projected since deploy; 1,358 legacy rows carry SKIPPED_LEGACY_IMPORT; ⚠ 446 legacy events were projected BEFORE the fix → DML card (backup, park the second copies) · prod probes: / 200, johnson-county 200, bare HLID 308 → slug URL, SRG 200 · mirror-docs green · the KC force run RAN (kc-weekly-report success 09:08:42Z; the row regenerated, headline "KC Metro Holds 7,542 Active Listings at 2.7 Months Supply") · Keren's replay RAN and was REFUSED by design (fub-webhook-router → fub-lead-import: "duplicate real event … ever means ever", classified tour_requested) → carded: her card files through the writer once the atomic mint (G) lands · fub-mint-has-signal --cage GREEN (gated 0 after the watermark; run it WITH --env-file — without the env it prints "NOT checked", never a pass) · judge-prod-walk-0911 VERDICT RED on 4 rows, no rollback (.claude/reports/the-fix-stream/VERDICT-prod-walk-2026-09-11.md): 2d the home count 7,544 vs the cited 7,263 (Zach's call 44197e70) · 3d the county chat 1,872 "across Johnson County" vs the page's 1,959 (the tool expanded the county to 15 cities) → lane-county-tool-scope (Opus, c1d5721e) · 3f the strip's Chat-with-me pill still lands on "search didn't carry over" (the string road, carded for cutover) · 6b one first-landing dashboard turn zero showed the maintenance voice while readiness said available (carded; capture the wire). GREEN: SRG, the V5 band, the shelf card 780.0/780.0, the KC report 0.3 s, the F3.4 binding, the F1.6 subject read, the readiness fence. Radar: four county counts + a garbled article sentence (carded). Zach told twice (Telegram 3239, 3241). THEN: the walk's verdict → Zach; the day desk holds the Sonnet/Opus slots for Saturday's push (F3.5, F1.4b, F4.2b, F5.2–5.4 DDL, the C2 closing move). — PRIOR (labels ~+5 h off): THE PUSH LANDED (run 7): origin/houseloop-app 08037a1e82 → 8871d31514, 121 cages green (120 run, 1 out-of-area). Runs 1–6 fell, each on one cage class, each fixed at the desk: 41 arrival ratchet (live prod fires; signed 146→147→148→150 with attribution) · 51 activity harness (the re-proven receipt had to be COMMITTED by path — memory banked) · 77 landing anchor (an orphan receipt + MVP overrides) · the chat-readiness test still named the deleted converse/flow-answer routes → the desk put the readiness fence on the ssot road's ingress and hoisted the dashboard fence above the opener (a real hole: turn zero composed in her voice during maintenance), re-pointed the test, one live turn 200 (119 homes). POST-DEPLOY (background task): wait for both Railway builds → stamp fub-mint-has-signal.watermark at the deploy instant → PUT /api/inngest both apps → probe four prod URLs. THEN: Inngest function-list counts (web +2: chat-outbox-drain, kc-weekly-report; crm +1) · the first chat-outbox-drain run must log skipped 1449 legacy rows · the KC weekly force run (through Inngest) · replay fub.events 1836987 → the tour_requested card on lead de8dbcce · fub-mint-has-signal --cage · the SRG prod judge (Fable) · a 390 prod walk of / and a county page. The scraper-health.mjs set-aside edit restored (uncommitted, unknown author). EARLIER — F3.4 BUILT + TICKED (8e581904: lib/myli/conversation-binding.ts — webchat adopts by shape then ownership, the dashboard id is DERIVED dashboard:&lt;personId&gt;, no DDL; live turn 2 same id history 3, a second sitting 5→7; the dashboard double-filing closed by rule; arm 5 departed on measurement → F3.5 carded: the dashboard ledger backfill + the model window behind a second opinion; ONE projector for the dashboard road carded; the ownership-column DDL carded). actor-binding fell 37→25 (the lane folded eleven session→lead selects across dashboard-chat, opener, thread); the floor file reads 25 — RECORD CORRECTION: my commit e986e5830f says "36 → 28", the file it wrote is 25 (the message is stale, the file and the live count agree). Three cards from the lane: the cage misses session.user!.id (a blind spot), rung 1 conflates "no lead" with "lookup failed" (409 where 500 belongs), ActorInput needs a known-personId input for load-client-dashboard.ts:504. THE ONLY REDS LEFT are in lane-push-reds (Sonnet: pill-href-road, source-citation + route-map, stranger-engine tap targets, KC model seam, sentence-width via 1335). THEN THE LAUNCH CHAIN (~18:00 EDT). EARLIER — THE CAPSTONE: 107 cages + tsc ×2 ran; 20 reds triaged. GREEN AT THE DESK (committed): NUL byte (escape) · web-storage-gate re-seeded (20260911c) · signals-lock re-signed (1331 relays) · door-zero 649 · catalog-superset (1334 sort_order) · activity harness (five proofs re-run, 82 proven) · brain-reader 244→251 signed with per-file attribution · party-resolution 28 (the containment proof excepted, test-harness class) · landing-anchor 5 arms (23 v1.1 overrides, 3 stale removed, MVP ceiling 454→447, page regenerated) · unknowns-first (two docs written: myli-ssot-fold, website-ui) · arrival-boundary 146→147→148 signed with attribution (Keren's arriving tour signal → the ratchet exemption carded; Roger's zillow_message_received: 4 fires / 3 cards → a fire-without-card defect carded on the-foundation). ⚠ this ratchet measures LIVE prod fires — re-run at launch and attribute any further move before signing. IN LANES: lane-push-reds (Sonnet: pill-href-road, source-citation + route-map regen, stranger-engine tap targets, KC model seam, sentence-width via 1335 for the desk to apply) · lane-fold-conversation-binding (Opus, F3.4 + the 8 actor-binding sites; 17:00 rail). AT LAUNCH (regen from the record, no commit after): build.mjs --promote --commit → route-map build → check-doc-drift → re-promote drifted → check --quiet → launch. Zach's eight calls stand on the sitting. EARLIER — THE PUSH BLOCKER IS CLEARED: lane-tool-boundary extracted execTool (1,172 lines) into lib/search/agent-tools.ts + agent-turn-shared.ts; context-purity GREEN 0/0 with toolReach 35 printed; floor re-seeded 34→0 (064282e1e8); 53626b42 + 34362218 ticked; the band anchor landed (895962a9 ticked; the journey now reaches moves 0–1 and exposes the dynamic-pills wait + NUMBER_UNTRUE 77 vs 467 → qa-bench card). THE CAPSTONE is running (scratchpad/desk/capstone.sh → capstone.out: all 107 battery cages + tsc ×2). LANES LIVE: lane-fold-conversation-binding (Opus, F3.4 8e581904 — BUILT clean by 17:00 EDT or STOP). Sonnet slot held (no more hands in the runner on push day). THE LAUNCH (~18:00 EDT): quiet tree → 3× 200 on :3001 → prompt-size + no-scripted-line pre-flight → build.mjs --promote --commit → check-doc-drift → re-promote drifted → check --quiet → `git pull --rebase` → `git push origin houseloop-app` (the battery ~10 min) → the post-deploy checklist (PUT /api/inngest both apps + function-list counts; the chat-outbox-drain's first run must log skipped 1449 legacy_import rows; the KC report force run; the FUB watermark + the 1836987 replay + fub-mint-has-signal --cage; the SRG prod judge; mirror-docs; a walk of / and a county page on prod at 390). EARLIER — F3.3 + F6.1c BUILT + TICKED (c1013f54, 53c9caee: 38 prose blocks in tools/myli/prompt-bundle.json, promptBundleVersion pb1:dcf12514ff53 on both surfaces, prompt-size byte-identical; the dashboard road commits through the renderer; claim-refs floor 2→1 committed). TWO HARD FINDINGS: (1) ⛔ F3.4 THE CONVERSATION BINDING carded P0 — 11/25 live turns run conversation=none (every owner_dashboard turn: dashboard-opener passes an empty id); the ledger window (arm 2) waits on it; (2) ⛔ CONTEXT-PURITY IS RED 35/34 on agent-voice.ts:90 → contract/place.ts → inventory-snapshot.ts → prisma (a TOOL reach the cage cannot classify) → lane-tool-boundary (Opus, 53626b42 + 34362218: extract execTool into lib/search/agent-tools.ts, the cage gets TOOL_BOUNDARY, the floor re-seeds down) — PUSH DAY RAIL: BUILT clean by 15:00 EDT or STOP. The flow-judge move-0 anchor → lane-band-anchor (Sonnet, 895962a9: a data-hl on the V5 band button + the selector). Cards: WALKER_THREAD_DEAD (qa-bench), delete buildContractSystem (floor 7→2). FRIDAY'S PUSH cannot launch until context-purity is green. EARLIER — THE BIRTH REPLAY RETIRED on the ssot road (fcb09a7f ticked): conversationIsSsot() in chat-ledger.ts, both call sites skip by name, legacy replays; F1.4 proved (with a control) the immutable link ALONE attributes pending NO_LEAD outbox rows — no DDL. Memory banked: a backup taken by the mutating script is not a backup. LANES LIVE: lane-fold-prompt-bundle (Opus, F3.3 c1013f54 + F6.1c folded — the ONE prompt/voice bundle, the model window = the ledger projection, the owner-dashboard road onto the assembler/renderer/writer, the flow-judge selector re-anchored). Sonnet slot free — hold. EARLIER — F1.4 BUILT + TICKED (95dd6952: lib/myli/actor.ts + actor-contract.ts; the link is an append-only identity.linked ledger event with its evidence — no DDL; the journey proved history never rewritten (6 rows md5-identical, 6→12 by append); the outbox drain's leadIdFor folded onto the binding; actor-binding cage born at 36). Manifest row corrected + committed; C3 v1.1 delta written (the link as an event; a LEAD-BEARING ActorRef → F1.4b carded; hl_chat_actor_link DDL carded for next week with the roles). NO LANES LIVE — the Opus slot is free: next fold slices are F1.4b (lead actor), F3.3 (prompt bundle), F4.2b, F6.1c, the C2 closing move; F5.2–5.4 need the desk's DDL. EARLIER — the greeting reads the ledger (28defc22 ticked). DESK MEASUREMENT: the BIRTH REPLAY (replayVisitorConversations, website-chat-log.ts) reads the blob F1.6 stopped authoring → an ssot conversation replays nothing when its visitor becomes a lead; the outbox's pending NO_LEAD rows + F1.4's link are the C2 road → carded, and lane-fold-identity notified to prove it in its journey arm. (dashboard-chat/route.ts:435 is NOT a blob reader — it reads the CRM person story.) LANES LIVE: lane-fold-identity (Opus, F1.4 95dd6952). The Sonnet slot is free; nothing spec'd and non-Myli remains — hold it. EARLIER — F1.6 BUILT + TICKED (50705b88: lib/myli/turn-writer.ts owns the ssot road's ledger appends + identity stamp + CRM filing; the blob is no longer authored on ssot_v1 — live blob 0 / ledger 5; ssot-reachability 3→1 and authority conversation.ledger 3→1, re-seeded + committed). RULINGS: the direct CRM intake STAYS (it carries applyConversationMove + requestMemoryProcess — the C2 closing move carded: move them onto /api/internal/chat-events); C3 (resolveActor/linkActorToPerson) does NOT exist yet → F1.4 spawning (Opus, 95dd6952) — load-bearing for identity AND the guest-lead binding; the anonymous-greeting lastChatSubject regression → Sonnet (28defc22). LANES LIVE: lane-fold-identity (Opus, F1.4) · lane-greeting-ledger-read (Sonnet). FOLD STATE: F0–F2.3, F3.1, F3.2, F4.1, F4.2, F5.1, F6.1, F7.1, F8.1, F1.6 ✓ · live F1.4 · open: F3.3, F4.2b, F5.2–5.4 (desk DDL + PARK DML), F6.1c, F2.4, the C2 closing move, F9.x. EARLIER — the nav border folded (bfdc94e3 ticked: nav.top === sheet.bottom, 780/780). The LEAD_SUMMARY_SLOTS shrink STOPPED honestly: three CRM surfaces hand-copy DIFFERENT subsets of the five → blocked on zach: (the sitting now carries EIGHT calls; brief re-promoted 08:33 EDT). A pre-existing web vitest alias failure (@/lib/search/agent-embed) carded. LANES LIVE: lane-fold-persist-split (Opus, F1.6 50705b88). The Sonnet slot is idle by choice — nothing spec'd and non-Myli is left; F4.2b and F6.1c are Opus-class and queue behind F1.6. DAY DESK (Zach awake ~08:00 CT): his eight calls · F5.2/F5.3 DDL with the STRUCTURE_GATE interview + the PARK DML with a backup · Friday's push. EARLIER — THE DRAIN ORIGIN FILTER BUILT + TICKED (6d54a3f6, e22b274934): legacy_import rows are skipped by name and marked SKIPPED_LEGACY_IMPORT; MEASURED 1,449 pending legacy rows across 224 conversations — FRIDAY'S PUSH CHECKLIST: the first chat-outbox-drain run on prod must log "skipped 1449 legacy_import rows" (or the live count) and project ONLY ssot_v1 rows; read the CRM hl_messages count before/after. LANES LIVE: lane-fold-persist-split (Opus, F1.6 50705b88) · lane-crm-slot-polish (Sonnet: the LEAD_SUMMARY_SLOTS floor shrink + the nav border fold bfdc94e3). EARLIER — F8.1 BUILT + TICKED (16bd356d: the ssot-reachability cage born at the measured 3 — 2 via persist-conversation.ts, 1 via dynamic-pills.ts; nothing new died, the ability to grow a new hatch died). THREE MEASURED REFUSALS RULED: (1) persist-conversation.ts is the ssot road's turn-completion writer (assistant.answered appended INSIDE it) → F1.6 SPLIT carded and spawning (Opus); (2) death by permission is BLOCKED on a prerequisite the plan never named — ONE superuser login role on both DBs → decision card: mint least-privilege roles next week, then the REVOKE set; (3) geo-block.ts is ALIVE (a generator slices it) → the audit + second opinion CORRECTED and committed; memory instance banked. LANES LIVE: lane-drain-origin-filter (Sonnet, 6d54a3f6) · lane-fold-persist-split (Opus, F1.6) spawning now. FOLD STATE: F0–F2.3, F3.1, F3.2, F4.1, F4.2, F5.1, F6.1, F7.1, F8.1 ✓ · live: F1.6 · open: F3.3, F4.2b, F5.2–5.4 (desk DDL + PARK DML), F6.1c, F2.4 likeness, F9.x. EARLIER — F7.1 BUILT + TICKED (7733db5d: the web transcript reads the ledger — the blob had 11 divergent conversations, one repeating turns 1–6 verbatim; the CRM transcript already a resolveCard projection; detector C on projection-composer floor 4: two await the conversation_id column DDL, two are the agent dock). ⛔ FRIDAY BLOCKER FOUND: drainChatOutbox() has no origin filter — ~200 F1.3 legacy_import conversations sit PENDING and the first deploy would project them into the CRM twice → RULED: legacy_import never projects → lane-drain-origin-filter (Sonnet, 6d54a3f6); PUSH CHECKLIST gains: the first drain run prints its skipped count. Memory banked: a shrink-only floor is a CEILING — red-prove toward MORE. LANES LIVE NOW: lane-fold-escape-hatches (Opus, F8.1 v2 16bd356d — the reachability cage, the blob writer killed for ssot_v1, the F2.1 zero-reachability kills, the REVOKE table for the desk) · lane-drain-origin-filter (Sonnet). FOLD STATE: F0–F2.3, F3.1, F3.2, F4.1, F4.2, F5.1, F6.1, F7.1 ✓ · live: F8.1 · open: F2.1 (folding into F8.1), F3.3, F4.2b, F5.2–5.4 (desk DDL + the PARK DML), F6.1c, F9.x. EARLIER — F5.1 BUILT + TICKED (8890e02d: tools/myli/fact-catalog.json → three generated consumers; the write boundary at upsertFact; C8 v1.1 delta: assertFact lands with F5.2's DDL, unknown slots go to the REVIEW QUEUE (unregistered) never dropped, percent is F5.3's). Census re-seeded money_banks 29→28. The desk's nine uncommitted record files (floors, 1333, 1331 header, the generated alias map, kind-vocab-ignore, the contracts deltas, the KC second opinion) COMMITTED by pathspec — script edits are NOT autosaved, only tool edits; check `git status` on desk-touched files every hour. F5.2 inherits the 53-row fold-map job → RULED: PARK with a backup, never delete (card filed). LANES LIVE NOW: lane-fold-projections (Opus, F7.1 7733db5d — the web transcript + the CRM transcript as ledger projections; the costume floor → 0 on those two). EARLIER — F6.1 BUILT + TICKED (1b94f39e + ad047194): claim-refs.ts + turn-renderer.ts the sole committer at the stream gate and the return; counts bind to their own scope's snapshot, prices to the card's hlid; C10 v1.1 delta written (the citation half deferred by the charter's zero headroom — binding:"resolved" until F9.1; turn-renderer.ts ratified; PERCENT_REF refused until F5.3; two committers floored at 2: the owner-dashboard route → card F6.1c, legacy dies 9/15). The flow-judge myli-consumer journey's selector is STALE (button[aria-label^="Ask Myli"] matches nothing on the V5 home) → qa-bench card. ZACH'S MORNING BRIEF SENT (Telegram 3230): the two clicks + five calls, all on /boards/morning-brief. LANES LIVE NOW: lane-fold-fact-catalog (Opus, F5.1 8890e02d — no DDL; the fold-map UPDATE sizes come back for the desk's F5.2). NEXT: F7.1 (projections stay stupid) or F3.3 on the Opus slot after F5.1; F5.2/F5.3 DDL at the desk with the STRUCTURE_GATE interview (day desk, Zach awake). EARLIER — THE MORNING BRIEF REGENERATED + RE-PROMOTED (/boards/morning-brief): the Morning Sitting carries SEVEN calls blocked `zach:` with the desk's recommendation on each — the Apify subscription (75df21d7) · the DataForSEO signup (708c1413) · the home-page count's status set (44197e70) · the DRAFT listing wireframe's seven questions (0acce8dd) · the master-sheet trio (2f41e653) · the SEO dashboard wireframe (b52577b9) · the instant-report preview card (698d2523). The mechanism: `task.mjs block --on "zach: …"` → `tools/pm/reconcile.mjs` (writes drift.json + regenerates + re-promotes) — a block alone does not reach the board. The SEO hub links + the FAQ number source BUILT + ticked (578921ee, a0cb73bf; a THIRD stale source, geo-content.generated.json, carded as a decision). LANES LIVE NOW: lane-fold-claim-refs (Opus, F6.1). NEXT: Zach's plain-English Telegram brief at ~06:30 EDT (or when F6.1 lands) · then F3.3 / F7.1 on the Opus slot. EARLIER — F4.2 BUILT + TICKED (9254d153: turn-status.ts, the enum-only status frame, status-copy.ts, the status-fsm cage; the "Couldn't re the result" composer never existed — the model rephrased a handed sentence, cured structurally). C9 v1.1 delta written: scope reaches the client via the snapshot frame (never a sentence on the wire); tool-status.ts retires as F4.2b (delete-by 9/15); failure phases are TURN outcomes. LANES LIVE NOW: lane-fold-claim-refs (Opus, F6.1 1b94f39e + ad047194 — THE RENDERER as the sole committer, COUNT/PRICE/PAYMENT/VALUE refs, the bare-number cage; closes F4.1 §6b and VOCAB_TRUTH W-1/W-2) · lane-seo-hub-and-faq (Sonnet). FOLD STATE: F0 · F0.2b · F1.1 · F1.2 · F1.3 · F2.1b · F2.2 · F2.3-mech · F3.1 · F3.2 · F4.1 · F4.2 ✓ — open: F2.1 kill, F3.3, F4.2b, F5.1–5.4 (desk DDL), F6.1 (live), F7.1, F8.1, F9.x. NEXT: the morning brief board regen ~05:00 EDT. EARLIER — the dead 9/07 generation DELETED (ed16e76c ticked; the lane caught undeclared-pill.mjs SCOPE naming two of the files — memory banked: a delete census runs from the repo root, tools/ included); pill-nowrap floor re-seeded 395→390; two duplicate cards closed (bd060a0d ticked, dfdb97cf retired on the 8/08 ruling); MEMORY.md trimmed to 12 KB (36 lines → index_archive_2026_09_11). LANES LIVE NOW: lane-fold-status-fsm (Opus, F4.2) · lane-seo-hub-and-faq (Sonnet: 578921ee the KC hub links both ways · a0cb73bf placeFaqs + the frozen narrative read the ONE resolved row). The search-demand 503 (fb6419ce) needs an authenticated measurement — day desk. NEXT: the morning brief board regen ~05:00 EDT. EARLIER — THE SIGNED-IN HOME (bc89a4d0) TICKED on the Fable audit: the premise no longer holds — anon/active-buyer/past-client render the SAME MyliShowcaseCard at 390 and 1440 on HEAD and prod (the 12:07–12:51 CT fold 353f66f2fe closed it; prod since ~17:00 CT); if Zach still sees the old strip it is a stale tab. The dead 9/07 generation (ReturningStrip/ReturningHome/ReturningAskBar, zero importers) → lane-delete-returning-gen (Sonnet, ed16e76c). LANES LIVE NOW: lane-fold-status-fsm (Opus, F4.2 9254d153) · lane-delete-returning-gen (Sonnet). NEXT: the morning brief board regen ~05:00 EDT (Zach's two clicks · the five estates · the DRAFT wireframe + seven questions · the alias defect fixed · the coming-soon ruling · the home-page count DECISION card · F-slices landed F0→F4.2). EARLIER — F4.1 BUILT + TICKED (1b2bb68a): resolveInventorySnapshot() at lib/market/inventory-snapshot.ts; the chat's resolve_place 495→465 = the city page's fingerprint; no live number moved. COMING-SOON RULED (C7 v1.1 delta in the contracts doc): #257.466 (4) binds her PICKS; the pages keep the 6/17 default and say "for sale or coming soon"; excludeComingSoon is STATED per scope; the label derives from the set (F6.1's COUNT_REF rule); the home page's active-only accident is a DECISION card, not re-defaulted. F6.1 first move carded (the search_homes tool quoted its own 1,872 over a page citing 1,950). The two desk-only floor shrinks re-seeded (authority cage, ssot census). LANES LIVE NOW: lane-fold-status-fsm (Opus, F4.2 9254d153) · audit-signed-in-home (Fable, bc89a4d0 spec). NEXT: the audit's spec → a Sonnet lane · the morning brief board regen ~05:00 EDT. EARLIER — THE SHELF-CARD RADAR: 3 of 4 BUILT + ticked (aea9c7cb the empty band → adaptive peek height; 088d8959 the seam → the grid pinned to the constant, residual −1px border carded; 35608b0d bare /home/HLID resolves + 301s); 55ab8f29 the green dot RETIRED on the record (Zach 2026-08-08 locked the status-dot colour in pill-class.ts; the brand doc the card cited governs the dashboard product). The SRG carousel/pill/duo items ticked against lane-srg-carousel's BUILT receipt (397c6218 F2.4 likeness stays open — only the SRG half landed). The two affordability-pill wrap items (e167b51b/36a37f5a) sit on the LEGACY road (myli-legacy registry) — not worth a lane before cutover. LANES LIVE NOW: lane-fold-inventory-snapshot (Opus, F4.1) · audit-signed-in-home (Fable, design/spec only: bc89a4d0 "the Myli section / hero renders crushed after sign-in", Zach live 9/10) → its file:line spec feeds the next Sonnet lane. NEXT: F4.2 the status FSM (Opus) when F4.1 lands · the morning brief board regen ~05:00 EDT. EARLIER — THE KC PROJECTION BUILT + TICKED (6c88fec9/1d3faaa4/0d2e3aa2: the page reads only the stored row via React cache(); the generator lives only in the Inngest fn with 3 attempts + a RED throw; America/Chicago weeks; the guard catches $500K/500k/$1.2M; 57/57 tests). Its lane published a 2m28s broken HEAD by deleting an export before switching the caller — briefs now spell the ORDER as steps (memory instance 3). LANES LIVE NOW: lane-fold-inventory-snapshot (Opus, F4.1 1b2bb68a) · lane-shelf-card-radar (Sonnet: aea9c7cb empty band · 088d8959 the 1.4px seam · 55ab8f29 the green dot · 35608b0d bare /home/HLID 404). Two duplicate radar records I added by mistake were removed surgically from floors.json (the judge had already filed aea9c7cb/088d8959). NEXT on a free slot: F4.2 the status FSM (Opus) · the morning brief board regen near 05:00 EDT · Friday's push checklist gains: PUT /api/inngest + the first Monday cron fire (or a force event through Inngest) as the KC schedule's only proof. EARLIER — F3.2 BUILT + TICKED (55543a69): buildPlacePageModel() in lib/myli/page-context.ts, the signed page token (no store), PAGE_DIVERGENCE live, page-context-parity cage born at 0; / declares headerGreeting null (no rendered count — accepted as the honest arm); the route's pageContext STRING deliberately alive for the legacy consumers → carded for cutover. LANES LIVE NOW: lane-fold-inventory-snapshot (Opus, F4.1 1b2bb68a — the 8 WHERE shapes → one resolver; inherits snapshotsFrom() from F3.2) · lane-kc-weekly-projection (Sonnet, the KC page as a projection of Monday's row). NEXT on a free slot: F4.2 the status FSM (Opus) · the morning brief board regen near 05:00 EDT. EARLIER TONIGHT — ⛔ LIVE DEFECT FOUND AND FIXED AT THE DESK: migration 1331's alias re-point (registration → portal_registration_received) also re-routed the WEBSITE's fallback birth kind (catalog.ts REGISTRATION_KIND = canonicalEventType('registration') → birthKindForAction → process-intake regAction = action ?? 'registration'): on HEAD a website registration without an action filed "registered on Zillow" (lane-harness-and-purity proved it with a test fixture). The gate's lookup is SYMMETRIC through the alias, so the re-point was never needed. FIX: 1333_registration_alias_restored applied (~03:50Z), event-kinds.generated.ts regenerated (back to origin's value — prod never affected), 1331's header corrected; proofs registration_completed (reshaped as the website road + a negative arm) and portal_registration_received both GREEN; c1dbf01c ticked. The context-purity rescope (34362218) is BLOCKED on 53626b42 (extract execTool out of agent-voice.ts after F3.2 — the boundary is two regions of one file). LANES LIVE NOW: lane-fold-page-snapshot (Opus, F3.2 55543a69) · lane-kc-weekly-projection (Sonnet, cards 6c88fec9/1d3faaa4/0d2e3aa2 per the ChatGPT-adopted design). NEXT on a free slot: F4.1 the inventory snapshot (Opus) · the morning brief board regen near 05:00 EDT. — PRIOR: THREE LANES LANDED 21:53–22:02 EDT: the shelf-card judge GREEN · F3.1 BUILT (65b2796b ticked; the four rulings answered: C5 v1.1 delta written in the contracts doc [run(ctx, legacy) with a named dated bag, C11 persona next week, messages fold at F3.3]; the two C8 discriminants added to tools/schema/kind-vocab-ignore.txt; the context-purity floor RE-SCOPED by ruling [tool-layer reaches are informational, card 34362218]; the 51-s broken HEAD banked as the required-prop clause) · the FUB kind proofs BUILT (fa34a66f ticked; registration_completed proof is a STALE GREEN → card c1dbf01c) · the wireframe redraw #3 re-promoted DRAFT (sha 2db604f57251), Zach told. KC CACHE: ChatGPT second opinion captured + receipt (.claude/reports/seo-revamp/SECOND-OPINION-kc-weekly-cache-2026-09-10.md): the page is a PROJECTION of Monday's stored row; generator only in Inngest with 3 attempts; cards 6c88fec9/1d3faaa4/0d2e3aa2 → the NEXT Sonnet lane after lane-harness-and-purity frees. LANES LIVE NOW: lane-harness-and-purity (Sonnet: c1dbf01c + 34362218) · lane-fold-page-snapshot (Opus: F3.2, 55543a69). CLOCK CORRECTION: the :3001 restart landed at 21:54:14 EDT (the process table), not "22:10" — I wrote an estimate instead of the measured time and a lane chased it; write the measured time from the task output, never an estimate. The 9222 bridge hung on a zombie GOOGLE CONSOLE tab (closed via /json/close after probing each page ws; cdp-probe.mjs). — PRIOR HEADER: :3001 RESTARTED CLEAN at 22:10 EDT (pid 76881; the KC report REUSES at 1.2 s — the lane's diagnosis held). THE COLD PATH EXPOSED THREE KC DEFECTS, carded on seo-revamp (desk-raised): (1) generateMetadata + the page body both call getKcWeeklyReport() → TWO concurrent model calls per cold load; (2) PROSE_NUMBER_NOT_IN_PAYLOAD 500s the visitor (29 s, the model wrote 3650); (3) the reuse check rebuilds the payload from LIVE data, so any drift (hash 3689d580→33efffbe in 30 min) makes the next visitor pay a 20 s model call — the page should project the cron's stored row. ChatGPT second opinion in flight (q-kc-weekly-cache.txt); build it on the Sonnet slot when the proofs lane frees. The shelf card judged GREEN (ba6ba0ef ticked); Zach told. LANES LIVE: lane-mobile-listing-wireframe (Fable, redraw #3) · lane-fold-assembler (Opus, F3.1) · lane-fub-kind-proofs (Sonnet, fa34a66f). — PRIOR HEADER: ⚠ PENDING: a CLEAN :3001 RESTART (the Prisma client was regenerated at 21:36 EDT AFTER the 20:21 boot — the running server cannot read hl_seo_artifact_log.payload/prose; the KC report page shows GENERATING every time until the restart) — do it the moment judge-mobile-shelf-card reports (it is mid-walk on :3001), wait for the old PID, then re-curl /kansas-city-market-report (expect REUSED) and notify lane-fold-assembler with the timestamp (its live turn must be after the restart). The listing wireframe (redrawn) is on the board as DRAFT; the KC storage swap is BUILT (three-run proof). — SINCE 21:15: F1.3 COMPLETE (4403/4403 · unreconciled 0 · ruled 14 — 24 diverging positions all ASSISTANT turns: the replay regenerated Myli's side; the ledger wins) · F2.2 + F2.3-mechanical ✓ · the FUB intake fold ✓ (1330/1331/1332 applied; the board did not lie — bool_or over two false variants; Zach's row was General Inquired) · the KC Weekly Market Report BUILT (storage DDL 20260911c applied: hl_seo_artifact_log payload/prose/body_hash; the lane swaps its cache for the row now) · ZACH 21:15–21:30 EDT: the mobile listing shelf-card REGRESSION (#257.495 → lane-mobile-shelf-card, Opus, restoring the Apple glass from git; a Fable judge after) + the REDESIGN candidate in Zillow's shape (#257.496/.496a → lane-mobile-listing-wireframe, Fable design-only: the thin action bar always, the HouseLoop menu on scroll-back, the iOS bar under it; the top header swipes to the next listing). LANES LIVE: shelf-card (Opus) · listing wireframe (Fable) · KC weekly storage beat (Sonnet). NEXT: the Fable judge on the shelf card → F3.1 on the Opus slot → the harness proofs for the two FUB kinds on Sonnet. — FRIDAY'S PUSH CHECKLIST (the FUB intake fold): after deploy → set tools/vocab/fub-mint-has-signal.watermark to the deploy instant · replay fub.events 1836987 through the router (inn.gs `fub/webhook.received` &#123;event:'eventsCreated', resourceId:1836987&#125;) · confirm the tour_requested card on lead de8dbcce… carries source_mapping_id=fub_general_inquiry_zillow_relay_tour · run fub-mint-has-signal --cage · PUT /api/inngest both apps (new fns: chat-outbox-drain, kc-weekly-report) + the function lists 59→60/61 and 174→175 · the two new kinds' harness proofs BEFORE the push (carded). 1330 + 1331 APPLIED tonight. — LANES LIVE (night mode + Zach's one-lane exception): lane-fold-backfill (Opus, F1.3 b725fe95 — the 4,336 conversations; the origin CHECK admits legacy_import, mig 20260911b applied) · lane-fub-intake-fold (Opus, ded5dbc3 + dc678771 — THE FUB INTAKE FOLD per #257.494; it will hand the desk HOLD_ DDL files to SIGN: activity provenance columns, run-log decision columns, the review-queue table) · lane-fold-bundle-consumers (Sonnet, F2.3 mechanical: TOOLS export, the five flow-id maps → bundle reads, tool maps typed, the optional contractBundleVersion). F2.2 BUILT + ticked (contract-bundle.v1.json + the --check cage). ⭐ TONIGHT SO FAR: F1.1+F1.2 THE SPINE BUILT + ticked (outbox in the event's tx, the one CRM receiver + projector, fold-invariants.mjs; the drain's Inngest RUN is unproven until Friday's deploy registers it — dev :3001 was restarted cleanly by the desk, PUT ok, the cloud list is prod's 59) · SEO 6C + 2B + 3B BUILT + ticked · the boards fold beat 1b BUILT + ticked; the card split DONE (seller-engine card minted, home-value-report renamed MY HOME VALUE) · THE FROZEN TEN: 6/10 → value stays ninja, Apify adopted for identity + Realtor-first photos (#257.492); Apify OFF · KEREN AGUILAR (Zillow, 50570) hunted down: the registration toggle was OFF at the gate (three homes for one toggle — carded), her tour request was a Premier Agent relay swallowed by the junk drop (two relay rows added), she is lead de8dbcce… (7:12 PM CT) — rulings #257.490/.491; the whole FUB intake estate is with ChatGPT (chat 6aa34858-ef10-83e9-8973-04dc9497574e, capture running → .claude/reports/the-foundation/SECOND-OPINION-fub-intake-2026-09-10.md); ZACH: 'just fix it and implement the fix chat gpt outputs along with your logic' → the desk adopts + spawns the intake fix lane the moment the capture lands (a one-lane exception to night mode, his direct order) · market.knowledge = the eleventh authority (F4.4, #257.489) · LANES LIVE: lane-fold-bundle (Sonnet, F2.2). NEXT OPUS SLOT: F1.3 the backfill (4,336 conversations) once the intake fix is spawned. ⭐⭐ NIGHT MODE ON (#257.487, Zach signed off 'night night'): cap = the desk (Fable) + 1 Opus + 1 Sonnet; /loop all night; Telegram only for a question/blocker; the foreman's Fable spawn orders (qa-bench, myli-os) are OUTRANKED tonight by the fold + the overnight bar. THE BAR by morning: boards organization DONE (lane-seller-boards-fold-2 Opus running: beat 1b + MAJOR BUILD blocks; then the desk's card split) · SEO 6C (lane-seo-6c-dashboard-2 Sonnet running) then 2B/3A/3B/the KC weekly on the Sonnet slot · the master rounds 12+13 stand LOCKED (builds carded behind the fold) · wave A started (the desk DDL first, then the Opus slot → F1.1/F1.2 the spine). APIFY: the 20:01 EDT frozen-ten run is the LAST discretionary spend; after it NO Apify calls until Zach starts the subscription. MORNING BRIEF hands Zach two clicks: start the Apify subscription · DataForSEO signup (6B). Earlier header follows.  (⚠ THE MACHINE CLOCK IS EDT — `date` prints EDT; every 'CT' stamp the desk wrote tonight from the shell is EDT, one hour ahead of Zach's Kansas City time; the 00:01 UTC Apify run = 20:01 EDT = 19:01 CT, waiter PID 72929 alive) — ⭐ ACCOUNT SWITCHED at the session limit (~19:21 EDT → zach@houseloop.ai, session 01A6WiVfHa1dvgNEsUqT3ZAr): three lanes died mid-beat and were landed/resurrected by the desk — F0.2b vector floors COMMITTED by the desk (76e34732ac) + ticked · SEO 6C page (711 lines, tsc 0, autosaved) → lane-seo-6c-dashboard-2 (Sonnet) finishing eslint/301/walk/receipt · boards fold beat 1 BUILT (promote.mjs --kind/--stamp/--survivor; 1 LOCKED + 12 LEGACY) + ticked → lane-seller-boards-fold-2 (Opus, 1aa20076) applying Zach's settled stamps (#257.483/.485) + the seller-engine rename + the #257.486 MAJOR BUILD block. lane-home-value-master-sheet RUNNING round 13 (#257.484 headshot on white, the 8/28 address flow folded in, the print/email view). ZACH ANSWERED EVERY QUESTION (#257.482–.486). NIGHT MODE (#257.481) not yet signed — he said 'ready to attack'; running at capacity until he says go. (earlier note: my stamps read ~2 h fast — a CT/UTC flip the sell lane caught; the times in the rulings #257.472–.481 are ~17:10–18:10 CT) — ⭐ THE NIGHT PLAN IS `.claude/state/lee-handoff.md` (Telegram msg 3216 sent 18:12 CT with the game plan + 4 questions; NIGHT MODE #257.481 = 1 Fable + 1 Opus + 1 Sonnet after Zach says go). LANES LIVE: lane-fold-vector-floors (Opus, F0.2b) · lane-fold-kill-now (Opus, F2.1b) · lane-seller-boards-fold (Opus, 35af207e) · lane-home-value-mechanism-doc (Fable, d6178b8e) · lane-sell-home-value-4 (fires 00:01 UTC). BUILT + ticked tonight: F0.1/.2/.3/.5, the audit, the audit→ChatGPT, the pill-nowrap cage (floor 395), the boards census; BUILT awaiting a prod judge after Friday's push: SRG carousel/pill/headshot (ca921f67, 44297d60), round 12 (b935f173, Zach locks). 7d3588a4 duo motion BLOCKED on Zach (measured live on prod). ⭐⭐ F0 OF THE MYLI SSOT FOLD IS IN HEAD: `.claude/rules/MYLI_AUTHORITY_MAP.md` (the law, ENFORCED BY) · `tools/myli/authority-manifest.json` (TEN authorities now — `likeness.assets` added under #257.475/#257.480: one active headshot via /api/myli/avatar + a POSE CATALOG by name, never a path on a surface) · `tools/vocab/myli-authority-cage.mjs` (imports + INSERTs + LITERALS arms; selftest 3 plants/4 controls GREEN; wired in pre-push.sh area any; floor `tools/vocab/myli-authority.floor` = 83 — re-baselined 50 → 83 for the literal arm's scope growth, reason in commit ac60101f19) · `THE-MYLI-CONTRACTS-V1-2026-09-10.md` (C1–C10 frozen; `appendChatEvent()` is the writer, not the plan's `recordEvent()`). THE AUDIT LANDED (`AUDIT-myli-ssot-2026-09-10.md`: 38 duplications, six diseases, nine open questions; linked on the card). #257.476: the audit + contracts went to ChatGPT (chat 6aa331de-f4b8-83ea-92c0-406c8220a13e; `capture` running → `SECOND-OPINION-myli-ssot-audit-hardening-2026-09-10.md`, then the four-heading receipt + plan v3 edits BEFORE wave A spawns ~06:00 CT Fri). ZACH'S EVENING RULINGS FILED #257.472–#257.480: the selling consultation is MYLI'S CONVERSATION (step 1 blue card, step 2 picker dimmed; address → got it → improvements/condition/loan with rather-not-say) + multi-property cap 5 → lane-home-value-master-sheet ROUND 12 (design only) · A PILL NEVER WRAPS → lane-pill-nowrap-cage (Opus, the cage; 291b1cbb) + the SRG pill's phone copy "Get started with Myli" · Myli's likeness ONE URL (above) · the SRG duo photo's locked MOTION missing · the home value master is LOCKED (#257.478) + ONE BOARD PER SELLER ESTATE + THE CARD LAW v2 (#257.479: one PRD + checklist per major build; wireframes LOCKED green / LEGACY yellow; intake asks REDESIGN/ADDITIVE/NEW) → lane-seller-boards-census (Fable; 79c91466) writes BOARDS-CENSUS-AND-FOLD; the desk applies the fold. lane-srg-carousel carries the carousel + pill + headshot + duo motion (one BUILT). ONE PUSH A DAY: nothing launches tonight; tomorrow's push carries F0 + these. STILL QUEUED: 00:05 UTC "run the frozen ten" to lane-sell-home-value-4 · the owner-greeting docs paragraph · the brain-map drift re-promote before the push · the review-queue residual cards. Prior lock text follows.

PREVIOUS: 2026-09-10 13:45 CT — ⭐ ZACH IS READING THE MASTER SHEET LIVE and firing micro-edits; SIX rulings filed and batched to lane-home-value-master-sheet for ROUND 3 (record 672ea8999c): #257.443 home card first, Myli UNDER the pictures · #257.444 the 15-year line is grabber-blue with green ▲ / red ▼ markers, no green line (footer Q1 closed) · #257.445 ONE STRUCTURE every state — value-only graph (equity + search demand OFF it), Your home records on every state, the MASK not micro-unlocks (Q3 closed) · #257.446 masthead → pictures → Myli at every width · #257.447 bottom order neighborhood → records → equity → thinking about selling · #257.448 the node-line stepper ships REFINED (CRM stepper pixel language; carded as a build). PUSH #23: run 42 fell at the ACTIVITY HARNESS (3 kinds re-proven 3/3, receipts f4d858c82c); run 43 fell at FACT-EVIDENCE (5 &gt; floor 4: four Probe test-band facts whose writers fill source_activity_ids but leave evidence_activity_id NULL — quarantined, reversal ~/hl-reversals/fact-evidence-probe-quarantine-BEFORE-20260910T184010Z.json, twin-column root CARDED on myli-os; the r10 Morgan control fixture left active on purpose); RUN 44 IN FLIGHT (log scratchpad/desk/push23-run44.log). ⚠ the ai_searched harness proof RE-CREATES the derived Probe fact every re-proof — until the twin folds, quarantine it again before each launch. RECORD TODAY: ticked 4A e9162c5a, 5B bf31eb0e, 2C 183bde2e, 8A 6634790c, 2D fece69cf, the /sell 4 s 976262fb (R31 §1 — receipt lives at .claude/reports/grinder/home-value-report/RECEIPT-sell-home-value-2026-09-10.md, rounds appended); 11 cards filed (e2456291 /sell reads the HELD value first + Ninja off the seller road → lane-sell-home-value-4 Opus LIVE · 6f8c592d 5A sold side unreachable for metro/county → lane-seo-5a-soldside Opus LIVE · d9aa4f16 listingWhereFor + the listDate ::date sweep → lane-seo-5b-where Sonnet LIVE · d6be3f1e 4A.6 size is a listing-grid/flight item · 570d3d3f 2C.4 AFTER DEPLOY: GSC submit + DELETE the 11 retired /sitemap/N.xml (desk's hand, outward) · 7a8b6672 2C.5 neighborhoods with 1–2 homes: desk-ruled same floor as 2B · 44edbdfc SHOW_ALL_PARITY snapshot drift · 87ee949d the CRM files the conversation TWICE (array-position key → hl_chat_events.id; ChatGPT ASKED, answer NOT yet captured — conversation c/6aa2f773…; run `desk-chatgpt.mjs capture` then write the SECOND-OPINION receipt) · 21866836 the phone step 7.7 s (birth + Twilio on the critical path)). LANES LIVE: grind-myli-os-3 (Fable, 075d1774, real-person hold mode, switch OFF until Zach) · judge-qa-bench-r11 · lane-home-value-master-sheet (round 3) · lane-owner-myli-greeting · lane-sell-home-value-4 · lane-seo-5a-soldside · lane-seo-5b-where. tsc houseloop-web = 0 at 18:25Z. The morning-brief board's SEO row note rewritten in tools/pm/major-builds.json (uncommitted; regen + promote after the push lands). QUEUED CARDS (next record beat, after run 44 exits): /sell route's remaining ~1.3 s = market stats + geo + the GSC demand read in one Promise.all (R32 arm A 1465/1341/1359 ms; the value half now 0 calls) · the stored-vs-live Zillow gap (15.4 % on one real home, 493,153 vs 427,125) with no "as of" — the Zillow columns carry no timestamp: a copy + timestamp question (DDL at the desk) · lane-sell-home-value-4 step 2 RULED: the live fallback = the Apify ADDRESS LEG only (2.4 s) behind a coverage gate measured after 00:00 UTC (adopt at ≥ 7/10), the details actor stays async, Ninja's seller-road call site retires; the pair measured 10.1 s median (2385 + 7646). lane-seo-5a-soldside corrected: shape (a) (90-day figures from month rows) is OFF — medians do not sum; (b) real trailing_3mo rows at metro/county or (a') recompute from closings. 17:25 CT — ⭐⭐⭐ THE MYLI SSOT FOLD IS THE WEEKEND (Zach #257.468/#257.471/#257.471a): the CARD is minted (`myli-ssot-fold` in initiatives.json + major-builds.json; the plan promoted at /boards/myli-ssot-fold; the checklist at /boards/master-schedule-myli-ssot-fold; 29 slices on floor 42's "THE MYLI SSOT FOLD" band; 39 absorbed myli-os items retired under #257.471/#257.471a; MVP 489 → 454, ratcheted). THE PLAN v2 (`THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md`) = the second opinion's architecture + its plan review adopted whole: the weekend = THE WEBCHAT AUTHORITY CUTOVER; F0/F0.5 frozen contracts (desk, serial, TONIGHT) → F1 webchat spine + the outbox/projector (Opus ×2 + desk DDL, Fri AM) ∥ F2 registry (Sonnet) ∥ F4/F5 contract impls → F1B cutover → F3 the one assembler → Sat: F5 memory, F6 claims (dangerous classes), F7 Monday's two projections, F8 death by permission/import → F9.0 Fri deploy (expand) · F9.1 Sat deploy (migrate, behind the ONE ingress switch legacy_responder↔ssot_v1) · F9.2 Sun invariants + four journeys pass×2 → cut over; rollback = flip the switch (#257.455a emergency exception). NEXT WEEK BY NAME: SMS, ids, generalized derived values, broad CRM cleanup, destructive DDL, F4.3 polish, universal claims, tools + the Academy. THE DESK'S F0 TONIGHT: .claude/rules/MYLI_AUTHORITY_MAP.md · tools/myli/authority-manifest.json · the moratorium cage · the frozen contracts (a contracts doc + the branded types' stubs) — then spawn wave A at ~06:00 CT Fri. LANES LIVE: lane-myli-ssot-audit (Fable; §2's count feeds the plan), lane-seo-5b-writer, lane-srg-carousel (Sonnet, ca921f67), lane-home-value-master-sheet round 11 (the consultation: no notice sentence, near slots read as taken 9–7, the home required before the picker). 16:30 CT — ✅ DEPLOYED (both apps SUCCESS at t+390 s; prod smoke: / 200, /kansas/johnson-county 200, /sitemap/geo.xml 200, CRM 307). POST-DEPLOY DONE: Inngest re-synced both apps, proven by the FUNCTION LIST (CRM 174 fns incl. seo-nightly; web 59 incl. market-stat-compute, seo-facet-inventory-nightly, seo-index-monitor, geo-market-stats) · the market-stat writer PROVEN ON PROD: market-stat/manual.run → hl_run_log success 21:10:32→21:21:22, 21,417 rows written through the six-column ON CONFLICT (tomorrow's 0 11 * * * is safe) · 2C.4 DONE: the 11 retired /sitemap/0..10.xml DELETED from GSC (204 ×11, backup ~/hl-reversals/gsc-sitemaps-BEFORE-20260910T211848Z.txt), seo-index-monitor submitted the 16 family shards (run success, childrenOk 16) and GSC FETCHED 12 of 16 within a minute · morning-brief regenerated + re-promoted (fab417847c). ⚠ Inngest v2 `/events/<id>/runs` returns 500 "Unable to fetch event runs" tonight — hl_run_log is the proof road. LANES: grind-myli-os-r2b3 BUILT (orders 1/3/5 done, 2 page-honest, 4 RED on two measured tools/** blockers: the walker has no cookie arm + needs a held draft) → judge-myli-os-r2 (Fable) LIVE, round 2 of 2 — the desk batches after · judge-qa-bench-r13 RED (fold 2: the WIRE still carries "Connect me with a lender" at dynamic-pills.ts:72 + "a lender handoff" at :92) → grind-qa-bench-r14 (Sonnet) LIVE · lane-seo-5b-writer (Opus) LIVE · lane-home-value-master-sheet round 7 BUILT (fc97d0f95d), idle. TWO CARDS from myli-os r2: the intent gate's ai_master read on the dispatch road (send-safety, UNVERIFIED) · the wire recorder overwriting decision_reason (proof arms ⑧/⑨). RADAR from judge-qa-bench-r13 (desk items): her prose says "Before you talk to a lender" at 390 · debt chips under a yes/no ask at 1440 · pill-anon journey's scripted arrival string. TONIGHT'S QUEUE: 00:05 UTC → message lane-sell-home-value-4 "run the frozen ten" (coverage + photo table, ~$0.53) · the docs paragraph (owner greeting) → guides/houseloop-home-value.mdx · the desk's DDL for conversation_id (STRUCTURE_GATE interview) · the brain-map drift re-promote before tomorrow's push. 16:10 CT — ✅✅ PUSH #23 LANDED: run 54 went 105/105, 631471b5ef..3d44ab4a7a on origin (runs 48–53 each fell one cage later: MVP floor → mid-edit board drift → route-map page → the web DDL floor (re-seeded for the SIGNED facet_slug DDL) → stale schedule pages → SHOW_ALL_PARITY (the two mover pills read a 600 s cache while the destination counts live → they read live now, e14328956b) + a dynamic-pills.test.ts type error (homes not hlids)). Railway BUILDING both apps (watcher bjtvnq3y1: waits for SUCCESS SUCCESS then smokes prod). POST-DEPLOY, IN ORDER: (1) `curl -X PUT https://crm.houseloop.ai/api/inngest` + the web app's PUT (new functions: the whole-place nightly, the 5A sold-side pour) — proof = the function APPEARS in /v1/apps/&lt;app&gt;/functions; (2) the market-stat writer's proof: tomorrow's 0 11 * * * cron must succeed (write.ts's six-column ON CONFLICT is live now) — or invoke `market-stat-compute` once via the Inngest dashboard tonight and read the run; (3) 2C.4 (570d3d3f): run seo-index-monitor's submit for the 16 family shards AND delete the 11 retired /sitemap/0..10.xml from Search Console (the desk's hand, outward); (4) the docs paragraph from the owner-greeting receipt → guides/houseloop-home-value.mdx. RESURRECTED after the push: grind-myli-os-r2b3 (Opus; orders 1/3/4/5 + page-honest order 2) · lane-seo-5b-writer (Opus; 498a3219, the facet writer through local Inngest) · judge-qa-bench-r13 (Fable; ee25436e). #257.462 (the month only in the cover stamp) → round 7 applying now. 15:10 CT — ⭐ ZACH'S SELLER-TOOL REDLINE filed as #257.456–.461 (2451cfb7bf) and sent to lane-home-value-master-sheet as ROUND 6: masthead = candidate B in LIGHT BLUE, one thin band, no "Monthly" eyebrow, toggle out of the phone masthead · ONE Myli shape on every state (state 2 = the dashboard's big shape) · the trend line loses the ▲▼ markers, ONE green "+" equity figure beside Today/You bought (#257.444 revised) · equity = today − paid, a real number; the loan balance lives ONCE under Thinking about selling, the timing pills become the section's own question · never "locked": every masked section carries an "Unlock to see …" button opening Road A, Myli's closing line explains them once; Thinking about selling + the consultation never masked · ROAD A IS IN-CHAT (four chat moments; the code a big inline tool), no modal (#257.442 revised). RUN 48 fell at the LANDING ANCHOR (MVP 488 &gt; 468): the afternoon's 24 desk cards filed under v1.1 by item override (MC-57 default), the duplicate wireframe-bands row removed, the owner-greeting build carded + ticked under its true item (its receipt cites 7508d4cf, the intake key it was spawned under), county tiles ticked (1a03a601), the projection copy committed LAST (the post-commit hook regenerates it after every floors commit — commit it after the last record write), 5 arms green at 466 ≤ 468. RUN 49 CHAIN IN FLIGHT (log push23-run49.log) — still today's ONE push (#257.455; reason: the dead market-stat writer). grind-qa-bench-r13 BUILT ee25436e folds 2+3 (judge brief qa-bench-judge-r1-1789070234390.md — spawn the Fable judge AFTER the push lands, never beside the battery: run 47's OOM). QUEUED CARDS: three test reds it found (contract.test affordability rounding vs afb33be661 · myli-state ARM B carry count vs R24/#257.372 · controls.test credit slider #257.269a(b)) · the Redfin gallery parser gap · #257.453's build (after a second opinion). lane-seo-county-tiles-2 BUILT + ticked. 14:45 CT — ⚠ SESSION LIMIT + MEMORY KILL: the account switched (admin@houseloop.ai, session 01K65wGgRNzbswPJexitNJFE) at 19:21Z; the limit killed lane-seo-5b-grain-2 (the facet WRITER, 83 rows into the metro level; its compute/types/write.ts edits were committed at 15:15 CT + a small uncommitted diff), lane-seo-county-tiles (at the shots step; autosaves 15:15–15:16), grind-myli-os-r2b2 (orders 1/3/4/5 PENDING, order 2 partial, arms ⑧/⑨ left red on purpose; receipt 1789067727-receipt-r2.md). Run 47's chain was killed by the OS (low memory: the :3020 next-server had grown to 4.4 GB) inside cage 62 — the desk restarted :3020 cleanly (waited for the old PID; 6 GB free), unstaged the launcher's forced-restore index, and fired RUN 48 (log push23-run48.log) as today's one push. RESURRECTED: lane-seo-county-tiles-2 (Sonnet) · grind-qa-bench-r13 (Opus, ee25436e the capability registry — the foreman's order; census first). TO RESURRECT AFTER RUN 48 EXITS (their files are in the launcher's set-aside stash until then): grind-myli-os-r2b2 (from its receipt, order 2 re-cut to page-honest per the desk's ruling) · the 5B facet writer (from RECEIPT-seo-5b-grain §12+, Inngest proof through the local dev server). 14:40 CT: RUN 46 fell at the NUL-BYTE cage (two source files carried a raw NUL as a key delimiter: real-person-hold.test.ts:228 — the desk's own Edit rendered ` ` as a raw byte — and conversation-evidence-contract.ts:215 from 07:35Z); both re-encoded as ` ` escapes (same string at runtime, text stays greppable), tsc both apps gated, RUN 47 CHAIN IN FLIGHT as today's ONE push retried (reason under #257.455: the dead market-stat writer must reach production before tomorrow's 11:00Z cron; log push23-run47.log). NOTHING launches after run 47 until the evening. 14:25 CT: ⭐ #257.455 ONE PUSH A DAY (Zach): lanes stay on localhost, the desk fires the battery ONCE at the end of the day (~18:00 CT); run 46 (cage 62 when ruled) finishes as today's push; NOTHING launches after it until the evening; exceptions need a written reason. THE GOOGLE CLICK, RESOLVED: signed in to the console as admin@houseloop.ai via the secure-login tool (the vault typed CRED_GOOGLE_CLOUD_CONSOLE_HOUSELOOP_PASSWORD; the desk never saw it) on project houseloop-496200 (753242193019). The three Business Profile APIs the code uses are ENABLED (performance, account management, business information); the REVIEWS live in the legacy My Business API v4, which is HIDDEN from the project — the console page fails to load for the owner and the service account gets "Permission denied to get service"; only Google's Business Profile API access form grants it. Places API (New) is ALREADY ENABLED and carries rating + userRatingCount + 5 reviews with publishTime → CARDED as the reviews road (6A follow-up). Ana's GBP login is NOT needed. lane-seo-county-tiles (Sonnet, 1a03a601) LIVE. d9aa4f16 ticked (the DATE-vs-timestamp sweep closed, 67/67). 14:10 CT: ✅ CENTERBEAM RESTARTED by the desk (Zach authorized "now"): ALTER SYSTEM dynamic_shared_memory_type='mmap' + Railway deploymentRestart 51b61182… on houseloop-website-db (928d67cf…) at 19:01:22Z; up since 19:01:40Z, dsm=mmap, parallel=0 (leave off until measured), Listing 377,799; backup ~/hl-reversals/centerbeam-settings-BEFORE-20260910T190044Z.txt. ⛔ THE GOOGLE CLICK IS AT THE PASSWORD PROMPT: local Chrome on :9222 (PID 47765, Zach's Default profile) is SIGNED OUT of every Google account; the desk chose zach@spradlingrealtygroup.com on the chooser and Google demanded a password — never typed, by law. The tab sits at accounts.google.com/…/challenge/pwd in his Chrome; the moment he types it, `node scratchpad/desk/gcp-click.mjs click` finishes it. RUN 45 fell at the COSTUME DETECTOR (207 &gt; 206): the site was load-lead-feed.ts:1054 `meta->>'conversation_id'` in WHERE (the funnel lane's pull-forward that rode into b8bdc96d0a) → filtered in JS now, conversation_id column CARDED; the real-person-hold probe likewise. RUN 46 CHAIN IN FLIGHT (costume + tsc gates first; log push23-run46.log). judge-myli-os-4 RED (narrow: Approve shows one screen for sent vs refused; 390 unusable; the retired fixture; no catalog card) → grind-myli-os-r2b2 (Opus, round 2 of 2) LIVE on the r2 brief. QUEUED CARD (next beat): Redfin PHOTOS cannot come through our client — parseRedfinRecords drops the gallery (RedfinEstimate = &#123;address, value, url&#125;); the ladder cannot consume a "Redfin first" photo table without a parser change; the #257.454 census reads the raw record directly (one-off, not a pattern). The 00:05 UTC run is WIRED (b6b7580546): coverage first and banked, photos second, "-" = UNMEASURED, ~$0.53 / 32 runs against the 40 cap. QUEUED CARDS (next record beat): a catalog kind for the human's review decision (judge finding 4) · Mission Control's testBandOnly default hides real people until ?band=all · #257.454 photo-source table (in the 00:05 UTC run) · the two lane-sell-home-value-4 instrument lessons → memory. #257.453 THE ARRIVAL MODAL sent to lane-home-value-master-sheet as ROUND 5 (design only; the build needs a second opinion first — Myli conversation change). 14:05 CT: lane-sell-home-value-4 STEP 2 BUILT behind the gate (live-zestimate.ts owns the provider; LIVE_VALUE_PROVIDER="ninja" until coverage ≥ 7/10; route.ts no longer imports Ninja; 16/16 + 5/5; R32.6 at 952078b241). NIGHT-SHIFT ORDER: at ~00:05 UTC message lane-sell-home-value-4 "run the frozen ten" → zestimate-present/10 + timings → the desk flips the provider in a commit carrying the number (or STOPs below 7). Apify cap now 46/40 (5 were R32.1's). CARD TO FILE: zillowZpidByAddress parses PropertyZPID inline, untested — extract to a pure function (small, another lane's file area). ZACH 13:55 CT: Q2 answered (#257.450 the building state is NOT a screen → four states) · Q4 answered (#257.451 plain sign-in unlocks the whole page from anywhere; Google on both roads) · #257.452 the masthead gets a real design pass (2–3 candidates on the sheet, Zach picks) — all sent to lane-home-value-master-sheet for ROUND 4 with #257.447/.448. ZACH AUTHORIZED THE DESK to do the reviews-API console click and the centerbeam restart himself-less ("you can control my computer… do whatever you need"): the Chrome extension is NOT connected (tabs_context refused at 18:55Z) and the 9222 bridge is chatgpt.com-only, so the click waits for a connected Chrome or his own one click — NEVER type a Google password. THE RESTART WINDOW is the desk's: at ~03:00 CT tonight, `ALTER SYSTEM SET dynamic_shared_memory_type = 'mmap'` on centerbeam then a Railway service restart (~30 s website DB blip; parallel plans stay off until measured); backup = the current postgresql.auto.conf reading printed before. DataForSEO does NOT block the build (6B's adapter builds against the docs with a stub; the key is the last step) — Zach signs up when ready. Speed answers given from R29/R31/R32: HLID + card ~1.4 s (held home), live value ~2.6–4 s, Apify address→zpid 2.4 s, details (photos + zestimate) 7.6 s, the full ladder incl. Redfin 13–23 s; the report page does not yet re-render when the ladder lands (card 6db38f6c). Zach still owes: the masthead pick (round 4), DataForSEO signup, the reviews-API click if Chrome stays disconnected. Prior lock text follows.

PREVIOUS: MORE QUEUED CARDS (home-value-report): the Google sign-in road cannot reach the phone-CODE step (callbackUrl=/auth/complete-profile; the claim branch unreachable; AuthGateModal discards nextUrl) — build item behind the master sheet (#257.442) · Redfin $273,607 vs our $202k–$232k range with no explanation on the seller's dashboard (copy/design for the master sheet) · the beat copy 'Myli sees the mobile number on your account' ninety seconds after the account was born (copy) · the cap refusal is silent (being made loud by lane-sell-home-value-3). F3 IS BUILT: address → report → claim → code → signed in → /my-houseloop with the home (8/8 at 390). QUEUED RECORD BEAT (after run 40 exits — never a record write mid-run): tick 1A (f9cabe31) with the 2a receipt · card: the bench lease must cover a persona born through the PRODUCT'S registration road (a lane walking /register creates newly-registered as a side effect; no bench script consults the lock) — a chat-road lease for test-band identities or per-run cloned personas · card: the two slot vocabularies (registry RECITED_SLOT vs on-record SLOT_WORDS disagree on '$20k down') — ONE_ANSWER_TABLE · card: the audit's 1A.4 receipt line counts zip children (correct the line to 'zero links to the BARE city path') · card: 4 test personas on the 'website' source resolve a real handoff partner (already filed) · the /dev/shm restart-window fix for centerbeam (Zach). 2026-09-10 16:05 CT — ⛔ ZACH #257.441 (14:00 CT, three screenshots saved): the SHIPPED owner dashboard's Myli leaks the buyer funnel's memory (down payment / income / credit / affordability, four pills, an upgrade-math push) and the 390 band scrolls with the page; the three seller wireframe boards sprawled past what he can hold. TWO FABLE LANES SPAWNED: lane-owner-myli-greeting (fix at the root: the owner greeting reads the HOME's story only; ONE paragraph → THREE big numbers → ONE closing line → ≤3 pills; the band in the flow at 390) and lane-home-value-master-sheet (ONE master document, four states — signed-out instant · signed-in locked · signed-in unlocked · blank/no-address — his desktop layout: blue HouseLoop card + Zillow + Redfin, range left / 15-year graph right in a white outlined card with blue under the line and a GREEN trend line, no second HouseLoop number; Myli always at the top, never asking to sign in; slug home-value-master-2026-09-10; the three old boards retire behind it). lane-home-value-preview-wireframe STOPPED. PUSH #23: run 38 in flight (37 fell at door-zero — three 'door' words in seo-nightly comments, reworded). Runs 30–37 each fell at a different cage, every one at the root (brain-map drift ×2 → the launch chain ENDS with drift-check→re-promote→launch; ledger gap 1325/1327; a test lead on a production-partner source; the schedule cage; door-zero). BUILT since the switch: SEO 1A closed (+ the two KC pages were misclassified 'static' — fixed), 6A ticked (composed run), 5B batch resolver + the UNCUT landmine disarmed (facet_slug DDL applied 368da211f9), the sell lane's Apify-vs-Ninja coverage+latency table (a profile = $0.10, Redfin 78%; the number takes ~4 s cold AND warm = the route's own live call; the ladder lands 13–23 s later and the modal does not refresh), the dispatcher race + loud refusal ordered. Zach owes: the one-page/master-sheet sign-off, the reviews-API click, DataForSEO. Prior lock text follows.

PREVIOUS: 2026-09-10 15:45 CT (ACCOUNT SWITCHED 17:20Z after the session limit killed 7 lanes; new session 01LaacGrz6CxYZMkcri8LYKx) — ⭐ RESURRECTED on the new account: grind-qa-bench-r11 (Opus; r10 RED on the fourth authority — hl_affordability_estimate as a derivation that outlived its parked inputs; ChatGPT turn 18 adopted: read-time recomputation, one shared verdict for pills, per-segment slots + id-collapse in the mirror, a round LEASE on bench personas — brief amended), lane-sell-home-value-3 (Opus; owes Zach's Apify-vs-Ninja coverage+latency TABLE, then un-gate Redfin + the re-entrancy fix, then F3 = SignInModal at the phone step after the claim (#257.436), then F4), lane-seo-2a-inventory-2 (Opus; 1A close via the geo_city contract + cityPath, then 2C), lane-seo-6a-nightly-2 (Opus; the credentialed-step proofs), lane-seo-4a-home-2 (Opus; the home page in the law's order), lane-seo-5b-grain-2 (Opus; batch resolver + place×facet grain + 5A.7), lane-one-voice-funnel-2 (Fable; FIRST the push blocker: transition-road.ts:47 raw read → readChatEvents (brain-reader 245&gt;244), then #257.436 identity, then acceptance A–I). Still live from before: lane-home-value-preview-wireframe (Riley re-pinned to HLID100795; re-shooting the dashboard board's left column). PUSH #23 status: run 29 fell at cage 13 (brain-reader +1, the funnel lane's raw read) — re-fire run 30 the moment lane-one-voice-funnel-2 messages the sha; runs 23/25/26 died on launch races (dirty files at the pull step) — the launcher retries 4× with 25s gaps now; QUIET WINDOW protocol: lanes commit by pathspec 5 min before, no writes for 4 min around launch. DESK INCIDENT 17:11Z: my own probe carried a bare `git stash` (62 files reverted ~1 min, popped clean) — the ban I wrote an hour earlier; lanes notified. BUILT TODAY (not pushed): SEO 2A/2B/1A/5A/6A(partial)/8A+2D; the signed-in home band (one band, time-aware greeting, pill flip, node-line stepper, picks fold, 'Welcome back' never 'I'm Myli'); hero copy; mig 1325 (provenance columns), 1326–1328 (SEO tables); the home value engine on two off-market homes via Apify only (Ninja retired on the seller road), existing-row road; the funnel steps 1–6 + the CRM two-card split fixed (pagination). ZACH SIGNED: the SEO dashboard wireframe (#257.439) → DataForSEO signup steps given; the seller wireframes (instant + dashboard) await his 10 questions + the re-shot left column; his one click owed: enable the My Business reviews API (console URL in #257.435 correction). :3001 torn-manifest lesson banked (wait for the old PID; never a hot restart). Prior lock text follows.

PREVIOUS: 2026-09-10 15:55 CT — ⭐ PUSH #23 RUN 20 IN FLIGHT (log scratchpad/desk/push23-run20.log). Run 17 cage 53 (costume 208&gt;206) ROOT-FIXED: mig 1325 hl_messages.origin+content_kind promoted out of metadata (666b17783f; writer logMessage, reader readThread, 670 rows promoted, sentinel 33/33, costume 206=floor). Run 18 fell on my own interview header (parenthetical before the colon; 3f3273f047). Run 19 fell on cage 9 board drift from judge-myli-os's GREEN tick (master-schedule ×3 + brain-map re-promoted, 0ad6676257+). #257.423 ONE VOICE + two receipts committed (df93dcc3b3). ⚠ RULE: any grinder tick or board rebuild during a run reddens cage 9 — re-promote + re-fire, never raise. FLOOR (all holding apps/** + tools/** until 'battery done'): lane-one-voice-funnel (Fable, brief desk-one-voice-funnel), judge-qa-bench-r9 (Fable), lane-sell-home-value-2 (Opus; 3 yeses sent: .env.local token line, accept 10011 W 95th, /my-houseloop walk; veto ships as ordered), lane-hero-and-room-turn (Sonnet; job 2 DONE fireplace turn 21/0 verified; job 1 hero measured 32px@390 / 86px@1440), lane-seo-2a-inventory (Opus; SERVED SET confirmed ~18,400/night; DDL 2026-09-10-seo-page-inventory.sql written — DESK applies + web-schema-lock in one commit AFTER the push lands). judge-myli-os-3 GREEN (f6627842 ticked; 2 residuals batched: relTime ratchet arm, stack under prior-day turn). NEXT after push: 'battery done' to all → apply seo DDL → spawn grind-myli-os 075d1774 (Fable, foreman order) → S-30 re-park sequence → file QA-barrage cards. Prior lock text follows.

PREVIOUS: 2026-09-10 14:40 CT — ⭐ P0 ROOT-FIXED (37bcecc136): the Row 2 send failure was the W-2 price-parity guard convicting 10006 W 91st STREET over the correctly priced 9127 W 91st TERRACE (street-name-only anchor), no repair path on the streamed road → blank answer; fixed (house number + street; whole-token digits; every terminal null names itself; [nlp] TURN_FAILED reason= phase=; floor = reason code); 194 tests; 3 of 3 live; lane-chatgpt-benchmark re-driving Row 2 on video as the independent PASS. FOUR LAWS SIGNED TODAY from second opinions: #257.413 ATTRIBUTE LAW (catalog live 2ad9d6e3e9; slice 1 building) · #257.415 ROOM LAW (hl_listing_room + median benchmark with spoken n; Repliers imageInsights per-photo room class never stored — free; no vision spend; lane-room-law building A+B) · #257.416 GUARD LAW (PASS|CONVICT|ABSTAIN; conversation evidence projection from hl_chat_events — she cannot repeat last turn's prices; lane-evidence-corpus building) · #257.417 FACT ADMISSIBILITY (value support not citation; 134 active facts on 21 REAL leads cite only Myli's own voice → S-25/S-26 SIGNED: guard first then park + validator over all actives; grind-qa-bench-r8 building). Judge r7 RED (own-voice facts; Riley recites $12,250 laundered from her own sentence). Push #23 HELD until attribute-slice1 is out of agent-voice.ts. Browser: ONE tool desk-chatgpt.mjs; project view reached via sidebar; 8 stray chats still to move (Sonnet card). Running: attribute-slice1, room-law, evidence-corpus, grind-qa-bench-r8 (all Opus), lane-chatgpt-benchmark (Fable walker), lane-memory-drive (Opus). Prior lock text follows.

PREVIOUS: 2026-09-10 12:20 CT — ⭐ #257.413 THE ATTRIBUTE LAW (Zach: every askable fact structured for Myli; ChatGPT architecture adopted: hl_attribute_catalog + hl_listing_attribute + observations, room as a dimension, resolver→constrained grammar→deterministic SQL, subdivision as an entity, nightly UNREACHABLE ASKABLE FACTS, zero new tools). lane-attribute-slice1 (Opus) building slice 1 (catalog seed from 143 vision keys + feature arrays, backfill, resolver, search_homes.attribute_filters); the desk applies its DDL + regenerates web-schema-lock in one commit. Measured: neighborhood = MLS subdivision (11,313/2,850 distinct); the `subdivision` column is dead (4 rows) — corrected in the record. THE BROWSER: ChatGPT's PROJECT page errors on a direct URL and renders via the sidebar click; a crashed target hangs connectOverCDP — ONE desk tool now: scratchpad/desk/desk-chatgpt.mjs health|ask|capture (memory written + indexed); chats are born inside HouseLoop Engineering; 8 stray chats still to move (Move-to-project submenu returned 0 matches — UI changed, carded for Sonnet). Running: lane-send-failure (Fable, P0), lane-memory-drive (Opus), lane-attribute-slice1 (Opus), grind-qa-bench-r6 (PART 2 re-run), lane-brief-gate (fixes). Judge r7 held for r6's PART 2 receipt. Prior lock text follows.

PREVIOUS: 2026-09-10 11:30 CT — ⭐ ZACH'S MORNING RULING #257.410: test-ness attaches to the LEAD (disposition=test), no is_test on conversation rows; the vacuous control is an ADOPTION fact of beta, not a defect; the ≥622 web rows + 21-23 CRM rows are test-lead rows → desk's call (backup first); EVERY judge finding/regression/imperfect fix → ChatGPT ("what went wrong + next move"). Desk #257.411: traffic_class stamped ONCE at the conversation boundary FROM lead.disposition (or walker origin), immutable — carded, after P0. ⛔ P0 TODAY = THE SEND FAILURE: Row 2 (school rating) reproduced it on a PLAIN flow, verified send, turn 3 ("Worked for 15s" → "That one didn't come through"); measured mechanism: nlp/route.ts:2428-2446 answers floor:"guard-refused" whenever lib/search/agent-voice.ts returns null (26 null sites, none named on the wire). lane-send-failure (Fable) is hunting it with ChatGPT's order (terminal decision backward; TURN_FAILED reason= phase= elapsed_ms=; one constrained regeneration, never strip). Turn-7 second opinion captured + receipted (SECOND-OPINION-send-failure-and-test-disposition-2026-09-10.md; plan measured "plus"; the PROJECT view crashes the tab — ask at root, move after). DDL SIGNED: migration 1323 hl_lead_facts.status admits 'quarantined' (550769b062, lock regenerated) → r6 can now apply S-16 PART 2; r6 ordered to apply, measure, reword door-zero, and file a NEW round-7 receipt — the Fable judge is HELD until that receipt exists (the grinder currently points round 7 at the r6 receipt). Row 2's five findings carded; brief gate (issue-brief.mjs) live and used once: 2 true catches, 2 false positives, 1 digit/placeholder bug → sent back to lane-brief-gate. Push #23 still one door-word from firing. Prior lock text follows.

PREVIOUS: 2026-09-10 10:40 CT — ⭐ THE LEDGER EXISTS: hl_chat_events + hl_turns are LIVE in the WEB db (66706f51a4), dual-writing, NOT NULL enums with no defaults, schema lock regenerated; the history guard repairs the projection (only ever ADDS) and logs HISTORY_LOSS_COMMITTED_ASSISTANT by name; the $8,000 tap writes lead·ui_confirmation·structured_answer with body NULL — ZERO prose rows. Round 6 landed provenance across the CRM wire as a REQUIRED authorKind (fail-closed; binary → three-enum alignment is round 7); S-16 SIGNED but NOT YET APPLIED (judge measured 22 of 23 still 'lead' at ~10:45; r6 asked to run or report) for the 21-23 test-band rows — ⚠ and the is_test control is VACUOUS: 527/527 inbound rows are test leads because NO REAL BUYER HAS EVER USED THIS SURFACE (adoption, not safety). Row 4 PASS-with-three on video; Row 2 driving. SRG LIVE (was missing _railway-verify TXTs, not DNS). Push #23 one comment-word from firing (door-zero, route.ts:49/56, with r6). Playwright cache deleter was OUR 04:00 cron — line removed. Prior lock text follows.

PREVIOUS: 2026-09-10 00:50 CT — ⛔ TWO WAYS THE ONE STORE STOPPED BEING A RECORD, both found tonight,
both with the write STOPPED or SPECCED and the historical data UNTOUCHED pending Zach's signature.

1) FABRICATED BUYER TESTIMONY — **≥622 rows across 255 conversations** where role:"user" holds a sentence
   the PRODUCT composed ("About $8,000 a month before taxes"). ⚠ THE NUMBER IS A TYPE, NOT A SCALAR:
   622 is EXACT for the four templates of producer `describeMoneyCommit`; coverage is complete for that
   producer, NOT system-wide; two more producer paths are known and unaudited (controls.ts MIX — WIDER,
   adds credit score + monthly comfort; MyliMoneyAnswer.legacy.tsx). The desk relayed the judge's
   explicitly-labelled FLOOR of 245 as a bare total and it hardened across four messages.
   ⚠⚠ **WITHDRAWN 2026-09-10 01:25 — "A SECOND LIVE PRODUCER KEPT WRITING" IS FALSE.** MIX is gated OFF
   (`MYLI_CONTROLS` absent from every `.env` in the tree AND from the running process; both gates need
   `==="1"`), and the legacy card is excluded from the other side by `NEXT_PUBLIC_MYLI_NEXT=1`. The likely
   truth: ONE producer caught MID-HOT-RELOAD — the modal is a dynamic import, a loaded chunk runs until
   refreshed, and **eight Fast Refresh rebuilds landed in exactly that window.** Not proven either, but the
   claim REQUIRED a second live producer and the only candidate is measured off. ⚠ **The desk relayed the
   withdrawn claim to Zach as fact before it fell.**
   ⚠ **AND THE INSTRUMENT TRAP: `ps eww` IS NOT AUTHORITATIVE FOR NEXT ENV VARS** — Next loads `.env.local`
   into `process.env` itself. Check the FILES and the PROCESS; never a file mtime.
   **FOURTH DORMANT MECHANISM FIXED TONIGHT** (guidedGreeting ×2, the legacy twin, MIX): *"'this code emits
   the bad string' is cheap to establish and 'this code RUNS' is expensive, so every census stops at the
   first question."* **S-13: every producer census now carries a REACHABILITY COLUMN — LIVE / FLAG-GATED
   (naming the flag, checked in the .env files AND the process) / DEAD (initializer checked, not setters).**
   ⭐ **THE REAL BLOCKING FINDING IS S-12, verified twice:** `website-chat-turns.ts:41-50` reads `role`,
   `content`, `hidden` — and has **NO FIELD for `actionReceipt`**. `turnsFromTranscript` is the wire to the
   CRM's fact extractor, so a product-composed receipt arrives at `extract-context.ts` **as literal buyer
   speech** — a flag respected only by its author, at the seam that WRITES FACTS ONTO A LEAD. ⛔ Do NOT fix
   it by dropping the row: the CRM must still know the buyer confirmed a number, and must know who said it.
   **THE WRITE STOP ITSELF STANDS (both producers fixed), 2026-09-10 00:28 and 01:2x.** The desk reported it stopped; round 3
   proved otherwise. Conversation `e38679ef`, born **11 minutes AFTER the fix**, carries BOTH shapes in one
   thread: debts and down payment produced the new receipts, **monthly_income produced the old fabrication**
   — its own internal control. `describeMoneyCommit` is NOT at fault (it handles income correctly); the live
   income card is served by a DIFFERENT producer, and two candidates emit the byte-identical string
   (`lib/myli/controls.ts:234` — live MIX wiring, the likelier — and `MyliMoneyAnswer.legacy.tsx:96`).
   ⚠ **THIRD TIME TONIGHT FOR THIS SHAPE: a census enumerated what it could SEE, the fix was scoped to
   those, the class was wider.** Round 4's S-7 orders a census **BY STRING, not by producer.**
   ⚠ **AND THE ACTOR IS STILL WRONG on the fixed road:** the receipt rows still store `role:"user"` — the
   fix changed the wording and added a flag, not the actor, so every `role`-filtering consumer is unchanged.
   The flag is *"respected only by its author"* — the round-1 `provisional` shape again. Consumers move
   FIRST; the `COMMITTED_SAID` tails are load-bearing and must not change until they can.
   PARTIAL WRITE STOP (one producer of at least three): now emits `✓ Confirmed …` as an action chip, threaded through the RESTORE
   path too. The ≥622 rows are UNTOUCHED — remediation is designed (#257.405 / the mis-attributed-records
   receipt) and needs Zach + a backup.
2) THE RETRY PATH ERASES HER ANSWERS (data loss, by census not reproduction): route.ts has NO req.signal
   observation, so the server completes and persists when the client has gone; persistConversation REPLACES
   rather than appends; the outbound body filters `synthetic` but NOT pending:"failed"; `Ask again` sends a
   NEW message. A give-up over a completed turn writes the buyer's line twice and erases her first answer.

THE LAWS SIGNED TONIGHT (all in .claude/reports/myli-os/ZACH-REVIEW-RULINGS-2026-08-27.md):
· #257.393 one-author turn contract — one reducer → deterministic state-owned UI → ONE generative author.
· #257.394 "everywhere" is not ticked by narrowing it — bf3304cd is a PARENT of four roads.
· #257.399 the address veto is REVERSED — geometry loses identity authority; normalization does the work.
· #257.400 a raw-SQL reading of a RENDERED number is a different instrument.
· #257.401 a false word about a true set is a SAY defect; a SEE fix makes the product worse.
· #257.402 THE_SECOND_OPINION now binds EVERY DESK TURN, and asks about what the DESK DID.
· #257.404 SYMPTOM-LAYER ANCHORING — file the symptom where observed, route to the first boundary where
  state becomes wrong; "UI-only" forbidden until the renderer is shown to have received correct state.
· #257.405 turns have durable identity independent of client attachment (persist but mark UNPRESENTED, and
  EXCLUDE unpresented answers from memory) + a measured value is never a bare scalar.

DESK ERROR RECORD TONIGHT, kept deliberately: SEVEN orders named mechanisms nobody had verified (all seven
caught by the receiving lane, zero at the desk, two signed by a judge) · FOUR numeric errors from one habit
(relaying a figure without re-deriving what produced it) · one defect routed to the wrong layer entirely.
The retro audit (.claude/reports/myli-os/AUDIT-symptom-layer-2026-09-09.md) found EVERY miss came from
evidence with PIXELS.

PUSH #23: still UNBLOCKED, still NOT FIRED — 1,142+ commits ahead. ⚠ Do not fire mid-write; lanes are
writing. One RED test is NAMED AND NOT OURS: contract.test.ts:948, stale $5,000-rounding expectations from
afb33be661; owner is the audit-defects lane. PROD IS STALE — everything tonight is localhost-only.

BROWSER: 127.0.0.1:9222 is now a LOCAL Chrome on a COPY of Zach's profile (the ezra-tunnel LaunchAgent that
pointed it at Camp David is unloaded, backed up in ~/hl-reversals/). chatgpt.com ONLY on that browser.
ChatGPT project "HouseLoop Engineering" holds the consultations. Plan reads "personal" — NO model picker,
every consultation is default-routed.

ZACH'S OPEN DECISIONS: the ≥622-row remediation · abandon-vs-persist on client disconnect · the two-line
credit tile at every width · the spent-control residual · #257.390 vs a44c1588 · Apify (~$20/mo, buys
nothing until a consumer is built) · a paid ChatGPT tier · the 17s dead air.

## Related documentation

Doc sections this work creates or updates:

- [`guides/intent-workflows`](/guides/intent-workflows)
- [`guides/workflows`](/guides/workflows)
- [`guides/registration`](/guides/registration)
- [`guides/web-registration-and-intake`](/guides/web-registration-and-intake)
- [`guides/tour-and-contact-requests`](/guides/tour-and-contact-requests)
- [`fub/trigger-events`](/fub/trigger-events)
- [`fub/lead-import`](/fub/lead-import)
- [`platform/message-catalog`](/platform/message-catalog)
