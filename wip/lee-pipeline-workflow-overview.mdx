---
title: "Lee (Pipeline and Workflow Master Overview)"
description: "Work-in-progress dossier — Lee · Infra · Pipeline & Workflow · FUB · Notifications. Pipeline and Workflow Master Overview."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/lee/pipeline-workflow-overview.md` on every push. **Progress: 9/21 done (43%).** _Updated 2026-07-09._</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 482h ago. If Lee has shipped since, this dossier under-reports — refresh `.claude/state/wip/lee/pipeline-workflow-overview.md`.</Warning>

**Owner:** Lee — Infra · Pipeline & Workflow · FUB · Notifications

## Checklist

**⭐ 2026-07-09 — WORKSTREAM ABSORBED into the reorganized docs.** The old canonical
`guides/pipeline-workflow-overview` was DELETED in the doc consolidation (redirects to /myli-master); the living
story is now the ONE "Pipeline & Workflow" nav group ordered **Lead Integration → Trigger Events → Workflows**
(`guides/web-registration-and-intake` → `guides/intent-workflows` → `guides/workflows`), with the FUB side in
**`fub/trigger-events` ("FUB Trigger Events & Notifications", the 22-event master)** and notifications in
`platform/message-catalog`. The checklist below is the historical build record of the reorg that shipped.

**North star (as-built):** HouseLoop's lead engine to MVP. ONE platform (HouseLoop, never named); everything is a
Lead Source with events; `Workflow = f(source × event)` for the registration family, one intent canvas each.
HouseLoop is the first-class notifier; Realloop retired.

### 🆕 ACTIVE WORKSTREAM (2026-07-03) — Lead-Detail Parity Lanes + Polymorphic Assignment
**PRD/SSOT:** `GT/GT - ACTIVE/GT_HOUSELOOP_LEAD_DETAIL_PARITY_AND_ASSIGNMENT_PRD.md` · **scope:** `session-scope-parity-lanes.lock` · **handoff:** `.claude/state/lee-handoff.md` · **prod baseline:** `98650ba504`
Both lead-detail lanes (LEFT = human + AI clone · RIGHT = Myli) share ONE skeleton: `📋 Tasks & Appointments`
(ALWAYS on) + `🤖 AI Workflow` (gated). Grouped polymorphic `Assign to` (Agents · AI Agents · TCs). Clone
task-level override (clone-assigned task fires without global AI mode).

**SHIPPED (pushed, deploying):** Phase 0 docs/PRD (3 new + fixes) · Phase 2 mig 0306 (clone actor_type) ·
Phase 3 grouped assignee picker · Phase 4 parity lanes (fixed the `aiActive` gate that hid human
tasks/appointments — T&A now always-on both lanes; per-lane scoping + faint/mirror; bold toggle) · v2 Tags
under T&A · Myli 4-row Contact. Awaiting Zach's final live sign-off.

**🎯 THE GOAL — NEXT: Phase 5 (Zach-directed 2026-07-03), build end-to-end THEN test.**
(1) **Clone config UI** — surface `POST /api/team/clone` + `hl_member_role` persona/tools override in Team
settings so a member creates + configures their AI Clone. (2) **Task-level override DISPATCH** — a task
assigned to a clone actually FIRES the clone (eligible types only) without the global AI toggle, via
`hl_scheduled_actions.actor_type='clone'`, clone acting AS its human. (3) Model-row real value + Myli
iMessage-reassign → Team settings. (4) Live end-to-end test → Zach sign-off.

### 🧪 SIGN-OFF MATRIX — every webhook, top to bottom (Zach + Lee, iterate live)
**How we run it:** go row by row. Lee hands the **test-lead payload** for `localhost:3020`, Zach fires it, we **log
every gate**. A row **signs off (✅)** only when: (1) the webhook lands + the lead/activity card renders correctly,
(2) Creates-Lead behaves as set, (3) it's **wired to the CORRECT notification** (notify_class / notify_owner — this is
also checking **Benji's** notification work), and (4) its workflow (if any) fires. `⬜` = not yet verified.
_Source of truth: `hl_fub_webhook_settings` (FUB) · `hl_trigger_events` (HouseLoop, notify_class) — pulled 2026-07-02._

### A. Follow Up Boss webhooks (`hl_fub_webhook_settings`)  — all `notify_owner=realloop`, no workflow bound yet
| # | Webhook | Creates Lead | Notify → | Workflow | Result |
|---|---|---|---|---|---|
| A1 | `property_inquiry` | ✅ | realloop | — | ⬜ |
| A2 | `tour_request` | ✅ | realloop | — | ⬜ |
| A3 | `contact_request` | ✅ | realloop | — | ⬜ |
| A4 | `rental_inquiry` | ✅ | realloop | — | ⬜ |
| A5 | `seller_inquiry` | ✅ | realloop | — | ⬜ |
| A6 | `virtually_toured` | ✗ | realloop | — | ⬜ |
| A7 | `text_message` | ✗ | realloop | — | ⬜ |
| A8 | `in_app_message` | ✗ | off | — | ⬜ |
| A9 | `notification` | ✗ | off | — | ⬜ |
| A10 | `email_received` | ✗ | off | — | ⬜ |
| A11 | `property_viewed` | ✗ (counted signal) | off | — | ⬜ |
| A12 | `property_saved` | ✗ | off | — | ⬜ |
| A13 | `property_shared` | ✗ | off | — | ⬜ |
| A14 | `insight_high_intent_buyer` | ✗ | off | — | ⬜ |
| A15 | `insight_high_intent_seller` | ✗ | off | — | ⬜ |
| A16 | `insight_viewed_10x_7d` | ✗ | off | — | ⬜ |
| A17 | `insight_new_area` | ✗ | off | — | ⬜ |
| — | `_global` (master switch) | n/a | n/a | — | n/a |

### B. Phone-call webhooks (FUB → the Phone Numbers section)
| # | Webhook | Creates Lead | Notify → | Workflow | Result |
|---|---|---|---|---|---|
| B1 | `phone_call` (generic) | ✗ | realloop | — | ⬜ |
| B2 | `phone_call_company` | ✗ | realloop | — | ⬜ |
| B3 | `phone_call_sign` (sign/listing line) | ✗ | realloop | — | ⬜ |
| B4 | `phone_call_past_agent` | ✗ | realloop | — | ⬜ |
| B5 | `phone_call_ana` (Ana's old cell) | ✗ | realloop | — | ⬜ |

### C. HouseLoop webhooks (`hl_trigger_events`, platform `houseloop`)
| # | Webhook / event | Creates Lead | Notify class | Workflow | Result |
|---|---|---|---|---|---|
| C1 | New Lead — General Registration | ✅ | `lead_created` | ✅ | ⬜ |
| C2 | New Lead — Liked Home | ✅ | `home_liked` | ✅ | ⬜ |
| C3 | New Lead — Saved Home | ✅ | `home_saved` | ✅ | ⬜ |
| C4 | New Lead — Saved Search | ✅ | `saved_search` | ✅ | ⬜ |
| C5 | New Lead — Tour Request | ✅ | `tour_request` | ✅ | ⬜ |
| C6 | New Lead — Contact Request | ✅ | `contact_request` | ✅ | ⬜ |
| C7 | New Lead — Seller Get Report | ✅ | `home_value_report` | ✅ | ⬜ |
| C8 | New Lead — Seller Get Started | ✅ | `seller_request` | ✅ | ⬜ |
| C9 | New Lead — Shared Home | ✅ | `home_shared` | ✅ | ⬜ (now in `hl_trigger_events`, mig 0287) |
| C10 | System — Property Viewed | ✗ (counted signal) | `property_viewed` | — | ⬜ |
| C11 | System — Property Liked | ✗ | `home_liked` | — | ⬜ |
| C12 | System — Property Saved | ✗ | `home_saved` | — | ⬜ |
| C13 | System — Property Shared | ✗ | `home_shared` | — | ⬜ |
| C14 | System — Saved Search (client) | ✗ | `saved_search` | — | ⬜ |
| C15 | System — Saved Search · Agent | ✗ | `saved_search` | — | ⬜ |
| C16 | System — Saved Search · System | ✗ | `saved_search` | — | ⬜ |
| C17 | System — Tour Request (existing lead) | ✗ | `tour_request` | — | ⬜ |
| C18 | System — Contact Request (existing lead) | ✗ | `contact_request` | — | ⬜ |
| C19 | System — Seller Get Report (existing lead) | ✗ | `seller_request` | — | ⬜ |

**Gaps flagged from the pull (2026-07-02):** (1) C9 Shared Home missing from `hl_trigger_events`. (2) Every FUB row is
still `notify_owner=realloop` — the HouseLoop-notifier migration (per-row flip) hasn't started; each becomes a matrix
sign-off as it moves to `houseloop`. (3) No FUB row has a `workflow_id` bound yet.

---

### SHIPPED (Phase 0 — foundation)
- [x] Lead Sources = ONE page (`/settings/lead-platform`): source block = header + KPI funnel + events + Configure
- [x] Collapsed Registration Events + Lead Sources + FUB Settings into the one page
- [x] Workflows grouped BY lead source; `Workflow = f(source × event)` (reversed the old "agnostic" model)
- [x] Dynamic Back → "Back to Lead Sources"; canonical `HlBackLink`
- [x] FUB webhook config (per-type toggles: Process / Creates-Lead / Notify / Notify-Via) + 17-event matrix (migs 0274/0275)
- [x] Phone Numbers registry `hl_phone_numbers` (41 FUB lines) + `/settings/phone-numbers`
- [x] Notifications = Everything Phase 1 (catalog-derived notification list from the same event tree)
- [x] R2/R3/R4 shipped (mig 0270 `notify_owner`): HouseLoop native notifier + notify-toggle repoint + per-event Realloop kill-switch

### ARCHITECTURE RAILS (must be green before matrix rows pass)
- [ ] R1 Native FUB ingestion (Realloop mirrors `fub.*` data only, stops being the notifier)
- [ ] R8 Webhook test harness (fire/replay a real FUB webhook + verify every gate)
- [ ] R9 Native dialed-line preserved (Ana 913-954-7123 — never the forward target)

### OPEN BUILDS (the keystones)
- [ ] 1d. FUB SUB-SOURCE dimension — split FUB events by portal so `(FUB × sub-source × event)` binds its own workflow (the keystone for source-specific FUB copy)
- [ ] 1e. Email received (`emailsCreated`) — settings row + native HL notify + matrix row
- [ ] 2. A workflow (text + email + nurture) for EVERY event (add missing text steps; DRAFT→ACTIVE the existing-lead doors)
- [ ] 3. Zillow-inbound render MVP (Zillow logo, "Find on HouseLoop", iMessage-bubble restyle)
- [ ] 4. FUB Phone Call Activity engine — ONE `(FUB · Phone Call)` workflow, Myli reads the dialed line → category → copy; text-back from BlueBubbles 913-208-1633; Ana email bridge; then the Twilio port
- [ ] 5. Past-client reconciliation (single canonical table → listing page → seller report → Ana campaign)
- [ ] 6. Go-live — quarantine legacy FUB Zillow doors, E2E-verify each real inquiry in the Run Log

### SIGN-OFF MATRIX — recent edits
- [x] **Viewed home** added to the HOUSELOOP → Existing-lead matrix as a **COUNTED signal** (Zach 2026-07-01):
  no workflow — increments the lead's Visits/Properties counter + "viewed N×"; can gate a workflow with a count
  condition. Backed by `hl_trigger_events.property_viewed` (system, houseloop, mig 0276).

### NEXT CONCRETE ACTION (Lee)
- [ ] KEEP THE OVERVIEW DOC CURRENT — reconcile it against today's changes: Benji's saved-search 3-trigger split (saved_search=client · saved_search_agent · system) is not reflected in the HouseLoop registration/existing-lead matrix; update it.
- [ ] Then resume the Sign-Off Matrix one row at a time (Zach coming back with direction on lead sources + event triggers).

---

## PRD · `GT/GT - ACTIVE/GT_HOUSELOOP_LEAD_PLATFORM_PRD.md`

#### GT — HOUSELOOP LEAD PLATFORM PRD (Zach + Lee, 2026-06-26) — ✅ SIGNED, BUILD

**Owner:** Lee (end-to-end). **Status:** model locked + signed by Zach; building now.
**Design SSOT:** `.claude/checkpoints/lead-system-MAP-2026-06-26.md`. **Scope lock:**
`.claude/state/session-scope-platform-source.lock`. Supersedes the platform/source UI direction in
`GT_HOUSELOOP_LEAD_FLOW_UI_PRD.md §12` (that doc's attribution columns ship as-is; the NAV/screens here win).

---

##### 1. THE PROBLEM
HouseLoop is the website AND the CRM in one product — and Follow Up Boss is a legacy CRM piping in leads with
history. "Lead source" alone can't express that. We spent a day circling because two things were tangled:
**where a lead came from** vs **what it does on our site**. This PRD untangles it.

##### 2. WHAT THE RESEARCH SAID (5 CRMs — HubSpot, Salesforce, UTM/GA4, FUB/kvCORE/Sierra, Pipedrive/GHL/AC)
Unanimous findings (full briefs in session history):
- **Two fields, never one:** a TRUE ORIGIN (Zillow) kept separate from the CONNECTOR (Follow Up Boss). Origin
  is first-touch + immutable. (HubSpot Original vs Record Source; Salesforce LeadSource vs Source System.)
- **Events ≠ source.** Activity is a separate growing stream; source is a stamp set once. (We already do this:
  `hl_lead_activity`.)
- **Trigger on the EVENT, filter on SOURCE/PLATFORM.** Never one workflow per source. Merge fields fill the
  words. (Every CRM. Sierra/GHL/AC explicit.)
- **No "type" field** for origin — "type" = the event (already our catalog) or the pipeline role (already our
  Buyer/Seller pipelines). Don't add it.
- **Don't prefix `fub-zillow.com`** — keep source = the real origin (Zillow), connector separate (live FUB data
  with 48k people confirmed this). Platform = the connector/world.

##### 3. THE MODEL (LOCKED)

###### 3.1 The keystone — TWO buckets, TWO routing keys
| Bucket | Keyed on | Meaning |
|---|---|---|
| **New Lead Registration** | the lead's **ORIGIN** (source + platform) | "how they came in." realtor.com/Zillow via Follow Up Boss → that source's registration (the reactivation). Origin is **set once, permanent** — a merge field forever, but stops STEERING after registration. |
| **(HouseLoop) System Events** | the **SURFACE** (where the action fired) | "what they do on HouseLoop." ANY lead — regardless of origin — who likes a home / requests a tour on HouseLoop.ai fires the **HouseLoop** workflow. |
| **Follow-Up** | schedule | the nurtures |

**Proof (Zach's, universal):** lead acquired platform=Follow Up Boss, source=Zillow.com → registration runs
**Reactivation** ("you worked with Alicia… homes on HouseLoop.ai"). Origin stays Zillow forever. They then like
a home **on HouseLoop.ai** → SURFACE = HouseLoop → runs the **HouseLoop liked-home workflow**, not the FUB one.

###### 3.2 The cascade (first match wins)
```
A lead's event fires →
  1. SPECIFIC workflow for (this source + this event)?    → run it
  2. else a workflow for (this platform)?                 → run it   (e.g. FUB → Reactivation)
  3. else GENERAL REGISTRATION (the catch-all floor)      → always runs ({{lead_source}} fills the words)
```
Already engine-true: `dispatchDoor` routes existing-lead events by event SURFACE and keeps registration
ORIGIN-scoped (built P3). New sources work instantly off the floor; specials are opt-in.

###### 3.3 Scale = MERGE FIELDS
One event → one workflow → `{{first_name}}` `{{home_address}}` `{{lead_source}}` `{{platform}}` fill the rest.
Never a workflow per source. Adding a source = a new ROW, not a rebuild.

##### 4. NAV (LOCKED) — 3 items (Lead Flow folded away)
```
Pipeline & Workflow
  ├─ Lead Platform     ← master: Platform → Source → registration doors + system events
  ├─ Lead Sources      ← thin list: the 3 sources
  └─ Workflows         ← the categorized workflow library
```

##### 5. LEAD SOURCES — EXACTLY 3 (all else killed)
| Source | Platform | Type | Default Stage |
|---|---|---|---|
| HouseLoop.ai | HouseLoop | web | New Lead |
| Zillow.com | Follow Up Boss | portal (fix from `manual`) | New Lead |
| Manual entry | HouseLoop | manual | New Lead |
KILL (deactivate, reversible): Referral · SRG-Follow Up Boss (`fub_import`) · `__platform_houseloop__` ·
`__platform_fub__` · IDX Sign-up. **Build-order care:** `fub_import` holds 6 FUB leads + their reg doors →
REASSIGN leads + doors onto Zillow.com FIRST, then retire it. Nothing lost.

##### 6. EVENT NAMING — the "New Lead –" prefix IS the registration-vs-system tell
- **Registration doors** (origin): prefixed **"New Lead – …"**. HouseLoop.ai = 8: New Lead – General
  Registration · Liked Home · Saved Home · Saved Search · Tour Request · Contact Request · Seller Get Report ·
  Seller Get Started. (3 need relabel today: liked_home/saved_home/general_registration; other 5 already correct.)
  Zillow.com = 2: New Lead – Tour Request · New Lead – Contact Request (= "property inquiry"). (Move from
  fub_import; rename "FUB Property Tour"→"New Lead – Tour Request"; drop FUB's general/seller/saved-search regs.)
- **System events** (surface): UNPREFIXED: Tour Request · Contact Request · Saved Search · Seller Get Report ·
  Seller Get Started · Property Viewed · Property Liked · Property Saved · Property Shared.

##### 7. THE SCREENS (wireframes — final, real names)

###### 7.1 LEAD PLATFORM  → cascades into Workflows (click an event row → its workflow)
```
╔═ HouseLoop ════════════════════════════════════════════ platform ═╗
  ─ SOURCES (origin → New Lead Registration) ──────────────────────
   🌐 HouseLoop.ai — Organic ················· 1 lead · 8 doors
        New Lead – General Registration  → General Welcome
        New Lead – Liked Home            → Liked Home
        New Lead – Saved Home            → General
        New Lead – Saved Search          → General
        New Lead – Tour Request          → General
        New Lead – Contact Request       → General
        New Lead – Seller Get Report     → General
        New Lead – Seller Get Started    → General
  ─ HOUSELOOP SYSTEM EVENTS (surface → regardless of source) ──────
        Tour Request · Contact Request · Saved Search · Seller Get Report ·
        Seller Get Started · Property Viewed · Property Liked · Property Saved · Property Shared
  ─ FOLLOW-UP ──  (none yet)
╔═ Follow Up Boss ═══════════════════════════════════════ platform ═╗
  ─ SOURCES (origin → New Lead Registration) ──────────────────────
   🌐 Zillow.com ····························· 6 leads · 2 doors
        New Lead – Tour Request          → Reactivation Campaign
        New Lead – Contact Request       → Reactivation Campaign
  ─ SYSTEM EVENTS ──  (none — activity happens on HouseLoop)
```
Layout: blue platform header → source sub-section → bucket sub-headers → event rows (Event · Workflow ·
Active). Brand: white-on-blue header. Tables, not chevron trees.

###### 7.2 LEAD SOURCES  → cascades into Lead Platform (click a source → its block)
```
 SOURCE          PLATFORM          TYPE      DEFAULT STAGE   LEADS   REG DOORS
 HouseLoop.ai    HouseLoop         web       New Lead          1        8
 Zillow.com      Follow Up Boss    portal    New Lead          6        2
 Manual entry    HouseLoop         manual    New Lead          0        0
```

###### 7.3 WORKFLOWS  → cascades into the canvas. The automation LIBRARY (grain = one row per WORKFLOW)
Categorized Registration / System / Follow-Up (NOT "Other"). Columns: Workflow · Trigger · Steps · Completed ·
Status (+ Platform · Source as light context). Steps = counted from the canvas; Completed = `hl_workflow_runs`.
```
═ REGISTRATION ═══════════════════════════════════════════════════════════
 WORKFLOW                         TRIGGER                STEPS  COMPLETED  STATUS
 New Lead – General Registration   New Lead – Gen. Reg.    4    138/142    active
 Reactivation Campaign             New Lead – Tour Req.    6      6/7      active
═ SYSTEM ═════════════════════════════════════════════════════════════════
 Liked Home                        Liked Home             3     12/12     active
═ FOLLOW-UP ══════════════════════════════════════════════════════════════
 90-Day Nurture                    schedule               8      4/20     active
```
NOT a clone of Lead Platform: Lead Platform rows = DOORS (by source); Workflows rows = WORKFLOWS (the canvas +
its health). One workflow can serve many doors.

###### 7.4 LEAD DETAIL (already shipped — confirm labels)
`Source: Zillow` + `Platform: Follow Up Boss` in the Contact card.

###### 7.5 WORKFLOW TRIGGER (the one engine add)
Open a workflow → trigger node: `Event [▾] · Platform [▾ any/HouseLoop/FUB] · Source [▾ any/Zillow/…]`.
This is what makes "Registration WHERE platform = Follow Up Boss → Reactivation" buildable.

##### 8. BUILD PHASES (Lee, all reversible/additive where possible)
- **P1 — Data cleanup migration:** reassign FUB leads+doors → Zillow.com; retire junk sources (deactivate);
  fix Zillow type→portal; relabel the 3 HouseLoop doors + the Zillow doors with "New Lead –".
- **P2 — Lead Platform page:** Platform → Source → (New Lead Registration · System Events · Follow-Up), real
  data, tables, blue headers, workflow-per-row. (Replaces the scrapped tree.)
- **P3 — Lead Sources page:** the thin 3-row table (Source · Platform · Type · Default Stage · Leads · Reg Doors).
- **P4 — Workflows page:** categorize Registration/System/Follow-Up + Steps/Completed columns.
- **P5 — Workflow trigger:** Platform + Source filter dropdowns on the trigger node.
- **P6 — Nav:** Lead Platform · Lead Sources · Workflows (remove Lead Flow).
- **P7 — Verify on prod (Playwright) + handoff/docs.**

##### 9. WHAT'S ALREADY BUILT (don't redo)
Lead `platform`/`channel` cols + backfill (mig 0247) · lead-detail Platform/Source rows · dispatchDoor
origin-vs-surface routing (P3) · `hl_lead_activity` event stream · the workflow canvas · the 3-bucket Lead Flow
grouping (becomes the System Events section). The work is the SCREENS + the data cleanup + the trigger filter.

##### 10. NON-GOALS
No `fub-` prefix. No "type" field. No per-source events (merge fields handle words). No Lead Flow nav page.
No inventing sources beyond the real 3. Grows by ROWS.

---

## Scope lock · `.claude/state/lee.lock`

AGENT: Lee
ROLE: Chief of Staff / Systems Architect
LAST_UPDATED: 2026-07-28 ~21:15 CT (Fable 5, incident-recovery session)

##### ⛔ CONTEXT — THE 2026-07-28 TAXONOMY INCIDENT (recovery night)
My taxonomy work (migs 0475-0489) broke prod 4 ways. Zach ordered a revert; Adam's manifest
(.claude/state/LEE-REVERT-MANIFEST-2026-07-28.md) proved a code-only revert is WORSE (DB already
renamed/deleted; no pre-day backup). Recovery went FORWARD, surgically. THE TAXONOMY / CATALOG
PROJECT IS FROZEN until Zach green-lights it. No renames. No new migrations beyond fixes.

##### RECOVERED TONIGHT (all verified on prod with my own eyes)
1. LEADS MINT AGAIN — root cause: classifyFubEvent emits canonical tokens (tour_requested) while
   hl_fub_webhook_settings keys old spellings (tour_request); exact-match lookup missed → fail-closed
   read Zach's ON toggles as OFF. Fix: alias-closure lookup (both sides canonicalize through
   hl_trigger_event_alias) in fub-lead-import.ts (cfg + notifyCfg) + canonical keys added to both
   KIND_TO_TRIGGER maps. Adam separately restored 2 dropped creates_lead toggles on the FUB source.
2. HOUSELOOP SECTION BACK on Trigger Events — page contract (mig 0380): section=NULL means "group
   by source"; my migrations filled section on every row → every door diverted to custom folders.
   Fix: mig 0490 (section=NULL, idempotent) + applied live. Verified: 🌐 HouseLoop.ai Website band renders.
3. MESSAGES/CARDS RENDER — Adam repointed badge catalog + writers to 0488 names (ec06703ce2).
   Verified on Evelyn Morales: 9 messages w/ bodies + house cards + via-Zillow chips; Website pills
   All 1306 / Views 844 / Saves 338 / Shares 104.
4. PURPLE-HAND ICON FIX (Zach's screenshot 8:51 PM): rowKindForActivity canonicalized then compared
   OLD tokens → sms_*/phone_call_* fell to 'request' (Hand). All CenterLane comparisons now
   canonicalize + accept both generations. Pushed ~21:05 CT — VERIFY ON PROD after deploy (Lucy's lead).
5. BRADLEY BENGEL MINTED — lead 37a6106c-c2ce-4635-adb9-045a3f4292f4 (FUB 44854), via the
   single-person pull-in, 4 tour requests for Jul 29.

##### STILL PAUSED ON PURPOSE (mig 0487 — do NOT un-pause without Zach)
23 workflows · AI email+voice · ai_mode on 17 leads (snapshot hl_settings 'ai_mode_snapshot_0487').
Zach asked for notifications + lead flow + rendering back (done). Outbound-to-lead workflows stay
paused until his explicit word. The audience-gate rebuild (authorized-send re-key) is still the real fix.

##### KNOWN REMAINING (not tonight)
- ~500 code sites still name old tokens; tsc shows type-union errors (builds pass via
  ignoreBuildErrors; logActivity canonicalizes at write time so runtime is safe). The sweep is the
  FROZEN project — needs Zach's go.
- "AI Cockpit 12 failed" tile seen in sidebar — triage.
- Likes pill = 0 forever (nothing writes home_liked) — pre-existing.
- RETIRED- sources (Agent Referral / Google PPC / HouseLoop PPC / Webinars): verify no other
  toggles were dropped by the consolidation (Adam flagged).
- Verify Bradley notification fired + tour_requested minting works end-to-end on the NEXT live webhook.

##### THE LESSON (again, in writing)
A rename is done when: rows migrated + union regenerated + catalog repointed + EVERY reader AND
writer repointed + parity gate green — in ONE commit. An alias map protects reads, never comparisons.
"Referenced by no table" ≠ "read by no code." AUDIT MECHANICALLY.

##### 2026-07-28 ~11:00 PM — GATE 0 EXECUTED (Zach authorized 1-11 autonomous)
Receipts published in the artifact (🧾 GATE 0 RECEIPTS block, before Gate 1).
Verdicts: #2 CONFIRMED (82/82 match FUB is_incoming=false — split was right) · #3 CLEAN ·
#4 CLEARED (0 FK-death writers of 22) · #5 FREEZE HOLDING (105 tonight rows = all archive imports,
0 occurred live) · #6 CONFIRMED (parity 361 + screens: Evelyn messages, Osmar 29 calls) ·
#9 all five confirmed · #10 all four confirmed · #1 SUPERSEDED (89/79, 37 never-fired, 12 label≠key
residue) · #7 MOSTLY (2 flags: RETIRED-Webinars orphaned registration toggle; Adam's 2-row binding
restore not visible — mints via hl_fub_webhook_settings layer regardless, flagged to Adam) ·
#8 PENDING — zero live FUB events since the 7:49 PM fix; stays open until the next real Zillow
inquiry mints on its own. Router healthy (106 runs/0 fail).
MORNING CHECK: (a) did a live FUB inquiry mint overnight? (b) Zach reads receipts, checks Gate 0
boxes, then Gate 1 begins (the cage).

##### 2026-07-29 ~7:10 AM — THE NOTIFICATION GAUNTLET: GREEN, ALL ARMS, WITH RECEIPTS
Zach's order executed end-to-end by me (Adam = research only, per Zach).
FIXES SHIPPED (commits through b45efefdd8 + 624834bcd3): ① dispatch resolves prefs by CLASS passed
down from create's RETURNING (no re-query/race/swallow) ② drop trail records classKey+prefKeys
③ router FUB_EVENT_CLASS + import FUB_NOTIFY speak canonical tokens (mint fires the TOUR card)
④ fresh-person API seed runs on ANY import (mirror race killed 4 live mints) ⑤ fub-body-recovered
throws-to-retry on mirror-miss (Adam's root cause; killed Msale's 5 in-app notifies 7/28).
RECEIPTS: Zach Test tour → created class tour_request → sms-dispatch success to 4074038088 (7:01:14) ·
Gate Receipt + FUB-Test-dup MINTED 7:00:45 → '📅 requested a tour' cards +sms 7:04 · Msale in-app
replays delivered staggered (#5 ~7:07) — format = message-only + links.
DOCS LIVE (published:true): crm/business-settings-notifications (delivery resolution chain + catch-up
runbook) · fub/master-scraper-ssot (the mirror-race law, dated update-log entry).
STILL OPEN: sweep stamps notified_at on CONFIRMED carding (fix #2) · cleanup of test leads
(Gate Receipt 0e98c5d9, FUB-Test-dup 3809edc3, dup FUB person 50547) when Zach says · Gate 1 next.

##### 2026-07-29 — GATE 2 RULINGS (Zach, in-chat — bake into the Schema-Lock Package)
DECISION #1 (sections, PROPOSED+leaning): six DOMAINS, never person-categories:
  Comms · Website · Work · Deals · Content · System. No Leads/Agent/AI sections — WHO is the user
  chip (type: lead/agent/ai/system/bot/visitor), filterable everywhere. System sub-grouping via LENS
  (jobs/integrations/ai/notifications), never a new column. Content = SEO now, YT/IG/podcast later.
  IDX = System (machinery), not Website (behavior). AI cost = fact ON rows, dashboards not sections.
DECISION #2 (insights) — DECIDED: INSIGHTS ARE EVENTS, in the log, Website section, "Insights" LENS.
  An insight = a DERIVED CONCLUSION computed from atomic activities, delivered as a signal with a
  payload, usable as an automation trigger. Zillow pushes theirs; HouseLoop computes its own — SAME
  KINDS, the source column says who noticed (locked law: same kind, different source).
  THE CANONICAL FIVE (rename *_flagged → insight_*; FUB had the naming right, "flagged" dies):
    insight_high_intent_buyer   "Insight - High-Intent Buyer"    (FUB emits now; HL later)
    insight_high_intent_seller  "Insight - High-Intent Seller"   (FUB emits now; HL later)
    insight_viewed_10x_7d       "Insight - Viewed Home 10x in 7 Days" (both)
    insight_new_area            "Insight - Looking in a New Area" (2+ views in a zip, none prior 60d) (both)
    insight_buyer_reengaged     "Insight - Buyer Re-Engaged" (activity after 14+ days silence; NO parenthetical) (both)
  NOT insights (they are ACTIVITIES/requests): tour_requested · contact_requested ·
  seller_report_requested · home_viewed (atomic, rides with views lens).
  A source emits only what it computes — HL needs no fake composites until its algo ships.
  IMPLICATIONS: naming-law carve-out for the insight_ family (noun conclusions, not object_verbPast);
  the *_flagged→insight_* rename goes through ONE_COMMIT_RENAME + rename-readiness (first real test
  of the cage), at catalog-build time (Gate 3), NOT ad hoc.
STATUS: building the Schema-Lock Package + Draft Catalog on these rulings (Step 1 of the plan).

##### 2026-07-29 — GATE 2 SITTING CONTINUED (Zach's batch rulings — bake ALL into Draft Catalog)
LIFECYCLE LAW: every editable object gets full CRUD events — note_added/edited/deleted ·
call_note_created/edited/deleted · task_created/completed/edited/deleted ·
appointment_created/edited/rescheduled/cancelled/deleted(+outcome model below) ·
followup_scheduled/completed/edited/deleted. (Also fixes hard-delete-no-trace.)
PROPERTY_ NOT HOME_ (REVERSES the home_ law — Zach: could be land/commercial; home is a TYPE):
property_viewed/saved/unsaved/liked/hidden/shared/searched · property_inquired (payload type from IDX:
home/duplex/land/condo). rental_inquired stays. Goes through ONE_COMMIT_RENAME at Gate 3.
REGISTRATION: no atomic seller registrations — registration_completed + payload &#123;type: seller|buyer,
entry: seller_get_started|get_report|...&#125;. seller_get_started catalog row dies into payload.
prequalified MERGES → pre_approval_approved.
EXTRA INSIGHTS NAMED: insight_actively_searching · insight_specific_property_interest.
PAYLOAD-CARRIES-VARIANTS LAW (his words): stages/tags/appointment types/document types are PAYLOAD,
never kinds — tag_added payload&#123;name&#125; · stage_changed payload&#123;from,to&#125; · appointment payload&#123;type:
buyer_appt|listing_appt|showing|meeting|phone|video|open_house|consult&#125; · document payload&#123;form&#125;.
contact_updated payload&#123;field,from,to&#125;; name_changed MERGES in (Lee rec, pending nod).
CONTENT: seo_article_written · seo_article_published.
SEARCH LIFECYCLE: Website: search_saved/edited/paused/deleted · System: search_alert_sent.
JUNK CONFIRMED: manual_enrollment · clone_takeover_scheduled (never scheduled — toggle model) ·
ai_create_saved_search (tool). agent_ack_request = notify-class (separate cabinet pass; rename cand.).
OPEN (Lee RECs pending Zach): ① appointment outcomes model (completed w/ outcome payload + no_show
event) ② lead_created vs user_created&#123;type&#125; ③ AI clone events (enabled/disabled + human_stepped_in;
kill stepped_in ambiguity) ④ director_move (= AI brain's next-move decision record; explain+name).

##### 2026-07-29 — GATE 2 SITTING, ROUND 3 (Zach rulings)
AI CLONE: ai_clone_enabled / ai_clone_disabled LOCKED (payload &#123;by: user|system, reason&#125;). NO
human_stepped_in kind — REJECTED as drift-prone. THE RULE instead: a human message while clone
active AUTO-DISABLES the clone → ai_clone_disabled &#123;by: system, reason: human_message&#125; — structurally
impossible for AI to keep talking. Re-enable runs the clone playbook; step 1 of EVERY playbook = read
the handoff (full lead contact+comms+work history) then decide next move per playbook goal.
ai_clone_stepped_in (3 rows) dies into this model.
BINARY TOGGLE PATTERN (answered): state column on the record = read-model; the two flip EVENTS in the
log = the truth; both written in ONE transaction; state always rebuildable from flips.
APPOINTMENTS: appointment_dispositioned RETURNS (reverses mig 0484 kill — Zach: UI says Disposition)
with payload &#123;outcome: showed_up|no_show|listing_signed|listing_not_signed|...&#125;. Lifecycle events stay:
created/edited/rescheduled/cancelled/deleted. LEE'S MAPPING (pending nod): disposition = after-the-fact
outcome record; if the disposition menu picks cancelled/rescheduled the UI fires THOSE events instead
(no double-write). appointment_met DIES — first-appt-set/met etc are STAGES (stage_changed payload).
USERS: user_created&#123;type: lead|agent|lender|showing_partner&#125; LOCKED (lead_created = that, type=lead) +
user_edited&#123;field,from,to&#125; + user_deleted. contact_updated AND name_changed both die into user_edited.
MYLI CREATES A SEARCH = same kind as anyone creating one, user=Myli(type ai). ⚑ NAMING FLAG: Zach said
search_created; website pile earlier said search_saved — ONE kind law. Lee rec: rename lifecycle to
search_created/edited/paused/deleted (fits CRUD law; 'saving a search' IS creating one).
ai_director_move_chosen LOCKED (Zach's name).
REMAINING ⚑: search_created-vs-saved nod · disposition mapping nod · final section-list nod. Then the
Draft Catalog is COMPLETE.

##### 2026-07-29 — GATE 2 SITTING, ROUND 4
THE QUESTION TEST (new naming law, Zach-derived): name a kind so the natural-language question maps to
ONE WHERE clause ("how many saved searches has Myli created?" → saved_search_created WHERE user=myli).
SEARCH — FINAL: saved_search_created / saved_search_edited / saved_search_paused / saved_search_deleted
(the OBJECT is 'saved search'). The ACT of searching = property_searched &#123;searchType: address|filters|ai,
query, resultCount&#125; — one kind for search-bar, filter, AND AI search (kills ai_search as a kind); user
chip = web visitor / lead / agent / myli / bot. System: search_alert_sent (the digest send).
APPOINTMENT DISPOSITION — ONE-DOOR RULE (explained to Zach): cancelled + rescheduled are FIRST-CLASS
events and the ONLY way those facts are recorded; the Disposition MENU is just UI — picking cancelled
there FIRES appointment_cancelled (menu ≠ event). appointment_dispositioned records only the outcomes
with no first-class event: showed_up | no_show | listing_signed | listing_not_signed.
appointment_deleted = removing the record (distinct from cancelled = meeting called off), user chip who.

SAVED_SEARCH_ALERT_SENT locked (was search_alert_sent) — machine send named after the object. 

##### 2026-07-29 — GATE 2 SITTING, ROUND 5
#25 SECTIONS APPROVED: Comms · Website · Work · Deals · Content · System — LAW.
PRE-APPROVAL rework (Lee REC pending nod): pre_approval_started · pre_approval_submitted ·
pre_approval_granted · pre_approval_denied (granted kills the approval_approved double; his 'status_'
infix dropped as noise).
#29 AI SAFETY BLOCKS = YES, rows required. Shape per payload-variants law (REC): ai_send_blocked
&#123;reason: master_off|lead_muted|banned_phrase|no_persona|leak_guard|no_intent&#125; — ONE kind, reason
payload (collapses most of the 27 AI audit gaps). Toggle cascade: ai_clone_enabled/disabled (locked,
per-lead) + ai_toggle_enabled/disabled &#123;scope: master|role:&lt;isa&gt;|user:&lt;myli&gt;&#125; for the cascade levels
(REC pending nod — avoids kind-per-scope infinite list).
#30 UN-PAUSE POLICY: AI outbound-to-lead STAYS PAUSED; document state in dev docs under AI
authentication (doing now).
ENFORCEMENT BLUEPRINT question answered: the approved-materials list = catalog + payload schemas; the
inspector = FK + generated union + ESLint + label==key + parity + caged push + ONE-WRITE + append-only
trigger (Gate 3 adds payload validation + runlog append-only enforcement at the DB).
BUILDING NOW: Schema-Lock Package + Draft Catalog INTO the master artifact above Gate 2.

##### 2026-07-29 — GATE 2 SITTING, ROUND 6: CONFIG LIFECYCLES (Zach walked the left-hand menu)
NEW CLASS: configuration-object lifecycles = ADMIN AUDIT events → System section, config lens,
lead_id NULL (the nullable-lead design carries it). DISTINCTION LOCKED: tag_added/removed = ON a lead
(Work); tag_created/edited/deleted = the DEFINITION (System/config). Same for stage_changed vs
stage_created/edited/deleted.
CONFIG LIFECYCLES ADDED: stages · tags · appointment_types · appointment_outcomes · lead_sources ·
custom_fields · merge_fields · relationship_types · text_templates · email_templates · email_campaigns
(+email_campaign_sent) · email_lists (+lead_added_to_list/lead_removed_from_list under Work) ·
lead_table_filters · phone_number_added/deleted &#123;carrier&#125;.
USER LIFECYCLE COMPLETED: user_trashed (soft, mobile Trash) · user_restored · user_deleted (hard) ·
user_reassigned &#123;from_agent, to_agent&#125; (per-row; bulk = N rows; the big-deal assignment event).
calendar_invite_sent &#123;appt_id, to&#125; → Work/appointments.
REC pending nod: settings_updated &#123;scope: business|profile, field, from, to&#125; — ONE kind per payload
law, covering his business_settings_updated + profile_settings_updated pair.

##### 2026-07-29 — GATE 2 SITTING, ROUND 7
settings_updated &#123;setting: &lt;json path&gt;, from, to&#125; — LOCKED (one kind, JSON says which setting).
FOLLOWUP FAMILY KILLED — follow-up is a TASK TYPE, not an object. Task payload schema (from UI):
&#123;type: call|text|email|follow_up|send_listing|other, name, status: pending|in_progress|completed|
cancelled, due_date, due_time, assigned_to, priority: low|medium|high|urgent, description&#125;.
Events stay task_created/edited/completed/deleted; cancellation = task_edited&#123;status→cancelled&#125;
(a cancelled task is a field change; a cancelled APPOINTMENT is a business event — asymmetry deliberate).
APPOINTMENT payload schema (from UI): &#123;title, event_details, event_type: &lt;from appt-type settings&gt;,
status: scheduled|completed|no_show|cancelled (READ-MODEL — derived from the events), created_by, date,
time, duration, all_day, location, people, related_lead, assigned_to, invitees, invite_message&#125;.
NODS REMAINING: ai_toggle_&#123;scope&#125; · ai_send_blocked&#123;reason&#125; — then catalog COMPLETE.

##### 2026-07-29 — GATE 2 SITTING, ROUND 8 (Zach caught Lee's asymmetry — two unifying RECs pending)
REC A — THE STATE-MACHINE LAW: every object with a status enum emits ONE transition event
X_status_changed &#123;from, to, detail?&#125; — the pattern stage_changed and transaction_status_changed
already use. Consequences if nodded: task_completed dies (→ task_status_changed to=completed) ·
appointment_cancelled + appointment_dispositioned die (→ appointment_status_changed
&#123;to: completed|no_show|cancelled, detail: listing_signed|listing_not_signed&#125;) ·
appointment_rescheduled dies (a time change = appointment_edited &#123;field: time, from, to&#125;).
Playbooks stack filters (WHEN appointment_status_changed IF to=cancelled) — importance never hardcoded.
REC B — THE TOGGLE REGISTRY: kinds stay TWO (ai_toggle_enabled/disabled); every UI toggle gets a
unique IDENTITY in the payload &#123;toggle: master|receptionist|inside_sales|clones|nurture|
database_manager|transaction_coordinator|content_manager|clone with lead_id, by, reason&#125; + a toggle
REGISTRY config table (itself covered by config-lifecycle events) defining each toggle + cascade
parent. Mirrored toggles across 3 surfaces = ONE toggle id = lock-step by construction (separate keys
per surface COULD drift — one identity cannot). New AI role next year = a registry row, zero catalog
changes (the Ylopo test). Fold ai_clone_enabled/disabled in as &#123;toggle: clone, lead_id&#125; for full
consistency (Zach may keep it separate — his call).

##### 2026-07-29 — GATE 2 SITTING, ROUND 9 — THE CATALOG IS COMPLETE
STATE-MACHINE LAW LOCKED (final form): one transition event per state machine, VALUES AS PAYLOAD,
suffix follows the UI's own noun — stage_changed · task_status_changed &#123;from,to&#125; (task_completed dies) ·
appointment_dispositioned &#123;outcome: &lt;from the appointment_outcomes CONFIG TABLE — open set:
showed_up|no_show|cancelled|rescheduled|...&gt;, from_time?, to_time?&#125; · transaction_status_changed.
Appointment lifecycle FINAL: created · edited · deleted · dispositioned (cancelled + rescheduled are
OUTCOMES — rescheduled carries the time pair; the outcomes list is CONFIG DATA, never an enum in code).
TOGGLE LAW LOCKED (Zach verbatim: "it can only be ai_toggle... otherwise we will drift"):
ai_toggle_enabled / ai_toggle_disabled are the ONLY toggle kinds. ai_clone_enabled/disabled KINDS DIE —
clone = payload &#123;toggle: clone, lead_id&#125;. Toggle registry = config table (each toggle id + surfaces +
cascade parent); mirrored toggles = one id = lock-step by construction.
ai_send_blocked &#123;reason&#125; LOCKED under the blocks-get-rows ruling + the payload law.
CATALOG COMPLETE at ~138 rows. NEXT: Zach reads the package top-to-bottom → #31 signature → Gate 2
CLOSES → Gate 3 builds hl_activity_catalog + payload schemas + ONE WRITE from this exact table.

##### 2026-07-29 — TASK STATUS UI BUG (Zach, sitting round 10 — LOGGED, fix later, not now)
Zach: task CREATE modal shows a status dropdown, but the RENDERED task only offers a "Completed"
button — no way to move a task through any other status after creation. Lead-detail task rows have
edit + trash but no status control. So the UI exposes ONE transition (→completed) of a state machine
that officially has several. This VALIDATES task_status_changed &#123;from,to&#125;: completed is just one
status, and the vocabulary already models what the UI can't do yet. FIX BELONGS with the Gate 3+
catalog work (when ONE WRITE lands, the task UI gets a real status control that emits
task_status_changed; today's Completed button becomes a shortcut for to=completed — same event, same
door). Lane: Adam (CRM) builds the control when scheduled; do NOT hotfix mid-Gate-2.

##### 2026-07-29 — GATE 2 SITTING, ROUND 10 (transaction docs + ai_ prefix + atomicity line)
EVIDENCE FIRST (hl_lead_activity, live): fact_captured=375 · context_built=14 · property_looked_up=2
(all source=ai, Myli toolbox) · property_reviewed=0 EVER (garage phantom) · lead_added_to_list=0 EVER.
RULINGS APPLIED:
- Document lifecycle ADDED per the lifecycle law: document_created/edited/deleted &#123;doc_id, form/field,
  loop_id&#125;. sent/opened/signed STAY as moments (repeat per person, not one-way status flips).
- transaction_loop_edited ADDED &#123;loop_id, field: participants|price|address|…, action?, user?&#125; —
  add/remove an agent on a loop = field:participants. Scale answer: any future doc-loop capability is
  lifecycle + status + moment + payload — never new vocabulary.
- AI PREFIX LAW: AI-NATIVE actions (only the machinery does them) carry ai_ → ai_fact_captured,
  ai_context_built, ai_property_looked_up. SHARED actions keep the plain kind with actor=ai (Zach round-5
  Myli saved-search ruling preserved — needs his explicit nod that the distinction stands).
- property_reviewed DELETED (0 rows ever). property_looked_up ≠ property_searched: it is Myli pulling
  property data (AI toolbox) — person on search bar = property_searched, person on a home page =
  property_viewed (both already locked).
- phone_number_added/deleted DELETED as kinds — they were the BUSINESS comms numbers (twilio/bluebubbles/
  gmail carriers), NOT lead fields → fold to settings_updated &#123;section: comms_numbers, action, number,
  carrier&#125; per his settings ruling. Lead fields were never atomic kinds: lead_edited &#123;field: phone|email|name&#125;.
- lead_added_to_list DELETED (0 rows, ambiguous garage token; campaign membership = campaign_enrolled;
  real lists later = config object with lifecycles).
- JSON-trust answer given: payload schema enforced per row at the ONE WRITE (Gate 3), jsonb is indexed +
  queryable, and the alternative IS the atomic explosion. The kind is the sentence; payload is the details.
Catalog holds at 138. Awaiting: ai-prefix-distinction nod + package read + #31 signature.

##### 2026-07-29 — GATE 2 SITTING, ROUND 11 (Zach correction — LOCKED)
- property_looked_up is DEAD in EVERY form (incl. my ai_property_looked_up rename — wrong). Myli hits
  the same search door as a human, just via API → property_searched with user_type = ai. The 2 legacy
  ai rows remap property_looked_up → property_searched in the Gate 3 data migration.
- TERMINOLOGY LOCKED: not "actor" — the USER model. Every event row carries user_id → hl_user, which
  carries type (ai | lead | human/agent...) + role + id. Shared action = standard key + user_type=ai.
  AI-native machinery (only the machine does it) = ai_ prefix: ai_fact_captured, ai_context_built,
  ai_director_move_chosen, ai_send_blocked, ai_toggle_*.
- Catalog at 137. Remaining: package read + #31 signature.

##### 2026-07-29 — GATE 2 SITTING, ROUND 12 (the missed twin + email lists)
Zach caught lead_removed_from_list still present (I killed lead_added_to_list in round 10 and missed
the sibling — the exact one-shovel-at-a-time failure this sitting exists to prevent). KILLED.
RULING: the email list IS a real config object → email_list_created / email_list_edited /
email_list_deleted. Membership (the add/remove-member button) is PART OF EDITING THE LIST:
email_list_edited &#123;field: members, action: added|removed, lead_id&#125;. Zach verbatim: otherwise "we get
into the same shit of moving every atomic json object outside of the key and it turns into 10,000
hardcoded lines." Same shape as transaction_loop_edited &#123;field: participants&#125; — membership of a
container is a FIELD EDIT on the container, everywhere, forever.

##### 2026-07-29 — GATE 2 SITTING, ROUND 13 (screenshot: Add-deal modal · transaction model REC)
LIVE EVIDENCE: hl_lead_activity has ZERO rows for summary/memory/star/deal/transaction kinds — the
whole family is clean slate. Screenshot (Desktop 1.51.47 PM): "Add deal" modal = State pending/closed/
lost · address · price · closing date · commission %/$ (3% default, GCI) · Zillow paid (Flex fee, agent
net) · notes · "Save & mark Closed" · feeds the Transactions dashboard. Deals + Starred tabs visible.
LOCKED (features Zach stated exist, were missing from catalog): ai_summary_generated &#123;lead_id, trigger:
initial|refresh&#125; (generated+refreshed = ONE kind per payload law — flagged for veto) · ai_memory_updated
&#123;lead_id, section?&#125; · activity_starred / activity_unstarred &#123;activity_id&#125;.
REC (NOD pills on the board): DEAL = LOOP = TRANSACTION, ONE OBJECT. transaction_created/edited/deleted
+ transaction_status_changed &#123;from, to: pending|closed|lost, close_price?, close_date?, reason?&#125; +
transaction_milestone_reached &#123;milestone: &lt;config&gt;&#125;. DIED into the laws: transaction_closed +
transaction_lost (state-machine law — same asymmetry Zach caught on appointments), transaction_
coordinator_assigned (field-edit law → transaction_edited &#123;field: coordinator&#125;), transaction_loop_*
(renamed — "loop" is the dotloop UI word, "Deals" is the tab label, transaction is the ONE machine name).
"Save & mark Closed" = transaction_created + transaction_status_changed &#123;to: closed&#125; — two honest rows.
Documents now carry transaction_id (was loop_id).
BUGS PARKED: star button not working on lead detail (vocab ready) · notification copy "captured a
detail" vs UI "Facts Myli tracks" → both derive from the ONE catalog label at Gate 3.
Catalog at 138. Awaiting: transaction-family NOD + package read + #31.

##### 2026-07-29 — GATE 2 SITTING, ROUND 14 — FULL NOD. CATALOG LOCKED END TO END.
Zach nodded: transaction restructure (deal=loop=transaction, "Add a deal" stays the UI label, same
mechanism underneath) · ai_summary_generated ONE kind &#123;trigger: initial|refresh&#125; · activity_starred/
unstarred · transaction dates (closing, under_contract) + prices (listing, contract) are FIELDS on the
transaction (transaction_edited field enum widened) · transaction tasks/docs/emails/attachments ride
the existing families carrying transaction_id — "all that fits under our current convention" (Zach).
TC-loop features not yet built (listing summary, commission, financing under transaction summary) need
ZERO new vocabulary when they land. ALL pills LOCKED. ONLY REMAINING GATE-2 STEP: Zach reads the
Schema-Lock Package top to bottom → signs #31 → Gate 3 build begins (hl_activity_catalog + hl_user +
payload-schema enforcement at the ONE WRITE + catalog CRM page + legacy-kind remap incl.
property_looked_up→property_searched and the 2-generation aliases).

##### 2026-07-29 — GATE 2 SITTING, ROUND 15 — THE THREE WORDS (transaction/deal/loop) SETTLED
Zach correction accepted: loop ≠ transaction. LOOP = the CONTAINER holding a transaction's documents +
files. He floated deal_* keys, then landed it himself: "deal created is just... surface level for the
agent... but that actually keys off transaction_created." LOCKED:
- Keys: transaction_* ONLY — one canonical list. No deal_* keys ever.
- "Deal" = agent-facing UI copy (Add a deal / Deals tab) — a skin over transaction_created etc.
  TCs talk transaction milestones/documents — the machine talks like the TCs.
- Loop = container, NO keys of its own; doc/file/task/email events carry transaction_id.
- TC assigned = transaction_edited &#123;field: coordinator, user&#125; — Zach: a boolean/dedicated key "can drift".
- transaction_created payload: type: buyer|seller (was side).
Catalog holds at 138, all LOCKED. Remaining: the package read + #31 signature.

##### 2026-07-29 — GATE 2 SITTING, ROUND 16 — LOOP IS ITS OWN OBJECT (Zach correction #2 on loops)
Round-15 "loop gets no keys" was WRONG — Zach: dotloop is standalone software with unique loop ids
(dotloop.com/my/loop/301276423); you create a loop on its own, rename it, change its status. LOCKED:
loop_created &#123;loop_id, name, transaction_id?&#125; · loop_edited &#123;loop_id, field: name|transaction_id|…&#125; ·
loop_deleted · loop_status_changed &#123;loop_id, from, to: &lt;loop statuses config&gt;&#125;. Documents/files live IN
the loop → document events carry loop_id (reverted from transaction_id); the loop carries
transaction_id when tied to a deal — a loop can pre-exist its transaction and link later.
Catalog now 142 rows, all LOCKED. Remaining: package read + #31.

##### 2026-07-29 — ✍️ GATE 2 CLOSED — ZACH SIGNED ("ok that is good enough for me now! signed off")
16-round sitting complete. THE SIGNED CATALOG: 142 kinds / 6 sections (Comms 19 · Website 29 · Work 24 ·
Deals 15 · Content 2 · System 53), zero dupes, extracted to tools/vocab/activity-catalog.signed-2026-07-29.json
(commit 90b8385b87) — THE seed for Gate 3. Board updated: all 7 Gate-2 items checked, header stamped
CLOSED/SIGNED 7/29, signature note replaced the awaiting-nod warn. Dev doc guides/activity-catalog.mdx got
the signed banner (6 laws + seed pointer) + the home_/property_ reversal fix; MDX code-span-wrap error fixed;
mirror publish running in background (verify "published": true before claiming docs live).
Discovery during extraction: round-9 pill-flip regex had silently FAILED to update ai_toggle_* payloads
(still scope-based) — fixed to the toggle-registry payload before the seed shipped. Lesson: parse-verify the
board, don't trust the edit.
NEXT SECTION = GATE 3 (per the signed plan): hl_activity_catalog table seeded from the signed JSON +
hl_user (type/role/id) + payload-schema enforcement at the ONE WRITE + the catalog CRM page + legacy remap
(property_looked_up→property_searched ×2 rows, two-generation aliases burn-down).

##### 2026-07-29 — GATE 3 AUTONOMOUS RUN (Zach at school pickup; orders: nothing destroyed, no notif/
##### trigger touched, software never stops) — FOUNDATION SHIPPED, commit 8961af0620
DONE (all additive): mig 0491 hl_activity_catalog (142 signed kinds, 6 sections, payload_schema jsonb
&#123;"hint"&#125;, lens col, emitted measured live = 24/142) + hl_user (210,036 rows: 6 agent · 1 ai · 36 lead ·
533 visitor · 209,460 bot; earliest visitor stamped on identified leads) — applied DIRECTLY first so
deploy migrate = recorded no-op (zero-downtime path). mig 0492 trigger hl_user_mirror_lead_trg (every
future lead mint auto-mirrors). /settings/activity-catalog page (MenuTable + HlDataTable read-only,
eslint-sanctioned) + sidebar above Trigger Events. user_created wired in manual Add Lead. Generator
unions catalog (184 kinds, --check green; insight_* nouns lawful; 4 irregular-verb grandfathers added;
property_ reversal noted in comments).
FOUND + FIXED ON THE WAY: committed event-kinds.generated.ts was 24 kinds STALE (ignoreBuildErrors:true
means deploys never typecheck) — 16 writer literals compiled against ghosts (call_received, text_sent/
received, call_note, in_app_message_received×3, transaction_under_contract). All repointed to canonical;
PROVEN zero-runtime-change (live rows already canonical: sms_received 726 / sms_sent 601 /
phone_call_received 468 / zero on old spellings). deal route under_contract branch → transaction_
status_changed &#123;to: under_contract&#125; (signed state-machine kind; transaction_closed untouched — live
wired trigger).
DEFERRED WITH RECEIPTS (.claude/reports/gate3-foundation-receipts-2026-07-29.json): FK move (26 orphan
kinds today — whole pre-sitting home_* generation, home_viewed 11,020); registration-clone delete
(3 two-flags mismatches recorded); badges fold-in (41 unreconciled keys); "~500 kinds" item SUPERSEDED
by the signed 142. property_looked_up remap deferred (8 code sites incl 4 comparisons — readiness ✗).
PRE-EXISTING, NOT MINE: playbooks page type error (slotKeys vs slotLabels, Adam's WIP).
PENDING: prod verify of /settings/activity-catalog (poll running) → screen-walk → docs update → notify.

##### 2026-07-29 15:10 CT — GATE 3 RUN COMPLETE (deploy verified)
Railway SUCCESS 15:07 on commit 8961af0620. Boot log: "[migrate] up to date (523 migrations, all
recorded)" — 0491+0492 pre-applied, deploy migrate = no-op, ZERO DOWNTIME (Zach's order held). "Ready
in 789ms". Docs published (mirror true). Artifact stamped + republished. Zach notified via notify.sh.
AWAITING ZACH: eyes on crm.houseloop.ai/settings/activity-catalog (six tabs) = the human half of the
Gate 3 screen-walk; then his word on the three deferred mutations (FK move · clone delete · badges
fold-in), each with its readiness receipt in gate3-foundation-receipts-2026-07-29.json.
Note for later: Dockerfile CMD uses "next start" but next.config has output:standalone (warning in boot
log, pre-existing, works — flag to Zach before touching).

##### 2026-07-29 16:52 CT — CATALOG PAGE v2 LIVE (Zach's screen-walk fixes, commit 62c599c739)
Railway SUCCESS 16:51. Shipped: payload column = real JSON layout (one field per line, never
truncated) · "Lens"→"Website Pill" + tooltip · "Emits"→"In Use" + tooltip · "Live rows" (was
truncating)→"Live Runs", clickable → NEW drill-down /settings/activity-catalog/[kind] (every recorded
run via alias closure, lead names link to lead detail). Also this session: diagnosed Zach/Adam's 500s
= transient DNS to autorack (ENOTFOUND, ~9k log lines old, all healthy now, zero real prod errors).
NEXT GATE-3 PASS (Zach soft-confirmed interest): convert 142 payload hints → machine-enforceable
schemas; validation at logActivity in WARN-FIRST mode (log would-be rejections, break nothing), then
hard-enforce on Zach's word. Config-table-driven values (outcomes, statuses) validate against their
tables at runtime, never hardcoded enums.

## Related documentation

Doc sections this work creates or updates:

- [`guides/intent-workflows`](/guides/intent-workflows)
- [`guides/workflows`](/guides/workflows)
- [`guides/registration`](/guides/registration)
- [`guides/web-registration-and-intake`](/guides/web-registration-and-intake)
- [`guides/tour-and-contact-requests`](/guides/tour-and-contact-requests)
- [`fub/trigger-events`](/fub/trigger-events)
- [`fub/lead-import`](/fub/lead-import)
- [`platform/message-catalog`](/platform/message-catalog)
