---
title: "Lee (Pipeline and Workflow Master Overview)"
description: "Work-in-progress dossier — Lee · Infra · Pipeline & Workflow · FUB · Notifications. Pipeline and Workflow Master Overview."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/lee/pipeline-workflow-overview.md` on every push. **Progress: 9/21 done (43%).** _Updated 2026-07-09._</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 796h ago. If Lee has shipped since, this dossier under-reports — refresh `.claude/state/wip/lee/pipeline-workflow-overview.md`.</Warning>

**Owner:** Lee — Infra · Pipeline & Workflow · FUB · Notifications

## Checklist

**⭐ 2026-07-09 — WORKSTREAM ABSORBED into the reorganized docs.** The old canonical
`guides/pipeline-workflow-overview` was DELETED in the doc consolidation (redirects to /myli-master); the living
story is now the ONE "Pipeline & Workflow" nav group ordered **Lead Integration → Trigger Events → Workflows**
(`guides/web-registration-and-intake` → `guides/intent-workflows` → `guides/workflows`), with the FUB side in
**`fub/trigger-events` ("FUB Trigger Events & Notifications", the 22-event master)** and notifications in
`platform/message-catalog`. The checklist below is the historical build record of the reorg that shipped.

**North star (as-built):** HouseLoop's lead engine to MVP. ONE platform (HouseLoop, never named); everything is a
Lead Source with events; `Workflow = f(source × event)` for the registration family, one intent canvas each.
HouseLoop is the first-class notifier; Realloop retired.

### 🆕 ACTIVE WORKSTREAM (2026-07-03) — Lead-Detail Parity Lanes + Polymorphic Assignment
**PRD/SSOT:** `GT/GT - ACTIVE/GT_HOUSELOOP_LEAD_DETAIL_PARITY_AND_ASSIGNMENT_PRD.md` · **scope:** `session-scope-parity-lanes.lock` · **handoff:** `.claude/state/lee-handoff.md` · **prod baseline:** `98650ba504`
Both lead-detail lanes (LEFT = human + AI clone · RIGHT = Myli) share ONE skeleton: `📋 Tasks & Appointments`
(ALWAYS on) + `🤖 AI Workflow` (gated). Grouped polymorphic `Assign to` (Agents · AI Agents · TCs). Clone
task-level override (clone-assigned task fires without global AI mode).

**SHIPPED (pushed, deploying):** Phase 0 docs/PRD (3 new + fixes) · Phase 2 mig 0306 (clone actor_type) ·
Phase 3 grouped assignee picker · Phase 4 parity lanes (fixed the `aiActive` gate that hid human
tasks/appointments — T&A now always-on both lanes; per-lane scoping + faint/mirror; bold toggle) · v2 Tags
under T&A · Myli 4-row Contact. Awaiting Zach's final live sign-off.

**🎯 THE GOAL — NEXT: Phase 5 (Zach-directed 2026-07-03), build end-to-end THEN test.**
(1) **Clone config UI** — surface `POST /api/team/clone` + `hl_member_role` persona/tools override in Team
settings so a member creates + configures their AI Clone. (2) **Task-level override DISPATCH** — a task
assigned to a clone actually FIRES the clone (eligible types only) without the global AI toggle, via
`hl_scheduled_actions.actor_type='clone'`, clone acting AS its human. (3) Model-row real value + Myli
iMessage-reassign → Team settings. (4) Live end-to-end test → Zach sign-off.

### 🧪 SIGN-OFF MATRIX — every webhook, top to bottom (Zach + Lee, iterate live)
**How we run it:** go row by row. Lee hands the **test-lead payload** for `localhost:3020`, Zach fires it, we **log
every gate**. A row **signs off (✅)** only when: (1) the webhook lands + the lead/activity card renders correctly,
(2) Creates-Lead behaves as set, (3) it's **wired to the CORRECT notification** (notify_class / notify_owner — this is
also checking **Benji's** notification work), and (4) its workflow (if any) fires. `⬜` = not yet verified.
_Source of truth: `hl_fub_webhook_settings` (FUB) · `hl_trigger_events` (HouseLoop, notify_class) — pulled 2026-07-02._

### A. Follow Up Boss webhooks (`hl_fub_webhook_settings`)  — all `notify_owner=realloop`, no workflow bound yet
| # | Webhook | Creates Lead | Notify → | Workflow | Result |
|---|---|---|---|---|---|
| A1 | `property_inquiry` | ✅ | realloop | — | ⬜ |
| A2 | `tour_request` | ✅ | realloop | — | ⬜ |
| A3 | `contact_request` | ✅ | realloop | — | ⬜ |
| A4 | `rental_inquiry` | ✅ | realloop | — | ⬜ |
| A5 | `seller_inquiry` | ✅ | realloop | — | ⬜ |
| A6 | `virtually_toured` | ✗ | realloop | — | ⬜ |
| A7 | `text_message` | ✗ | realloop | — | ⬜ |
| A8 | `in_app_message` | ✗ | off | — | ⬜ |
| A9 | `notification` | ✗ | off | — | ⬜ |
| A10 | `email_received` | ✗ | off | — | ⬜ |
| A11 | `property_viewed` | ✗ (counted signal) | off | — | ⬜ |
| A12 | `property_saved` | ✗ | off | — | ⬜ |
| A13 | `property_shared` | ✗ | off | — | ⬜ |
| A14 | `insight_high_intent_buyer` | ✗ | off | — | ⬜ |
| A15 | `insight_high_intent_seller` | ✗ | off | — | ⬜ |
| A16 | `insight_viewed_10x_7d` | ✗ | off | — | ⬜ |
| A17 | `insight_new_area` | ✗ | off | — | ⬜ |
| — | `_global` (master switch) | n/a | n/a | — | n/a |

### B. Phone-call webhooks (FUB → the Phone Numbers section)
| # | Webhook | Creates Lead | Notify → | Workflow | Result |
|---|---|---|---|---|---|
| B1 | `phone_call` (generic) | ✗ | realloop | — | ⬜ |
| B2 | `phone_call_company` | ✗ | realloop | — | ⬜ |
| B3 | `phone_call_sign` (sign/listing line) | ✗ | realloop | — | ⬜ |
| B4 | `phone_call_past_agent` | ✗ | realloop | — | ⬜ |
| B5 | `phone_call_ana` (Ana's old cell) | ✗ | realloop | — | ⬜ |

### C. HouseLoop webhooks (`hl_trigger_events`, platform `houseloop`)
| # | Webhook / event | Creates Lead | Notify class | Workflow | Result |
|---|---|---|---|---|---|
| C1 | New Lead — General Registration | ✅ | `lead_created` | ✅ | ⬜ |
| C2 | New Lead — Liked Home | ✅ | `home_liked` | ✅ | ⬜ |
| C3 | New Lead — Saved Home | ✅ | `home_saved` | ✅ | ⬜ |
| C4 | New Lead — Saved Search | ✅ | `saved_search` | ✅ | ⬜ |
| C5 | New Lead — Tour Request | ✅ | `tour_request` | ✅ | ⬜ |
| C6 | New Lead — Contact Request | ✅ | `contact_request` | ✅ | ⬜ |
| C7 | New Lead — Seller Get Report | ✅ | `home_value_report` | ✅ | ⬜ |
| C8 | New Lead — Seller Get Started | ✅ | `seller_request` | ✅ | ⬜ |
| C9 | New Lead — Shared Home | ✅ | `home_shared` | ✅ | ⬜ (now in `hl_trigger_events`, mig 0287) |
| C10 | System — Property Viewed | ✗ (counted signal) | `property_viewed` | — | ⬜ |
| C11 | System — Property Liked | ✗ | `home_liked` | — | ⬜ |
| C12 | System — Property Saved | ✗ | `home_saved` | — | ⬜ |
| C13 | System — Property Shared | ✗ | `home_shared` | — | ⬜ |
| C14 | System — Saved Search (client) | ✗ | `saved_search` | — | ⬜ |
| C15 | System — Saved Search · Agent | ✗ | `saved_search` | — | ⬜ |
| C16 | System — Saved Search · System | ✗ | `saved_search` | — | ⬜ |
| C17 | System — Tour Request (existing lead) | ✗ | `tour_request` | — | ⬜ |
| C18 | System — Contact Request (existing lead) | ✗ | `contact_request` | — | ⬜ |
| C19 | System — Seller Get Report (existing lead) | ✗ | `seller_request` | — | ⬜ |

**Gaps flagged from the pull (2026-07-02):** (1) C9 Shared Home missing from `hl_trigger_events`. (2) Every FUB row is
still `notify_owner=realloop` — the HouseLoop-notifier migration (per-row flip) hasn't started; each becomes a matrix
sign-off as it moves to `houseloop`. (3) No FUB row has a `workflow_id` bound yet.

---

### SHIPPED (Phase 0 — foundation)
- [x] Lead Sources = ONE page (`/settings/lead-platform`): source block = header + KPI funnel + events + Configure
- [x] Collapsed Registration Events + Lead Sources + FUB Settings into the one page
- [x] Workflows grouped BY lead source; `Workflow = f(source × event)` (reversed the old "agnostic" model)
- [x] Dynamic Back → "Back to Lead Sources"; canonical `HlBackLink`
- [x] FUB webhook config (per-type toggles: Process / Creates-Lead / Notify / Notify-Via) + 17-event matrix (migs 0274/0275)
- [x] Phone Numbers registry `hl_phone_numbers` (41 FUB lines) + `/settings/phone-numbers`
- [x] Notifications = Everything Phase 1 (catalog-derived notification list from the same event tree)
- [x] R2/R3/R4 shipped (mig 0270 `notify_owner`): HouseLoop native notifier + notify-toggle repoint + per-event Realloop kill-switch

### ARCHITECTURE RAILS (must be green before matrix rows pass)
- [ ] R1 Native FUB ingestion (Realloop mirrors `fub.*` data only, stops being the notifier)
- [ ] R8 Webhook test harness (fire/replay a real FUB webhook + verify every gate)
- [ ] R9 Native dialed-line preserved (Ana 913-954-7123 — never the forward target)

### OPEN BUILDS (the keystones)
- [ ] 1d. FUB SUB-SOURCE dimension — split FUB events by portal so `(FUB × sub-source × event)` binds its own workflow (the keystone for source-specific FUB copy)
- [ ] 1e. Email received (`emailsCreated`) — settings row + native HL notify + matrix row
- [ ] 2. A workflow (text + email + nurture) for EVERY event (add missing text steps; DRAFT→ACTIVE the existing-lead doors)
- [ ] 3. Zillow-inbound render MVP (Zillow logo, "Find on HouseLoop", iMessage-bubble restyle)
- [ ] 4. FUB Phone Call Activity engine — ONE `(FUB · Phone Call)` workflow, Myli reads the dialed line → category → copy; text-back from BlueBubbles 913-208-1633; Ana email bridge; then the Twilio port
- [ ] 5. Past-client reconciliation (single canonical table → listing page → seller report → Ana campaign)
- [ ] 6. Go-live — quarantine legacy FUB Zillow doors, E2E-verify each real inquiry in the Run Log

### SIGN-OFF MATRIX — recent edits
- [x] **Viewed home** added to the HOUSELOOP → Existing-lead matrix as a **COUNTED signal** (Zach 2026-07-01):
  no workflow — increments the lead's Visits/Properties counter + "viewed N×"; can gate a workflow with a count
  condition. Backed by `hl_trigger_events.property_viewed` (system, houseloop, mig 0276).

### NEXT CONCRETE ACTION (Lee)
- [ ] KEEP THE OVERVIEW DOC CURRENT — reconcile it against today's changes: Benji's saved-search 3-trigger split (saved_search=client · saved_search_agent · system) is not reflected in the HouseLoop registration/existing-lead matrix; update it.
- [ ] Then resume the Sign-Off Matrix one row at a time (Zach coming back with direction on lead sources + event triggers).

---

## PRD · `GT/GT - ACTIVE/GT_HOUSELOOP_LEAD_PLATFORM_PRD.md`

#### GT — HOUSELOOP LEAD PLATFORM PRD (Zach + Lee, 2026-06-26) — ✅ SIGNED, BUILD

**Owner:** Lee (end-to-end). **Status:** model locked + signed by Zach; building now.
**Design SSOT:** `.claude/checkpoints/lead-system-MAP-2026-06-26.md`. **Scope lock:**
`.claude/state/session-scope-platform-source.lock`. Supersedes the platform/source UI direction in
`GT_HOUSELOOP_LEAD_FLOW_UI_PRD.md §12` (that doc's attribution columns ship as-is; the NAV/screens here win).

---

##### 1. THE PROBLEM
HouseLoop is the website AND the CRM in one product — and Follow Up Boss is a legacy CRM piping in leads with
history. "Lead source" alone can't express that. We spent a day circling because two things were tangled:
**where a lead came from** vs **what it does on our site**. This PRD untangles it.

##### 2. WHAT THE RESEARCH SAID (5 CRMs — HubSpot, Salesforce, UTM/GA4, FUB/kvCORE/Sierra, Pipedrive/GHL/AC)
Unanimous findings (full briefs in session history):
- **Two fields, never one:** a TRUE ORIGIN (Zillow) kept separate from the CONNECTOR (Follow Up Boss). Origin
  is first-touch + immutable. (HubSpot Original vs Record Source; Salesforce LeadSource vs Source System.)
- **Events ≠ source.** Activity is a separate growing stream; source is a stamp set once. (We already do this:
  `hl_lead_activity`.)
- **Trigger on the EVENT, filter on SOURCE/PLATFORM.** Never one workflow per source. Merge fields fill the
  words. (Every CRM. Sierra/GHL/AC explicit.)
- **No "type" field** for origin — "type" = the event (already our catalog) or the pipeline role (already our
  Buyer/Seller pipelines). Don't add it.
- **Don't prefix `fub-zillow.com`** — keep source = the real origin (Zillow), connector separate (live FUB data
  with 48k people confirmed this). Platform = the connector/world.

##### 3. THE MODEL (LOCKED)

###### 3.1 The keystone — TWO buckets, TWO routing keys
| Bucket | Keyed on | Meaning |
|---|---|---|
| **New Lead Registration** | the lead's **ORIGIN** (source + platform) | "how they came in." realtor.com/Zillow via Follow Up Boss → that source's registration (the reactivation). Origin is **set once, permanent** — a merge field forever, but stops STEERING after registration. |
| **(HouseLoop) System Events** | the **SURFACE** (where the action fired) | "what they do on HouseLoop." ANY lead — regardless of origin — who likes a home / requests a tour on HouseLoop.ai fires the **HouseLoop** workflow. |
| **Follow-Up** | schedule | the nurtures |

**Proof (Zach's, universal):** lead acquired platform=Follow Up Boss, source=Zillow.com → registration runs
**Reactivation** ("you worked with Alicia… homes on HouseLoop.ai"). Origin stays Zillow forever. They then like
a home **on HouseLoop.ai** → SURFACE = HouseLoop → runs the **HouseLoop liked-home workflow**, not the FUB one.

###### 3.2 The cascade (first match wins)
```
A lead's event fires →
  1. SPECIFIC workflow for (this source + this event)?    → run it
  2. else a workflow for (this platform)?                 → run it   (e.g. FUB → Reactivation)
  3. else GENERAL REGISTRATION (the catch-all floor)      → always runs ({{lead_source}} fills the words)
```
Already engine-true: `dispatchDoor` routes existing-lead events by event SURFACE and keeps registration
ORIGIN-scoped (built P3). New sources work instantly off the floor; specials are opt-in.

###### 3.3 Scale = MERGE FIELDS
One event → one workflow → `{{first_name}}` `{{home_address}}` `{{lead_source}}` `{{platform}}` fill the rest.
Never a workflow per source. Adding a source = a new ROW, not a rebuild.

##### 4. NAV (LOCKED) — 3 items (Lead Flow folded away)
```
Pipeline & Workflow
  ├─ Lead Platform     ← master: Platform → Source → registration doors + system events
  ├─ Lead Sources      ← thin list: the 3 sources
  └─ Workflows         ← the categorized workflow library
```

##### 5. LEAD SOURCES — EXACTLY 3 (all else killed)
| Source | Platform | Type | Default Stage |
|---|---|---|---|
| HouseLoop.ai | HouseLoop | web | New Lead |
| Zillow.com | Follow Up Boss | portal (fix from `manual`) | New Lead |
| Manual entry | HouseLoop | manual | New Lead |
KILL (deactivate, reversible): Referral · SRG-Follow Up Boss (`fub_import`) · `__platform_houseloop__` ·
`__platform_fub__` · IDX Sign-up. **Build-order care:** `fub_import` holds 6 FUB leads + their reg doors →
REASSIGN leads + doors onto Zillow.com FIRST, then retire it. Nothing lost.

##### 6. EVENT NAMING — the "New Lead –" prefix IS the registration-vs-system tell
- **Registration doors** (origin): prefixed **"New Lead – …"**. HouseLoop.ai = 8: New Lead – General
  Registration · Liked Home · Saved Home · Saved Search · Tour Request · Contact Request · Seller Get Report ·
  Seller Get Started. (3 need relabel today: liked_home/saved_home/general_registration; other 5 already correct.)
  Zillow.com = 2: New Lead – Tour Request · New Lead – Contact Request (= "property inquiry"). (Move from
  fub_import; rename "FUB Property Tour"→"New Lead – Tour Request"; drop FUB's general/seller/saved-search regs.)
- **System events** (surface): UNPREFIXED: Tour Request · Contact Request · Saved Search · Seller Get Report ·
  Seller Get Started · Property Viewed · Property Liked · Property Saved · Property Shared.

##### 7. THE SCREENS (wireframes — final, real names)

###### 7.1 LEAD PLATFORM  → cascades into Workflows (click an event row → its workflow)
```
╔═ HouseLoop ════════════════════════════════════════════ platform ═╗
  ─ SOURCES (origin → New Lead Registration) ──────────────────────
   🌐 HouseLoop.ai — Organic ················· 1 lead · 8 doors
        New Lead – General Registration  → General Welcome
        New Lead – Liked Home            → Liked Home
        New Lead – Saved Home            → General
        New Lead – Saved Search          → General
        New Lead – Tour Request          → General
        New Lead – Contact Request       → General
        New Lead – Seller Get Report     → General
        New Lead – Seller Get Started    → General
  ─ HOUSELOOP SYSTEM EVENTS (surface → regardless of source) ──────
        Tour Request · Contact Request · Saved Search · Seller Get Report ·
        Seller Get Started · Property Viewed · Property Liked · Property Saved · Property Shared
  ─ FOLLOW-UP ──  (none yet)
╔═ Follow Up Boss ═══════════════════════════════════════ platform ═╗
  ─ SOURCES (origin → New Lead Registration) ──────────────────────
   🌐 Zillow.com ····························· 6 leads · 2 doors
        New Lead – Tour Request          → Reactivation Campaign
        New Lead – Contact Request       → Reactivation Campaign
  ─ SYSTEM EVENTS ──  (none — activity happens on HouseLoop)
```
Layout: blue platform header → source sub-section → bucket sub-headers → event rows (Event · Workflow ·
Active). Brand: white-on-blue header. Tables, not chevron trees.

###### 7.2 LEAD SOURCES  → cascades into Lead Platform (click a source → its block)
```
 SOURCE          PLATFORM          TYPE      DEFAULT STAGE   LEADS   REG DOORS
 HouseLoop.ai    HouseLoop         web       New Lead          1        8
 Zillow.com      Follow Up Boss    portal    New Lead          6        2
 Manual entry    HouseLoop         manual    New Lead          0        0
```

###### 7.3 WORKFLOWS  → cascades into the canvas. The automation LIBRARY (grain = one row per WORKFLOW)
Categorized Registration / System / Follow-Up (NOT "Other"). Columns: Workflow · Trigger · Steps · Completed ·
Status (+ Platform · Source as light context). Steps = counted from the canvas; Completed = `hl_workflow_runs`.
```
═ REGISTRATION ═══════════════════════════════════════════════════════════
 WORKFLOW                         TRIGGER                STEPS  COMPLETED  STATUS
 New Lead – General Registration   New Lead – Gen. Reg.    4    138/142    active
 Reactivation Campaign             New Lead – Tour Req.    6      6/7      active
═ SYSTEM ═════════════════════════════════════════════════════════════════
 Liked Home                        Liked Home             3     12/12     active
═ FOLLOW-UP ══════════════════════════════════════════════════════════════
 90-Day Nurture                    schedule               8      4/20     active
```
NOT a clone of Lead Platform: Lead Platform rows = DOORS (by source); Workflows rows = WORKFLOWS (the canvas +
its health). One workflow can serve many doors.

###### 7.4 LEAD DETAIL (already shipped — confirm labels)
`Source: Zillow` + `Platform: Follow Up Boss` in the Contact card.

###### 7.5 WORKFLOW TRIGGER (the one engine add)
Open a workflow → trigger node: `Event [▾] · Platform [▾ any/HouseLoop/FUB] · Source [▾ any/Zillow/…]`.
This is what makes "Registration WHERE platform = Follow Up Boss → Reactivation" buildable.

##### 8. BUILD PHASES (Lee, all reversible/additive where possible)
- **P1 — Data cleanup migration:** reassign FUB leads+doors → Zillow.com; retire junk sources (deactivate);
  fix Zillow type→portal; relabel the 3 HouseLoop doors + the Zillow doors with "New Lead –".
- **P2 — Lead Platform page:** Platform → Source → (New Lead Registration · System Events · Follow-Up), real
  data, tables, blue headers, workflow-per-row. (Replaces the scrapped tree.)
- **P3 — Lead Sources page:** the thin 3-row table (Source · Platform · Type · Default Stage · Leads · Reg Doors).
- **P4 — Workflows page:** categorize Registration/System/Follow-Up + Steps/Completed columns.
- **P5 — Workflow trigger:** Platform + Source filter dropdowns on the trigger node.
- **P6 — Nav:** Lead Platform · Lead Sources · Workflows (remove Lead Flow).
- **P7 — Verify on prod (Playwright) + handoff/docs.**

##### 9. WHAT'S ALREADY BUILT (don't redo)
Lead `platform`/`channel` cols + backfill (mig 0247) · lead-detail Platform/Source rows · dispatchDoor
origin-vs-surface routing (P3) · `hl_lead_activity` event stream · the workflow canvas · the 3-bucket Lead Flow
grouping (becomes the System Events section). The work is the SCREENS + the data cleanup + the trigger filter.

##### 10. NON-GOALS
No `fub-` prefix. No "type" field. No per-source events (merge fields handle words). No Lead Flow nav page.
No inventing sources beyond the real 3. Grows by ROWS.

---

## Scope lock · `.claude/state/lee.lock`

AGENT: Lee
ROLE: Chief of Staff / Systems Architect / signing desk

⭐⭐⭐⭐⭐ 8/8–8/9 — THE CARD RENDER PORT (active; scope: .claude/state/session-scope-card-catalog.lock).
Goal: ONE catalog → resolveCard → &lt;ActivityCard&gt; rendered by EVERY surface, no forks. Walk each family on
Lucy Hartman (714cb133…), commit LOCAL ONLY. ⛔ DO NOT PUSH until the whole port is done (Zach).
- DONE + SIGNED OFF (local): PROPERTY card (viewed/saved/liked/shared, eed260923c) · HERO-ENGAGEMENT
  (virtual tour/tour request/inquiry — blue, Zillow WHITE BOTTOM BAND not a pill, buyer msg italic in body,
  links-vs-message rule, address→Zillow link; 48ca7ebfda/efcd85705c) · SAVED-SEARCH card (criteria in the
  address slot; mig 0716; 1c91376548). Root: ONE caller in CenterLane (fork A killed), resolveCard
  densityFor home_card/insight→hero, portal fallback fub→Zillow Preferred/native→HouseLoop.ai, eye+requests+
  insights→blue.
- BUILT NOT WALKED: INSIGHT card (mig 0715 card_layout=insight; pill line1 + signal text line2, no trigger
  line). Needs a recent-insight lead to walk (Lucy's are ~1000 rows deep; feed loads a recent window only).
- DIAGNOSED, NEEDS ZACH: impostor sms_sent residual = 73 ORPHANED runlog rows (backfill 8/4 copied them from
  hl_messages before the 8/7 sweep; sweep didn't cascade to hl_activity_runlog). FIX = export + DELETE 73
  (reversible) → needs TYPED Rule-0 go on hl_activity_runlog. Item #26 on ac8e921b.
- ARTIFACTS: checklist ac8e921b = 8 done (updated + republished); locked spec = wireframe 154c7e90.
- NEXT: in-app messages · Website-tab (/api/pixel/events) unification (#14) · price reliability (#20).

⭐⭐⭐⭐ 8/7 LATE PM — THE SCHEMA LOCK is the ONE SSOT + comms cutover + §5 composer. (memory: project_houseloop_schema_lock_is_the_one_ssot · _comms_cutover_2026-08-07 · _saved_search_crm_and_person_model)
- GOLDEN THREAD: promoted the Activity Catalog artifact (ac8e921b) to THE SCHEMA LOCK (SSOT). Canon written ONCE
  (.claude/artifacts/_architecture-canon.mjs) + stamped as a POINTER on all 8 boards (stamp-canon.mjs). ACTOR IS BACK
  (reverses #578) — stale "no actor" text reversed at source (schema-atlas.mjs generator + overview.mdx + ai-auth +
  transaction-coordination guides). All docs mirrored (published:true).
- SIGNED RULINGS locked in the Schema Lock (all BUILD PENDING): saved search = CRM object (hl_saved_search, keyed to
  person, source houseloop|zillow, upsert (lead_id,source,criteria_key), activity family created/edited/paused/deleted/
  alert_sent) · PERSON MODEL (person=entity, lead=a type; hl_leads is de-facto people table; rename hl_leads→hl_people +
  people/NNN GATED) · WEBSITE MODEL (hl_pixel_event→hl_website_activity [rename, 66 files, GATED bridge], site=tag reusing
  Listing.mlsName, insights+engagement DERIVE from it, NO reinvent — Listing/hl_property/HLID already exist).
- COMMS CUTOVER DONE + verified prod: Adam split the writer (transport vs comms + backstop, d90fb8d1f8/0820dc8142, proven
  638→638). Lee deleted 638 impostor cards from hl_messages (Zach TYPED Rule-0 go), reversible snapshot
  .claude/reports/comms-cutover-2026-08-07/. Mary/Alyce feeds verified clean. RESIDUALS (flagged, NOT done): 97 blank
  inbounds (34 gmail by-design + 63 SMS Twilio-SID, no bulk) + FUB "Assigned to…" scrape-noise (Adam import lane).
- §5 RENDER COMPOSER: SHIPPED + DEPLOYED (commit 8d3f779cd7) + AUDITED LIVE ON PROD. Catalog-driven grammar
  (migs 0702/0703, columns registered in the signed diet). Endpoint composes row.sentence; CenterLane renders it
  (calls/email/doc keep signed wordings). RENDER AUDIT on Msale Olongo (live): 21 kinds, 18 pass, +full-matrix
  catalog coverage — stored in ac8e921b Audit Results + real screenshot (.claude/reports/s5-audit-2026-08-08/).
  5 FINDINGS filed (items 13-16), root-diagnosed: note_added/zillow_message_sent render "An agent" because the
  WRITER doesn't stamp the actor (58/638 notes resolve) — §5/composer is CORRECT, the gap is upstream actor-stamping
  (§3 not fully applied). clone_activated/lead_assigned carry full passive sentences (not composable). REMAINING loop:
  the interact/import E2E (rehome test contact zach@spradlingrealtygroup.com/4074038088, perform each UI action ×
  FUB/human/system, verify table-write + import 8m relay + render + shot, then restore + purge) — relay-gated.
- (prior) §5 Phase 1+2 was: composeSentence() in lib/activity/render-activity.ts,
  grammar for all emitted kinds, acceptance test render-activity.sentence.test.ts = 14/14 signed sentences, tsc 0.
  scope-lock: session-scope-s5-render-composer.lock. PHASE 4 NEXT (walk w/ Zach): route Run Log→iPad feed→Website→mobile
  through .sentence, delete hand-rolled headlines. Board ac8e921b active band leads with §5 (item 2).
- ALSO PARKED (Zach signed, not built): hl_pixel_event→hl_website_activity rename (bridge approach, 66 files) · hl_home_interest
  (saved/liked/hidden homes managed table).

⭐⭐⭐ 8/7 PM — ACTIVITY CATALOG UNIFICATION + CUTOVER CLEANUP (board ac8e921b, PRD GT_HOUSELOOP_ACTIVITY_CATALOG_UNIFICATION_PRD).
SHIPPED+DEPLOYED today: §1 actor_type (mig 0699) · §2 fold user ai→ai_agent + hl_system_actor.type + houseloop_ai (0700) ·
insights defined+typed job (0701) · §3 writers stamp actor (log-activity.ts) DEPLOYED · A2 ROOT FIX: backfill-fn.ts excludes
recipient_role='agent' receipts (the "Zach texted" leak — the backfill replayed hl_messages agent-alerts as lead sms_sent,
bypassing the live logMessage isAgentAlert guard) DEPLOYED. Actor canon SIGNED (kind&#123;user·lead·system&#125;×type&#123;human·ai_agent·
ai·job&#125;), §5 render grammar captured (Zach's who-did-what sentences), THE CLEANUP GAME PLAN on the board (A writers·B data·
C render·D finish PRD).
DATA CLEANUP DONE (reversible — rows exported to .claude/reports/notif-cutover-2026-08-07/deleted-rows-snapshot.tsv, 316 rows):
  deleted 273 notification-as-lead-text activity rows (B3/B3b/B3c, Zach TYPED Rule-0 approval) + 43 generic null-date
  saved-search rows (B1). Real events (phone_call_received etc.) verified to exist separately (7/7). ⚠ snapshot TABLE was
  dropped (triggered schema cage) — the .tsv IS the reversal now.
OPEN (awaiting Zach): (1) hl_messages→hl_notification CUTOVER — 356 agent-receipts pollute the comms table (156 already in
  hl_notification, 200 orphans). Zach said "do it correctly" but I FLAGGED it's Adam's live notification lane + a core-comms
  design change; asked proceed-now vs pause. (2) B2 dedup saved-search generations (Shamir 16→~5). (3) A3 saved-search import
  fix. (4) §5 the ONE who-did-what render composer (render-activity.ts → every surface). NOTE: Zach caught me vibe-coding
  (reactive body-LIKE deletes); corrected to root-cause (the backfill guard). Zach: no texting other agents.

LAST_UPDATED: 2026-08-07 ~13:05 CT — Opus session, mid-flight on the IDENTITY MODEL loop (599-607).

⭐ ACTIVE: THE IDENTITY MODEL — who acted is USER · LEAD · SYSTEM (board 209ff52e, active band 599-607).
Zach said GO 8/7 + "execute in a loop." Task list #1-#7 = board #601-#607.

DONE THIS SESSION (committed):
- #599 vocabulary + #600 hl_system_actor registry (prior, signed).
- #601 (r66, committed d289aa900a): by_kind/by_user_id/by_lead_id/by_system_id/by_name land beside the old
  actor trio on hl_activity_runlog (mig 0694). FK ON DELETE SET NULL + by_kind CHECK. Proven live.
- #602 (r67, d289aa900a): backfill 29,854 rows into by_* (mig 0697, psql -f NOT the runner), blanks left
  honest (14,540 lead + 6,023 system + 1,051 human carry no invented id), 0 FK orphans.
- #603 (committed + PUSHED 0cebef89e2, DEPLOYING): logActivity dual-writes by_*; THE CLONE bills the CLONE
  (deriveByColumns resolves clone_of_user_id → the clone user; robust to actor=human-id AND actor=clone-id).
  tsc EXIT 0. Verification: background monitor bbnkive36 watching for post-deploy by_kind rows. The 9
  historical clone rows already billed the clone correctly (measured, no fix needed).

NEXT IN LOOP:
- #603 verify (deploy landing) → tick + r68 + republish.
- #604 readers (~90 files) + SCREEN WALK. #605 hl_messages + 5 small tables. #606 GATED (Zach day-of word
  + post-pour, IRREVERSIBLE). #607 the anti-drift gate.

MY OTHER OPEN ITEMS:
- #608 (Zach ruled OPTION 1 8/7: derive dialer identity from SESSION). SCOPED: token-mint + call-mobile
  ALREADY session-bound; fix is CONTAINED to voice/outbound/route.ts (read identity from Twilio-signed From
  / session-set query, drop the client userId override). Multi-file security, mine, un-started.
- #609 FILED (Zach 8/7): website-activity digest leaks raw &#123;&#123;#show_bots&#125;&#125; (composeTemplate only matches
  namespaced &#123;&#123;#ns.field&#125;&#125;; digest passes flat scope) + fires 3× as "replied by email". Notification
  reconciliation lane (#231/#542). Evidence: Zach's 8:04 screenshot.

TEAM UNBLOCKED THIS SESSION (signing desk, ~5×): signed hl_document_template_set+_item (Benji #156) ·
regenerated lock for hl_contract_fill→hl_signed_document (Benji #157 rename) · coordinated migration
numbers (Benji 0695, Adam 0696) · fixed my own hl_contract_fill diet-rename that orphaned 0677 ·
committed my untracked 0694/0697 that blocked Adam. Adam shipped #54-#56. Benji shipped the Loop 156/157.

⭐⭐⭐ CANON SIGNED 8/7 (Zach: "lock it, this has to be canon") — THE ACTOR MODEL. actor = top-level word for
who/what did a thing = &#123;kind · type · id · name&#125;. kind &#123;user·lead·system&#125; × type &#123;human·ai_agent·ai·job&#125;:
human=real person · ai_agent=AI PERSONA holding a seat (Myli, a clone; name+id, clone_of? distinguishes Myli
NULL vs clone→human) · ai=system COMPUTE (vision/SEO, carries model+cost) · job=plain automation. RULES: actor
= who CAUSED it (human clicks refresh-AI-summary → user/human, NOT Myli); cost rides on the ROW not the type.
⚠ REVERSES #578 (which killed "actor" — now it's the canonical umbrella). Locked in board record §1 (artifact
ac8e921b), memory project_houseloop_actor_model_canon, ai_agent added to Lee-owned kind-vocab-ignore.txt.
EXECUTION = step-0/#604-607: rename by_*→actor_* + add actor_type, fold user-type ai→ai_agent (2 clones),
grow hl_system_actor (type col + ai functions). NOT built yet — this is the DEFINITION signed, build follows.
TODO NEXT: mirror the canon into docs-site target-model.mdx (the signed-rulings doc).

⭐⭐ ACTIVE NOW (Zach pivoted 8/7 PM — "the ONLY thing I want to work on"): THE ACTIVITY CATALOG MASTER AUDIT.
Reconcile the HouseLoop activity catalog vs the FUB-imported one so everything renders through the right lens,
writes through the right cards, notifications + automation triggers all link to the catalog (no standalone
trigger events). ME + ZACH ONLY, one list 1→∞, one by one, receipts — NOT a workflow.
  ARTICLE: .claude/artifacts/activity-catalog-master-audit.json → artifact ac8e921b-f4ba-4aed-bd05-674345a102fd.
  SPINE PROVEN: hl_activity_catalog (186 kinds) already carries the lens columns (sentence/lens/card_layout ·
    notify/notify_class · automation · is_system/category/emitted). Gap-shape: 18 no-sentence · 55 emitted (131
    dead) · 2 notify-on-no-class · sms_sent AND sms_received both render "sent a text" (the direction+actor bug).
  THE 5 LENSES per kind: render (actor+verb+direction+counterparty) · native write · FUB-import map (100%) ·
    notification (catalog key) · automation trigger (from catalog).
  ✅ ALL 7 SOURCES CONSOLIDATED (8/7, Opus): fanned out 5 readers (①③④⑤⑥) in parallel, each distilled its
    board into catalog-relevant findings only (kept my context clean). ①lead-engine=richest (catalog arc/#526,
    direction/#231, identity/#601-603, defects #610/#541/#598). ③tc-loop=deal family (LOOP≠TRANSACTION #17,
    commission kinds). ④connected-people=NO writer emits the connect event. ⑤seller-engine=home-side kinds,
    ownership_status_changed unwired (#234). ⑥leads-table=touches ZERO catalog kinds (honest negative).
  ✅ LIVE CATALOG RE-VERIFIED from the DB (table is the truth): 187 kinds, 55 emitted. RENDER: 6 emitted have
    NO sentence (email_sent, email_received — email has NONE — + 4× houseloop_insight_*); collisions "sent a
    text" ×3 + "sent an in-app message" ×2. NOTIFY: delivery_failed + port_updated notify-on-NO-class (2 live
    breaks). AUTO: hl_trigger_events STILL a base table (226 rows) — #526 retirement not done. DEAL DRIFT:
    tc_status_changed emitted (class=tc_assigned, wrong) vs transaction_status_changed dead (emitted=0).
  ✅ SEEDED the 55-kind × 5-lens audit matrix into board.json (record section, computed verdicts + defect
    overlay, honest "?" where unaudited): 11 render-RED, 4 native-RED, 2 notify-RED. Receipt:
    catalog-snapshot-2026-08-07.tsv. Script: scratchpad/consolidate-audit.mjs.
  ✅ OPENED 5-item fix backlog (items 2-6, active band): 2=composed-sentence render fn · 3=wire missing
    writers (connected_person_*/ownership) · 4=bind class to delivery_failed+port_updated · 5=reconcile deal-
    status kind · 6=finish hl_trigger_events retirement. Republished artifact ac8e921b.
  NEXT: walk each RED with Zach one by one, receipts. Item 1 done (all sources in).
  #610 (FUB import attribution) is now a SLICE of this master audit (defect ① notif-as-text + ② voicemail-as-
  text, both diagnosed on the board). #604-607 identity items remain future.

(superseded active) #610 (Zach put it active + "fix in /loop until done, leads walked, screens shotted, rich real
FUB leads, proper attribution — folds into the identity model"). DIAGNOSIS COMPLETE + recorded on the board:
  DEFECT ① notification-as-lead-text: owner-alert SMS (real twilio send to the AGENT phone, e.g. hl_messages
    5edf2f52: +19138004470→4074038088, conversation_key=NULL) is logged by logMessage (lib/messages/
    log-message.ts ~420-463) as an sms_sent LEAD activity, meta MISSING recipient_role=agent. CONFIRMED
    REDUNDANT: Bren has 24 real ai_fact_captured + 1 appointment_created; the "Zach sent a text" rows are
    just the delivery re-logged (2+1 dupes). FIX: agent-facing notification-delivery must NOT create a lead
    sms_sent activity (the announced event is already logged). Writer fix = non-destructive, do first.
  DEFECT ② voicemail-as-text: WRITER ALREADY FIXED (backfill-fub-comms.ts CALL_NOTICE regex). Existing bad
    rows need BACKFILL; verify fub-lead-import + fub-webhook-router honor the same rule.
  BACKFILL (78+ notif rows / 32-54 leads + voicemail rows): &lt;100 so not Rule-0, but it's Zach's prod feed —
    DECISION PENDING: delete-the-redundant-rows (event already logged) vs re-type. Snapshot first either way.
  NEXT STEP: fix the logMessage/send path skip for agent-facing sends, then backfill, then WALK Bren + rich
    real leads (NON-Nations) in Playwright + screenshots. lead: 29f0adc8-f646-4051-a1a6-c07c027df584.

STANDING ORDERS (unchanged): NEVER touch the Nations/pour (named only as evidence) · Camp David staleness is
design · import-silence law never weakens · the board.json is a SHARED file that gets clobbered — re-verify
item presence after edits (#608 was dropped once, restored).

⭐ 8/8 — THE §5 AUDIT'S REAL PAYOFF = #610 SCOPED. §5 (render) is built/deployed/proven live (21 kinds on Msale,
18 pass). The audit found the deep bug: the FUB import BULK-ATTRIBUTED every activity (texts/calls/notes/all) to
Ana Spradling / Ben Froeschl (team-exit accounts) — NOT the real actor — so §5 correctly renders the wrong name /
"An agent". THE TRUE ACTOR IS IN THE MIRROR (Zach: "it should have it"): fub.text_messages.actor = real senders
(April Atkinson/Carrie Markway/Chris Bishop/Donna Mason/…), inbound=lead; fub.calls.user_name; fub.users +
fub.team_members roster. NOTES: fub.notes.user_name IS the Ana/Ben bulk artifact → render a "team: exit" tag where
the real author is unknown (do NOT stamp Ana/Ben — Zach: "not correct"). #610 = the FUB-mirror actor-resolution
pass (writers stamp resolved actor + a non-destructive by_name backfill + re-walk) — build-ready spec on ac8e921b.
CORRECTNESS-CRITICAL (a wrong actor is a lie) → build FOCUSED, verify vs the mirror, no 1am vibe. §15 fixed live
(clone_activated/lead_assigned/clone_engaged now composable, mig 0704). Fixes #13/#14 = #610.

##### GATE 2 — EXECUTED 2026-08-11 14:40 (Zach's "yes gate 2 approved")
- Archive arm live (hl_activity_archive 575 rows + feed filter) · HOLD_0728 applied (3,768 re-keyed,
  companions same push) · merge real-run idempotent · third arm 1,544 actors · FUB-id exoneration ·
  ten-for-ten vs FUB (Kristen 109/109, Donna 130/130, Zairanett 132/132) · 28/28 proofs green.
- Receipts: .claude/reports/bedrock-receipts/gate2-execution/ · artifact 8fe35d30 (gate2-executed).
- AWAITING ZACH: item-34 freeze-lift sign + the last Rule-0 drop (direction/channel columns).
- Named opens: 5 archived stragglers (Msale x4, Zairanett x1, one DELETE each to restore) · 74
  pre-fix native "system" sends (stamp only vs phone-route log) · items 51/54/55 fresh-eyes.

##### MASTER QA BUILD — state @ 2026-08-11 11:20 (post freeze-lift)
- Zach signed the foundation ("i sign off on it all") = freeze LIFTED; Phase 6 = the Master QA Build
  (board items 56-70; his 4 interview rulings: own test identities · un-mute per lane · fix-as-found ·
  Website→CRM→FUB→TC). QA artifact (his window): 3c3eb715-3c43-4470-a8e9-0e004f2135e9.
- DONE today: items 56 (actor sweep, 11 sites + 2 lib threads), 57 (edit-card sentences), 60
  (identities: qa-probe@houseloop.ai IMAP + Twilio test lines; coverage ledger 179 surfaces), 69
  (channel column DROPPED, reversal kept; direction awaits writer migration — logActivity writes it).
- IN FLIGHT: promote-docs subagent building tools/boards/promote-doc.mjs (signed docs → Mintlify
  Command Center; Zach's account-portability directive — artifacts are scratch, sign-off ⇒ promote).
- NEXT: review promote-docs output → commit/push (docs auto-publish; mirror-docs --dry MUST be clean
  first) → open Lane 1 (item 61, website walk vs the ledger's 36 web surfaces, fix-as-found,
  receipts to tools/qa/walk-receipts/ + .claude/reports/bedrock-receipts/phase6-cards/).
- Standing: daily summary on the QA artifact; ping Zach only for taste-level reds.

##### LOOP STATE @ 8/11 11:40 — /loop THE ENTIRE BOARD (Zach: receipts + bug logs + fixes as you go)
- Board: 79 items, 40 done. QA campaign = items 56-79. Zach's depth charter + 2nd pass filed
  (Test Town 71 · bug 73 John Nelson email routing · inbox study 74 · charter 75 · filters 76 ·
  visibility matrix 77 · email telemetry 78 (DWD into zach@houseloop.ai CONFIRMED) · TC doc names 79).
- 🟢 JUST DONE: item 72 Jasmeet hidden-leak FIXED (leads-query workingStateGate: default view
  excludes status='hidden', Hidden filter shows them; proof 1/0/1; 97fe20eac8; receipt + build log).
- NEXT (in order): item 73 (trace inbound lender-email routing — why John Nelson's email w/ Conner+
  Jasmeet attached never hit their feeds; DWD Gmail read available) → item 71 Test Town build
  (2nd Workspace inbox for test agent; reassign idle Twilio numbers — authorized) → Lane 1 website
  walk (36 surfaces, ledger tools/qa/coverage-ledger.mjs, receipts to tools/qa/walk-receipts/).
- The living QA plan: artifact 3c3eb715 + Command Center master-qa-plan (re-promote on every update
  via node tools/boards/promote-doc.mjs). Build log lives IN that doc.

##### ⭐⭐⭐ THE FULL-CONTROL DELEGATION (Zach, 2026-08-11, verbatim anchors)
"i give you full control to implement and build… your job is to audit, then come up with a plan
then implement that plan as you go to the best of your ability without me." Bar: "would HubSpot be
built this way… the simplest solution that is also the correct solution" — enterprise quality.
+ same-day: kind sign-offs at Lee's desk ("you can sign off on it… we have the pattern now").
SCOPE: audit → plan → IMPLEMENT autonomously, including schema/table/architecture redesigns, all
receipted on the board + build log so Zach can audit after the fact.
THE TWO CARVE-OUTS THAT STAY ON ZACH'S DESK (his own protections, not bottlenecks):
1. RULE 0 — destructive SQL still needs his typed line, per instance, table named. Constitutional
   since the 4/26 TRUNCATE disaster; a delegation cannot waive it by design.
2. BLOW-IT-UP SCALE — a ground-up rebuild of a whole estate gets a written plan PRESENTED first
   (his explicit words: "if… we have to blow this thing up").

##### LANE 1 CLOSED @ 8/11 14:45 — LANE 2 OPENS
- Item 61 done (receipt item61-website-walk.md): 8 flows E2E w/ delivery receipts, 8 bug classes
  fixed. Item 83 filed (send hygiene). Board 47/83. Un-muted classes: lead_created, tour_request,
  contact_request. Cadence: 60s wakeups, ≥1 worker always in flight, sensitive ops quarantined.
- LANE 2 (item 62, CRM walk): walker fleet per nav section vs tools/qa/coverage-ledger.mjs (143 crm
  surfaces, 24 sections); per-action bar = card + notification + runlog; fix-as-found through me.
- Also delegating: item 82 (merge-resolver unification). Pending my desk: item 83, All-feed log-key
  decision, item 74 inbox study, items 76-79 (filters/matrix/telemetry/TC docs), 63-64 lanes, 67-68.

##### @ 8/11 15:05 — state for continuity (context deep)
- Board 49/83. SHIPPED today (all pushed through db4589099f/6c77e97f42+): Gate2, freeze lift, Test
  Town, Lane 1 CLOSED, per-lead activity key (0731), one merge engine (82), render/copy auditors,
  raw_diff heal 51→0, item 70 impostors archived, un-muted classes lead_created/tour/contact.
- IN FLIGHT: crm-walker-1 (Primary) + crm-walker-2 (Pipeline&Workflow) — Lane 2, 24 sections.
- MY-DESK QUEUE: item 83 (send hygiene + HVR fork decision), direction writer-migration → approved
  drop, items 51/67/74/76-79, lanes 63/64, item 68 census.
- Engine note: prod now logs "[merge] UNRESOLVED TOKENS" lines — watch them as the real-hole meter.

## Related documentation

Doc sections this work creates or updates:

- [`guides/intent-workflows`](/guides/intent-workflows)
- [`guides/workflows`](/guides/workflows)
- [`guides/registration`](/guides/registration)
- [`guides/web-registration-and-intake`](/guides/web-registration-and-intake)
- [`guides/tour-and-contact-requests`](/guides/tour-and-contact-requests)
- [`fub/trigger-events`](/fub/trigger-events)
- [`fub/lead-import`](/fub/lead-import)
- [`platform/message-catalog`](/platform/message-catalog)
