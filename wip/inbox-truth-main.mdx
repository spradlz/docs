---
title: "Inbox-truth (inbox-truth LANE — LEDGER 111 (2026-08-14))"
description: "Work-in-progress dossier — Inbox-truth. inbox-truth LANE — LEDGER 111 (2026-08-14)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/inbox-truth.md` on every push. **Progress: checklist pending.**</Info>

**Owner:** Inbox-truth

## Checklist

### STATE: BLOCKED ON LEE, twice. Nothing uncommitted; every file I touched is clean in the tree.
1. **The `fromMatch` shape** (ruling 1) — I stopped rather than build it; as worded it regresses a signed fix. See below.
2. **The `target-verdicts.json` entry for `hl_comm_seat`** — the framework gate refuses CREATE TABLE without one; the file is Lee-owned.

### THE ROOT CAUSE (dated, proven)
Conner Kelley's lead carried NO resolvable email until Zach hand-edited it at **2026-08-14 17:24:49Z**
(`hl_activity_log` lead_edited, meta `{"field":"contact","email":"connerck@outlook.com"}`).
Every mis-filed row is BEFORE that stamp; everything after is correct.
`leadIdForAny`/`leadIdsForAll` (lib/email/gmail-realtime.ts:104,136) walk from→to→cc and take the
first address that RESOLVES — an unresolvable sender inherits a co-recipient's identity.

### CENSUS — CLOSED (tables queried directly; NEVER the 300-capped endpoint)
Gmail truth: thread `1a000c676dc9254d` = 11 messages (7 out, 4 in), all carrying BOTH parties on To+Cc.
**Capture 11/11 — nothing lost at ingest.** All defects are attribution + surfacing.
⭐ **258/266 outbound email rows in `hl_messages` had a NULL sender (97%); 91/99 last 30d. Inbound 0/2,910.**
Still open: Conner has no seat on the 15:07 inbound; the 17:11 row's `lead_id` was re-pointed by a
20:23:36 backfill while `matched_on` stayed 'cc' (base column and join disagree on provenance).

### SHIPPED — all in HEAD. ⚠ The per-file auto-save hook committed all four before my own `git commit`
### could land (first attempt died on `cannot lock ref 'HEAD'`), so they carry generic tags, not `[inbox-truth]`.
- **d1d7c2b409** gmail-realtime.ts — the missing `logMessage` fold on realtime **INBOUND**. Notify now
  implies an inbox row. Idempotent on `(channel, external_id)` = the Gmail id. (Bundled by the hook with
  another lane's HANDOFF.md.)
- **4316de7c4c** log-email-send.ts — `sender: a.from ?? null`.
- **4a26864a7a** sync-now/route.ts — `sender: outbound ? acct.account_email : fromEmail`.
- **bc7918720c** dispatch.ts — the self-heal hoisted ABOVE the actor block (:834 now precedes :838).
GATES on this content: tsc 0 · test:email-singleness 13/13 · test:notification-copy green · migration-lint 4/4 ·
eslint warnings unchanged, all pre-existing on untouched lines.

### ⚠⚠ TWO CORRECTIONS TO MY OWN WORK — read before trusting the above
1. **THE HOIST DOES NOT CLOSE ITS HAZARD. Do not tick ruling 2.** `resolveInboundEmail` (dispatch.ts:79)
   reads the FUB mirror and returns `{subject, snippet}` — no from address — and `fub.emails` has **no
   sender column at all** (verified live: id·person_id·user_id·user_name·subject·body·is_incoming·status·
   action_plan_id·fub_created·synced_at·content_status). So `enrich.email.from` is still absent and the
   actor still falls back to the lead. It also only fires for leads with a `fub_person_id`. The ordering is
   correct and harmless — keep it — but the closure is the seat model (the from-seat carries the address).
2. **RULING 1 AS WORDED DELETES 7 REAL FILINGS.** "Unresolvable sender → NULL" is exactly item 73's signed
   Kelley/Kaur LENDER case (John Nelson, not a lead, never resolvable, writes about both buyers). Measured:
   `hl_inbound_email_leads` = 21 rows — 14 from · 3 cc · 2 manual · 1 to · 1 to_or_cc → **7 ride a
   non-sender rung.** The real defect is narrower: **`matches[0]` is used as BOTH the filing key AND the
   actor.** Filing fan-out is CORRECT (leave item 73 alone); the ACTOR must come from the FROM address
   alone and be NULL for a stranger.
   ⚠ **SHARPEST EDGE, unruled:** `dispatchInboundToAi({ leadId })` (gmail-realtime ~:670) gets that same
   `matches[0]` — an unresolvable sender points **the AI at a co-recipient's lead**. Same value gates the
   mint gate, which is why an unknown sender cc'd beside a known lead can never be minted. NOT TOUCHED
   (AI_CONTAINMENT-adjacent).
   PROPOSED: add `fromMatch` (FROM-address resolution only) beside `matches`; `matches` keeps filing,
   `fromMatch` drives actor + AI dispatch + mint gate. Test the 15:07 shape: filing still reaches the cc'd
   lead AND actor/AI resolve to NULL.
   ON "the review-queue path": **no destination exists.** The only quarantine tables are `hl_srg_*`, FROZEN
   by ONE_ANSWER_TABLE. Proposal: the review queue IS the seat table — `external` seats with
   `resolved_by='unresolved'`. No second table.

### ALREADY FIXED BY OTHER LANES — VERIFIED, DO NOT REBUILD
LEDGER 76 (actor by address, dispatch.ts:818) · ledger 74 (the `{{notify.source}}` "from Manual entry"
phrase — the live `hl_notify_classes` row is clean) · LEDGER 81 (the parallel-loop race behind the
18:08/19:15 doubles; committed 20:17Z, AFTER both).

### THE SEAT DDL — `.claude/reports/inbox-truth/PROPOSED_0772_hl_comm_seat.sql`
Parked OUTSIDE `db/migrations/` on purpose. Next free number 0772.
- **The fact is `hl_messages`** — both email estates are already sentenced `fold_into:hl_messages`.
- **THREE proto-seat tables**, not two: Caleb's `hl_message_thread_participant`, the e-sign per-recipient
  rows, and **`hl_inbound_email_leads`** (canonical, Lee 8/11 — already a fact-scoped seat table).
  Proposed both re-verdict to `fold_into:hl_comm_seat`.
- `scope` discriminator ('fact' | 'thread'); per-seat transport columns; `visibility='private'` for BCC.
- Proven in a scratch schema inside BEGIN…ROLLBACK, 0 residue: one-from-seat enforced · two to-seats
  allowed · case-insensitive replay refused · lead-kind-without-lead_id refused · dual-anchor refused.

### HANDED OFF — stream-rows.ts is r2-cards', not mine (I never opened it)
Their measurement beats mine and should be treated as canonical: 5 comma-string rows in `hl_messages`,
all email — **2 phantoms (mine) and 3 INVISIBLE ENTIRELY** (`6de2bdf5` NULL sender · `92a7dd9a` a lender ·
`6cc42888` RealScout). The 3 are the worse defect and my census missed them: the SQL predicate at
stream-rows.ts:105 uses exact `LOWER(recipient) = ANY(doors)`, which a joined string can never satisfy.
They stay open for the seat read-path. Note: the 2 phantoms are REAL 2-recipient sends, so the right
answer is 2 counterparties, not 1 cleaned string.

### CLOSED — Heather Banwart is NOT a second fall-through (disqualified by date)
Her cards are **2025-01-22 → 2025-07-17**; the "Myli over Jessica Coggins' words" pair is 2025-07-17,
thirteen months before LEDGER 76. Every `email_sent` row in the log with `by_name='Myli'` is 2025
(73 rows, ZERO since 2025-07) — the mechanism is dead, not merely old. Each has a twin row at the same
timestamp naming the real human with `source` NULL. Left over: those 73 legacy rows still render wrongly
if scrolled to (a backfill question), and her `email_received` rows carry no actor at all.
⚠ I initially misread these as 2026 because I printed `MM-DD` without a year — my own
[[feedback_dates_always_iso_with_years]], broken while applying
[[feedback_date_the_failure_before_naming_the_owner]]. Caught before it reached a conclusion.

### NEXT (all gated on Lee)
`fromMatch` → 0772 migrates → seat writers at capture on all 3 ingest paths (ONE resolver, `resolved_by`
recorded) → `logActivity` per lead-seat → repoint inbox/feed/notification onto seats (retires the
comma-predicate + name-inheritance + the 300-cap together) → backfill (15:07-Conner attach, the two
Jasmeet-only sends, the 17:11 provenance repair, and the 258 NULL-sender rows) through writers with
idempotent keys → the BCC harness proof → gauntlet.

### RAILS HELD
Real-lead production data: read-only against Gmail and prod, no sends, no destructive SQL, no raw UPDATEs.
Receipts: `.claude/reports/inbox-truth/` (screenshot · gmail-census JSON + reader · the DDL proposal).
⚠ Note for my successor: I used `git stash` once on a single file to compare lint counts. It round-tripped
clean, but [[feedback_never_git_stash_in_the_shared_tree]] says don't — use a second checkout or just
count warnings by line instead.
