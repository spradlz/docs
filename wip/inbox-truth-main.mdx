---
title: "Inbox-truth (inbox-truth LANE — LEDGER 111 · SEAT MODEL SLICE 1 (2026-08-14))"
description: "Work-in-progress dossier — Inbox-truth. inbox-truth LANE — LEDGER 111 · SEAT MODEL SLICE 1 (2026-08-14)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/inbox-truth.md` on every push. **Progress: checklist pending.**</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 359h ago. If Inbox-truth has shipped since, this dossier under-reports — refresh `.claude/state/wip/inbox-truth.md`.</Warning>

**Owner:** Inbox-truth

## Checklist

### STATE: seats are WRITTEN but not yet READ. Next = the remaining 3 ingest paths, then the read-path repoint.
Lane tree clean. Nothing uncommitted. All work verified in HEAD **by content** (see the hook-race note).

### THE ROOT CAUSE (dated, proven)
Conner Kelley's lead carried NO resolvable email until Zach hand-edited it at
**2026-08-14 17:24:49Z** (`hl_activity_log` lead_edited, meta `{"field":"contact","email":"connerck@outlook.com"}`).
Every mis-file is BEFORE that stamp; everything after is correct. `leadIdForAny`/`leadIdsForAll` walk
from→to→cc and take the first address that RESOLVES — so an unresolvable sender inherited a
co-recipient's identity. **A fall-through to a different human is identity fabrication.**

### CENSUS — CLOSED (tables queried directly; NEVER the 300-capped endpoint)
Gmail truth: thread `1a000c676dc9254d` = 11 messages (7 out, 4 in), ALL carrying both parties on To+Cc.
**Capture 11/11 — nothing lost at ingest.** Every defect was attribution + surfacing.
⭐ **258/266 outbound email rows in `hl_messages` had a NULL sender (97%); 91/99 last 30d. Inbound 0/2,910.**

### SHIPPED (all in HEAD; slice 1 rode the train — f842d4b712 is on origin)
| what | where |
|---|---|
| realtime INBOUND `logMessage` fold (notify implies an inbox row) | d1d7c2b409 |
| `sender` stamped on both outbound fold writers (defect A) | 4316de7c4c · 4a26864a7a |
| dispatch.ts self-heal hoisted above the actor block | bc7918720c |
| **mig 0772 `hl_comm_seat` + schema re-lock (212 tables)** | **f842d4b712** (wears r2-cards' message) |
| `fromMatch` — the FROM seat is the actor; no AI dispatch on null | da933b4e14 · ba7d34243d · 17999247fa |
| **`lib/comms/seats.ts` — THE seat writer + realtime inbound wiring** | 1917bb5a12 · a3de4e869a · 0f98491680 |

**PROOFS:** `test:seat-writer` 16/16 · `test:from-seat` 11/11 (red-first against the REAL pre-fix blob
d1d7c2b409) · `test:email-singleness` 13/13 · `test:notification-copy` green · migration-lint 4/4 ·
tsc 0 throughout. The 6 DDL constraint proofs ran against the LIVE table inside BEGIN/ROLLBACK with
0-row residue verified.

### ⚠ THE HOOK RACE — FIVE OCCURRENCES TONIGHT, READ THIS BEFORE TRUSTING COMMIT MESSAGES
The per-file auto-save committed my work before every one of my own `git commit` calls, twice sweeping
it into OTHER LANES' commits (mig 0772 rode `[r2-cards] Retire the two stale stage_changed icon
answers`). One attempt died on `cannot lock ref 'HEAD'` mid-commit. **Verify by CONTENT, never by
commit message.** Lee logged it; the desk owns a structural fix later (candidate: lanes claim a commit
window, or the hook tags sweeps with the EDITOR lane name). Do not rewrite history to fix attribution.
⚠ Also: a commit message containing a quoted email pair broke shell quoting and half-staged. Use
`git commit -F <file>` for any message with quotes.

### ⚠ TWO THINGS LABELLED HONESTLY — do not read either as done
1. **The dispatch.ts self-heal hoist does NOT close its hazard.** `resolveInboundEmail` reads the FUB
   mirror, and `fub.emails` has **no sender column at all** (verified live), so `enrich.email.from`
   stays absent and the actor still falls back to the lead for a caller that passes no enrich. The
   ordering is correct and harmless — keep it — but the closure is the seat read-path. *A half-fix
   labelled half beats a tick.*
2. **Seats are WRITTEN, not READ.** Only the realtime INBOUND path writes them. The realtime SENT
   branch and both pollers (inbound-sync, sync-now) do NOT. The read path still reads the old columns.
   The RENDER half of the bcc law is explicitly unproven — the test prints that caveat to stdout.

### ⚠ ONE RESOLVER GAP — FLAGGED TO LEE, NOT PATCHED
`resolveRecipientActor` matches leads on `lower(email)` ONLY; it does not check `data->'alt_emails'`,
which `leadIdsForEmail` does. A lead reachable only by an alt address resolves `unresolved` in seats
while the old filing path finds them — **seats would be WEAKER than what they replace** for that
population. The fix belongs INSIDE the one resolver (shared with the e-sign estate = cross-lane); a
second lookup here would be the exact disease the ONE resolver exists to cure.

### RULINGS SETTLED AT LEE'S DESK (don't re-litigate)
- **Filing vs actor:** `matches` drives FILING (item 73 untouched — 7 of 21 join rows ride a
  non-sender rung, incl. the signed Kelley/Kaur LENDER case). `fromMatch` drives ACTOR, NULL for a stranger.
- **AI dispatch:** fromMatch null ⇒ **no dispatch at all**, and the refusal is a ROW — the seat writer
  records the sender as `external`/`resolved_by='unresolved'`, so refusals are queryable. The
  `console.warn` is an operational convenience, NOT the record.
- **The mint gate does NOT widen.** John-Nelson-the-lender is also an unknown sender cc'd beside known
  leads — resolvability is not mint-worthiness. Mint-from-review is a HUMAN action on the queue.
- **Review queue = the seat table** (`resolved_by='unresolved'`, `idx_comm_seat_unresolved`). No new table.
- **The fact is `hl_messages`** — both email estates are sentenced `fold_into:hl_messages`.
- **Three proto-seat tables fold in**, not two: `hl_inbound_email_leads` (its `matched_on` rungs become
  `resolved_by`) + `hl_message_thread_participant`. Verdicts committed at 7a16b97840.

### NEXT — Lee's confirmed §4 order
1. capture-time seats on the **remaining 3 ingest paths** (SENT branch + inbound-sync + sync-now)
2. `logActivity` per lead-seat, idempotent on (fact, lead)
3. **read-path repoint** — retire r2-cards-2's interim IN the same commit so it never outlives its
   successor. This is what actually retires the phantom counterparty, the name inheritance and the
   300-cap together. *Recommend this BEFORE the backfills — until something reads seats, none of it is
   visible to Zach.*
4. backfills through writers with idempotent keys: the 15:07-Conner attach · the two Jasmeet-only sends ·
   the 17:11 `matched_on` provenance repair · the **258 NULL-sender rows** (⚠ this will turn
   r2-cards-2's deliberate absence-assertion on `6de2bdf5` RED — by design; delete it then)
5. the **BCC harness proof** — a `visibility='private'` seat must NEVER render on another participant's
   surface, red-proven.

### FROM r2-cards-2 (stream-rows is THEIRS; their measurement outranks my census where they disagree)
Shipped `5cbc252331`: 105 → 107 conversations; the Fountain Mortgage email to Trish + Zach is reachable.
Their three learnings, all of which bite the seat read-path too:
- **Reachable ≠ in the window** — their assertion first failed because the row sits 1,478 messages back
  and the reader caps at 500. Read DEEP or chase a predicate bug that isn't there. (Third lane to hit
  this: also allfeed-101's stalled paginator and m10-delta's 39-of-370.)
- The list must split in the WHERE **and** the row shaping together, or the reader disagrees with itself.
- Shape: zero angle brackets / zero quoted display names in 5,921 rows, but mixed case is real.
  `addrs()` already parses the `Name <a@b>` form, so the seat path has one home for it.

### CLOSED
Heather Banwart = **not** a second fall-through. Her cards are 2025-01→2025-07; every `by_name='Myli'`
`email_sent` row in the log is 2025 (73 rows, zero since 2025-07) — dead mechanism. Legacy rows fold
with the backfill. ⚠ I misread these as 2026 because I printed `MM-DD` without a year — my own
[[feedback_dates_always_iso_with_years]], broken while applying
[[feedback_date_the_failure_before_naming_the_owner]]. Caught before it reached a conclusion.

### TASK #56 (the "wrong-name bells") — THE URGENT HALF DOES NOT EXIST. Re-scope before building.
Escalated as P0: ceremony 53 rang THREE bells at **2026-08-14 22:24:39Z** all reading *"Conner Kelley
signed Exclusive Buyer Agency Contract"* for three different signers.

**DISQUALIFIED BY THE SAME DATING DISCIPLINE AS HEATHER.** I ran the LIVE `composeNotification`
against tonight's exact inputs (leadId=Conner, actorName=each signer's address, real
`tc_document_signed` class row). **Current code renders three DIFFERENT names:**
- `jasmeetkh17@gmail.com` → "✍️ Jasmeet Kaur signed …"
- `connerck@outlook.com` → "✍️ Conner Kelley signed …"
- `mo.sign@exprealty.net` → "✍️ mo.sign@exprealty.net signed …" (the honest address — ledger 76's
  "a null resolves to the address itself, never to the lead")
The wiring is complete end to end: `app/api/esign/webhook/route.ts:439` passes `actorEmail` per signer
→ `lib/tc/loop-engine.ts:691` maps it to `actorName` → `dispatch.ts:838` resolves it. **Both halves
are ON ORIGIN.** LEDGER 89 committed 16:28 EDT; origin tip was 18:19 EDT (=22:19Z) and the bells fired
22:24:39Z — five minutes later, which no Railway build finishes in. **Those bells ran on the previous
image.**

⚠ **CORRECTION TO THE BRIEF'S PREMISE** (read the RAW columns, never the derived `actor_name` —
m10-delta's trap): the signed rows are **not** all anonymous. Tonight's three:
`…:signed:connerck@outlook.com` → `by_name='Conner Kelley'` ✓ · `…:signed:jasmeetkh17@gmail.com` →
`by_name='Jasmeet Kaur'` ✓ · `…:signed:mo.sign@exprealty.net` → blank ✗.
**The webhook writer ALREADY stamps `by_name`/`by_lead_id` whenever the address resolves.** Jasmeet's
row was correctly stamped and her bell still said Conner — which is what pointed at the build, not the
writer.

**THE ONE GENUINELY OPEN ITEM (small, NOT a P0):** `mo.sign@exprealty.net` resolves to nobody and no
`nameHint` reaches `resolveRecipientActor` at that site, so the broker's FEED CARD is anonymous while
his BELL honestly reads the address. A missing name, not a fabricated identity. The participant row
carries `full_name`; passing it as the nameHint resolves him to `via:'named'`. NOT DONE — needs a
red-first test and re-prioritising behind the read-path repoint.
✅ **CONFIRMED BY THE DEPLOY LOG (desk, 2026-08-14) — this is now FACT, not inference.** Railway deploy
`682eb415` build **STARTED 22:19:53Z** and was **still BUILDING at ~22:26**; SUCCESS observed later.
The bells fired **22:24:39Z**, so the new image was definitively NOT live when they rang. Nothing to
reopen. (I had flagged this as my one unverified link and asked for it to be checked rather than
closed on my word — the desk had the log.)
Also ruled: Ana's 19:51Z restored row never rang (backfill healed the record, skipped the bell) —
historical, no backward fix, and D4 holds: **the backfill pass must NOT retro-ring bells**
(`is_backfill` never fires).

### RAILS HELD
Real-lead production data: read-only against Gmail and prod; no sends; no destructive SQL; no raw
UPDATEs on history. The seat test writes real rows against SYNTHETIC fact ids and deletes exactly
those (table verified back to 0). Receipts: `.claude/reports/inbox-truth/` (Zach's screenshot ·
gmail-census JSON + the reader script).
⚠ Successor note: I used `git stash` once on a single file to compare lint counts. It round-tripped
clean, but [[feedback_never_git_stash_in_the_shared_tree]] says don't — count warnings by line instead.
⚠ `node tools/schema/model-lock.mjs` ALONE leaves the census stale (reports 211 and looks like success);
only `tools/schema/schema-lock.mjs` regenerates it.

### ⭐ #56 ROOT CAUSE FOUND (2026-08-14, after Zach identified the broker) — THE FIX IS SMALLER AND BETTER
Zach: `mo.sign@exprealty.net` is eXp's DESIGNATED-BROKER signing address (a role mailbox); the human is
**Chris Cribb**.

**THE TELL WAS ALREADY IN MY OWN MEASUREMENT AND I WALKED PAST IT:** on ceremony 53, the
`document_opened` row for `mo.sign@exprealty.net` carries **`by_name='Chris Cribb'`** while the
`document_signed` row for the SAME address is **blank**. Same ceremony, same address, same webhook,
two different answers — so the identity was never missing, only unreached on one branch.

**WHY:** `app/api/esign/webhook/route.ts`
- the **OPENED** branch (~line 117) calls **the ONE resolver**:
  `resolveRecipientActor(rc.email, { nameHint: rc.name })` — `rc.name` is the e-sign RECIPIENT name
  from Documenso, which carries "Chris Cribb". → resolves `via:'named'`, `by_name` stamped. ✓
- the **SIGNED** branch (~lines 376-390) **hand-rolls its own lookup**: raw SQL `hl_users` by email,
  then raw SQL `hl_leads` by email, then falls back to `part?.full_name` (the ceremony PARTICIPANT
  row, which is blank for the broker). It never calls the one resolver and never sees `rc.name`. ✗

**SO THE FIX IS NOT "add a nameHint" — it is "the SIGNED branch must call the ONE resolver, with the
recipient's name as the nameHint, exactly like the OPENED branch fifteen lines above."** That single
change:
- fixes the broker's name (`via:'named'` → "Chris Cribb"),
- **deletes a SECOND address→party resolver** — the exact disease the one resolver exists to cure, and
  the same class as the two disagreeing email lookups I closed earlier tonight,
- makes the signed path **inherit the alt-email rung** I just added (the hand-rolled SQL queries
  `lower(email)` only, so alts are invisible to it — the divergence I closed in the resolver does NOT
  reach this branch today).
⚠ Do NOT add an address→name map for `mo.sign@` in the resolver. The mapping's home is the ceremony's
own recipient/participant record (reference data the send flow writes), per Lee's ruling.
NOT BUILT — needs a red-first test on ceremony-53's shape (broker's bell renders "Chris Cribb", not the
raw address) and sits behind the read-path repoint. Tonight's blank `…:signed:mo.sign@exprealty.net`
row can be name-stamped in the same backfill pass, through the writer, idempotent, NO retro-bell (D4).
