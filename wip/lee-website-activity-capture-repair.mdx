---
title: "Lee (Website Activity Capture Repair)"
description: "Work-in-progress dossier — Lee · Infra · Pipeline & Workflow · FUB · Notifications. Website Activity Capture Repair."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/lee/website-activity-capture-repair.md` on every push. **Progress: 5/6 done (83%).**</Info>

**Owner:** Lee — Infra · Pipeline & Workflow · FUB · Notifications

## Checklist

Repairing the HouseLoop website → CRM behavioral-capture chain (pixel + Run Log feed + listing-detail
consistency) so every real visitor action lands, attaches to a lead, and is visible. Zach-driven, live
E2E on prod, 2026-07-01.

### Checklist
- [x] **Like/save pixel decouple** — removed the phone gate that made a phoneless signed-in like re-prompt
      for a phone and never fire → likes/saves landed **0 all-time**. Login is the only gate now; the pixel
      fires the moment the visitor is authenticated (Zach LOCKED 2026-07-01). Live `0caf6bd7`. **Verified:**
      Zach's like (12816 Sagamore) + save (3805 W 123rd) landed, attached to his lead.
- [x] **Run Log → Website Activity restored to the raw pixel stream** — the 2026-06-21 "People Catalog SSOT"
      refactor switched this tab from `hl_pixel_event` to the identified-only `hl_lead_activity`, silently
      dropping every bot + anonymous visitor + the Humans/Bots toggle (looked frozen; only ~191 rows).
      Restored `WEBSITE_SELECT` (pixel stream): Who = lead name · `Bot · <crawler>` · `Visitor <id8>`, with
      the audience toggle. Live `b8d79836`. **Verified on prod DB:** 191 → ~95k rows (94,584 bots / 327
      visitors / 202 leads), labels correct, Comms/Email tabs unbroken.
- [x] **Listing-detail bar consistent from any gallery** — home-page shelves never wrote the results-context,
      so a shelf click landed on a bare detail page (no back / address / stepper). Shelves now write their
      search as the context; the identical `DetailSearchAnchor` bar renders. Live `73823d94`.
- [x] **Shelf stepper = the FULL search, not the 12 preview** — the bar now lazy-loads the shelf's full
      ordered search (`/api/listings/search` + `/api/listings/count`) → reads "3 of 447" and steps the whole
      Overland Park newest search; carousel stays 12 by design. Live `cb145929`. **Verified:** count=447.
- [x] **Pixel-tracking dev doc corrected** — `docs-site/guides/pixel-tracking.mdx`: accurate event catalog
      (removed unwired `share_home`/`account_created`/`login`/`cta_click`; added `search`/`ai_search`/
      `contact_request`/`identify`), new **Social engagement** section (view=open, like/save=login-gated +
      phone-decoupled, share=modal-only/untracked).
- [ ] **Shared-home = login-gated registration event + cloned workflow** — Lee, end-to-end, real workflow
      cloned off Liked Home (copy swapped), no vibe code. `share_home` pixel + login gate + the
      `shared_home_registration` catalog event + workflow. NOT started.

· `components/explore/ListingRow.tsx` · `components/search/ResultsContextWriter.tsx` ·
`apps/houseloop-crm/lib/run-log/feed.ts` · `docs-site/guides/pixel-tracking.mdx`

_related:_ pixel client `lib/pixel/hl-pixel.ts` · ingest `apps/houseloop-crm/app/api/pixel/track/route.ts`
· ALL_FEED_IS_EVERYTHING law · the shared-home build is the one open item.

## Related documentation

Doc sections this work creates or updates:

- [`_`](/_)
- [`apps/houseloop-web/components/listing/ListingEngagementBar`](/apps/houseloop-web/components/listing/ListingEngagementBar)
- [`ListingActionReplay`](/ListingActionReplay)
- [`DetailSearchAnchor.tsx`](/DetailSearchAnchor.tsx)
