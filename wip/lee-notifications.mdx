---
title: "Lee (Notifications = Everything)"
description: "Work-in-progress dossier — Lee · Infra · Pipeline & Workflow · FUB · Notifications. Notifications = Everything."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/lee/notifications.md` on every push. **Progress: 0/3 done (0%).** _Updated 2026-07-01._</Info>

**Owner:** Lee — Infra · Pipeline & Workflow · FUB · Notifications

## Checklist

**GOAL (the whole point):** the notification list is **AUTO-DERIVED from the ONE Unified Trigger Catalog**
(`hl_trigger_events` / `CANONICAL_LEAD_EVENTS`) — **every** registration + system event automatically
concatenates into `/settings/notifications` as its own row, with a good per-event notification ("New tour
request from Jon", "Ana viewed 12162 E 39th Terrace"). Add an event to the catalog → it shows up as a
notification automatically. **No hand-maintained list.** Same catalog the workflow-builder palette + the
Registration/System-Event pages derive from — one source, no drift.

### WHERE WE'RE WORKING FROM
- Page: `apps/houseloop-crm/app/(crm)/settings/notifications/page.tsx` (the notification section)
- Registry: `apps/houseloop-crm/lib/notifications/template-registry.ts`
- Catalog it MUST read: `hl_trigger_events` + `components/settings/lead-sources/lead-source-event-constants.ts`
  (`CANONICAL_LEAD_EVENTS`) — Benji's Unified Trigger Catalog.

### THE BLOCKER (now cleared)
This was **WAITING ON BENJI's canonical event-trigger catalog** — one canonical list of every trigger. **Benji
shipped it** (`hl_trigger_events`, migration 0276: `property_viewed`/`saved`/`liked`/`shared` + registration
events, `category`/`family`/`platforms`). **So it's UNBLOCKED now** — the notifications page can finally derive
from the real catalog instead of a hardcoded list.

### REALITY TODAY (verified 2026-07-01 — the old checkbox was WRONG)
- ❌ **The list is HARDCODED, not catalog-derived.** `SECTIONS` is a literal array (`page.tsx:36`);
  `NOTIFICATION_TEMPLATES` is a hardcoded `Record<>` (`template-registry.ts:23`). Adding a catalog event
  (e.g. `property_viewed`) does NOT surface a notification — you'd hand-edit these lists. That's the drift the
  catalog exists to kill. (The prior "catalog-derived list — done" checkbox was inaccurate.)
- ✅ Shipped separately (notifier plumbing, NOT the derivation): `notify_owner` (mig 0270) — HouseLoop native
  notifier + the per-event Realloop→HouseLoop cutover toggle.

### OPEN WORK (what "finish it" means — now unblocked by Benji's catalog)
- [ ] **P1 — Make the notification list catalog-derived.** Replace hardcoded `SECTIONS` + `NOTIFICATION_TEMPLATES`
      with a build-from-catalog function that iterates `hl_trigger_events` (grouped by `family`) so every event
      auto-appears. One source, no drift.
- [ ] **P2 — Each event fires its OWN good, named notification** ("New tour request from Jon"), not a generic ping.
- [ ] Verify: add a new event to the catalog → it appears on `/settings/notifications` with zero code edits.

### OWNER / COORDINATION
Derivation = **Lee**; it reads **Benji's catalog** (`hl_trigger_events`). Coordinate so the notifications page
consumes the SAME catalog the workflow builder + Registration/System-Event pages do — not a parallel list.

## Related documentation

Doc sections this work creates or updates:

- [`guides/notifications`](/guides/notifications)
- [`guides/pipeline-workflow-overview`](/guides/pipeline-workflow-overview)
