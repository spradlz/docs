---
title: "Benji (Saved Search Process — nail down the full lifecycle)"
description: "Work-in-progress dossier — Benji · HouseLoop Website. Saved Search Process — nail down the full lifecycle."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/benji/saved-search-process.md` on every push. **Progress: 5/10 done (50%).** _Updated 2026-07-01._</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 405h ago. If Benji has shipped since, this dossier under-reports — refresh `.claude/state/wip/benji/saved-search-process.md`.</Warning>

**Owner:** Benji — HouseLoop Website

## Checklist

depends_on: trigger-events
checkpoint: .claude/checkpoints/workflow-trigger-catalog.md

**North star:** ONE clear, correct saved-search process across all three origins, each its own trigger event
(NOT a hidden code condition) — this is the thread that started the whole trigger-events rebuild. A saved
search is created three ways, and each must behave right:
- **Client** creates one (existing lead saves a search themselves) → `saved_search` trigger → the client
  "New Lead — Saved Search" workflow (Myli text/email).
- **System** creates one (registration auto-seed) → `saved_search_system` trigger → silent (optional internal notify).
- **Agent** creates one FOR a lead ("Create Saved Search as Agent") → `saved_search_agent` trigger → silent,
  NEVER the client workflow. Agent blasts current matches via "Send Now".

### Already shipped this session (the foundation)
- [x] 3-origin taxonomy `createdBy = system | client | agent` (data remapped) + agent create-modal (agent copy)
- [x] "Create Saved Search as Agent" (CRM lead detail → magic link → site as client → agent modal); sticky asAgent
- [x] Digest email cards == count (render the new-since homes, not full criteria)
- [x] Per-search tracking (no lead-wide fallback → honest dashes on a fresh search)
- [x] Agent/system create is SILENT — origin-specific event emission (not a hidden condition), so the client
      workflow never fires for an agent-created search

### Still to nail down (with Zach, AFTER the trigger-events catalog is canonical)
- [ ] Confirm the 3 trigger events fire correctly + are attachable to their own workflows (needs P5 door reg)
- [ ] Agent modal copy fix (kill the client "unsubscribe from email alerts anytime" language)
- [ ] Custom subject line on the agent modal (show current default subject + let the agent override)
- [ ] "Send Now" on the create modal (all matching / new since last sent / today / 2d / 7d — blast current matches)
- [ ] Write the saved-search-process PRD once the above is locked

**Blocked by →** [[trigger-events]] (the catalog must be canonical first so the 3 origins are real trigger events).

---

## PRD · `TBD — write after the trigger-events catalog lands`

_(File not found in-tree — path recorded for reference.)_
