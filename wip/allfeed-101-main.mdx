---
title: "Allfeed-101 (allfeed-101 LANE HANDOFF — LEE-WRITTEN BACKSTOP (2026-08-14 ~21:50Z, pre-account-switch))"
description: "Work-in-progress dossier — Allfeed-101. allfeed-101 LANE HANDOFF — LEE-WRITTEN BACKSTOP (2026-08-14 ~21:50Z, pre-account-switch)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/allfeed-101.md` on every push. **Progress: checklist pending.**</Info>

**Owner:** Allfeed-101

## Checklist

# (If allfeed-101 wrote its own version, that one wins — this is the desk's map of the lane.)

### MISSION — LEDGER 101 (scope lock: session-scope-allfeed-101.lock — READ IT, it survives)
The lead-detail "All" tab renders EVERY row the activity API returns (ALL_FEED_IS_EVERYTHING).
MEASURED BREACH: /api/leads/&lt;conner-TEST&gt;/activity returns 104 rows; the DOM renders 58 cards;
document_sent missing at every scroll position.

Deliverables per the lock:
1. The drop census — which rows drop and WHY, grouped by MECHANISM.
2. Classification: DELIBERATE (signed rule, cited) vs DEFECT.
3. Fix the DEFECT class — every entitled row renders as SOME card (catalog card_layout drives it;
   an unrouted kind falls back to the compact ActivityCard, NEVER to nothing).
4. THE COMPLETENESS EQUATION as a TEST: rendered cards + documented deliberate drops = API rows.

### SCOPE BOUNDARY (coordinate via Lee's desk)
IN: components/leads/detail/CenterLane.tsx (All-feed COMPOSITION only) + app/api/leads/[id]/activity/
route.ts + the new completeness test + .claude/reports/allfeed-101/**.
OUT: ActivityHeroCard.tsx + lib/activity/render-activity.ts (card INTERNALS — r2-cards owns, ledger 103).

### STATE AT SWITCH
- Lane was STILL WORKING; last known edits (progress tracker): route.ts, CenterLane.tsx,
  EmailsStream.tsx around 21:11-21:13Z. Census results NOT yet reported to the desk.
- Auto-save commits carry the edits: `git log --oneline -15 -- apps/houseloop-crm/components/leads/detail/ apps/houseloop-crm/app/api/leads/`

### NEXT SHIFT
1. Read the scope lock + this file + .claude/reports/allfeed-101/ (any census artifacts).
2. Diff the lane's commits to see how far the fix got; re-run the 104-vs-58 measurement on
   Conner-TEST (lead 90818711-0955-4655-b875-26a652e6d635) to get the CURRENT number.
3. Finish the census → classify → fix → the completeness test. A row that drops must be a CITED
   deliberate rule or it's a defect. document_sent missing is the known smoking gun to chase first.
