---
title: "Lee (Lee — Current Workflow (WIP))"
description: "Work-in-progress dossier — Lee · Infra · Pipeline & Workflow · FUB · Notifications. Lee — Current Workflow (WIP)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/lee.md` on every push. **Progress: checklist pending.**</Info>

**Owner:** Lee — Infra · Pipeline & Workflow · FUB · Notifications

## Checklist

_Last updated: 2026-07-13 PM_

### 🗺️ THE MASTER SSOT — Lead-Flow Architecture
**Artifact:** https://claude.ai/code/artifact/0b65eae5-d1e2-4a15-afb0-973eba93d6fb
The single source of truth for the whole lead-flow system. Everything below builds to this doc.

**The spine (locked with Zach 2026-07-13):** `Lead Integration → Source → Trigger Event → (fires a Workflow + sends a Notification)`.
Three shared axes across Trigger Events / Workflows / Notifications:
- **① Family (Integration):** `houseloop` · `follow_up_boss` · `inbox` (NEW — Company Inbox / GMB + company #s) · `transaction` · `custom`. Source = trigger-event `platform`.
- **② Lead Stage (arm):** `new_lead` (Registration) · `existing_lead`. = which of the two arm-slots a workflow fills.
- **③ Type (band):** `activity` · `communication` · `insight`. Source = trigger-event `band`.
- **Validity matrix:** Insights = Existing-only; Communication mints a new lead ONLY via Company Inbox (through the CRM intake identity gate — Adam's inbox has zero minting, confirmed). TC = existing-only.

### ✅ SHIPPED TO PROD (2026-07-13)
- Trigger Events **Configure** (two-arm workflow+notification) replacing the Create modal / landing pages / webinars; ⋮ = Configure + Pause (no Delete). Commit `44c003529d`.
- FUB + TC wired to Configure; **TC single-arm** "Existing Leads Only" (table + modal, primary columns).
- Wider Configure modal · centered ⚡ counts · TC column aligned to FUB.
- **Sectioned notification dropdown** (gray band headers, full 10-band catalog) — `lib/notifications/notification-sections.ts`. Commit `27e0f15aca`.
- Lead Source **Edit-Source / Manage-Routing split** (identity vs routing; Myli/human-owner/Pond/handoff/stage/tags).
- Dual-notify plumbing (mig 0391) + the existing-lead `lead_activity` silent-notification bug fix (earlier).

### 🟡 BUILT — GREEN, PENDING DEPLOY
- **HouseLoop Insights → single-arm existing-only** (`Existing Leads Only` pill + single existing workflow + single notification, like TC). `TriggerEventsBoard.tsx`, tsc+eslint green. Ships next push.

### 🔵 BUILDING NOW
- **Workflows taxonomy end-to-end** — the Create Workflow cascade (Family → Lead Stage → Type, validity-matrix-filtered) + grouped trigger/workflow pickers + shared `lib/workflows/workflow-taxonomy.ts` catalog + Company Inbox family in WorkflowsTable + `lead_stage` column. (agent in flight)

### ⚪ NEXT / COORDINATION
- Company Inbox family: minting via the intake gate (my lane); comms surface = Adam's `hl_inboxes` (coordinated — no parallel mint path).
- Configure modal workflow pick-existing dropdown → group by Family → Type.
- Docs reconcile (trigger-events / lead-sources / workflows guides) once the taxonomy ships.

### SHARED-TREE NOTE
Adam active in CRM (inbox/phone lane). Push pattern: commit only my files → stash Adam's uncommitted → standalone `tsc --noEmit` → push → restore. Never ship his WIP.
