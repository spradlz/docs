---
title: "Lee (Lee — WIP (updated 2026-08-08))"
description: "Work-in-progress dossier — Lee · Infra · Pipeline & Workflow · FUB · Notifications. Lee — WIP (updated 2026-08-08)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/lee.md` on every push. **Progress: checklist pending.**</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 122h ago. If Lee has shipped since, this dossier under-reports — refresh `.claude/state/wip/lee.md`.</Warning>

**Owner:** Lee — Infra · Pipeline & Workflow · FUB · Notifications

## Checklist

### ACTIVE: THE ONE ACTIVITY CARD CATALOG (Zach 8/8)
Scope lock: `.claude/state/session-scope-card-catalog.lock` (READ FIRST — the round-hole plan + phases).
Board: `.claude/artifacts/activity-catalog-master-audit.json` item id `card-catalog`.

**Goal:** one catalog defines the card → `resolveCard` resolves ONE `CardModel` → endpoint returns it →
every client (All feed, Website tab, mobile) renders `<ActivityCard>`. New kind = a catalog row, no client code.
Zach: "do it correctly, not vibe. not rendered at view time."

### 🟢 DONE + LIVE (committed f6b9005d6e P1 · ba4daa4371 P2)
- P1: `lib/activity/render-activity.ts` → `CardModel` + `resolveCard` + `densityFor` (density = DATA from
  `card_layout`+category+hasHouse, ZERO kind literals). Migration 0707 fixed the 2 drifted kinds
  (virtually_toured, zillow_message_received → `card_layout='request'` = hero-engagement). Endpoint
  `app/api/leads/[id]/activity/route.ts` returns `card` per row. Test `render-activity.card.test.ts` 14/14.
- P2: `components/leads/detail/ActivityCard.tsx` — the ONE renderer (CardModel → ActivityHeroCard hero/compact),
  no actor chip.

### 🟡 NEXT — resume here
- **P3 — All feed (CenterLane.tsx):** render `<ActivityCard model={a.card} timeStr=…>` for ACTIVITY rows
  (the endpoint now returns `a.card`). Retire AllStreamRow's bespoke headline/actor/density/picture. Keep
  StarWrap + hover actions + call player (CallCard) + doc/appt specials. Verify live on Melissa
  (bb1412cd-e764-44f2-b8c4-c9f1a5c9444e): property_viewed = compact, NO gap, NO black actor chip.
- **P4 — Website tab (PixelFeedStream.tsx):** render `<ActivityCard>` for activity rows; REVERT the interim
  per-frame patches (heroTitle/meta.label/actor + the `sentence` threading + the cast on line ~303). Deals +
  standing saved-searches keep their own cards. Verify live: name shows, VIA blue, one vocab across All/Website/Inquiries.
- **P5 — Docs + board:** update `docs-site/guides/lead-detail.mdx` "activity card catalog" section to document
  CardModel + resolveCard + `<ActivityCard>` (one catalog, all clients incl mobile). Flip board item `card-catalog`
  to done. Melissa walk screenshots → `.claude/reports/card-catalog-2026-08-08/`.

Gate each phase: `npx tsc --noEmit` = 0 · card test green · push · Railway deploy · live screenshot (cache-bust ?cb=).
CRM prod = https://crm.houseloop.ai. Deploy latency ~8-12 min.
