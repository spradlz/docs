---
title: "Caleb (SRG Past Client Reconciliation — WIP Log)"
description: "Work-in-progress dossier — Caleb · Data intel · past-client resolver. SRG Past Client Reconciliation — WIP Log."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/caleb/resolver.md` on every push. **Progress: 12/17 done (71%).** _Updated 2026-07-03 (Adam's addresses signed off — prepping the reconcile-engine RUN)._</Info>

**Owner:** Caleb — Data intel · past-client resolver

## Checklist

&gt; ## 📍 WHICH DOC IS WHICH (read first)
&gt; - **THIS PAGE = the live WIP LOG** (status + checklist + what's left).
&gt; - **GROUND TRUTH = [`guides/srg-reconciliation-engine`](/guides/srg-reconciliation-engine)** — architecture,
&gt;   laws, table map, tool catalog, card + dossier, audit UI, honest state, full running log.
&gt; - **BUILD CONTRACT = the scope-lock PRD** `.claude/state/session-scope-pastclient-reconciliation-promote.lock`.

**Owner:** Caleb — CRM child + transaction/agent/dossier resolution. Adam owns the address/HLID/value lane
([`wip/adam-pastclient-address-resolver`](/wip/adam-pastclient-address-resolver)) — **DONE + Zach-signed 2026-07-03.**

### 🎯 THE END GOAL (Zach LOCKED 2026-07-03)
Run the reconciliation engine across all data → **TWO PERFECT TABLES:**
1. **`hl_srg_past_clients`** — 1 perfect row / person·household + the **2-paragraph dossier + facts matrix**.
2. **`hl_srg_past_client_homes`** — everything under ONE address: one HLID per home, the client's multiple
   transactions grouped under it, each with its agent-of-record + disposition + Adam's clean address/HLID/value.
So Zach can send every past client a dead-on **HouseLoop Home Value Report**. Accuracy is the only benchmark.

### 🧭 LOCKED STRATEGY — THE DEAL LEDGER (Zach LOCKED 2026-07-03)
**Match DEALS, not addresses.** A deal fingerprints as client name + price + close date; two independent
ledgers join deterministically — **Edge 1** SISU↔MLS (name + price ±$3k + date ±20d: **85% of closings,
1,452/1,780 clients**) and **Edge 2** the MLS row's own client name (**1,782/1,971 rows named; 1,492/1,780
clients**). Three hops: CLIENT → MLS# → HLID → child row — the MLS# names the TRUE UNIT. All matches
materialize ONCE in **`hl_srg_deal_ledger`** (CRM `autorack`): both edges agree = GREEN · one = YELLOW ·
conflict/no key = RED → the LLM **explains** the residue into a review queue (`hl_srg_deal_overrides` —
it proposes, code disposes, a human confirms; **an LLM never assigns identity**). Adam's fork classes A–F
run as SQL **detectors over the ledger**; the Rule #0 fix map is *generated*. Full section:
[`guides/srg-reconciliation-engine`](/guides/srg-reconciliation-engine) §THE DEAL LEDGER · build contract
`.claude/state/session-scope-deal-ledger.lock`.

### Checklist (live 2026-07-03 evening — 13/17 done · 🏆 **SCOREBOARD: 1,799/1,799 — 100% RESOLVED, 0/0/0**)

- [x] **1 — Dedup** — 54 merges (1,834 → 1,780), reversible (`hl_srg_dedupe_log`).
- [x] **2 — Brain** (`hl_fub_person_facts`) — 2,178/2,178 household members.
- [x] **3 — MLS rooftop** (`hl_mls_property_history`) — 100%; 6,056 buyer-side agents; 151 cols.
- [x] **4 — HLID on the MLS table** — re-stamped from the web authority `hl_property`; cross-city 221 → 0.
- [x] **5 — Adam's address/HLID/value spine — DONE + signed off** — `pastclient-resolved-EXPORT-2026-07-03.csv`
  (1,951, all valued), 63 wrong-house links re-pinned, 22 out-of-area flagged, value engine locked. Consume it.
- [x] **6 — Dossier render + persist** — render was already wired (`card-note.mjs` DOSSIER section + AuditReport);
  built `persist-dossier.mjs` (gated, jsonb-merge, idempotent) → 19 new-format Stage-2 dossiers persisted to
  `hl_srg_past_clients.dossier_json` (verified by query + render). 6 legacy-format rows re-run in step 7; pc9 merged→#56 skipped.
- [x] **7 — Run the reconcile engine per client (FIRST 20 DONE 2026-07-03)** — built `refresh-pastclient-gt.mjs`
  (synced `hl_srg_pastclient_gt` from Adam's final EXPORT: 1,924 updated + 12 inserted + 33 history restamps —
  killed the Hutchens holden-mo poison) → complete-closings (2 inserts, 8 no-HLID flags) → link-contacts-fub →
  persist-reconcile → 71 rows staged (label 2026-07-03), 20/20 cards sweep clean on the Reconciled tab. FIXED
  persist-reconcile: a BUY we repped no longer carries sold_not_by_us (Nations 301 E 68th was dropped from
  TRANSACTIONS + lost row double-rendered). ⚠️ 5 fork pairs found INSIDE Adam's export (same slug, 2 HLIDs:
  Fontana-306, Granada-230, 10240-96th-F, 10136-96th-B, 1676 NW 850th bates/holden) — pinged Adam #11317.
- [ ] **8 — Prove 20 → sign-off** — READY: Zach walks `/settings/seller-report/audit` → Reconciled tab (label
  2026-07-03; pc1 Nations = spec-exact: 5 txns · 2 owns · lost 68th Ter + red Oakview) → `resolution-gate --signoff`.
  **⚠️ FORK-PAIR HOLD (Adam retracted #11324):** his first keeper calls were malformed (ranked by render/value;
  the AUTHORITY = the client's SISU closing mls#, which names the UNIT). Keeper application fully REVERSED +
  query-verified (5/5 GT rows restored, 8/8 history stamps back — reversal `defork-keeper-reversal-2026-07-03.json`).
  **850th since VERIFIED (#11327) + applied surgically** (GT HLID232431 out, mls 2388535 → HLID100059 child
  stamp, reversal `defork-850th-verified-reversal-2026-07-03.json`): **pc15 + pc4 UNBLOCKED** — pc4 kept her
  850th BUY (refuted Adam's "not on 850th" with her SISU Closed buyer row = the other side of Duston's sale).
  **Only pc7 Richardson still HELD** — Adam's verified map (#11330) calls her staged HLID100386 a **PHANTOM**
  (mls 2076493 not in the 10136 building); her real sale = mls 2471893 unit B (Kuchta pc139 bought the other
  side). She re-stages after Adam's Rule #0 split pass mints the real unit-B keeper. → **19 of the first 20
  promote-ready.** Also refuted (MLS 2543272): Hutchens' 1621st sell is REAL, not a mislabel — her card keeps
  sell 1621st $545k + buy 850th $387k (same day) + buy Nottingham. Adam's procedure (locked): same-unit
  relists/buy+sell MERGE into one HLID as priceHistory; different units SPLIT to their own HLID.
  **PROMOTE TOOL BUILT:** `sign-off-reconcile.mjs` — value-preserving merge (staged reconcile fields win, old
  home's value/enrichment cols carry over by HLID/rooftop, old rows → quarantine, per-pc transaction). Dry-verified
  on the 20: 73 old → 71 reconciled, agents 63/71, values carried. `--write` REFUSES without `--zach-approved` (Rule #0).
- [ ] **9 — PROMOTE → scale to 1,780** — staging → live child + parent (replaces stale 21/19; full write = Rule #0).
  Promote tool BUILT + dry-verified (`sign-off-reconcile.mjs`, value-preserving, refuses --write w/o --zach-approved).
- [x] **L1 — Build `hl_srg_deal_ledger` — WRITTEN 2026-07-03**: 1,898 deal rows / 1,780 clients · GREEN 1,564 ·
  YELLOW 132 · RED 202 (the L3 residue) · clients matched 1,545 (GREEN-best 1,434 + YELLOW-best 111) · 82 have
  no SISU deal. Third edge added (price+date-unique) catches cross-side deals — the ledger reproduced Hutchens'
  3 deals + the Duston mirror + Nations' one-HLID pairs on its own. Spot-checked by query.
- [x] **L2 — Detectors A–F → verdict map GENERATED 2026-07-03**: `fork-verdict-map-2026-07-03.csv` — **751
  verdicts** (A=27 MERGE · B=2 SPLIT · C=299 SEAT · D=45 REPIN · E=26 QUARANTINE · F=352 verify-provenance).
  Regression: all 5 hand-verified rooftops re-found by detector. Feeds Adam's ONE Rule #0 pass.
- [ ] **L3 — Residue queue** — RED rows → LLM-explainer (propose-only) → human review → `hl_srg_deal_overrides`.
- [ ] **L4 — Downstream reads the ledger** — resolver/persist/GT pins consume ledger matches; re-derivation dies.

### 🏠 THE MASTER HOUSE LIST — M-phases (Zach LOCKED 2026-07-03 PM: "one master list, every house
### one house with its transactions under it — first and foremost; nothing reconciles until resolved")
**Target:** `hl_srg_master_houses` — ONE row per HOUSE (HLID = the permanent home id + canonical address +
`/home` URL + value) with EVERY transaction beneath it: date · client (linked) · side we repped · price ·
agent of record · provenance. A live scoreboard on top: **N houses fully resolved / M with a named gap —
zero silent gaps.** Why 3 weeks failed: address-text matching + matches never saved + corrupt seed fields
($0 prices, 1970 epochs, 2021→2001 date truncation — 312 rows counted). The ledger fixed the method; the
M-phases finish the job.

- [x] **M1 — Close the match gaps — DONE 2026-07-03 PM**: Edge 4 (`hl_fub_sisu_link` person-ID, 674 clients
  linkable) + the HOUSE-PLACEMENT hop (off-market deals inherit the HLID from the client's GT-repinned child
  row by deal fingerprint). Ledger v2: 1,908 deals · **1,892 placed under a house (99.2%)** · GREEN 1,565 /
  YELLOW 135 / RED 208 (193 of the REDs still house-placed — off-market ≠ homeless).
- [x] **M2 — THE MASTER LIST IS LIVE — DONE 2026-07-03 PM**: VIEW **`hl_srg_master_houses`** (CRM) + CSV
  `master-house-list-2026-07-03.csv` + THE SCOREBOARD: **1,792 houses · 1,758 FULLY RESOLVED (98.1%) ·
  34 named gaps · 16 unplaced deals · 44 missing a GT address.** One row per house, every transaction
  beneath it (client · date · side · price · agent · mls · tier · provenance). Proof rows: 1676 NW 850th =
  one house, Hutchens buy + Duston sell beneath it; 23309 W 45th Ter = Nations buy 2020 + sell 2021, both Ana.
  Rebuild anytime: `build-deal-ledger.mjs --write` → `build-master-houses.mjs`.
- [x] **M4 — TAIL TO ZERO — DONE 2026-07-03 evening. 🏆 THE SCOREBOARD READS 100%:**
  **1,799 houses · 1,799 FULLY RESOLVED · 0 gaps · 0 unplaced · 0 missing addresses.** Zach walked the tail
  live and every verdict landed as an evidence-stamped override (32 total): Engel REO condo (Zach-values beat
  SISU) · Harrison 40-acre parcel (HLID192896 existed; card enriched, Sold event) · Centlivre FSBO (ALTA
  \$549k 11/6/24; 2024 Sold event over the stale 2022) · Szymanski new-construction (builder contract; Ninja
  re-valued \$331,800) · Mettman + Ward + White = the **SISU signing-price-ghost class** (2nd entry at LIST
  price on the contract date — 4 caught, EXCLUDED with receipts) · Bryant (buyer-side mls_deals gap; history
  stamp had it) · Worten \$0-price fix via history (\$57k 8/12/24 — AND she flipped it \$158k 3/19/25 without
  us = prime reactivation) · 3 named non-deals excluded (Arnago no-data referral · Radke family/internal-DNC ·
  Burdette realtor/agent-DNC). Every exclusion is audit-preserved in the ledger, never deleted.
- [ ] **M3 — Structural fix pass (post-100%, cosmetic-identity)**: the merges/splits from the verdict map
  (Granada merge, Fontana/10136 unit splits, the "6200-robinson-3rd-street" mis-parse, 20 building-lookups)
  as ONE Rule #0 worklist — these fix web-side HLID hygiene, not card truth (all client cards already resolve).

### Next concrete action
Zach walks the **Reconciled tab** of `/settings/seller-report/audit` (the fresh 2026-07-03 staging — first 20,
Nations pc1 first). On his GO: `resolution-gate --signoff` per client → **PROMOTE staging → live child
(Rule #0: typed Zach approval naming `hl_srg_past_client_homes`)** → scale the same motion to all 1,780.
Waiting on Adam (#11317): which HLID wins each of the 5 fork pairs in his export.

### Related documentation
- [`guides/srg-reconciliation-engine`](/guides/srg-reconciliation-engine) — **THE GROUND-TRUTH MASTER.**
- [`wip/adam-pastclient-address-resolver`](/wip/adam-pastclient-address-resolver) — the address/value lane (DONE).
- [`guides/property-data-and-address-identity`](/guides/property-data-and-address-identity) · [`guides/sold-data-and-price-history`](/guides/sold-data-and-price-history) · [`guides/houseloop-home-value`](/guides/houseloop-home-value) · [`guides/home-value-report`](/guides/home-value-report) — the why/how/end-state chain.

---

## PRD · `GT/GT - Pending/GT_SRG_RECONCILIATION_ENGINE_WIP.md`

#### SRG RECONCILIATION ENGINE — WORK IN PROGRESS (Caleb + Zach)

**_Last amended: 2026-07-03._** — forensic audit + doc reconciliation: the guide + the WIP build log are
now combined into the ONE master Mintlify guide `guides/srg-reconciliation-engine` ("SRG Past Client
Reconciliation — Master"); every count re-queried live; the child-table regression + env-var + bucket
drifts caught and documented.

&gt; **This is the private canonical WIP. The full rich master (architecture, table map w/ live counts, quoted
&gt; laws, tool/process catalog, card + relationship dossier, 3-tab Audit UI, reference cases, honest state,
&gt; fuck-up log, and the ONE running log) is the Mintlify guide `guides/srg-reconciliation-engine`.** Rule:
&gt; prove on the **FIRST 20 (Nations first)** before anything touches 1,780. "Done" = correct data VERIFIED
&gt; IN THE TABLE by query — never "the script ran."

##### THE ONE JOB
Every SRG past client → one accurate reconciliation card (person + household + every transaction we did
with the agent who worked it, even if they left + deed-confirmed current home + lost opportunities + a
relationship dossier), so we can send each one a dead-on **HouseLoop Home Value Report** (the end state —
`guides/home-value-report`). Accuracy is the only benchmark. The table is the truth.

##### ✅ MASTER CHECKLIST — the ONE, live (2026-07-03)

- [x] **Box 1 — Dedup** — 54 merges (1,834 → 1,780), 0 orphans, reversible (`hl_srg_dedupe_log`).
- [x] **Box 2 — Brain** (`hl_fub_person_facts`) — 2,178/2,178 past-client household members.
- [x] **Box 3 — MLS rooftop** (`hl_mls_property_history`, 7,765 rows) — 100%; 6,056 buyer-side agents; 151 cols.
- [x] **Box 4 — HLID on the MLS table** — re-stamped from the web authority `hl_property` (unit-aware
  identityKey); cross-city collapse 221 → 0; `hl_property_id` on 7,085 rows.
- [~] **Box 5 — Dotloop `.loop`** (`hl_srg_loop`) — ~86.9% agent / 88.7% address on the 2,128 real closings.
- [~] **Box 6 — RECONCILE ENGINE** — most pieces built: HLID-anchored resolver + S-Agent owns/lost-buy +
  de-fork (37) + not_srg (6) + GT canonical-address adoption (`hl_srg_pastclient_gt`) + 🔒 relationship
  dossier LOCKED. **NOT DONE:** the dossier isn't rendered in `card-note.mjs`, isn't persisted to the
  person row, and the resolver output has **not been PROMOTED** to the live child.
- [ ] **Box 7 — Prove first 20** — dossier God-tier on Nations + Hutchens; **13 signed off**. Then dossier
  on all 20 → render → Zach signs off → PROMOTE → scale to 1,780. No auto-publish to FUB.

##### ⚠️ HONEST STATE (2026-07-03) — the drift the audit caught
- **The live child `hl_srg_past_client_homes` is STALE/thin:** `determined_agent` on only **21** rows ·
  `disposition` on only **19** (earlier docs claimed 535/591; a canonical re-seat thinned them). Snapshotted
  in `hl_srg_pch_quarantine` (undo). Box 6's PROMOTE step has NOT run — that is the gap.
- **Bucket column live:** team 1,251 · ana-only **434** · ana-handoff 82 · agent-dnc 12 · internal 1. (The
  doctrine's "Ana-Only 461" was pre-dedup; use the live column.)
- **Env-var correction:** WEB DB = `HOUSELOOP_DATABASE_URL` (centerbeam). `DATABASE_URL` = the Camp David
  FUB mirror (192.168.1.211, no SSL). CRM = `HL_CRM_DATABASE_URL` (autorack).
- **Adam's clean spine:** `hl_srg_pastclient_gt` (CRM, 1,937 CASS-clean addresses) + the WEB VIEW
  `hl_pastclient_resolved` (1,951/1,951 HLID). Join by `website_hlid = mls_hlid` → `identity_key`, never a
  stored hlid (poison).
- **Schema-of-record gap:** several tables/columns exist in prod with no committed migration
  (`hl_fub_person_facts`, `hl_mls_property_history`, `hl_mls_rooftop_status`, `hl_srg_loop`,
  `hl_dedup_quarantine`, and parent cols `website_hlid/website_home_url/resolved_canonical_address/
  houseloop_resolved_at/houseloop_resolved_by/fub_identity`) — fold into migrations before "done."

##### ⭐ ARCHITECTURE (Zach LOCKED 2026-07-01)
The resolver is the brain. Every prior output — the old child + the 1,458 rich `resolver_block`s — is a
read-only SOURCE it ingests (NOT a thin SISU-spine, which regressed Nations). Union all transactions →
canonical-source-per-field → write the NEW child. Prove Nations → sign-off → scale. Quarantines
(`hl_srg_resolver_quarantine`, `hl_srg_pch_quarantine`) stay as provenance/undo.

##### 🔒 CANONICAL-SOURCE-PER-FIELD (Zach LOCKED 2026-07-01)
transaction = UNION of all sources (dedup by rooftop+side) · agent = SISU → dotloop licensee → FUB
Team-Exit → investigation (Ben Froeschl NEVER; Zach→Ana; keep the departed agent) · bucket = the SISU
`bucket` column · price/date = SISU · address = MLS by mls_number → Adam's CASS GT · current home = county
deed (competitor-bought → RED lost purchase) · lost listing = MLS later-sold-non-SRG (Bazalgette) · URL =
child by HLID · dossier = brain + FUB Railway timeline + resolver_block.

##### NEXT CONCRETE ACTION (Box 6 → 7)
Run `triangulate-stage2.mjs` (dossier) on the 20 → wire `card-note.mjs` to render the 2-paragraph dossier +
facts matrix + persist to `hl_srg_past_clients.dossier_json` → `persist-reconcile.mjs` stages the 20 homes
(GT-clean address) into `hl_srg_pch_staging` → Zach walks the 20 on `/settings/seller-report/audit` →
`resolution-gate.mjs --signoff` → **PROMOTE** staging → child (value-preserving; replaces the stale 21/19)
→ scale to 1,780 (Rule #0 typed go for the full child write).

##### REFERENCE DOCS (the why/how/end-state)
- `guides/property-data-and-address-identity` — HLID + address identity (the foundation).
- `guides/sold-data-and-price-history` — sold + price history (past-client parallel pipeline).
- `guides/houseloop-home-value` — the 5-bucket value fork + monthly refresh chain.
- `guides/home-value-report` — **THE END STATE** (the seller-report modal + monthly email).
Subordinate PRDs: `GT_HOUSELOOP_PASTCLIENT_SSOT_PRD.md` · `GT_HOUSELOOP_PASTCLIENT_DETERMINISTIC_RESOLVER_PRD.md`
· `GT_HOUSELOOP_SRG_PAST_CLIENT_HOMES_PRD.md` · `GT_HOUSELOOP_PASTCLIENT_MLS_RESOLVER_PRD.md` (Adam) ·
`GT_RELATIONSHIP_DOSSIER_PRD.md` · `HOUSELOOP_DATA_DOCTRINE.md` §4b.

---

## Scope lock · `.claude/state/session-scope-pastclient-reconciliation-promote.lock`

#### SCOPE LOCK / PRD — SRG PAST-CLIENT RECONCILIATION → THE TWO PERFECT TABLES
#### Owner: Caleb — SOLE, END-TO-END (Zach directive 2026-07-03: Adam OUT of the execution path entirely;
####   the "Adam's lane" references below are now HISTORY/read-only inputs, not active ownership. Every
####   remaining step — including web-side Listing fixes — executes in THIS terminal, gated only by Zach's
####   typed Rule #0 approvals.)
#### Mode: BUILD · Zach-approved 2026-07-03 (restart onto Fable 5)
#### Tree: /Users/zachspradling/Code/BossLoop-Gold · Branch: houseloop-app
#### Handoff: .claude/state/caleb-handoff.md · Master doc: docs-site/guides/srg-reconciliation-engine.mdx
#### Private PRD: GT/GT - Pending/GT_SRG_RECONCILIATION_ENGINE_WIP.md

##### 🎯 GOAL (the ONE outcome — Zach LOCKED 2026-07-03)
Run the reconciliation engine across ALL data (Caleb's sources + Adam's now-complete address/value spine)
and produce TWO PERFECT TABLES that power a HouseLoop Home Value Report for every SRG past client:

1. **`hl_srg_past_clients`** (parent) — 1 perfect row / person·household: name + household (deduped, spouse
   merged) + `bucket` + the **relationship dossier (2 paragraphs + the extracted facts matrix)** + pointer
   to their home(s). No placeholders, no stale fields.
2. **`hl_srg_past_client_homes`** (child) — everything under ONE address: **ONE canonical HLID per physical
   home, the client's MULTIPLE transactions (buy/sell/lost) grouped under it**, each transaction with its
   **agent-of-record** (even a departed teammate), the home's **disposition** (owns/moved/lost-purchase/
   lost-listing), and the **correct address + HLID + `/home` URL + value** adopted from Adam's spine.

##### ✅ ACCEPTANCE CRITERIA — the definition of "perfect" (per client, verified by query AND by eye)
- [ ] Person + household deduped (one card; spouse merged by FUB id).
- [ ] Every transaction sits under ONE canonical HLID (all deals at one address share one home).
- [ ] Every transaction has a real `determined_agent` from the waterfall — NEVER "SRG"/placeholder/Ben Froeschl.
- [ ] Every home has a `disposition` (owns / moved / lost-purchase / lost-listing).
- [ ] Address + HLID + `/home` URL + value = Adam's clean spine (join `website_hlid=mls_hlid`→`identity_key`).
- [ ] `bucket` from the SISU-derived column (not `is_ana`).
- [ ] Dossier = 2 paragraphs (`personality_paragraph` + `transaction_paragraph`) + `dossier_facts` matrix,
      persisted to `hl_srg_past_clients.dossier_json`, Sierra-Interactive-gap handled.
- [ ] The card renders correctly on `/settings/seller-report/audit` (Follow-Up Boss Notes section) AND in
      the /home render where value shows (Adam's "render with eyes" law).

##### 📋 EXECUTION STEPS (ordered; all gated, DRY-by-default, reversible)
1. **Dossier render + persist** — `card-note.mjs` renders the 2 paragraphs + facts matrix at card bottom;
   persist Stage-2 dossier (`hl_srg_resolver_stage2`) → `hl_srg_past_clients.dossier_json`.
2. **Per-client reconcile:** `triangulate-resolver.mjs` (Stage-1 $0, unions sources) → `triangulate-stage2.mjs`
   (Haiku dossier) → adopt Adam's clean address/HLID/value by `website_hlid=mls_hlid` → `complete-closings.mjs`
   (--write) → `link-contacts-fub.mjs` (--write) → `persist-reconcile.mjs` (--write) into `hl_srg_pch_staging`.
3. **Prove 20** (Nations first, then Hutchens + the golds) — Zach walks the audit → `resolution-gate.mjs
   --signoff` each.
4. **PROMOTE** staging → live `hl_srg_past_client_homes` + parent (value-preserving; replaces stale 21/19).
5. **Scale** to all 1,780. Then `fub-publish.mjs --write` (refuses un-resolved) after Zach signs off process.

##### 🟢 IN SCOPE
- The two CRM tables (`hl_srg_past_clients`, `hl_srg_past_client_homes`) + their staging/quarantine.
- The dossier render in `card-note.mjs` (CRM) + persist to the person row.
- Adopting Adam's address/HLID/value spine (read-only consume; never re-derive the address).
- The audit surface where it reflects the reconciled card.

##### 🔴 OUT OF SCOPE (other lanes — do NOT touch)
- Adam's HOME side: address resolution, HLID minting/de-fork, the value engine, website `Listing` seating,
  the off-market/cancelled RENDER pill (Adam's open front-end item). It is DONE + signed off — consume it.
- Website `Listing` writes (Rule #0, Lee/Adam lane).
- FUB address updates / referral-attribution (Benji/Zach).
- The 22 flagged out-of-area/bad-source addresses (`repin-crm-flagged-2026-07-03.txt`) — do NOT force-resolve;
  mark investigation/out-of-area. (e.g. CHICAGO MO, PHILADELPHIA 16601, HART TOWNSHIP.)

##### 🧱 THE TABLES + GRAIN (the contract)
- `hl_srg_past_clients` — 1 row / person·household. Grain unchanged; fill dossier_json + confirm bucket + link.
- `hl_srg_past_client_homes` — 1 row / (client × home-transaction). Multiple transactions at one address ALL
  carry the SAME canonical HLID (Adam's re-pin ensured 1,912 distinct HLIDs over 2,168 rows). The card groups
  them under one address block. Fill `determined_agent`, `determined_agent_source`, `disposition`,
  `disposition_source`, adopt address/HLID/URL/value.

##### 🔒 GATES / LAWS
- Rule #0: the full child PROMOTE write + any `Listing` write = TYPED Zach approval naming the table.
- Haiku only in the resolve/dossier path (`claude-haiku-4-5-20251001`, key `ANTHROPIC_API_KEY_HOUSELOOP`).
- "Done" = data verified in the table by query + rendered correctly in Chrome. Prove 20 before scale.
- No auto-publish to FUB. Reconcile, don't rebuild. The table is the truth.

##### 📎 KEY INPUTS
- Adam's spine: `.claude/reports/pastclient-resolved-EXPORT-2026-07-03.csv` (1,951; key `mls_hlid`+`hlid`) ·
  WEB VIEW `hl_pastclient_resolved` · CRM `hl_srg_pastclient_gt` · flagged `repin-crm-flagged-2026-07-03.txt`.
- Sources: SISU (`sisu_transactions`) · MLS (`hl_srg_mls_deals`, `hl_mls_property_history`) · dotloop
  (`hl_srg_loop`) · brain (`hl_fub_person_facts`) · prior (`hl_srg_resolver_quarantine`).

---

## Handoff · `.claude/state/caleb-handoff.md`

#### CALEB HANDOFF — SRG RECONCILIATION: THE LEDGER IS AT 100%. EXECUTE THE GATES.
**Rewritten 2026-07-03 evening after THE MILESTONE. Branch `houseloop-app` · Tree `/Users/zachspradling/Code/BossLoop-Gold`**
**You wake up cold: read this top-to-bottom, then `.claude/state/caleb.lock`. You own this END-TO-END
(Zach directive 2026-07-03 — Adam is OUT of the execution path; Zach's typed Rule #0 is the only external gate).**

##### 0. WHERE WE ARE — 🏆 THE SCOREBOARD READS 100%
&gt; **`hl_srg_master_houses`: 1,799 houses · 1,799 FULLY RESOLVED · 0 gaps · 0 unplaced · 0 missing addresses.**

Zach walked the entire residue live on 2026-07-03. Every verdict = an evidence-stamped row in
**`hl_srg_deal_overrides`** (32 total — they survive every rebuild; NEVER hand-edit the ledger itself).

##### 1. THE ARCHITECTURE THAT WON (Zach LOCKED — never regress to address-matching)
**Match DEALS, not addresses.** `hl_srg_deal_ledger` (CRM autorack) = 1 row per client×deal, built by
`tools/houseloop-cards/build-deal-ledger.mjs --write` (UPSERT-only, idempotent). Edges: SISU↔MLS
name+price±3k+date±20d · MLS-row client name · price+date-unique (cross-side) · `hl_fub_sisu_link` person-ID ·
child-row fingerprint placement (pre-2010 child dates = the corrupt class → price-unique-within-client).
PENDING=CLOSED (date falls back forecast→UC; SISU died 6/19 so Pending froze). OVERRIDE FIELDS WIN
(agent/close_date/price/side/hlid/tier + `excluded` for verified duplicates/non-deals).
**THE MASTER LIST** = `hl_srg_master_houses` VIEW ← `build-master-houses.mjs` (also exports the CSV + scoreboard).
Doc: `guides/srg-reconciliation-engine` §THE DEAL LEDGER · contract `.claude/state/session-scope-deal-ledger.lock`.

##### 2. WHAT ZACH'S WALK TAUGHT (the classes — recognize on sight)
- **SISU signing-price ghosts** (4 caught: Mettman/Ward/White + variants): a 2nd SISU row at LIST price on the
  contract date. Verdict: `excluded=true` w/ receipts. NEVER delete.
- **Off-market is a SIGNAL** (FSBO Centlivre-ALTA · builder Szymanski · land Harrison · REO Engel): empty MLS
  = expected; resolve via ALTA/dotloop/builder contract/Zillow zpid; enrich the Listing (zestimate +
  priceHistory Sold event — the pill derives from the NEWEST Sold event).
- **Named non-deals**: Arnago $2.1k referral (no data, closed as named grave) · Radke (family→bucket
  `internal`) · Burdette (realtor→bucket `agent-dnc`).
- **Worten flip** = the reactivation archetype: bought $57k 8/12/24 w/ Lana Mount, flipped $158k 3/19/25
  WITHOUT us (FUB 36899, 816-878-2272 — reassigned to Zach+Ana May 2026).

##### 3. THE REMAINING GATES (in order — everything else is DONE)
1. **Re-derive staging from the 100% ledger**, then **Zach walks FUB Notes → Pending** on
   `/settings/seller-report/audit` (rebuilt page: Master Houses · Needs Resolution · Transaction History ·
   FUB Pending · FUB Final).
2. **THE PROMOTE** — `tools/houseloop-cards/sign-off-reconcile.mjs <pcs> --label <date> --write --zach-approved`
   (value-preserving merge, old rows→quarantine, per-pc transaction). **REFUSES without --zach-approved =
   Zach's TYPED Rule #0 naming `hl_srg_past_client_homes`.** Then `resolution-gate.mjs --signoff` per client.
3. **M3 HLID hygiene pass (Rule #0, ONE worklist)**: verdict map `fork-verdict-map-2026-07-03.csv`
   (regenerate: `deal-ledger-detectors.mjs`) — Granada merge, Fontana/10136 unit splits, the
   "6200-robinson-3rd-street" mis-parse, 20 building-lookups (4 logs at /tmp/mls-*.log on Camp David).
   Web-side `Listing`/`hl_property` mutations — Zach types the GO, I execute (Adam is out).
4. **THE PRIZE**: dossiers to all 1,780 (stage2 scaled) → the Home Value Report send (`guides/home-value-report`).

##### 4. TOOLS + TABLES (all mine, all built)
`build-deal-ledger.mjs` · `build-master-houses.mjs` · `deal-ledger-detectors.mjs` · `fill-gaps-dotloop.mjs`
(Zach's taught method: dotloop parties → side/agent/date/price) · `refresh-pastclient-gt.mjs` (GT sync from web
authority) · `persist-dossier.mjs` · `persist-reconcile.mjs` (BUY never carries sold_not_by_us — the lost sale
is its OWN row) · `sign-off-reconcile.mjs` · `resolution-gate.mjs` · `complete-closings.mjs` · `link-contacts-fub.mjs`.
Tables: ledger/overrides/master-view + GT (`hl_srg_pastclient_gt`, 1,949+, synced from `hl_property`) + the
two perfect tables (parent/child) + staging/quarantines. DBs per the lock file table (CRM=autorack ·
WEB=HOUSELOOP_DATABASE_URL · never DATABASE_URL for web).

##### 5. LAWS (unchanged, burned-in)
Accuracy only · the table is the truth · overrides never hand-edit the ledger · Ben Froeschl NEVER · keep
departed agents (the relationship IS the asset) · bucket column not is_ana · render with eyes · Rule #0 typed
per destructive op · no FUB publish before process sign-off · docs auto-mirror on commit (fix MDX errors or
the mirror silently aborts) · stash-dance pushes (never pop what you didn't push; re-park others' stashes).

## Related documentation

Doc sections this work creates or updates:

- [`guides/srg-reconciliation-engine`](/guides/srg-reconciliation-engine)
