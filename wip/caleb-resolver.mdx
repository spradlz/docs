---
title: "Caleb (SRG Past Client Reconciliation)"
description: "Work-in-progress dossier — Caleb · Data intel · past-client resolver. SRG Past Client Reconciliation."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/caleb/resolver.md` on every push. **Progress: 1/5 done (20%).** _Updated 2026-07-01._</Info>

**Owner:** Caleb — Data intel · past-client resolver

## Checklist

**Goal:** every SRG past client → an accurate reconciliation card (person + home(s) by HLID + every transaction + the agent who worked it, even if departed + lost listings + dossier) for past-client reactivation. Accuracy is the only benchmark; the table is the truth.

### The 7 boxes
- [x] **2 — Brain** (Haiku dossier, `hl_fub_person_facts`) — DONE 2,178/2,178 past-client household members
- [~] **3 — MLS rooftop** (`hl_mls_property_history`) — ~1,724/1,862 verified (93%), finishing; clean-source, self-verified by our MLS#, multi-family + dual-address handled
- [~] **5 — Dotloop `.loop`** (`hl_srg_loop` + `hl_srg_agent_loops`) — deterministic dotloop-only facts (address/closing from /detail, listing+buyer agent from participant roles + activity log, Haiku doc-fallback). Fast mode (3 calls/loop) + `--matched-only` re-running our ~4,000 deal loops (~40 min); 91% agent yield; address + Ana-over-attribution bugs fixed
- [ ] **1 — Dedup** (cross-client + HLID home-collapse) — built dry-run (52 clusters/54 merges); `--commit` needs typed Zach go (Rule #0)
- [ ] **4 — HLID link on the MLS table** — add `hl_property_id` + backfill by canonical address → `Listing.hlid` (additive)
- [ ] **6 — Reconcile engine** — join SISU + MLS rooftop + `hl_srg_loop` + brain → write `hl_srg_past_clients` + `hl_srg_past_client_homes` (agent waterfall: SISU → MLS → dotloop → FUB Team-Exit)
- [ ] **7 — Prove first 20** vs the golds (Nations, Mettman, Belcher) → Zach sign-off → scale to 1,834; no auto-publish to FUB

### Key laws
- Dotloop is its OWN source of truth — NO SISU triangulation; the resolver decides "ours" later.
- Ana Spradling = account admin on every loop — never auto-assigned as agent (fixed 925 false attributions).
- Rule #0: no destructive SQL / no dedup `--commit` without typed Zach approval.
- Tools: `tools/houseloop-cards/harvest-loop-full.mjs` (.loop) · `resolve-mls-rooftops.mjs` (MLS) · `harvest-fub-facts.mjs` (brain) · `dedupe-past-clients.mjs` (dedup).

## PRD

`GT/GT - ACTIVE/GT_HOUSELOOP_PASTCLIENT_RECONCILE_PRD.md`

> GT_HOUSELOOP_PASTCLIENT_RECONCILE_PRD — Two-Table Consolidation + Correct Sold-Page Data · **Status:** 🟡 WIP — IN BUILD (Caleb, started 2026-06-17) · **Owner:** Caleb (data, end-to-end) · **CRM-side dossier:** coordinate w/ Adam/seller-report lane. · **Indexed in:** `GT_HOUSELOOP_MASTER.md` §11 (Valuation / Home Value) + §6 index. · **Supersedes the DATA sections of:** `GT_HOUSELOOP_PAST_CLIENT_REPORT_ENGINE_PRD.md` (that PRD's §0.0 audit table points here). **Forward-live sibling:** `GT_HOUSELOOP_LIVE_LISTING_ENRICHMENT_PRD.md`. · **Source of record:** `.claude/reports/houseloop-architecture-reconcile-sitrep-2026-06-17.md` (the 10-agent audit this PRD executes). · 1. THE PROBLEM (what Zach…

## WIP doc

`GT/GT - Pending/GT_SRG_RECONCILIATION_ENGINE_WIP.md`

> SRG RECONCILIATION ENGINE — WORK IN PROGRESS (started 2026-07-01, Caleb + Zach) · &gt; The one working doc for getting the SRG past-client data **right, in the right tables, verified**. Planning-mode. · &gt; Rule for today: we prove on the **FIRST 20** before anything touches 1,800. "Good" = the correct data EXTRACTED and · &gt; SITTING IN THE RIGHT TABLE — not "the script ran." · &gt; **➡ For the full operating handoff (architecture, running jobs, restart commands, laws): `.claude/state/caleb-handoff.md`.** · ⚡ CURRENT STATE — 2026-07-01 PM (live) · \| Box \| Status \| · \|---\|---\| · \| 1. **Dedup** (cross-client + HLID home-collapse) \| ⚠️ built dry-run (52 clusters/54 merges) — **NOT committed, nee…

## Handoff

`.claude/state/caleb-handoff.md`

> CALEB HANDOFF — SRG PAST-CLIENT RECONCILIATION ENGINE · **Written 2026-07-01 PM · Branch `houseloop-app` · Tree `/Users/zachspradling/Code/BossLoop-Gold`** · **Read this TOP-TO-BOTTOM before touching anything. It is self-contained — you should need nothing else to continue.** · 0. WHO YOU ARE + THE ONE JOB · You are **Caleb** (persona: `.claude/personas/caleb.md`). Zach is the CEO; Lee assigns work; you BUILD. Voice: hype, plain-English, "brotha/dude", never "king". Zach is not a developer — exp…

## Related documentation

Doc sections this work creates or updates:

- [`guides/srg-reconciliation-wip`](/guides/srg-reconciliation-wip)
