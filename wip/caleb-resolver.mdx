---
title: "Caleb (SRG Past Client Reconciliation — WIP Log)"
description: "Work-in-progress dossier — Caleb · Data intel · past-client resolver. SRG Past Client Reconciliation — WIP Log."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/caleb/resolver.md` on every push. **Progress: 5/9 done (56%).** _Updated 2026-07-03 (Adam's addresses signed off — prepping the reconcile-engine RUN)._</Info>

**Owner:** Caleb — Data intel · past-client resolver

## Checklist

&gt; ## 📍 WHICH DOC IS WHICH (read first)
&gt; - **THIS PAGE = the live WIP LOG** (status + checklist + what's left).
&gt; - **GROUND TRUTH = [`guides/srg-reconciliation-engine`](/guides/srg-reconciliation-engine)** — architecture,
&gt;   laws, table map, tool catalog, card + dossier, audit UI, honest state, full running log.
&gt; - **BUILD CONTRACT = the scope-lock PRD** `.claude/state/session-scope-pastclient-reconciliation-promote.lock`.

**Owner:** Caleb — CRM child + transaction/agent/dossier resolution. Adam owns the address/HLID/value lane
([`wip/adam-pastclient-address-resolver`](/wip/adam-pastclient-address-resolver)) — **DONE + Zach-signed 2026-07-03.**

### 🎯 THE END GOAL (Zach LOCKED 2026-07-03)
Run the reconciliation engine across all data → **TWO PERFECT TABLES:**
1. **`hl_srg_past_clients`** — 1 perfect row / person·household + the **2-paragraph dossier + facts matrix**.
2. **`hl_srg_past_client_homes`** — everything under ONE address: one HLID per home, the client's multiple
   transactions grouped under it, each with its agent-of-record + disposition + Adam's clean address/HLID/value.
So Zach can send every past client a dead-on **HouseLoop Home Value Report**. Accuracy is the only benchmark.

### Checklist (live 2026-07-03 — 5/9 done)

- [x] **1 — Dedup** — 54 merges (1,834 → 1,780), reversible (`hl_srg_dedupe_log`).
- [x] **2 — Brain** (`hl_fub_person_facts`) — 2,178/2,178 household members.
- [x] **3 — MLS rooftop** (`hl_mls_property_history`) — 100%; 6,056 buyer-side agents; 151 cols.
- [x] **4 — HLID on the MLS table** — re-stamped from the web authority `hl_property`; cross-city 221 → 0.
- [x] **5 — Adam's address/HLID/value spine — DONE + signed off** — `pastclient-resolved-EXPORT-2026-07-03.csv`
  (1,951, all valued), 63 wrong-house links re-pinned, 22 out-of-area flagged, value engine locked. Consume it.
- [ ] **6 — Dossier render + persist** — wire `card-note.mjs` to render the 2 paragraphs + facts matrix +
  persist Stage-2 dossier (`hl_srg_resolver_stage2`, 26 staged) → `hl_srg_past_clients.dossier_json`.
- [ ] **7 — Run the reconcile engine per client** — resolver → stage2 → adopt Adam's clean home data →
  complete-closings → link-contacts-fub → persist-reconcile to `hl_srg_pch_staging`. Fills agent + disposition
  (currently STALE: 21 / 19).
- [ ] **8 — Prove 20 → sign-off** — Nations + Hutchens + golds; Zach walks the audit → `resolution-gate --signoff`.
- [ ] **9 — PROMOTE → scale to 1,780** — staging → live child + parent (replaces stale 21/19; full write = Rule #0).

### Next concrete action
Wire the dossier render in `card-note.mjs` + persist to the person row, then run `triangulate-stage2.mjs` on
the first 20 (Nations first) → `persist-reconcile.mjs` to staging (adopting Adam's clean address/HLID by
`website_hlid=mls_hlid`) → Zach walks `/settings/seller-report/audit` → `resolution-gate --signoff` → PROMOTE
→ scale. Full contract: the scope-lock PRD.

### Related documentation
- [`guides/srg-reconciliation-engine`](/guides/srg-reconciliation-engine) — **THE GROUND-TRUTH MASTER.**
- [`wip/adam-pastclient-address-resolver`](/wip/adam-pastclient-address-resolver) — the address/value lane (DONE).
- [`guides/property-data-and-address-identity`](/guides/property-data-and-address-identity) · [`guides/sold-data-and-price-history`](/guides/sold-data-and-price-history) · [`guides/houseloop-home-value`](/guides/houseloop-home-value) · [`guides/home-value-report`](/guides/home-value-report) — the why/how/end-state chain.

---

## PRD · `GT/GT - Pending/GT_SRG_RECONCILIATION_ENGINE_WIP.md`

#### SRG RECONCILIATION ENGINE — WORK IN PROGRESS (Caleb + Zach)

**_Last amended: 2026-07-03._** — forensic audit + doc reconciliation: the guide + the WIP build log are
now combined into the ONE master Mintlify guide `guides/srg-reconciliation-engine` ("SRG Past Client
Reconciliation — Master"); every count re-queried live; the child-table regression + env-var + bucket
drifts caught and documented.

&gt; **This is the private canonical WIP. The full rich master (architecture, table map w/ live counts, quoted
&gt; laws, tool/process catalog, card + relationship dossier, 3-tab Audit UI, reference cases, honest state,
&gt; fuck-up log, and the ONE running log) is the Mintlify guide `guides/srg-reconciliation-engine`.** Rule:
&gt; prove on the **FIRST 20 (Nations first)** before anything touches 1,780. "Done" = correct data VERIFIED
&gt; IN THE TABLE by query — never "the script ran."

##### THE ONE JOB
Every SRG past client → one accurate reconciliation card (person + household + every transaction we did
with the agent who worked it, even if they left + deed-confirmed current home + lost opportunities + a
relationship dossier), so we can send each one a dead-on **HouseLoop Home Value Report** (the end state —
`guides/home-value-report`). Accuracy is the only benchmark. The table is the truth.

##### ✅ MASTER CHECKLIST — the ONE, live (2026-07-03)

- [x] **Box 1 — Dedup** — 54 merges (1,834 → 1,780), 0 orphans, reversible (`hl_srg_dedupe_log`).
- [x] **Box 2 — Brain** (`hl_fub_person_facts`) — 2,178/2,178 past-client household members.
- [x] **Box 3 — MLS rooftop** (`hl_mls_property_history`, 7,765 rows) — 100%; 6,056 buyer-side agents; 151 cols.
- [x] **Box 4 — HLID on the MLS table** — re-stamped from the web authority `hl_property` (unit-aware
  identityKey); cross-city collapse 221 → 0; `hl_property_id` on 7,085 rows.
- [~] **Box 5 — Dotloop `.loop`** (`hl_srg_loop`) — ~86.9% agent / 88.7% address on the 2,128 real closings.
- [~] **Box 6 — RECONCILE ENGINE** — most pieces built: HLID-anchored resolver + S-Agent owns/lost-buy +
  de-fork (37) + not_srg (6) + GT canonical-address adoption (`hl_srg_pastclient_gt`) + 🔒 relationship
  dossier LOCKED. **NOT DONE:** the dossier isn't rendered in `card-note.mjs`, isn't persisted to the
  person row, and the resolver output has **not been PROMOTED** to the live child.
- [ ] **Box 7 — Prove first 20** — dossier God-tier on Nations + Hutchens; **13 signed off**. Then dossier
  on all 20 → render → Zach signs off → PROMOTE → scale to 1,780. No auto-publish to FUB.

##### ⚠️ HONEST STATE (2026-07-03) — the drift the audit caught
- **The live child `hl_srg_past_client_homes` is STALE/thin:** `determined_agent` on only **21** rows ·
  `disposition` on only **19** (earlier docs claimed 535/591; a canonical re-seat thinned them). Snapshotted
  in `hl_srg_pch_quarantine` (undo). Box 6's PROMOTE step has NOT run — that is the gap.
- **Bucket column live:** team 1,251 · ana-only **434** · ana-handoff 82 · agent-dnc 12 · internal 1. (The
  doctrine's "Ana-Only 461" was pre-dedup; use the live column.)
- **Env-var correction:** WEB DB = `HOUSELOOP_DATABASE_URL` (centerbeam). `DATABASE_URL` = the Camp David
  FUB mirror (192.168.1.211, no SSL). CRM = `HL_CRM_DATABASE_URL` (autorack).
- **Adam's clean spine:** `hl_srg_pastclient_gt` (CRM, 1,937 CASS-clean addresses) + the WEB VIEW
  `hl_pastclient_resolved` (1,951/1,951 HLID). Join by `website_hlid = mls_hlid` → `identity_key`, never a
  stored hlid (poison).
- **Schema-of-record gap:** several tables/columns exist in prod with no committed migration
  (`hl_fub_person_facts`, `hl_mls_property_history`, `hl_mls_rooftop_status`, `hl_srg_loop`,
  `hl_dedup_quarantine`, and parent cols `website_hlid/website_home_url/resolved_canonical_address/
  houseloop_resolved_at/houseloop_resolved_by/fub_identity`) — fold into migrations before "done."

##### ⭐ ARCHITECTURE (Zach LOCKED 2026-07-01)
The resolver is the brain. Every prior output — the old child + the 1,458 rich `resolver_block`s — is a
read-only SOURCE it ingests (NOT a thin SISU-spine, which regressed Nations). Union all transactions →
canonical-source-per-field → write the NEW child. Prove Nations → sign-off → scale. Quarantines
(`hl_srg_resolver_quarantine`, `hl_srg_pch_quarantine`) stay as provenance/undo.

##### 🔒 CANONICAL-SOURCE-PER-FIELD (Zach LOCKED 2026-07-01)
transaction = UNION of all sources (dedup by rooftop+side) · agent = SISU → dotloop licensee → FUB
Team-Exit → investigation (Ben Froeschl NEVER; Zach→Ana; keep the departed agent) · bucket = the SISU
`bucket` column · price/date = SISU · address = MLS by mls_number → Adam's CASS GT · current home = county
deed (competitor-bought → RED lost purchase) · lost listing = MLS later-sold-non-SRG (Bazalgette) · URL =
child by HLID · dossier = brain + FUB Railway timeline + resolver_block.

##### NEXT CONCRETE ACTION (Box 6 → 7)
Run `triangulate-stage2.mjs` (dossier) on the 20 → wire `card-note.mjs` to render the 2-paragraph dossier +
facts matrix + persist to `hl_srg_past_clients.dossier_json` → `persist-reconcile.mjs` stages the 20 homes
(GT-clean address) into `hl_srg_pch_staging` → Zach walks the 20 on `/settings/seller-report/audit` →
`resolution-gate.mjs --signoff` → **PROMOTE** staging → child (value-preserving; replaces the stale 21/19)
→ scale to 1,780 (Rule #0 typed go for the full child write).

##### REFERENCE DOCS (the why/how/end-state)
- `guides/property-data-and-address-identity` — HLID + address identity (the foundation).
- `guides/sold-data-and-price-history` — sold + price history (past-client parallel pipeline).
- `guides/houseloop-home-value` — the 5-bucket value fork + monthly refresh chain.
- `guides/home-value-report` — **THE END STATE** (the seller-report modal + monthly email).
Subordinate PRDs: `GT_HOUSELOOP_PASTCLIENT_SSOT_PRD.md` · `GT_HOUSELOOP_PASTCLIENT_DETERMINISTIC_RESOLVER_PRD.md`
· `GT_HOUSELOOP_SRG_PAST_CLIENT_HOMES_PRD.md` · `GT_HOUSELOOP_PASTCLIENT_MLS_RESOLVER_PRD.md` (Adam) ·
`GT_RELATIONSHIP_DOSSIER_PRD.md` · `HOUSELOOP_DATA_DOCTRINE.md` §4b.

---

## Scope lock · `.claude/state/session-scope-pastclient-reconciliation-promote.lock`

#### SCOPE LOCK / PRD — SRG PAST-CLIENT RECONCILIATION → THE TWO PERFECT TABLES
#### Owner: Caleb · Mode: BUILD · Zach-approved 2026-07-03 (restart onto Fable 5)
#### Tree: /Users/zachspradling/Code/BossLoop-Gold · Branch: houseloop-app
#### Handoff: .claude/state/caleb-handoff.md · Master doc: docs-site/guides/srg-reconciliation-engine.mdx
#### Private PRD: GT/GT - Pending/GT_SRG_RECONCILIATION_ENGINE_WIP.md

##### 🎯 GOAL (the ONE outcome — Zach LOCKED 2026-07-03)
Run the reconciliation engine across ALL data (Caleb's sources + Adam's now-complete address/value spine)
and produce TWO PERFECT TABLES that power a HouseLoop Home Value Report for every SRG past client:

1. **`hl_srg_past_clients`** (parent) — 1 perfect row / person·household: name + household (deduped, spouse
   merged) + `bucket` + the **relationship dossier (2 paragraphs + the extracted facts matrix)** + pointer
   to their home(s). No placeholders, no stale fields.
2. **`hl_srg_past_client_homes`** (child) — everything under ONE address: **ONE canonical HLID per physical
   home, the client's MULTIPLE transactions (buy/sell/lost) grouped under it**, each transaction with its
   **agent-of-record** (even a departed teammate), the home's **disposition** (owns/moved/lost-purchase/
   lost-listing), and the **correct address + HLID + `/home` URL + value** adopted from Adam's spine.

##### ✅ ACCEPTANCE CRITERIA — the definition of "perfect" (per client, verified by query AND by eye)
- [ ] Person + household deduped (one card; spouse merged by FUB id).
- [ ] Every transaction sits under ONE canonical HLID (all deals at one address share one home).
- [ ] Every transaction has a real `determined_agent` from the waterfall — NEVER "SRG"/placeholder/Ben Froeschl.
- [ ] Every home has a `disposition` (owns / moved / lost-purchase / lost-listing).
- [ ] Address + HLID + `/home` URL + value = Adam's clean spine (join `website_hlid=mls_hlid`→`identity_key`).
- [ ] `bucket` from the SISU-derived column (not `is_ana`).
- [ ] Dossier = 2 paragraphs (`personality_paragraph` + `transaction_paragraph`) + `dossier_facts` matrix,
      persisted to `hl_srg_past_clients.dossier_json`, Sierra-Interactive-gap handled.
- [ ] The card renders correctly on `/settings/seller-report/audit` (Follow-Up Boss Notes section) AND in
      the /home render where value shows (Adam's "render with eyes" law).

##### 📋 EXECUTION STEPS (ordered; all gated, DRY-by-default, reversible)
1. **Dossier render + persist** — `card-note.mjs` renders the 2 paragraphs + facts matrix at card bottom;
   persist Stage-2 dossier (`hl_srg_resolver_stage2`) → `hl_srg_past_clients.dossier_json`.
2. **Per-client reconcile:** `triangulate-resolver.mjs` (Stage-1 $0, unions sources) → `triangulate-stage2.mjs`
   (Haiku dossier) → adopt Adam's clean address/HLID/value by `website_hlid=mls_hlid` → `complete-closings.mjs`
   (--write) → `link-contacts-fub.mjs` (--write) → `persist-reconcile.mjs` (--write) into `hl_srg_pch_staging`.
3. **Prove 20** (Nations first, then Hutchens + the golds) — Zach walks the audit → `resolution-gate.mjs
   --signoff` each.
4. **PROMOTE** staging → live `hl_srg_past_client_homes` + parent (value-preserving; replaces stale 21/19).
5. **Scale** to all 1,780. Then `fub-publish.mjs --write` (refuses un-resolved) after Zach signs off process.

##### 🟢 IN SCOPE
- The two CRM tables (`hl_srg_past_clients`, `hl_srg_past_client_homes`) + their staging/quarantine.
- The dossier render in `card-note.mjs` (CRM) + persist to the person row.
- Adopting Adam's address/HLID/value spine (read-only consume; never re-derive the address).
- The audit surface where it reflects the reconciled card.

##### 🔴 OUT OF SCOPE (other lanes — do NOT touch)
- Adam's HOME side: address resolution, HLID minting/de-fork, the value engine, website `Listing` seating,
  the off-market/cancelled RENDER pill (Adam's open front-end item). It is DONE + signed off — consume it.
- Website `Listing` writes (Rule #0, Lee/Adam lane).
- FUB address updates / referral-attribution (Benji/Zach).
- The 22 flagged out-of-area/bad-source addresses (`repin-crm-flagged-2026-07-03.txt`) — do NOT force-resolve;
  mark investigation/out-of-area. (e.g. CHICAGO MO, PHILADELPHIA 16601, HART TOWNSHIP.)

##### 🧱 THE TABLES + GRAIN (the contract)
- `hl_srg_past_clients` — 1 row / person·household. Grain unchanged; fill dossier_json + confirm bucket + link.
- `hl_srg_past_client_homes` — 1 row / (client × home-transaction). Multiple transactions at one address ALL
  carry the SAME canonical HLID (Adam's re-pin ensured 1,912 distinct HLIDs over 2,168 rows). The card groups
  them under one address block. Fill `determined_agent`, `determined_agent_source`, `disposition`,
  `disposition_source`, adopt address/HLID/URL/value.

##### 🔒 GATES / LAWS
- Rule #0: the full child PROMOTE write + any `Listing` write = TYPED Zach approval naming the table.
- Haiku only in the resolve/dossier path (`claude-haiku-4-5-20251001`, key `ANTHROPIC_API_KEY_HOUSELOOP`).
- "Done" = data verified in the table by query + rendered correctly in Chrome. Prove 20 before scale.
- No auto-publish to FUB. Reconcile, don't rebuild. The table is the truth.

##### 📎 KEY INPUTS
- Adam's spine: `.claude/reports/pastclient-resolved-EXPORT-2026-07-03.csv` (1,951; key `mls_hlid`+`hlid`) ·
  WEB VIEW `hl_pastclient_resolved` · CRM `hl_srg_pastclient_gt` · flagged `repin-crm-flagged-2026-07-03.txt`.
- Sources: SISU (`sisu_transactions`) · MLS (`hl_srg_mls_deals`, `hl_mls_property_history`) · dotloop
  (`hl_srg_loop`) · brain (`hl_fub_person_facts`) · prior (`hl_srg_resolver_quarantine`).

---

## Handoff · `.claude/state/caleb-handoff.md`

#### CALEB HANDOFF — SRG PAST-CLIENT RECONCILIATION → THE TWO PERFECT TABLES
**Written 2026-07-03 for a RESTART ONTO A NEW MODEL (Fable 5) · Branch `houseloop-app` · Tree `/Users/zachspradling/Code/BossLoop-Gold`**
**You wake up cold. Read this TOP-TO-BOTTOM. It is self-contained + execution-ready. The scope-lock PRD
(`.claude/state/session-scope-pastclient-reconciliation-promote.lock`) is your build contract; the master
doc (`docs-site/guides/srg-reconciliation-engine.mdx`) is the deep reference. This file is the fast path.**

---

##### 0. WHO YOU ARE + THE ONE JOB + THE END GOAL
You are **Caleb** (persona `.claude/personas/caleb.md`; voice `.claude/SOUL_LOCK.md` — hype, plain-English,
"brotha/dude", never "king"). Zach is CEO; Lee assigns; you BUILD. Zach is not a developer — explain simply.

**THE END GOAL (Zach LOCKED 2026-07-03 — this is what "done" is):** run the reconciliation engine you built
across ALL the data (yours + Adam's, now complete) and produce **TWO PERFECT TABLES:**

1. **`hl_srg_past_clients`** (parent) — ONE perfect row per past-client person/household: name + household
   (deduped, spouse merged) + `bucket` + the **relationship dossier (2 paragraphs + the extracted facts
   matrix)** + the pointer to their home(s). Perfect = every field from its canonical source, nothing
   placeholder, nothing stale.
2. **`hl_srg_past_client_homes`** (child) — the perfect home/transaction table: **everything under ONE
   address — ONE canonical HLID per physical home, with the client's MULTIPLE transactions (buy, sell,
   lost) grouped under that one address** — each transaction carrying its **agent-of-record** (even a
   departed teammate), the home's **disposition** (owns / moved / lost purchase / lost listing), the
   **correct address + HLID + value** (from Adam's signed-off spine), so the card renders one home block
   with its deals beneath it.

**Why:** so Zach can send every past client a dead-on **HouseLoop Home Value Report** (the end state —
`guides/home-value-report`). *"I have to have perfect data in order to get to this."*

**THE DOCTRINE (Zach said it 50×):** "Accuracy is the only benchmark." Every field from EXACTLY ONE
canonical source. **"The table is the truth"** — every number from ONE live SQL query, never a JSON file,
never memory, never re-derived twice. **"Done" = correct data verified IN the table by query** (and, per
Adam's hard-won law, **verified rendering in Chrome with real eyes** for anything that shows on a card) —
never "the script exited 0." **Prove the first 20 (Nations first) → Zach signs off → THEN scale. No
auto-publish to FUB.**

---

##### 0.5 THE DOCS + HOW TO PUBLISH (two docs; nothing is live until you mirror)
- **GROUND TRUTH = `docs-site/guides/srg-reconciliation-engine.mdx`** ("SRG Past Client Reconciliation —
  Master"). Architecture, laws, table map, tool catalog, card + dossier, audit UI, honest state, running log.
- **WIP LOG = `.claude/state/wip/caleb/resolver.md`** → auto-generates `docs-site/wip/caleb-resolver.mdx`.
  Status/checklist ONLY; points at the master. Progress % counts `- [x]` ÷ (`[x]`+`[ ]`); **`- [~]` is NOT
  counted** (reads 100%) — use `[ ]` for open.
- **SCOPE-LOCK PRD = `.claude/state/session-scope-pastclient-reconciliation-promote.lock`** — your build
  contract (goal, in/out scope, exact steps, acceptance criteria).

**PUBLISHING (Mintlify silently goes stale — the mirror aborts on ANY MDX error):**
```bash
DEPLOY_AGENT=caleb node tools/docs/mirror-docs.mjs --dry   # preview + see reconcile errors
DEPLOY_AGENT=caleb node tools/docs/mirror-docs.mjs         # regen WIP + reconcile + push → Mintlify
```
Fix listed MDX errors first (escape bare `<x>` → `&lt;x&gt;`; strip leaked `</content>`/`</invoke>` tool
tags; blank-line `<Warning>`/`<Note>` around block children), then re-run. Live: houseloop.mintlify.app.

---

##### 1. ADAM'S LANE IS DONE + SIGNED OFF — the clean spine you build ON (do NOT redo it)
Adam owns the HOME side (address, HLID, `/home` URL, value). **As of 2026-07-03 late marathon it is
COMPLETE and Zach-signed.** You now have every address. What he delivered:

- **THE CLEAN RESOLVED SPINE (join against THIS):** `.claude/reports/pastclient-resolved-EXPORT-2026-07-03.csv`
  — **1,951 rows**, cols: `mls_hlid` (= your `website_hlid` = `Listing.hlid` = `hl_heartlandmls_<mls>`) ·
  `hlid` (HLID) · `address` · `city` · `state` · `zip` · `houseloop_url` (`/home/<slug>/<HLID>`) · `hl_value`
  · `status` · `value_bucket`. **Also the WEB VIEW `hl_pastclient_resolved`** (centerbeam, 1,951/1,951 HLID)
  and the CRM GT table `hl_srg_pastclient_gt` (1,937). **JOIN by `website_hlid = mls_hlid`, fallback
  `identity_key` — NEVER a stored hlid (poison).**
- **Addresses resolved:** MLS#-anchored (the MLS# overrides the typed source address every time —
  12673 S Hallet not 13673; 7224 Widmer Rd not 72245; 105 E Washington Paola KS not Attica IN). 1,020
  MLS-locked + 833 address-match + stragglers + hand-verified.
- **Value engine LOCKED (Zach 2026-07-03):** TWO monthly engines + Active — ① Past-Client·Ninja Monthly
  (`Zestimate × 1.005`, past-clients only) · ② Sold·ZHVI Monthly (`sold price × ZHVI[now]/ZHVI[soldMonth]`,
  the default for all types incl. land) · Active (`list × 1.005`, flips on close). Land is NOT held flat.
  Only anchorless organic new-builds are held flat. Fork ORDER fix shipped (appreciation before stored
  fallback; `FLAT_BUG=0`). Detail: `guides/houseloop-home-value`.
- **63 wrong-house links re-pinned** — `hl_srg_past_client_homes.website_hlid` had 87 fuzzy-collision
  mis-pins (336 S 78th → 336 S 42nd, etc.); re-pinned collision-safe via `identityKey`
  (`repin-crm-master.mts`), reversal logged. **22 out-of-area / bad-source edge cases flagged:**
  `.claude/reports/repin-crm-flagged-2026-07-03.txt` (e.g. CHICAGO MO, PHILADELPHIA 16601, HART TOWNSHIP —
  genuinely garbled source addresses; do not force-resolve, treat as investigation/out-of-area).
- **The 28 "no-sold-data" homes ALL resolved** (SISU → dotloop → Zillow → Redfin → settlement statement);
  Cancelled/Expired/Withdrawn listings are correctly **VALUE-ONLY** cards (we were the listing agent, it
  never sold, the client still owns it — the relationship is the reactivation asset). 9 remain value-only
  (never sold = correct).
- Adam's WIP: `wip/adam-pastclient-address-resolver`. His open items are RENDER/front-end + FUB (his lane),
  not your data build.

**⚠️ Adam's re-pin touched the `website_hlid` POINTER only (address/HLID/value). It did NOT fill
`determined_agent`/`disposition` — those are still yours to reconcile (they read 21/19, stale).** Confirm
his EXPORT join key with him if unsure (asked in msg #11301: pointer-only? keyed by mls_hlid).

---

##### 2. YOUR LANE — the honest live state (2026-07-03, queried)
`hl_srg_past_clients`: **1,780** clients · **13** resolved (`houseloop_resolved_at`) · **481** dossier_json ·
bucket: team 1,251 / ana-only 434 / ana-handoff 82 / agent-dnc 12 / internal 1.
`hl_srg_past_client_homes`: **2,168** rows · **1,912 DISTINCT `website_hlid`** (unique homes) · role buy
1,596 / sell 572 · **`determined_agent` on only 21 · `disposition` on only 19** (STALE — the resolver
output was never PROMOTED). Snapshotted in `hl_srg_pch_quarantine` (undo).
Dossier engine: `hl_srg_resolver_stage2` = 26 dossiers staged · `hl_srg_pch_staging` = 81 · GT = 1,937.

**Read:** the ~2,168 rows over 1,912 homes already means "multiple transactions under one address" exists
as data (buy+sell share a home). The gap is: fill agent + disposition per row, adopt Adam's clean
address/HLID, generate the 2-paragraph dossier per client, then PROMOTE staging → the live tables.

---

##### 3. THE EXECUTION PLAN — build the two perfect tables (the whole job, ordered)
**Full contract + acceptance criteria: the scope-lock PRD. This is the ordered summary.**

**A. WIRE THE DOSSIER RENDER + PERSIST (mechanical, do first).**
- `apps/houseloop-crm/lib/pastclient/card-note.mjs`: render `personality_paragraph` + `transaction_paragraph`
  + the 2-col `dossier_facts` matrix at the BOTTOM of the card (reads `dossier_json`).
- Persist Stage-2's dossier from `hl_srg_resolver_stage2` → `hl_srg_past_clients.dossier_json`.

**B. RUN THE RECONCILE ENGINE PER CLIENT (the loop unit — all gated, DRY-by-default, reversible):**
1. `triangulate-resolver.mjs <ids>` — Stage-1, deterministic $0: unions the 5 harvest tables + roster +
   Adam's GT/EXPORT → transactions (dedup rooftop+side) → agent waterfall → tiers G/Y/R.
2. `triangulate-stage2.mjs <ids>` — Stage-2 **Haiku**: reconcile conflicts + write the 2-paragraph dossier
   + facts → `hl_srg_resolver_stage2`.
3. **Adopt Adam's clean home data** — join each home by `website_hlid = mls_hlid` (EXPORT / VIEW) → take the
   correct address, HLID, `/home` URL, value, value_bucket. NEVER re-derive the address.
4. `complete-closings.mjs <pc> --write` — materialize each SISU/lost closing as its own child row under the
   right HLID (buy / our-sell / lost-sell).
5. `link-contacts-fub.mjs <pc> --write` — resolve each household member's FUB id onto `dossier_json.contacts`.
6. `persist-reconcile.mjs <ids> --write` — stage into `hl_srg_pch_staging` (the "Reconciled" audit tab).

**C. PROVE 20 → SIGN OFF → PROMOTE:**
7. Zach walks the 20 on `/settings/seller-report/audit` (the "Follow-Up Boss Notes" section = the full card).
8. `resolution-gate.mjs <pc> --signoff` — the deterministic bar; stamps `houseloop_resolved_at` (refuses a
   FAIL). **The ONLY way a card becomes "resolved" — CLI, no UI button.**
9. **PROMOTE** staging → the live child `hl_srg_past_client_homes` (value-preserving; REPLACES the stale
   21/19). **The full child write = Rule #0 TYPED ZACH GO** naming the table.
10. **SCALE** the same motion to all 1,780. Then `fub-publish.mjs <pc> --write` (refuses un-resolved) —
    only after Zach signs off the process.

**Acceptance ("perfect"), per client:** person + household deduped · every transaction under ONE HLID with
its agent-of-record (waterfall, never a placeholder) · disposition set · address = Adam's clean spine · value
from the fork · dossier = 2 paragraphs + facts matrix · renders correctly on the audit card. Verified by
query AND by eye on the card.

---

##### 4. THE PER-FIELD SOURCE LAW (memorize — where accuracy lives)
| Field | THE canonical source | NEVER |
|---|---|---|
| A deal exists | UNION of all sources (SISU ∪ resolver_block ∪ child ∪ MLS ∪ dotloop ∪ brain), dedup rooftop+side | — |
| **Agent of record** (waterfall) | (1) **SISU `agent`** (client first+last + price ±$3k + date ±20d) → (2) **Repliers/MLS our-side** → (3) Ninja → (4) **dotloop** licensee → (5) **FUB `Team Exit -` tag** → else NOT resolved | ❌ `closings.agent`/`is_ana`; ❌ MLS *listing* agent on a deal we BOUGHT; ❌ **Ben Froeschl** (sales mgr); ❌ "SRG"/placeholder. **Keep the departed agent — the relationship IS the asset.** |
| Bucket | the SISU-derived **`bucket` COLUMN** (team 1,251 · ana-only 434 · ana-handoff 82 · agent-dnc 12 · internal 1) | the `is_ana` flag |
| Price / date | SISU (MLS agrees) — the deal's `original_price` (historical, NOT current value) | — |
| **Address / HLID / URL / value** | **Adam's EXPORT / `hl_pastclient_resolved` VIEW** by `website_hlid = mls_hlid` → `identity_key` | raw SISU verbatim; a stored hlid/slug (poison); re-deriving the address |
| Current home / disposition | County DEED (`resolved_current_home`); competitor-bought → RED lost purchase | inferring from one Repliers sale |
| Dossier | brain (`hl_fub_person_facts`) + FUB Railway timeline + resolver_block.summary | — |

**⚠️ SIERRA-INTERACTIVE FIX (dossier):** SRG ran on Sierra Interactive until late 2023; transactions moved to
SISU but early COMMS didn't. FUB comms are only the ~2024+ slice — the dossier must NOT treat the FUB
first-touch as "when we first connected." Triangulate the TRANSACTION CADENCE as the relationship spine.

---

##### 5. DB CONNECTION PATTERN + STATUS SNAPSHOT
| DB | Env var | Host | SSL |
|---|---|---|---|
| **CRM** (people/deals/harvest — your tables) | `HL_CRM_DATABASE_URL` | autorack.proxy.rlwy.net | `{rejectUnauthorized:false}` |
| **WEB** (`Listing`, `hl_property`, `hl_pastclient_resolved`) | **`HOUSELOOP_DATABASE_URL`** | centerbeam.proxy.rlwy.net | on. **NOT `DATABASE_URL`** |
| **FUB mirror (dev)** | `DATABASE_URL` | 192.168.1.211 (Camp David) | **NO SSL** |
| **FUB mirror (Railway)** | `FUB_RAILWAY_DATABASE_URL` | yamabiko.proxy.rlwy.net | on |
```bash
cd /Users/zachspradling/Code/BossLoop-Gold
node -e 'import("module").then(async ({createRequire})=>{const require=createRequire(new URL("file:///Users/zachspradling/Code/BossLoop-Gold/apps/houseloop-crm/package.json"));const pg=require("pg");const fs=require("fs");const e=k=>{const m=fs.readFileSync("/Users/zachspradling/Code/BossLoop-Gold/.env","utf8").match(new RegExp("^"+k+"=(.*)$","m"));return m?m[1].trim().replace(/^["\x27]|["\x27]$/g,""):null;};const c=new pg.Client({connectionString:e("HL_CRM_DATABASE_URL"),ssl:{rejectUnauthorized:false}});await c.connect();for(const q of [["clients","SELECT count(*) n FROM hl_srg_past_clients"],["resolved","SELECT count(*) n FROM hl_srg_past_clients WHERE houseloop_resolved_at IS NOT NULL"],["child agent","SELECT count(*) n FROM hl_srg_past_client_homes WHERE determined_agent IS NOT NULL"],["child disp","SELECT count(*) n FROM hl_srg_past_client_homes WHERE disposition IS NOT NULL"],["stage2","SELECT count(*) n FROM hl_srg_resolver_stage2"]]) console.log(q[0],(await c.query(q[1])).rows[0].n);await c.end();});' 2>&1 | grep -v Warning
```

##### 6. LAWS THAT BURNED US (don't relearn the hard way)
- **Rule #0:** no destructive SQL / no `Listing` merge-delete without TYPED Zach approval naming the table.
  `ADD COLUMN`/`CREATE TABLE`/`CREATE VIEW`/`UPDATE` are fine. The full child PROMOTE write = Rule #0.
- **Reconcile, don't rebuild** — the prior resolver_block (1,458) + old child are the RICHEST inputs; union
  them; never re-derive from a thin SISU-spine (that regressed Nations to 3 txns, no links).
- **The address is the home key** — adopt Adam's clean spine; NEVER fuzzy name-match, NEVER a stored
  hlid/slug (poison). The MLS# overrides the typed address.
- **Agent from SISU, bucket from the `bucket` column** — never `closings.agent`/`is_ana`. Ben Froeschl NEVER.
  Zach→Ana. Keep the departed agent.
- **Render with eyes (Adam's law):** if Zach can't SEE it render right in Chrome, it's not done. Screenshot.
- **"Done" = data verified in the table.** Prove 20 before scale. No auto-publish to FUB.
- **One tree, one branch:** `/Users/zachspradling/Code/BossLoop-Gold` on `houseloop-app`; per-file
  auto-commit; `git pull --rebase` before push. **Docs (`docs-site/`, `.claude/state`) are NOT CRM app
  files — safe from the :3020 HMR freeze; but don't `git add .claude/state`.**

##### 7. CANONICAL TEST CASES (verify ANY resolver/card change against these)
- **Nations (pc1)** — 5 deals with us (all Ana, each a `/home` link: 23309 W 45th Terrace buy+sell, 816
  Saddlewood buy, 801 Poplar buy, 301 E 68th Terrace buy+sell) + LOST 🔴 **7325 Oakview St, Shawnee KS
  66216 — bought through a competitor**, buyer agent Audrie King (manual override — MLS never recorded the
  sale). If the resolver produces only 3 txns / no links, you regressed to the thin spine.
- **Jennifer Hutchens (pc4)** — ~2,000 texts; dossier must be genuinely personal (divorce life-transition,
  ice-cream-sundae detail, departed agent April Atkinson as the warm reference). Bar: "God-tier."

##### 8. FILE MAP
**Docs:** master `docs-site/guides/srg-reconciliation-engine.mdx` · WIP `.claude/state/wip/caleb/resolver.md`
· scope-lock `.claude/state/session-scope-pastclient-reconciliation-promote.lock` · PRD
`GT/GT - Pending/GT_SRG_RECONCILIATION_ENGINE_WIP.md` · doctrine `.claude/rules/HOUSELOOP_DATA_DOCTRINE.md`.
**Adam's deliverables:** `.claude/reports/pastclient-resolved-EXPORT-2026-07-03.csv` (the spine) ·
`repin-crm-flagged-2026-07-03.txt` (22 flagged) · `repin-crm-master-reversal-2026-07-03.json` (undo) ·
WEB VIEW `hl_pastclient_resolved` · CRM `hl_srg_pastclient_gt`.
**Tools (`tools/houseloop-cards/`):** `triangulate-resolver.mjs` · `triangulate-stage2.mjs` (dossier) ·
`complete-closings.mjs` · `link-contacts-fub.mjs` · `persist-reconcile.mjs` · `resolution-gate.mjs`
(`--signoff`) · `fub-publish.mjs` (`--write`) · `build-resolution-table.mjs` (Adam's VIEW).
**Card builder:** `apps/houseloop-crm/lib/pastclient/card-note.mjs`. **Audit UI:**
`app/api/settings/seller-report/audit/route.ts` + `components/seller-report/AuditReport.tsx`.
**Key tables (CRM):** `hl_srg_past_clients` · `hl_srg_past_client_homes` · `hl_srg_pastclient_gt` ·
`hl_srg_resolver_stage2` / `_pch_staging` / `_resolver_quarantine` / `_pch_quarantine` · `sisu_transactions`
· `hl_mls_property_history` · `hl_srg_mls_deals` · `hl_srg_loop` · `hl_fub_person_facts`. **(WEB):** `Listing`
· `hl_property` · `hl_pastclient_resolved` (VIEW).

**LAST WORD:** the data war is WON across both lanes — Adam's addresses/HLIDs/values are signed off, your
sources are all landed. The remaining job is not building the pipeline, it's RUNNING it: dossier render →
reconcile per client (adopting Adam's clean spine) → prove 20 → Zach sign-off → PROMOTE into the two perfect
tables → scale to 1,780. Don't rebuild what's built. Don't triangulate dotloop from SISU. Prove 20 before
scaling. Accuracy is the only benchmark. The table is the truth. Render with eyes.

## Related documentation

Doc sections this work creates or updates:

- [`guides/srg-reconciliation-engine`](/guides/srg-reconciliation-engine)
