---
title: "Caleb (SRG Past Client Reconciliation)"
description: "Work-in-progress dossier — Caleb · Data intel · past-client resolver. SRG Past Client Reconciliation."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/caleb/resolver.md` on every push. **Progress: 4/4 done (100%).** _Updated 2026-07-02 PM — dossier LOCKED._</Info>

**Owner:** Caleb — Data intel · past-client resolver

## Checklist

**The ONE master doc is the Mintlify guide → `docs-site/guides/srg-reconciliation-engine.mdx`
("calebwip — Spradling Realty Group Past Client Reconciliation").** Architecture, canonical-source-per-field law,
live table map, card format, 3-tab Audit UI, Nations reference, fuck-up log all live there.

### ✅ MASTER CHECKLIST — the ONE, live
_Current as of the latest amendment below._

- [x] **1 — Dedup** — 54 merges (1,834 → 1,780), reversible (`hl_srg_dedupe_log`).
- [x] **2 — Brain** (`hl_fub_person_facts`) — 2,178/2,178 household members.
- [x] **3 — MLS rooftop** (`hl_mls_property_history`) — 100%; scraper now captures the **buyer-side S Agent** + stores all **151 cols** in `raw`.
- [x] **4 — HLID on the MLS table** — **re-stamped from the web authority `hl_property`** (unit-aware `identityKey`); cross-city collapse **221 → 0**; `canonical_slug` stamped so the table carries the full `/home/<slug>/<HLID>` URL.
- [~] **5 — Dotloop `.loop`** — 86.9% agent / 88.7% address on the 2,128 real closings.
- [~] **6 — Reconcile engine** — ACTIVE, most pieces DONE. HLID-anchored resolver + S-Agent owns/lost-buy + de-fork (37) + `not_srg` (6 pulled) + **GT canonical-address adoption (Adam's CASS table by HLID)** + **🔒 relationship dossier LOCKED** (2 paragraphs + facts matrix, Sierra-Interactive gap handled). STILL TO WIRE: `card-note.mjs` render of the dossier + persist dossier to person row + persist homes to child.
- [~] **7 — Prove first 20** — dossier proven God-tier on Nations + Jennifer Hutchens; 20 homes staged. NEXT: run dossier on all 20 → render on card → Zach signs off → PROMOTE to child (replaces the stale 591/535) → scale to 1,780.

### 📝 AMENDMENTS (newest → oldest)

**Amended 2026-07-02 PM — 🔒 RELATIONSHIP DOSSIER LOCKED (Zach "God-tier") + GT canonical-address adoption + honest child-table status.**

- **🔒 THE RELATIONSHIP DOSSIER IS LOCKED (Zach 2026-07-02).** Built into `triangulate-stage2.mjs` (Haiku one-shot,
  `claude-haiku-4-5-20251001`, `ANTHROPIC_API_KEY_HOUSELOOP`, ~$0.011/client → ~$23 for all 1,780). The card's dossier
  (bottom of the card) = **TWO PARAGRAPHS + a FACTS MATRIX:**
  1. **`personality_paragraph`** — the RELATIONSHIP STORY (who they are + key moments): lead-in date+source, first contact,
     communication style, and 1-2 REAL moments quoted from their FUB texts/notes. Pulls from a NEW **FUB Railway timeline**
     leg (`fub.people.fub_created` + `text_messages`/`notes`/`calls` by `person_id`, household = client fub_id + spouse
     fub_identity).
  2. **`transaction_paragraph`** — the deals + agent (former teammate, past tense) + current home, LEADING with the
     lost-listing reactivation headline.
  3. **`dossier_facts`** — a deterministic 2-col matrix grouped from `myli_facts` by domain →
     👨‍👩‍👧 Family · 📋 Logistics · 📍 Current Situation · 💬 Communication · 💵 Financial Motivation (no extra LLM cost).
  - **⚠ THE SIERRA-INTERACTIVE FIX (locked):** SRG ran on Sierra Interactive until **late 2023**; transactions migrated to
    SISU but the **early COMMS never did**. So the FUB comms are only the recent (≈2024+) slice — the dossier must NOT treat
    the FUB first-touch as "when we first connected." The packet flags this + the prompt **triangulates the TRANSACTION
    CADENCE as the relationship spine** (came in &lt;date&gt; via &lt;source&gt; → first deal shortly after → later deals), weaving comms
    in as they fit. Fixed the "didn't connect until April 2024" bug on Nations.
  - **PROVEN:** Nations (pc1, investor fix-and-flip + lost listing) and **Jennifer Hutchens (pc4, ~2,000 texts)** — the
    enriched one produced a genuinely personal dossier (divorce life-transition, ice-cream-sundae detail, departed agent
    April Atkinson kept as the warm relationship). Zach: "God-tier, lock it in."
  - **STILL TO WIRE (mechanical):** render the 2 paragraphs + the matrix at the BOTTOM of the card in `card-note.mjs`;
    persist the dossier to the PERSON row (`hl_srg_past_clients.dossier_json`). Stage2 currently writes
    `hl_srg_resolver_stage2` (staging).

- **📍 GT CANONICAL-ADDRESS ADOPTION (address reconciliation, with Adam).** Root cause of "wrong address on the website" =
  the **MLS source itself is dirty** (agent-typed garbage: wrong zip, double-suffix street). Example 10908:
  `10908 E 35th St. S Street, Independence, 66052` (66052 = rural KS!) → correct `10908 E 35TH ST S, Independence, MO 64052`.
  Adam built the ONE canonical **CASS/USPS-validated GT table** (`.claude/reports/pastclient-canonical-gt-2026-07-02.csv`,
  1,936 homes; loaded to CRM table **`hl_srg_pastclient_gt`**). The resolver now **ADOPTS Adam's correct address + slug +
  `/home` URL + `zestimate_status` by HLID** (final pass in `assemble()`; join `child.hl_property_id = gt.hlid`, 2,166/2,168
  match). Raw `hl_srg_mls_deals` stays dirty-but-true provenance; child adopts clean; website reads clean child. **When Adam
  ships his de-forked canonical list, it's a drop-in: reload `hl_srg_pastclient_gt` + re-run — no code change.** State
  backfilled from zip (63/64/65→MO, 66/67→KS) when CASS leaves it blank.

- **⚠ HONEST CHILD-TABLE STATUS — NOT cleaned yet.** The live child `hl_srg_past_client_homes` STILL holds the **STALE 591
  disposition / 535 agent** from the 2026-06-30 ad-hoc-writer era (sediment from ~15 different scripts). It is safely
  snapshotted in `hl_srg_pch_quarantine` (2,168 rows, the undo). The cleanup = the **PROMOTE step** (rebuild the child from
  the resolver per client → sign-off → scale), which has NOT run. What IS done is everything that FEEDS a correct rebuild:
  clean addresses (GT), de-forked HLIDs (37), `not_srg` (6 non-ours pulled + filtered), the dossier engine.

- **WHERE WE'RE GOING (the finish line for the 20 → 1,780):**
  1. Run `triangulate-stage2.mjs` (dossier) on all 20 → staging.
  2. Wire `card-note.mjs` to render the 2-paragraph dossier + facts matrix; persist dossier to the person row.
  3. Persist the 20 homes with the GT-clean address into staging (Reconciled tab).
  4. Zach walks the 20 on the Audit → signs off the PROCESS.
  5. **PROMOTE** staging → child (value-preserving, per Adam's value-columns handoff) — this REPLACES the stale 591/535.
  6. Scale the same motion to all 1,780 (Rule #0 typed go for the full child write).

**Amended 2026-07-02 AM — HLID DE-FORK (home identity cleaned) + enrollment gap found + not_srg wired.**
- **HLID directional/suffix de-fork on our past-client MLS-history.** One physical home was landing under TWO HouseLoop IDs
  because the MLS wrote the address two ways (`928 NE 87th Terrace` = HLID101864 vs `928 87th Terrace` = HLID255314, 1 m
  apart), which split a home's transactions across both IDs and broke disposition (buy under one, later sale under the other
  → card read "owns" instead of "lost listing"). **37 real same-rooftop forks** consolidated onto their keeper (reversible via
  `hl_mls_property_history.hl_property_id_old2`, `hlid_source='defork'`). First pass over-merged to a false 113 using a
  self-referential distance gate — corrected to a keeper→loser ≤30 m gate. **928 (Mettman/pc5) now correctly flips to a
  lost listing** (sold 2023-08-29 $295k by Benjamin Lytle, without us).
- **The identity doctrine (locked):** same-home = a **shared MLS number** across two HLIDs (bulletproof; coords are geocoded
  off the address so they lie on a bad suffix — coords only a ≤30 m tiebreaker). "Is it OUR home" = a **Spradling roster agent
  on any transaction** (7600 133rd St had Ana; the Terrace twin had Keller Williams → different house). That agent test took
  the fork list 37 → 31 confirmed; **6 non-Spradling rooftops** (17201 E 32nd, three 2029 Wyandotte units, 611 N 8th, 518 E
  Cedar) were pulled out of past-client attribution (`not_srg=true`, 19 rows, reversible) — NOT deleted.
- **not_srg wired into the resolver** — all 5 `hl_mls_property_history` reads filter `NOT COALESCE(not_srg,false)`, so the 6
  can never resurface. Verified no regression on Mettman.
- **Enrollment gap found (handed to Adam, web lane):** the CRM master `hl_srg_past_client_homes` (SSOT) and the web worklist
  `hl_past_client_homes` (what the value/history Inngest jobs read) DON'T line up — **141 real-HLID homes not enrolled** (928
  was one → stale $215k value). Deeper: **`last_cached_at` NULL on ALL 1,836** worklist rows (value job never ran). Handed Adam
  the real-HLID bucket (`.claude/reports/pastclient-homes-REAL-HLID-2026-07-02.csv`, 2,166 rows / 1,936 homes). Adam fixed 928
  live (~$338,866 + 175 mo history). **NO CRM dedup** — the 143 "duplicate" rows are legit multi-closing rows (distinct
  `closing_key`), deleting would destroy transaction history.
- **STATE NOW:** 1,780 clients · 2,166 real-HLID homes · value coverage ~92% · **but disposition filled on only 591/2,168 and
  a determined agent on only 535.** Engine proven on ~23 staged clients. **Next lever: full 1,780 run → staging → Zach sign-off
  → promote.**

**Amended 2026-07-01 ~11:15 PM EDT — Lost-buy card PLACEMENT fixed + RELATIONSHIP DOSSIER design LOCKED (Zach).**
- LOST BUY now renders under **LOST PROPERTIES** as red "OWNS" (not under CURRENT) — `🔴 OWNS 7325 Oakview St $620,000 06/12/2026 — Audrie King (bought without us)`. `card-note.mjs`: `lostBuys` → `lost` array (kind 'OWNS'); `current` filtered to exclude `lostBuyKeys` (no double-render). Verified on Nations.
- **RESOLUTION MATRIX (pc 1–23)** re-staged through the wired resolver into `hl_srg_pch_staging` — these are the MANUALLY-done set Zach still VERIFIES (not signed off). Real `hl_srg_past_client_homes` UNTOUCHED. 2 lost buys (Nations, Franzon), 10 lost listings.
- **DATA MAP (Zach): source tables = SISU + hl_srg_mls_deals + hl_mls_property_history (+ S Agent) + resolved_current_home. We do NOT need county deed for the ~1,312 missing — current home = the home we sold them that never re-sold (from our data) + soft messaging. Fixed the numbered-street search (Matrix wants "28" not "28th") + multi-word SSH quoting in scrape-deed-homes.mjs.**
- **🔒 RELATIONSHIP DOSSIER — DESIGN LOCKED, NOT YET BUILT → PRD: `GT/GT - Pending/GT_RELATIONSHIP_DOSSIER_PRD.md` (the next build).** the card's dossier must be a **genuinely written, personality-driven relationship paragraph** (unique human details + evidence quotes + an actionable angle — NOT the "did 5 deals with Ana" transaction trend), synthesized by a focused **Haiku pass** over each household member's `hl_fub_person_facts.facts` (+ `evidence`). UNDER it, a **compact 2-column table** of the categories Zach picked: **👨‍👩‍👧 Family · 📋 Logistics · 📍 Current Situation · 💬 Communication · 💵 Financial Motivation**. TWO renderings from one source: card = paragraph + 2-col table; Myli's KB = paragraph + labeled bullets. Facts link by `fub_person_id` (past_client_id is NULL on all 5,694 rows) → pull the household by the client's fub_ids. `/tmp/reldossier.mjs` is a working prototype of the grouped output.

**Amended 2026-07-01 ~7:15 PM EDT — LOST BUY wired end-to-end (resolver → persist → card) + Nations manual override.**
- `triangulate-resolver.mjs` §4c now classifies the current home deterministically: OWNS (SISU/closings match, streetKey collapses arborridge=arboridge so our own deals aren't mislabeled) vs LOST BUY (competitor on the MLS S Agent) vs OWNS-RECOVERED (S Agent=Spradling → ours, SISU missed it). **DATE-GUARD** drops any sale &gt;2yr older than the client's own history (kills the false Sandra-Kenney-2019 pick).
- **NATIONS MANUAL OVERRIDE (Zach-directed, unresolvable case):** 7325 Oakview's sale was never recorded in the MLS (listing agent Terrell Walls never converted the Active listing). Hardcoded in `MANUAL_OVERRIDES[1]`: LOST BUY, buyer agent = **Audrie King** (the agent on their OWN lost listing, NOT Terrell Walls), date = **2026-06-12** (their 68th Terrace close), price = **$620,000** (list price), URL = `/home/7325-oakview-street-shawnee-ks-66216/HLID101829`.
- `persist-reconcile.mjs` writes lost_buys as child rows (`disposition='lost_buy'`). `card-note.mjs` renders them RED under CURRENT PROPERTIES with the competitor agent + price + date, and EXCLUDES them from "TRANSACTIONS WITH US". **Nations' card verified correct**: 5 deals with us · owns 801 Poplar + 816 Saddlewood + 7325 Oakview (lost buy) · lost listing 301 E 68th Terrace.
- **NEXT:** run the 20 (+ the 28 lost buys) through the wired resolver → stage → spot-check; write back the OWNS deals the MLS recovered that SISU lacks (Earl Wilson, Burchfield, Burdge, Byers); fix numbered-street/MLS# search for the 15 empties; distill current-home for the other ~990.

**Amended 2026-07-01 ~6:35 PM EDT — S-AGENT DISCRIMINATOR = the deterministic owns/lost-buy classifier (Zach's "gold").**
- Ran all **468** deed current homes through the MLS buyer-side **S Agent**: **293 OWNS · 28 REAL LOST BUYS · 29 no-data** (`/tmp/classify.mjs`).
- **The MLS recovers deals SISU is missing** — Earl Wilson 8510 NE 108th, Burchfield, Burdge, Byers = ours via MLS S-Agent="Spradling Group", NOT in SISU.
- **28 real lost buys**, each a named competitor to reclaim (Nations→Sandra Kenney KW $372k; Franzon→Bryan Huff KW $1.1M; Calkins→Katie Adams $1.075M; Demore→Jacob Klenda KW $840k …). My earlier "36 / 30-no-MLS" was garbage — checked our table instead of the real MLS.
- Doctrine locked: **deterministic SISU+MLS = ground truth; the $200 Sonnet `resolver_block` is ONE source, never a verdict** (it falsely labeled our own deals "lost"). Built `tools/houseloop-cards/scrape-deed-homes.mjs` (scrapes deed homes from Matrix MLS; `--all` pulled 20/35). Known false-positives in the 28 (Chari "arborridge" vs "arboridge" spelling) → fix with the triangulation picker.

**Amended 2026-07-01 ~5:10 PM EDT — HLID RE-STAMP (the Chari cross-city bug) + resolver HLID-anchored.**
- `tools/houseloop-cards/restamp-mls-hlid.mts` re-stamps `hl_mls_property_history.hl_property_id` from `hl_property` (the website authority) by exact unit-aware `identityKey`; **HLIDs spanning &gt;1 zip: 221 → 0** (HLID100198 was on 3 cities → now only Chari's 5200 Sycamore Ave 64129). Reversible (`hl_property_id_old`, `--revert`).
- `triangulate-resolver.mjs`: home identity = OUR MLS# → identityKey → HLID → transactions under that HLID; **deleted the fuzzy `%street%` disposition** that invented Chari's false lost listings. Chari now GREEN by himself, 4 homes, real `/home/<slug>/<HLID>` links.
- Scraper STORAGE fix (Zach: "storage, not the scraper"): `tools/mls-scraper/mls-address-lookup.mjs` address-path now emits `sellingAgent` (S Agent) + returns all 151 cols; deployed to Camp David.

**Amended 2026-07-01 03:43 PM EDT — dedup committed (54 merges), MLS 100%, HLID linked, dotloop 87%, docs consolidated.**

### Next concrete action
**The identity foundation is clean (de-fork + not_srg done, 928 proven).** Next lever = the **full-scale run**:
`triangulate-resolver.mjs --all` → `persist-reconcile.mjs --all` (write to `hl_srg_pch_staging`) → Zach reviews the whole
2,168-home set on the Reconciled tab → sign-off → promote staging into the real child. That's what turns disposition from
591/2,168 and agent from 535 into a full card per client. Parallel (Adam, web lane): enroll the 141 un-enrolled real-HLID
homes + run the value/history Inngest job (cached 0/1,836). Deferred: Relationship Dossier build (PRD), card display-collapse
(one address → one home block).

---

## PRD · `GT/GT - Pending/GT_SRG_RECONCILIATION_ENGINE_WIP.md`

#### SRG RECONCILIATION ENGINE — WORK IN PROGRESS (Caleb + Zach)

**_Last amended: 2026-07-01 03:43 PM EDT_** — dedup COMMITTED (54 merges, 1,834→1,780) · MLS rooftop 100% · HLID linked on MLS table · dotloop `.loop` 87% on the 2,128 real closings · the 3 reconciliation docs consolidated into the ONE master Mintlify guide `guides/srg-reconciliation-engine` ("calebwip — Spradling Realty Group Past Client Reconciliation").

&gt; **This is the private canonical WIP. The full rich master (architecture, table map, quoted laws, card format, 3-tab Audit UI, Nations case, fuck-up log) is the Mintlify guide `guides/srg-reconciliation-engine`.** Rule: prove on the **FIRST 20 (Nations first)** before anything touches 1,780. "Done" = correct data VERIFIED IN THE TABLE by query — never "the script ran."

##### ✅ MASTER CHECKLIST — the ONE, live (updated 2026-07-01 03:43 PM EDT)

- [x] **Box 2 — Brain** (`hl_fub_person_facts`) — **2,178/2,178** past-client household members (Haiku dossiers). ✅
- [x] **Box 3 — MLS rooftop** (`hl_mls_property_history`) — **100% (1,955/1,955)**; **6,017 selling agents** (was 0%); all **151** MLS columns searchable; MLS#-direct Speed-Bar recovery closed the malformed-address tail. ✅
- [x] **Box 4 — HLID on the MLS table** — `hl_property_id` on **7,152 rows / 1,886 homes**. ✅
- [x] **Box 1 — Dedup** — **54 merges (1,834→1,780 cards)**, 0 orphans, reversible via `hl_srg_dedupe_log`. ✅ COMMITTED (was blocked on Rule #0; Zach gave the go).
- [~] **Box 5 — Dotloop `.loop`** (`hl_srg_loop`) — **86.9% agent / 88.7% address** on the **2,128 real closings** (the 871-gap `--real-closings` harvest is done; ~278 remain genuinely dotloop-empty → waterfall covers them). 🟢
- [ ] **Box 6 — RECONCILE ENGINE** — 🔴 **ACTIVE.** Rework `triangulate-resolver.mjs` to ingest ALL sources (esp. the 1,458 rich `resolver_block`s in `hl_srg_resolver_quarantine`) → union transactions → canonical-source-per-field → write the NEW child.
- [ ] **Box 7 — Prove first 20** — Nations first, then the golds → Zach signs off → scale to 1,780. No auto-publish to FUB.

---

##### ⭐ THE ARCHITECTURE (Zach LOCKED 2026-07-01)
The resolver is the brain. Every prior output — the old child + the **1,458 rich `resolver_block`s** — is a read-only SOURCE it ingests (NOT a thin SISU-spine, which regressed Nations). Union all transactions → canonical-source-per-field → write the NEW child. Prove Nations → sign-off → scale. Quarantines (`hl_srg_resolver_quarantine`, `hl_srg_pch_quarantine`) stay as provenance/undo.

##### 🔒 CANONICAL-SOURCE-PER-FIELD (Zach LOCKED 2026-07-01)
transaction = UNION of all sources (dedup by rooftop+side) · agent = SISU → dotloop licensee → FUB Team-Exit → investigation (Ben Froeschl NEVER; Zach→Ana; keep departed agent) · bucket = the SISU `bucket` column (Ana-Only 461) · price/date = SISU · address = MLS by mls_number · current home = county deed (competitor-bought → RED lost purchase) · lost listing = MLS later-sold-non-SRG (Bazalgette) · URL = child by HLID · dossier = brain + resolver_block.

##### 🗺️ WHERE EVERYTHING LIVES (live counts 2026-07-01)
**CRM (`HL_CRM_DATABASE_URL`, autorack):** `hl_srg_past_clients` 1,780 (13 resolved · 481 dossier · 1,743 HLID) · `hl_srg_past_client_homes` 2,168 (535 agent · 591 disp · 2,165 HLID/URL) · **`hl_srg_resolver_quarantine` 1,473 / 1,458 `resolver_block`** (988 resolved/358 needs_review/84 needs_agent_recovery/35 complete/8 dnc) · `hl_srg_pch_quarantine` 2,168 · `hl_srg_pch_staging` 50 · `hl_srg_dedupe_log` 54 · `sisu_transactions` 11,636 (1,945 Closed+44 Pending) · `hl_mls_property_history` 7,713 (7,152 HLID · 6,017 selling agents) · `hl_srg_mls_deals` 1,971 · `hl_srg_loop` 13,449 (2,128 real closings) · `hl_srg_dotloop_contracts` 13,257 · `hl_srg_dotloop_loops` 17,455 · `hl_fub_person_facts` 5,694 · `hl_fub_deals` 4,554 · `hl_fub_sisu_link` 4,442 · `hl_srg_agent_roster` 136.
**FUB mirror** (Camp David `192.168.1.211/bossloop` `DATABASE_URL` + Railway `yamabiko` `FUB_RAILWAY_DATABASE_URL`): `fub.*` — people 49,229 · texts 444,864 · notes 140,223 · calls 39,867 · events 146,292.
**Website** (`HOUSELOOP_DATABASE_URL`, centerbeam): `Listing` — one `/home/<slug>/<HLID>` per house.

##### 📍 NATIONS REFERENCE CASE (pc1 — what CORRECT looks like)
6 transactions with `/home` links (45th Terrace buy+sell, Saddlewood buy, 801 Poplar buy, 68th Terrace buy+sell — all Ana) + CURRENT = 🔴 7325 Oakview St, Shawnee — bought through a competitor (LOST PURCHASE, red), deed-confirmed. The thin resolver produced only 3 txns, no links → proved the union-all approach is correct.

##### THE FUCK-UP LOG + LESSONS (locked)
Fuck-ups: from-scratch resolver ignored s6's discoveries + wrote to a scrap table the audit never read · exact first-name match dropped SISU deals ("Kyle" vs "Kyle & Karly") · brain harvest on the wrong pool · silent-whiff "done." Lessons: the table is the truth (ONE source) · **reconcile, don't rebuild** · "done" = data verified in the table by query · match fuzzy (surname + first-token; households merge) · **prove on 20 before scale.**

##### NEXT CONCRETE ACTION (Box 6)
Rework `triangulate-resolver.mjs` assemble(): add the `resolver_block` + quarantined-child source legs → union transactions (dedup by rooftop+side) → agent waterfall → carry URLs + deed current home → prove on Nations (all 6 txns + 7325 Oakview red) → Zach sign-off → write new child → scale.

## Related documentation

Doc sections this work creates or updates:

- [`guides/srg-reconciliation-engine`](/guides/srg-reconciliation-engine)
