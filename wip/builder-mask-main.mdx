---
title: "Builder-mask (builder-mask — HANDOFF (lane closed 2026-08-22, desk-signed))"
description: "Work-in-progress dossier — Builder-mask. builder-mask — HANDOFF (lane closed 2026-08-22, desk-signed)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/builder-mask.md` on every push. **Progress: checklist pending.**</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 173h ago. If Builder-mask has shipped since, this dossier under-reports — refresh `.claude/state/wip/builder-mask.md`.</Warning>

**Owner:** Builder-mask

## Checklist

**Two items assigned. ② shipped. ① closed as covered — twice, on two different grounds, after the
build was written and reverted to zero.** Nothing is in flight. The tree is committed; no pushes
(the desk owns those). Last commits: `112ef42579` · `25710306aa`.

---

### ② THE ZILLOW IN-APP BODY-HEADER PARSER + THE PARKED 17 — SHIPPED, DESK-ACCEPTED

**Receipt: `.claude/reports/inapp-header-17/RECEIPT.md`.**
Reversal dump: `~/hl-reversals/inapp-header-0822/before-apply-2026-08-22T20-25-34-235Z.json`.

- `apps/houseloop-crm/lib/fub/inapp-body-header.ts` — parses the header Zillow/FUB embeds in the
  scraped body. Structural anchors only (the `Reply` marker, a date token, the single
  lowercase→uppercase seam between glued names). **Never reads `actor` / `user_name` /
  `is_incoming`, not even as a fallback** — those are the fabricated columns it exists to replace.
- Gate: **`npm run test:inapp-header` — 67 assertions, green.** Red-first confirmed.
- **Outcome on the 17: 5 corrected · 1 already right · 11 refused.** The two the census called
  "our card looks wrong" carry no header and are REFUSED — reading like an agent is not the row
  recording who wrote it. They remain open with no instrument that can settle them.
- `scripts/inapp-header-correction.ts` (dry-run default, `--apply`) · `scripts/inapp-header-sweep.ts`
  (read-only production sweep).

### Open, framed, in the desk's queue
**The 2,479-row class.** The parser reads 2,479 of 15,365 `zillow_inapp` rows (16.1%), and where the
header and the tape's `actor` both speak they **disagree 53.3% (226/424)**. That corroborates the
fabrication finding on a far larger sample than the 5-of-17 it rested on. **Approved shape: a
READ-ONLY proposal — counts, per-row verdicts, confidence tiers, dump plan. No writes.** Not started.

### Parked by the desk
The **130-row encoding question**: `zillow_message_sent` rows use `by_kind='system'` with a HUMAN's
name in `by_name`. I matched the existing 118 rather than invent a second shape on five rows; the
desk parked whether the whole class should be re-seated under the actor canon.

---

### ① THE LEGACY MASK — CLOSED AS COVERED (twice)

**Closure: `.claude/reports/legacy-mask/CLOSURE.md`** (§1–§5) + `shots/`.

- **§1 TextThreadStream** — the band had no reachable input (254 rows book-wide survive its filters,
  0 pre-mint). Superseded by Zach's re-ruling, then resolved by §4.
- **§2 DealsStream** — REFUSED and standing: the deals estate has no FUB provenance at all
  (`hl_lead_deals` has no `archived_from`), so a band there would file every past client's SISU
  closing as Follow Up Boss legacy. **If ever wanted it needs real provenance on the deal row — a
  column and a writer — never a clock inference.**
- **§3** — Zach's actual law is satisfied by construction: `CenterLane.tsx:2985` wraps the
  POST-filter set, so every filter pill bands.
- **§4 THE BUILD WAS WRITTEN AND REVERTED TO ZERO.** `TextThreadStream` has **zero JSX callers** —
  Zach retired it 7/23 ("no separate thread component"). The lifted guard was NOT harmless:
  `bbEvents` still feeds the unread badge and **`mostRecentTouch`** (a lead's LAST TOUCH), so it
  pushed **4,058 imported rows across 61 leads** into those numbers while changing nothing on
  screen. `FubLegacyBand.tsx` / `TextThreadStream.tsx` are byte-identical to their pre-lane state;
  the two SQL sites keep only a comment recording why the guard stays.
  **Walked and proven already-live:** Zairanett `fa1dada2` Messages tab — band renders, and inside
  the wash the actor alternates *Gina Julian → Zairanett → Gina Julian*, which is the ruling verbatim.

### ✅ RUNG ② EXECUTED (desk ruled GO, same night) — 55 written, 5 degraded
`scripts/legacy-actor-rung2.ts` · dump `~/hl-reversals/thread-actor-0822/` · commit `b4ef65ee48`.
All 55 through `restampActivityActor` in CORRECTION mode (drift guard + before-block + chain
evidence), stamped `meta.actor_source='fub_assignment_derived'`. Ashmir Mehandi 43 · Raul Lene 5 ·
Tevin Jacutin 3 · Zach Spradling 2 · Kathy Shen 1 · one joint account kept as a team label.
Replay writes 0. Walked: Timothy R Wilkinson shows "Raul Lene sent a Zillow in-app message to
Timothy R Wilkinson" in the wash (`shots/rung2-raul-lene-derived.png`).
⚠ The 55 reason strings carry an Eastern-locale timestamp; the script now emits ISO and the rows
were NOT re-stamped (a second entry changing no behaviour is worse than the blemish).
⏳ NEW, cheap, high-visibility: the raw scraped header leaks into card BODIES
(`"SShani_usman44Raul LeneMar 26 Reply…"`). `parseInappHeader` already returns the clean text.

### The measurement behind rung ② (kept — it is what makes the stamp load-bearing)
`.claude/reports/legacy-mask/rung2-proposal.json` — per-row, read-only, ready to execute.

- **Rung ① is moot.** Its target (`hl_messages.metadata.fub_user_name`) has no reader; the live
  activity log already matches the tape **1,350 of 1,350**.
- **The real gap is 60 cards, not 1,523** — legacy outbound cards with no actor name. 55 derivable,
  5 chainless.
- **`fub.assignment_history` has NO time dimension: `changed_at` is NULL on all 22,449 rows.** Only
  `scraped_at` (when WE looked). A custody chain is recoverable; its boundaries are not.
- **Control on 126,281 witnessed rows: 88.3% exact · 6.6% spelling variant · 1.0% team label ·
  ⛔ 4.13% WRONG PERSON.** ~2 of the 55 would name the wrong person — which is exactly why Zach's
  `actor_source='fub_assignment_derived'` provenance stamp is mandatory, not decorative.
- **Why I did not execute:** the useful target is `hl_activity_log.by_name`, an ACTOR COLUMN the desk
  parked, on a DIFFERENT table from the ordered `hl_messages.metadata`. Redirecting a write onto a
  parked column is the §4 error wearing the other hat.
- **To run it (~15 min):** additive · dump-first to `~/hl-reversals/thread-actor-0822/` ·
  provenance-stamped · spelling variants normalized · the 5 chainless left degraded · verify by
  re-read · idempotent.

### Filed defect classes (not mine to fix)
- The FUB import stamped **`recipient_role='agent'` on 52** of Zairanett's outbound legacy rows.
- **`hl_messages` collapsed actor** — CRM actor vs tape `user_name` **agree 3, disagree 1,520** of
  1,590; 26 real agents folded onto Zach / Ana / Myli / nobody. Harmless *today* because nothing
  renders that table — **a live landmine for the next surface that reads it.** The desk has filed
  first-class actor columns on `hl_messages` under the seat model as the formal cure.

---

### GATES AT CLOSE
`tsc --noEmit` clean for this lane. ⚠ Two PRE-EXISTING errors remain in
`apps/houseloop-crm/lib/leads/all-comm-clock.test.ts` — **another lane's uncommitted #242 work**,
flagged not touched. All 8 cages green **from the repo root** (door-zero floor held at 694; a
transient +1 was my own comment prose tripping the text matcher — reworded, never re-baselined).
`test:restamp-actor` · `test:correct-kind` · `test:claimed-actor` re-run ALL GREEN after touching
their callers. `test:inapp-header` 67/67.

### THE TWO LESSONS WORTH CARRYING
1. **A named component is not a live surface — grep for its render site before editing it.** The
   re-ruling, the desk's scope, and my own plan all named a file that had not rendered since July.
   The build was well-formed, tested, and aimed at nothing; the only thing it would have shipped was
   a regression in two numbers nobody was watching.
2. **A probe shaped like its expected answer confirms itself.** The census's "the header is
   zillow_inapp-only" came from grepping for `• Delivered`, which only matches Zillow's rendering.
   Running the finished parser over all 399,840 rows found the same header on 4,475 `group_sms`
   rows. The production sweep also caught two real defects no hand-picked sample showed — couple
   avatars (`O&Owen & Lori Maier`) and a case-insensitive `[a-z]*` that ate a recipient's surname.
