---
title: "Avatar-one (avatar-one LANE HANDOFF — LEE-WRITTEN BACKSTOP (2026-08-14 ~21:50Z, pre-account-switch))"
description: "Work-in-progress dossier — Avatar-one. avatar-one LANE HANDOFF — LEE-WRITTEN BACKSTOP (2026-08-14 ~21:50Z, pre-account-switch)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/avatar-one.md` on every push. **Progress: checklist pending.**</Info>

**Owner:** Avatar-one

## Checklist

# (If avatar-one wrote its own version, that one wins — this is the desk's map of the lane.)

### MISSION — LEDGER 102 (the ONE-AVATAR law, Zach verbatim)
"the actor profile pictures in the ipad lane must be driven from the profile cards" + Myli's
canonical headshot = the WHITE-BACKGROUND Pixar (the website-chat one,
.claude/reports/homeowner-dashboard-design/myli-avatar-pixar-white.png). Edit Myli in ONE place
(her profile) → fans out everywhere. Sent emails keep their baked originals (Zach: "i agree").

### WORK ITEMS (state as known at the desk)
1. White-Pixar → Myli's hl_users.avatar_url row — state UNCONFIRMED at switch.
2. Upload path in app/api/team/users/[id]/route.ts — IN FLIGHT; a coherent buffer of theirs was
   audit-committed by Lee earlier (Lee also reworded 2 banned door-words in it: "had no way in",
   "at the boundary instead"). Check git log for that file.
3. iPad lane (CenterLane actor avatars) reads the PROFILE, not a static — UNCONFIRMED.
4. Website chat/search honors Myli's profile avatar — UNCONFIRMED.
5. Signature re-render triggers on avatar change (today it only re-bakes on name/phone) — UNCONFIRMED.
6. https-only normalization on avatar URLs (vCard check) — UNCONFIRMED.
7. agent.jpg quarantine — UNCONFIRMED.

### CONTEXT (the census, from the desk's earlier audit)
26 avatar sources found; humans already flow via hl_users.avatar_url through sender-identity.ts and
actor_avatar COALESCE; Myli was 3-4 unsynced statics; NO in-app avatar_url writer existed before
this lane.

### NEXT SHIFT
`git log --oneline -15 -- apps/houseloop-crm/app/api/team/users/` + query
`SELECT avatar_url FROM hl_users WHERE ...myli...` to see how far item 1-2 got, then walk the list.
