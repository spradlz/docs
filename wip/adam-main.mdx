---
title: "Adam (Adam — WIP (2026-07-18))"
description: "Work-in-progress dossier — Adam · HouseLoop CRM · Developer Docs. Adam — WIP (2026-07-18)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/adam.md` on every push. **Progress: 7/11 done (64%).**</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 59h ago. If Adam has shipped since, this dossier under-reports — refresh `.claude/state/wip/adam.md`.</Warning>

**Owner:** Adam — HouseLoop CRM · Developer Docs

## Checklist

### CURRENT (2026-07-18): AI-agnostic first-touch + FUB-inquiry clone-initiates + Mack AI-vs-AI test
Plan: `~/.claude/plans/sharded-roaming-whisper.md` · Handoff (resume here): `.claude/state/adam-handoff-mack-clone-test.md`
- [x] FIX 1 (core): composeAiMessage resolves the acting actor from the lead (new `resolveLeadActor` + shared
      `CLONE_LANE_PREDICATE` in resolve-send-identity.ts) → workflow ai_message first-touch composes AS the lead's
      actor (Ana's clone), matching the send. Committed `2fa3ef3bb7`, tsc clean, NOT pushed.
- [x] FIX 2 (labels): notification card + leads-table "who" resolve the acting agent's NAME (join hl_users on the
      actor UUID) → clone reads "Ana (AI)"/"Ana Spradling", not "Myli". Verified live (5 mislabeled rows fixed). Same commit.
- [ ] FIX 3 (needs Zach — canvas + live-fire): build the FUB Property-Inquiry first-touch workflow on the Lead Flow
      canvas (first node `ai_message`, reuse sys_reg_liked_home pattern; reenrollment_policy='never'); bind Path A
      (`hl_fub_webhook_settings.workflow_id` for property_inquiry/contact_request/tour_request); seat = Ana + clone_default_on.
- [ ] TEST (needs Zach sign-off): fire a REAL `fub/lead.import` through Inngest with the test contact (+19137333704 /
      titan@aititans.ai) → clone first-touch as Ana → AI-vs-AI (port pa-test-harness runJennyFastConversation, inject
      via /api/bb/inbound?password=myli2026bb) → nudge/saved-search/follow-up/summary-email/handoff → capture to artifact.
- [ ] FIX 2 deferred: context-blocks.ts:344 email voice (MYLI_IDENTITY) → actor-parameterized (test routes around it).
Real Mack Moser (802-855-3550 / FUB 50450) must NEVER be contacted — test line + titan@ only.

---

# Adam — WIP (2026-07-16)

### Today (Zach directive): tasks 1-6 = FUB Phase 0 hardening, then 7 = Closed-Loop Follow-Up
Task board lives in the Claude task manager (7 tasks). Full state: `.claude/state/adam.lock`.
Handoff/spec: `.claude/state/adam-handoff-fub-trigger-audit.md` (Phase 0) → `.claude/state/adam-handoff.md` (Phase 1 Buyer Loop).

- [x] 1 · watchdog doom loop FIXED + VERIFIED (PUT /json/new + idle heartbeat; self-heal login chain proven live;
        steady state held past the old 6-min kill mark, restarts:0)
- [x] 2 · mirror-sync sequence rewind race fixed (advance-only setval) — deployed; Shamir sync passed through it ✅
- [x] 3 · person_rescrape queue-widen migration committed + verified (4b0245d41d)
- [x] 4 · E2E lock-step PROVEN on Shamir 50442 — full chain in ~95s (queue #3166 → consumer → CD flip → claim in
        10s → scrape 48 homes/5 searches → rebackfill). ROOT-CAUSED + FIXED the dead hop: consumer pm2 env had
        DATABASE_URL=Railway (stale first-registration env) — delete+start from ecosystem.config.cjs.
- [x] 5 · cron-armed reconcile — code+registry (e20e684f5a) + all 4 docs flipped to truth, Mintlify build GREEN
- [~] 6 · Phase 0B-0F: mig 0414 (lead_id nullable + fub_person_id, applied prod) · recordFubTriggerFire in
        fub-lead-import (every serviced live event → ledger; 3 call sites) · insight_re_engaged wired (2,791 notes!)
        + high-intent feed cards (1,437) · virtually_toured un-suppressed + map parity · property_shared = EXTINCT
        (0 events ever, documented) · generic note = notify-only BY DESIGN · fire-counts + fires drill-down → ONE
        spine (hl_trigger_fire_live) · scrapePersonFull login → fub-login-bot · deploy.sh PROCS += backfill-b ·
        docs updated. REMAINING: live 0B proof on Cheryl once Railway deploy 46c4fff245 lands (watcher buj39arx6).
        NOTE: Benji caught an orphaned BEHAVIOR_ONLY ref that hit origin (fixed e48272aaf8) + my 0413 renumbered
        to 0414 (his actor_kind owns 0413).
- [ ] 7 · Closed-Loop Follow-Up (blocked on 6 + Zach Phase-0 sign-off)

Discoveries worth remembering: Chrome 111+ /json/new needs PUT (GET = about:blank tab, URL dropped);
worker pages idle at about:blank so tab-reapers can kill them (FATAL page-closed crashes); pm2 reload keeps
stale env (verify pm2 env &lt;id&gt;); fub_scraper_status MCP = stale (screen model); FUB re-engaged copy =
"started searching again" (Zillow's #1: 2,791 notes).

### THE DONNA CALL SAGA (2026-07-16 PM — live with Zach)
- Donna pulled in quiet (lead 5a675232); 24h comms-lookback floor amendment (Zach) = the call that caused a
  pull-in rides into the feed. Call hero card in All (call_logged) + CallsStream = summary + player when data.
- FUB masks call recording/transcript/summary in the API ("* hidden for privacy *") — unmask guard in bridge.
- FUB call-summary EXTRACTOR WORKS (Dec summaries on Camp David) — gap = webhook-stored calls live on RAILWAY
  only, never seeded to CD, so the scraper can't match new calls. Optional seed-CD fix pending (history only).
- ROUTING (Lee, live 2026-07-16): 5-bucket model — ALL FUB past-agent/company/sign calls funnel through OUR
  Twilio (675-9877 etc.) → forward to Zach's cell → OUR recording + Deepgram→Claude summary. New calls need
  NO FUB scraping. Donna's 423-3348 = past_agent bucket confirmed. Zach's test (Dylan Hines # from Myli's
  cell) → forwarded ✓ w/ bucket caller ID, subtype phone_call_past_agent ✓.
- 🔴→✅ ANSWERED FORWARDS NEVER RECORDED: voice webhook &lt;Dial&gt; had no record attr (only voicemail &lt;Record&gt;
  recorded) → bare rows, no analyzer, no summary. FIX: record="record-from-answer-dual" +
  recordingStatusCallback on both Dial blocks, gated on hl_settings('call_recording').recordingEnabled
  (ON). Same /recording callback → CALL_RECORDED → analyze-fn (Deepgram→Claude). Pushed ea2139b4fb,
  Railway deploying (watcher b8qyi8v1v). VERIFY: Zach test call post-deploy → recording + summary on card.
