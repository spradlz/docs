---
title: "Adam (Adam — WIP (2026-07-16))"
description: "Work-in-progress dossier — Adam · HouseLoop CRM · Developer Docs. Adam — WIP (2026-07-16)."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/adam.md` on every push. **Progress: 1/3 done (33%).**</Info>

**Owner:** Adam — HouseLoop CRM · Developer Docs

## Checklist

### Today (Zach directive): tasks 1-6 = FUB Phase 0 hardening, then 7 = Closed-Loop Follow-Up
Task board lives in the Claude task manager (7 tasks). Full state: `.claude/state/adam.lock`.
Handoff/spec: `.claude/state/adam-handoff-fub-trigger-audit.md` (Phase 0) → `.claude/state/adam-handoff.md` (Phase 1 Buyer Loop).

- [x] 3 · person_rescrape queue-widen migration committed + verified (4b0245d41d)
- [~] 1 · watchdog doom loop fixed (PUT /json/new + idle heartbeat) — deployed, steady-state monitor running
- [~] 2 · mirror-sync sequence rewind race fixed (advance-only setval) — deployed, watching for dup-key errors
- [~] 4 · E2E lock-step proof on Shamir 50442 — job #3164 in flight, baseline 130 feed rows
- [~] 5 · cron-armed reconcile — code+registry pushed (e20e684f5a); docs pass pending task-4 proof
- [ ] 6 · Phase 0B-0F doctrine fixes + make the 4 FUB docs true
- [ ] 7 · Closed-Loop Follow-Up (blocked on 1-6 + Zach Phase-0 sign-off)

Discoveries worth remembering: Chrome 111+ /json/new needs PUT (GET = about:blank tab, URL dropped);
worker pages idle at about:blank so tab-reapers can kill them (FATAL page-closed crashes); watchdog restart
counter resets on reload (pm2 crash-restarts ≠ watchdog restarts); fub_scraper_status MCP = stale (screen model).
