---
title: "Benji (BENJI — WIP)"
description: "Work-in-progress dossier — Benji · HouseLoop Website. BENJI — WIP."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/benji.md` on every push. **Progress: checklist pending.**</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 623h ago. If Benji has shipped since, this dossier under-reports — refresh `.claude/state/wip/benji.md`.</Warning>

**Owner:** Benji — HouseLoop Website

## Checklist

_Updated 2026-07-30. Current plan: `.claude/plans/declarative-leaping-perlis.md`._

### MISSION (2026-07-30): AGENT LICENSING — a mismatch WARNS at signing ✅ SHIPPED

Broker Chris Cribb 2026-07-29: Ana is licensed MO + KS; Zach is KS-only and cannot be designated on MO
property. The CRM had NO license concept — two live MO loops carried an unlicensed contract agent.

- **`hl_user_license`** (mig `0509`, applied) — one row per agent per state: `state` (51-code CHECK),
  `status` (`active`|`not_active`), `renewal_date`. Status is the BROKER's word, never derived from the date.
- **Schema ruled by Lee**: store relational, SERVE as JSON on the user payload (his Gate B connections
  pattern). Per-state is mandatory — Zach is KS-active and MO-inactive at once.
- **UI**: Team → member → Profile → **LICENSING** (state / status / renewal date, + Add state). Admin-only.
- **`lib/tc/license-check.ts`** — THE one resolver. Warns, never blocks. `ok:true` on unknown state so it
  never cries wolf. The suggested licensed agent is LOOKED UP, so warnings self-retire when Zach regains MO.
- **Surfaces**: AddPersonModal (at selection) · FillReviewModal · SendForSignatureModal · create-fill-send
  (headless, logs). Retired the always-Ana stopgap (`contract-signer.ts` + the `hl_settings` row, both gone).
- **Dead field fixed**: `websiteHlid` was posted by CreateLoopModal, declared by the API, and dropped. Now
  persisted → the property state comes from structured `Listing.state`; address regex is only the fallback.
- Commit `40128da9af`. Verified live on all 6 loops: 4 KS silent, both MO warn naming Ana.

**Open / next:** ⏳ confirm on PROD once Railway finishes. Lee queued "license edits join the runlog" as a
Gate-7 item — when `hl_activity_runlog` lands, add ONE `logActivity({ kind: 'user_edited' })` call to the
licensing PATCH (deliberately omitted now: a NULL-lead row in `hl_lead_activity` renders in nobody's feed).
**Scope was SIGNING only** — showings are explicitly out (Zach 2026-07-30).

### ⏸ PAUSED: SAVED SEARCH first-class flow (17 of 23)
Living artifact + build tracker. Remaining: 19 (prove a real alert lands — needs Zach), 17 (alert-by-text
for the 7 phone-only leads), 20 (phone login + texted code), 21-22 (176 Zillow searches → pre-fill),
23 (pin the wireframe).

### MISSION: "Myli: Standing By" — standby-as-a-real-workflow + honest workflow pill

### ✅ SHIPPED (committed on houseloop-app; deploys on next push)
- **Lint compliance** — tokenized all raw-style debt in the lead-detail redesign; `hl/*` gate passes clean (0 violations).
- **P0** (Adam) — inbound reply obeys `ai_responder`; lead Myli toggle truly silences her.
- **P1** — `MyliWatchingBadge` is workflow-aware: reads `active_workflow_id → hl_workflows.name` + `hl_workflow_runs.current_node_id` step off `/api/leads/[id]/ai-activity`, live over SSE. "Myli · Standing By" idle · "AI paused" master-off. Replaces static "AI watching".
- **P2 (dark)** — `ai_converse` (Listen & Reply) + `ai_standby` (Set Next Floor Touch) node types in `lib/workflows/registry.ts` (+ RUNNABLE_STEP_TYPES) with master-gated executors in `lib/workflows/execute-step.ts`. Mig `0434_myli_standby_workflow.sql` seeds the **Myli: Standing By** workflow (kind=myli_auto, status=DRAFT, folder=AI Tools, canvas: Manual → Listen & Reply → Set Next Floor Touch). Applied to prod CRM DB. Commit `609be0c154`. Verified rendering on :3020.

### 🔒 THE ai_converse BRAIN — locked with Adam (bring to joint plan review with Zach, UNAPPLIED)
Adam's state-watcher research (`.claude/reports/lead-texting-prompt-research-2026-07-20.md`, sourced
Structurely/Ylopo/LPMAMA/NAR) locks the `ai_converse` design: **NOT free chat** — a CODE-owned ledger +
skippable state ladder. The node I shipped in P2 is just the dark SHELL (free-chat placeholder); the real
brain is:
- **mig 0435 `hl_lead_convo_state`** (DRAFT ONLY — do not apply before the joint review): `lead_id` uuid PK
  FK→hl_leads CASCADE · `state` (S0_confirm_tour..S6_nurture) · `mode` ('conversational'|'behavioral') ·
  `ledger` jsonb (keys: tour_time_confirmed, representation_status, email_captured, on_our_search_account,
  motivation_timeline, price_target, lender_status, buyer_agreement_signed, human_takeover, takeover_reason) ·
  `updated_at`.
- **4 rules:** (1) SINGLE WRITER = the watcher; the composer reads the ledger READ-ONLY and voices ONLY the
  lowest unfilled slot (resolver-never-eats-own-dogfood). (2) Every transition + captured fact → `logActivity`
  (All feed shows the ladder move). (3) Full gate stack `aiMasterEnabled AND service AND ai_responder!='none'`;
  `buyer_agreement_signed=false` HARD-BLOCKS tour language in CODE (NAR 8/17/24). (4) NOT columns on hl_leads,
  NOT run-scoped, NOT the AI context prose.
- **The ladder:** S0 confirm tour → S1 representation → S2 migrate off Zillow (email only) → S3 qualify
  (motivation/timeline before price) → S4 financing → S5 buyer agreement (HARD GATE) → S6 behavioral nurture.
- **ai_converse contract:** reads the ledger each turn, emits a structured `HANDOFF{reason}` the watcher routes
  on (then Myli goes silent). TCPA 8-8 local + STOP enforced by the WATCHER, not the prompt.
- `ai_standby` = the S6 BEHAVIORAL mode (quiet → portal-activity re-engage) over the existing floor engine.

### ⛔ REMAINING — needs Zach + Inngest Run Log (do NOT flip blind)
- **P3 — Auto-enroll + override/resume + the state-watcher brain above.** ACTIVATION = flip `Myli: Standing By` to `status='active'`, then enroll idle leads + repoint `handoffToStandby` (`lib/ai/ensure-floor.ts:266`) to park the lead on Standing By. ⚠️ HAZARD: the runner calls `handoffToStandby` after EVERY run *including Standing By's own* → self-re-enroll loop; guard by skipping enroll when the just-finished run WAS standby (2-min dedupe alone is not enough). Enroll via `inngest.send({ name: 'houseloop/workflow.run', data: { workflowId, leadId, triggeredBy, triggerPayload } })` (see `runtime.ts:406`). Prove on ONE test lead in the Run Log before the global backfill.
- **P4 — Retire `ai_conversations` / `ai_closed_loop` code toggles.** LAST. `ai_closed_loop` is fail-CLOSED — seed a successor gate BEFORE removing the key or nurture dies silently. Typed Zach approval + Run-Log proof required (Rule #0-adjacent).

### Docs
- `closed-loop-follow-up.mdx` — updated + published for P0/P1 (pill truth + mic gating).
- `workflows.mdx` — **owes a P2 update** (the `ai_converse`/`ai_standby` nodes + the Myli: Standing By workflow now exist). Do on next docs pass now that P2 shipped.

### Coordinate
- **Adam** owns `ai_responder`/resolver + the inbox model; ping before P3's resolver touch + inbound routing.
- Caleb's rooftop-guard push is unblocked (my WIP committed).

---

# TC LOOP — CONTRACT ENGINE (2026-08-03, benji)

**Board artifact:** https://claude.ai/code/artifact/cd761514-1600-4e12-aaf1-2af99c1ae98f
Source: `.claude/artifacts/tc-loop-board.html` (edit + republish with the Artifact tool, same URL).

### SHIPPED THIS SESSION

### Arc 2 — the section builder (CLOSED)
- `c8b2198e9e` — section DELETE offers **move-then-delete** (`reassign_to`) instead of refusing.
  Zach: *"otherwise its basically a UI that is 1 and done."* TC check runs BEFORE the stage-count
  check on purpose (a TC section also has stages, so counting first gives a misleading reason).
- `88c70f643e` — the **builder UI**: `SectionsTable` (add/rename/recolour/drag-reorder), `SectionModal`,
  and `ReassignDeleteModal` **extracted** from `StageDeleteModal` (which now delegates) — not copied.
  ⚠ `SectionModal` uses `StageColorSwatches`, NOT `ColorPicker`: the API validates against
  `STAGE_COLOR_HEXES` and 5 of ColorPicker's 10 swatches are NOT in it (would 400).
- `e7dc503083` — **add a stage inside a section** + move one between sections.
  ONE shared validator `lib/stages/section-assignment.ts` used by POST and PATCH (this is exactly the
  check that drifts: inline in one, forgotten in the other). Same pipeline + never `tc_milestones`,
  enforced in the API because a dropdown is not a security boundary.
- **Board was WRONG on `advances_to_stage_id`**: it said 0/30. Truth = 23/30, all resolving to real
  stages, and **the other 7 SHOULD be NULL** (funded/possession are post-close, inspection_resolution
  is a sub-step, buyer_agency_prepped is a pre-step — no stage exists for any). Chasing "30/30" would
  have wired 7 milestones to wrong stages.

### Arc 4 — the date engine (CLOSED)
- `6ee24ca689` — **`computeLoopSchedule` did not exist.** It was a named ghost in 4 comments, each
  deferring to the others. `computeDue` (13 passing tests) was fully built and its ONLY caller was its
  own test file. Wrote `lib/tc/loop-schedule.ts` + wired it into `PATCH /dates`.
  Invariants: never invents a date (no anchor → NULL, counted in `skipped`); never touches
  `is_overridden`; clearing an anchor clears the dates it fed.
- `5f38f345e9` — **`TcTasksPanel`** — the Tasks dock tab was a stub, so 51 computed dates were invisible.
  ⚠ `fmtDueDate` splits the string: `new Date('2026-09-08')` is UTC midnight and renders as **Sep 7**
  in Central. Every task would have read one day early.
- `ca8e136cbd` — **key dates are enterable**: generalised `PATCH /dates` to any `is_key_date`
  milestone_key (validated against the loop's side; unknown keys REPORTED not ignored) + `TcKeyDatesPanel`
  on the Dates dock tab. `listMilestones` now returns `is_key_date`; `TcLoopBundle` finally declares
  `milestones` (the API always returned them — the type never said so, which is why they were unreachable).
  Proven: typed Inspection Period Ends → **"Saved — 7 task dates recalculated"** → loop 58/58 scheduled.

### ⚠ NOTE ON HISTORY
The auto-save hook swept some of this in as `[lee] route.ts…` commits (author is still benji; the
MESSAGE prefix is wrong, and `apps/houseloop-crm/app/api/calls/log/route.ts` — Lee's file — landed in
`f3a1c28c6a`). Did NOT rewrite: that range contains other agents' commits. Real messages are in this file.

### NEXT (in board order)
- **Arc 3** — per-page coordinate conversion (two page sizes 978×1266 / 978×1511) · Inngest importer ·
  overlay-verify every field map · **bucket 58 forms with Ana** (needs Ana).
- **Arc 5** — needs **Lee** (5 renames) + **Zach's typed Rule-0 approval** to drop `hl_tc_phase`.
- **Arc 6** — Documenso `distributionMethod: NONE` · HouseLoop shell invite · deliver the STAMPED copy ·
  `/v/<code>` verification page.
- Arc 1's last item is Zach's own acceptance test (disposition Lawrence Guenther).

### STILL OPEN / HONEST GAPS
- `on_market` (sale side) now HAS a writer via the Key Dates panel, but no loop has used it yet.
- Existing client loops keep NULL task dates until someone enters a key date on them (or a backfill is
  approved). I did NOT bulk-run the cascade on real client loops — that's a real-data write for Zach to OK.
- Pre-existing bug, NOT mine: `app/(crm)/leads/[id]/page.tsx:267` reads `lead.id` two lines BEFORE its own
  `if (!lead) notFound()` guard, so a deleted lead 500s instead of 404ing. Hit it on the soft-deleted
  "Zach Test" lead.
