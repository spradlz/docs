---
title: "G14-go (g14-go — HANDOFF (2026-08-15 ~10:50Z) · STANDING BY)"
description: "Work-in-progress dossier — G14-go. g14-go — HANDOFF (2026-08-15 ~10:50Z) · STANDING BY."
---

<Info>Live dossier — auto-generated from `.claude/state/wip/g14-go.md` on every push. **Progress: checklist pending.**</Info>

<Warning>⚠ **Possibly stale:** this work-stream file was last written 328h ago. If G14-go has shipped since, this dossier under-reports — refresh `.claude/state/wip/g14-go.md`.</Warning>

**Owner:** G14-go

## Checklist

**One line:** the phone typo is purged, the corrected group test reproduced the "no group forms"
negative cleanly, the Address Configuration is in and proven safe, and **the no-go gate is ARMED but
unfired — it needs exactly one text from Zach's phone to 913-286-1761.** G14 remains **NO-GO**.

Receipts (all under `.claude/reports/g14-group-text/v2/`):
`RECEIPT-4-CORRECTED-HANDSET-RUN.md` · `RECEIPT-5-ADDRESS-CONFIG-AND-ARMED-GATE.md` ·
logs `12`–`19`.

### WHAT IS TRUE NOW

1. **The phone correction.** Zach's real line is **+14074038088**. `+14074034088` is a transposed
   digit belonging to **a live stranger** who received 2 delivered test texts on 8/14. Purged from
   the cage allowlist and from the one live Twilio binding that still held it; kept in `hl_messages`
   / `hl_activity_log` as the record. The `g14-transport` WIP doc (which auto-publishes) carried the
   stranger's number as "his personal" — corrected.
2. **The group still does not form.** Corrected-handset send (conv `CH6b63b447…`) produced the same
   wire as 8/14: two individually-addressed **1-segment, media=0 SMS**, `msgSvc=(none)`,
   $0.022/leg. Zach's leg **delivered**. Not the number, not the carrier, not the messaging-service
   pin. Bindings remain correct — **zero proxy addresses**.
3. **Address Configuration `IG0160c798f4e8abd6db340e6cdee29054`** on `+19132861761`,
   **auto-creation OFF** (the desk's OFF ruling, not the brief's ON).
   Revert: `DELETE https://conversations.twilio.com/v1/Configuration/Addresses/IG0160c798f4e8abd6db340e6cdee29054`
4. **It is SAFE — measured, not assumed.** An ordinary 1:1 inbound still writes a CRM **inbound**
   row, i.e. the plain SMS webhook still fires. Real leads do text this line; no regression.
5. **⭐ THE GATE IS ARMED.** A deliberately v1-shaped probe (Address **+ ProxyAddress**) **CAPTURED**
   a private 1:1 and was torn down. So the capture machinery is provably live, and a future
   "the v2 group did not capture it" is a **real exclusion, not a vacuous green**. This is the piece
   8/14 lacked.
6. **Why it has not fired.** The test needs a 1:1 from a v2 **seat-holder**. Owned Twilio numbers
   still cannot hold a v2 seat (**50407 re-verified today, cuts the same way**), and Myli's handset
   **cannot send SMS** — BlueBubbles returns `200 Message sent!` while the message sits on the Mac
   `isDelivered:false` and never reaches Twilio.

### THE ONE ACTION THAT CLOSES IT
Zach texts **913-286-1761** from his phone (any words). He is already a live seat in `CH6b63b447…`.
- **PASS** = lands as ordinary 1:1 (CRM inbound row + the `[g14/no-go]` webhook log line) and
  **never** appears in the group / never reaches Myli.
- **FAIL** = shows up in `CH6b63b447…` or fans out to Myli → the no-go defect survived; G14 dies.

### DO NOT
- **Do not delete conversation `CH6b63b447…` or thread `fe9c6766-…`** — it is the live fixture the
  test fires at.
- Do not flip the cage off `mode:test`. It is what keeps a real lead out of a group while the
  exclusion is unproven.
- Do not turn auto-creation ON without a Zach ruling — it diverts real lead inbound off the CRM
  webhook. (Only needed for the handset-origination test, which is separately held for Zach.)

### OPEN ITEMS RAISED
1. **Zach's one text** (desk carrying) — the last blocker on the gate.
2. **Myli's iPhone: Text Message Forwarding is off/broken.** Physical access needed. Until then the
   gauntlet has **no self-service real-handset sender** — a standing limit on every inbound test.
3. **BlueBubbles `200 Message sent!` is not proof of transmission.** Same class as the Inngest
   `modified:true` fake gate. Assert `isDelivered` or a provider record; never the POST status.
4. **The group-formation mechanism is still unexplained.** Eliminated: the number, the carrier, the
   messaging service, account entitlement (created 2020-03-17, pre-cutoff; line has `mms:true`), and
   — per Twilio's own docs — the Address Configuration, which is **inbound-only** and cannot govern
   an outbound send. Leading hypothesis, labelled as one: Twilio's documented group origination is
   **inbound** ("a group message reaches a projected address"), i.e. the group is created **on a
   handset**. Test held for Zach: he starts a group text from his phone with +19132081633 and
   +19132861761.
5. **Pricing for the go-live card:** $0.022 per leg, one leg **per participant** (~$0.20 for a
   9-person group).
