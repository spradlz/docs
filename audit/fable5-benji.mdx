---
title: "⭐ Fable 5 Master Audit — Benji"
description: "Benji's remediation lane: 6 subsystems, worked top to bottom with the resolve protocol (🟢 + timestamp + model + notes)."
---

<Info>**THE REMEDIATION PROTOCOL (Zach 2026-07-06):** work ONLY from this sheet, top to bottom. For each item:
(1) **deep-verify** the current functionality actually works — run it, click it, query it; never trust the doc or the code comment;
(2) fix per the prescription (full detail in the repo: `.claude/reports/audit/<key>.md`);
(3) flip the row to **🟢 RESOLVED** with the **date+time**, the **model you were running**, and **notes on what you did**.
Many tables are near-empty — only ~15 leads exist (a few real Zillow leads + test leads); most machinery has never seen real volume.
When a feature is spec-built-but-empty, say so explicitly and add a **visual of what the spec calls for** (screenshot or mock) so
Zach can see intended vs actual. An unverifiable claim stays ⬜ — never green a row you didn't prove.</Info>

<Warning>**🚨 RED lane:** Saved Searches & Alerts scored 9/30 — the engine that turns saved homes into fired leads is the sickest subsystem in the project. It goes first.</Warning>

> Master: [⭐ Fable 5 Master Audit (All)](/audit/fable5-all) · Full artifact: [the State of the Union](https://claude.ai/code/artifact/d5f808e0-3ab8-4fd3-a6b2-5a615d6b82a0)


## Saved Searches & Alerts — RED (10/19 good · 7 gaps)

Full report: `.claude/reports/audit/web-saved-searches.md`


**Headline gaps found by the audit:**

- Digest/alert emails NEVER send: sys_saved_search_digest workflow is archived (0 runs ever) and runtime.ts:502 skips non-active workflows
- Silent data-lie: website advances lastAlertSentAt/lastMatchCount before the CRM drops the send, so RECENT shows 80 sent with no email delivered
- Digest gate reports active via is_locked while the runner honors status='archived' - the two authorities disagree
- Send Now returns 409 (archived workflow); existing-lead client saves fire no active workflow (silent)
- Only 1 saved search exists in all of prod (system/weekly) - send path never exercised by a real client save
- Docs claim alerts are 'verified live' but the send-workflow was archived 2026-07-07 and gutted to 1 node (no email step)


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| web--1 | Rebuild + activate sys_saved_search_digest workflow's branded email step via the builder (canvas is truth); acceptance: hl_workflow_runs for fed47be6 goes 0-&gt;&gt;=1 with a real Resend id | ⬜ | — | — |
| web--2 | Reconcile digest-gate (is_locked) vs runtime.ts:502 (status) onto ONE authority so the website stops stamping watermarks for sends that never fire | ⬜ | — | — |
| web--3 | Wire or explicitly document sys_saved_search_existing for existing-lead self-saves (currently silent) | ⬜ | — | — |
| web--4 | Activate/verify saved_search_agent + saved_search_system trigger events (is_active=f but firing) | ⬜ | — | — |
| web--5 | ALTER SavedSearch.alertFrequency default daily-&gt;instant and add a RED 'alerts not sending' banner to the guide until #1 ships | ⬜ | — | — |


## Leads & Lead Detail — YELLOW (17/24 good · 6 gaps)

Full report: `.claude/reports/audit/crm-leads-detail.md`


**Headline gaps found by the audit:**

- DOC LIE: lead-detail-stages-and-deals.mdx cites hl_lead_deals 9x + migrations 0069/0070 — table does not exist; real table is hl_transactions (zero code refs to hl_lead_deals)
- TC Dashboard docket: 6 of 9 chips (Tasks/Emails/Attachments/Dates/Payout/Misc) are 'coming soon' stubs, but lead-detail.mdx:93 says 'Each opens a full modal'
- Dock unread badges hardcoded to 0 for Calls/Emails/Notes/Appts/Tasks/Deals — only Texts + Website wired
- Myli lane Brain Map is a stub (MyliLane.tsx:189)
- No Run-Log evidence leadActivityBackfill ever ran; 245 agent-notification sms mislabeled channel='sms' could leak into a real lead's feed on backfill re-run


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| crm--1 | Replace every hl_lead_deals reference in lead-detail-stages-and-deals.mdx with hl_transactions and fix the 0069/0070 migration citations; acceptance: grep hl_lead_deals in docs-site returns zero + mirror --dry passes | ⬜ | — | — |
| crm--2 | Reconcile lead-detail.mdx:93 TC docket claim with MilestoneBand.tsx stub list (or build the 6 stubs per Zach's call) | ⬜ | — | — |
| crm--3 | Wire per-category dock unread badges via /api/notifications/lead-badges (CenterLane.tsx:531), mirroring the homes/texts pattern | ⬜ | — | — |
| crm--4 | Trigger leadActivityBackfill through Inngest, capture a Run Log entry, and tighten its WHERE so agent-notification sms can't enter a lead's feed | ⬜ | — | — |
| crm--5 | Delete or document the disabled read-time home resolver in activity/route.ts:79-176 | ⬜ | — | — |


## TC Loop — Transactions & E-Sign — YELLOW (17/28 good · 7 gaps)

Full report: `.claude/reports/audit/crm-tc-loop.md`


**Headline gaps found by the audit:**

- AddTransactionFunnel is DEAD CODE, rendered nowhere, yet the intake guide says it shipped + prod-verified 2026-07-04
- computeLoopSchedule (G4) does not exist: entering the 2 anchor dates projects none of the other ~12 milestone/task dates
- Under-Contract stage move still opens DealMilestoneModal, not the TC funnel (creates orphan deal, no loop/dates/stepper)
- Reminder sweep is Inngest event-only: the daily cron is commented out, so nudges never fire automatically
- Zero prod e-sign/native-fill data: 1 loop, 0 hl_contract_fill rows, 0 sent/signed docs; 'proven live' claims UNVERIFIABLE


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| crm--1 | Wire AddTransactionFunnel to a real parent or git rm it and fix the intake guide's false shipped-status note | ⬜ | — | — |
| crm--2 | Build computeLoopSchedule in loop-engine.ts and call it from the dates route so tasks get projected due_dates | ⬜ | — | — |
| crm--3 | Route Under-Contract stage moves through the TC intake (create/link loop + write anchors + link deal) instead of DealMilestoneModal | ⬜ | — | — |
| crm--4 | Enable the esign-reminder-sweep cron after a Zach-signed live fire so reminders actually run | ⬜ | — | — |
| crm--5 | Reconcile the milestone value set (undocumented 'open' state) across guide, doctrine, and the DB CHECK | ⬜ | — | — |


## Home Detail & Owner View — YELLOW (18/24 good · 6 gaps)

Full report: `.claude/reports/audit/web-home-detail.md`


**Headline gaps found by the audit:**

- Self-claim path has produced ZERO rows (all 20 claims are pcsoi-reconciled) - unproven in prod
- PCSOI pre-claim runs as a one-off script, not Inngest (violates INNGEST_OR_IT_DOESNT_EXIST, parked)
- Dual claim truth: stale localStorage badge can show phantom 'Your Home' -&gt; 403 on View Report
- Flat-sold-home count is 40 live vs docs '~29' - backstop behind or doc stale
- Realie AVM read from data/realie-featured.json file cache on the hot page (undocumented)


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| web--1 | Prove or fix the self-claim POST path end-to-end (0 self-claims in DB since 2026-07-06 ship) | ⬜ | — | — |
| web--2 | Re-run sold-data-reconcile backstop via Inngest and reconcile flat-count 40 vs doc ~29 | ⬜ | — | — |
| web--3 | Make server claim the sole driver of 'claimed' UI for logged-in users; kill phantom localStorage badge | ⬜ | — | — |
| web--4 | Move pcsoi pre-claim behind a registered Inngest fn or get explicit Zach exemption | ⬜ | — | — |
| web--5 | Document city-free identityKey fallback, Realie file cache, and the zhvi_appreciation value source | ⬜ | — | — |


## Website Auth & Lead Intake — YELLOW (24/25 good · 6 gaps)

Full report: `.claude/reports/audit/web-auth-intake.md`


**Headline gaps found by the audit:**

- Guide describes REG_VARIANT as live but code says it is DEAD (v4 flat model, process-intake.ts:738)
- Guide points ACTION_TO_EVENT/REG_VARIANT at wrong file (route vs lib/lead-intake/process-intake.ts)
- Zero website-origin leads/registration fires in live DB (reset ~07-14) — end-to-end mint UNVERIFIED
- Stale crm-intake.ts comment claims note is ignored; it IS inserted to hl_notes
- Website dev server localhost:3001 returns HTTP 500 on homepage
- Web-route phone dedup full-table-scans all users in memory


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| web--1 | Rewrite guide intake section for v4 flat model + fix wrong file reference (docs-site/guides/web-registration-and-intake.mdx:72-198) | ⬜ | — | — |
| web--2 | Re-run live-fire matrix under flat model via registration harness; prove all 9 doors mint a website lead + fire correct arm | ⬜ | — | — |
| web--3 | Fix stale note comment in crm-intake.ts:37-40 | ⬜ | — | — |
| web--4 | Move web-route phone dedup into SQL (tour-requests/contact-agent) instead of in-memory full scan | ⬜ | — | — |
| web--5 | Restore clean localhost:3001 (off HTTP 500) before live testing | ⬜ | — | — |


## Website Home & Shelves — GREEN (14/17 good · 4 gaps)

Full report: `.claude/reports/audit/web-home-shelves.md`


**Headline gaps found by the audit:**

- Curated hero override path (FeaturedListing) has 0 active rows — built but unexercised live
- Geo personalization not built (honestly documented as absent; sortOrder reserved)
- hl_app_config mixes seeded product-config with runtime sync.* keys not in the seed file
- No admin UI: 'non-engineer can change shelves' is still an engineer seed-edit + db:seed workflow


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| web--1 | Insert one active FeaturedListing row and verify it renders first in the hero carousel end-to-end, then decide on an admin UI/runbook | ⬜ | — | — |
| web--2 | Add a comment in app-config.ts documenting the runtime-written sync.* keys so the seed matches the live table | ⬜ | — | — |
| web--3 | Fix home-page.mdx to state shelf changes are currently an engineer workflow, not self-service | ⬜ | — | — |
| web--4 | Only if greenlit: wire per-visitor geo shelf ordering with the city-&gt;region map in DB, not TypeScript | ⬜ | — | — |
