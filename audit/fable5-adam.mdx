---
title: "⭐ Fable 5 Master Audit — Adam"
description: "Adam's remediation lane: 6 subsystems, worked top to bottom with the resolve protocol (🟢 + timestamp + model + notes)."
---

<Info>**THE REMEDIATION PROTOCOL (Zach 2026-07-06):** work ONLY from this sheet, top to bottom. For each item:
(1) **deep-verify** the current functionality actually works — run it, click it, query it; never trust the doc or the code comment;
(2) fix per the prescription (full detail in the repo: `.claude/reports/audit/<key>.md`);
(3) flip the row to **🟢 RESOLVED** with the **date+time**, the **model you were running**, and **notes on what you did**.
Many tables are near-empty — only ~15 leads exist (a few real Zillow leads + test leads); most machinery has never seen real volume.
When a feature is spec-built-but-empty, say so explicitly and add a **visual of what the spec calls for** (screenshot or mock) so
Zach can see intended vs actual. An unverifiable claim stays ⬜ — never green a row you didn't prove.</Info>

> Master: [⭐ Fable 5 Master Audit (All)](/audit/fable5-all) · Full artifact: [the State of the Union](https://claude.ai/code/artifact/d5f808e0-3ab8-4fd3-a6b2-5a615d6b82a0)


## Myli AI Layer — YELLOW (20/28 good · 8 gaps)

Full report: `.claude/reports/audit/crm-myli-ai.md`


**Headline gaps found by the audit:**

- Myli conversation engine globally OFF: ai_conversations.enabled=false, pilot_lead_ids empty
- SECURITY: live anthropic + deepgram API keys stored plaintext in hl_settings.ai_keys
- 22 ai_agent activity rows have NULL actor_kind (mig-0413 backfill missed them)
- AI Voice on Gemini Live + Deepgram STT is untracked spend (ai_voice logs zero)
- Docs stale: 4 seeded roles documented vs 7 in DB; '20 built' capability count vs actual 25


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| crm--1 | Move anthropic/deepgram keys out of hl_settings.ai_keys to env/vault, then rotate the exposed keys | ⬜ | — | — |
| crm--2 | Fix '20 built' -&gt; 25 built:true (26 total) in ai-clone.mdx; document the 7 roles + pilot-OFF state | ⬜ | — | — |
| crm--3 | Backfill the 22 NULL-actor_kind ai_agent rows via logActivity's self-derive | ⬜ | — | — |
| crm--4 | Remove dead clone_of_user_id scaffolding from agent-cards + send-identity (behavior-preserving) | ⬜ | — | — |
| crm--5 | Close the voice-cost hole: per-minute pricing for Gemini + Deepgram, one ai_voice run-log row per call | ⬜ | — | — |


## Workflows Engine — YELLOW (14/22 good · 7 gaps)

Full report: `.claude/reports/audit/crm-workflows.md`


**Headline gaps found by the audit:**

- Seats toggled ON but bound to NULL/draft/inactive workflows (fail-closed misconfig)
- Observable window: 724 trigger fires, 0 matched; only 3 workflow_runs ever (all today)
- schedule_followup runs at runtime but is disabled in the palette (lockstep drift)
- folder doc-vs-code drift + incomplete normalizer files AI Tools/Intent/System under 'Other/Custom'
- sender_default column + SELECT lingers despite doc 'removed 2026-07-13'
- Linear (non-branch) workflows run in node-array order, ignoring connections (latent canvas-truth risk)


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| crm--1 | Verify a live end-to-end fire; fix every seat where an active arm points to a NULL/draft/inactive workflow (acceptance: newest hl_workflow_runs = completed, trigger_fire matched&gt;0) | ⬜ | — | — |
| crm--2 | Add 'schedule_followup' to RUNNABLE_STEP_TYPES so the palette enables the step the runtime already executes | ⬜ | — | — |
| crm--3 | Extend familyOfFolder/FOLDER_LABEL_TO_FAMILY to cover AI Tools, Intent Workflows, System Workflows, Follow-Up (or backfill folder to canonical tokens) | ⬜ | — | — |
| crm--4 | Reconcile workflows.mdx: folder semantics, Other/Custom catch-all, sender_default removal; fold the WIP overview into the canonical guide | ⬜ | — | — |
| crm--5 | Remove the dead sender_default read from runtime.ts (do NOT drop the column without Zach approval — RULE #0) | ⬜ | — | — |


## Dashboard/Tasks/Calendar — YELLOW (14/21 good · 6 gaps)

Full report: `.claude/reports/audit/crm-dashboard-tasks-cal.md`


**Headline gaps found by the audit:**

- Dashboard default view is EMPTY: defaults to HouseLoop brand (brokerage_id=2) which has 0 of 11,636 transactions
- Global search / command bar in topbar is a disabled dead placeholder input; no command palette exists anywhere
- Conversations nav item is a bare ComingSoon stub (hl_conversations=0 rows) and is undocumented
- Dashboard default year hardcoded to 2026 (SisuDashboard.tsx:632), will go stale in 2027
- Tasks doc Note is stale: claims Assign To is humans-only, but polymorphic picker is built (TaskFields.tsx:129-147)


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| crm--1 | Fix empty default dashboard: default to SRG brand or auto-detect the brokerage that has data (SisuDashboard.tsx:629/648) | ⬜ | — | — |
| crm--2 | Build or remove the disabled topbar search box; wire to existing /api/leads/search or delete the dead control (Topbar.tsx:44) | ⬜ | — | — |
| crm--3 | De-hardcode dashboard default year to availableYears[0] instead of literal 2026 (SisuDashboard.tsx:632) | ⬜ | — | — |
| crm--4 | Decide Conversations: build+doc it, or dim/hide the nav leaf that points at a bare ComingSoon stub | ⬜ | — | — |
| crm--5 | Update stale tasks-tasks.mdx Note to reflect the built polymorphic Agents/AI/TC assign picker | ⬜ | — | — |


## Settings Surfaces — YELLOW (21/34 good · 7 gaps)

Full report: `.claude/reports/audit/crm-settings.md`


**Headline gaps found by the audit:**

- POST /api/team/users has NO auth gate — any logged-in user can create/revive a member
- PATCH /api/lead-sources reorder is ungated
- All-Settings hub grid omits ~13 shipped panels (Custom Fields, Lead Integrations, Analytics, Seller Report, IDX, Content, Audit...)
- 3 lead_sources rows have NULL platform, contradicting integration-first doc
- settings/[section] catch-all still stubs forms + webhooks as Coming Soon; doc points forms at a stub


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| crm--1 | Add requireAdmin() to POST /api/team/users:137 (and PATCH/DELETE) mirroring lead-sources:91 | ⬜ | — | — |
| crm--2 | Gate PATCH /api/lead-sources:13 reorder with requireAdmin() | ⬜ | — | — |
| crm--3 | Rebuild settings/page.tsx GROUPS to cover every shipped panel (diff vs routes + doc table) | ⬜ | — | — |
| crm--4 | Reconcile/backfill the 3 NULL-platform lead sources (no delete — constitutional) | ⬜ | — | — |
| crm--5 | Fix docs all-settings Phone section + retire stale [section] Coming Soon META for shipped panels | ⬜ | — | — |


## Mobile CRM (/m) — YELLOW (10/21 good · 8 gaps)

Full report: `.claude/reports/audit/crm-mobile-pwa.md`


**Headline gaps found by the audit:**

- NOT a PWA: zero manifest, service worker, apple-meta or theme-color anywhere - 'PWA' label is aspirational, not installable
- Call/Text/Email are native tel:/sms:/mailto: only - mobile comms NOT logged to CRM, invisible in the very feed the app shows
- Filter allowlists HARDCODED not catalog-derived: rows show under All but vanish under Website/Texts sub-filters (brushes ALL_FEED law)
- Undocumented in docs-site AND GT master; only a stale memory note (which says 'no auto-redirect' but middleware now auto-switches)
- Thin parity: only 3 tabs + add-lead are mobile-native; drawer bounces to desktop pages; lead detail is read-only (no edit/stage/note)


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| crm--1 | Document the subsystem (new docs-site mobile-crm.mdx under HouseLoop Systems + fix stale memory note); state plainly it is responsive web, not installable PWA | ⬜ | — | — |
| crm--2 | Make it a real PWA (app/manifest.ts + apple meta + theme-color + service worker) OR rename subsystem to stop the false 'PWA' promise | ⬜ | — | — |
| crm--3 | Derive feed filters from activity-badges catalog (feedEventTypes/WEBSITE_ACTIVITY_BADGES) instead of hardcoded FILTER_KINDS so sub-filters stop under-reporting | ⬜ | — | — |
| crm--4 | Log mobile Call/Text/Email into hl_lead_activity via the existing desktop comms path (REUSE_NOT_RECREATE) for comms parity | ⬜ | — | — |
| crm--5 | Add mobile lead-edit (change-stage + add-note) and server-side leads search to replace the 200-row client filter | ⬜ | — | — |


## FUB Integration — YELLOW (15/24 good · 6 gaps)

Full report: `.claude/reports/audit/crm-fub-integration.md`


**Headline gaps found by the audit:**

- Bulk import never run at prod scale: 9 FUB-linked leads total, 0 with import_batch_id
- Source sweep BUILT-not-applied -&gt; fub.people.source is a mixed raw+bucket column (drift)
- FUB reply write-back still proxies to the 'retired' dashboard (localhost:5173) -&gt; BROKEN/UNVERIFIED
- Broken docs: redirects to /fub/lead-import,/fub/scraper,/fub/trigger-events that don't exist
- Code cites nonexistent docs-site/fub/trigger-events.mdx (real file is guides/trigger-events.mdx)
- property_shared toggle is an orphan FUB never fires; stale dashboard-webhook comments in 2 routes


**The prescriptions — flip each row when PROVEN done:**

| # | Prescription | Status | Resolved (date · model) | Notes |
|---|---|---|---|---|
| crm--1 | Fix broken FUB doc redirects + code pointers so Mintlify publish doesn't abort (all-or-nothing) | ⬜ | — | — |
| crm--2 | Decide/repair the FUB reply path: port to native fub-api or document the dashboard as sanctioned host | ⬜ | — | — |
| crm--3 | Run bulk import for real (dry-&gt;test-&gt;gated full) and capture import_batch_id + Run Log proof | ⬜ | — | — |
| crm--4 | Apply or retire the source sweep so fub.people.source stops being a mixed raw/bucket column | ⬜ | — | — |
| crm--5 | Remove the property_shared orphan toggle and correct the stale dashboard-webhook comments | ⬜ | — | — |
