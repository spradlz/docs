---
title: "Docs Health (nightly)"
description: "Auto-generated nightly by the docs guardian — structural integrity, ground-truth link health, and duplicate/deprecation candidates. Nothing is ever deleted automatically."
---

<Info>Regenerated every night by `tools/docs/nightly-docs-guardian.mjs` (deterministic, $0). It flags + redlines; it never deletes. **Last run: 2026-08-12 06:30Z.**</Info>

## Structural gate

- Nav regen: 🔴 nav regen ABORTED (fail-closed guard)
- Reconcile: 🔴 42 error(s) · 17 warning(s)
  - 🔴 ORPHANED PAGE: docs-site/crm/all-settings-all-settings.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/analytics-analytics.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/business-settings-notifications.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/calendar-calendar.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/content-blog.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/content-dashboard.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/content-templates.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/dashboard-dashboard.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/email-campaigns.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/email-lists.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/integrations-api-background-jobs.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/integrations-api-connections.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/lead-capture-forms-websites.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/messaging-imessage.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/messaging-text-templates.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/phone-voice-phone-provider.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/phone-voice-voice-recording.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/pipeline-workflow-appt-outcomes.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/pipeline-workflow-appt-types.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/pipeline-workflow-custom-fields.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/pipeline-workflow-merge-fields.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/pipeline-workflow-stages.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/pipeline-workflow-tags.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/tasks-tasks.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/team-settings-audit-log.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/team-settings-my-team.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/user-settings-business-settings.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/user-settings-profile-settings.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/website-settings-analytics.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/website-settings-google-ads.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/website-settings-idx-connection.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/website-settings-pixel-tracking.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/website-settings-registration-settings.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/crm/website-settings-seo.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/tc/email-queue.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/tc/forms.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/tc/milestones.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/tc/offer-writing-tool.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/tc/tasks.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/tc/tc-working-status.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/tc/transaction-status.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).
  - 🔴 ORPHANED PAGE: docs-site/tc/transaction-types.mdx exists but is in NO nav group (fell out of the nav — add it to docs.json or delete it).

## Ground truth

- ✅ Every `GT/GT - ACTIVE/*` reference in the docs resolves to a real file.

## Duplicate / deprecation candidates

Deterministic body-overlap detection (copy-paste level). Paraphrased overlap is caught by the weekly LLM audit. Rows at or above the redline bar are marked **Deprecated** in the nav + carry a banner; the rest are here for your eye only.

| Overlap | Keep (canonical) | Flagged / deprecated | Redlined? |
|--:|---|---|:--:|
| 0.885 | `legacy/audit/archive/2026-07-06/fable5-all` | `legacy/audit/fable5-all` | 🔴 yes |
| 0.829 | `legacy/ai-tools/cursor` | `legacy/ai-tools/windsurf` | 🔴 yes |
| 0.633 | `legacy/ai-tools/claude-code` | `legacy/ai-tools/cursor` | — |

_Redline bar: overlap ≥ 0.4. Report bar: ≥ 0.22. Never deletes — every action is a reversible git commit._
