---
title: "The Client Experience"
description: "THE one aggregate home for every client-facing view — the user dashboard (buyer + owner lenses), the Home Value Report, and every surface Myli appears on. Other sections point HERE."
---

**🕐 Last updated 2026-08-12 · dashboard_unify.**

**This page is the single place to see the whole client experience.** Every client-facing design lives
as a hosted CRM page (the Command Center protocol — artifacts are account-locked scratch; the CRM HTML
is the truth), and this page is the map. When another doc needs to reference a client view, it links
here — no copies, no siblings.

> Locked with Zach **2026-08-11** in a single design session: four directions → the Combine → the
> three-act page → the unified two-lens dashboard, with Myli AI first-class throughout.

> **Built & live 2026-08-12 — v1, the buyer / search side.** The light-brand **Home Search Dashboard**
> is shipped at `houseloop.ai/my-houseloop`, behind a new **phone-code login** (no shareable magic
> link — a 6-digit SMS code, verified once, on a 90-day device session). v1 deliberately OMITS the
> not-yet pieces (the Myli-ask bar, What's-My-Home-Worth, payment / pre-approval) — those return per
> the vision above. The agent works it from the CRM iPad **Search** tab (a full Search Dashboard modal
> that opens over the client card) and a far-right **Client Dashboard** tab that renders **the client's
> own component** — see the next section. Proven end-to-end on prod 8/12 (real SMS code → session →
> dashboard). Locked wireframe: `docs/wireframes/saved-search-redesign-2026-08-12.html`; E2E receipts:
> `.claude/reports/home-search-e2e/`.

## The agent's mirror — ONE component, two apps (2026-08-12)

**The CRM's `Client Dashboard` button renders the same React component the client sees at
`houseloop.ai/my-houseloop`.** Not a port of it, not a lookalike — the component itself
(`HomeSearchDashboard`), which is pure and props-first, fed the CRM's data by
`apps/houseloop-crm/lib/dashboard/to-client-dashboard-props.ts` from `GET /api/leads/[id]/dashboard`
plus the lead's saved searches. It embeds with `showTopBar={false}` (the modal already carries the
chrome) and `linkTarget="_blank"` (an agent clicking a home never loses the CRM page).

Until 8/12 that button rendered a **placeholder**: a vertical stack with no rails, an agent card
hard-coded to "The Spradling Group", and `Message` / `Call` buttons that were `disabled`. The agent's
"what the client sees" mirror showed a screen no client has ever seen. The swap was the code's own
`TODO(shared-render)`, unanswered from the day the web build landed.

### Why the CRM carries a GENERATED copy, and why you must never edit it

The two apps **build in disjoint Docker contexts** — each `Dockerfile` does `COPY . .` from its own
app directory, so neither image contains the other app's source **or** `packages/`. They are also on
different Next majors (CRM 15.5.x, web 16.2.x) with separate `node_modules`. A cross-app import
compiles fine under `next dev` and then **breaks the Railway image**.

So the component is authored **once**, on the website, and mirrored into the CRM's build context by a
script:

```bash
node tools/shared-ui/sync-client-dashboard.mjs           # regenerate the CRM copy
node tools/shared-ui/sync-client-dashboard.mjs --check   # exit 1 if it has drifted
```

`--check` runs in the pre-commit hook (CHECK 8) whenever either side is staged, so a stale copy
**blocks the commit**. The generated files carry a `⚠ GENERATED FILE — DO NOT EDIT` banner naming
their source. **Edit `apps/houseloop-web/components/dashboard/HomeSearchDashboard.tsx`, then run the
script** — editing the CRM copy fails the gate. The team agent card lives on the shared component as
`TEAM_AGENT` for the same reason: one definition, so the mirror can never name a different agent than
the client's card does.

This is an interim mechanism for a deploy-topology problem. The real fix — one workspace package plus
a repo-root build context for both images — is written up for Lee and Zach in
`.claude/reports/project-audit/cross-app-shared-ui-infra.md` and is **queued, not built**.

### What the mirror can and cannot know

The client's live screen is fed by the **WEB** database (`ListingView` / `SavedListing` /
`TourRequest` / `SavedSearch`); the CRM mirror is fed by the **activity log**, and the CRM never
reaches across (the two-database law). Same render, the CRM's record of the same behaviour. Two
fields have no CRM source and render **blank rather than guessed**: a tour's private note, and a
future scheduled tour (the log records that a tour was *requested*, not when it is booked, so every
toured card reads past-tense).

### The defect this uncovered — every home viewed ON THE SITE was being dropped

`GET /api/leads/[id]/dashboard` read the address as `meta->'property'->>'address'`. But the
**website-pixel writer** — the exact behaviour this dashboard exists to show — spells it in parts
(`street` · `city` · `state` · `zip`) and has **no `address` key at all**. Every pixel-sourced home
resolved to `NULL` and was filtered out. Measured on one lead: **18 of 18 views dropped**, the rail
rendering "no homes yet" while the photos and prices sat in the tape.

The read now composes the line from the parts (guarded on `street`, so a row with only a city can
never become an "address"), taking property-row address coverage to **22,461 of 24,652**. A second
fix in the same pass: one rooftop arriving under two address spellings rendered twice — the adapter
now collapses on the canonical slug and adds the view counts up.

<Info>
**Both were found by walking the surface in a real browser, not by reading the code.** The endpoint
was reviewed as "solid" in the audit that scoped this work — and on the code it was. The empty rail
only appears when you open it on a lead whose homes came from the pixel.
</Info>

## The one experience — how it fits together

One dashboard at `houseloop.ai/my-houseloop`. **Data decides the lens** (like the value fork —
never a setting): a closing with us OR an added home → **owner lens**; otherwise → **buyer lens**.
A buyer who closes graduates automatically. Both lenses share one spine — hero photos, the navy
band, horizontal rails, a pinned right rail, and the Myli dock — with the sections reweighted.

| Surface | The locked design (hosted, account-proof) | What it is |
|---|---|---|
| **The User Dashboard** (the synthesis) | [crm.houseloop.ai/boards/user-dashboard](https://crm.houseloop.ai/boards/user-dashboard) | Both lenses on one spine: Timothy (buyer — buying power band, tour/saved/viewed rails, pre-approval unlock) and Heather (owner — value range band, journey feed, walk-away unlock). Real homes, real photos, real logos. |
| **The Home Value Report UI** (owner deep-dive) | [crm.houseloop.ai/boards/home-value-report-ui](https://crm.houseloop.ai/boards/home-value-report-ui) | The full locked owner design: three acts, the range band with the married graphs, the journey feed, the unlock funnel, the walk-away reveal, the split Myli dock. |
| **Myli — AI Companion UI** (her whole identity) | [crm.houseloop.ai/boards/the-myli-book §5](https://crm.houseloop.ai/boards/the-myli-book#s5) (the companion UI board folded into the Myli Book; no redirect, Zach #257.11) | Her Pixar avatar (from the real headshot), the tagline system, the introduction suite, tap-open + first-visit flows, and the Myli × Fountain financing loop. |
| **The recovered original** (reference) | [crm.houseloop.ai/boards/your-houseloop-dashboard](https://crm.houseloop.ai/boards/your-houseloop-dashboard) | The two-view design rescued from the other account 8/11 — source of the horizontal-rails and pinned-right-rail insights. |

## Everywhere Myli appears — the one view of her experience

Myli AI is the interaction layer of every client surface. Her name is **never naked** — always
"Myli AI · your home **value / buying / selling** companion" (the role rotates by surface), always
the same avatar, and her first message introduces her.

| Where | Her role there | Designed in |
|---|---|---|
| The dashboard dock (desktop, 75/25 split) | Title banner + the freshest-signal greeting + question pills; the side door beside her carries THE one human CTA | user-dashboard · home-value-report-ui |
| The mobile sticky bar | Compact bar, unread badge ("1 new about your home"); the sheet slides up on tap | home-value-report-ui |
| First contact | She introduces herself — "I'm the AI side of The Spradling Group, powered by HouseLoop" | myli-companion-ui |
| Every rail card | **Questions are the interface** — each card's action is a question chip wearing her face ("What's the payment?" · "Why did it go up?") | user-dashboard |
| The unlock funnels | The walk-away number (owner, 3 taps) and instant pre-approval (buyer, 3 questions via Fountain Mortgage) are HER funnels | user-dashboard · myli-companion-ui |
| The comps deck | The curiosity law: the dashboard never dumps comps — she reveals them on ask | home-value-report-ui |
| **The anonymous home page — "Meet Myli" (V5, the merry-go-round carousel)** | Free-floating (no card outline), her portrait far left (bigger on desktop — #257.347c), a 3D coverflow of her homes centered on the right — THE WEBSITE HOUSE CARD in every slot, exactly three ever visible (the front of the wheel: outer edges recede), the last position a blue-outlined Myli CTA end card. A caption pill + its "pro" line pop ABOVE the wheel on every center-index change, cycling her five locked lines ("She understands you" · "She sees more" · "She stays with you" · "She finds what fits" · "She can create saved searches"), each with its own icon and sub-line. The centered card carries a brand-blue ring and a small icon-only badge (never the letters "AI"). Her aura is baked into her own portrait art, not a CSS glow. The big "Get started with Myli" button, then "What Myli knows" — a light surface panel (never navy), brand-blue numbers and bar, always the honest zero state here (no session exists to read for). Phone: she gets her own section (just her, one line, one bubble), then the same carousel, then the same panel. | `docs/wireframes/website-wireframes-2026-09-08.html` `id="p6"`, rulings #257.328a/.329/.334/.347/.347c — `components/home/MyliShowcaseCard.tsx` + `home-metrics.ts` |

## The Myli chat window — threads, the wave, and the lending-partner rule (#257.346, 2026-09-08)

Zach's affordability walk on prod (ruling #257.346) found nine defects in one chat; this is what the chat window does now, and the rule behind each.

| What you see | The rule |
|---|---|
| **A thread column** — a rail on desktop, a drawer behind the history button on the phone — listing this visitor's chats newest first, titled by what they were about ("What can I afford · Sep 8"), **New chat** on top. | The rail is the existing history road (`GET /api/search/nlp/history`, one row per conversation in `hl_ai_search_conversation`, keyed by the visitor cookie / the user / the lead; anonymous rows attach at sign-in). No new storage. |
| **Tapping a pill whose chat is already open resumes it** — the number stays, the next step is where she left it. It never asks question 1 again and never re-runs the estimate. In a new window the tap opens clean and the old chat sits in the rail. | The flow entry checks the thread in this tab before it sends a turn; the second-window law (no replay) stands. |
| **Her reaction stays; the thinking line is its own line under it; the answer renders under both.** The reaction ("Nice — $8,000 a month is solid ground") is a bubble of its own for the rest of the thread; the grey status line collapses to "Worked for Ns". | The streamed ack is persisted as its own assistant bubble on `done`; the wait box renders only the status line and the draft. |
| **Under a funnel pill the ack says what is about to happen** ("four quick questions, then your number"), never "running your numbers now". | The ack call is handed a FACT: whether the next turn is a question (and which) or the computation — context, never a sentence. Judge row `FUNNEL_ACK_NAMES_THE_QUESTION`. |
| **One way to answer a funnel question** — the slider or the chips, plus "Not sure" — never both, never an escape or intent pill under a question, never a slider that comes back after you answered it. | The escape binds only under a free-text funnel question; a typed answer under a question counts as committed. |
| **Numbers keep their meaning.** A credit band is "a 700–739 credit score", never a price range; the credit chips carry the calculator's own bands (excellent 740+ · good 700–739 · fair 640–699 · building under 640). | Judge rows `NUMBERS_NAME_THEIR_CONCEPT` · `CREDIT_CHIPS_ARE_THE_ESTIMATORS`. |
| **No lender is ever named.** She says she can connect you with one of our local lending partners who can lock in your real rate and get you pre-approved; the lender introduces themselves. | RESPA, Zach verbatim: "we don't mention John Nelson and we don't say he's a teammate." The roster name is stripped from every model-facing string (the lender facts block, the tool notes, the charter, the SEO FAQ); judge row `RESPA_NO_NAMED_LENDER`. |
| **New chat is clean** — no ack, no funnel state, no stale "Worked for Ns". | New chat resets the wait state with the thread. |
| **QA records the stream.** Every walk of this window carries a video and a 100 ms text timeline of the chat column, because these defects live between frames. | `THE_GOAL_WALK` amendment; the walker lives with the lane's receipt. |

## The design laws (locked 8/11 — apply to every client view)

- **The hierarchy law:** genuine value first — the house value is the hero, Myli provides the
  intrigue and interaction, the human CTA is a small sub-thing. Never a lead-capture page.
- **CTA singularity:** ONE standing solid button per lens, page-wide — **Schedule a selling
  consultation** (owner) · **Schedule a tour** (buyer) — living in exactly two homes (pinned rail +
  dock side door). Everything else is a Myli question chip.
- **The range law:** OUR value renders Low · Estimated · High over the narrow-bar bell curve;
  Zillow and Redfin stay single hard numbers (their real logos shown). A range never corners us.
- **Color doctrine:** blue = the brand instrument (range, bell, markers) · green = money grown
  (the history line, gains, equity milestones) · never decoration.
- **The number budget:** money figures live in the navy band only; everywhere else speaks language.
- **The unlock rule:** never headline "thinking about selling?" — lead with the reward ("What would
  you walk away with?"); every funnel step has a one-tap "no worries" default and can never dead-end.
- **The journey feed:** their milestones + curated similar-solds, ONE differentiating fact per card,
  the client draws the conclusion; the origin card ("you bought it with us") anchors every scroll.
- **Rails + the pinned rail:** sections swipe horizontally, never stack into a scroll graveyard;
  searches, your home, and the team card never leave the screen.
- **No-wrap pills · logos are images · the identity rule** (person card only when the relationship
  is truly theirs, else the team lockup) — all carried from the source pages above.

## How this section is maintained

Iterate fast as claude.ai artifacts (disposable, account-locked) → on Zach's sign, **promote to the
CRM via `tools/boards/promote.mjs --section "Website"`** (same slug = updates in place) → this page
links the hosted truth. The [Command Center](/command-center) is the full index across all teams;
this page is the client-experience view of it. Full protocol: the Command Center page itself.
