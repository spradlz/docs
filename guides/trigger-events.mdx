---
title: "Trigger Events"
description: "THE trigger events page — trigger events are CONFIGURED not created (⋮ → Configure), each with a two-arm New Lead / Existing Lead workflow+notification, grouped by integration section with Activity·Communication·Insights bands, plus drag-reorder and the hl_trigger_fire ledger."
---

**In the app:** Settings → **Trigger Events** — [`/settings/trigger-events`](https://crm.houseloop.ai/settings/trigger-events)

<Info>
**THE CANONICAL TRIGGER-EVENT CATALOG (Zach's law, 2026-07-17): every trigger event, every family, lives on
THIS page** — HouseLoop.ai website · the inbox lane · Follow Up Boss · Transaction Coordination · AI Tools ·
insights. **Running-document law:** the update log below is newest-first; every change stamps an entry AND
re-edits the body to current truth. The matrix below is a CODE AUDIT (2026-07-17, three agents, every claim
traced to file:line) — 🟢 WIRED = a real emitter fires it · 🟡 PARTIAL = fires but incompletely ·
🔴 VIBE = the row exists, NO code fires it · ⏳ PENDING = wired, waiting on external (a number port).
</Info>

## Update log

- **2026-07-17 (v2)** — Matrix rewritten to mirror THE LIVE BOARD 1:1 (Zach: "it doesn't match my
  screen") — same sections, same rows, same order, one color per row.
- **2026-07-17** — THE FULL WIRING AUDIT (3 code agents), every event wired-vs-vibe with receipts.
  Fixed live: Zach's FUB seat text door (mig 0418 seat + router text-parity wire). Flagged: the tc_*
  workflow arms are notification-only (🔴) · 8 hidden catalog keys with no emitters (not on the board —
  deletion queued) · a dead pixel-side insight emitter. FUB event family moved here from the retired
  fub/trigger-events (canonical-catalog law).
- **2026-07-16** — THE INBOX LANE shipped: per-inbox board sections, per-line fires (lead-less included),
  creates-lead mints at ring — live-proven on an organic Zillow caller.
- **2026-07-13** — The Configure model (events configured, never created) + the fire ledger.

## ⭐ THE CANONICAL MATRIX — the board, row by row, wired vs vibe (code audit 2026-07-17)

**This table mirrors the live board 1:1** (same sections, same rows, same order as
`/settings/trigger-events`). 🟢 = the row genuinely fires (emitter verified at file:line) · 🟡 = fires
partially / with a caveat · 🔴 = looks functional but is not · ⏳ = wired, waiting on an external event.

### 🌐 HouseLoop.ai Website (16)

| Row | Fires from | Status |
|---|---|---|
| Tour Request | site showing-request → `process-intake.ts:187` | 🟢 |
| Contact Request | contact-agent form → `process-intake.ts:188` | 🟢 |
| Direct Registration | sign-up / Google sign-in / manual Add Lead → `process-intake.ts:199` · `api/leads/route.ts:365` | 🟢 |
| Seller Get Report | seller inquiry/claim + seller-demand pixel → `process-intake.ts:192-194` · `pixel/track:248` | 🟢 |
| Saved Search | lead saves a search → `process-intake.ts:196` (system-seeded starters deliberately don't fire) | 🟢 |
| Seller Get Started | the seller flow → intake base key | 🟢 |
| Saved Home / Liked Home / Shared Home | pixel save/like/share + intake → `pixel/track:250-252` | 🟢 |
| Text Received | inbound SMS/iMessage → `log-message.ts:281` (one choke point) | 🟢 |
| Phone Call Received | inbound call → voice webhook (`fireInboxDoor`, lead-less incl.) | 🟢 |
| Email Received | inbound email → `gmail-realtime.ts:222` + `inbound-sync:105` | 🟢 |
| Re-Engaged / Viewed 10x in 7 Days / Looking in a New Area | the hourly `insight-engine` cron over OUR pixel data | 🟢 (⚠ a pixel-side repeat-views detector emits a dead event name — the cron is the real emitter; cleanup queued) |
| Property Viewed (Insights band) | pixel view, identified leads → `pixel/track:249` | 🟢 |

**All 16 rows on your board are REAL.** (The audit also found 8 hidden catalog keys with no emitters —
`general_registration`, `manual_enrollment`, `saved_search_agent/system`, etc. — they do NOT render on
the board; they're catalog debris queued for deletion, not lies you can see.)

### 🔗 Follow Up Boss (19)

| Row | Fires from | Status |
|---|---|---|
| Property / Tour / Contact / Rental / Seller Inquiry | FUB `eventsCreated` → router classifier | 🟢 (Tour count 7 = real fires) |
| Virtually Toured | `eventsCreated` (wired 2026-07-16 — was silently dropped) | 🟢 |
| Property Viewed / Property Saved | FUB `notesCreated` ("Property Viewed/Saved" notes) | 🟢 (175 / 25 — the firehose is live) |
| Property Shared | listener wired — but FUB has NEVER emitted a share (zero in the 400k-person mirror; extinct upstream) | 🟡 wired-but-extinct |
| Text Messages | `textMessagesCreated` (masked bodies → Lane-2 recovery) | 🟢 |
| In-App Messages (Zillow) | Lane-2 recovered body → `fub-body-recovered.ts:100` | 🟢 (count 1) — depends on the scraper's body recovery |
| Notifications (generic notes) | `notesCreated` unclassified | 🟡 notify-only BY DESIGN (comms policy: a human FUB note is not website activity — no feed card) |
| Email Received (reply) | `emailsCreated` | 🟢 |
| The 5 Insights (High-Intent Buyer/Seller · 10x/7d · New Area · Re-Engaged) | `notesCreated` Zillow-wording regexes | 🟢 (HI-Seller 1 · New Area 3 = real fires) |

### 🤝 Transaction Coordination (11)

| Row | Fires from | Status |
|---|---|---|
| Document Sent / Opened / Signed | e-sign send + Documenso webhooks → the full door path (ledger + canvas) | 🟢 |
| The 8 loop events (Loop Created · Under Contract · Milestone · Closed · Lost · Status Changed · TC Assigned · Key Date) | `fireLoopNotification` in the loop engine | 🔴 **the workflow arm is fake** — these fire NOTIFICATIONS only; no door, no ledger row, a bound workflow will never run, the count stays 0 forever. Needs real doors wired (decision pending). |

### 🧰 AI Tools (20)

| Row | Fires from | Status |
|---|---|---|
| Create a Saved Search → **Saved Search Setup** · Schedule an Appointment → **Appointment Set** | `execTool` → tool-fire ledger + `aiDoorIfBound` → the bound canvas runs | 🟢 |
| The other 18 tools | the same choke point writes the tool-fire ledger on every use; the DOOR fires only when an active workflow is bound (none yet — "None — set via ⋮" is honest) | 🟡 ledger-real, workflow-unbound (bind a canvas → it goes green) |

### 📞 The Inbox sections (Company 5 · Past Agents 2 · Ana 2 · Zach 2)

| Row | Fires from | Status |
|---|---|---|
| Company — call + text (320-0906 · 354-8600) | voice/sms webhooks: fire on EVERY inbound (lead-less incl.), mint-at-ring on Creates-Lead | 🟢 |
| Company — Email Received | matched leads only (realtime + poller) | 🟡 per-address creates-lead seats = next build (the row says so) |
| Past Agents — call + text (675-9877) | same webhooks; `ForwardedFrom` = whose old line | 🟢 (count 1 = the organic Zillow caller, live-proven) · text note: forwarding texts across the FUB hop depends on carrier; direct texts fire |
| Ana — call + text (954-7123) | seats ready | ⏳ number ports ~2026-07-21; fires nothing until it lands |
| Zach seat — call (490-2988) | FUB `callsCreated` → seat resolve → inbox fire + mint | 🟢 |
| Zach seat — text (490-2988) | FUB `textMessagesCreated` → seat resolve (seat mig 0418 + router parity, SHIPPED today) | 🟢 fresh — first fire pending |

## 2026-07-13 — SHIPPED: trigger events are CONFIGURED, not created

**The model, in one breath (Zach locked it live):** a trigger event is **ONE row per human action**, named exactly
what it is — Contact Request, Saved Home, Property Viewed. **You never hand-create a trigger event.** Every event
either comes from a **lead integration** (HouseLoop.ai, Follow Up Boss, Company Inbox, Transaction Coordination
auto-populate their events) or is **Manual**. What you do to a row is **Configure** it — the ⋮ (kebab) menu on each
row is **Configure** + **Pause**. There is no "Create Trigger Event" modal anymore.

### The Configure modal — two arms

**Configure** opens a modal with up to **two arms**, and each arm is exactly **one workflow + one notification + an
on/off**:

- **New Lead arm** — shows **only when the event can mint a lead** (the identity gate, `target.canMint`). It carries
  a workflow dropdown (+ *create* jumps to the builder), the automatic **"New Lead" notification class** (fixed),
  and its on/off. When the event can't mint, this arm is hidden.
- **Existing Lead arm** — always shows. Workflow dropdown (+ *create* → builder), a **notification dropdown** (the
  full catalog, see below), and its on/off.

**Single-arm events.** When an event can't mint (`canMint=false`), only ONE arm renders and the modal reads
**"Existing Leads Only"**:

- **HouseLoop Insights** (the computed-pattern band) is **single-arm "Existing Leads Only"** — an insight is always
  about a lead we already know (hard-locked existing).
- **Transaction Coordination** is **single-arm "Existing Leads Only"** — a loop always hangs on an existing lead.

The arm's three flags (`creates_lead` / `workflow_active` / the workflow id, and the existing-arm's
`existing_workflow_*`) are written as a unit through `PATCH /api/trigger-events`. Config lives in
`components/settings/trigger-events/ConfigureTriggerEventModal.tsx`.

### The notification dropdown is sectioned

The Existing-Lead arm's notification picker is the **full notification catalog**, grouped into the **same gray band
headers the Notifications settings page shows, 1:1** (New Lead Activity · Existing Lead Activity · Communications · …)
— driven by `lib/notifications/notification-sections.ts` and rendered with the sanctioned `HlDropdown` (a bare
`<select>` can't draw gray dividers). The New-Lead arm's notification is the fixed "New Lead" class.

### Sections = integrations · in-table bands = Activity / Communication / Insights

The board groups every trigger event under its **integration section** (a blue section header), and inside a
section, Excel-style band rows split the events by TYPE:

| Section (integration) | What it holds |
|---|---|
| **HouseLoop.ai Website** | the native website events — browsing, inquiries, registration, and the computed insights |
| **Follow Up Boss** | FUB webhook signals — the full FUB event family is cataloged [below](#the-fub-family--every-fub-trigger-event-moved-here-from-fubtrigger-events-2026-07-17); the pipeline lives on the [⭐ FUB SSOT](/fub/master-scraper-ssot). **Zero phone rows**: phone triggers live in the inbox sections (2026-07-16) |
| **Inbox sections** (one per inbox with doors) | **Company Inbox** (913-320-0906 · sign 913-354-8600 · email) · **Past Agents Inbox** (913-675-9877, the 37-number FUB pool) · **personal inboxes** (Zach's FUB seat 913-490-2988 · Ana legacy 913-954-7123). Rows chaptered 📞/💬/📧; a FUB-carried door's line reads "FUB …" |
| **Transaction Coordination** | e-sign documents + loop milestones (existing-lead only) |
| **Manual** | hand-run events with no integration (the catch-all — "Other/Custom" was retired) |

The in-table **bands** come from the `band` column on `hl_trigger_events`:

- **ACTIVITY** — everything the person DOES (browsing + inquiries + registration).
- **COMMUNICATION** — an actual communication received (Text / Call / Email Received — LIVE wires fired by the
  inbound writers, minting through Company Inbox).
- **INSIGHTS** — computed patterns. **Property Viewed now lives in the Insights band** (moved there in **mig 0395**).

### Drag-reorder persists (sections AND events)

Both the **integration sections** and the **individual trigger events within them** can be **drag-reordered**, and the
order **persists to `hl_lead_flow_section_order` (mig 0396)** via **`/api/lead-flow/order`**. The Trigger Events board
and the Workflows page share ONE order — reorder on either, it holds on both.

### Native insights (Zillow-parity rules)

The computed insights — **Viewed Home 10x in 7 Days** (the same home) · **Looking in a New Area** (2+ views in a ZIP
never browsed in the prior 60 days) · **Re-Engaged** (active after 14+ days of silence) — are computed hourly by the
registered Inngest fn `insight-engine` over our own pixel data (deterministic SQL, receipts in every fire, a hard
14-day per-lead-per-insight gate checked against `hl_trigger_fire`, no tags). Thresholds live in `hl_settings` key
`insight_engine`. An insight only ever aggregates patterns — it never re-fires a single event that has its own trigger.

## What a trigger event is

A thing that happened (someone did something) that can **create a lead** and/or **fire a workflow**. Every trigger
event is a row on the board with its own arms. It is **auto-populated by its integration or Manual** — you Configure
it, you don't create it.

## THE ONE-SWITCH LAW (workflow on/off lives HERE)

**A workflow is turned on or off in exactly one place: its arm's on/off inside the trigger event's Configure modal on
this board.** Flipping an arm drives that workflow's real status (`hl_workflows.status`) — the Workflows page shows a
**read-only** status badge that mirrors this toggle, never an independent switch. Locked infrastructure workflows
(e.g. saved-search alert delivery) are always-on and exempt.

The event itself always fires once it exists — the arm toggles gate the lead and the workflow, never the firing.

## THE FIRE LEDGER — `hl_trigger_fire` (why the count never lies)

Every genuine door dispatch writes **exactly one row** to `hl_trigger_fire` (`lead_id` · `event_key` · `platform` ·
`matched` · `routed_by`), written by `dispatchDoor` in `lib/workflows/runtime.ts` — the one place every
`houseloop/door.<key>` flows through.

- The board's **TRIGGERED count** and its **click-through drill-down** ("which leads fired this") read this SAME table
  — they agree by construction.
- The native board is scoped `platform <> 'follow_up_boss'` — a FUB-origin fire can never appear on a HouseLoop.ai row
  (and vice versa).
- **Only a dispatch is a fire.** The lead activity feed is history, not a fire source — imported FUB history,
  system-seeded searches, and replayed pre-registration browsing can never masquerade as trigger fires.
- `lead_id` is `ON DELETE CASCADE` — deleting a lead removes its fires.

## THE INBOX LANE — every call/text to an owned line FIRES, and creates-lead MINTS AT RING (2026-07-16)

Live-proven the day it shipped on an organic Zillow caller (Sachind Jamesh → Alisha Hanks' old number).

- **Every inbound call/text to an owned line writes a trigger event — lead-less included** (50 calls = 50
  fires; a spam-gated call still fires with `routed_by='spam-gate'`; our own pool/dialer numbers are plumbing
  and never fire). Fired by the voice/sms webhooks via `fireInboxDoor` (`lib/workflows/fire-door.ts`):
  lead-bearing fires ride the door → `dispatchDoor`; lead-less fires write `hl_trigger_fire` directly
  (lead_id NULL), stamped `source = the line's lead-source key` — which is exactly what each per-line board
  count keys on (`event_key × source`).
- **Creates-Lead is real**: unknown caller + the line's toggle ON → the lead is minted DURING the ring
  (`lib/leads/mint-phone-lead.ts` — one writer, shared with Myli's wrapup; the caller's NAME resolves from
  the FUB people mirror at ring time via `lib/phone/caller-context.ts`). Minted → the door fires
  `created:true` (New-Lead arm); matched → `created:false`. Phone sources carry owner + stage defaults
  (mig 0416) — a phone-minted lead is never ownerless.
- **`seatSource` tier-0**: the door dispatch resolves the LINE's seat (`hl_lead_source_events` by source
  key), so a website-born lead calling the company line routes the company line's workflow arm — not its
  birth source's. Fail-closed like every other tier.
- **ONE call = ONE trigger event — the lane that ANSWERS the number fires it.** Calls landing on our Twilio
  (company / sign / past-agent pool / Ana) fire from OUR voice webhook; FUB's `callsCreated` echo of those
  calls is skipped entirely. FUB-native lines (Zach's seat 913-490-2988, never porting) fire from the FUB
  router into the inbox lane (`suppressLedger` keeps the import from writing a second row).
- **Which past agent's line**: Twilio `ForwardedFrom` rides the FUB forward hop — the fire payload, the
  call row, and the notification all know "past client calling Alisha Hanks' old number."
- **Email**: `email_received` fires for matched leads from BOTH the Gmail realtime path and the inbound-sync
  poller. Per-address lead-less email seats are the documented next build (the board says so honestly).

## Routing (what happens when a door fires)

`dispatchDoor` resolves the workflow through strict tiers and **picks the arm from the dispatch's created-flag**:
`created=true` reads the New-Lead columns, `created=false` the Existing-Lead columns — fail-closed, so an unbound or
toggled-off arm is a logged skip, never a fallback guess. **Registration doors route ONLY via their per-source
assignment** — the fallback tiers carry a `*_registration` guard so a first-touch outreach workflow can never fire
from a fallback match (the guard that closed the 1,421-import near-miss, 2026-07-09).

## Where the messaging lives

What each door actually SENDS (the two-beat: brand ack + Myli at 2 minutes) is documented on the
[HouseLoop.ai Lead Integration](/integrations/houseloop-ai) page, message by message.


## THE FUB FAMILY — every FUB trigger event (moved here from fub/trigger-events, 2026-07-17)

The FUB *pipeline* (capture → import → sync → scraper) lives on the ONE
[FUB SSOT](/fub/master-scraper-ssot); the events themselves live HERE, canonical.



> Count, honestly (2026-07-16): **19 FUB rows live** — the original 23 minus the 4 phone subtypes deleted by
> mig 0415 (phone triggers moved to the board's inbox lane; the generic `phone_call` row survives as hidden
> notify plumbing and renders nowhere). Earlier copies of this doc said "22" and "23" in the same breath —
> that drift dies here: the table below is one row per live `hl_fub_webhook_settings` row.

**This is the master map.** One row per live `hl_fub_webhook_settings` row. Read a row left→right and you
have the whole story: *the door → the FUB signal that creates it → the notification class it rolls up to →
the card the agent sees.* Every event carries the **four toggles** (Sync · Creates Lead · Workflow · Notify)
— ✅ **LIVE since 2026-07-09 night**: `_global` master ON; `property_viewed` notify muted by Zach (the
firehose). **Creates Lead governs PER EVENT (Zach 2026-07-11 — no hardcoded never-mint list):** a behavioral
signal (save/view/in-app) MINTS a new HouseLoop lead when its own `creates_lead` is ON, exactly like the
inquiry rows — see [Behavioral signals can mint](#behavioral-signals-mint-when-creates-lead-is-on) below.

#### How a row becomes a card (the chain every event follows)

```
FUB fires a webhook → POST crm.houseloop.ai/api/fub/webhook → fubWebhookRouter classifies it
  → the hl_fub_webhook_settings ROW gates it (Sync? Creates Lead? Workflow? Notify?)
  → notify ON → its notify_class row in hl_notify_classes renders the One-Card
  → the lead OWNER's bell + SMS + email (per their /settings/notifications prefs)
```

#### Registration-family (a first touch — can mint a new lead)

FUB lumps all lead-initiated property contact under ONE event type (**Property Inquiry**); the real intent lives
in the message text + source, and `classifyFubEvent` (`lib/fub/classify-fub-event.ts`) splits it into the atomic
doors below (mig `0285`). Any doc/code still treating "Property Inquiry" as one door is stale.

| # | Trigger event | Created from (the FUB signal) | Notification class | The card |
|---|---|---|---|---|
| 1 | `tour_request` | `eventsCreated` · Property Inquiry with tour language ("interested in touring/seeing X") | `lead_created` | 🏠 *registered* (`{{contact.full_name}}`) |
| 2 | `contact_request` | `eventsCreated` · Property Inquiry with reach-out language ("have an agent call me") | `lead_created` | 🏠 *registered* |
| 3 | `rental_inquiry` | `eventsCreated` · Property Inquiry, source **Zillow Rentals** | `lead_created` | 🏠 *registered* |
| 4 | `property_inquiry` | `eventsCreated` · Property Inquiry (the default — "I am interested in `<addr>`") | `lead_created` | 🏠 *registered* |
| 5 | `seller_inquiry` | `eventsCreated` · **Seller Inquiry** ("I want to sell my home") | `lead_created` | 🏠 *registered* |

#### System-family (activity on an existing lead)

| # | Trigger event | Created from (the FUB signal) | Notification class | The card |
|---|---|---|---|---|
| 6 | `property_saved` | **`notesCreated`** — FUB logs each save as a note with the address ⚠️ | `home_saved` | 🔖 *saved a home* |
| 7 | `property_viewed` | **`notesCreated`** — same note mechanism ⚠️ | `property_viewed` | 👀 *viewed a home* |
| 8 | `property_shared` | ⚠️ signal essentially extinct — 4 share notes EVER in the mirror, last Jan 2025 (no share events exist). Row stays for the board; expect ~never | `home_shared` | 📤 *shared a home* |
| 9 | `virtually_toured` | `eventsCreated` · Virtually Toured Property (passive 3D tour) | `property_viewed` | 👀 *viewed a home* |
| 10 | `text_message` | `textMessagesCreated` (real SMS; ~7% of bodies masked → Lane 2) | `new_message` | 💬 *sent a message* |
| 11 | `in_app_message` | ⚠️ **NOT exposed by the FUB API** (falsified 2026-07-09: all 10,588 `zillow_inapp` mirror rows are scraper-minted ids; zero ever arrived via `textMessagesCreated`, and only 33 relay emails ever). Native capture needs its own build — currently the FUB-UI scraper was the only source | `new_message` | 💬 *sent a message* |
| 12 | `email_received` | `emailsCreated` (body mostly masked → Lane 2 recovery, then notify WITH body) | `email_received` | 📧 *replied by email* |
| 13-17 | ~~phone rows~~ **MOVED TO THE INBOX LANE (Zach 2026-07-16)** | Phone triggers live on the [Trigger Events board's per-inbox sections](/guides/trigger-events), never on the FUB board. Rows 14/15/17 (company · sign · Ana) were DELETED (mig 0415 — those numbers ported out of FUB). The past-agent pool fires from OUR Twilio front door (913-675-9877, `ForwardedFrom` says whose old line); Zach's FUB seat (913-490-2988, never porting) fires from the FUB router INTO the inbox lane (`suppressLedger` = one call, one ledger row). The generic `phone_call` settings row survives ONLY as hidden notify plumbing for other FUB seats' call cards — it renders nowhere and its `callsCreated` echo of Twilio-landed calls is skipped entirely (one call = one trigger, the lane that answers it fires it). The call card prints FUB-style facts (`signalCall`; the Donna Lemmons Lewis stale-classification fix, 2026-07-16). | — |
| 18 | `notification` | `notesCreated` — every generic note (agent notes, FUB nudges, pre-approval interest) EXCEPT machine junk (LinkPreview blobs, Sisu sync notes, RealScout stamps, bare-URL notes). Wired 2026-07-09 | `fub_note` | 📝 *Note on the lead* (`{{contact.full_name}}`) |
| 19 | `insight_high_intent_buyer` | **`notesCreated`** — Zillow insights arrive as NOTES, never events (mirror ground truth 2026-07-09: 1,429 high-intent notes, zero insight event types). Classified by Zillow's own wording "high intent buyer" | `lead_intent_insight` | 🎯 *high-intent signal* |
| 20 | `insight_high_intent_seller` | **`notesCreated`** — same note mechanism, wording "high intent Sellers" | `lead_intent_insight` | 🎯 *high-intent signal* |
| 21 | `insight_new_area` | **`notesCreated`** — wording "is now looking in KS 66046" (AREA_RE in convert-fub-signal.ts; 41 live rows, current 2026-07-06) | `lead_activity_insight` | 🔁 *activity insight* |
| 22 | `insight_viewed_10x_7d` | **`notesCreated`** — wording "They have viewed the property 10 times in 7 days" (INTENT_RE; 30 live rows, current 2026-07-02) | `lead_activity_insight` | 🔁 *activity insight* |
| 23 | `insight_re_engaged` | **`notesCreated`** — "Buyer has started searching again … after being inactive for at least 14 days". **Zillow's #1 live insight by volume** (269 since May 2026). Boarded 2026-07-09 (mig 0361, Zach: "that is a big one") | `lead_activity_insight` | 🔁 *activity insight* |

<Warning>
**⚠️ Two known catalog quirks (documented, not bugs to "fix" silently):**
1. **Saves/views arrive as `notesCreated`, not `eventsCreated`.** The standalone view/save webhooks died
   2026-05-21 and FUB now logs each as a **note** — the live per-event source is `notesCreated`. The
   `hl_fub_webhook_settings.fub_event` column (display-only) was corrected to `notesCreated` for
   `property_saved`/`property_viewed` on 2026-07-11.
2. **Phone triggers left the FUB board entirely (SHIPPED 2026-07-16).** The Twilio build landed: calls to
   our owned lines fire per-line inbox trigger events (lead-less included, mint-at-ring via creates_lead),
   and the FUB router resolves the CALLED number — Twilio-landed calls are skipped (our webhook owns them),
   Zach's FUB-native seat fires into the inbox lane. See [the board SSOT](/guides/trigger-events) §THE INBOX
   LANE for the full pipeline. The 3 dead subtype rows were deleted (mig 0415).
</Warning>

## Behavioral signals MINT when Creates Lead is ON

**The `Creates Lead` toggle means the same thing on every row — behavioral events included (Zach 2026-07-11).**
A `property_saved` / `property_viewed` / `in_app_message` from a person who is **not yet** a HouseLoop lead now
**mints a new lead when that event's `creates_lead` is ON** (and `_global` on), instead of the old notify-only
short-circuit. Turned OFF, it stays a lead-less "heads-up" notify. Each behavioral event is independent — Zach
turns Create-Lead on only for the ones he wants to become leads.

- **What fires on a mint:** a SAVE fires the real **`saved_home_registration`** door (its seat row already lives
  in `hl_lead_source_events`); a view/in-app has no registration door of its own so it mints via the generic
  first-touch **`direct_registration`**. Either way `importFubLead` mints/links the `hl_leads` row (source folded
  to its six-bucket canonical), `backfillFubBehavior` logs the save as a `save_home` feed card, `dispatchDoor`
  writes the `hl_trigger_fire` ledger row + runs any assigned workflow, and the owner gets a **New-lead** card
  carrying the saved home.
- **An EXISTING lead's behavioral signal is unchanged** — activity card + the `FUB_NOTIFY[webhookType]` ping, no
  registration door (the `NON_DOOR_WEBHOOKS` gate still holds for `created = false`).
- **Fail-closed preserved:** minting is still gated on `_global` + the row's `creates_lead`; nothing mints
  without an explicit toggle. Code: `lib/inngest/functions/fub-lead-import.ts` (behavioral fast-path →
  fall-through to the mint path; `saved_home` trigger/door; `doorEligible` opens only when `created`).

## THE TRANSACTION / TC-LOOP FAMILY

The Transaction Coordination section on the board — events fire on the TRANSACTION (a loop always hangs on
an EXISTING lead; no Creates-Lead): e-sign document events (sent · viewed · signed · completed via
Documenso) and loop milestone events (the TC loop stage moves). Deep law: the TC Loop guides.

## THE AI-TOOLS FAMILY

Every AI Toolbox capability is a trigger (migs 0407-0408): when the AI clone picks up a tool
(`ai_schedule_appointment`, `ai_create_saved_search`, `ai_send_document`, …) the tool call IS the trigger
event and the workflow bound to it runs — fires ledger to `hl_tool_fire` + the board's AI Tools section.
Catalog: `lib/agent-runtime/capabilities.ts`.

## Related

- [HouseLoop.ai integration](/integrations/houseloop-ai) · [Follow Up Boss integration](/integrations/follow-up-boss)
- [⭐ FUB SSOT](/fub/master-scraper-ssot) — the FUB pipeline
- [Workflows](/guides/workflows) — the engine · [Lead Sources](/guides/lead-sources) — view + route
