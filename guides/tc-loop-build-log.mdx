---
title: "TC Loop Build-Out (WIP Log)"
description: "The running work-in-progress log of building TC Loop — HouseLoop's AI-driven Transaction Coordination product. Architecture decisions, data model, build checklist, and a dated changelog. Updated as the build progresses."
---

<Info>
**Living document.** This is the build log for [TC Loop](/guides/transaction-coordination) — updated as work
happens. It's the single place to see what's decided, what's built, what's staged, and what's next. Owner: Adam.
Canonical spec: `GT_HOUSELOOP_TRANSACTION_COORDINATION_PRD.md` §12 (status/milestone model) + §13 (A-Frame engine).
</Info>

## Status (last updated 2026-06-27)

**Phase: Configure template builders DONE (incl. dynamic dates); next is runtime.** Engine done. Configure
builders all live: **Stages ✅, Milestones ✅ (full set + offset engine), Tasks ✅ (139 A-Frame steps + the
dynamic-date rule + collapsible folders + phase filter)**. Next: per-loop instantiation + the live cascade
(runtime, with loop-birth), then Merge Fields, Email/Text templates, Forms.

| Layer | State |
|---|---|
| Reverse-engineering (A-Frame + dotloop) | ✅ complete |
| Status + milestone model | ✅ **locked** (PRD §12 + §13) |
| Foundation data model (migrations 0253–0258) | ✅ applied |
| Loop engine + API | ✅ createLoop · completeMilestone (forward-only) · GET loop |
| **Configure → Stages** builder | ✅ reuses `StagesPage` scoped to TC |
| **Configure → Milestones** builder | ✅ full set (14 buyer / 16 seller) + offset engine, editable |
| **Configure → Tasks** builder | ✅ 139 tasks + **dynamic-date rule** + collapsible folders + phase filter |
| Dynamic-date engine (`lib/tc/date-engine.ts`) | ✅ pure `computeDue`, 13/13 tests (business/calendar/holiday roll) |
| **Configure → Merge Fields / Email / Text / Forms** | ⬜ |
| TC surface tables — row kebab + bulk-delete | ✅ (CRM-wide convention + lint rule) |
| **Runtime: per-loop instantiation + live cascade** | 🟡 next |
| Appointment-set → Loop birth · TC loop-detail page · Myli-TC | ⬜ |

## What we're building

TC Loop = HouseLoop's own dotloop + A-Frame + Documenso, AI-driven. A **self-contained, role-gated surface**:
a Transaction Coordinator logs in and has everything inside TC Loop (Dashboard · Loops · lead/loop detail ·
Configure) and never touches the CRM pipeline. Built human-first — a person can run a loop entirely by hand;
**Myli-TC is just another caller of the same API endpoints** (no AI backdoor). Every layer is editable +
behind a REST API so Myli drives it the same way a human does.

## How A-Frame actually works — the 3-layer engine + dynamic dates (LOCKED 2026-06-27)

Grounded in the A-Frame recon (`.claude/reports/aframe-recon/TC_MILESTONE_TASK_MAP.md`) + the TC-software
research (`.claude/reports/tc-dynamic-dates-research-2026-06-27.md` — Open To Close gold reference + TRID).
Three layers + an offset engine:

1. **STATUS (11)** — the headline word. = our **Stages**. ~5/side.
2. **DATES — 4 KEY DATES you ENTER drive everything:** `effective` (Under Contract) · `inspection_end` ·
   `closing` · `on_market`. These are the **milestones'** anchors.
3. **TASKS (~139, in phase folders)** — every task = a **relative rule** (`anchor + offset`), grouped into
   phases (Intake · Contract Review · Scheduling · Communication · Compliance · Inspections · Appraisal ·
   Closing; listing adds Pre-Live Stages 1–5).

**Dynamic dates = the offset engine done right (mig 0258 + `lib/tc/date-engine.ts`):** a task/milestone stores
a RELATIVE rule, and the due date **computes from the live key date — so it cascades when the key date moves.**
The rule: `anchor` (a key date) **or** another task (single-hop chain) · signed offset · **`day_unit`**
(calendar / business / calendar-skip-weekend / week / month) · **`business_day_def`** (TRID `precise` counts
Saturday, skips Sun + federal holidays; `general` skips Sat too) · **weekend/holiday roll** (lands on a
non-working day → next/prev business day) · versioned **holiday calendar** (US_FEDERAL). Plus **conditional
"If Applicable"** tasks (~25% — LBP/HOA/VA) + `task_type` (action/document). Engine proven: 13/13 unit tests
(Friday-closing Sat-counts, weekend+holiday roll chain, month clamp).

> **Status = headline · Milestones = the key dates that drive everything · Tasks = the auto-scheduled
> checklist hanging off those dates.** Enter 4 dates → the whole plan schedules; move one → it all re-shifts.

### The full milestone set (mig 0256, Configure → Milestones)
`hl_tc_milestone_template`. **KEY DATE** (`is_key_date`) = entered per loop, drives offsets. `▸` = advances status.
- **🔑 Buyer (14):** Exclusive Buyer Agency Prepped · Buyer Agreement Sent · Buyer Agreement Signed ·
  **Under Contract** `[KEY] ▸` · EMD Due `eff+3` · **Inspection Period Ends** `[KEY]` · Inspection Resolution
  `insp+5` · Title Commitment `eff+10` · Appraisal Received `eff+25` · Clear to Close `close−3` · Final
  Walkthrough `close−1` · **Closing** `[KEY] ▸` · Funded · Possession.
- **🏠 Seller/Listing (16):** Pre-Listing Packet (CMA) · Listing Agreement Sent · Listing Agreement Signed ·
  Photos/Media `onmkt−4` · **On-Market** `[KEY] ▸Active` · **Under Contract** `[KEY] ▸` · EMD Received `eff+3` ·
  **Inspection Period Ends** `[KEY]` · Inspection Resolution `insp+5` · Title Commitment `eff+10` · Appraisal
  `eff+25` · Clear to Close `close−3` · Final Walkthrough `close−1` · **Closing** `[KEY] ▸` · Funded · Possession.

> Buyer agreements (Single Property → Exclusive) are **milestones, not stages** — signing the Exclusive advances
> to `Active Buyer`. No upstream "Showing" stage (re-confirmed 2026-06-27).

## Architecture (locked)

1. **API-first.** Every loop action is a REST endpoint; UI + Myli call the same one.
2. **Role-gated `/tc/**` surface.** No CRM↔TC toggles.
3. **Reuse the engine, scope to TC. Never hand-roll** — every Configure page is identical (Add/Refresh header
   + `PipelineSwitcher` + `HlDataTable`; modals = `HlFormModal`; filters = `HlFilterPills`; no KPI cards on a
   template editor).
4. **Side-aware** (TC Buyer vs TC Listing).
5. **E-signature = Documenso.**
6. **Real CRM tables** — `HlDataTable` (`hl/table-requires-actions`) + canonical `HlFormModal` modals.

## Data model — migrations 0253–0258

`apps/houseloop-crm/db/migrations/` (additive + reversible; `node db/migrate.mjs` against `HL_CRM_DATABASE_URL`):
- **0253** — foundation: loop columns + child tables + `hl_templates.scope`.
- **0254** — TC pipelines in `hl_stages` · repoint `tc_stage_id` · `lost_reason` · `hl_tc_milestone_template` + extend `hl_tc_loop_event`.
- **0255** — milestone `color` + `icon`.
- **0256** — full milestone set + offset-engine columns (`anchor`, `is_key_date`).
- **0257** — `hl_tc_task_template` (139 A-Frame tasks).
- **0258** — task **date-rule** (`day_unit`, `business_day_def`, weekend/holiday roll, `skip_holidays`, chains: `anchor_type`/`anchor_task_id`/`anchor_on`, `is_conditional`, `task_type`) · `hl_tc_phase` **folders** (`is_gated`) · `hl_holiday_calendar` (US_FEDERAL).

## The loop engine + date engine (built + proven)

- `lib/tc/loop-engine.ts`: `createLoop` · `completeMilestone` (forward-only) · `getLoopStatus`/`listMilestones`.
- `lib/tc/date-engine.ts`: pure `computeDue(anchor, rule, holidays)` — calendar/business/week/month + TRID
  business-day defs + weekend/holiday rollover. **13/13 unit tests** (`npx tsx lib/tc/date-engine.test.ts`).
- REST: `/api/tc/loops[/:id]` · `/api/tc/milestones[/:id]` · `/api/tc/tasks[/:id]`.

## Changelog

### 2026-06-27 — Dynamic dates + folders + phase filter (Configure → Tasks)
- **Research** (3 agents → `.claude/reports/tc-dynamic-dates-research-2026-06-27.md`): Open To Close is the
  gold reference; TCWorkflow confirms automatic cascade; TRID has two business-day defs; manual dates should be
  sticky with a drift "re-link" nudge. Validated our anchor+offset model.
- **Migration 0258** — task **relative-date rule** (anchor or single-hop task→task **chain** · `day_unit`
  calendar/business/skip-weekend/week/month · `business_day_def` precise|general · weekend/holiday **roll** ·
  `skip_holidays` + `hl_holiday_calendar` US_FEDERAL) · **conditional "If Applicable"** · `task_type` ·
  **folder system** `hl_tc_phase` (gating-ready `is_gated`).
- **`lib/tc/date-engine.ts`** — pure `computeDue`; **13/13 tests** (Friday-closing precise Sat-counts, weekend
  + holiday roll chain, month clamp, roll-off, null anchor).
- **Configure → Tasks** — collapsible **phase folders** + **phase filter pills** (`HlFilterPills`, new canonical
  generic filter-pill — reuse not fork) + full **date-rule modal** (anchor/chain, offset + counted-as,
  business-day basis, weekend/holiday roll, conditional, type). API carries it all. Playwright-verified.
- **Runtime is next:** per-loop instantiation + live cascade (recompute on key-date change, audit, sticky
  overrides), folder gating enforcement, the TC loop-detail page. Commit `dafd8beb5b`.

### 2026-06-27 — Task layer (Configure → Tasks): the 139 A-Frame checklist steps
- **Migration 0257** — `hl_tc_task_template` seeded with all 139 A-Frame tasks (58 buyer / 81 sale), generated
  from the recon map. API `/api/tc/tasks[/:id]`. Configure → Tasks builder mirrors the Stages/Milestones shell.
- Also: Milestones page mirrors the Stages shell exactly; removed the meaningless KPI cards from template editors.

### 2026-06-27 — A-Frame 3-layer engine reverse-engineered + full milestone set seeded
- Reverse-engineered A-Frame (status/dates/tasks + offset engine) → recon artifact + PRD §13. Migration 0256:
  full milestone set (14 buyer / 16 seller) + offset-engine columns. Milestones builder fully editable.

### 2026-06-26 (PM, late) — Milestones builder · table row-actions sweep · canonical HlFormModal
- Per-side Milestones builder. Every CRM table got the ⋮ kebab + bulk-delete (`hl/table-requires-actions`).
  All Configure modals use the canonical blue-header `HlFormModal`.

### 2026-06-26 (PM, late) — status/milestone model locked; pivoted to reuse + built the engine
- Killed the hand-rolled TC Stages builder → reuse `StagesPage`. Locked the two-axis model (PRD §12).
  Migration 0254. Built + proved the loop engine.

### 2026-06-26 — reverse-engineering + foundation
- Reverse-engineered A-Frame + dotloop. Migration 0253. Scope lock.
