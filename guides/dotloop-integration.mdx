---
title: "dotloop Integration"
description: "How HouseLoop connects to dotloop via the Public API v2 (OAuth 2.0) to read a brokerage's loops, participants, folders, and documents — AND download actual document CONTENT (PDFs) straight from the API. Includes the full endpoint matrix, entity schemas, loop lifecycle, Loop-It create facade, webhooks, and the operational-hardening rules (token self-heal + include_archived) that make a bulk contract harvest actually work. The base of the Transaction Coordination section."
---

<Note>
**Live + verified.** dotloop Public API v2 access is granted, the Application Client (OAuth 2.0) is
created, and the brokerage account is connected — the CRM reads loops, profiles, participants, and
loop detail end-to-end. **As of 2026-06-26, document FILE content (PDFs) downloads directly through
the OAuth API** once the client carries the `document:*` scope. **As of 2026-06-30, bulk document
harvesting is hardened** — see [Operational hardening](#operational-hardening-token-self-heal--archived-folders).
This page is both our OAuth wiring AND the full reverse-engineered API reference (endpoint matrix +
live entity schemas), triangulated against live dumps from our authenticated account (Ana / Spradling
Group, profile 249848, loop 223215989, captured 2026-06-26).
</Note>


<Warning>
**🔑 TOKEN DEAD? THE RE-AUTH RUNBOOK — front and center (Zach directive 2026-07-06).** Symptom:
every dotloop call fails **401**, including the refresh itself (`dotloop refresh failed 401`). Cause:
dotloop **rotates the refresh_token on every refresh** — one lost race (two processes refreshing at
once) and the whole chain is dead forever. `expires_at` in `hl_dotloop_connection` in the past with
no newer unrevoked row = confirmed dead. **What the API can do:** detection and normal renewal only —
`getToken()` (`tools/houseloop-cards/dotloop-auth.mjs`) self-heals any *live* connection automatically;
you never manually re-auth a working token. **What it can't:** a dead grant needs ONE OAuth consent
click — no API shortcut exists (that's OAuth by design). The fastest path, proven end-to-end
2026-07-06 (~3 min, any agent can run it with the MCP browser):

1. **Remove the dead grant:** browse to `dotloop.com/my/account#applications` signed in as the
   brokerage data account (**Ana** — creds in `.env` under `CRED_DOTLOOP_ANA_DATA_ACCOUNT_*`; login
   goes through Zillow Pro and the browser profile usually has it saved) → **Remove App** on
   **"Zach Spradling"** ONLY (leave Zapier / KW Command / Open To Close / Apination alone). dotloop
   refuses a fresh grant while the dead one exists ("has already been connected"). A press-and-hold
   human check may appear — ask Zach to clear it if it blocks.
2. **Re-grant:** browse to `https://crm.houseloop.ai/api/dotloop/oauth/start` → **Approve**. The
   callback writes a new row to `hl_dotloop_connection` and lands on `/integrations?dotloop=connected`.
3. **Verify (don't trust the redirect):** `getToken()` then `GET /public/v2/profile` must return
   HTTP 200 with the 10 SRG profiles. The new row's `expires_at` sits ~12h out; self-heal owns it
   from there.
</Warning>

<Warning>
**LESSON LEARNED — read this before you debug a dotloop 403 (so we never lose an hour again).** If
a document download returns **403 "Access is denied"**, it is **NOT** a platform limitation and the
fix is **NOT** a cookie/session workaround. It means the **OAuth token lacks the `document:*` scope**.
The scope IS available — dotloop CS enabled `document:*` + `admin:read` on our client on 2026-06-22
(see [Authorization & support thread](#authorization--support-thread-the-proof)). The catch: a token
minted *before* the scope was turned on keeps its old scopes forever (refresh preserves them). **The
fix is a fresh consent:** in the brokerage dotloop account → **My account → Apps → Remove App** on
our app, then re-run `/api/dotloop/oauth/start` and **Approve** again. The new token carries
`document:*` and the exact same download call returns the PDF. Proven end-to-end 2026-06-26.
</Warning>

## What it does

dotloop is the transaction-management platform many brokerages use to run a deal from contract to
close — each transaction is a **loop** containing participants, documents, folders, and milestone
dates. The dotloop integration lets HouseLoop **read that transaction data through dotloop's
official Public API v2** and link each loop back to the HouseLoop lead/deal it belongs to.

We read the brokerage's *own* dotloop account. We don't move the data out of dotloop — dotloop
stays the system of record; HouseLoop reads from it.

Unlike A-Frame (closed), dotloop publishes a [developer API](https://dotloop.github.io/public-api/).
Together with the [A-Frame dossier](/guides/aframe-overview) and [eSignature](/guides/esignature),
this integration is the blueprint for our own AI-driven TC platform.

## The mental model

> **A loop = one transaction.** It is a container of **folders → documents**, **participants** (people
> by role), **tasks** (in task-lists), **detail** (a sectioned field map), and an **activity** feed.
> Loops live under a **profile**; profiles live under an **account**. Templates pre-populate new loops.
> A **webhook** system pushes real-time events. E-signature exists in the product but is **not in the
> public API**.

This is the same shape as A-Frame's "xaction" — loop ≈ transaction, folder/document ≈ attachment
template, participant ≈ participant role, detail ≈ custom-field tree, activity ≈ activity types.
dotloop adds first-class **documents + e-sign** and a **webhook** system; A-Frame adds the
**task-automation/email** engine. Our platform fuses both.

## The two-account model

dotloop's API separates the **application** from the **data**, and so do we:

| Account | What it is | Holds |
|---|---|---|
| **Developer account** (`admin@houseloop.ai`) | the "app" — where the Application Client lives | the Client ID + Secret. No loops, no plan needed. |
| **Brokerage account** (Spradling Group) | the "data" — the real transaction file | every loop, participant, document. Untouched. |

The developer account is an empty, free dotloop account whose only job is to hold the API client
(dotloop recommends a dedicated account for this). The brokerage account is where the loops live;
it **authorizes** our client via OAuth consent, and dotloop then issues a token scoped to *that*
account's loops. The brokerage's plan, data, and workflow are never altered.

## OAuth 2.0 flow

dotloop uses three-legged OAuth 2.0 (`authorization_code` + `refresh_token` grants).

```
1. Redirect the brokerage user to the authorize URL (response_type=code, client_id, redirect_uri, state)
2. They sign in to dotloop + grant consent, dotloop redirects to our redirect_uri with ?code=...
3. Exchange the code at the token URL (grant_type=authorization_code) for access_token + refresh_token
4. Call the API with: Authorization: Bearer <access_token>
5. Access token expires (~12h), refresh with grant_type=refresh_token (the old access token is then invalid)
```

| Thing | Value |
|---|---|
| Authorize URL | `https://auth.dotloop.com/oauth/authorize` |
| Token URL | `https://auth.dotloop.com/oauth/token` |
| Revoke | `https://auth.dotloop.com/oauth/token/revoke` |
| API base | `https://api-gateway.dotloop.com/public/v2/` |
| Redirect URI | `https://crm.houseloop.ai/api/dotloop/oauth/callback` |
| Access-token lifetime | ~12 hours (refresh with the refresh token) |
| Grants | `authorization_code`, `refresh_token` |
| Rate limit | 100 req/min per user; watch `X-RateLimit-*` headers |
| Pagination | `batch_size` (≤100) + `batch_number` |
| Errors | RFC-style `{ "errors": [{ code, source, detail }] }` — codes 400/401/403/404/422/429 |

There is **no `scope` parameter on the authorize request** — dotloop grants whatever the client is
provisioned for at consent time, and returns the granted scopes on the token (`token.scope`). The
Client ID and Secret are stored as secrets (`DOTLOOP_CLIENT_ID` / `DOTLOOP_CLIENT_SECRET`) and are
never committed or exposed client-side. The connection (access + refresh token + granted scopes) is
persisted in the CRM Postgres table `hl_dotloop_connection`; `lib/dotloop/client.ts` →
`getValidAccessToken()` auto-refreshes it (and so does the harvest auth — see Operational hardening).

## Access scopes

The Application Client is provisioned with these scopes (verified on the live token):

| Scope | Covers |
|---|---|
| `account:*` | account details |
| `profile:*` | the user's profiles |
| `loop:*` | the user's loops (transactions) |
| `contact:*` | the user's contacts |
| `template:*` | loop templates |
| `admin:read` | **office / all-profiles data (admin read)** |
| `document:*` | **document file content — upload AND download** |

<Warning>
**Scopes are NOT permanently locked at client creation — but widening them takes two manual steps.**
To add a scope to an existing client: (1) **Email `apisupport@dotloop.com`** and ask them to enable
the capability; (2) **Re-consent** — a token minted *before* the scope was enabled keeps its old
scopes (refresh preserves consent-time scopes), so in the **brokerage** dotloop account → **My
account → Apps → Remove App**, then re-run `/api/dotloop/oauth/start` and **Approve** again. Confirm
with `/api/dotloop/status?probe=1` or by reading `hl_dotloop_connection.scopes`.
</Warning>

## Endpoint matrix (full API surface)

All paths are relative to the API base and require the `Authorization: Bearer` header. List
responses are wrapped in `{ "data": [ ... ], "meta": { "total": N } }`.

| Resource | Method · Path | Purpose |
|---|---|---|
| **Account** | `GET /account` | authenticated user (id, email, name, defaultProfileId) |
| **Profile** | `GET /profile` · `GET /profile/:id` | list / get profiles (INDIVIDUAL/TEAM/OFFICE/COMPANY/ASSOCIATION) |
| | `POST /profile` · `PATCH /profile/:id` | create / update profile |
| **Loop** | `GET /profile/:pid/loop` | list loop summaries (`?limit=&sort=updated&direction=desc&filter=&include_details=true`) |
| | `GET /profile/:pid/loop/:id` | get loop (301-redirects if merged) |
| | `POST /profile/:pid/loop` · `PATCH …/:id` | create / update loop (name, status, transactionType) |
| **Loop detail** | `GET …/loop/:id/detail` · `PATCH …/detail` | the sectioned field map (read / partial-update) |
| **Folders** | `GET …/loop/:id/folder` · `GET …/folder/:fid` | list / get folders (`include_documents`, `include_archived`) |
| | `POST …/folder` · `PATCH …/folder/:fid` | create / rename folder |
| **Documents** | `GET …/folder/:fid/document` · `GET …/document/:did` | list / get document metadata |
| | `GET …/document/:did` (`Accept: application/pdf`) | **download PDF bytes** (needs `document:*` — see below) |
| | `POST …/folder/:fid/document` | upload (multipart/form-data) |
| **Participants** | `GET …/loop/:id/participant` · `GET …/participant/:id` | list / get participants |
| | `POST …/participant` · `PATCH …/:id` · `DELETE …/:id` | add / update / remove participant |
| **Tasks** | `GET …/loop/:id/tasklist` · `…/:tid/task` · `…/task/:id` | task-lists + tasks (**read-only via API**) |
| **Activity** | `GET …/loop/:id/activity` | loop activity feed (`meta.total` = -1, paginate; `?batch_size=200`) |
| **Loop-It** | `POST /loop-it?profile_id=:pid` | **create + populate a loop in one call** (the create facade) |
| **Contacts** | `GET /contact` · `GET /contact/:id` | list / get contacts (account rolodex) |
| | `POST /contact` · `PATCH /contact/:id` · `DELETE /contact/:id` | create / update / delete contact |
| **Templates** | `GET /profile/:pid/loop-template` · `…/:id` | loop templates (**read-only via API**) |
| **Webhooks** | `GET /subscription` · `GET /subscription/:id` | list / get subscriptions |
| | `POST /subscription` · `PATCH /:id` · `DELETE /:id` | create / update / delete subscription |
| | `GET /subscription/:id/event` · `…/event/:eid` | delivered event log + delivery history |

<Note>
**There is no documented loop-sharing / "accept invitation" endpoint.** A loop that was shared to our
account but not yet accepted cannot be accepted via the API — do it in the dotloop UI. In practice the
brokerage's own loops (the team profile) already carry the documents, so this rarely bites; the real
blockers were the token + the archived folders, not sharing.
</Note>

## Entity schemas (live)

### Account
```json
{ "id": 5177309, "email": "ana@spradlingrealtygroup.com", "firstName": "Ana", "lastName": "Spradling", "defaultProfileId": 12451292 }
```

### Profile (account has many; `meta.total` 11)
```json
{ "id": 4115518, "name": "Reece & Nichols Town Center", "type": "INDIVIDUAL", "company": "...", "phone": "", "fax": "", "address": "11601 Granada", "city": "Leawood", "state": "KS", "zipcode": "66211", "country": "US", "deactivated": true }
```
Loops are read **through a profile**; only **INDIVIDUAL** profiles support loops; deactivated/inactive
profiles return 403.

### Loop (summary)
```json
{ "id": 223215989, "profileId": 249848, "ownerProfileId": 17708777, "name": "Bradley and Kayla Burton - 900 Oak Crest Dr, Gardner, KS 66030", "transactionType": "LISTING_FOR_SALE", "totalTaskCount": 0, "completedTaskCount": 0, "status": "NO_STATUS", "created": "2022-10-01T19:30:17Z", "updated": "2026-06-20T00:45:41Z", "loopUrl": "https://www.dotloop.com/m/loop?viewId=223215989" }
```

### Loop detail (sectioned field map)
```json
{
  "Property Address": { "Full Address": "900 Oak Crest Dr, Gardner, KS 66030", "Country": "USA", "Street Number": "900", "Street Name": "Oak Crest Dr", "City": "Gardner", "State/Prov": "KS", "Zip/Postal Code": "66030", "MLS Number": "2407092" },
  "Financials": { "Sale Commission Split % - Buy Side": "2.5", "Sale Commission Split % - Sell Side": "2.5" },
  "Contract Dates": { "Closing Date": "11/17/2022" },
  "Listing Information": { "Expiration Date": "04/04/2023", "Listing Date": "10/04/2022", "Original Price": "325,000" },
  "Geographic Description": { "Legal Description": "COTTAGE CREEK IV LT 14 BLK 2 GAC 310 2 14" }
}
```
Sections (vary by template): Property Address · Financials · Contract Dates · Offer Dates · Contract
Info · Referral · Listing Information · Geographic Description · Property (beds/baths/sqft/lot/year/school).

### Participant (`meta.total` 6 on this loop)
```json
{ "id": 321175316, "fullName": "Chris Cribb", "email": "mo.sign@exprealty.net", "role": "MANAGING_BROKER", "Country": "USA" }
```

### Folder (`meta.total` 12) · Document
```json
{ "id": 237727475, "name": "Folder", "created": "2026-06-20T00:45:37Z", "updated": "2026-06-20T00:45:37Z" }
// document: { "id", "name" (filename), "created", "updated" }  — bytes via Accept: application/pdf
```

### Loop template (`meta.total` 3)
```json
{ "id": 143141, "profileId": 12451292, "name": "2026 EXP Referral", "transactionType": "REAL_ESTATE_OTHER", "shared": true, "global": false, "sortOrder": 3 }
```

### Activity (feed; `meta.total` -1)
```json
{ "message": "Ana Spradling exported Exclusive Right to Sell Contract to a digitally verified, tamper-evident PDF.", "date": "2026-06-26T16:20:05Z" }
```

### Contact (account rolodex; `meta.total` 4067)
```json
{ "id": 110572008, "firstName": "1", "middleName": "Big", "lastName": "Red LLC", "created": "2020-02-24T19:52:35Z", "updated": "2020-02-24T19:52:35Z" }
```

## Loop lifecycle (status by transaction type)

`status` is scoped to `transactionType` — the *stage set itself* switches with the transaction type,
rather than statuses mapping into a fixed enum:

| transactionType | Status flow |
|---|---|
| PURCHASE_OFFER | PRE_OFFER → UNDER_CONTRACT → SOLD → ARCHIVED |
| LISTING_FOR_SALE | PRE_LISTING → PRIVATE_LISTING → ACTIVE_LISTING → UNDER_CONTRACT → SOLD → ARCHIVED |
| LISTING_FOR_LEASE | PRE_LISTING → PRIVATE_LISTING → ACTIVE_LISTING → UNDER_CONTRACT → LEASED → ARCHIVED |
| LEASE_OFFER | PRE_OFFER → UNDER_CONTRACT → LEASED → ARCHIVED |
| REAL_ESTATE_OTHER / OTHER | NEW → IN_PROGRESS → DONE → ARCHIVED |

(Plus `NO_STATUS`.)

## Participant roles (built-in)

`ADMIN · APPRAISER · BUYER · BUYER_ATTORNEY · BUYING_AGENT · BUYING_BROKER · ESCROW_TITLE_REP ·
HOME_INSPECTOR · INSURANCE_REP · LANDLORD · LISTING_AGENT · LISTING_BROKER · LOAN_OFFICER ·
MANAGING_BROKER · PROPERTY_MANAGER · SELLER · SELLER_ATTORNEY · TENANT · TRANSACTION_COORDINATOR`
(+ custom). Some roles accept extra fields (License #, ID, Marital Status). Each participant is
`{ fullName, role, email }`.

<Warning>
**A departed agent is DELETED from the participant list.** When a team agent leaves the brokerage,
dotloop removes them from the loop's people — so `/participant` shows only the agent-of-record
(often the team lead, e.g. Ana) + the clients, **not the agent who actually worked the deal**. That
agent survives ONLY inside the activity log (best) and the signed contract PDF (fallback) — see
[the activity-log unlock](#the-activity-log-unlock-better-than-the-contract-pdf-for-the-departed-agent).
</Warning>

## Operational hardening (token self-heal + archived folders) — LOCKED 2026-06-30

Two bugs silently capped our first bulk contract harvest at **82 of 12,096 loops** (89 licensees).
Both are now fixed and are MUST-know for anyone reading documents at scale.

### 1. The token must SELF-HEAL on every 401 — never trust `expires_at` alone

The connection token (~12h) can be **rotated out from under a long-running job.** The CRM's own
`getValidAccessToken()` and any batch script share the **one** `hl_dotloop_connection` row, and a
refresh by either one **rotates the refresh_token and invalidates the other's access_token** — while
leaving a *future* `expires_at` on the now-dead token. A naive "is the timestamp still in the future?"
check then hands back a **dead** token, and every call silently `401`s for the rest of the run. That
is exactly what killed the first harvest mid-stream (it died ~partway → only the early loops got PDFs).

**The fix (brittle → resilient):** the low-level `dl()` fetch helper now **self-heals** — on ANY `401`
it FORCE-refreshes the token (`getToken(crm, {force:true})`, bypassing the `expires_at` "stillGood"
check) and **retries the call once.** Register the crm client once via `setAuthCrm(crm)` and a token
that dies mid-run heals transparently instead of poisoning the rest of the batch.
(`tools/houseloop-cards/dotloop-auth.mjs`.) **Rule: any long dotloop job must self-heal on 401; never
gate a refresh on `expires_at` alone.**

### 2. `include_archived=true` — closed-loop documents live in ARCHIVED folders

The folder-listing endpoint takes query params:
`GET /profile/:pid/loop/:lid/folder?include_documents=true&include_archived=true`. **Without
`include_archived=true` you do NOT see archived folders**, and a SOLD/closed loop keeps its signed
contracts (the "Exclusive Buyer Agency Contract", "Exclusive Right to Sell", etc.) in ARCHIVED
folders — so you find zero documents and never reach the licensee. **Always pass both params when
reading documents from closed loops.**

**Proof (2026-06-30):** with the fresh self-healing token + `include_archived=true`, loop
`248724234` returned **9 folders / 67 downloadable PDFs**, and the binding contract PDF yielded
`Licensee Assisting Seller: Bree Archer · Buyer: JaNell Simpson`. The re-harvest of all
SOLD/UNDER_CONTRACT loops then recovered real designated agents (Jodie Brethour, Alisha Hanks,
Kathy Shen, Jessica Rich, …) that `/participant` had dropped.

## HOW TO GET IN — the 60-second path (verified 2026-07-30)

<Note>
**You do not need to re-authorize, and you do not need a browser.** Read this before touching OAuth —
2026-07-30 an agent spent 40 minutes concluding "dotloop is dead" and asking Zach to log in. It was never
down.
</Note>

**Step 1 — wake the connection. This is the whole trick.**

```
https://crm.houseloop.ai/api/dotloop/status?probe=1
```

Hit that in the browser (you're already signed in to the CRM). It calls `getValidAccessToken()` **on the
server**, which auto-refreshes an expired token **and writes the fresh one back to
`hl_dotloop_connection`**. A healthy response proves the whole chain:

```json
{"ok":true,"connected":true,"scopes":"account:* profile:* loop:* contact:* template:* admin:read document:*",
 "probe":{"ok":true,"account":{"id":5177309,"email":"ana@spradlingrealtygroup.com"},"profiles":9,"loopTotal":17035}}
```

**Step 2 — use that fresh token from anywhere.**

```bash
U=$(grep -m1 '^HL_CRM_DATABASE_URL=' .env | cut -d= -f2- | tr -d '"')
AT=$(psql "$U" -X -q -t -A -c "SELECT access_token FROM hl_dotloop_connection ORDER BY updated_at DESC LIMIT 1;")
curl -s -H "Authorization: Bearer $AT" https://api-gateway.dotloop.com/public/v2/account
```

<Warning>
**`DOTLOOP_CLIENT_ID` / `DOTLOOP_CLIENT_SECRET` are NOT in the local `.env`** — they live only in the
Railway environment. So a **refresh attempted from a local shell returns `401` and looks exactly like a
dead integration.** It isn't. The refresh must happen server-side (step 1); after that the stored token
works fine from a local `curl`. The `CRED_DOTLOOP_API_CLIENT_*` entries in `.env` are the *credential
record*, not the runtime pair — do not try to refresh with them.

Only re-run `/api/dotloop/oauth/start` when the **refresh token itself** is dead (a real `invalid_grant`
after step 1 fails), or when re-consenting to widen scopes.
</Warning>

## ⭐ THE TEMPLATE LIBRARY + PRE-MAPPED FIELDS (the internal REST API, found 2026-07-30)

<Warning>
**The public API cannot see blank forms.** Verified against dotloop's own docs: the Templates section
exposes exactly two endpoints — `GET /profile/:id/loop-template` and `…/loop-template/:id` — and those are
**LOOP** templates (which folders a new loop starts with). There is **no public endpoint for document
templates / blank forms**, and every guess returns 404 even with `template:*` scope. This is a platform
limit, not an auth problem. Stop trying to reach the form library through `api-gateway.dotloop.com`.
</Warning>

**The form library lives on dotloop's INTERNAL REST API** (`www.dotloop.com/my/rest/v1_0/…`), the one its
own web UI calls. It is **session-authenticated** (browser cookies), not bearer-token — so drive it from a
logged-in browser, not curl.

```
GET /my/rest/v1_0/folder/:folderId/document?batchNumber=1&batchSize=100   → the templates in that folder
GET /my/rest/v1_0/document/:documentId                                    → pages[] + FIELDS[]
GET /my/rest/v1_0/document/:documentId/page/:n                            → that page as a PNG
```

### 🔑 dotloop already stores the field map — do not re-detect it

`GET /my/rest/v1_0/document/:id` returns a **`fields[]` array that IS the signature map**, authored in
dotloop, exact:

```json
{ "pageNumber": 2, "type": "SIGNATURE", "x": 692, "y": 742, "height": 36, "width": 256,
  "assigneeRoleId": 1, "cellNumber": "S1", "dataId": "55052764320" }
```

Type · page · x/y · width/height · **which signer role**. For a migration this is decisive: **importing
dotloop's own coordinates beats detecting them.** No vision, no OCR, no geometry gate, no hallucination
risk — and no per-form cost. Reserve `vision-field-builder.mjs` for forms that are NOT in the dotloop
library (see `.claude/rules/SIGNATURE_ANCHORING.md`).

⚠ Coordinates are in dotloop's own page space, not PDF points — convert against the page image
dimensions before use, and **overlay-verify** like any other map.

### The library that matters

Templates → Documents → **`2026 SPRADLING REALTY GROUP MASTER FOLDER`** = folder **`227653290`** on
profile `12451292`. **58 documents**, all eXp/KCRAR current, e.g. `Kansas Brokerage Relationships
Disclosure (Buyers)` `1423017762` · `MO Broker Disclosure - Buyer` `1428581661` · `Exclusive Buyer Agency
Contract` `1423017753` · the Kansas/Missouri Exclu + Non-Exclu + Single Property eXp agreements ·
ABA Disclosures · EXP Wire Fraud Notice · Consumer Choice · Spradling Group Buyer's Offer Sheet.

<Warning>
**Do NOT harvest blank forms out of old loops.** Three sibling folders are literally named **"DO NOT USE"**
(including *DO NOT USE - EXP DOCUMENTS*). On 2026-07-30 the two state disclosures were pulled from a real
buyer loop and turned out to be **ReeceNichols** forms — Ana's former brokerage — complete with their logo,
Leawood address, and *pre-checked* agency relationships. The whole `12451292` profile is the
ReeceNichols-era account. **The master folder above is the source of truth; a loop is not.** Always check
branding (`pdftotext | grep -i "reece\|exp realty"`) before adding any form to the catalog.
</Warning>

## Finding a form or a document (the path shape that actually works)

The download path needs **profile → loop → folder → document**. Omitting the `folder` segment (or the
`profile` prefix) returns the **dotloop marketing HTML page**, not JSON and not a 404 — which reads like a
broken token when it is really a malformed URL.

```bash
A="https://api-gateway.dotloop.com/public/v2/profile/$PROFILE_ID"
curl -s -H "Authorization: Bearer $AT" "$A/loop?batch_size=20&sort_by=updated&sort_direction=desc"   # find a loop
curl -s -H "Authorization: Bearer $AT" "$A/loop/$LOOP/folder?include_documents=true"                 # folders + docs + IDs
curl -s -H "Authorization: Bearer $AT" -H "Accept: application/pdf" \
     "$A/loop/$LOOP/folder/$FOLDER/document/$DOC" -o out.pdf                                          # the bytes
```

**Key IDs (Spradling, verified 2026-07-30)**

| Thing | Value |
|---|---|
| Account | `5177309` — ana@spradlingrealtygroup.com |
| **Team profile** (has the templates + brokerage loops) | **`12451292`** — Spradling Realty Group |
| Loop templates | `217364` 2026 Spradling Buyer Template · `217365` Seller · `143141` eXp Referral |

**A blank brokerage form is not a template — it lives inside a real loop.** `GET …/loop-template/:id`
returns only header metadata (name, transactionType), never its document list. To get a standard form,
open a recent loop of that type and download it from its folder. That is how the two state disclosures
were retrieved:

| Form | Loop | Folder | Doc |
|---|---|---|---|
| KS Real Estate Brokerage Relationships (F323) - Buying | `116090826` | `86376717` | `500659618` |
| MO Broker Disclosure (F318) - Buying | `116090826` | `86376717` | `500659620` |

## Downloading document content (PDFs)

```
GET https://api-gateway.dotloop.com/public/v2/profile/:profileId/loop/:loopId/folder/:folderId/document/:documentId
Authorization: Bearer <access_token>
Accept: application/pdf
→ 200, Content-Type: application/pdf, body = the PDF bytes
```

- **`document:*` scope required** — without it = 403 (a *missing-scope* signal, not a platform limit).
- **`Accept: application/pdf` switches metadata → file** (`application/json` returns metadata only).
- **Find IDs first:** `/folder?include_archived=true` → `/folder/:id/document` → download.
- **Retry transient 500s** (~0.75s). Helper: `lib/dotloop/client.ts → dotloop.downloadDocument(...)`.

**Why it matters — the designated agent on departed-agent loops:** a fallback programmatic source of a
departed agent's name is the signed contract PDF → `pdftotext` → the "Licensee Assisting Seller/Buyer"
field. `tools/houseloop-cards/dotloop-contract-fetch.mjs` does this end-to-end (tier1 participants/detail
free, tier2 downloads the binding PDF), persisting to `hl_srg_dotloop_contracts`.

## The `.loop` fact table (`hl_srg_loop`) — deterministic dotloop facts (2026-07-01)

dotloop is **its own source of truth.** `hl_srg_loop` holds the DETERMINISTIC facts of every loop — one
row per loop — so the past-client resolver has a clean ground-truth source. **No SISU triangulation, no
LLM guessing who is "ours"** — we record who the listing agent + buyer agent were, by address, plus the
closing facts; the resolver decides "ours" later. Rolled up by `hl_srg_agent_loops` (one agent → their
loops, mirror of one home → many transactions).

**Facts per loop:** Property (`/detail`: full_address, street#, street, city, state, zip, county, legal)
· Closing (`/detail`: sale_price, earnest money, contract_date, **closing_date**, offer_date) ·
`listing_agent` · `buyer_agent` (each `{name,email,brokerage}`) · `transaction_coordinator` ·
`spradling_agents[]` (every @spradlingrealtygroup.com actor = resolver candidates) · buyers[] ·
sellers[] · participants[] · activity_actors[] · documents[] · referral_direction.

**Sourcing — deterministic-first:** (1) participant **ROLE** (`LISTING_AGENT` / `BUYING_AGENT` /
`BUYER` / `SELLER`) → (2) the **ACTIVITY LOG** → (3) documents (Haiku) only as a last-resort fallback.

### THE ACTIVITY-LOG UNLOCK (better than the contract PDF for the departed agent)

`GET /profile/:pid/loop/:lid/activity?batch_size=200` is an audit trail of every action with the
actor's name + email + brokerage: *"Walter Hawkins added document … whawkins@spradlingrealtygroup.com"*.
**dotloop scrubs a departed teammate from `/participant` but NOT from the activity log** — so a
`@spradlingrealtygroup.com` actor in the log IS the agent, recovered deterministically without reading a
single PDF. Proof — **11436 Conser**: `/participant` lists only Wendy Manning (`LISTING_AGENT`, the
other side); the buyer agent **Walter Hawkins** appears ONLY in the activity log. The contract-PDF
route is now the *fallback*, not the primary, for the departed agent.

⚠️ **Ana Spradling is the account admin on every loop** — excluded from the single-candidate agent
auto-fill unless she's the only Spradling actor (else she'd falsely become the agent on everyone's deals).

⚠️ **Rate limits:** dotloop throttles concurrent reads (100 req/min per user). `dl()` backs off
(exponential, jittered, ≤3 retries) on 429/5xx, and harvesters run ~3 parallel workers. A fetch-failure
guard means a rate-limited loop is retried, never persisted as a blank "done" row. Tool:
`tools/houseloop-cards/harvest-loop-full.mjs`.

## Loop-It — the create facade

`POST /loop-it?profile_id=:pid` creates **and populates** a loop in one call — the endpoint to drive
from an AI agent when a new deal starts:

```json
{
  "name": "900 Oak Crest Dr, Gardner, KS 66030",
  "transactionType": "LISTING_FOR_SALE",
  "status": "PRE_LISTING",
  "streetNumber": "900", "streetName": "Oak Crest Dr", "city": "Gardner", "state": "KS", "zipCode": "66030", "county": "Johnson", "country": "USA",
  "participants": [ { "fullName": "Bradley Burton", "email": "...", "role": "SELLER" } ],
  "templateId": 143141,
  "mlsPropertyId": "...", "mlsId": "...", "mlsAgentId": "...", "nrdsId": "..."
}
```
Returns the loop + `loopUrl`. (Loop-It is POST-only; a GET returns 404, as confirmed live.)

## Webhooks (real-time events) — how the AI stays in sync

A client-app subscription pushes events to an HTTPS URL.

- **Targets:** `USER` or `PROFILE` (+ `targetId`). **Create:** `POST /subscription { targetType, targetId, eventTypes[], url, signingKey?, externalId? }`.
- **USER events:** CONTACT_CREATED/UPDATED/DELETED · PROFILE_UPDATED · USER_PROFILE_ACTIVATED/DEACTIVATED · USER_ADDED_TO_PROFILE · USER_REMOVED_FROM_PROFILE.
- **PROFILE events:** LOOP_CREATED · LOOP_UPDATED · LOOP_MERGED · LOOP_PARTICIPANT_CREATED/UPDATED/DELETED.
- **Payloads** are polymorphic: `{ id }` (loop/contact/profile), `{ id, participantId }` (participant), `{ id, fromId, toId }` (merge).
- **Verification:** `X-DOTLOOP-SIGNATURE` (HMAC-SHA1 of `timestamp.body` with `signingKey`) + `X-DOTLOOP-TIMESTAMP`.
- **Delivery:** at-least-once (dedupe), 5s timeout, backoff 30s → 1m → 15m → 30m → 1h → 2h → 4h → 8h; events retained 90 days; query the delivery log via `GET /subscription/:id/event`.

<Note>
**No saved-search / browsing webhooks** — dotloop webhooks are loop/contact/profile lifecycle only.
(This mirrors the FUB walled-garden pattern: transactional events survive, behavioral ones don't.)
</Note>

## E-signature

**Not in the public API v2.** dotloop's e-sign is a product feature (the activity feed shows
"exported … to a digitally verified, tamper-evident PDF") but there is **no documented endpoint to
request or track signatures**. For our platform, e-sign = **Documenso** (HouseLoop eSignature) — see
[eSignature](/guides/esignature). We pull the *signed result* PDF from dotloop via document download;
we never *initiate* signing through dotloop's API.

## Using dotloop as a confirmation source (resolver)

dotloop is a **ground-truth confirmation layer**: per transaction it yields the **client name + email
+ role**, the **property address** (street/city/state/zip/county + MLS number), the **price + close
date**, and the **signed contract PDFs** (→ the departed working agent). To confirm a past client:
match the loop's address → `Listing`; take the BUYER/SELLER participant → the client + side; recover
the designated agent from the activity log (or the contract PDF as fallback). dotloop is the final say
on *who actually transacted what address with us*.

## Building our own (AI-driven) — what dotloop gives us

| Capability | dotloop API | Use in our platform |
|---|---|---|
| Loop/transaction CRUD | ✅ loop + Loop-It facade | AI agent opens a loop on a new deal via one Loop-It call |
| Sectioned detail (address/financials/dates) | ✅ detail GET/PATCH | maps to our custom-field tree (A-Frame model) |
| Folders + documents + **download** | ✅ full | the document spine; pull signed PDFs |
| Participants by role | ✅ full CRUD | the people on the deal |
| Tasks / templates | ⚠️ read-only | we run the **automation** (A-Frame's task/email engine), not dotloop |
| Activity feed | ✅ read | mirror into our run-log; recover departed agents |
| Real-time sync | ✅ webhooks (loop/contact/profile) | keep our DB live without polling |
| E-signature | ❌ not in API | **Documenso** |
| Contacts | ✅ full CRUD (4,067 live) | optional contact sync |

**The synthesis:** dotloop supplies the *loop/document/participant/webhook* substrate; A-Frame supplies
the *task-stage automation + templated-email* engine; Documenso supplies *e-sign*; an AI agent (Myli-TC)
drives all three. That's our AI-driven TC platform.

## Connecting an account

1. Admin opens **Settings → Integrations** → **Manage** on the dotloop card (or `/api/dotloop/oauth/start`).
2. Sign in as the **brokerage** account and grant consent.
3. dotloop redirects back; the CRM exchanges the code and stores the connection.
4. Verify at `/api/dotloop/status?probe=1`.

## Authorization & support thread (the proof)

We are an approved dotloop **Integration Partner** — access requested 2026-06-16, granted on
`admin@houseloop.ai`; OAuth Application Client ("HouseLoop", Client ID `1a48c25d-…` — full ID + secret
in the vault), authorized by `ana@spradlingrealtygroup.com`. **Document download + Admin read enabled
by dotloop CS 2026-06-22.** Developer guide (source of truth):
**[dotloop.github.io/public-api](https://dotloop.github.io/public-api/)**. API support:
`apisupport@dotloop.com`. Onboarding guides: "[Step 1] Getting Started with the dotloop Public API
(v2.5)" + "[Step 2] Quick Start Guide". Document templates pulled into
`tools/houseloop-contracts/dotloop-forms/`. Live schema dumps:
`.claude/reports/dotloop-recon/dotloop-live-dump.json` · `dotloop-schema-shapes.json`.

## Change log

- **2026-07-01** — **The `.loop` fact table + the activity-log unlock.** Built `hl_srg_loop` (one row/loop: address, price, closing_date from `/detail`; listing+buyer agent from participant roles + the ACTIVITY LOG; Haiku doc-fallback) + `hl_srg_agent_loops` (agent → their loops). Key discovery: the **activity log** (`/loop/:id/activity`) exposes an agent scrubbed from `/participant` (Walter Hawkins, buyer agent on 11436 Conser) — deterministic, no PDF needed. dotloop treated as its own source of truth (no SISU triangulation). Added rate-limit backoff to `dl()`. (Caleb)
- **2026-06-30** — **Bulk document harvest hardened (token self-heal + archived folders).** The first
  contract harvest pulled only 82/12,096 PDFs because (a) the OAuth token lapsed/rotated mid-run →
  silent 401s, and (b) the folder call omitted `include_archived=true` so closed-loop docs (in archived
  folders) were invisible. Fix: `dl()` now FORCE-refreshes + retries on any 401 (self-healing token via
  `setAuthCrm` in `dotloop-auth.mjs`), and the folder call passes
  `include_documents=true&include_archived=true`. Proven on loop 248724234 (9 folders / 67 PDFs → Bree
  Archer / JaNell Simpson). Re-harvest (`reharvest-dotloop-contracts.mjs`) recovers departed-agent
  licensees at scale. Added the **Operational hardening** section. (Caleb)
- **2026-06-26** — Document download proven through the OAuth API (`document:*` + re-consent). Added
  `dotloop.downloadDocument()` with transient-500 retry. Cookie-session web endpoint superseded. Full
  API reverse-engineering (endpoint matrix + live entity schemas) captured against Ana / Spradling Group.
- **2026-06-22** — Live + verified. dotloop Public API v2 access granted; Application Client created;
  CRM OAuth routes + `hl_dotloop_connection` store + Integrations card shipped; reading loops/profiles/
  participants/detail end-to-end.
