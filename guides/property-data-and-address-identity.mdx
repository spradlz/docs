---
title: "Property Data Model & Address Identity"
description: "How HouseLoop keys homes by address — the ONE address-keyed table model, how an address becomes a stable USPS-conformed identifier, how the resolver matches an incoming listing to one owned home (forward), and how pre-existing duplicates are collapsed reversibly against ground truth."
---

HouseLoop keys every home by its **address** — not by the MLS number, not by a per-source id. This page documents the property data model, how addresses are normalized into a stable identifier, how the resolver matches an incoming listing to the one home we already own, and how the pre-existing duplicate rows are collapsed.

<Note>
**Current status (be precise).** The address resolver runs on the live ingest path, so it prevents **new** duplicates going forward. The historical data still contains pre-existing duplicate rows (the same physical home stored more than once from before the resolver existed); a careful, verified, **reversible** collapse of those is **in progress**. So "one home = one row" is the model the resolver enforces forward, and the collapse below is bringing the existing rows to that state.
</Note>

## The one table (the target model)

There is one property table: `Listing`. The intended model is that a home is a single row for its entire life, and the lifecycle is a `status` field that flips on that same row:

```
coming_soon → active → pending → sold        (status changes; same row, same URL)
```

- A status change should **never** create a new row, a second slug, or use a per-source id as identity.
- `mlsNumber`, `zpid`, `parcelNumber` are **columns** (source references), not identity.
- Sold homes are rows with `status='sold'`. (Historically these lived in a separate `ListingHistory` archive; that table was absorbed into `Listing` and renamed `ListingHistory_deprecated` — a reversible backup. See [Sold Data & Price History](/guides/sold-data-and-price-history).)

Reads filter the live, visible set with `deletedAt IS NULL AND quarantinedAt IS NULL`. `quarantinedAt` is a non-destructive "hide this row" flag used to retire a duplicate without deleting data.

## Identity = the canonical address

A home's identity is its **canonical, normalized address** — the one thing true across every data source (MLS, Zillow, county), while each source has its own id. Keying on a single source id breeds duplicate rows for the same house (which is exactly how the historical duplicates were created — see below).

### Layer 1 — deterministic normalization (offline, free)

`lib/listings/canonical-address.ts` standardizes a raw address with zero API calls (runs at render time, in the sitemap, and as the redirect resolver):

- **Street suffix → one full word:** `Rd`/`Rd.`/`Road` → `road`; `St` → `street`; `Ave` → `avenue`.
- **Directionals → one abbreviation:** `North`/`N` → `n`.
- **Unit fragments dropped** from the street line.

```
canonicalSlug({ street: "1230 W 31st St", city: "Independence", state: "MO", zip: "64055" })
  → "1230-w-31st-street-independence-mo-64055"
```

Same physical home → same slug, regardless of which spelling a source sent. Handles ~97% of addresses.

### The URL contract

- Each home renders at **`/home/{canonicalSlug}`** — one canonical URL per home.
- A non-canonical variant (`…-rd-…` when canonical is `…-road-…`) **308-redirects** to the canonical URL; the page sets `rel=canonical`.
- The sitemap emits **one URL per home** (distinct `canonicalSlug`).

## Address as a USPS-conformed unique ID

The deterministic layer can't fix everything: two sources may disagree on the **city** ("Baldwin" vs "Baldwin City" for the same ZIP) or on a suffix abbreviation. USPS is the ground truth for "is this a single deliverable address," so addresses conform to USPS. This is the `lib/listings/address-identity.ts` service.

### Layer 2 — the city-independent identity key (free)

`identityKey({street, unit, city, state, zip})` = **canonical street + unit + 5-digit ZIP — city deliberately excluded.** The ZIP pins the locale, so "Baldwin" and "Baldwin City" produce the **same key**. It is persisted on every `Listing` row as the indexed `identityKey` column, so the resolver matches against it directly.

The **USPS-preferred city** for a ZIP comes from `zip-preferred-city.generated.json` (built from the most-common city per ZIP among real MLS rows). `preferredCity(zip)` resolves it and `canonicalSlugPreferred()` builds the URL slug with it.

<Note>
**Why city is never part of the key.** KC-metro ZIPs straddle municipal lines: `66215` is **Lenexa** per USPS but **Overland Park** per Zillow; `66216` shows as both **Shawnee** and **Shawnee Mission**. If the city were in the match key, the same home would split into two records the moment two sources disagreed on the label. The ZIP pins the geography and the house number pins the home — the city is a display attribute, never an identifier.
</Note>

### Layer 3 — CASS validation (paid, reserved)

`validateAddress(parts)` standardizes against USPS via a pluggable provider — **Smarty** → **Google Address Validation** → **offline** — returning the standardized address, preferred city, ZIP+4, and the **DPV delivery-point key**. It never throws; with no provider it degrades to the offline result. Reserved for new ingest + the ambiguous residual, never a blind sweep of the whole base.

## The resolver — matching an incoming listing to one owned home

`resolveHome(db, input)` (`lib/listings/resolve-home.ts`) returns a verdict — `matched` (with the owned `hlid`) or `mint` (it's new) — using three rungs:

1. **Identity key** — match an existing owned home on `identityKey()` (city-free street + ZIP).
2. **Physical point** — when the key misses but coordinates are present, match on **lat/lng within ~25 m**.
3. **Mint** — no owned home matches → create one row and stamp its identity.

**Where it runs (live, forward).** The resolver is wired into the ingest path (`lib/listings/cache.ts`): every Repliers sync resolves the incoming address **before** the upsert. If we already own the home under a different `hlid` (a relist under a new MLS number), the write targets that owned row — so going forward the home updates in place instead of forking a new duplicate.

<Warning>
**Why duplicates existed.** The ingest historically upserted by `hlid = hl_heartlandmls_<mlsNumber>` — keyed on the MLS number, not the address — so every relist under a new MLS number forked a second row. The resolver fixes this **for new ingests**; the **pre-existing** duplicate rows are collapsed by the reversible process below.
</Warning>

## Collapsing pre-existing duplicates (reversible)

The historical duplicates are collapsed by a **multi-stage funnel** — cheapest, most-certain check first, each stage handling only what the prior couldn't. **Every collapse is reversible** (a flag, never a delete), and a stage that's unsure leaves the rows alone rather than guessing.

| Stage | Resolves |
|---|---|
| Deterministic cluster | groups candidate twins by house number + first street word + ZIP |
| LLM grouping + verify | a model reads unit + beds/baths/sqft and groups same-home rows; a stricter second pass re-confirms before any merge |
| Unit guard | a deterministic rule that can't be overridden — rows may only merge if they share the same explicit unit, or all have no unit and identical specs |
| Review pass | re-reads the quarantine set and **restores** any false positive (spelling variants like `Dr`=`Drive`, missing suffix, casing are the same home; different units are not) |
| Geocode sieve | the single-family ambiguous tail — Google Geocoding returns a `place_id` per address; the same `place_id` is the same home (excludes units and lots, which geocode resolves to a building or road) |
| **Redfin ground truth** | the **condo / multi-unit** tail — see below |

**The reversible mechanism** — every stage writes the same three records, so any collapse can be undone:

- `Listing.quarantinedAt = now()` — the row drops out of search but is fully restorable.
- `hl_dedup_quarantine` — an audit log of `(loser, winner, addresses, reason)`.
- `hl_slug_alias` — maps the retired URL to the keeper so the old link **301-redirects** instead of breaking.

The **keeper** of each group is chosen by completeness: most enriched (value history, photos, value) → live over sold → cleanest address → most recent.

### Redfin ground truth for condos

Condo and multi-unit buildings are the hard case: our records sometimes lack the unit number, so distinct units can look like one cluster. To resolve them we read **Redfin's address autocomplete**, which returns a building's real unit inventory — the base building **and** each unit, every one a distinct Redfin property id. Each of our rows is mapped to a Redfin property id by unit (no-unit → the base building). Rows that land on the **same** Redfin property id are the same home and collapse; rows on different ids are genuinely different units and are left alone. A row is only used if its Redfin match shares our house number and street, so fuzzy autocomplete neighbors can't cause a wrong merge.

This runs as a standalone, resumable scraper (real browser, human-paced, parallel instances) that **only reads** an address queue and **writes a results file** — it never touches the database. The database changes are applied separately by the same reversible quarantine mechanism above.

### Keeping it collapsed (forward)

The same Redfin ground truth becomes a **fallback** on the live path: when an incoming new or sold listing can't be confidently reconciled against a home we already own, it's resolved against Redfin **once**, and the resulting perfect address is stored on the canonical record — so the next time that address appears, it resolves instantly against our own data and never needs an external lookup again.

## Data sources for a home

Each field has exactly one authoritative source:

- **Structural facts** (beds/baths, square footage, year built, style, lot, features, HOA, assigned schools): the **MLS record via Repliers**.
- **Sold price and date**: the **MLS** (`soldPrice` + `timestamps.closedDate`), captured always, **display-restricted** in non-disclosure states.
- **HouseLoop Value (active listing)**: derived from the **current list price** (Zillow publishes no Zestimate for on-market homes). See [Home Valuation](/guides/home-valuation-dashboard).
- **HouseLoop Value (sold / past-client)**: anchored on the sold price (or, for already-enriched past-client homes, a Zillow Zestimate captured in a prior one-time backfill).

Geographic appreciation (`lib/valuation/appreciation.ts`) drifts an anchor value over time for the value-history chart. See [Sold Data & Price History](/guides/sold-data-and-price-history).

## Key files

| Concern | File |
|---|---|
| Deterministic normalizer (suffix/directional) | `lib/listings/canonical-address.ts` |
| Address-as-ID service (identityKey, preferredCity, validateAddress) | `lib/listings/address-identity.ts` |
| Address resolver (identity → coords → mint) | `lib/listings/resolve-home.ts` |
| Listing ingest (calls the resolver before upsert) | `lib/listings/cache.ts` |
| ZIP → USPS-preferred-city map | `lib/listings/zip-preferred-city.generated.json` |
| Duplicate collapse (Redfin ground-truth tool) | `tools/houseloop-reconcile/` |
| Geographic appreciation model (value drift) | `lib/valuation/appreciation.ts` |
| `/home` page + canonical redirect | `app/home/[slug]/page.tsx` |
| Sitemap (one URL per home) | `app/sitemap.ts` |
