---
title: "AI Voice"
description: "THE one AI Voice doc — Myli answers the phone. CURRENT runtime (2026-07-18) = GEMINI LIVE native-audio SPEECH-TO-SPEECH (one model, audio→audio; gemini-2.5-flash-native-audio, voice Leda) hosted on PIPECAT CLOUD (apps/myli-voice-agent/pipecat_bot.py, MYLI_ENGINE=s2s), WIRED to Twilio via the CRM voice route (phone_provider.pipecat_cloud_agent). The old Deepgram→Haiku→Cartesia cascade is RETIRED — kept only as the MYLI_ENGINE=cascade fallback. This page reads top-to-bottom: the S2S stack, the lines she covers, her prompt architecture (lean base + 4 buckets), the CRM seam, the host, cost, then go-live (gauntlet + polish). Cascade / Retell / LiveKit+Gemini are quarantined in HISTORY."
---

<Info>
**Running-document law (Zach 2026-07-17).** The UPDATE LOG below is newest-first; every change stamps an
entry AND re-edits the body to current truth — no stacked "superseded STATE" blocks (git history holds the
past). Everything under **HISTORY** at the bottom is quarantined dead eras, not current.
</Info>

## Update log

- **2026-07-18 (PM)** — **The wire is IN.** The CRM voice route now streams myli-mode calls to the deployed
  Pipecat Cloud agent: new `hl_settings.phone_provider.pipecat_cloud_agent` config (env `PIPECAT_CLOUD_AGENT`
  overrides; prod = `myli-voice.spradling-group`) → `<Stream url="wss://api.pipecat.daily.co/ws/twilio">` with
  the `_pipecatCloudServiceHost` parameter (§06). Fallback chain: cloud agent → ngrok `pipecat_ws_host` →
  LiveKit SIP → voicemail. Commit `b45e401fd4`, deploy verified SUCCESS + Inngest re-synced. Next: the
  913-937-9359 gauntlet call, then transcript + `ai_voice` cost polish.
- **2026-07-18** — **The speech-to-speech pivot + a real host.** Ripped out the 3-hop cascade
  (Deepgram STT → Claude Haiku → Cartesia TTS) and put Myli on **Gemini Live native-audio speech-to-speech**
  — ONE model, audio in → audio out (`gemini-2.5-flash-native-audio-preview-09-2025`, voice **Leda**,
  `thinking_budget=0`) behind a `MYLI_ENGINE=s2s` flag in `pipecat_bot.py`. Zach A/B'd it: *"night and day"*
  better — no metallic TTS, no serial-hop lag. **Diagnosed the real latency killer:** the compounding delay
  (1→3→14 s) was never the model — it was the free-ngrok tunnel on a laptop (network backpressure). Fix =
  **deployed to Pipecat Cloud** (agent `myli-voice`, org `spradling-group`, agent-2x, us-west, cold-start).
  Re-architected the prompt into a **lean triage base + 4 specialized buckets** (seller/buyer/past_client/other)
  in `hl_settings.myli_voice_buckets` (migrations 0419 + 0420), selected mid-call by a `set_caller_type` tool.
  **The one thing left:** wire Twilio → the deployed agent (§06) — *done later same day, see the PM entry.*
  The cascade is retired to the fallback flag.
- **2026-07-17 (PM)** — Operation One Provider, voice edition: cascade brain OpenAI `gpt-4o-mini` → Claude
  Haiku 4.5, mouth ElevenLabs → Cartesia Sonic-3; built the Pipecat runtime; wired `ai_voice` cost. *(All
  superseded 2026-07-18 by the S2S pivot — the cascade this shipped is now the `MYLI_ENGINE=cascade` fallback.)*
- **2026-07-17 (AM)** — Corrected the brain lie (documented as Claude, code was `gpt-4o-mini`); found the
  deploy truth: the Pipecat bot was never hosted; last real call was July 11.
- **2026-07-15** — LiveKit+Gemini native S2S abandoned (crashed on 8 kHz phone audio) → cascade built. *(The
  crash was a fixed µ-law/16 kHz plumbing bug, NOT a model failure — Pipecat's `TwilioFrameSerializer` now
  handles the resample; that's why S2S works today. See HISTORY.)*
- **2026-07-11** — Retell era (built, then reverted).

<Note>
**The single doc for the agent that answers.** The telephony half — which numbers exist, porting, forwarding,
routing groups, A2P — lives in [Phone Numbers, Call Routing & Number Porting](/guides/phone-numbers-and-call-routing).
Person-to-person call *records* live in [Calls & Comms](/guides/comms-calls). This page is **Myli, the voice agent**.
</Note>

# ═══ 01 · THE STACK (one model, audio in → audio out) ═══

Myli is built on **Pipecat** (Daily.co) — the voice framework that solves the hard audio problems (barge-in,
turn-taking, VAD, low-latency streaming, the Twilio transport) as defaults. We bring only the logic: her prompt
and the CRM wrap-up. The engine is **Gemini Live native-audio speech-to-speech** — ONE model that takes the
caller's audio and speaks back directly. No STT step, no separate TTS step, no metallic voice, no serial-hop lag.
This is the architecture UWM's "Mia" runs (Google Vertex AI / Gemini Live); we validated it against that
blueprint and the official Pipecat reference preset.

The signal flow is now two hops, not four:

| Layer | Choice | Billed by | Notes |
|---|---|---|---|
| **Transport** | Twilio Media Streams → `<Connect><Stream>` | per-minute | Live G.711 µ-law audio over WebSocket; `TwilioFrameSerializer` handles the µ-law↔PCM resample (the bug that killed the 2026-07-15 attempt). |
| **The model (S2S)** | **Gemini Live** `gemini-2.5-flash-native-audio-preview-09-2025`, voice **Leda**, `thinking_budget=0` | per audio token | Audio→audio in one model. Gemini's own VAD (LOW/LOW ~800 ms) owns turn-taking. `GEMINI_API_KEY`. |
| **Seam** | CRM `persona` (in) + `wrapup` (out) | — | Prompt + buckets in at call start; the one write out at hangup. |

**Config = the official reference preset, nothing more.** Only 4 Settings fields: `model`, `voice`,
`system_instruction`, `thinking=ThinkingConfig(thinking_budget=0)`. We tried the v1alpha "affective dialog" /
"proactive audio" knobs and **removed them** — they add latency for no clear win (`MYLI_GEMINI_EXPRESSIVE=0`
by default). The greeting is triggered with a `role:"developer"` message +
`inference_on_context_initialization=False` (so she opens on cue, not on connect).

**The runtime files** (`apps/myli-voice-agent/`): `pipecat_bot.py` (the bot — `MYLI_ENGINE=s2s` picks the
Gemini branch; `cascade` = the retired fallback) · `requirements-pcc.txt` (deps for the Pipecat Cloud image) ·
`Dockerfile.pcc` (`FROM dailyco/pipecat-base`) · `pcc-deploy.toml` (the Pipecat Cloud deploy config) ·
`pcc-build/` (the minimal 3-file build context). Every model/voice value is env-overridable
(`MYLI_ENGINE`, `MYLI_GEMINI_MODEL`, `MYLI_GEMINI_VOICE`) so we retune without a code change.

# ═══ 02 · THE LINES SHE COVERS ═══

Myli is the **AI Receptionist**. The number a caller dials **is** the intent — each line feeds one CRM inbox:

| Bucket | Number | Feeds inbox | `answer_mode` today | Myli's job |
|---|---|---|---|---|
| **Test line** | 913-937-9359 | — | `myli` (armed) | The gauntlet line — where we prove her before going live. |
| **Past Agents** | 913-675-9877 | Past Agents | `forward` | ⬅️ **first to flip** — reactivation: name the departed agent, take a callback. |
| **Company** | 913-320-0906 | Company | `forward` | Speed-to-lead: buyer/seller triage, capture + route. |
| **Sign Call** | 913-354-8600 | Company | `forward` | High-intent: "which property are you standing at?" |
| **Ana's line** | 913-954-7123 | Ana | `forward` | Ana's past clients — always AI: "I'll have Ana call you right back." |

Each line stays on cell-forward until Myli passes the gauntlet **and** Zach signs off — then it flips to
`answer_mode='myli'` one at a time. Cell-forward is the instant rollback. The four production buckets are parked
per the [routing untangle](/guides/phone-numbers-and-call-routing).

# ═══ 03 · HER PROMPT — LEAN BASE + FOUR BUCKETS ═══

Native-audio models get **worse** as the prompt gets longer and vaguer, so Myli's prompt is deliberately
small and structured. It has two parts:

**1. The lean triage base** (`hl_role.persona` on the AI Receptionist role, edited in **My Team → AI
Receptionist**). Structure, in order — the Google Live-API prompting shape:

- **Identity** — Myli, Spradling Group's front desk. Warm, upbeat, casual, quick — never a stiff IVR.
- **Voice / style** — one question per turn; she listens, she doesn't monologue; spell numbers out; never
  read a list aloud; never narrate calling a tool.
- **Job, in order** — figure out who's calling (seller / buyer / past client / other), then run that bucket.
- **Guardrails** — never fakes being human, never live-transfers; on hangup she writes the lead + a callback.

**2. The four specialized buckets** (`hl_settings.myli_voice_buckets`, seeded by migrations **0419** +
**0420**). The moment Myli identifies the caller she calls the **`set_caller_type`** function tool
(`seller` / `buyer` / `past_client` / `other`) — that RECORDS the label for the wrap-up, and the matching
bucket is the playbook she follows. Each bucket is a **collect-in-order checklist with the VERBATIM question
for each field**, so she can't paraphrase the intent away:

- **seller** → *"What's the address of the home you're thinking of selling?"* with a CAPS rule: **DO NOT accept
  just a city or an area — you need the full street address** (if they give a neighborhood, follow up "and
  what's the street address there?", then read it back). This bucket is why v2 exists — v1 said "roughly where"
  and Myli asked *"what area?"* instead of the address.
- **buyer** → *"What area are you looking to buy in?"* (or the specific listing's address if they name one),
  then budget / timeline.
- **past_client** → greet like a friend of the team, *"What can I help you with?"*, capture the reason; if it
  turns into a sale/purchase, move into the seller or buyer questions.
- **other** → vendor / general question / wrong number / asking for Zach or Ana directly — take a message + callback.

<Note>
In the S2S engine the base + all four bucket playbooks are inlined into the single `system_instruction` at call
start (native-audio can't hot-swap the system prompt mid-turn), and `set_caller_type` just records which bucket
applies for the wrap-up. Editing a bucket = a SQL update to `hl_settings.myli_voice_buckets`; editing the base
= the role persona in My Team. Neither touches the bot.
</Note>

# ═══ 04 · THE CRM SEAM (two endpoints) ═══

The bot talks to the CRM at exactly two seams:

| Route | Method | One line |
|---|---|---|
| `/api/phone/myli/persona` | GET | Returns `{ persona (lean base), ground_truth, buckets }` — everything the bot inlines into the prompt. |
| `/api/phone/myli/wrapup` | POST | **THE ONE WRITE SEAM** — every post-call CRM effect. |

**The wrap-up receipts (one POST, every effect):** spam → silent flag (no lead, no ping) · find-or-**create**
the lead (door law: source `phone_call`, assignment/stage from the source defaults, never hardcoded) · Myli
joins the care team · `hl_calls` gets summary + role + callback · a callback task on the assigned agent ·
`logActivity` `call_logged` (the All-feed + AI Follow-Up row) · `notifyLeadEvent` → the assigned agent.

<Warning>
**Transcript capture is a known follow-up.** The cascade era captured the transcript via a `TranscriptProcessor`
in the pipeline; that processor was **removed** because it isn't in the Pipecat Cloud base image's newer Pipecat.
Native-audio transcript-capture via the aggregator turn events is the re-add — tracked in §06. Until then the
wrap-up records the summary + role + callback, but not a full turn-by-turn transcript.
</Warning>

**The answer gate — 3 layers, all must pass:** (1) org master `hl_settings('ai_conversations').voice_enabled`
(fail-closed) · (2) per-line `hl_phone_numbers.answer_mode='myli'` · (3) role capability `voice_inbound`.

# ═══ 05 · THE HOST — PIPECAT CLOUD ═══

The bot no longer runs on a laptop tunnel — it's deployed to **Pipecat Cloud** (Daily's managed host for Pipecat
agents). This is the fix for the compounding latency: a free-ngrok tunnel on a laptop can't keep up with
real-time audio backpressure, so time-to-first-byte grew every turn (1→3→14 s). On a real host, latency is flat.

| Field | Value |
|---|---|
| **Org / workspace** | `spradling-group` (login = GitHub OAuth, `zachspradling@spradlingrealtygroup.com`) |
| **Agent** | `myli-voice` |
| **Profile** | `agent-2x` (1 vCPU / 2 GB dedicated — real-time audio needs non-throttled CPU) |
| **Region** | `us-west` (revisit `us-east` for Twilio latency later) |
| **Scaling** | **cold-start** (`min_agents=0`) — $0 idle, ~few-sec spin-up on the first call after idle (Zach's call: the line gets spam, so warm-idle isn't worth ~$43/mo yet). Flip to `min_agents=1` if the cold-start delay bugs us. |
| **Secret set** | `myli-voice-secrets` (`MYLI_ENGINE=s2s`, `GEMINI_API_KEY`, `HL_CRM_BASE_URL`, `HL_INTERNAL_SECRET`, `MYLI_VOICE_SECRET`, `TWILIO_*`) |
| **Status** | Deployment **ready** (2026-07-17). **Wired to Twilio 2026-07-18** — the CRM voice route streams myli-mode calls here (§06). |

**The deploy pipeline** (config in `pcc-deploy.toml`):

```bash
# from apps/myli-voice-agent/, with PIPECAT_TOKEN in env, using the venv CLI:
.venv-pipecat/bin/pipecat cloud deploy --build-dir pcc-build --dockerfile Dockerfile.pcc --yes
```

- The build context is the **minimal `pcc-build/`** dir (bot.py + Dockerfile + requirements-pcc.txt, ~32 KB).
  The full app dir is 686 MB (the venv) — over Pipecat Cloud's build-context cap, so it must NOT be the context.
- The base image imports `bot(runner_args)` from `bot.py`, so `pipecat_bot.py` is copied in as `bot.py`.
- Secrets: `.venv-pipecat/bin/pipecat cloud secrets set myli-voice-secrets --file .env.pcc --skip` (needs a
  PTY — run it under `script -q /dev/null …`).

**Cost.** Cold-start = **$0 idle**. Per active minute ≈ Gemini Live audio tokens + Twilio (~$0.0125/min) + the
Pipecat Cloud agent-2x host (~$0.02/min). Rough all-in ≈ **$0.06–0.09/min** of talk time.

<Warning>
**S2S cost metering is NOT yet wired.** The `MyliUsageObserver` + `logAiCost({category:'ai_voice'})` path was
built for the **cascade** (LLM tokens + TTS chars + Deepgram minutes). Gemini Live bills by **audio tokens**,
which the observer doesn't sum, so a live S2S call currently does NOT land a real cost row on `/ai-cost`. Re-wiring
the cost meter for the audio-token model + the Pipecat Cloud host cost is a go-live follow-up (§06).
</Warning>

# ═══ 06 · GO-LIVE — the wire is in; gauntlet + polish remain ═══

**The Twilio → Pipecat Cloud wire SHIPPED 2026-07-18** (commit `b45e401fd4`, CRM deploy verified SUCCESS).
The CRM voice route (`app/api/phone/webhooks/voice/route.ts`) reads `hl_settings.phone_provider.pipecat_cloud_agent`
(env `PIPECAT_CLOUD_AGENT` overrides; set to `myli-voice.spradling-group` in prod) and, when present, emits:

```xml
<Connect>
  <Stream url="wss://api.pipecat.daily.co/ws/twilio">
    <Parameter name="_pipecatCloudServiceHost" value="myli-voice.spradling-group"/>
  </Stream>
</Connect>
```

(us-west = the bare `api.pipecat.daily.co`; other regions prefix the host.) **Fallback chain preserved:**
cloud agent → ngrok `pipecat_ws_host` → LiveKit SIP → `<Record>` voicemail. Instant rollback = clear
`pipecat_cloud_agent` from the config (no deploy needed).

**Go-live checklist:**

- [x] S2S engine built + A/B-approved (Gemini Live, voice Leda) — *"night and day"* better than the cascade.
- [x] Lean base + 4 buckets seeded (migrations 0419 + 0420); `set_caller_type` records the bucket.
- [x] Deployed to Pipecat Cloud (`myli-voice`, agent-2x, us-west, cold-start) — status **ready**.
- [x] Secrets set (`myli-voice-secrets`, `MYLI_ENGINE=s2s`).
- [x] **Wire Twilio → the deployed agent** (the `_pipecatCloudServiceHost` TwiML above) — shipped 2026-07-18, commit `b45e401fd4`.
- [ ] Test-call **913-937-9359**: she greets once, latency flat ~1 s, seller flow asks the ADDRESS, `set_caller_type` fires (watch `pipecat cloud agent logs myli-voice`).
- [ ] Re-add transcript capture (aggregator turn events) → the wrap-up.
- [ ] Re-wire `ai_voice` cost for the audio-token model + the Pipecat Cloud host cost.
- [ ] Decide `min_agents=1` (always-warm) vs cold-start after hearing the real cold-start delay; consider `us-east`.
- [ ] Zach sign-off → flip the four buckets to `answer_mode='myli'` one at a time (cell-forward = rollback).

# ═══════════════════════════════════════════════════════════════════════════════
# HISTORY — DEAD ERAS (not current; git holds the rest)
# ═══════════════════════════════════════════════════════════════════════════════

<Warning>
**Everything below is quarantined, reverse-chronological — none of it is the current runtime** (top of page:
Gemini Live S2S on Pipecat Cloud). Kept for receipts only.
</Warning>

- **The Pipecat CASCADE (2026-07-16 → 2026-07-18, RETIRED to the fallback flag).** Deepgram STT (`nova-2-phonecall`)
  → Claude Haiku 4.5 → Cartesia Sonic-3 (voice *Skylar*). It worked, but it was **laggy** (~1.5 s floor from three
  serial network hops) and **metallic** (Cartesia TTS at 8 kHz). Replaced 2026-07-18 by Gemini Live S2S. **The
  code still lives** behind `MYLI_ENGINE=cascade` (the `else` branch in `pipecat_bot.py`) as a fallback if the S2S
  model has an outage — it is NOT the runtime. Its 3-vendor cost wire (`MyliUsageObserver` → `logAiCost`) is what
  §05 flags as not-yet-ported to the S2S audio-token model. An even earlier cascade used an OpenAI `gpt-4o-mini`
  brain + ElevenLabs TTS; that brain died when the OpenAI account hit $0 on 2026-07-16.
- **LiveKit + Gemini native S2S (2026-07-15, crashed — but the crash was a fixable bug, not a model failure).**
  `agent.py` — LiveKit Agents + Gemini `gemini-2.5-flash-native-audio-latest` (voice Leda), on LiveKit Cloud
  (agent `CA_PCXvBdEGhXgZ`, project `houseloop`). It threw Gemini WebSocket close codes `1007`/`1011` on real
  8 kHz phone audio — which at the time we read as *"raw speech-to-speech is banned on telephony."* **That was
  wrong.** The real cause was a µ-law↔16 kHz/24 kHz sample-rate mismatch in the audio plumbing; Pipecat's
  `TwilioFrameSerializer` resamples it correctly, which is exactly why S2S works today on Pipecat. The LiveKit
  zombie may still be deployed with a `GEMINI_API_KEY` — tear-down candidate. The `deploy-livekit.sh` /
  `livekit.toml` / old `Dockerfile` in `apps/myli-voice-agent/` target this dead era.
- **Retell AI (2026-07-11, reverted).** Myli briefly ran on Retell (agent `agent_e4122038…`, 11labs-Grace voice,
  ~580–620 ms). Zach: *"retell was a shit show."* The `/api/phone/myli/retell-*` routes and the
  `phone_provider.livekit_sip_domain` key (`sip.retellai.com`) are dead leftovers — deletion candidates.

**Config surfaces (unchanged across eras):** the AI Voice master toggle (`/ai-follow-up` →
`hl_settings('ai_conversations').voice_enabled`, fail-closed) · the **ANSWERED BY** per-line select
(`/settings/phone-numbers` → `answer_mode`) · the **Calls & Voice** capability tree (My Team role editor →
`voice_inbound`) · the persona layers (role editor + per-agent override) · the `/ai/voice` equipment page.
