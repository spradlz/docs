---
title: "Phone Porting PRD — Own the Numbers"
description: "The product requirements doc for the HouseLoop phone PORTING initiative: port the 3 real assets into our Twilio, forward the legacy pool to one line, and unify call capture + the phone_call_received notifications so HouseLoop owns every business number and every call record. Decisions, requirements, acceptance criteria, phases, and owners. (The Myli Voice agent that answers these lines rolls in AFTER porting — see the Myli Voice page.)"
---

<Info>
**This is the porting PRD — the spec the phone-ownership work executes against.** The how-to / reference lives in
[Phone Numbers, Call Routing & Number Porting](/guides/phone-numbers-and-call-routing). The **voice agent that
answers these lines is a separate, later phase** — full spec on the [Myli Voice](/guides/myli-voice) page. Owner:
**Caleb** (capture) · **Lee** (notifications) · Zach (telephony/porting).
</Info>

<Note>
**Scope of THIS doc = ownership + capture + notifications.** Porting the numbers and capturing every call natively
comes first. Putting Myli on the lines as the voice agent is **parked until porting is done** and lives on the
Myli Voice page — not here.
</Note>

## Goal

**Own every business number in our own Twilio, and capture every call natively — records + notifications.** Kill
the three-source shit-show (FUB / Twilio / Google Voice) and collapse it to one pipe: **Twilio captures, FUB
mirrors.** (Once we own the lines, the [Myli Voice](/guides/myli-voice) agent becomes the front door — that's the
next phase, not this one.)

## The problem (why this exists)

- Phone-call notifications (Company / Sign / Past-Agent / Ana) are **documented but fire 0** — the trigger
  `phone_call_received` is wired to Twilio, but the numbers route through FUB + Google Voice, so nothing reaches it.
- FUB **call capture broke** at the webhook cutover (stale since ~2026-07-07) and **never captures cold/unknown
  callers** to the company line.
- **Ana's calls run through Google Voice** — invisible to both FUB and Twilio.
- No transcripts exist anywhere (FUB has none).

## The decisions (LOCKED 2026-07-09)

| # | Decision |
|---|---|
| D1 | **Port 3 assets → our Twilio:** Company `913-320-0906`, Sign/Listings `913-354-8600`, Ana `913-954-7123` (out of Google Voice). |
| D2 | **Forward the legacy pool** (`913-453-5505`, `913-270-0645`, +37 past-agent) → ONE dedicated "Legacy Agents" Twilio line. Stay in FUB; do NOT port. |
| D3 | **One notify key `phone_call_received`, 5 subtypes** resolved by dialed-number → `hl_phone_numbers.category` (company/sign/past_agent/ana + generic). Shared resolver used by BOTH the Twilio webhook and the FUB path. |
| D4 | **Ported numbers mirror back into FUB** via `POST /v1/calls` so the FUB timeline stays intact (they leave FUB on port-out). |
| D5 | **A2P 10DLC is already registered** under our Twilio account — the ported numbers get **added to the existing campaign**, no new brand/campaign registration needed. |
| D6 | **The line owner is the agent (Myli Voice), NOT part of this PRD.** Who *answers* the owned lines is the [Myli Voice](/guides/myli-voice) build — it rolls in after porting. This PRD only gets the numbers owned + captured. |

## Requirements

<AccordionGroup>
  <Accordion title="R1 — Ownership & routing">
    - Port D1's three numbers into our Twilio (own them natively).
    - Forward D2's legacy pool → the single "Legacy Agents" Twilio line (same-day FUB config).
    - Caller-ID pass-through on every forward (see who actually called, not the FUB inbox number).
    - Add the ported numbers to the **existing A2P campaign** in Twilio.
  </Accordion>
  <Accordion title="R2 — Capture (Caleb)">
    - Store every call natively on Twilio inbound (metadata + recording + which line), **including cold/unknown
      callers** to the company line.
    - Shared subtype resolver `resolvePhoneCallSubtype(dialedNumber) → hl_phone_numbers.category → subtype`,
      called by both the Twilio webhook and the FUB path.
    - `mirror_to_fub` — write each ported-number call back to FUB (`POST /v1/calls`) with attribution + recording.
    - Google Voice ingest for Ana until her number is ported (then retire GV).
  </Accordion>
  <Accordion title="R3 — Notifications (Lee)">
    - `phone_call_received` fires the correct 1-of-5 typed card by subtype.
    - Lead-less path for cold callers (no person match required).
    - `fub_created` recency gate so a one-off catch-up never notifies on old activity.
  </Accordion>
</AccordionGroup>

## Acceptance criteria

- ☐ Call any of the 4 lines → a lead appears in HouseLoop CRM with the correct source + recording.
- ☐ The matching typed notification (`phone_call_company/sign/past_agent/ana`) fires — no more 0s.
- ☐ A cold unknown caller to the company line is captured + notified (lead-less).
- ☐ A ported-number call also appears on the FUB timeline (mirror).
- ☐ Ana's calls captured (GV bridge now; native after port).

## Phases & owners

<Steps>
  <Step title="Phase 1 — Legacy line live (same-day, no porting)">
    Forward the legacy pool → the Twilio "Legacy Agents" line. Capture the call (`create_lead` + recording) +
    fire the notification. Proves the full capture loop without waiting on carriers. **Owner: Caleb** (capture),
    Zach (FUB forward config).
  </Step>
  <Step title="Phase 2 — Capture + notify unified (in parallel)">
    Shared subtype resolver · store cold/unmatched calls + recording · `phone_call_received` fires all 5 subtypes ·
    `mirror_to_fub`. **Owner: Caleb** (capture) + **Lee** (notify).
  </Step>
  <Step title="Phase 3 — Port the 3 assets (1–2 weeks, carrier-gated) — IN PROGRESS">
    Port Company + Sign + Ana into Twilio; add to the existing A2P campaign; retire Google Voice. **Owner: Zach**
    (port requests) + Caleb (webhook wiring on landing). **Live status below.**
  </Step>
</Steps>

## Porting status (as of 2026-07-10)

<Note>
**Ana's (913) 954-7123 (Google Voice → Twilio) — filed, diagnosed, awaiting address propagation.**
Filed + LOA signed by Ana, but **rejected twice `INVALID_ADDRESS` (22154)**. Root cause is a **timing trap, not a
wrong value**: the port validates the service address against the **losing carrier's (Bandwidth) record**, and
Google warns **"Service addresses register within 2 business days"** — the Florida service address was changed but
hadn't propagated when we submitted. **Plan: re-POST the same values ~2026-07-13** (a reminder is scheduled).
Two hard-won method learnings, now baked into the [how-to / port-out playbook](/guides/phone-numbers-and-call-routing#port-a-google-voice-workspace-number-twilio-the-913-954-7123-recipe-battle-tested-2026-07-09):
- **Use the Twilio Porting API, not the beta UI** — the "Port-in US Local (Beta)" wizard silently drops the request
  when driven by automation. `POST numbers-upload.twilio.com/v1/Documents` (bill) → `POST numbers.twilio.com/v1/Porting/PortIn`.
- **Google Workspace Voice port-out creds:** PIN from **Admin → Google Voice → Number porting → "Port-out info"**;
  account number = the **Google Billing ID** (no dashes), not the phone number.

**FUB ports (Company 913-320-0906 · Sign 913-354-8600):** port-out packet requested from FUB support; a daily watcher
surfaces the reply. **Legacy line:** the Twilio "Legacy Agents" line **(913) 675-9877** is LIVE and call-verified; the
39 past-agent FUB numbers (incl. the departed-agent Ben Froeschl 913-379-3429 seat) forward here — flip pending.
</Note>

## → Next phase (NOT this PRD): Myli Voice

Once the lines are owned + captured, **[Myli Voice](/guides/myli-voice)** becomes the front door — one AI agent
across all four lines, per-line prompt by dialed number, intent routing, live-transfer, appointment booking. That
buildout is fully spec'd on its own page and is **parked until this porting PRD is done.**

## Open items / risks

- **FUB `POST /v1/calls`** — confirm the API supports logging a call back to a person (for D4/mirror). Caleb to verify.
- **Google Voice → Twilio port** sometimes rejects direct (VoIP); two-step via a prepaid SIM is the fallback.
- **Legacy line can't distinguish which original number was dialed** — all resolve to `phone_call_past_agent` (FUB still holds the exact number if needed).

## Reference

- [Phone Numbers, Call Routing & Number Porting](/guides/phone-numbers-and-call-routing) — the how-to + port-out playbook.
- [Myli Voice](/guides/myli-voice) — the voice-agent build-out (the phase after porting).
