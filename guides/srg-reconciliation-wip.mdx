---
title: "SRG Reconciliation Engine — WIP"
description: "The working doc for getting SRG past-client data right, in the right tables, verified. Planning + build log."
---

<Warning>
**Active WIP (2026-07-01).** Rule for today: prove on the **FIRST 20** before anything touches 1,800. "Good" = the
correct data EXTRACTED and SITTING IN THE RIGHT TABLE — not "the script ran." Canonical source: `GT/GT - Pending/GT_SRG_RECONCILIATION_ENGINE_WIP.md`.
</Warning>

## What we've done
Harvested 5 source tables over 2 days (SISU, FUB deal report, MLS rooftop history, dotloop, FUB Haiku brains). The **s6**
agentic resolver (~$200 Sonnet) ran on all ~1,834 on the **29th** → populated the cards with deals, the **household FUB-ID
resolution** (`fub_identity`, 94% coverage, dedup + spouse merge), and live GIS/deed discoveries (current homes, lost
buyers). **13 gold cards were hand-corrected by Zach on the 28th = ground truth.**

## The fuck-up
1. Built a **from-scratch resolver** that ignored s6's discoveries and wrote to a **scrap table** the audit never reads → thin/wrong cards + constant "my list vs your page" divergence.
2. **Name-match bug:** exact first-name match dropped SISU deals filed under a partial name ("Kyle" vs "Kyle & Karly") → Nations lost 2 of 5 deals + its lost listing.
3. **Brain harvest ran on the WRONG pool** (`hl_fub_sisu_link`) → dossiered 3,542 non-clients, **skipped 1,044 past clients** incl. Nations.
4. **Dotloop licensee 85% empty** (10,361/12,143 NULL agent) — reported "done" off a process exit, never measured coverage.

## Lessons (locked)
- **The table is the truth. ONE source.** Resolver writes the SAME canonical tables the audit reads.
- **"Done" = correct data IN the table, verified by query.** Measure coverage, never trust "exit 0."
- **Reconcile, don't rebuild.** Combine s6's discoveries with the harvest tables; correct dispositions, keep GIS finds.
- **Match fuzzy (surname + first-token); merge households.** Prove on 20 vs the golds before scale.

## FUB-ID + household coverage (live)
| Metric | Count |
| --- | --- |
| Total past clients | 1,834 |
| **Have a resolved FUB household** (`fub_identity`) | **1,732 (94.5%)** |
| No FUB link at all | 100 |
| Duplicate FUB profiles detected + flagged | 366 |
| Spouse / 2nd member resolved | 185 |
| Distinct household FUB IDs mapped | 2,178 |

## The data map — where every piece lives (2026-07-01)

### CRM DB (`HL_CRM_DATABASE_URL`, autorack)
| Table | Rows | Contains | Written by |
| --- | --- | --- | --- |
| `hl_srg_past_clients` | 1,834 | card parent: name, bucket, `fub_identity` (1,732), dossier (473 carded, 13 gold) | s6 + Zach hand-fix |
| `hl_srg_past_client_homes` | 2,168 | deal/home child: address, role, `determined_agent` (535), disposition, resale | s6 / attach-closing-facts |
| `sisu_transactions` | 11,636 | closings (1,945 closed): amount, date, agent, address | SISU import |
| `hl_fub_deals` | 4,554 | FUB deal report (4 pipelines) | FUB deal-report harvest |
| `hl_mls_property_history` | 5,919 / 1,547 rooftops | rooftop sales: date, price, `list_agent` only (buyer-agent MISSING) | harvest-mls-history (Matrix) |
| `hl_srg_dotloop_loops` | 17,455 | loop index | dotloop-loop-index |
| `hl_srg_dotloop_contracts` | 12,143 | contract → licensee — **10,361 NULL agent**, all have PDFs | dotloop-contract-fetch |
| `hl_fub_person_facts` | 4,214 | Haiku brain (facts+dossier) — WRONG POOL, only 686 are past clients | harvest-fub-facts (Haiku) |
| `hl_srg_agent_roster` | 136 | every SRG agent | build-srg-roster |

### FUB mirror (`FUB_RAILWAY_DATABASE_URL`, `fub.*`)
`fub.people` 49,229 · `fub.text_messages` 444,864 · `fub.notes` 140,223 · `fub.calls` 39,867 · `fub.events` 146,292 — Camp David FUB DOM scraper.

## The model — HLID is the universal join key (locked)
One **HLID** per house, permanent. Every house-table keys on it:
- `hl_srg_past_client_homes` = **ONE row per HLID**, transactions (buy/sell/resale) UNDER it, disposition derived. Links to `/home/<slug>/<HLID>`.
- `hl_mls_property_history` = full ours-flagged history of that house — **must ADD an HLID column + backfill** (today it joins only by fuzzy address).
- website `Listing` = the public house page.
Join by HLID → "sold after we helped them buy?" = lost listing. **HLID coverage on past-client homes: 2,166/2,168 (99.9%)** — the only exclusion is W 263rd St (LAND, no physical address, client DNC → noted unresolvable in DB).

## Today's plan — checkboxes, in order
Each box's "done" = the correct data VERIFIED in its table.

- [ ] **1. DEDUPE (two kinds)** — (a) **cross-client** rows → one card per person (pc56→pc9 Belcher; keeper = richest DATA, never the name). (b) **within-client HOME collapse by HLID** — same HLID = one home row, buy+sell under it (Belcher 3 rows → 2 homes). ~154 rows collapse; fixes flattened-price bug.
- [ ] **2. BRAIN** — fix the Haiku pool → household FUB IDs on the cards (2,178); run the ~1,044 missing past clients (merge households). Tool already correct.
- [ ] **3. MLS** — every deal → its true MLS record + agents (`list_agent` + `selling_agent` both exist), address corrected off MLS.
- [ ] **4. ROOFTOP** — ADD HLID link to `hl_mls_property_history` + backfill; harvest the **~389 missing rooftops** (incl. 8748 Daniel Boone); full ours-flagged history per HLID.
- [ ] **5. LOOP** — re-parse the 10,361 dotloop contract PDFs → licensee.
- [ ] **6. RECONCILE** — combine s6 discoveries + tables → write CANONICAL at one-row-per-HLID grain.
- [ ] **7. PROVE THE FIRST TWENTY** — match the golds; Zach signs off → scale to 1,834.


## Audit matrix — SITREP (2026-07-01)
Legend: ✅ correct + not empty · 🔄 running now · ⚠ gap.

| Table / component | Have — shows | Missing | State |
| --- | --- | --- | --- |
| `hl_srg_past_clients` | 1,834 · 1,732 FUB-linked (94%) · 473 carded · 13 gold | ~50 dup rows · 1,361 no card | ⚠ dedup+rebuild |
| `fub_identity` household ids | 2,178 mapped · 366 dup profiles flagged | ~100 no link (dups) | ✅ |
| `hl_srg_past_client_homes` | 2,168 · **2,166 HLID (99.9%)** · /home URL | 154 same-home split (collapse by HLID); 263rd land excluded | ⚠ HLID collapse |
| `hl_fub_person_facts` (Haiku) | 4,214 — WRONG pool (only 686 clients) | **1,481 past-client household members** | 🔄 running |
| `hl_srg_mls_deals` (OUR deals) | 1,971 · both sides · our_roles | — | ✅ |
| `hl_mls_property_history` (scrape) | 5,919 / 1,547 rooftops · list+selling agent cols | **428 whiffed (0 records, ok=true = LIE)** + 131 err + NO HLID link | ⚠ re-scrape 428 + HLID |
| `hl_mls_rooftop_status` (ledger) | 1,975 processed · 1,844 ok · 428 zero · 131 err | must FLAG whiffs, not hide | ⚠ fix silent-whiff |
| `hl_srg_dotloop_contracts` | 12,143 · 1,782 licensee (15%) · all have PDF | **10,361 NULL licensee** | ⚠ extract 10,361 |
| `hl_srg_dotloop_loops` | 17,455 | — | ✅ |
| `hl_srg_agent_roster` | 136 | — | ✅ |
| `sisu_transactions` | 11,636 (1,945 closed) | — | ✅ |
| FUB mirror (`fub.*`) | people 49,229 · texts 444,864 · notes 140,223 · calls 39,867 · events 146,292 | — | ✅ |

**Why the 428 whiffs matter most:** a departed agent (e.g. Noelle Owen → Keller Williams) sells a past client's house under KW — NOT in our deals, only in the public MLS rooftop history. A rooftop returning 0 records hides that lost listing. **`ok=true` on 0 records is a LIE.** The MLS resolver must re-run the 428 agentically with wildcard (`*`) fuzzy search and flag genuine whiffs.

**Go-through (one at a time, sign off each):** 1. Brain (running) → 2. Dotloop 10,361 agents → 3. MLS 428 agentic wildcard → 4. add HLID link to MLS → 5. merge rows → 6. reconcile → 7. prove 20.
