---
title: "Transaction Coordination"
description: "TC Loop — HouseLoop's own Transaction Coordination platform (our own dotloop, owned end-to-end): transactions, fillable contracts, self-hosted e-sign, the compliance archive, plus the architecture, data model, and build state."
---

<Info>
📌 **Last updated: July 23, 2026 (2026-07-23).** The **TC Loop** is its own top-level tab — the left menu
breaks into **TC Loop** (this overview · Forms · Email Queue), **Configure** (Transaction Status · Milestones ·
Tasks), and **Reference** (E-Signature · **Dotloop Integration** · Native Intake · A-Frame). If you don't see
these menu items on the live site, hard-refresh (Cmd+Shift+R) — you're on a cached copy.
</Info>

<Note>
**🔨 ACTIVE WORK — Zach + Benji · Friday 31 July 2026, 2:30 PM CT**

**The board:** [TC Loop — Contract Engine Build Plan](https://claude.ai/code/artifact/cd761514-1600-4e12-aaf1-2af99c1ae98f)
— the live checklist, the corrections on the record, and the six arcs left.

**What we are doing:** rebuilding the contract engine so an executed HouseLoop contract is indistinguishable
from a dotloop one, and every email about it is ours. Started when a real executed Exclusive Buyer Agency
came back with four defects on one document — and the biggest was underneath them: **no filled data had
ever rendered on a delivered contract.**

**Shipped today:** the data-render fix · dotloop-exact signature and initials blocks · Ana's disclosure
packet · migration 0531 (sections + document categories) · `tc_status_changed` signed.
**Next:** the exit rows (so a lost listing has somewhere to go), then the dotloop form import.

**Schema law:** [Transaction & Loop Model](/schema-lock/transaction-model) — LOCKED, zero open signatures.
**Handoff state:** `.claude/state/benji-handoff.md`
</Note>

## What this section is

**Transaction Coordination (TC Loop)** is the HouseLoop CRM section for running a deal contract-to-close.
It is **our own dotloop, built and owned end-to-end** — documents, forms, fill, self-hosted e-signature,
the compliance archive, and a direct brokerage push all live on our side. It is a *light*, AI-native
(Myli-fillable) version of a dedicated TC tool, built on HouseLoop's own rails and reusing the CRM's
existing components.

Where [Website Integration](/guides/listing-lifecycle) is about the *listing* and the CRM guides are about
the *lead*, **Transaction Coordination is about the deal** — the contract-to-close record.

## The model

| Layer | Source of truth | HouseLoop role |
|---|---|---|
| **Listing** (the home) | `Listing` (web DB) | renders `/home/<slug>`, value, history |
| **Lead** (the person) | `hl_leads` (CRM DB) | CRM, AI follow-up, activity log |
| **Transaction / Loop** (the deal) | **`hl_transactions`** (CRM DB) | the deal record — written by disposition |

**The transaction record already exists: `hl_transactions`** (disposition-driven — marking a lead
*Under Contract* or *Closed* in the pipeline writes a row with address · price · closing · commission;
`milestone` = `under_contract` \| `closed` \| `fell_through`). **Past clients = leads with a `closed`
transaction.** A **loop is ONE side** (`hl_transactions.transaction_type`: `purchase` → Buyer, `sale` →
Seller); a client buying AND selling has **two loops**.

The child tables that make up the loop file: `hl_tc_loop_folder` / `hl_tc_loop_document` (**our document
source of truth**), `hl_tc_loop_participant` (people), `hl_tc_loop_task` (per-loop task instances),
`hl_contract_template` / `hl_contract_fill` (fillable forms + a filled contract), `hl_tc_email` (email
queue).

## ⚙️ HOW A LOOP IS RUN — the two dials (WORKED OUT 2026-07-31 · schema LOCKED)

<Info>
**Schema lives in [Transaction & Loop Model](/schema-lock/transaction-model)** — table names, columns,
constraints, migration sequence. This guide is the OPERATING story: what Ana sees, what she may change, and
what happens when she changes it. It never restates schema.
</Info>

Every loop carries **two dials that answer different questions**. One field could never hold both — proven
on a live loop the morning of 2026-07-31, when a signed showing agreement produced a status that did not
match the stage.

| | **Loop status** — *where is the deal?* | **TC status** — *what does this file need?* |
|---|---|---|
| Who moves it | **nobody.** Derived from the stage's section | **the coordinator**, by hand |
| Ana sees | a read-only chip | a dropdown she owns |
| Values | Pre-Contract · Pre-Listing · Under Contract · Closed · *the exits* | Processing · Paused/Blocked · Waiting for Closing · Complete |
| Before a TC is assigned | already correct | **empty** — then `Processing` on assignment |

**Read them together and the loop tells its whole story:**

```
Under Contract   +  Waiting for Closing   deal is live, Ana's work is done, waiting on the date
Closed           +  Processing            house closed, still chasing commission docs + compliance
Pre-Contract     +  Processing            buyer agreement out for signature, Ana chasing it
Archived         +  Processing            contract died, still releasing earnest money
```

<Note>
**Why "Waiting for Closing" is not the same as the Clear-to-Close stage.** Clear to Close is the *lender*
clearing the file. Waiting for Closing is *Ana* saying "nothing left for me until closing day." Two facts
about the same loop, and deleting either one loses information a coordinator needs.
</Note>

## 🚪 WHEN A DEAL DIES — the exits (SEQUENCED)

A loop is **one attempt at one deal**. Lawrence Guenther's listing is a loop. You did the presentation, you
did not win it — that loop ends, permanently, with every document in it. Lawrence is still Lawrence, still
in the pipeline, and if he calls next spring **that is a new loop**.

That is why a dead deal is a loop status and never a stage: *a lead has one stage; a lead has many loops.*
One buyer can carry four archived loops and a fifth that closes, and their pipeline position never moved
backwards.

| Ana picks | Side | Means |
|---|---|---|
| **Listing Not Won** | seller | did the presentation, did not get the listing |
| Listing Expired | seller | we had it, term ran out |
| Listing Cancelled | seller | we had it, seller cancelled |
| Offer Not Accepted | buyer | wrote the offer, seller took another |
| Fell Through | both | went under contract and died — inspection, financing, appraisal |
| Archived | both | the plain catch-all — buyer changed their mind, went quiet |

One dropdown, one chip: the reason **is** the status, so there is no second field to leave blank.

<Warning>
**TODAY there is no exit at all.** The seller pipeline runs Listing Appointment Met → Listing Agreement
Sent → Listing Agreement Signed with nothing in between for a loss. Lawrence Guenther has **nowhere to be
dispositioned right now**, and listing-presentation win rate cannot be computed. `listing_not_won` is
sequenced EARLY for exactly this reason — it is purely additive.
</Warning>

## 🧩 ADDING A STAGE, A SECTION, OR A STATUS — no engineer (PARTIALLY SHIPPED)

**Shipped 2026-07-31 (migration 0531):** sections are rows. Labels, colors and icons used to live in a
TypeScript file, so renaming "Working the Buyer" needed a deploy. Now it is one row, edited in
`tc/configure/stages`.

Every section declares **who may edit the stages inside it**:

| Section | Managed by | What Ana sees |
|---|---|---|
| Working the Buyer · Win the Listing · List Prep · Nurture | `pipeline` | edit stages inline |
| **Under Contract → Close** | **`tc_milestones`** | **read-only, with a link: *Managed in TC → Milestones*** |

The TC owns Under Contract → Close because **the TC owns the dates.** Those milestones carry anchors,
offsets, business-day math and holiday rolls, and 139 task templates hang off them. No other section has
any timing to configure — which is why it is the only one that redirects.

Adding something new is a row, in the matching configure page:

```
a new SECTION   → tc/configure/stages       label, color, icon, order, managed_by
a new STAGE     → tc/configure/stages       inside a `pipeline` section
a new MILESTONE → tc/configure/milestones   the anchor + offset it computes from
a new TASK      → tc/configure/tasks        the milestone it hangs off + its offset
a LOOP STATUS   → config row                its side, and its group (active/closed/archived)
a TC STATUS     → config row                the coordinator's dropdown
```

## 📅 MILESTONES AND TASKS — one hierarchy, not two (SEQUENCED)

**Ana's ruling, 2026-07-31: a task belongs to a milestone.** Not to a phase.

The data already agreed — all 139 task templates were anchored to a milestone date, and `phase` was a
second grouping on top saying nothing the anchor did not already say. It made 81 tasks on one loop look
like they lived in two hierarchies at once. **Phases are retired.**

```
MILESTONE  (a dated event)
   └── TASKS  (due = the milestone's date ± an offset)
   └── advances the STAGE, where one applies
```

A loop carries roughly **16 milestones and 81 tasks** — each milestone with its own date, its own tick, its
own who-and-when. That is why they are a checklist and not a single field: the TC overrides individual dates
constantly, because inspections extend and closings move.

Not every milestone owns a stage, and that is deliberate: **Funded** and **Possession** happen *after* the
deal closes, so the stage stays `Closed` while the dates keep landing. A pipeline cannot go past its own
end; a date list can.

`start` — the biggest bucket at 57 of 139 tasks — becomes a real milestone, **Loop Started**, so every task
nests under a milestone with no special cases.

<Warning>
**THE DATE ENGINE HAS NEVER BEEN SWITCHED ON.** Measured live 2026-07-31: **0 of 487 live tasks have a due
date**, and **0 of 30 milestones** are wired to advance a stage. The templates, anchors, offsets,
business-day rules and holiday rolls are all built. Nothing computes. **The machine is fully assembled and
has never been turned on** — which is why milestones and stages drifted into two hand-typed lists of nearly
the same words.

Related, and reassuring: the `is_active` toggle on a milestone is a **seed filter read once** when a loop is
created. It fires no automation and notifies nobody. Turning one off is safe.
</Warning>

## ✅ WHAT NEEDS TO GET DONE — the ladder

Sequenced by Lee 2026-07-31. **Additive first, the destructive step last.** Zach can watch this burn down.

<Steps>
  <Step title="Sections + document categories — SHIPPED 2026-07-31">
    Migration 0531. `hl_pipeline_section` (9 rows, both pipelines, Under Contract → Close = `tc_milestones`,
    54/54 stages linked) · `hl_tc_loop_document_category` (7 rows) · the contract-template columns for the
    dotloop import. Additive only; nothing renamed or dropped. ✅
  </Step>
  <Step title="The exit rows — listing_not_won FIRST">
    Purely additive, and it unblocks a number Zach cannot get today. Listing Not Won · Listing Expired ·
    Listing Cancelled · Offer Not Accepted · Fell Through · Archived.
  </Step>
  <Step title="The dotloop form import">
    58 forms, 3,910 fields already harvested from dotloop's own field maps. Runs as a registered Inngest
    function, manual trigger, no cron. **Every map is overlay-verified before it touches a contract** —
    "58 imported" is a count, not proof. Conversion is per-page: the library has two page sizes, so a
    shared scale constant would silently mangle 20 pages.
  </Step>
  <Step title="The status purge — one commit, no exceptions">
    Drop the four document rows from the TC status dropdown (they are events, already in the activity log),
    add Complete, rename Paused → Paused/Blocked. **The e-sign webhook writes two of those names as literal
    strings and MUST move in the same commit**, or the 0488 incident repeats in the TC lane.
  </Step>
  <Step title="Switch the date engine on">
    Wire `advances_to_stage_id` (0 of 30 today) and fix `computeLoopSchedule` (0 of 487 tasks dated). This
    is the step that makes milestones, tasks and stages one system instead of three lists.
  </Step>
  <Step title="Tasks move under milestones">
    Retire phases. `start` becomes the `loop_started` milestone. Migration is 1:1 from the existing anchors.
  </Step>
  <Step title="The five renames — one per push">
    Smallest reader-count first, each proved before it ships. Names in
    [Transaction & Loop Model](/schema-lock/transaction-model).
  </Step>
  <Step title="Retire hl_tc_phase — LAST">
    A DROP, so it snapshots first and **waits for Zach's typed Rule-0 approval naming the table**.
  </Step>
</Steps>

### Still unsigned

**`tc_status_changed`** — the activity kind for a TC status move. Lee approved it; it needs Zach's word
before it enters the catalog. Nothing above is blocked by it until the purge.

## Own the whole stack — why (LOCKED 2026-06-27)

TC Loop is **our own dotloop**, owned end-to-end. We do NOT hand a third party our finished documents.
Two hard reasons a wrapped dotloop can't satisfy:

1. **dotloop can't fill or sign via API.** No API-fillable smart fields, and you can't place signature
   fields or trigger a signature through its public API (UI-only) — so Myli could never run a contract
   through dotloop.
2. **SkySlope / eXp compliance requires we own the documents end-to-end** — we must push signed docs
   directly to SkySlope, only possible if the documents live on our side.

**dotloop is OUT of the critical path.** It is an optional legacy importer only — read historical SRG
loops via the read-only API in `lib/dotloop/client.ts`. See [dotloop integration](/guides/dotloop-integration).

## The stack

| Layer | What | Where |
|---|---|---|
| Loop | container + the two status dials | `hl_transactions` + `hl_tc_stage` → renaming to `hl_tc_loop_status` ([sequenced](/schema-lock/transaction-model)) |
| Documents | folders + document lines (**our source of truth**) | `hl_tc_loop_folder` / `hl_tc_loop_document` |
| Forms + fill | our PDF templates + data-fill → filled PDF (R2) into a loop folder | `lib/tc/{contract-forms,fill-contract,template-store}.ts` + `POST /api/tc/loops/[id]/documents` |
| **E-signature** | place fields · send · seal · audit | **Documenso, self-hosted** — the system of record |
| Compliance archive | signed PDF + completion certificate, immutable, retained | our storage (**R2**) |
| Broker integration | push signed docs to **SkySlope (eXp)** | to build |
| People / Tasks | participants + the A-Frame dated checklist | `hl_tc_loop_participant` · `hl_tc_loop_task` |
| The UI | the existing lead-detail page in TC `view` mode (reused, not rebuilt) | see below |

## E-signature = Documenso, self-hosted (the system of record)

**Documenso, self-hosted, is the system of record for signed documents** — the sealed final PDF plus its
completion certificate are archived to **our R2**. dotloop is only an optional legacy backup, out of the
critical path. This is not a plan: **e-sign shipped 2026-07-06** (instance live at `esign.houseloop.ai`;
multi-party signing — buyer / co-buyer / agent / broker — proven on all 3 contract forms; sealed PDFs
archived to R2; only the `DOCUMENT_COMPLETED` webhook terminates the flow). The send path is in
`apps/houseloop-crm/lib/esign/send-loop-document.ts`.

**The compliance bar.** US e-signatures are governed by the **ESIGN Act (federal) + UETA** (both adopted
by Kansas and Missouri). An e-signature binds an agency/purchase contract when we capture: (1) **intent to
sign**, (2) **consent** to do business electronically, (3) **attribution** (email/identity verify + IP +
timestamp), and (4) **integrity + retention** — a tamper-evident record (document hash / digital seal),
retained and reproducible. The legal backbone is the **audit trail / completion certificate** (who · when ·
IP · email-verified · doc hash · event log) plus the sealed final PDF. US real estate does **not** require
an RFC-3161 trusted-timestamp authority (that's EU/eIDAS) — a reliable system timestamp + audit log + seal
is the accepted standard.

**Documenso provides all of it** — audit log, PAdES cert-seal, completion certificate, and an API to place
fields, send, and webhook on completion — and is self-hostable, so we own the data and pay **$0 per
signature**. (Chosen over SignWell for ownership, no per-sig cost, and SkySlope-grade control.)

## The CRM section (navigation)

TC is a **drill-in section**: the main left nav has **TC Loop ›**; clicking it swaps the whole left menu for
the TC sub-nav + a **Back to CRM** link. The sub-nav (routes under `/tc/*`, additive — Dashboard/Leads
unchanged) matches the live left menu exactly:

- **Loops** — the loop card list (`/tc`, the home)
- **Forms** — the contract engine
- **Email Queue** — outbound TC email
- **Configure** — **Transaction Status · Milestones · Tasks**

## The TC loop view — REUSE the lead-detail in a TC view mode

**The TC loop view IS the existing lead-detail page, driven into a TC `view` mode. We REUSE the real
components — never recreate them.** The shell + lanes are canonical:

- **LEFT** `LeadLane` · **MIDDLE** the navy iPad `CenterLane` (its real `StatStrip` 6-KPI cards + the
  Buyer/Seller stage rows + the dock-bar tabs + the activity streams) · **RIGHT** `MyliLane`.
- "TC view" = those SAME components with a `view='tc'` flag that swaps data/labels: TC KPI metrics, the TC
  dock-tab set, and loop data (Tasks / Documents / People). **No `Tc*` components.** Tables stay
  `HlDataTable`; group headers stay `SectionBand`.
- The `/tc/transactions/[id]` route **redirects to `/leads/[leadId]?view=tc`** (the real page, TC mode). The
  `Agent | TC` toggle (TC/Admin only, inline in the navy lane header) flips `?view`.

**6 KPI cards (this deal, never system totals):** Last Ctct · Stage · UC Days · To Close · Docs (X/Y) ·
Awaiting — rendered by the existing `StatStrip` (labels kept ≤10 chars to fit the fixed `StatCard` width).

**9 dock tabs (mirror the lead view, swap 2):** All · Texts · Calls · Emails · Notes · Tasks · Appointments
· **Documents** · **People** (Website → Documents, Deals → People; Notes kept). Default TC tab = **Tasks**.
Comms tabs reuse the existing lead streams unchanged.

<Note>
**Agent view is byte-identical.** Without `?view=tc` the lead-detail page renders exactly as before — every
TC branch is gated on `isTcView`. A lead with no loop in TC view shows a tokenized **Create Loop** CTA in
place of the panel.
</Note>

<Warning>
**Recreating shells is banned and ENFORCED.** An earlier attempt hand-rolled plain-div lookalikes
(`TcLoopLane`, `TcCenterLane`, `TcStageStepper`, single-scroll `LoopDetail`) and introduced off-brand
green — rejected and deleted. Guards: `.claude/rules/REUSE_NOT_RECREATE.md` (constitutional, auto-injected)
and the **`hl/no-duplicate-shell-components`** ESLint rule — the build **fails** if a component named
`*Lane` / `*Stepper` / `*StatStrip` / `*DockBar` is defined outside `components/leads/detail/`. Brand:
white-on-blue, no green, tokens only.
</Warning>

## Multi-loop sit-rep header + Buyer/Seller toggle

The TC Milestones-band navy header is a **one-line sit-rep** so you can read both sides of a client's
business at a glance and flip between them:

```text
MILESTONES    CLIENT: JESSICA MOORE    BUYER · NEW LEAD · JUL 31    SELLER · ACTIVE LISTING · JUL 12    [ Buyer | Seller ]
```

- **No pills — big bold white ALL-CAPS text** on the navy header. `MILESTONES` label left; `CLIENT: <name>`
  + one line per loop (`<SIDE> · <stage> · <key date>`, **Buyer first**) centered; the `Buyer | Seller`
  toggle pinned right, shown only when the lead has BOTH a buyer and a seller loop. One line, never wraps.
- **Active side = full white + a thin underline accent;** the inactive side is muted
  (`--hl-on-navy-text-muted`) but still clickable. Per-side `status` = the lead's side-matched pipeline
  stage (falls back to the loop's Transaction Status); `key date` = the `closed` milestone date, else
  `under_contract`.
- **The active loop is a URL param** `?loop=<loopId>` (alongside `?view=tc`). Clicking a side / flipping the
  toggle routes to `/leads/<id>?view=tc&loop=<loopId>`; the page validates the loop belongs to the lead
  (else most-recent wins) and feeds that same `tcLoopId` to BOTH `CenterLane` and `MilestoneBand` — so the
  whole TC view re-points to the selected side.
- **Reuse, no new shell:** the milestone TRACK is unchanged; the sit-rep is plain tokenized text; the toggle
  reuses the canonical `hl-toggle hl-toggle--sm` switch. **Single loop (the 99%)** = just `CLIENT:` + that
  one side's line, no toggle.
- **Data/API:** `listLoopsForLead(leadId)` (`lib/tc/loop-engine.ts`) → `GET /api/tc/loops?leadId=<id>`
  returns `[{ id, side, stageName, ucDate, closeDate }]` newest-first. Files:
  `components/leads/detail/MilestoneBand.tsx`, `app/(crm)/leads/[id]/page.tsx` (the `?loop` seam),
  `app/api/tc/loops/route.ts`, `lib/tc/loop-engine.ts`.

## ⚖️ LICENSING — the agent carries their license, and a mismatch WARNS (LOCKED 2026-07-30)

<Warning>
**An agent may only be designated on a contract in a state they are actively licensed in.** HouseLoop now
knows each agent's licensed states and **warns** — loudly, with the broker's own words — when the
designated agent is not licensed where the property is. It **warns, it never blocks**: a human decides.
</Warning>

**Chris Cribb, Designated Managing Broker (MO & KS), eXp Realty — 2026-07-29:**

> Ana has an active MO and KS license to be able to sell real estate and can sign any forms or be
> designated for sales in either state. Zach has **ONLY a KS license** and can be designated to sign and
> show property in the state of KS, but **can NOT show, write contracts or be designated for properties
> in MO.**

### What was wrong

The contract's designated agent resolved to `COALESCE(tc_assignee, lead.assigned_user)` — whoever happened
to be on the loop — and there was **no license concept anywhere in the system**.

Measured 2026-07-30, before the fix: **two live Missouri loops had an unlicensed agent as the contract
agent** — `117 N Boardman Street, Pleasant Hill, MO` and `5388 N Palmer Avenue, Kansas City, MO`.

<Note>
**A retired stopgap.** The first fix (morning of 2026-07-30) forced *every* contract to print and sign as
one globally-configured signer via an `hl_settings.tc_contract_signer` row. Zach rejected it: *"Don't
hard-code anything. Don't put it on the backend. It needs to be driven off of the user's payload."*
`lib/tc/contract-signer.ts` and that settings row are **deleted**. If you find a doc or comment citing
`getContractSigner()`, it is stale.
</Note>

### The model — licensing lives on the USER

`hl_user_license` (migration `0509`), **one row per agent per state**:

| Column | Meaning |
|---|---|
| `user_id` | FK to `hl_users`, cascades on delete |
| `state` | two-letter code, CHECKed against the 50 states + DC |
| `status` | `active` or `not_active` — **the broker's ruling** |
| `renewal_date` | a human reminder only |

<Warning>
**`status` is never derived from `renewal_date`.** A past renewal date does **not** flip a license to
inactive — only a broker does. The date is a reminder; the status is the truth.
</Warning>

Status is **per state** by design: Zach is KS-active and MO-inactive *at the same time*, so a single
"license state + license status" pair could not represent even the first real user.

### Where it is edited — Team → member → Profile → Licensing

One repeatable row per state: **License state** · **License status** (Active / Not active) · **Renewal
date**, plus **+ Add state**. Shown for agent-role members; **admin-only to edit** — a license is the
broker's ruling about a person, never self-declared. Change a license here and every warning below
follows immediately: **no deploy, no code change**.

### How the property's state is resolved

1. **The listing (structured, preferred)** — `hl_transactions.website_hlid` → the web `Listing.state`.
   The Create Loop property search always knew the exact home; until 2026-07-30 the HLID was posted by the
   modal, declared by the API, and then **dropped on the floor**. It is now persisted.
2. **The address (fallback)** — a trailing `, MO` / `, KS 66085` parsed off `property_address`, for the
   six loops that predate the column.

If neither yields a state, the verdict is **ok** — an unknown state is not evidence of a violation, and a
warning that fired on every hand-typed address would train everyone to ignore it.

### Where the warning appears

`lib/tc/license-check.ts` is the **one** resolver — never re-implement the comparison. The verdict is
computed once in `buildLoopFillContext`, where the property and the agent are both in scope, so every
caller inherits it.

| Surface | File | When |
|---|---|---|
| Agent picker | `components/leads/detail/AddPersonModal.tsx` | the instant an agent is selected, before a document exists |
| Fill Review modal | `components/leads/detail/FillReviewModal.tsx` | before the PDF is stamped |
| Share / send modal | `components/leads/detail/SendForSignatureModal.tsx` | before the e-sign envelope goes out |
| Myli headless | `lib/esign/create-fill-send.ts` | no UI — logs loudly and returns `license` on the result |

All three UI surfaces render the same `LicenseWarning` component, which shows **nothing** when the agent
is licensed — so its presence always means something real. The replacement agent it suggests is **looked
up from the table**, never a name in code: the day Zach reinstates his MO license, the warnings simply
stop, with no deploy. That is the exact staleness Chris's email predicted — *"If you change this process
and Zach reinstates his MO licenses, both could be done and sign."*

### Verified live 2026-07-30

All six loops with a property address, through the authenticated API:

| Loop | Verdict |
|---|---|
| 10000 Knox Dr, Overland Park **KS** | silent |
| 1601 N 49th St, Kansas City **KS** | silent |
| 18275 Canterbury Rd, Stilwell **KS** | silent |
| 7410 Delmar St, Prairie Village **KS** | silent |
| 117 N Boardman St, Pleasant Hill **MO** | **warns** — names Ana as licensed |
| 5388 N Palmer Ave, Kansas City **MO** | **warns** — names Ana as licensed |

<Note>
**Scope: signing only.** The broker's email also says Zach may not *show* MO property. Showings run
through a different path (appointments / showing partners) and are **not** covered here (Zach 2026-07-30).
</Note>

## Forms — the Contract Engine

The **Forms** page turns the brokerage's flat dotloop contract PDFs into **fillable forms** HouseLoop
completes programmatically:

1. Each form's flat PDF + a coordinate **field-map** (PDF field → lead/deal data source) lives in
   `hl_contract_template` (`lib/tc/contract-forms.ts` holds the maps).
2. The engine (`lib/tc/fill-contract.ts`, pdf-lib) adds AcroForm fields and fills each from the lead (name,
   search area, budget, agent, dates) — Myli-verified values override.
3. Pick a form → fill → **preview the filled PDF inline** → download → push into the loop.

The **showing-door** forms (the buyer journey): **Single Property Buyer-Broker Rep** (default, one house) →
**Exclusive Buyer Agency** (multiple homes). Disclosures (wire fraud, ABA, broker disclosure) roll in later
at contract time, not at showing.

**Fill INTO the loop (G2):** `POST /api/tc/loops/[id]/documents` builds a `FillContext` from the loop's own
lead/deal → `getTemplateBytes` → `fillContract` → uploads the filled PDF to **R2**
(`tc-loops/<loopId>/<form>.pdf`) → writes `hl_tc_loop_document` (`file_url` + `contract_fill_id` +
`contract_key`). If the loop has no folder it auto-creates a "Forms" folder. Filled PDFs live in R2, never
base64'd into Postgres. Two entry points share this engine (`lib/tc/fill-loop-document.ts`, no fork):

- **`{ formKey, draft: true }` → the human path.** "Add Document" drops a **blank DRAFT** row (status
  `draft`, no fill, carries its `contract_key`). No auto-filled defaults — you fill it deliberately.
- **`{ formKey }` → Myli's headless path.** Fills straight from loop data (`createFillSend`).

See **The document workflow** below for how a draft becomes a filled, editable, signable document.

<Note>
**Local dev:** `R2_PHOTOS_*` live in repo-root `.env`; mirror them into `apps/houseloop-crm/.env.local`
(gitignored) or a local fill returns "storage not configured." Prod (Railway) already has them.
</Note>

## Loops home + Create + Workspace — the dotloop 1:1 port (P7)

The Loops surface is a **screen-for-screen port of dotloop**, skinned in HouseLoop tokens (white-on-blue) and
fed by our own leads + website `Listing` data. The design was locked as an HTML wireframe first, then ported
node-by-node (`.claude/wireframes/tc-loop-build-sheet-LOCKED-2026-07-09.html`; the
[wireframe-is-the-truth](/claude-process/overview) rule).

- **Loops home = a CARD LIST** (`TransactionsTable.tsx` → `LoopCard`), not a table. Each card shows the house
  photo, the **spelled-out** loop name `({Side}) {Client} - {Address}` (`resolveLoopDisplayName`), the
  **Created** date/time (top-right), a loop-**category badge** (🗂 Agency Loop vs 🏠 Transaction Loop), and
  four labeled columns — **Transaction Type** (Purchase / Listing for Sale), **Status**, **Price**,
  **Closing** — plus a tag + Archive footer and a working kebab.
- **Create Loop** (`CreateLoopModal`) is a 4-step wizard: **Loop Name → Template → Photo → Finish**. Step 1 is
  a `PropertySearchInput` that **looks addresses up from houseloop.ai** (`/api/tc/property-search`, full-address
  per-token match against the web `Listing`), then derives the name `({Side}) {Client} - {Address}`. The Photo
  step **auto-pulls the house photo** (`cover_image_url`) — no manual upload. From the same modal a
  `ClientPicker` lets you **search an existing lead OR add a new lead inline** (`POST /api/leads`).
- **Loop workspace** (`LoopDetailModal`) opens on the navy shell with the house photo + created date in the
  header. **People** are editable inline — Add / edit / remove participants (`AddPersonModal` +
  `POST/DELETE /api/tc/loops/[id]/participants`). **View Details** (`LoopSettingsModal`) is the edit surface for
  every loop field: loop-name mode (property address vs custom), people, property address, and financials
  (list/contract price, closing date).

## The document workflow — Add → Fill form → Sign

Every document on a loop is editable through a **form**, not by clicking fields on the PDF (dotloop's clunky,
old-school way). The loop is Zach's: *"a form that we fill out that updates the pdf."*

1. **Add Document** (`AddFromFormModal`, sends `draft: true`) → a **blank DRAFT** row appears with a **Fill
   out** button. Nothing is pre-stamped; no junk defaults from other docs.
2. **The kebab** (⋮, on every doc row via `HlDropdown`) → **Edit fields · Open PDF · Delete document**. (The
   menu portals *above* the modal — z-index `10080` — so it's actually usable; a doc name is clickable to the
   same form.) Delete → `DELETE /api/tc/loops/[id]/documents/[docId]` (blocks `signed` docs with a 409).
3. **Fill out / Edit fields** opens the **fill form** (`FillReviewModal`) — every field editable, pre-filled
   from the loop (buyer name, general location, price range, term dates). Save → the doc walks
   **draft → filled**, a fresh PDF is stamped to R2, and the edited values land in `hl_contract_fill.values`
   (which drives the PDF). Re-openable and re-stampable any time via the same form.
4. **Send for signature** → the Documenso e-sign flow (below).

<Note>
**Signatures are anchored, never eyeballed.** Signature/initials coordinates are **extracted from each PDF**
(`tools/houseloop-contracts/anchor-sig-fields.mjs`) — box Y differs per page, so a single hardcoded number is
always wrong. The extractor writes the field maps in `lib/tc/field-maps/*.json`. Law:
[`.claude/rules/SIGNATURE_ANCHORING.md`](/guides/esignature) — *"read the box, don't guess it."*
</Note>

## Waiting on Signatures — the loop's e-sign lifecycle (shipped 2026-07-14)

Once a form is sent, the loop tracks it end-to-end and gives the agent control without leaving the loop.

**Send (one call).** `createFillSend(leadId, formKey, opts)` (`lib/esign/create-fill-send.ts`) is THE call for
"send this agreement": reuse/create the loop → fill from lead + property → multi-party send → returns
`{ loopId, docId, esignDocId, signingUrl, sentTo, sentAs }`. For a **Single Property Showing Agreement** pass
`propertyAddress` — it creates a property-specific loop so the form's Property line fills from `deal.address`:

```ts
createFillSend(leadId, 'single_property_buyer_rep', { propertyAddress, agentUserId, senderUserId })
```

The `signingUrl` can be texted from any agent's line (`sendTextAs`) — the doc sends AS that agent. This is Myli's
/ the AI clone's document tool; no PDF or Documenso handling in the caller.

### The signature-request email — signature block, neutral body, and NO client-text leak (2026-07-24)

Three rules govern what each signer's Documenso email actually says (`lib/esign/send-loop-document.ts` +
`components/leads/detail/SendForSignatureModal.tsx`):

1. **The email body is NEUTRAL — the casual client text never leaks to the broker.** Documenso's `meta.message` is
   ONE message applied to EVERY signer (buyer + agent + broker). So the modal sends a professional message —
   *"Please review and sign the document. Once all parties have signed, everyone receives a completed copy…"* — as
   the e-sign email body. The agent's **casual note** (e.g. *"Trish will meet you at 1:30 😀"*) is sent SEPARATELY
   as the **client SMS only**, never as the Documenso email. (Before the fix the broker was getting the casual
   text in his signature-request email.)
2. **The sending agent's signature is appended.** `sendLoopDocument` resolves the agent's signature (name ·
   Spradling Group | HouseLoop · phone · email) from their dialer + connected inbox and appends it to BOTH the
   Documenso message and the logged thread copy (`logEmailSend`), so the request reads like a real email from the
   agent. Documenso renders plain text — a full graphic signature would require sending via Gmail instead.
3. **The signing email is logged to the Emails thread.** Documenso sends from its own SMTP, so the request would
   otherwise be invisible in the lead's Emails lens — `sendLoopDocument` records it via `logEmailSend` + an
   `email_sent` run-log row so it shows in the feed's Email card.

### Fillable §4 Agency Election / Designated Agent name — dynamic per assigned agent (2026-07-24)

All three buyer forms require the Designated Agent's name so the broker can sign. `contract-forms.ts` fills
`f_designatedAgent` on each form — coords read from the PDF underline per
[SIGNATURE_ANCHORING](/guides/esignature), source `agent.name` — resolved by `buildLoopFillContext` to the
loop's **assigned agent** (`tc_assignee`, then the lead's assigned user). Fills whoever is on the loop, never
hard-coded. Forward-only: re-send to pick up the name on a copy already out.

| Form | Field placement |
|---|---|
| **Single Property Buyer-Broker Rep** | Page 1 (0-indexed) — §4 Agency Election |
| **Non-Exclusive Buyer-Tenant Rep** | Page 2 (0-indexed) — §4 Agency Election |
| **Exclusive Buyer Agency Contract** | Page 5 (0-indexed, last page) — "Appointment of Designated Agent(s)" box |

**Waiting on Signatures.** While a doc is `sent` and unsigned, the loop band (`MilestoneBand`) AND each Loops-list
card (`TransactionsTable`) show a green **Waiting on Signatures** strip — the doc name, sent date, days-waiting, a
**reminder toggle**, and **Cancel document**. The stepper's Agreement-Sent node shows the real sent date.

**Reminders are HouseLoop-owned.** Documenso's own auto-reminder is OFF — we own on/off + cadence. The toggle
writes `hl_tc_loop_document.reminders_enabled`; the Inngest **`esign-reminder-sweep`** re-nudges the unsigned
signer every ~2 days (Myli's text with the link) while it's on. (Runs event-only until sign-off, then a daily cron.)

**Cancel + void-on-delete.** Cancel document sets the Documenso `Envelope` to `CANCELLED` (the reliable off switch
— reminders only fire on PENDING docs; clearing reminderSettings does NOT stop them) and marks the loop doc
`cancelled`. Deleting a lead auto-cancels its outstanding agreements (`purgeLead` → `cancelLeadEsignDocuments`), so
a dead lead never keeps nagging.

**Signed.** `DOCUMENT_COMPLETED` → `/api/esign/webhook` sets `status='signed'` + `signed_at`, archives the sealed
PDF to R2 (`file_url`), advances the loop to Agreement Signed, kills the nudge chain, and fires the
`document_signed` door (+ `document_opened` on open) — the callback other surfaces hook for the thread/context.

### Planned: the Single Property Showing Agreement workflow

A **manually-fired Transaction Coordination workflow** (spec locked at
`apps/houseloop-crm/docs/wireframes/tc-showing-agreement-workflow-plan-LOCKED.html`): Manual Trigger (property
input) → **Send Agreement** step (new step type → calls `createFillSend`) → Send Text (the signing link). Fired by
Myli, the AI clone, or the agent on a lead + a property. Same shape for all three TC agreements (single-property /
exclusive / non-exclusive) — they differ only in the form the Send step picks. The one new build is the "Send
Agreement" step type; the form, the engine, and the reminder/signed lifecycle are already live.

## The 3-layer engine + dynamic dates (A-Frame parity)

The stages, milestones, and tasks are an **EXACT MIRROR of A-Frame** today (names, phases, anchors, offsets,
assignees lifted verbatim from the recon). Only the UI chrome is ours. **Why:** mirror A-Frame 1:1 to
understand + measure it before changing anything — a divergence in task content is a bug, not a feature.
There are **no per-task instruction manuals** (A-Frame's task `description` is empty; verified). TC task rows
that read e.g. "SLACK: Front end TC accepts assigned transaction" / "AFRAME: Change Status" are the verbatim
A-Frame source text, not placeholder copy. Converting to a HouseLoop-native set is deferred.

Three layers + an offset engine:

1. **STATUS** — the headline word = our **Stages** (~5/side).
2. **DATES — 4 KEY DATES you ENTER drive everything:** `effective` (Under Contract) · `inspection_end` ·
   `closing` · `on_market` — the milestones' anchors.
3. **TASKS (139, in phase folders)** — each is a **relative rule** (`anchor + offset`). Buyer = Intake ·
   Contract Review · Scheduling · Communication · Compliance · Inspections · Appraisal · Closing. Listing
   adds the pre-market run-up: Within 24h of ERTS Signed · 10 Days Before Going Live · 5 Days Before Going
   Live · Listing Goes Live · Weekend After Going Live.

**Dynamic dates** (`lib/tc/date-engine.ts`): a task/milestone stores a RELATIVE rule; the due date computes
from the live key date and cascades when it moves. Rule = anchor (key date) or task→task chain · signed
offset · `day_unit` · `business_day_def` (TRID precise/general) · weekend/holiday roll · holiday calendar
(US_FEDERAL) · conditional "If Applicable" · `task_type`. Proven: 13/13 unit tests.

> **Status = headline · Milestones = the key dates · Tasks = the auto-scheduled checklist.** Enter 4 dates →
> the plan schedules; move one → it all re-shifts.

**Task counts (A-Frame parity):** 58 buyer + 81 listing = **139**. Listing's 81 = 57 contract-to-close +
24 pre-market.

### Re-platforming switch — `{{platform.tc}}`

The ~60 task labels that name the platform itself store the token `{{platform.tc}}`, resolved at
display time from `hl_settings` `tc_platform` → `value.name` (default **"AFRAME"**). Flip that one value to
**"TC Loop"** to re-platform every label at once. Only the platform word is tokenized — SLACK / DOTLOOP /
SKYSLOPE / FUB / SISU stay literal. Code: `lib/tc/platform.ts` (pure) + `lib/tc/platform-settings.ts`
(server). Parity-safe today.

## Architecture (locked)

1. **API-first.** Every loop action is a REST endpoint; UI + Myli call the same one.
2. **Role-gated `/tc` surface + an `Agent ⇄ TC` toggle** (TC/Admin only). `/tc/transactions/[id]` resolves
   the loop's lead and opens the real lead-detail page in TC `view` mode (`?view=tc`).
3. **Reuse, NEVER recreate** — `StagesPage`, `PipelineSwitcher`, `HlDataTable`, `HlFormModal`,
   `HlFilterPills`, `SectionBand`, and the lead-detail `LeadLane` / `CenterLane` / `MyliLane` + comms
   streams. Enforced by `REUSE_NOT_RECREATE.md` + the `hl/no-duplicate-shell-components` ESLint rule.
4. **Side-aware** (TC Buyer vs TC Listing).
5. **E-signature = Documenso, self-hosted** (own data, $0/sig, cert-sealed, ESIGN/UETA). dotloop NOT in
   the path.
6. **We own documents end-to-end** → push to SkySlope for compliance.

## Build state

**Phase 1 — Configure template builders: DONE.** A-Frame engine reverse-engineered + rebuilt as editable,
API-first Configure builders.

**Phase 2 — Build our own dotloop (owned end-to-end): BUILDING.**

| Gate | What | State |
|---|---|---|
| **G1** | TC loop view = the existing lead-detail page in TC `view` mode (REUSE, no new shells) | ✅ Shipped (P0–P6) + multi-loop sit-rep header |
| **G1b** | Loops home / Create / Workspace — dotloop 1:1 port (card list, create wizard, People + View Details editable, Add-as-draft, working kebab, fill-form) | ✅ Shipped 2026-07-09 (P7) |
| **G2** | Forms + fill — our PDF templates + data-fill → filled PDF (R2) into a loop folder | ✅ Shipped (fill → R2 → `hl_tc_loop_document`) |
| **G3** | E-sign — Documenso self-host: place fields → send → store sealed signed PDF + audit cert | ✅ Shipped 2026-07-06 (`esign.houseloop.ai`; 3 forms sig-verified; sealed PDFs → R2) |
| **G4** | Runtime cascade — `computeLoopSchedule` fills dates from the 4 key dates; recompute on change | ⬜ (mig 0262 ready) |
| **G5** | SkySlope / eXp — push signed docs for brokerage compliance | ⬜ |
| **G6** | Myli-TC — autonomous fill → sign → file via the same endpoints | ⬜ |

## Data model — migrations 0253–0263

`apps/houseloop-crm/db/migrations/` (additive + reversible; `node db/migrate.mjs` against
`HL_CRM_DATABASE_URL`):

- **0253** — foundation: loop columns + child tables (`hl_tc_loop_folder/_document/_participant/_event`) +
  `hl_templates.scope`. (`hl_tc_loop_document` already carries `file_url` + `contract_fill_id` — what G2
  fill uses.)
- **0254** — TC pipelines · repoint `tc_stage_id` · `lost_reason` · `hl_tc_milestone_template`.
- **0255** — milestone `color` + `icon`.
- **0256** — full milestone set (14 buyer / 16 seller) + offset-engine columns (`anchor`, `is_key_date`).
- **0257** — `hl_tc_task_template` (139 A-Frame tasks).
- **0258** — task **date-rule** + `hl_tc_phase` **folders** + `hl_holiday_calendar` (US_FEDERAL).
- **0259** — listing pre-market phase label rename + Stage-3 offsets −4d → −5d.
- **0260 / 0261** — `{{platform.tc}}` token + `hl_settings` `tc_platform` switch.
- **0262** — `hl_tc_loop_task` (per-loop task instances) + `provides_anchor` on key milestones.
- **0263** — `hl_role.is_system` + `grants_tc` (built-in undeletable Real Estate Agent + Transaction
  Coordinator; TC view gate).

## Integrations

<CardGroup cols={2}>
  <Card title="dotloop (legacy import only)" icon="file-signature" href="/guides/dotloop-integration">
    dotloop Public API v2 (OAuth 2.0), read-only — import historical SRG loops. Out of the critical path;
    Documenso handles signing, SkySlope is the compliance destination.
  </Card>
</CardGroup>

## Change log

- **2026-07-24** — **Designated-agent name field added to all three buyer forms.** Non-Exclusive (page 2 §4
  Agency Election) and Exclusive (page 5 "Appointment of Designated Agent(s)" box) now auto-fill
  `agent.name` from the loop assignee, matching Single Property. All three buyer-form sends now include the
  broker-required agent name. Re-send existing docs to pick it up (forward-only).
- **2026-07-14** — **Waiting-on-Signatures loop lifecycle shipped.** The loop band + each Loops-list card now show
  a green **Waiting on Signatures** strip (sent date · days-waiting · reminder toggle · **Cancel document**) while a
  doc is sent-unsigned; the stepper's Agreement-Sent node carries the real sent date. **HouseLoop owns reminders** —
  Documenso auto-reminder off, `hl_tc_loop_document.reminders_enabled` toggle drives the Inngest
  `esign-reminder-sweep` (every ~2 days). **Cancel** voids the Documenso `Envelope` (→ CANCELLED); **deleting a
  lead auto-cancels its outstanding agreements** (`purgeLead` → `cancelLeadEsignDocuments`). `createFillSend` gained
  a `propertyAddress` param → true one-call **Single Property Showing Agreement** send (proven live: 14637 Crystal
  Place, texted from Ana's line, signed end-to-end). Migration 0402 (`reminders_enabled` + `last_reminder_at`). The
  **Single Property Showing Agreement workflow** is spec-locked (manual trigger → Send Agreement step → text link).
- **2026-07-09** — **P7 shipped: the dotloop 1:1 UI port + document editing.** Loops home = dotloop card list
  (`LoopCard`, spelled-out name, created date, category badge, house photo); Create Loop wizard (property
  search against houseloop.ai, auto-pulled photo, add-a-lead inline); editable People (`AddPersonModal` +
  participants API) and View Details (`LoopSettingsModal`). **Documents:** Add Document now adds a **blank
  DRAFT** (no auto-filled junk); the per-doc **kebab works** (Edit fields · Open PDF · Delete, portaled above
  the modal); every field is edited via a **fill form** that re-stamps the PDF on save (`FillReviewModal` →
  draft→filled). New endpoints: `DELETE .../documents/[docId]`, `POST/DELETE .../participants`. Signatures are
  extracted per-PDF (`anchor-sig-fields.mjs`), never hand-typed — `SIGNATURE_ANCHORING.md`.
- **2026-07-06** — **G3 shipped: Documenso self-hosted e-sign is live** (`esign.houseloop.ai`). Multi-party
  signing (buyer / co-buyer / agent / broker) proven on all 3 contract forms; sealed PDFs + completion
  certs archived to our R2; only `DOCUMENT_COMPLETED` terminates. Send path:
  `lib/esign/send-loop-document.ts`. Documenso is the system of record; dotloop is optional backup only.
- **2026-06-27** — Multi-loop sit-rep header + Buyer/Seller toggle; **G2 shipped** (forms + fill into the
  loop, R2 + `hl_tc_loop_document`); **G1 shipped** (TC view mode P0–P6, open-loop-in-TC). Architecture
  locked: build our own dotloop, owned end-to-end, e-sign = Documenso self-host; dotloop leaves the critical
  path. Hand-rolled loop UI ripped out; `REUSE_NOT_RECREATE` + `hl/no-duplicate-shell-components` enforced.
  Platform re-brand token `{{platform.tc}}`; dynamic dates + folders + phase filter; task layer + full
  milestone set.
- **2026-06-26** — Model locked (two-axis status/milestone, PRD §12); Milestones builder; canonical
  `HlFormModal`; loop engine (mig 0253/0254); A-Frame + dotloop reverse-engineered.
- **2026-06-22** — Section shipped: TC drill-in nav, Transactions table (`hl_transactions`), Email Queue,
  and the Contract Engine (fillable Single Property / Exclusive buyer forms → fill from a lead → inline PDF
  preview).
