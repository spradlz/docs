---
title: "Pipeline & Workflow — Master Overview"
description: "The one map of how a lead enters HouseLoop and what happens next. There is ONE platform (HouseLoop) and everything is a LEAD SOURCE with registration events, on a single Lead Sources page. Workflows are source-specific — Workflow = f(source × event) — grouped by lead source; Follow Up Boss is a special source that pulls in its own sub-sources (Zillow, realtor.com). Includes the architecture rails, the sign-off matrix, the build phases, and the quarantined legacy FUB door model."
---

<Note>
This is the **master reference + working doc** for everything under Settings → Pipeline & Workflow:
**Lead Sources**, **System Events**, and **Workflows**. The **Model** section defines how it fits together;
the **Work In Progress** scoreboard (Rails → Sign-Off Matrix → Build Phases) tracks the MVP build; the
**Current State** documents what ships today; the **Quarantine** section records the legacy model we are
retiring (kept, never deleted). Model LOCKED by Zach 2026-06-30 PM — it diverged hard from the earlier
"two platforms / agnostic engine" plan; that language is dead.
</Note>

## 🚧 WORK IN PROGRESS — HouseLoop MVP build (Lee, Zach-approved 2026-06-30)

> The live scoreboard for HouseLoop's lead engine to MVP. **Read top-to-bottom:** first the **Model** (below),
> then the **Architecture Rails** (the plumbing that must be green before ANY event can pass), then the
> **Sign-Off Matrix** (every event is a row; every column is a gate we sign off), then the **Build Phases**
> (the sequence). We work the matrix **one row at a time**, top to bottom, by section. **HouseLoop becomes the
> first-class notification system, retiring Realloop event-by-event as each row goes live** (kills the
> double-notification: FUB's own ping + the Realloop Twilio "355" text + dupes).

## 🧭 THE MENTAL MODEL — Everything is a Lead Source; `Workflow = f(source × event)` (Zach LOCKED 2026-06-30 PM)

**There is ONE platform: HouseLoop.** Like Sierra / Follow Up Boss / SureSend, you never name it — there's only
one. So there is **no "platform" and no "integration" layer.** **Everything is a LEAD SOURCE**, and a lead source
has **registration events**. That's the whole model.

| Concept | What it is | Lives (data) | Shows (UI) |
|---|---|---|---|
| **Lead Source** | How/where a lead enters — **HouseLoop.ai · Follow Up Boss · Manual Entry** (later: Google PPC…). Everything is one of these. | `hl_leads.source` · `hl_lead_sources` | The **Lead Sources** page (each source = a block) + the lead detail page |
| **Event** | What the lead *did*: registered, tour request, liked a home, called… | the trigger (`hl_lead_source_events`; FUB via `hl_fub_webhook_settings`) | Under its source on the Lead Sources page |
| **Workflow** | The steps that fire for a `(source × event)` — **source-specific, not agnostic** | `hl_workflows` (bound via the event row) | The **Workflows** page, **grouped by lead source** |

### The keystone — `Workflow = f(lead source × event)` (this REVERSES "agnostic")
The earlier plan said workflows were *agnostic* — "trigger on the event, filter on source, never one workflow per
source, merge fields fill the words." **That was wrong.** The **same event from a different source is a different
workflow with different copy.** Merge fields alone can't carry the difference in voice. So:

- The **Workflows page GROUPS registration workflows BY lead source** (a band per source: HouseLoop.ai · Follow Up
  Boss · Manual Entry). System Events + Follow-up workflows stay agnostic (no source — they fire for any lead).
- `/api/workflows` resolves each workflow's source from **both** binding tables (`hl_lead_source_events` **and**
  `hl_fub_webhook_settings`, since FUB binds via its own table).

### Follow Up Boss is a SPECIAL source — it pulls in sub-sources
FUB *creates* its 5–6 webhook events **and pulls in the sub-source already stamped in FUB** (Zillow, realtor.com…).
So a FUB event carries a **sub-source**, and `(FUB · Zillow · Tour Request)` is its **own** workflow — distinct from
a **native** `(Zillow · Tour Request)` — because the voice differs:

```
FUB-import · Zillow · Tour Request   → "Hey Jon, this is Myli — just got your tour request for 123 Main from
                                        Zillow. You worked with Alicia Hanks on our team before — what time
                                        works best?"                          (reactivation: FUB pulled in the history)

Native · Zillow · Tour Request       → "Hey Jon, this is Myli with HouseLoop — we're the local preferred Zillow
                                        partner in Overland Park. I see you want 123 Main today at 3:00, getting
                                        that set up. Any other homes you're interested in?"   (fresh partner intro)
```

**⚠️ OPEN ARCHITECTURAL BUILD (NOT built yet — Lee to spec):** `hl_fub_webhook_settings` today has **one row per
webhook TYPE** (`property_inquiry` · `text` · `in_app` · `phone_call` · `notification`), **not per sub-source**.
To bind a distinct workflow per `(FUB × sub-source × event)`, FUB events need a **sub-source dimension** (the portal
FUB pulled from). This is the keystone the two-different-Zillow-workflows depends on.

### The surfaces (no overlap)
- **`/settings/lead-platform` — Lead Sources (the one page).** Every source is a block: split header
  (`<name>` · **⚙ Configure**) → **KPI funnel** → its **registration events** (ON · EVENT · WORKFLOW · ACTIONS).
  Order: HouseLoop.ai · Follow Up Boss · Manual Entry. Merges the old Registration Events + Lead Sources pages.
- **Configure** (per source) — that source's **defaults** (name · AI responder · human owner · messages-from ·
  tags · API key), one first-class page. FUB's Configure (`/settings/fub`) also holds its webhook config
  (process · creates-lead · notify · notify-via + master switch). The **only** place "Follow Up Boss" internals live.
- **`/settings/system-events` — System Events.** Existing-lead activity (tour/contact/saved…), its own page —
  it's not lead-creation.
- **`/workflows` — Workflows**, grouped by lead source (registration) + agnostic System/Follow-up bands.
- **Lead detail page** — where **source** renders for an actual lead.

**Nav under "Pipeline & Workflow": Lead Sources · System Events · Workflows.** (The standalone Registration
Events + Lead Sources + FUB Settings nav items are gone — collapsed / folded in.) **Retiring a superseded page =
redirect the route + repoint inbound links.**

## 🏛️ LEAD SOURCES — the one page (shipped 2026-06-30 PM)

Everything resolves into **ONE page** — there is no separate "Registration Events" and "Lead Sources" anymore.
Each lead source renders as a single block:

```
LEAD SOURCES  (/settings/lead-platform)
┌─ HouseLoop.ai ································· [⚙ Configure]      ← split blue header
│    KPIs:  Total Leads · Buyers UC/Sold · Sellers UC/Sold · Volume     ← the funnel
│    Registration events:  ON · EVENT · WORKFLOW · ACTIONS(→ builder)     ← ordered by the Workflows order
├─ Follow Up Boss ······························· [⚙ Configure]
│    KPIs  ·  its webhook events (from hl_fub_webhook_settings)
└─ Manual Entry ································· [⚙ Configure]
     KPIs  ·  Lead Added Manually
```

- **Configure** (header, far-right) → the source's **defaults** page (one place). FUB's adds the webhook config.
- **Open in Builder** from any event carries `?from=/settings/lead-platform` → the builder's Back reads
  **"Back to Lead Sources"** (dynamic Back — returns where you came from, not the generic Workflows list).
- Shipped: collapse (`a5a595db94`) · first-class Configure (`fe4b10d415`) · workflows grouped by source
  (`7e09aea7cc`) · dynamic Back (`c8697f88ad`).

**Open follow-ups:** the FUB sub-source dimension (above — the big one); downhill origins (PPC · Zillow · Sphere ·
Referral) become **tags** under a source vs their own blocks; FUB's single Save at the bottom of its Configure.

## 🛤️ ARCHITECTURE RAILS — the preamble (must be ✅ before any matrix row passes)

These are the shared rails every event rides. **No event row can be signed off until the rails it needs are
green.** This is the "build the architecture correctly first" layer — the answer to "what has to be true before
the first inquiry can flow natively into HouseLoop and notify you cleanly."

| Rail | What it is | Why it matters | Status |
|---|---|---|:--:|
| **R1 — Native FUB ingestion** | FUB webhook data flows into HouseLoop directly; the Realloop dashboard keeps mirroring `fub.*` data **only** — it is no longer the notifier/in-between | Removes the realloop.co relay that double-fires | ◐ partial (notify native; data still mirrors via dashboard) |
| **R2 — HouseLoop notifier** | ONE native notification system (`hl_notification` → your channel), fired per-event, deduped | HouseLoop = first-class, canonical notification | ✅ |
| **R3 — Notify-toggle repoint** | The FUB **Notify VIA** control drives **HouseLoop's** notify (not the dashboard's) | One switch, one place | ✅ |
| **R4 — Realloop kill-switch (per-event)** | The dashboard's `notifyZach*` (Twilio **355** text + Telegram) gated OFF per-event as HouseLoop takes that event | Stops the duplicate 355 text, one event at a time | ✅ |
| **R5 — Lead create/match engine** | Native create-or-match (person / caller number, company-line verified) | The lead exists before anything renders | ✅ (existing) |
| **R6 — Card render** | Agnostic plain-text feed (scales infinitely) + the Zillow-inbound render MVP | The activity is visible in HouseLoop | ◐ (feed ✅; Zillow render MVP ☐) |
| **R7 — Workflow dispatch** | The event's `workflow_id` attach + the dispatcher fires the right `(source × event)` workflow | The lead gets handled | ✅ (existing) |
| **R8 — Webhook test harness** | Fire/replay a real FUB webhook on demand + verify every gate | Proof, per INNGEST_OR_IT_DOESNT_EXIST | ☐ |

**R2–R4 shipped 2026-06-30** (commits `e98e38e081`, `8d1f08a320`): migration `0270` adds
`hl_fub_webhook_settings.notify_owner` (`realloop` default / `houseloop`). When a row is flipped to
`houseloop`, `fub-lead-import` fires the native `createNotification` (bell + per-user SMS/email + Run Log)
and the dashboard suppresses its hardcoded "355" SMS for that event. The cutover lever is the **NOTIFY VIA**
control on the FUB source's Configure, which links to **Notification Settings** (the *New lead created* alert
governs the channels + sender). Default `realloop` preserves current behavior until a row is deliberately migrated.

## ✅ THE SIGN-OFF MATRIX — every event is a row, every column is a gate

**The six gates (what we sign off on, per event):**

| Gate | Question | Proof |
|---|---|---|
| **Lead** | Did a lead get created/matched in HouseLoop? | `hl_leads` row |
| **Card** | Did the card / text get pulled in + render? | `hl_lead_activity` row, visible in the feed |
| **Workflow** | Is a workflow (text + email) built + active to handle it? | `hl_workflows` active, canvas visible |
| **HL Notify** | Did the notification fire **from HouseLoop**? | one clean ping to you |
| **RL Off** | Did Realloop's notification get retired for this event? | no 355 text, no dupe |
| **✅ Verified** | Fired E2E through the Run Log + Zach signs off? | Run Log run-id + your ✅ |

### 🟦 FUB — Property inquiry (Zillow inbound)
| Event | Lead | Card | Workflow | HL Notify | RL Off | ✅ |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| General property inquiry | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| Tour request | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| Contact request | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| Seller request | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| Virtual tour | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |

### 🟦 FUB — Inbound activity
| Event | Lead | Card | Workflow | HL Notify | RL Off | ✅ |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| Zillow in-app message received | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| Phone call received | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| Text message received | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |

### 🟦 FUB — Website activity
| Event | Lead | Card | Workflow | HL Notify | RL Off | ✅ |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| Property viewed | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| Property saved | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| Property shared | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |

### 🟦 FUB — Zillow insights *(→ route to Myli/AI, not auto-blast)*
For insights the **Workflow** gate = "routed to AI weighting" (a signal, not an outbound blast); **HL Notify** is optional/digest.
| Event | Lead | Card | Workflow (AI) | HL Notify | RL Off | ✅ |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| High-intent buyer | ☐ | ☐ | ☐ | — | ☐ | ☐ |
| High-intent seller | ☐ | ☐ | ☐ | — | ☐ | ☐ |
| Viewed home 10× in 7 days | ☐ | ☐ | ☐ | — | ☐ | ☐ |
| Looking in new area | ☐ | ☐ | ☐ | — | ☐ | ☐ |

### ☎️ PHONE — inbound number classes
**Workflow** gate here = the per-number routing + Myli text-back (Phase 4). Twilio port + voice agent are infra rows in Build Phase 4.
| Event | Lead | Card | Workflow | HL Notify | RL Off | ✅ |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| Google My Business number | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| Sign-call number | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| Agent phone numbers | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |

### 🏠 HOUSELOOP — Registration (new lead)
HouseLoop-source events — **no Realloop to retire**, so **RL Off = n/a**.
| Event | Lead | Card | Workflow | HL Notify | ✅ |
|---|:--:|:--:|:--:|:--:|:--:|
| General registration | ☐ | ☐ | ☐ | ☐ | ☐ |
| Tour request | ☐ | ☐ | ☐ | ☐ | ☐ |
| Contact request | ☐ | ☐ | ☐ | ☐ | ☐ |
| Liked home | ☐ | ☐ | ☐ | ☐ | ☐ |
| Saved home | ☐ | ☐ | ☐ | ☐ | ☐ |
| Saved search | ☐ | ☐ | ☐ | ☐ | ☐ |
| Seller report | ☐ | ☐ | ☐ | ☐ | ☐ |
| Seller get started | ☐ | ☐ | ☐ | ☐ | ☐ |
| ~~Shared home~~ — removed (don't force registration just to share; Zach 2026-06-30) | | | | | |

### 🏠 HOUSELOOP — Existing lead
| Event | Lead* | Card | Workflow | HL Notify | ✅ |
|---|:--:|:--:|:--:|:--:|:--:|
| Liked home | — | ☐ | ☐ | ☐ | ☐ |
| Saved home | — | ☐ | ☐ | ☐ | ☐ |
| Shared home | — | ☐ | ☐ | ☐ | ☐ |
| Saved search | — | ☐ | ☐ | ☐ | ☐ |
| Property tour | — | ☐ | ☐ | ☐ | ☐ |
| Contact request | — | ☐ | ☐ | ☐ | ☐ |

\* Existing-lead events match an EXISTING lead (no new lead created) — the **Lead** gate is n/a.

### 🔄 PAST-CLIENT RECONCILIATION *(different shape — its own gates)*
| Item | Table | Listing page | Seller report | Ana campaign | ✅ |
|---|:--:|:--:|:--:|:--:|:--:|
| Reconcile all past clients → single table | ☐ | ☐ | ☐ | ☐ | ☐ |

---

## 🏗️ THE BUILD PHASES (the sequence — checklists inside)

### ✅ PHASE 0 — Foundation (shipped)
- ✅ FUB webhook config (per-webhook toggles + Workflow attach: Open in Builder / Create / Clone) — now on the FUB source's Configure (`/settings/fub`)
- ✅ **Lead Sources** — the one page: each source = header + KPI funnel + its registration events (ON toggle + workflow Actions)
- ✅ Phone Numbers registry (`hl_phone_numbers`, 41 FUB numbers) — `/settings/phone-numbers`
- ✅ Unified Event table · dispatcher (`trigger_type`) · FUB source pass-through · Clone-workflow API · plain-text feed
- ✅ **Workflows grouped by lead source** + **dynamic Back** to Lead Sources (2026-06-30 PM)

### ⏳ PHASE R — Architecture Rails (the preamble — do this FIRST)
The shared plumbing from the Rails table above. **No matrix row passes until the rails it needs are green.**
- ☐ R1 Native FUB ingestion (Realloop mirrors `fub.*` data only, stops being the notifier)
- ☐ R2 HouseLoop notifier (`hl_notification` → your channel, deduped) · ☐ R3 Notify-toggle repoint → HouseLoop
- ☐ R4 Per-event Realloop kill-switch (gate the dashboard's 355/Telegram `notifyZach*`)
- ☐ R5 Lead create/match · ☐ R6 Card render · ☐ R7 Workflow dispatch · ☐ R8 Webhook test harness
- **The Realloop→HouseLoop notification migration is built into every FUB row's sign-off:** as a row goes live
  (HL Notify ✅), its Realloop notify is retired (RL Off ✅). We never cut Realloop wholesale — it migrates down the matrix.

### ⏳ PHASE 1 — Events & triggers exist for every door
- ✅ 1a. Registration/existing doors confirmed present (New-Lead-Shared-Home removed per Zach). Inbound (phone/text/in-app) fire via the **FUB source** config (no door); insights → AI (no door).
- ☐ 1b. Wire **virtual tour** + the 4 **insights** signals into the catalog/AI weighting.
- ☐ 1c. Inbound phone/text/in-app `creates_lead` → flip ON once the text-back workflow exists (4b).
- ☐ **1d. FUB sub-source dimension** — split FUB events by the incoming portal (Zillow/realtor.com/…) so each `(FUB × sub-source × event)` binds its own workflow. **The keystone for source-specific FUB copy.**

### ☐ PHASE 2 — A workflow (text + email + nurture) for EVERY event
- ☐ 2a. Add the missing **text step** to seller-report + seller-get-started
- ☐ 2b. Existing-lead viewed/liked/saved/shared/saved-search DRAFT→ACTIVE w/ text+email (clone the Liked-Home playbook)
- ☐ 2c. Workflows for the remaining events (virtual tour, inbound, insights)
- ☐ 2d. **Nurture** drips for all

### ☐ PHASE 3 — Zillow-inbound render MVP *(native-iMessage feel)*
- ☐ 3a. **Zillow logo** in the icon tile (compact + hero)
- ☐ 3b. **"Find on HouseLoop"** — look the house up on click, not preloaded
- ☐ 3c. iMessage-bubble restyle

### ☐ PHASE 4 — Phone system *(4 numbers: Company · Signs · Ana old · Agent-fwd)*
- ☐ 4a. Per-number routing: inbound `to_number` → registry → fire its workflow
- ☐ 4b. **Myli text-back** workflow (company-call MVP) + bind to company numbers
- ☐ 4c. Decouple `hl_dialer_assignments` (multiple numbers per user)
- ☐ 4d. FUB Team-Inbox sync (real forwarding, not seeded)
- ☐ 4e. **[Zach + external]** Port the 4 numbers to Twilio + repoint Ana's Google Voice (5–10 biz days/number)
- ☐ 4f. **[later]** Voice agent answers all calls (Twilio → LiveKit bridge)

### ☐ PHASE 5 — Past-client reconciliation
- ☐ 5a. Reconcile all past clients into the single canonical table
- ☐ 5b. HouseLoop listing page · ☐ 5c. Seller report · ☐ 5d. Ana AI-assist follow-up campaign

### ☐ PHASE 6 — Go-live 🔒 *(Zach's typed word)*
- ☐ 6a. Quarantine the legacy FUB Zillow doors → real inquiries fire real workflows (E2E-verify each in the Run Log first)

---

## Current State — the three boxes today

HouseLoop's pipeline is "everything is a workflow" (see `GT_HOUSELOOP_EVERYTHING_IS_A_WORKFLOW_PRD.md`). A lead
enters through a **lead source**, fires an **event**, and that `(source × event)` runs its **own** workflow.

### Lead Sources *(route `/settings/lead-platform`)*
The one page. Each lead source — **HouseLoop.ai · Follow Up Boss · Manual Entry** — is a block: split header
(`<name>` · **⚙ Configure**) → **KPI funnel** (total leads · buyers/sellers UC·Sold · volume) → its
**registration events** (ON · EVENT · WORKFLOW · ACTIONS). **Configure** = that source's defaults (AI responder ·
human owner · messages-from · tags · API key); FUB's Configure also carries its webhook config (process ·
creates-lead · notify · notify-via + master switch). Backed by `hl_lead_sources` + `hl_lead_source_events`
(FUB via `hl_fub_webhook_settings`). Canonical: `GT_HOUSELOOP_LEAD_PLATFORM_PRD.md` *(its §2 "never one workflow
per source / merge fields fill the words" is SUPERSEDED — see the Model above)*, [Lead Sources](/guides/lead-sources).

### Workflows *(grouped by lead source)*
The canvas engine — triggers, conditions, branches, the sender chain. **Registration workflows are grouped by
lead source** (a band per source); System + Follow-up stay agnostic. Every step is visible on the canvas
(WORKFLOW_CANVAS_IS_TRUTH) and every run lands in the Run Log (INNGEST_OR_IT_DOESNT_EXIST). System workflows are
locked defaults; custom workflows are takeovers. Opening a workflow from a source event carries `?from=` so the
builder's Back returns to Lead Sources. See [Workflows](/guides/workflows), `GT_HOUSELOOP_WORKFLOW_TRIGGERS_SSOT.md`.

### System Events *(route `/settings/system-events`)*
Existing-lead activity — tour/contact/saved/liked/shared/saved-search fired by a lead that already exists. Its
own page (not lead-creation). Attach a workflow to each; agnostic (fires for any lead regardless of source).

### Follow Up Boss — the special source *(config shipped 2026-06-29; folded into the FUB source's Configure)*
FUB is a lead source that *creates* webhook events **and pulls in its own sub-sources** (Zillow, realtor.com…).
Backed by `hl_fub_webhook_settings` (migration 0267) — one row per FUB webhook **type** with three toggles + a
global master switch:

| Webhook Event | Process | Creates Lead | Notify |
|---|---|---|---|
| Property Inquiries | ✓ | ✓ | ✓ |
| Text Messages | ✓ | (opt-in) | ✓ |
| In-App Messages | ✓ | (opt-in) | — |
| Phone Calls | ✓ | (opt-in) | ✓ |
| Notifications | ✓ | — | — |

- **Process** = ingest the webhook into the lead's history. **Creates Lead** = if no lead matches, this event
  spins one up (phone calls match the caller number with company/shared-line verification; texts/in-app match
  the person). **Notify** = ping Zach when it fires. **Notify VIA** = Realloop (default) → HouseLoop (per-event cutover).
- The dashboard webhook (`apps/dashboard/server/fub-webhooks.ts`) reads this table live (60s cache) and gates
  notify + lead-creation per row. Canonical: `GT_HOUSELOOP_FUB_LEAD_IMPORT_PRD.md`, [FUB Lead Import](/fub/lead-import).
- **The open build:** these rows are per webhook **type**, not per **sub-source** — so today all Zillow / realtor
  inquiries share one workflow. The **sub-source dimension** (Phase 1d) is what lets a FUB-Zillow tour request fire
  a different workflow than a FUB-realtor one, and both differ from a native Zillow lead.

---

## 🗄️ Quarantine — the legacy per-source FUB door model (retired, NOT deleted)

The original model made Follow Up Boss a "platform" with a Zillow.com "source" carrying per-source
registration **doors** bound to placeholder workflows. We retired it in favor of the FUB source's webhook config +
`(source × event)` workflows. These rows are **kept for audit** (Rule #0 — no destructive deletes) and hidden from
the live pages:

- `hl_lead_sources` rows with `platform='follow_up_boss'` — `fub_import` ("SRG-Follow Up Boss", displayed as
  **Follow Up Boss**, 8 inactive doors) and `zillow_com` ("Zillow.com", 2 active doors). Zillow.com survives as a
  plain **source tag** (a sub-source, not its own block).
- `hl_lead_source_events` FUB door rows (`tour_request_registration`, `contact_request_registration`, …).
- The **Coming Soon** placeholder workflows (`sys_coming_soon`, `sys_coming_soon_contact`) — zero-node no-ops
  the Zillow doors pointed at.
- The dispatch's per-source door resolution (`fub-lead-import.ts` `TRIGGER_DOORS`) → replaced by the FUB webhook
  `workflow_id` attach.

Why quarantine and not delete: the rows carry historical attribution (which leads came through which door) and
removing them is a destructive prod operation. They are flagged + hidden; the new path is the FUB source's config.
