---
title: "Website Registration & Lead Intake"
description: "THE guide for how a houseloop.ai visitor becomes a CRM lead — the sign-in modal and auth gates, the phone policy and Finish/X law, the ACTION_TO_EVENT intake contract, the 9 registration doors and their full two-beat messaging map, tour & contact requests, the shared-home gate, and the pixel identity stitch."
---

<Note>
**THE single registration & intake guide (consolidated 2026-07-10, Adam).** Absorbs the former standalone
*Registration & Sign-in*, *Tour & Contact Requests*, and *HouseLoop.ai Lead Integration* pages (their URLs
redirect here). The doors' on/off switches live on [Trigger Events](/guides/trigger-events); the engine under
each door's canvas is [Workflows](/guides/workflows).
</Note>

## The pipeline in one picture

```
visitor on houseloop.ai
  → SignInModal (one unified modal, every entry point)
  → auth (Google · email+password · magic-token)
  → the phone step (Finish or X — THIS is when the lead is born)
  → POST /api/integrations/website/lead-intake   (the ONE intake door, per-source secret)
  → ACTION_TO_EVENT → the registration door fires (hl_trigger_fire ledger row)
  → the door's workflow runs (the two-beat messaging)
```

## The unified sign-in modal

Every auth/registration entry point opens **one** component — `components/site/SignInModal.tsx` — identical
everywhere (like a home, save a search, the profile "Sign in" button). Single-page join-or-sign-in with an
optional phone step: `identify` (Google on top, or email) → `details` (password; "Welcome back" for a known
email, "Create your account" for a new one — decided by a read-only `GET /api/auth/email-exists` lookup) →
`profile` ("You're almost there" — name + phone).

**The auth gate** (`components/site/auth-gate.tsx`) is the single choke point: a signed-out buyer clicking a
gated action (like / save / save-search / nav headings) gets the modal with contextual copy, and the pending
like/save is stashed and **replays after auth**. Sharing a home opens for everyone — the gate fires at SEND
(see the shared-home gate below).

**Google returns to the modal, not a white page:** `app/auth/complete-profile/page.tsx` is a pure router — the
return lands back in the modal (`?hlAuthProfile=1`) at the profile step over the blurred original page.

## The three ways in (auth — `apps/houseloop-web/auth.ts`)

1. **"Continue with Google"** — `GoogleProvider` via PrismaAdapter.
2. **Email + password** — `CredentialsProvider` (bcrypt vs `User.passwordHash`).
3. **Magic token** — `verifyMagicToken`: signed per-lead links auto-log the lead in (also how "Create Saved
   Search as Agent" opens the site AS the lead).

Account linking: email+password first, Google later (same email) lands on the SAME `User` row. Sessions are JWT.

## The phone policy + the Finish/X law

One CRM setting (**Settings → Website → Registration Settings**, `social_phone_requirement`) controls the phone
step: **Required** (no friendly skip, corner X only) · **Optional** ("Skip for now") · **Hidden** (step not
shown). There is **always an X** — you can't cage a buyer into a real number.

**A lead is created at registration completion, never at login.** Authenticating only makes a website `User`.
The CRM lead is born when the phone step resolves, exactly two ways:

```
auth succeeds → NO CRM lead yet
   ├─ Finish (enter phone) → lead WITH phone + the stashed like/save REPLAYS → home saved
   └─ X / Skip             → lead with email+name + "no phone" note (phoneSkipped) → the like/save is CLEARED
```

Only Finish saves the home — the phone is the carrot. A logged-in, phone-less buyer gets **re-prompted** on
every subsequent like/save (`needsPhone` in the engagement bar) until they give a number. The phone step carries
the TCPA consent fine print (`ConsentFinePrint.tsx`); Finish is the consent action. The four **high-intent
forms** (tour, contact, home-value report, seller get-started) hard-require a phone on the form itself,
independent of the global policy.

## The intake contract — `ACTION_TO_EVENT`

Every site action that should touch the CRM POSTs to **`/api/integrations/website/lead-intake`** (per-source
`x-houseloop-intake-secret`). The payload's `action` maps through **`ACTION_TO_EVENT`** to ONE event key, and
the **`REG_VARIANT`** map splits by the `created` flag — a brand-new person fires the `*_registration` door, a
known lead fires the base door:

| `action` | new lead (`created`) fires | existing lead fires |
|---|---|---|
| `google_signin` (bare signup) | `general_registration` | — |
| `liked/saved/shared_home_registration` | that entry-point's door | — |
| `showing_request` | `tour_request_registration` | `tour_request` |
| `contact_agent` | `contact_request_registration` | `contact_request` |
| `saved_search` | `saved_search_registration` | `saved_search` (+ `_agent` / `_system` origin variants) |
| `seller_request` / `claim` | `home_value_report_registration` | `home_value_report` |
| `seller_get_started` | `seller_get_started_registration` | `seller_get_started` |

**An unmapped action creates the lead with NO event** (warn-logged) — no tag, no workflow. Every genuine door
dispatch writes ONE row to the **`hl_trigger_fire` ledger** — the same table the
[Trigger Events](/guides/trigger-events) board's count AND drill-down read.

## The 9 registration doors → the two-beat messaging

houseloop.ai is our own site, so this pipe needs no API key — each action fires one of the **9 doors** below,
and every door binds ONE workflow ("New Lead — X") that sends the **two-beat**. Live-fire verified 2026-07-09
(all 9 doors, both phone variants, timing measured).

### The two-beat law

Every new registrant gets exactly two beats:

1. **Beat 1 — the branded acknowledgment, instantly.** Email from **HouseLoop `<updates@houseloop.ai>`** + an
   ack text from the **branded line (913) 286-1761**. Corporate voice, signed "— HouseLoop." Never a person.
2. **Beat 2 — Myli, personally, at ~2 minutes.** Email from **Myli `<myli@houseloop.ai>`** + a text from **her
   own line** (BlueBubbles iMessage by default; her Member Connections toggle can flip her to Twilio SMS — the
   toggle drives the live send path). Human voice, referencing exactly what the person did or asked — the
   `ai.*` sections are **composed live per lead** by Myli, each with an editable static fallback. She never says
   "an agent will reach out" (the no-handoff law — she books directly).

Sender identities are configuration, not code: the brand line = `notification_sender.sms_from_number`, Myli's
line = her dialer/BlueBubbles assignment. **No phone number is hardcoded anywhere** (verified: zero grep hits).

### The doors → workflows → messaging

| Door | Workflow | Beat 1 (brand) | Beat 2 (Myli) |
|---|---|---|---|
| General Registration | New Lead — General Registration | "Welcome to HouseLoop" email (saved search set up + starter matches) + welcome ack SMS | `ai.general_reg_intro` — personal opening from their actual browsing + `ai.general_reg_text_hook` |
| Liked Home | New Lead — Liked Home | "The home you liked — plus a few like it" (hero + similar) + favorites-saved ack SMS | `ai.liked_home_intro` — her one-sentence reaction to THE home + text hook |
| Saved Home | New Lead — Saved Home | "The home you saved…" + saved ack SMS | `ai.saved_home_intro` + hook |
| Saved Search | New Lead — Saved Search | "Your search is saved — here's what matches" (live match cards) + search-live ack SMS | `ai.saved_search_intro/seen/close` — grounded in THEIR criteria + what it returns right now |
| Shared Home | New Lead — Shared Home | "The home you shared…" + the RECIPIENT gets "`{{name}}` shared a home with you" + ack SMS | `ai.shared_home_intro` + hook |
| Tour Request | New Lead — Tour Request | "We've got your tour request — `{{address}}`" (date/time card) + received ack SMS | `ai.tour_reg_intro` — confirms she's lining it up, ANSWERS their typed question, says what happens next |
| Contact Request | New Lead — Contact Request | "Thanks for reaching out" + received ack SMS | `ai.contact_reg_intro` — acknowledges THEIR question and answers it when the facts allow |
| Seller — Get Report | New Lead — Seller Get Report | "Your home valuation request is in" + report-building ack SMS | `ai.seller_reg_intro` — report teaser (value · comps · live buyer demand) + one natural timeline question |
| Seller — Get Started | New Lead — Seller Get Started | "We received your selling request" + options ack SMS | `ai.seller_reg_intro` (tier-aware — references the plan they picked) |

Templates are editable rows in `hl_templates` (Settings → Email / Text Templates); the `ai.*` context blocks
(prompt + fallback) live there too.

### Sample: Tour Request (live-fire capture)

<div style={{border: '1px solid #DFE6F0', borderRadius: '12px', overflow: 'hidden', margin: '16px 0'}}>
  <div style={{background: '#EAF0FF', padding: '8px 14px', fontSize: '12px'}}>📧 <b>0:00</b> · FROM <b>HouseLoop &lt;updates@houseloop.ai&gt;</b></div>
  <div style={{padding: '10px 14px', fontWeight: 700}}>We've got your tour request — 888 Green Bubble Way</div>
  <div style={{padding: '0 14px 14px', fontSize: '14px'}}>Hey Zach, your tour request for 888 Green Bubble Way on Wed, Jul 15 at 10:00 AM has been received… — The HouseLoop Team</div>
</div>

<div style={{background: '#12325E', color: '#EAF0FF', borderRadius: '16px 16px 16px 4px', padding: '10px 14px', margin: '10px 0', maxWidth: '560px', fontSize: '14px'}}>
  🏢 <b>0:00 · from (913) 286-1761:</b> Hi Zach, your tour request for 888 Green Bubble Way, Shawnee on Wed, Jul 15 at 10:00 AM has been received. — HouseLoop
</div>

<div style={{background: '#7C3AED', color: '#F3EDFD', borderRadius: '16px 16px 16px 4px', padding: '10px 14px', margin: '10px 0', maxWidth: '560px', fontSize: '14px'}}>
  🤖 <b>~2:00 · from Myli's line:</b> Hey Zach, it's Myli - got your tour request for 888 Green Bubble Way on July 15 at 10 AM, and I'm on it. I'll confirm the exact time shortly, and I'll check on the roof question for you too.
</div>

### The canvas shape (every door, identical skeleton)

```
stage → (door seed/note) → BRAND EMAIL (Send as: HouseLoop) → phone branch
  ├─ has phone:  BRAND ACK SMS → wait 2m → MYLI EMAIL → MYLI SMS → done
  └─ no phone:   wait 2m → MYLI EMAIL → done   (texts correctly skipped, never a failed run)
```

Every send step declares its sender explicitly on the canvas; the no-phone branch still gets both emails
(verified: run completes, 2-minute gap measured 121–146s across all doors). Verification receipts: live-fire
matrix `.claude/reports/trigger-events-test-matrix-2026-07-09.md`; every fire = exactly ONE `hl_trigger_fire`
ledger row (board count == drill-down, by construction); the channel toggle was proven both directions on a
real phone (BlueBubbles blue ↔ Twilio green) on the same registration.

## Tour & contact requests (absorbed from the former standalone page — Lee's 2026-07-09 rewrite)

The old "agent Accepts/Declines, then an agent reaches out" flow is **retired — Myli books directly and hands
off**. Mechanics:

- **Templates, nothing hardcoded:** every message is an `hl_templates` row, editable under Settings → Email /
  Text Templates (Tour Request + Contact Request categories): `tour_request_received(_sms)`,
  `tour_request_agent_alert(_sms)`, `tour_confirmation(_sms)`, and the contact equivalents.
- **Merge tokens:** `{{request.address|date|time|message|photo|price|beds|baths|type|url}}` —
  `{{request.url}}` deep-links to the exact request under the lead's **Homes → Requests**.
- **Senders are a setting** (`hl_settings.notification_sender`): agent alerts from `notifications@houseloop.ai`,
  lead acks from `updates@houseloop.ai`, all SMS from the notification number (changeable, no deploy).
- **Inngest auto-alerts:** `tour-requested-alert` / `contact-requested-alert` fire from
  `houseloop/tour|contact.requested` (emitted by the pixel ingest when the event stitches to a lead) — each
  send is a durable `step.run`, logged to `hl_email_send_log`, audited in **Run Log → alerts**.
- **The request item:** property card + buyer's message + status (**Pending → Confirmed/Declined**) on
  `hl_pixel_event.event_payload.status`. Myli advancing the status is the primary path; the inline
  Accept/Decline buttons are the agent's manual override.

## The shared-home gate

Sharing is a **login-gated registration event**: the share modal opens for everyone, the gate fires at SEND — a
signed-out sharer registers first (`shared_home_registration` door) and the RECIPIENT gets the branded
"`{{contact.first_name}}` shared a home with you" email.

## The pixel identity stitch

The first-party pixel (`lib/pixel/hl-pixel.ts` → CRM `/api/pixel/track`) tracks anonymous browsing; at
registration the visitor's anonymous events are stitched to the lead (their pre-registration views appear on
the timeline and power the system-seeded starter saved search). Feed rows are history — **they are never
trigger fires** (only a real door dispatch writes the ledger).

<Warning>The old third-party SureSend pixel is DEAD — the first-party `hl-pixel` is the only tracker.</Warning>

## Reference

- `apps/houseloop-web/components/site/SignInModal.tsx` · `auth-gate.tsx` · `ListingEngagementBar.tsx` (`needsPhone`)
- `apps/houseloop-web/app/api/auth/complete-profile/route.ts` — the single lead-creation point (`pushLeadToCRM`)
- `apps/houseloop-crm/app/api/integrations/website/lead-intake/route.ts` — `ACTION_TO_EVENT` / `REG_VARIANT`
- `apps/houseloop-crm/lib/notifications/request-alerts.ts` — the tour/contact Inngest alerts
- [Trigger Events](/guides/trigger-events) · [Workflows](/guides/workflows) · [Lead Activity Log](/guides/lead-activity-log) · [Follow Up Boss integration](/integrations/follow-up-boss) (the other lead pipe)
