---
title: "Phone Numbers, Call Routing & Number Porting"
description: "The complete SRG/HouseLoop phone system — every line and inbox, how the dialed number drives Myli's dynamic read, the FUB phone events, the God-tier endgame (port every number out of Google Voice / Follow Up Boss into our own Twilio so HouseLoop owns the number and the conversation), and the porting PRD it executes against. Sections: (1) Events, Inboxes & Routing; (2) The Port-Out Playbook; (3) The Porting PRD — decisions, requirements, acceptance criteria, phases."
---

<Note>
**Internal command surface — the single deep doc for everything phone.** The
[Pipeline & Workflow Overview](/guides/pipeline-workflow-overview) has a phone summary + the Sign-Off Matrix's
**FollowUpBoss — Phone Call Activity** row; THIS page is the authority (how-to reference **and** the porting spec —
the former PRD is folded in as Section 3). Canonical GT: `GT_HOUSELOOP_MASTER.md`
§8b + `.claude/checkpoints/srg-twilio-numbers-2026-06-22.md`. Credentials live in the vault / Master Sheet
(never printed here). The voice agent that *answers* the front door is Phase D — see [Myli Voice](/guides/myli-voice).
</Note>

# ═══ WHERE WE ARE — THE ONE FRONT DOOR (status as of 2026-07-11) ═══

**The architecture, locked by Zach 2026-07-11 (his words):** *"we just forward everything to ONE Twilio number
and have Myli the voice agent filter through what the caller is looking for — 'Spradling Group, this is Myli,
how can I help you?' — then Myli puts them in the correct disposition. Way more simple."*

**913-675-9877 is THE FRONT DOOR.** Every line funnels into it: Zach's FUB ring, Company + Signs, the 39 legacy
past-agent numbers (via FUB member ring — no per-number flip needed), and Ana's Google Voice 954-7123. Interim
behavior: the front door rings Zach's cell for 18s; a miss rolls to OUR voicemail with recording + typed
notification. Phase D puts Myli on the line.

**Full receipts artifact (screenshots · live call legs · DB rows · the dedup fix):**
[🚪 THE ONE FRONT DOOR — Phase A+B receipts](https://claude.ai/code/artifact/1d4dd9ba-47aa-4cca-8feb-7198a8ea9f80)

## The checklist — done vs. remaining

**Done (every item live-fired, receipts below + in the artifact):**

- [x] **Voice webhook honors routing groups** — ring targets actually ring; the settings UI is no longer
  cosmetic (the "Jeremy David" class of miss). *Adam · 2026-07-11*
- [x] **Carrier-voicemail race killed** — Dial timeout 25→18s so OUR recorder wins, not Zach's cell VM.
  Found by a live call, not a guess. *Adam · 2026-07-11*
- [x] **Cold/unknown callers notify** — typed by line ("Front Door", "Company line"…); previously silent.
  *Adam · 2026-07-11*
- [x] **Zach's FUB ring → 675-9877** — verified on the FUB settings page + team roster (screenshots below).
  *Adam via browser automation in Zach's session · 2026-07-11 ~15:30 UTC*
- [x] **Ana's FUB seat → 675-9877** — kills the old FUB→GV→FUB loop. Roster-verified. *Adam · 2026-07-11*
- [x] **Ben Froeschl's seat** — already on 675-9877, re-verified. *Adam · 2026-07-11*
- [x] **End-to-end FUB funnel proof** — real call: FUB line 490-2988 → forwarded (caller ID preserved) →
  front door → cell → our voicemail + notification. *2026-07-11 15:43 UTC*
- [x] **Google Voice 954-7123 → 675-9877 ONLY** — 675-9877 linked + code-verified; 453-5505 and 407-403-8088
  forwarding OFF. Live hop proven with a real call. *Adam · 2026-07-11 15:58–16:01 UTC*
- [x] **Notification dedup ROOT-FIXED** — dedup keys on the exact call row (signal id), not display text.
  *Adam · 2026-07-11 · commits `cb56d6a116` + `5e3dadfe0b`*

**Remaining for COMPLETE:**

- [ ] **Company (320-0906) + Signs (354-8600) land in our Twilio** — internal FUB→us transfer, Zach approved
  "proceed ASAP" 2026-07-10, Twilio ticket `28049501`. Chaser if not landed EOD 07-11. On landing run the
  landing checklist: A2P pool add → voice/SMS webhooks → front-door routing group → `hl_phone_numbers` rows →
  test call + SMS.
- [ ] **Ana's port re-file — Monday 2026-07-14** — 4× rejected `INVALID_ADDRESS` (22154): the GV
  service-address change takes ~2 business days to propagate to the losing carrier. Address confirmed
  letter-for-letter on her GV screen 2026-07-11 (2097 Valencia Blossom St, Clermont FL 34711). Re-POST via
  the Porting API per [the playbook](#port-a-google-voice-workspace-number--twilio-the-913-954-7123-recipe-battle-tested-2026-07-09); ticket `28052075`.
- [ ] **Phase D — Myli answers the front door** ("Spradling Group, this is Myli — how can I help you?").
  ONE agent, ONE line, ONE greeting; reuses the in-house LiveKit voice framework with 4 HouseLoop tools
  (capture caller · triage buyer/seller/vendor/spam → disposition · notify Zach · take message).
  **GATED — needs Zach's design sign-off before build.** Spec home: [Myli Voice](/guides/myli-voice).
- [ ] **CNAM registration (optional, Zach undecided)** — outbound shows "Spradling Group" instead of a number.

**Caveats / watch items:**

- ⚠️ **Interim:** calls hopping through Ana's GV show caller ID 954-7123 (new GV has no incoming passthrough
  setting). Dies when the port lands.
- ⚠️ **Watch:** Twilio sometimes re-requests the voice webhook (duplicate 0-second dial legs + double
  recordings). Harmless — the exact-signal dedup absorbs it — but on the watch list.

## Decision log

| When (UTC) | Who | Decision |
|---|---|---|
| 2026-07-09 | Zach | Port-vs-Forward locked: we OWN numbers in our Twilio; every number joins the A2P campaign on landing (LAW). |
| 2026-07-10 | Zach | FUB internal transfer of Company + Signs approved — "proceed ASAP" (ticket 28049501). |
| 2026-07-11 ~14:30 | Zach | **THE ONE FRONT DOOR locked** — everything forwards to ONE Twilio number (675-9877); Myli answers in Phase D; interim it forwards to Zach's cell with capture + notification. |
| 2026-07-11 | Zach | Routing calls: his FUB ring → 675-9877 (all-in) · Company + Signs via the front door · Ana → same door · GV 954-7123 → 675-9877. |
| 2026-07-11 | Zach | Renamed his phone contact for 675-9877 → "Spradling Group Incoming (all calls)". |
| 2026-07-11 14:5x–16:1x | Adam | Executed Phase A (routing code) + Phase B1 (FUB + GV clicks via browser automation); live-fired every hop; root-fixed notification dedup. |

## The receipts (native)

### FUB — all three seats ring the front door

Executed inside Zach's real FUB session via browser automation, re-verified live at 16:1x UTC when these
screenshots were taken:

![Zach's FUB Incoming Calls setting — "Ring desktop then forward to (913) 675-9877"](/images/phone/front-door-fub-zach-ring.png)

![FUB team roster — Zach Spradling, Ana Spradling, Ben Froeschl all showing (913) 675-9877](/images/phone/front-door-fub-roster.png)

**End-to-end funnel proof** (test call into Zach's FUB line — the whole chain):

```text
outbound-api   913-733-3704 → 913-490-2988   (test call into the FUB line)
inbound        913-733-3704 → 913-675-9877   ← FUB forwarded; REAL caller ID preserved
outbound-dial  913-675-9877 → 407-403-8088   (front door rang Zach's cell 18s, no answer)
DB row:        outcome=voicemail · recording=true · subtype=phone_call_past_agent
NOTIFICATION:  "Missed call — Front Door (legacy pool)"  created 2026-07-11 15:44:07Z
```

### Google Voice — 954-7123 → the front door

Google refuses to text a verification code to a VoIP line, so we used verify-by-phone — and **the front door
verified itself**: Google's robot called 675-9877, the call rang Zach 18s, rolled to OUR voicemail, our system
recorded it, and the 6-digit code was transcribed out of our own recording (Deepgram: *"again your code is
4 7 7 4 2 5 goodbye"*).

![Google Voice — Your phone number is linked](/images/phone/front-door-gv-verified.png)

![Google Voice settings — 3 linked numbers including 913-675-9877, service address 2097 Valencia Blossom St Clermont FL](/images/phone/front-door-gv-linked.png)

![Google Voice call forwarding — 913-453-5505 OFF, 407-403-8088 OFF, 913-675-9877 ON](/images/phone/front-door-gv-toggles.png)

**Live hop proof** — a real call to 954-7123 immediately after the flip:

```text
inbound   913-954-7123 → 913-675-9877   2026-07-11 16:00:30Z · 61s  (GV → front door)
DB row:   outcome=voicemail · recording=true · subtype=phone_call_past_agent
```

### The dedup fix (found BY these tests)

The GV hop test left a voicemail but no notification — the dedup suppressed it because it keyed on the
notification's display TITLE in a 5-minute window, so two DIFFERENT missed calls with the same title collapsed
to one ping. Root fix (2026-07-11): `createNotification` now dedups on the **exact signal** — recipient +
event + `entityType`/`entityId` (the `hl_calls` row id) — and the title heuristic survives only as a fallback
for callers that don't pass a signal id yet. Distinct callers always notify; Twilio double-callbacks still
collapse; card wording no longer affects dedup behavior. Files: `lib/notifications/create.ts` +
`app/api/phone/webhooks/recording/route.ts`.

---

## 💸 Why this exists — the money bleeding out of the inbox

Verified live in the FUB inboxes 2026-06-30, warm messages that **nobody answered**:

- **Kyle Nations** (Apr 28): *"this is Kyle Nations — you've helped us buy 2 apartments, 2 houses, and sell 2
  houses. We're ready to sell and buy again."* A past client we did **~$5M** with; a ~$650K buy + $600K sell ≈
  **$75K commission — sat unanswered.**
- **Salim Rahman** — a **$1.2M** buyer we closed: *"I called to speak with Anna regarding my property, could you
  send me her contact?"* Reaching for Ana (the 913-954-7123 line) — unworked.
- **"Mobile Massage For You"** (Jun 2025, **13 months** unworked): *"I got your number through Zillow partners —
  looking to sell my rental, vacant and almost ready to show."* A net-new Zillow **seller** lead handing us a listing.

Past clients, past buyers reaching for Ana, and fresh Zillow seller/buyer leads — **all dying in the same
1,245-unread Company inbox.** One recovered Kyle pays for this entire build. That's the mandate.

## The North Star

**All comms run through HouseLoop.** FUB is no longer where we talk to people — every call, text, and email flows
through HouseLoop / **Myli**. The endgame (Section 2): **we OWN every number in our own Twilio**, so inbound hits
HouseLoop natively and outbound sends AS the line. FUB is Zillow-owned and progressively closing its data taps
(the walled-garden doctrine) — owning the numbers is how we own the conversation.

---

# ═══ SECTION 1 — EVENTS, INBOXES & MYLI'S ROUTING ═══

## FUB is our phone system today (pre-Twilio)

Verified from the FUB API: **42,382 calls on record; ~66% auto-match a person.** Critical constraint:
**FUB has NO programmatic outbound-SMS API** — you cannot send a text from a FUB inbox number in code (Zach
tested; blocked). And we don't own these numbers in a Twilio account. So today's outbound is **Myli from her own
BlueBubbles number 913-208-1633**; native send-as-the-line waits on the port (Section 2).

## The 3 FUB inboxes (all route to Zach)

| Inbox | FUB id | Unread | What's actually in it |
|---|---|---|---|
| **My Inbox** | Zach's user inbox (FUB line **913-490-2988**; cell 407-403-8088) | 169 | Zach's assigned contacts — **email-campaign replies** (e.g. Halimo replied "Yes" to "Top reasons to own your home"), voicemails, texts |
| **Company** | teamInbox 1 | **1,245** | The company line's catch-all — mostly **UNMATCHED cold inbound** (missed calls, voicemails, "Hi Ana" texts, "Kyle with Zillow"). The unworked speed-to-lead mountain. |
| **Past Agents** | teamInbox 3 | 333 | Departed-agent lines — **FUB natively TAGS each conversation with the agent** (Walter · Glen · Raul · Carrie…). A MIX of BrokerBay showing-feedback (short code `78340` — noise) + **real humans reaching the old agent** (reactivation gold). |

## The number a caller dials = their intent (what Myli reads)

Every inbound carries the line they dialed (`To`). **That line IS the intent.** Mirrored in `hl_phone_numbers`
(41 rows: `number · e164 · label · category · forwards_to · assigned_user_id · workflow_id · creates_event`).

| Source category | Number(s) | Means | Myli's opening frame |
|---|---|---|---|
| **Company / Google My Business** | **913-320-0906** (FUB's texting line) | new prospect found us on Google | speed-to-lead — "saw you called SRG, buyer or seller?" |
| **Sign box / Listings** | **913-354-8600** ("LISTINGS - KEEP") | caller standing at a yard sign | high intent — "which property are you looking at?" |
| **⭐ Ana's old cell (Google Voice)** | **913-954-7123** | an **Ana past client** (her real old cell, in thousands of past-client phones) | warm — look them up in Ana's book, past-client reactivation |
| **Past-agent line** | ~35 lines, each **tagged with the departed agent** | a client of a departed agent | reactivation — **name the agent** (native tag + `hl_srg_agent_roster`) |

## The 5 FollowUpBoss event groups (the phone/webhook taxonomy)

| Group | Events |
|---|---|
| **Property Inquiry** | General inquiry · Tour request · Contact request · Seller request · Virtual tour |
| **Inbound Activity** | Zillow in-app message · Phone call received · Text message received · **Email received (reply)** ← `emailsCreated`, already flows (dashboard catches it, body hidden) |
| **Website Activity** | Property viewed · saved · shared |
| **Zillow Insights** | High-intent buyer/seller · viewed 10× in 7d · looking in new area (→ route to AI weighting, not auto-blast) |
| **Phone Call Activity** | Every inbound call → the ONE workflow below (Myli reads the dialed line → source) |

## Myli's dynamic read — ONE workflow, not one per number

It is **one `(FUB · Phone Call)` workflow.** On `callsCreated` (or inbound text), Myli:

1. **Classifies human vs automated** — skips BrokerBay short-code `78340`, spam, robocalls. Only real humans get a text.
2. **Reads the dialed `To` line → its source category** (company · sign-box · Ana-cell · past-agent) → picks the copy.
3. **Sends the text-back.** Today: **from Myli's own BlueBubbles number 913-208-1633**, as herself — *"Hey, this
   is Myli — just saw a call come into our company line, wanted to reach out personally. What are you looking
   for?"* Past-agent → names the agent; Ana-cell → past-client voice. **After the port (Section 2), the reply comes
   AS the dialed line.**

## Inbound capture — every call & text lands on the lead

<Check>
**Live (shipped 2026-06-22).** Inbound calls, texts, and emails store to the lead record — **including callers
who aren't a lead yet.**
</Check>

The root cause we fixed: FUB delivers unknown callers with **`personId = 0`**, and the handler did
`if (!personId) return` — and `!0` is `true`, so **every missed call, voicemail, and first-touch text from an
unknown number was silently dropped.** Now `handleCall` / `handleTextMessage` / `handleEmail`
(`apps/dashboard/server/fub-webhooks.ts`) store `person_id = 0` as an *unmatched* record keyed by phone number.
The full inbox — missed calls and voicemails included — is captured and converts to a lead when identified.

## ⚠️ The Ana 913-954-7123 Google Voice gotcha (critical — do not lose this)

**913-954-7123 is Ana Spradling's actual old cell.** She released it; we captured it on **Google Voice**; a huge
number of **past clients still have it saved as her cell** → any inbound to it = a potential past client. The problem:

- **Google Voice has NO API** to read texts/voicemails (Google never shipped one; unofficial scrapers are
  unreliable + ToS-violating + against our no-fragile-scraping rule).
- Today it **forwards to the FUB inbox 913-453-5505 (and emails to a black-hole SRG inbox nobody reads)**.
  **Forwarding COLLAPSES the true destination** — FUB sees the forward target, not Ana's line, so we **LOSE** the
  "this was Ana's cell → past client" signal.

**Requirement (Rail R9): we must know NATIVELY that the contact came to 913-954-7123.** Two paths:

- **Interim (buildable now) — the email bridge via the Gmail API.** Google Voice forwards **every text +
  voicemail (with transcription) to email**, and that email identifies **which GV line it hit** + the sender's
  number + the body. Point the Gmail API (we have Workspace admin) at that inbox → Myli parses each GV email →
  *"inbound to Ana's line from `<past client>`"* → fires reactivation. The black-hole inbox becomes Myli's ears —
  and it's *cleaner* than the FUB forward because the email preserves the native line.
- **Permanent — port it out of Google Voice into Twilio** (Section 2). Then it's native: inbound webhook +
  send-as-Ana. **Stop forwarding it.**

---

# ═══ SECTION 2 — THE PORT-OUT PLAYBOOK: OWN EVERY NUMBER IN TWILIO ═══

**The God-tier endgame:** every business line lives in **our own Twilio**, not FUB or Google Voice. Then inbound
SMS/voice hits a **Twilio webhook → HouseLoop native → the Myli workflow**, and outbound sends **AS the exact
line** the person dialed. No middleman, no forwarding shell game, no lost source. We already own **two Twilio
accounts** (HouseLoop + SRG, `support@spradlingrealtygroup.com`, vault / Master Sheet row 78).

## Port a Google Voice (Workspace) number → Twilio (the 913-954-7123 recipe, battle-tested 2026-07-09)

A **Google Workspace** Voice number (managed under our domain) ports differently from a consumer GV number — there's
no `voice.google.com/about/port` unlock screen. The credentials + the address all live in the **Admin Console**.

1. **Get the port-out credentials from Google Admin.** Admin Console → Apps → Google Workspace → **Google Voice →
   Number porting** → the **"Port-out info"** button (top-right of the Ports list). It shows the **US/Canada
   port-out PIN** (10 digits; the reveal eye/copy are trusted-click only — read it off-screen if automating).
   - **Account number** = the **Google Billing ID** from the invoice (e.g. `9620-6665-8983` → `962066658983`) — **NOT**
     the 10-digit phone number, despite what Google's generic help says.
   - **PIN** = that port-out PIN.  **BTN** = `+1<number>`.
2. **Submit the port-in via the Twilio Porting API — not the beta UI.** The "Port-in US Local (Beta)" wizard's
   eligibility step downloads a CSV via a blob link that **silently kills the request when driven by automation** (it
   never persists). Use the API (creds `TWILIO_ZACH_ACCOUNT_SID` / `TWILIO_ZACH_AUTH_TOKEN`):
   - Upload the billing statement: `POST https://numbers-upload.twilio.com/v1/Documents` (multipart `File`,
     `document_type=utility_bill`) → returns `RD…` doc SID.
   - Create the port-in: `POST https://numbers.twilio.com/v1/Porting/PortIn` — **must** include top-level
     `account_sid` + `target_port_in_date` + `notification_emails[]` + `losing_carrier_information{…}` +
     `phone_numbers[{phone_number, pin}]` + `documents[RD…]`.
   - Twilio auto-generates the **LOA** and emails it via **HelloSign** to `authorized_representative_email` for
     signature. On signature → `port_submitted`. Full runbook: memory `reference_google_voice_port_out_playbook`.
3. **Land it.** Point the number's **Messaging + Voice webhooks at HouseLoop** → native inbound; send AS it.

<Warning>
**⚠️ THE #1 REJECTION CAUSE — `INVALID_ADDRESS` (code 22154) + the 2-BUSINESS-DAY PROPAGATION TRAP.**
The port validates the service address against the **losing carrier's (Bandwidth) record**, which = the **Google
Voice "Service address"** (voice.google.com/settings → Service address, per-number), **NOT** the billing/invoice
address. Two attempts for Ana's number bounced `INVALID_ADDRESS`: first the stale Kansas address, then the *correct*
Florida one — because Google's own screen says **"Service addresses register within 2 business days."** When you
**change** the service address it renders instantly in the UI but takes **up to 2 business days to propagate to
Bandwidth**, and the port validates against the not-yet-updated carrier record. **So: fix the Google Voice service
address → WAIT ~2 business days → THEN (re)submit the port with that exact string.** `customer_name` + address must
match letter-for-letter. Twilio opens a Zendesk ticket per attempt but only echoes the `22154` code; reply-to-ticket
reaches Porting Ops if you need the exact CSR address.
</Warning>

<Warning>
**Secondary GV gotcha (VoIP direct-reject):** if a GV number rejects for reasons *other* than address, the
battle-tested fallback = a **two-step port**: GV → a cheap **prepaid mobile SIM** (Tello / US Mobile, ~$10) first,
let it settle a few days, **then** port that SIM → Twilio. Slower, near-100%. Try direct first.
</Warning>

## Port-vs-Forward — the LOCKED decision (Zach 2026-07-09)

We do **not** port all ~35. We port the **3 real assets** and **forward the legacy pool** to one line — far
less porting, same capture.

| Number | Role | Action | Fires |
|---|---|---|---|
| **913-320-0906** | Company (Google My Business) | **PORT** out of FUB → Twilio | `phone_call_company` |
| **913-354-8600** | Sign / Listings | **PORT** out of FUB → Twilio | `phone_call_sign` |
| **913-954-7123** | Ana (real) | **PORT** out of Google Voice → Twilio | `phone_call_ana` |
| 913-453-5505 · 913-270-0645 · +37 past-agent lines | Legacy / departed agents | **STAY in FUB → forward to one "Legacy Agents" Twilio line** | `phone_call_past_agent` |

**Why forward the legacy pool instead of porting it:** those numbers aren't assets worth owning — they just
need to not fall through. Forwarding is a same-day FUB config change (no port wait), and the numbers **stay in
FUB so FUB keeps logging them natively**. They forward to a single dedicated Twilio "Legacy Agents" number that
Myli answers (legacy intro) and that rings your cell.

<Warning>
**Two things this creates:**
1. **Ported numbers leave FUB.** Once 320-0906 / 354-8600 / 954-7123 are on Twilio, FUB no longer logs them —
   so each call is **mirrored back into FUB via the API** (`POST /v1/calls`, the `mirror_to_fub` step) to keep
   the FUB timeline intact.
2. **The legacy line can't tell which original number was dialed.** 40 numbers forwarding to one Twilio line all
   read as "someone called the legacy line" — so they resolve to ONE subtype (`phone_call_past_agent`). FUB
   still holds the exact number if per-number detail is ever needed. **Caller-ID pass-through** must be set on
   every forward, or you see the FUB inbox number instead of who actually called.
</Warning>

For the FUB ports: **request a port-out packet from Follow Up Boss support** (account# + PIN + LOA per number) →
Twilio port-in. Confirm each line in **FUB Admin → Phone Numbers** first; each port is a real **cutover**, not a
copy. The forwarding pool needs no port — it's a routing change in FUB.

## The rollout — pilot ONE, then batch

1. **Pilot: 913-954-7123 (Ana's).** Port it first, prove the full **Twilio → HouseLoop native inbound + Myli
   send-as-Ana** flow end-to-end. It's the highest-value line and its own worst gotcha (Google Voice) — nail it
   and everything else is easier.
2. **Then the owned business lines:** 913-320-0906 (Company/GMB) · 913-354-8600 (Sign/Listings).
3. **Forward the past-agent pool** (453-5505, 270-0645, +37) → the single "Legacy Agents" Twilio line. This is
   a same-day FUB routing change and does **not** wait on any port — do it FIRST to prove the loop.
4. **Re-point the SRG `913-347-xxxx` numbers** (Commission Grids, etc.) off GoHighLevel (`leadconnectorhq.com`) →
   Myli. We already own them.

## A2P 10DLC — already registered, and EVERY number joins it (LAW, Zach 2026-07-09)

US carriers require **A2P 10DLC brand + campaign registration** to send application SMS from a 10-digit number.
We have a **VERIFIED campaign** (brand STANDARD/APPROVED, service "Low Volume Mixed", `MG05ab6dd6…`) — no fresh
registration ever needed.

**THE LAW (locked 2026-07-09 after the 30034 incident): every number joins the campaign's sender pool THE MOMENT
it lands in the account — buy or port, BEFORE its first send.** One API call:
`POST messaging.twilio.com/v1/Services/MG05ab…/PhoneNumbers {PhoneNumberSid}`. Note carrier provisioning takes
hours after adding — a number can be in the pool and still bounce 30024 briefly; **verify one delivered test
message before declaring any send-path live.**

<Warning>
**Why this is law:** on 2026-07-08 the systems-health line sent 21 notification texts before it was on the
campaign — all silently blocked (30034), the compliance subscore cratered to 6/20, and the "fix" was never
delivery-verified (its own test bounced 30024 mid-provisioning). Audited + closed 2026-07-09: **every account
number now registered** (incl. the new (913) 800-4470 Notifications line, joined before its first send), and the
systems-health cockpit (Phase D) gains a probe — any outbound message with an error code = RED + text Zach.
</Warning>

## Cost (rounding error vs one Kyle Nations)

| Item | Cost |
|---|---|
| Google Voice port-out unlock | **$3 / number** |
| Twilio port-in | ~$1 / number (one-time) |
| Twilio number hosting | ~$1.15 / number / month |
| A2P 10DLC campaign | **already registered — $0** (just add each number to it) |
| Prepaid SIM (only if two-step GV port) | ~$10 / number |

## Who does what

- **Human (Ezra / Zach):** sign the LOAs, pay the GV $3 unlock, click through the Twilio port-in console, the
  FUB support port-out requests. Porting can't be done headless.
- **Lee / automation:** the Twilio side — subaccount setup, the **port-in via Twilio's Porting API**, the
  **receiver webhooks** (Twilio → HouseLoop), **adding each ported number to the existing A2P 10DLC campaign**, and wiring each ported line's
  `workflow_id` in `hl_phone_numbers` → the Myli phone workflow.

## The receiver architecture (post-port, per line)

```
Inbound call/text → the ported number (in OUR Twilio)
        → Twilio Messaging/Voice webhook → HouseLoop (native ingest, hl_lead_activity)
        → match/create lead → the ONE (FUB→now-Twilio · Phone Call) workflow
        → Myli reads the To line → source category → classify human → text-back AS the dialed line
```

---

# ═══ SECTION 3 — THE PORTING PRD (the spec this work executes against) ═══

<Info>
**Ownership + capture + notifications is the scope.** Owner: **Caleb** (capture) · **Lee** (notifications) · Zach
(telephony/porting). The voice agent that *answers* these lines — [Myli Voice](/guides/myli-voice) — is a separate,
**later** phase, **parked until this porting work is done**. It is NOT part of this spec.
</Info>

## Goal

**Own every business number in our own Twilio, and capture every call natively — records + notifications.** Kill
the three-source shit-show (FUB / Twilio / Google Voice) and collapse it to one pipe: **Twilio captures, FUB
mirrors.** (Once we own the lines, the [Myli Voice](/guides/myli-voice) agent becomes the front door — next phase.)

## The problem (why this exists)

- Phone-call notifications (Company / Sign / Past-Agent / Ana) are **documented but fire 0** — the trigger
  `phone_call_received` is wired to Twilio, but the numbers route through FUB + Google Voice, so nothing reaches it.
- FUB **call capture broke** at the webhook cutover (stale since ~2026-07-07) and **never captures cold/unknown
  callers** to the company line.
- **Ana's calls run through Google Voice** — invisible to both FUB and Twilio.
- No transcripts exist anywhere (FUB has none).

## The decisions (LOCKED 2026-07-09)

| # | Decision |
|---|---|
| D1 | **Port 3 assets → our Twilio:** Company `913-320-0906`, Sign/Listings `913-354-8600`, Ana `913-954-7123` (out of Google Voice). |
| D2 | **Forward the legacy pool** (`913-453-5505`, `913-270-0645`, +37 past-agent) → ONE dedicated "Legacy Agents" Twilio line. Stay in FUB; do NOT port. |
| D3 | **One notify key `phone_call_received`, 5 subtypes** resolved by dialed-number → `hl_phone_numbers.category` (company/sign/past_agent/ana + generic). Shared resolver used by BOTH the Twilio webhook and the FUB path. |
| D4 | **Ported numbers mirror back into FUB** via `POST /v1/calls` so the FUB timeline stays intact (they leave FUB on port-out). |
| D5 | **A2P 10DLC is already registered** under our Twilio account — the ported numbers get **added to the existing campaign**, no new brand/campaign registration needed. |
| D6 | **The line owner is the agent (Myli Voice), NOT part of this PRD.** Who *answers* the owned lines is the [Myli Voice](/guides/myli-voice) build — it rolls in after porting. This spec only gets the numbers owned + captured. |

## Requirements

<AccordionGroup>
  <Accordion title="R1 — Ownership & routing">
    - Port D1's three numbers into our Twilio (own them natively).
    - Forward D2's legacy pool → the single "Legacy Agents" Twilio line (same-day FUB config).
    - Caller-ID pass-through on every forward (see who actually called, not the FUB inbox number).
    - Add the ported numbers to the **existing A2P campaign** in Twilio.
  </Accordion>
  <Accordion title="R2 — Capture (Caleb)">
    - Store every call natively on Twilio inbound (metadata + recording + which line), **including cold/unknown
      callers** to the company line.
    - Shared subtype resolver `resolvePhoneCallSubtype(dialedNumber)` → `hl_phone_numbers.category` → subtype,
      called by both the Twilio webhook and the FUB path (`lib/phone/resolve-subtype.ts`).
    - `mirror_to_fub` — write each ported-number call back to FUB (`POST /v1/calls`) with attribution + recording.
    - Google Voice ingest for Ana until her number is ported (then retire GV).
  </Accordion>
  <Accordion title="R3 — Notifications (Lee)">
    - `phone_call_received` fires the correct 1-of-5 typed card by subtype.
    - Lead-less path for cold callers (no person match required).
    - `fub_created` recency gate so a one-off catch-up never notifies on old activity.
  </Accordion>
</AccordionGroup>

## Acceptance criteria

- ☐ Call any of the 4 lines → a lead appears in HouseLoop CRM with the correct source + recording.
- ☐ The matching typed notification (`phone_call_company/sign/past_agent/ana`) fires — no more 0s.
- ☐ A cold unknown caller to the company line is captured + notified (lead-less).
- ☐ A ported-number call also appears on the FUB timeline (mirror).
- ☐ Ana's calls captured (GV bridge now; native after port).

## Phases & owners

<Steps>
  <Step title="Phase 1 — Legacy line live (same-day, no porting)">
    Forward the legacy pool → the Twilio "Legacy Agents" line. Capture the call (`create_lead` + recording) +
    fire the notification. Proves the full capture loop without waiting on carriers. **Owner: Caleb** (capture),
    Zach (FUB forward config).
  </Step>
  <Step title="Phase 2 — Capture + notify unified (in parallel)">
    Shared subtype resolver · store cold/unmatched calls + recording · `phone_call_received` fires all 5 subtypes ·
    `mirror_to_fub`. **Owner: Caleb** (capture) + **Lee** (notify).
  </Step>
  <Step title="Phase 3 — Port the 3 assets (1–2 weeks, carrier-gated) — IN PROGRESS">
    Port Company + Sign + Ana into Twilio; add to the existing A2P campaign; retire Google Voice. **Owner: Zach**
    (port requests) + Caleb (webhook wiring on landing). **Live status in the build log below.**
  </Step>
</Steps>

## Open items / risks

- **FUB `POST /v1/calls`** — confirm the API supports logging a call back to a person (for D4/mirror). Caleb to verify.
- **Google Voice port `INVALID_ADDRESS` (RESOLVED, diagnosed 2026-07-10)** — the real cause was **not** a VoIP
  direct-reject; it was the service-address **2-business-day propagation delay** (change renders in Google instantly
  but reaches the carrier late). Fix = wait for propagation, then match the Google Voice service address exactly.
  Two-step-via-prepaid-SIM stays as the fallback only if a *non-address* reject shows up.
- **Legacy line can't distinguish which original number was dialed** — all resolve to `phone_call_past_agent` (FUB
  still holds the exact number if needed).

---

## Full FUB number inventory (as of 2026-06-22)

### Company
| Number | Label | Disposition |
|---|---|---|
| **913-320-0906** | Company number / Google My Business — **the FUB texting line** | **PORT → Twilio** |

### Company team inboxes
| Number | Label | Disposition |
|---|---|---|
| 913-270-0645 | Default Buyer's Agent # — OLD | drop (deprecated) |
| **913-354-8600** | LISTINGS — KEEP (the sign line) | **PORT → Twilio** |
| 913-453-5505 | "Ana old number 913-954-7123 for" (the forward target) | reconcile with the Ana GV port |

### Past Agents (each tagged; the reactivation lines)
| Agent | Number | Agent | Number |
|---|---|---|---|
| Laura Wonder | 913-423-9499 | Mylyn Haguy | 913-298-8660 |
| Jessica Coggins | 913-653-0624 | Debbie Hall | 913-423-3348 |
| Glen Purnell | 913-379-3059 | Stephanie Duston | 913-246-5062 |
| Dylan Hines | 913-423-6230 | Foram Patel | 913-794-3255 |
| Alex. AI Assistant | 775-522-8747 | Ryder Tarantino | 913-225-8960 |
| Abby Bachrodt | 913-361-5146 | Jenny Delich | 913-320-5495 |
| Amanda Heuertz | 913-956-0763 | Danielle Powers | 913-298-6115 |
| Briena Carlson | 913-382-2827 | Ken Lain | 913-728-4377 |
| Nathan Langseth | 913-565-2579 | Donna Mason | 913-355-6487 |
| DeJohn Nelson | 913-380-1653 | Megan Young | 913-701-3800 |
| Jeni Combs | 913-490-2797 | Carrie Markway | 913-372-3223 |
| Tyler Harris | 913-724-5793 | Raul Lene | 913-586-2668 |
| Melanie Clark | 913-286-6496 | Tommy Huggins | 913-795-7543 |
| Landon Nelson | 913-379-3174 | Tevin Jacutin | 913-358-6063 |
| Tony Salinas | 913-578-6228 | Andrew Sammis | 913-349-6986 |
| Benjamin Reddaway | 913-724-5443 | Alisha Hanks | 913-347-6406 |
| Bethany Rounds | 913-278-1213 | Dean Mauldin | 913-286-7163 |
| Franklin Lloyd | 913-724-5077 | Walter | 913-724-5810 |
| Brian Williams | 913-270-3458 | | |

### Team Members
| Member | Email | Number | Disposition |
|---|---|---|---|
| Zach Spradling | zach@spradlingrealtygroup.com | **913-490-2988** | **My Inbox** (Zach's personal FUB line) |
| Ana Spradling | ana@spradlingrealtygroup.com | 913-349-5536 | park, unused (Ana's active line is the GV 913-954-7123) |
| Francela Chavarria | tc@spradlingrealtygroup.com | 913-270-6213 | **Keep** — Transaction Coordinator; eventually → **tc@houseloop.ai** |
| Mylee | mylee@spradlingrealtygroup.com | 850-660-3173 | TBD |
| Aaron Foster | afoster@spradlingrealtygroup.com | 913-298-8760 | drop |
| Ben Froeschl | bfroeschl@spradlingrealtygroup.com | 913-379-3429 | **KEEP the seat** — Zillow Tech Connect is bound to his login email (deleting/renaming it risks the Zillow feed). Login **taken over** 2026-07-09; FUB profile phone → the Legacy Agents Twilio line **913-675-9877**; his old **913-379-3429** added to the **Legacy Agents** routing group. Full runbook: [Departed-Agent Zillow Binding](/fub/departed-agent-zillow-binding). |

<Tip>
A number provisioned *inside* FUB is owned by FUB — leaving FUB loses it unless you port it. Porting to our
Twilio (LOA + recent bill) makes it ours and lets us point Voice + SMS at Myli.
</Tip>

## 2026-07-09 — what shipped (the Phase-4 kickoff build)

<Update label="2026-07-09" description="Own-the-numbers Phase 4: page, routing, first port filed">
The night we started actually **owning** the phone system. All of this is live/filed:

**🖥️ Phone Number Management page** (`/settings/phone-numbers`, ported from the Zach-locked wireframe
`docs/wireframes/phone-management.html`) — ONE surface, four sections:
- **Number Ports** — the live port pipeline (`hl_number_ports`).
- **HouseLoop Numbers** — a mirror of our owned Twilio + BlueBubbles lines (buy on Phone Provider; edit
  label/type/group via ⋮). Includes an **A2P column** (live campaign membership, green YES / red NO).
- **Follow Up Boss Numbers** — grouped by FUB inbox, show-all expander, ⋮ to name & route a group.
- **Routing groups** — a group of numbers → who rings (real searchable team-member picker + Myli + any number)
  → what happens on no answer. Create / edit / **delete** (mig `0362`: `hl_routing_groups` + FK
  `hl_phone_numbers.routing_group_id`, ON DELETE SET NULL so deleting a group frees its numbers, never deletes them).

**📞 Owned-line native call routing** — a call to a Twilio number WE hold (`hl_phone_numbers.platform='twilio'`,
e.g. the new Legacy line) NEVER hands off to the FUB front-router: native forward per the line's `forwards_to` +
voicemail/recording/notification, with the dialed-line **subtype stamped** via the shared resolver
`lib/phone/resolve-subtype.ts` (dialed# → `hl_phone_numbers.category` → `phone_call_company/sign/past_agent/ana`).

**☎️ Legacy Agents line LIVE** — bought Twilio **(913) 675-9877**, wired to the CRM voice webhook, **verified with
a real call** (rings Zach, captured `phone_call_past_agent`). The 39 legacy past-agent FUB numbers forward here
(flip pending).

**🅰️ A2P 10DLC audited + locked** — every account number registered on the existing campaign; the 30034
"unregistered number" incident (July 8 systems-health line) closed; new law: every number joins the campaign the
moment it lands, verified by one delivered test. See the [A2P section](#a2p-10dlc-already-registered-and-every-number-joins-it-law-zach-2026-07-09).
Two visible **A2P columns** now make the trap impossible to miss: **HouseLoop Numbers** (green YES / red NO) and,
on the **Twilio** settings page, a per-number **A2P CAMPAIGN** column that shows the **date each line joined the
sender pool** — so a send BEFORE that date reads as unregistered at a glance (298-8778 only joined July 8 23:47,
which is exactly why its July 7–8 sends bounced).

**📴 Dedicated notification + health lines split (Zach 2026-07-09)** — HouseLoop notifications were shipping from
a line labeled "Zach AI Outbound" and getting **merged with systems-health**. Fixed: bought **(913) 800-4470
"HouseLoop Notifications"** (A2P-registered before first send) and pointed BOTH `notification_sender` and the
Senders registry at it; **(913) 298-8778** is now **Systems Health ONLY**; **(913) 286-1761** is back to Zach's
AI-outbound line alone; the 3 test lines were relabeled **"Available"** (Twilio friendly-name + registry). The
Settings nav item **"Phone Provider" was renamed "Twilio"** under Phone & Voice.

**☎️ Departed-agent seat takeover (Ben Froeschl)** — kept his FUB seat (Zillow Tech Connect binding), took over
the login without changing the login email, set his profile phone to the Legacy Agents Twilio line, and added his
old 913-379-3429 to the Legacy Agents group. Runbook: [Departed-Agent Zillow Binding](/fub/departed-agent-zillow-binding).

**⛳ First port — Ana's (913) 954-7123 (Google Voice → our Twilio) — IN PROGRESS, awaiting address propagation:**
Filed + LOA signed by Ana, but **rejected twice `INVALID_ADDRESS` (22154)** — first on the stale Kansas address, then
(`KW03a32aadd91…`, via the Porting API) on the *correct* Florida address. **Root cause = a timing trap, not a wrong
value:** the port validates the service address against the **losing carrier's (Bandwidth) record**, and Google's
own screen warns **"Service addresses register within 2 business days"** — Ana's Google Voice service address was
changed to `2097 Valencia Blossom St, Clermont, FL 34711` but hadn't propagated to Bandwidth when we submitted.
**Plan: wait for propagation, then re-POST the same values on ~2026-07-13.** Full runbook: memory
`reference_google_voice_port_out_playbook`. Records:
[Signed LOA (FL)](/images/port-records/ana-913-954-7123-signed-LOA-FL-2026-07-10.pdf) ·
[Google Voice invoice](/images/port-records/ana-913-954-7123-google-voice-invoice.pdf).
**Method note:** the Twilio beta port-in **UI silently drops the request under browser automation** — use the
**Porting API** (`POST numbers-upload.twilio.com/v1/Documents` for the bill → `POST numbers.twilio.com/v1/Porting/PortIn`
with `account_sid`); the LOA auto-emails via HelloSign. **Account number = the Google Billing ID without dashes**
(`962066658983`), **BTN in E.164**, **PIN from Admin → Google Voice → Number porting → "Port-out info"**, service
address = the **Google Voice Service address** (not the invoice bill-to).

**📨 FUB ports requested** — port-out packet for **(913) 320-0906** (Company) + **(913) 354-8600** (Sign) emailed to
FUB support; a daily watcher checks for the reply and surfaces it.
</Update>

## Still ahead (Phase 4 → 5)

<Info>
**Next:** flip the 39 legacy forwards → the Twilio Legacy line · file the two FUB ports when the packet lands ·
then **[Myli Voice](/guides/myli-voice)** (the agent answers every line) — parked until the ports settle.
</Info>
