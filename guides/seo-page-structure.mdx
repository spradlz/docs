---
title: "The one geo page shape"
description: "What every city, county, zip and neighborhood page renders, in order — and what was retired from it."
---

Every geo page on houseloop.ai renders from **one component stack**, not per-route JSX. This page is the
canonical description of that shape and of the September 2026 retirement of its previous generation.

## Where this fits among the SEO docs

Four pages cover SEO and they own different things. Start here for **what a geo page renders**; go
elsewhere for how it is built and served.

| Page | Owns |
|---|---|
| **This page** | The section order of a geo page, what each section is, what was retired |
| `legacy/guides/seo-pages-and-rendering` | Rendering strategy, the sitemap, the geo taxonomy, `lastmod`, how to create a new SEO page |
| `legacy/guides/programmatic-seo` | The Monday pipeline, coverage, schema.org, IDX compliance, the MCP server |
| `legacy/guides/seo-background-jobs` | The Inngest jobs behind the caches — `geo-neighborhood-stats`, the page builder |

## The stack

A geo route is a thin shell over the shared `SearchResultsPage`. The SEO block below the results is
`components/geo/LocationSeo.tsx`, which resolves the location once and composes:

1. **Breadcrumb** — `BreadcrumbList` JSON-LD alongside it.
2. **`GeoSeoContent`** — market trends · the welcome article · schools · the sixteen live-number FAQs
   (`FAQPage` JSON-LD, rendered expanded).
3. **`GeoPageBody`** — the overview prose · the counted facet bank · the listing-overview tiles.
4. **`WeeklyMovers`** — under contract and newly listed, when the geo has them.
5. **`GeoDirectory`** — the two market tables (below).

Sections render in **push order**. There is no re-sorting step.

## The two market tables

`GeoDirectory` renders labelled sections. Two survive, both as rich market tables reading
`hl_geo_market_stats`:

- **Property types in {Place}** — one row per home type that has homes for sale.
- **Neighborhood Market Statistics in {Place}** — one row per subdivision in the nightly cache.

### The 12-row cap

The neighbourhood table is capped at **12 rows** with a `Show all N ▾` pill
(`components/geo/MarketTableShelf.tsx`). Overland Park has 249 qualifying subdivisions.

The cap is a **real server-side slice**, not a CSS hide, and that distinction is the whole point:

| | before the cap | after |
|---|---|---|
| Anchors on the Overland Park page | 1,567 | **242** |
| From this one table | 1,225 (78%) | 60 |
| `?status=` parameter URLs | 735 | **36** |

Those `?status=closed\|pending\|comingSoon` variants self-canonicalise to the clean neighbourhood URL,
so they were never a duplicate-indexing problem — they were a **crawl-budget** problem. A canonical
stops indexing; it does not stop fetching.

<Note>
The home page's `ShowMoreGrid` shelf looks identical and does the **opposite**: it renders every item
and hides the overflow, because that tail's job is to put all 134 city links in the HTML for a crawler.
Do not copy one to the other. The rows past this cap still ship as inert JSON in the RSC payload (that
is how the pill expands without a round trip) — what the slice removes is the **anchors**.
</Note>

## Titles are built from the geo's own level

A section title is never concatenated onto a bare place name. `${place}` resolves to
`neighborhood ?? city ?? county ?? state`, which produced **"Property Types in Johnson"** on a county
page and **"Leawood Estates Neighborhood Homes"** on a neighbourhood page.

Titles now use `locationLabel()` and branch on level:

| Level | Neighbourhood table title |
|---|---|
| City | `Neighborhood Market Statistics in Overland Park, KS` |
| County | `Neighborhood Market Statistics in Johnson County, KS` |
| Zip | `Neighborhood Market Statistics in Overland Park, KS` — the **city**, because the rows are the city's subdivisions and the feed has no neighbourhood↔zip map |
| Neighbourhood | `Nearby Neighborhood Market Statistics` |

## What was retired

Six sections were rendering **below** the current shape rather than being replaced by it, so every geo
page shipped two generations at once — 15 `<h2>`s where the shape draws 9. All are gone:

- Find a Home You'll Love in {Place}
- Popular Searches in {Place}
- More to Explore in {Place}
- Explore Nearby Cities
- {Place} Homes by Zip Code
- Estimate Your Home Sale Proceeds

Their successors are the property-type, feature and nearby chip rows. `orderSections()` and the
`SECTION_ORDER` array went with them.

## Verifying a change

```bash
curl -s http://localhost:3001/kansas/johnson-county/overland-park \
  | grep -o '<h2[^>]*>' | wc -l          # 9 on a city page
npx tsc --noEmit -p apps/houseloop-web    # per-route builds hide type errors behind a 200
```

A 200 on the home page does not prove the tree type-checks — Next compiles per route.

## What the SEO Revamp shipped on 2026-09-08 (Phases 0, 2 and 3)

**Phase 0 — the entity.** Every page renders exactly one `Organization` JSON-LD node for HouseLoop (name, url,
logo, telephone, email, areaServed) with `sameAs` pointing at the Zillow and Google Business profiles and nothing
else; the blog and listing pages reference it by `@id` instead of inlining stubs. The cage arm `ORG_JSONLD`
refuses a page without it. The brand-term baseline was recorded on the day (0 impressions for "houseloop" or
"spradling" against 14,477 total).

**Phase 2 — the words.** The welcome-article slot prefers a real article over the weekly market blurb (the
precedence bug at the top of `GeoSeoContent`). The article comes from the Inngest function
`geo-welcome-article-generate` (Haiku, fed the census rows in `hl_geo_public_stat` + MLS figures, the ACS period
cited in prose) and is stored under `welcomeArticle` in `hl_geo_page.payload`; the weekly per-tier page jobs now
preserve that key on their rewrite. The four-tools row renders all four tools on city, county and zip pages.

**Phase 3 — the long tail.** Five cage arms landed first (`GATE_HOLDS`, `FACET_IS_A_SLUG`, `FACET_LINK_FILTERS`,
`NO_ORPHAN`, `PARAM_URL_BUDGET`). A facet is a real slug page (`/kansas/johnson-county/overland-park/wood-floors`),
filtered by the JSONB containment predicate on the feature module, gated at 20 homes for city and county (a thin
facet renders `noindex` with a canonical to its place page and mints no anchor), listed in the sitemap chunk that
carries geo pages, and reachable from the module's chips in the facet bank. `FOOTER_EVERY_PAGE` refuses a page
without the one site footer (the blog was missing it).

## The individual listing page — the lower order, to its lock (shipped 2026-09-09)

The listing page is the one genuine second shape of the site, and its lower half is built from the pinned lock
`docs/wireframes/listing-page-LOCKED-2026-09-09.html` (steps 3 and 4 of the SEO Revamp board, verbatim). Zach
walked production at 05:50 on 2026-09-09 and found three drifts from that drawing; all three are closed.

**The order, both faces.** … price history → tax → payment estimate → schools → **"Similar listings and the
{City} market"** → **"Look throughout {City}, {State}"** directly under it, inside the same box → **"Frequently
asked questions about {address}"** → the geo tail (popular searches → nearby cities → zips → counties) → the site
footer. The phone sheet renders the same order (rulings #257.319d/e); the earlier desktop order from the 9/08
Part 2 frame (Similar → FAQ → Look throughout) is superseded by the lock.

**Similar listings** (`components/listing/SimilarListings.tsx`) is now the one section: a two-tone heading
(`SectionHeading`, kicker "Similar listings" over the phrase "and the {City} market") above the bordered shelf box
(`SeoShelfBox`, the same `rounded-[14px] border-[1.5px] border-hl-brand` box the home and geo tails wear). Inside
the box: the three `ListingCard`s as a plain sideways carousel (one full-width card per swipe on the phone, three
across on desktop) — no "See all in this area" tile and no chevrons, because the lock draws neither and Zach's
words were "there is no see more, it's all in-page scroll" — then a hairline and the "Look throughout" block.

**Look throughout** (`components/listing/CityParagraph.tsx`) renders the lock's two paragraphs of real comparison,
every place a link: the city's for-sale count, under-contract count, median sale price and median days to offer,
then "This home is priced $X below/above that median"; then the county (its zip count and for-sale count) and the
biggest-inventory sibling city above and below this city's median. Every number is the nightly
`hl_geo_market_stats` row the city page itself renders, so the count here equals the count its link lands on.
ez's compare-and-search sentence and save-a-search nudge are gone as filler, per the lock's own note.

**FAQ** (`components/listing/ListingFaq.tsx`) wears the same two-tone heading and, on desktop, the same box; the
lock's phone frame draws the rows bare, so the box is `md:` only.

**"Already own a home?"** (the right rail on desktop, the sheet on the phone) was a navy-filled card; navy is the
page-top strip and nothing else. It is now the two-tone heading (kicker "Already own a home?" over "What's your
home worth right now?") above the shelf box, dark text, the /sell address field with its brand-blue Go button. The
component is still `SellerAddressInput` — the one Seller Report door.

**Verify:** `node tools/stranger-walk/home-render-census.mjs` (the cards are `ListingCard`, never a lookalike) and
the receipt `.claude/reports/grinder/seo-revamp/RECEIPT-listing-lock-2026-09-09.md` (DOM order before/after at
1440×900 and iPhone 14, the lock overlays, the navy census with verdicts).
