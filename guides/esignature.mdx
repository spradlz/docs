---
title: "E-Signature (HouseLoop eSignature)"
description: "HouseLoop's own white-labeled e-signature engine for Transaction Coordination — fill a contract from a lead, send for multi-party signature as HouseLoop eSignature, and archive the executed PDF to our own R2. Documenso is the SYSTEM OF RECORD; dotloop gets only a backup mirror. Self-hosted, $0/month."
---

HouseLoop eSignature is the e-signature layer of [Transaction Coordination](/guides/transaction-coordination). It takes a
real-estate contract (a flat PDF — KCRAR / Heartland MLS / eXp association forms), **auto-detects every fillable field**,
**fills it from the lead**, **sends it out for signature under the HouseLoop brand**, pulls the **executed PDF** back, and
**archives it to OUR OWN R2 on the loop document** (permanent URL — KS/MO retention). A **backup mirror** can push to [dotloop](/guides/dotloop-integration) for eXp/SkySlope compliance — dotloop is NOT the system of record (Zach locked Documenso-first 2026-07-06).

It is **self-hosted and open-source** — no per-document fees, no third-party "powered by" branding, no subscription.

---

## The pipeline

```
Contract PDF
  → 1. Detect fields      (CommonForms — free, self-hosted vision model)
  → 2. Fill from lead     (pdf-lib — name, price, term, address, etc.)
  → 3. Send for signature (HouseLoop eSignature — self-hosted Documenso)
  → 4. Signer signs        (browser / mobile, branded HouseLoop)
  → 5. Pull executed PDF   (signed doc + audit trail)
  → 6. Archive to our R2 (permanent) · optional backup mirror → dotloop (SkySlope / eXp compliance)
```

Every layer is **$0/month**: the only variable cost is a few cents of LLM tokens, one time per *new* form, to label the
detected fields.

| Layer | Technology | Cost |
|---|---|---|
| Field detection | CommonForms / FFDNet (self-hosted, Apache-2.0) | $0 |
| Field fill | pdf-lib (embedded font) | $0 |
| E-signature | HouseLoop eSignature (Documenso, self-hosted, AGPL-3.0) | $0 |
| Filing | dotloop API | $0 (existing) |

---

## What we built (proven)

- **Full fill → sign → file pipeline, verified end-to-end live.** A filled Exclusive Buyer Agency was sent for signature,
  signed in the browser, the executed PDF pulled back, and uploaded into a dotloop loop. The plumbing works.
- **Free automatic field detection, validated on our own forms.** CommonForms auto-detected **46 fields across all 6 pages**
  of the Exclusive Buyer Agency — every text blank, checkbox, signature line, date line, **and the per-page initials boxes** —
  placed accurately on the lines, with **no human nudging**.
- **HouseLoop eSignature — white-labeled.** Documenso cloned and fully rebranded to **HouseLoop eSignature**: HouseLoop logos,
  brand navy (`#1B3564`), product name throughout, transactional emails from `noreply@houseloop.ai`. Runs locally with a
  verified branded sign-in page.

## Why these choices

- **No per-deal human.** The industry hand-tags each association form once into a maintained library; we replace that with an
  open model that auto-detects the fields, so a brand-new form is set up once (a glance) and every deal after is fully
  automatic.
- **No subscription.** Hosted e-sign vendors charge a monthly platform fee and/or per-signature fees and stamp their own
  brand on the signing experience. Self-hosting open-source gives us our own brand, unlimited volume, and $0/month.

---

## 🚧 Work In Progress — updated 2026-07-06

Owners: Benji (CRM integration) · Lee (infra). Current build status:

- **DONE** — Field detection (CommonForms) validated on our forms; fill engine (pdf-lib); dotloop filing (read + upload);
  full pipeline proven end-to-end; HouseLoop eSignature (Documenso) cloned, rebranded, and running locally with the official
  HouseLoop brand assets.
- **DONE (2026-07-06)** — **`esign.houseloop.ai` is LIVE on Railway** with a real Let's Encrypt cert, served directly
  from Railway's edge (no proxies). `NEXT_PUBLIC_WEBAPP_URL`/`NEXTAUTH_URL` point at the branded domain, so signing
  links carry `esign.houseloop.ai`. The domain was wedged for 13 days: Railway requires a
  **`TXT _railway-verify.<domain>` record** (value = the `domains` query's `status.verificationToken`) before its edge
  will bind a custom domain, and **nothing in the API/status surface says so** — status just sits in
  `VALIDATING_OWNERSHIP` and the per-domain target 404s (`x-railway-fallback: true`). A Railway moderator confirmed on
  our support thread (resolved:
  [station.railway.com/questions/custom-domains-never-bind-at-edge-404-a-6b4b3cea](https://station.railway.com/questions/custom-domains-never-bind-at-edge-404-a-6b4b3cea)).
  If a cert stays stuck after that, the explicit `customDomainIssueCertificate(id)` mutation forces issuance in seconds.
  Full forensics + playbook: `.claude/reports/esign-cert-root-cause-2026-07-06.md`. (An interim Cloudflare Worker
  front-door carried the domain for ~1 hour during the outage; it was removed the same day once the real binding landed.)
- **IN PROGRESS** — Benji: the CRM integration (`lib/esign/` Documenso client + send route + webhook; agnostic sender
  via `hl_esign_sender`; e-sign nudge engine keyed to the showing appointment) — the buyer-agency / one-time-showing
  agreements as first-class TC documents.
- **NEXT** — One-click **"Send for signature"** on a lead (detect → fill → send → file), and a
  **Transaction Coordination → Forms** admin to drop in a new MLS PDF, auto-detect its fields, and save it as a reusable
  template (re-run yearly when associations revise a form).

> An earlier proof-of-concept used a third-party hosted e-sign service to validate the pipeline. That service is being
> **retired** in favor of HouseLoop eSignature (self-hosted, white-labeled, $0/month).
