---
title: "Floor 9 — Five layers, one dictionary"
description: "The tidy-up under the cascade design: the duplicate stage field dropped, the change ledger reconciled against real files, and a relationship taught to END instead of being erased."
---

**[← the punch list](/master-plan/punch-list)** · **[the 12-week schedule](/master-plan/schedule)** · [week 2 — The Cascade](/master-plan/week-2) · ✅ DONE — **5/5** punch-list items done

## What this floor is

> The tidy-up under the cascade design: the duplicate stage field dropped, the change ledger reconciled against real files, and a relationship taught to END instead of being erased.

## The record behind it

- **The floor nine receipt** — `.claude/reports/builder-floor9/RECEIPT.md`

## The punch list — what completes this floor

### One home per fact — 5/5

- [x] Drop the duplicate stage field that had been shadowing the real one — A lead’s stage had two homes and only one of them was the truth. The twin is gone, re-checked against the live record rather than the code, and a test now fails if it ever grows back. <sub>*Receipt: .claude/reports/builder-floor9/RECEIPT.md; closed 8/23 on live re-verification. Checked-at mined from that receipt’s commit.*</sub>
- [x] Reconcile the change ledger down from 24 unexplained entries to 6 signed ones — Twenty-four entries in the record of database changes pointed at nothing. Eighteen were duplicates and renumbering debris, cleared backup-first in one go; four turned out to be real changes nobody had filed and were recovered; the six that remain each carry a written reason. A new gate asks the question the old check could not - for every entry, does the file exist. <sub>*Receipt: .claude/reports/builder-floor9/RECEIPT.md; the signed survivors live in tools/vocab/migration-ledger-orphans.json. Checked-at mined from that file’s commit.*</sub>
- [x] Let a relationship END instead of being erased — Disconnecting two people used to delete the fact that they were ever connected. It now ends the stint and keeps the history, reconnecting opens a fresh one instead of colliding with its own tombstone, and all fifteen places that read connections go through one door so the rule cannot be forgotten at any of them. <sub>*Receipt: .claude/reports/builder-floor9/RECEIPT.md; the lifecycle migration commit is the mined time.*</sub>
- [x] Merge 228 houses that were living under two identities each — Two hundred and twenty-eight homes each had two records pretending to be different houses. Merged into one apiece and verified on the live site, so a home cannot show up twice or split its own history between two pages. <sub>*Lane receipt: .claude/reports/builder-hlidmerge/RECEIPT.md. Checked-at mined from that receipt's commit.*</sub> `TUE 8/25`
- [x] Write the Structure Gate law and wire the four checks that enforce it — A written rule about where a new thing is allowed to live, plus four mechanical checks that hold the door. It is not decoration: it stopped a push five separate times the day it went in, and each one was a genuine catch. <sub>*Lane receipt: .claude/reports/builder-structuregate/RECEIPT.md; the law at .claude/rules/STRUCTURE_GATE.md. Checked-at mined from that receipt's commit.*</sub> `TUE 8/25`

---
*Generated 2026-08-28 by `tools/master-plan/build-floors.mjs` — edit `tools/master-plan/floors.json`, never this page. Tick a box by setting that item's `"done": true`.*
