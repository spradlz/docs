---
title: "Floor 42 — Myli's State Stack — the brain that knows where everyone is"
description: "Your ruling, 8/23: *'the playbook is just a consequence of the state Myli's in.'* When someone pivots mid-conversation — asks about financing in the middle of scheduling a showing "
---

**[← the punch list](/master-plan/punch-list)** · **[the 12-week schedule](/master-plan/schedule)** · [week 2 — The Cascade](/master-plan/week-2) · 🟡 WORKING — **77/383** punch-list items done

## What this floor is

> Your ruling, 8/23: *"the playbook is just a consequence of the state Myli's in."* When someone pivots mid-conversation — asks about financing in the middle of scheduling a showing — Myli changes state, works the new goal, and pivots back knowing where she left off. And when a playbook finishes she sets herself a real task in the CRM, visible and assigned to her, and that task is what wakes her up later.

This is the piece that makes her different from a chat model, and you named the difference yourself: *"the only thing with chat models: they're NOT PROACTIVE — they sit in a dead state, they're not time-aware. MYLI IS ABSOLUTELY TIME-AWARE. Myli's moving towards goals. Myli knows tasks."*

The architecture is designed and **nothing is built** — no code, no migration, no data, no send. It is deliberately a stack of small bricks, and the order matters: the cheap zero-DDL path is built first so the expensive one is never built speculatively, and the one task door is built before anything mints a task through it — because minting Myli's first task through a sixth raw INSERT would be exactly the twin this design exists to refuse.

One thing on this floor genuinely cannot move without you, and it is first in the list: the financing line.

## The record behind it

- [THE MYLI OS — one document: states, directors, playbooks, workflows, AI tools](/guides/myli-os)
- **The state architecture (the brick design)** — `.claude/reports/myli-state/STATE_STACK_ARCHITECTURE.md`
- **Zach's state dictation, 2026-08-23** — `.claude/reports/myli-state/ZACH_STATE_DICTATION_2026-08-23.md`
- [Myli State Master — the goal side](/guides/myli-os)
- **Zach's rulings, dated and near-verbatim — including the financing line** — `.claude/reports/myli-state/ZACH_STATE_DICTATION_2026-08-23.md` — the §THE FINANCING RULING entry is the receipt

## The punch list — what completes this floor

- [x] Reallocate an existing Twilio test line for scenario 3 (#93.1): inventory the Twilio estate, pick an idle line, recategorize hl_phone_numbers.category='test' through the allocator census — no purchase — Take one of our spare Twilio numbers and make it the third test line. <sub>*GRINDER-LANDED r4 — receipt: .claude/reports/grinder/qa-bench/1788326729-receipt-r4.md · verdict: .claude/reports/grinder/qa-bench/verdict-r4-1788326729894.md*</sub> `MON 8/31`
- [x] #91 REAL CHANNELS ON THE BAND: checkable email inbox per persona · test-Anna dotloop signing (never Chris on the showing agreement) · test-Trish on our Twilio · Myli 1-on-1 Twilio text coordination (NO iMessage group — Twilio only, transport ruling) — Test people with real email, real texting, and a contract that can actually be signed. <sub>*GRINDER-LANDED r1 — receipt: .claude/reports/grinder/qa-bench/1788328130-receipt.md · verdict: .claude/reports/grinder/qa-bench/verdict-r1-1788328133695.md*</sub> `TUE 9/1`
- [x] THE CAST: give every test persona a real phone/inbox/dashboard — Kirsten has NO phone, so Myli cannot text her (#89.2) — Test people Myli can actually text and email. <sub>*DONE 2026-09-02 06:05Z at the desk — satisfied by bench items 1+2 for the closed-loop cast (conner, jasmeet, kirsten, jordan, gauntlet-walker + the reserved newly-registered identity): a real exclusive Twilio line each (item 1, verdict-r4-1788326729894.md GREEN), a readable houseloop.ai mailbox each (item 2, verdict-r1-1788328133695.md GREEN), and a website account each (registry webUserId, census-measured). The item text ("Kirsten has NO phone") predates item 1. Out-of-cast personas (rashaund/nadia/dana/walkrig) print NO MAILBOX by design. THE BENCH (ruling #218, Zach 9/01 ~19:15: "mint all three… that's the only way we're going to be able to test this thing for real") — carved to card qa-bench; Added 2026-08-30 via the task door.*</sub> `MON 8/31`
- [x] #91 THE PERSONA PIPELINE: new-lead persona born through the REAL general-registration road + the 10-day activation engine (calls/texts/emails) mimicking real dashboard behavior — A fake-but-real lead that registers like a person and acts like one for ten days. <sub>*DESK-LANDED 2026-09-02 (Lee, from the bench lane's receipt, re-verified: three signin --prod --verify green; live is_test = the four; 20 retired backed up): Taylor Newreg-TEST born through the real registration road + reset-to-unborn (pixels purged) · Morgan Activebuyer-TEST · Riley Pastclient-TEST pinned to HLID100795 via the claim road · the anonymous contract. RULING #228/#228.1 (Zach 9/02 10:45): the scenario leads are POLLUTED (Conner TEST: 126 msgs, 1,936 activity rows, a $2.4M/UMB pre-approval extracted from replayed scenario text six weeks before he existed). After the two layouts lock, THIS item rebuilds the cast: real test leads seeded through the real roads (the newly-registered persona born through registration every run) AND real SHADOW leads (a real lead's history cloned onto an is_test record, contact seats swapped to the bench lines/inboxes, name suffixed -TEST); the polluted scenario leads retire in the same pass, backup-first through the trash road. One coherent story per test lead; scenario replays never land on a standing persona again. THE BENCH (ruling #218, Zach 9/01 ~19:15: "mint all three… that's the only way we're going to be able to test this thing for real") — carved to card qa-bench; Added 2026-08-31 via the task door.*</sub> `MON 8/31`
- [x] The Playbook Shelf becomes the real page: gallery + canvas (small prompt, example bank, tool-grant cards with Academy certification, caps/gate), replacing the condemned playbooks surface (#256.29/.30 — Landing Plan move 3; lane playbook-shelf in build) — The playbooks screen becomes a clean gallery like ChatGPT: one card per playbook, and clicking one opens its real prompt, its real tools with Myli's certification on each, and its real examples. <sub>*Added 2026-09-03 via the task door. TICKED 2026-09-09 via the task entrance (auto:grinder) — receipt: .claude/reports/grinder/qa-bench/verdict-r2-1788928360544.md. GRINDER-LANDED r2 — receipt: .claude/reports/grinder/qa-bench/1788928300-receipt-r2.md*</sub> `THU 9/3`
- [x] Extraction P1 — the room fact extractor returns JSON reliably; Myli's memory writer stops failing open into silence (#256.33 move 3; lane extract-fix running) — When teammates talk in a thread, Myli reliably writes down the facts she hears instead of silently dropping them when her reader trips. <sub>*Added 2026-09-03 via the task door. TICKED 2026-09-09 via the task entrance (auto:grinder) — receipt: .claude/reports/grinder/qa-bench/verdict-r2-1788987163854.md. GRINDER-LANDED r2 — receipt: .claude/reports/grinder/qa-bench/1788983860-receipt-r2.md*</sub> `THU 9/3`
- [x] THE ONE MYLI SPINE (#256.34): one conversation store with titled conversations, the one agent loop as the only speaker everywhere, one memory through the one extractor, new-chat = zero leakage — THE ONE MYLI SPINE (#256.34): one conversation store with titled conversations, the one agent loop as the only speaker everywhere, one memory through the one extractor, new-chat = zero leakage <sub>*Added 2026-09-03 via the task door. TICKED 2026-09-10 via the task entrance (Lee) — receipt: .claude/reports/grinder/qa-bench/1789066682-receipt-r12.md. r11 RED (narrow) + r12: S-51 built — evidenceWindowFor() extracted pure into on-record.ts, section (f) drives the real function (63/0, red-proved by mutating factsWereRead); S-50 (the confirmed-output fact path the desk's S-43 ruling named — grep 0) DESK-CARDED as its own item; ticked on the desk's line per the two-round cap, the r11 verdict is the receipt (verdict-r11-1789063724709.md)*</sub> `THU 9/10`
- [x] THE CAPABILITY REGISTRY (#256.35): every capability = tool face + pill face + in-chat flow + availability conditions + persona + Academy rung; pill-actions/tool-registry/Toolbox-toggles/playbook-grants become views over it — THE CAPABILITY REGISTRY (#256.35): every capability = tool face + pill face + in-chat flow + availability conditions + persona + Academy rung; pill-actions/tool-registry/Toolbox-toggles/playbook-grants become views over it <sub>*Added 2026-09-03 via the task door. TICKED 2026-09-10 via the task entrance (auto:grinder) — receipt: .claude/reports/grinder/qa-bench/verdict-r2-1789075841883.md. GRINDER-LANDED r2 — receipt: .claude/reports/grinder/qa-bench/1789075739-receipt-r2.md*</sub> `THU 9/10`
- [ ] The dashboard chat mounts the ONE MYLI spine (thin mount: context assembler + shared chat UI; the scripted welcome/picks mouths die) — The dashboard chat mounts the ONE MYLI spine (thin mount: context assembler + shared chat UI; the scripted welcome/picks mouths die) <sub>*Added 2026-09-03 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471a: absorbed by THE MYLI SSOT FOLD v2 — F3 (the one context assembler; the owner dashboard as a webchat entry); its content is carried in the plan and ticks with that slice's YAML receipt.*</sub>
- [x] The home-value report chat mounts the ONE MYLI spine (value/comps context; owner and buyer goal-tracks) — The home-value report chat mounts the ONE MYLI spine (value/comps context; owner and buyer goal-tracks) <sub>*Added 2026-09-03 via the task door. TICKED 2026-09-04 via the task entrance (desk) — receipt: .claude/reports/client-dashboard/owner-mouth/RECEIPT.md. The owner report's mouth (#71): AskMyli mounted with room=owner on /my-houseloop's owner fork, running the ONE contract (contractSessionFor -> runAgentVoiceTurn -> bindActions) in the contract's own dashboard room through an owner LENS. Value/comps context is grounded through pageContext; both goal-tracks are live (she used the owner's equity and the buyer's saved search in one turn). Walked mobile on Riley by email under a bench PROOF hold; every number probed exact; turns filed as surface=owner. Four defects found and ratcheted.*</sub> `FRI 9/4`
- [ ] THE PLAYBOOK OBJECT (#256.26/.29): trigger + goal + a very small prompt + multi-shot examples + tool grants + caps/gate, authored on the shelf canvas through the one prompt writer, versioned; one CATEGORY per row — conversation (a closed set of six: Welcome · Matches · Money · Home value · Tour · Return) · lifecycle · event (#257.287 §1, #257.288); the examples are the ChatGPT goldens (hl_playbook_examples) — THE PLAYBOOK OBJECT (#256.26/.29): trigger + goal + a very small prompt + multi-shot examples + tool grants + caps/gate, authored on the shelf canvas through the one prompt writer, versioned; one CATEGORY per row — conversation (a closed set of six: Welcome · Matches · Money · Home value · Tour · Return) · lifecycle · event (#257.287 §1, #257.288); the examples are the ChatGPT goldens (hl_playbook_examples) <sub>*Added 2026-09-03 via the task door. REWORDED 2026-09-07 (board curation, lane-myli-os-board (Fable curator, under Lee's desk)): was "THE PLAYBOOK OBJECT (#256.26/.29): trigger + goal + a very small prompt + multi-shot examples + tool grants + caps/gate,…" — legacy vocabulary replaced under #257.286–.288/#257.277/#257.285.*</sub>
- [ ] THE MVP PLAYBOOK ROSTER AUTHORED (#256.27): new lead · tour request · reschedule · cancel · post-showing · showing homes · HOT/WARM/COLD nurture — LIFECYCLE and EVENT category playbooks (#257.288: conversation is a closed set of six, lifecycle and event are open-ended); each a row on hl_playbooks with its own mission, examples, tool grants and exits (#257.287 §1) — THE MVP PLAYBOOK ROSTER AUTHORED (#256.27): new lead · tour request · reschedule · cancel · post-showing · showing homes · HOT/WARM/COLD nurture — LIFECYCLE and EVENT category playbooks (#257.288: conversation is a closed set of six, lifecycle and event are open-ended); each a row on hl_playbooks with its own mission, examples, tool grants and exits (#257.287 §1) <sub>*Added 2026-09-03 via the task door. REWORDED 2026-09-07 (board curation, lane-myli-os-board (Fable curator, under Lee's desk)): was "THE MVP PLAYBOOK ROSTER AUTHORED (#256.27): new lead · tour request · reschedule · cancel · post-showing · showing homes…" — legacy vocabulary replaced under #257.286–.288/#257.277/#257.285.*</sub>
- [ ] THE MULTI-STATE PROMPT (Landing move 5): dossier → classify lifecycle → read signals → load the playbook → compose from full history; review queue, cockpit and live sends run the ONE prompt — THE MULTI-STATE PROMPT (Landing move 5): dossier → classify lifecycle → read signals → load the playbook → compose from full history; review queue, cockpit and live sends run the ONE prompt <sub>*Added 2026-09-03 via the task door. · SUPERSEDED 2026-09-07 by #257.286/.287 THE ONE PROGRAM (board curation 2026-09-07, lane-myli-os-board (Fable curator, under Lee's desk)).*</sub>
- [ ] The any-database read tools behind the playbooks — ALL TOOLS, ALWAYS, IN-PROCESS (#257.277 move 3: the playbook selects the exemplar and the rung, never the tool set; Myli can be asked about every database) — The any-database read tools behind the playbooks — ALL TOOLS, ALWAYS, IN-PROCESS (#257.277 move 3: the playbook selects the exemplar and the rung, never the tool set; Myli can be asked about every database) <sub>*Added 2026-09-03 via the task door. REWORDED 2026-09-07 (board curation, lane-myli-os-board (Fable curator, under Lee's desk)): was "The any-database read tools behind the multi-state prompt (Myli can be asked about every database)…" — legacy vocabulary replaced under #257.286–.288/#257.277/#257.285.*</sub>
- [ ] THE ROUNDS ENGINE (#256.27/.28): per-lead pulse → the look → three-tier interrupts with SLAs → anti-annoying armor → the Look Ledger; draft-first on the shelf's playbooks, one Inngest sweep — THE ROUNDS ENGINE (#256.27/.28): per-lead pulse → the look → three-tier interrupts with SLAs → anti-annoying armor → the Look Ledger; draft-first on the shelf's playbooks, one Inngest sweep <sub>*Added 2026-09-03 via the task door.*</sub>
- [ ] INTENT beside STAGE (#256.44 item 2, #256.45a): low/medium/high/very-high on the lead scorecard, moved by the intent engine over catalog events, never moving stage — STRUCTURE_GATE interview for the column — INTENT beside STAGE (#256.44 item 2, #256.45a): low/medium/high/very-high on the lead scorecard, moved by the intent engine over catalog events, never moving stage — STRUCTURE_GATE interview for the column <sub>*Added 2026-09-03 via the task door.*</sub>
- [ ] THE FOLLOW-UP POLICY TABLE (#256.44 item 3): per-stage cadence · channels · max proactive/day · signal-override-allowed, code-enforced; the playbook caps/gate read it; the model never decides permission — THE FOLLOW-UP POLICY TABLE (#256.44 item 3): per-stage cadence · channels · max proactive/day · signal-override-allowed, code-enforced; the playbook caps/gate read it; the model never decides permission <sub>*Added 2026-09-03 via the task door.*</sub>
- [ ] MULTI-SOURCE MYLI (#256.47): a web-search/browse tool in the capability registry beside the MLS tools — portals, builder sites, community data; MLS is the truth for listing facts; every claim source-cited; answer shape = ranked picks + reason + caveat + the agent's next move — MULTI-SOURCE MYLI (#256.47): a web-search/browse tool in the capability registry beside the MLS tools — portals, builder sites, community data; MLS is the truth for listing facts; every claim source-cited; answer shape = ranked picks + reason + caveat + the agent's next move <sub>*Added 2026-09-03 via the task door.*</sub>
- [ ] THE MYLI PROMPT CONTRACT (#256.50): the one small prompt (identity · never a dead end · the goal ladder) + per-turn context (starting pill · page · buyer profile · rung) + registry tools (IDX truth · web_search for the blanks · account/tour/save/lender) → words + TYPED BLOCKS (#257.277: the block contract replaces suggested_actions; text · bar · chips[registry] · slider · homes · map · table · cta) — replaces the website pill engine's state machine (#256.50a); no word budget (#257.285: her length is the golden's); the modal cuts over to it — THE MYLI PROMPT CONTRACT (#256.50): the one small prompt (identity · never a dead end · the goal ladder) + per-turn context (starting pill · page · buyer profile · rung) + registry tools (IDX truth · web_search for the blanks · account/tour/save/lender) → words + TYPED BLOCKS (#257.277: the block contract replaces suggested_actions; text · bar · chips[registry] · slider · homes · map · table · cta) — replaces the website pill engine's state machine (#256.50a); no word budget (#257.285: her length is the golden's); the modal cuts over to it <sub>*Added 2026-09-03 via the task door. REWORDED 2026-09-07 (board curation, lane-myli-os-board (Fable curator, under Lee's desk)): was "THE MYLI PROMPT CONTRACT (#256.50): the one small prompt (identity · never a dead end · the goal ladder) + per-turn cont…" — legacy vocabulary replaced under #257.286–.288/#257.277/#257.285.*</sub>
- [ ] THE GOAL LADDER TIERS (#256.50a) as persona-compass DATA: anonymous / returning / seasoned × account · profile complete · saved search · must-haves · affordability · lender intro · tour — read by the prompt per turn, scored by the judge as the goal line; the Myli Book §7(g) table is the starting data — THE GOAL LADDER TIERS (#256.50a) as persona-compass DATA: anonymous / returning / seasoned × account · profile complete · saved search · must-haves · affordability · lender intro · tour — read by the prompt per turn, scored by the judge as the goal line; the Myli Book §7(g) table is the starting data <sub>*Added 2026-09-03 via the task door.*</sub>
- [ ] RETIRE the hard-coded offer-after / per-state pill rules (#256.49): the R-rules that force a specific pill per state and the per-state pill lists leave composer code and become JUDGE assertions — 'did she offer a next step and was it executable'; #256.20's structure laws (tour on home-shown · one question · the out) survive as assertions only — RETIRE the hard-coded offer-after / per-state pill rules (#256.49): the R-rules that force a specific pill per state and the per-state pill lists leave composer code and become JUDGE assertions — 'did she offer a next step and was it executable'; #256.20's structure laws (tour on home-shown · one question · the out) survive as assertions only <sub>*Added 2026-09-03 via the task door.*</sub>
- [ ] Brain = Sonnet + web_search + IDX (#256.48): claude-sonnet-4-6 + Anthropic web_search (Zillow/Redfin/Realtor/Trulia blocked) + the IDX tools for every conversational turn, cheap classifier for deterministic turns, the deep tier QA/judge/desk only; per-ROOM length budgets in the charter (#256.48a: modal short · dashboard long-form · SMS shortest), judged as a rubric line — Brain = Sonnet + web_search + IDX (#256.48): claude-sonnet-4-6 + Anthropic web_search (Zillow/Redfin/Realtor/Trulia blocked) + the IDX tools for every conversational turn, cheap classifier for deterministic turns, the deep tier QA/judge/desk only; per-ROOM length budgets in the charter (#256.48a: modal short · dashboard long-form · SMS shortest), judged as a rubric line <sub>*Added 2026-09-03 via the task door.*</sub>
- [ ] Extraction refusals are stored as FACTS: 30 of 1,034 hl_lead_facts rows read 'Not stated. No indication…' and render on cards as if known (lane-b, 9/03) — the extractor must write nothing for a refusal; sweep + retire the 30 through the one writer — Extraction refusals are stored as FACTS: 30 of 1,034 hl_lead_facts rows read 'Not stated. No indication…' and render on cards as if known (lane-b, 9/03) — the extractor must write nothing for a refusal; sweep + retire the 30 through the one writer <sub>*Added 2026-09-03 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471a: absorbed by THE MYLI SSOT FOLD v2 — F5.4 (the one extractor + the evidence gate); its content is carried in the plan and ticks with that slice's YAML receipt.*</sub>
- [ ] Playbook: a saved search minted by the tour automation carries source=tour_derived at birth (#257.18) — A search we made for the buyer is labeled as ours, never as their stated criteria. <sub>*Added 2026-09-05 via the task door.*</sub>
- [ ] Inbound: 34 existing CRM cards credit the lead with an agent's words (one-log-wall UPDATE through the writer) — Cards that put an agent's text in the buyer's mouth get re-filed with the real speaker. <sub>*Added 2026-09-05 via the task door.*</sub>
- [ ] Inbound: two ghost cards on Blackwell's new record from tape rows 943081009/943081010 (Dennis McKinley's thread; the Mc-name seam) — Two cards on Bridget's record belong to a different person's thread; the name grammar must read 'McKinley'. <sub>*Added 2026-09-05 via the task door.*</sub>
- [ ] The Myli MVP closed loop reads PROVEN on all seven links (THE-MYLI-MVP-SPEC L0–L7) on a fresh bench persona twice in a row — The Myli MVP closed loop reads PROVEN on all seven links (THE-MYLI-MVP-SPEC L0–L7) on a fresh bench persona twice in a row <sub>*Added 2026-09-09 via the task door.*</sub>
- [x] Web chat: the pill tap order, funnel-only pills, deterministic status lines, one greeting per thread (#257.373–.375) — Web chat: the pill tap order, funnel-only pills, deterministic status lines, one greeting per thread (#257.373–.375) <sub>*Added 2026-09-09 via the task door. TICKED 2026-09-09 via the task entrance (desk) — receipt: .claude/reports/grinder/whole-loop/RECEIPT-webchat-r1-2026-09-09.md. desk-verified on prod 631471b5ef 14:05 ET: band pill road recorded, no self-intro, status lines every ~1-2 s, card after her words, [Browse homes] only, no erase*</sub> `WED 9/9`
- [x] Web chat funnel body (#257.382 Z1-Z5): the next card paints only after her question on every step · no [Browse homes] on funnel turns · the position advances only on a captured fact · a clarifying turn gets its own status lines · the ack-warmth judge row (matrix M70-M74) — Web chat funnel body (#257.382 Z1-Z5): the next card paints only after her question on every step · no [Browse homes] on funnel turns · the position advances only on a captured fact · a clarifying turn gets its own status lines · the ack-warmth judge row (matrix M70-M74) <sub>*Added 2026-09-09 via the task door. TICKED 2026-09-09 via the task entrance (reconcile-lane (Fable, under Lee's desk) 2026-09-09) — receipt: .claude/reports/grinder/whole-loop/RECEIPT-webchat-r2-2026-09-09.md. BUILT on localhost 9/09 (lane-sonnet-webchat-r2), desk-verified with a typed clarifying question; M74 (ack warmth) is a JUDGE row NOT yet judge-run; the prod after-recording is owed after push #23.*</sub> `WED 9/9`
- [x] The five-layer turn contract measured on the block road (#257.383 round 3): layer 3 was already live behind MYLI_NEXT=1; built the guard refusing a control for a captured fact · the [Not sure] zero-debt waiver · notSure:false honored (allowUnknown) · zero pills on a plain question (matrix M75-M80) — The five-layer turn contract measured on the block road (#257.383 round 3): layer 3 was already live behind MYLI_NEXT=1; built the guard refusing a control for a captured fact · the [Not sure] zero-debt waiver · notSure:false honored (allowUnknown) · zero pills on a plain question (matrix M75-M80) <sub>*Added 2026-09-09 via the task door. TICKED 2026-09-09 via the task entrance (reconcile-lane (Fable, under Lee's desk) 2026-09-09) — receipt: .claude/reports/grinder/whole-loop/RECEIPT-webchat-r3-2026-09-09.md. BUILT 9/09 (lane-sonnet-webchat-r2 round 3): blocks.test 141/141, funnel-by-code 32/32; nextStepsFromReply/bindStateRow/PILL_STATE_TABLE confirmed UNREACHED on the live web surface (a fallback for !agent.blocks) — desk ruled they stay. Unpushed — push #23.*</sub> `WED 9/9`
- [x] Per-lead memory freshness on the AI Summary band: 'Memory: processed X ago · N pending' computed from the memory pipeline's own scope SQL (audit gap G4) — Per-lead memory freshness on the AI Summary band: 'Memory: processed X ago · N pending' computed from the memory pipeline's own scope SQL (audit gap G4) <sub>*Added 2026-09-09 via the task door. TICKED 2026-09-09 via the task entrance (reconcile-lane (Fable, under Lee's desk) 2026-09-09) — receipt: .claude/reports/myli-os/RECEIPT-myli-build-2026-09-09.md. BUILT 9/09 (lane-sonnet-myli-build): hard-facts.ts loadMemoryFreshness reuses the pipeline's REMAINING_UNPROCESSED_SQL; rendered in LeadAiSummaryBand.tsx; live screenshot on the bench lead; tsc 0. Same receipt: L4 'stamped: 0' was the ONE-FACT-ONE-EVIDENCE guard refusing two facts with no evidence ref (not a broken pass); L6 split-brain and L3 fact quality were already fixed by earlier lanes and verified live. Unpushed — push #23.*</sub> `WED 9/9`
- [x] The funnel map (L0, #257.381.10) and the brain map redrawn to the chat contract (#257.381.2): docs/wireframes/myli-funnel-map-2026-09-09.html + brain-map-HORIZONTAL-2026-09-09.html, selftest-proven, hosted at /boards/myli-funnel-map and /boards/brain-map — The funnel map (L0, #257.381.10) and the brain map redrawn to the chat contract (#257.381.2): docs/wireframes/myli-funnel-map-2026-09-09.html + brain-map-HORIZONTAL-2026-09-09.html, selftest-proven, hosted at /boards/myli-funnel-map and /boards/brain-map <sub>*Added 2026-09-09 via the task door. TICKED 2026-09-09 via the task entrance (reconcile-lane (Fable, under Lee's desk) 2026-09-09) — receipt: .claude/reports/myli-os/RECEIPT-funnel-map-2026-09-09.md. BUILT 9/09 (lane-sonnet-funnel-map): 35 capabilities censused by file:line; promoted by the desk 14:10Z/14:13Z. ⚠ Its #257.383 banner is now STALE (round 3 measured layer 3 live behind MYLI_NEXT=1; the second-opinion receipt exists) — a separate open item carries the redraw.*</sub> `WED 9/9`
- [x] The conversation build log is a board (#257.384): .claude/reports/myli-os/THE-CONVERSATION-BUILD-LOG.md hosted at /boards/myli-conversation-build — the plan, every attempt with what it produced and why it failed, the rules paid for, the open questions — The conversation build log is a board (#257.384): .claude/reports/myli-os/THE-CONVERSATION-BUILD-LOG.md hosted at /boards/myli-conversation-build — the plan, every attempt with what it produced and why it failed, the rules paid for, the open questions <sub>*Added 2026-09-09 via the task door. TICKED 2026-09-09 via the task entrance (reconcile-lane (Fable, under Lee's desk) 2026-09-09) — receipt: .claude/reports/myli-os/THE-CONVERSATION-BUILD-LOG.md. Opened by the desk 9/09 15:20 ET; promoted 14:44Z (major-build:myli-mvp). The receipt is the record itself. Its §0 reads 'one Sonnet lane (webchat-r2) is building' — stale since round 3 closed; the desk rewrites §0.*</sub> `WED 9/9`
- [x] Re-entry (#257.372): the code-side journey resolver + the three JOURNEY_REENTRY roads — a re-tap after an interruption resumes with the facts and the number, an immediate re-tap simply continues, a completed funnel is a revisit, the second window stays clean — Re-entry (#257.372): the code-side journey resolver + the three JOURNEY_REENTRY roads — a re-tap after an interruption resumes with the facts and the number, an immediate re-tap simply continues, a completed funnel is a revisit, the second window stays clean <sub>*Added 2026-09-09 via the task door. TICKED 2026-09-09 via the task entrance (reconcile-lane (Fable, under Lee's desk) 2026-09-09) — receipt: .claude/reports/grinder/whole-loop/RECEIPT-reentry-2026-09-09.md. BUILT 9/09 (lane-reentry, to SECOND-OPINION-reentry.md); prod PASS by the desk's own hands 03:52 ET (new tab). The same-tab detour hint (reopened:true on a route change) landed in RECEIPT-chat-surface-2026-09-09.md; run 2's RE-ENTRY row (same-tab detour, no welcome-back) is re-walked on prod after push #23.*</sub> `WED 9/9`
- [x] Defect 30 (P1): the identity-turn ack invented '$75,000' for a $7,500 fact — ACK_FIGURE_EQUALS_FACT: no dollar figure reaches the screen that the ack was not handed (guard + 7 arms + bench) — Defect 30 (P1): the identity-turn ack invented '$75,000' for a $7,500 fact — ACK_FIGURE_EQUALS_FACT: no dollar figure reaches the screen that the ack was not handed (guard + 7 arms + bench) <sub>*Added 2026-09-09 via the task door. TICKED 2026-09-09 via the task entrance (reconcile-lane (Fable, under Lee's desk) 2026-09-09) — receipt: .claude/reports/grinder/whole-loop/RECEIPT-run2-p1-2026-09-09.md. CLOSED AT THE ROOT 9/09 (lane-run2-p1): ack-call.ts guard + tools/prompt-bench/ack-assertions-bench.mts. Unpushed — push #23.*</sub> `WED 9/9`
- [x] Defect 22 (P1): one affordability number on every mouth — she said $135,000, the card said $136K, the band said $136,363-$136,363 — ONE_NUMBER_EVERY_MOUTH + RANGE_OF_ONE_IS_ONE_NUMBER — Defect 22 (P1): one affordability number on every mouth — she said $135,000, the card said $136K, the band said $136,363-$136,363 — ONE_NUMBER_EVERY_MOUTH + RANGE_OF_ONE_IS_ONE_NUMBER <sub>*Added 2026-09-09 via the task door. TICKED 2026-09-09 via the task entrance (reconcile-lane (Fable, under Lee's desk) 2026-09-09) — receipt: .claude/reports/grinder/whole-loop/RECEIPT-run2-p1-2026-09-09.md. CLOSED AT THE ROOT 9/09 (lane-run2-p1): affordability-estimate.ts / file-affordability-estimate.ts / mission-control format.ts + LeadLane/LeadHardFacts; 11 + 5 arms. Unpushed — push #23.*</sub> `WED 9/9`
- [x] Turn-start reliability: a typed send during the opener window never sticks the 'Thinking…' header or drops the POST (morning report lead #1: 3 of 5 sends lost) — Turn-start reliability: a typed send during the opener window never sticks the 'Thinking…' header or drops the POST (morning report lead #1: 3 of 5 sends lost) <sub>*Added 2026-09-09 via the task door. TICKED 2026-09-09 via the task entrance (reconcile-lane (Fable, under Lee's desk) 2026-09-09) — receipt: .claude/reports/grinder/whole-loop/RECEIPT-webchat-2026-09-09.md. BUILT 05:20 ET 9/09 (milestone 1, commit d75f45156e) — in push #22, live on prod. The same receipt names the next cut still open: the opener does not STREAM (a separate open item).*</sub> `WED 9/9`
- [ ] Defect 31 (P1): after registering through the chat, the pill must not start her over at Question 1 — 'a clean thread that knows' (leadHeldFrom + the registeredThreadId second factor, session.ts, built in #22) is UNMEASURED on prod: judge POST_REGISTRATION_NO_Q1 in a real browser after push #23 — Defect 31 (P1): after registering through the chat, the pill must not start her over at Question 1 — 'a clean thread that knows' (leadHeldFrom + the registeredThreadId second factor, session.ts, built in #22) is UNMEASURED on prod: judge POST_REGISTRATION_NO_Q1 in a real browser after push #23 <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Defect 29 (P1, reproduced twice): a requirement inferred from viewings ('3 beds (inferred from 2 recent viewings)') reached her recap as something the buyer said — the writer refuses a search_criteria fact whose only cited source rows are browsing kinds (the prompt rule at extract-context.ts:2863 already says never; a code guard beside provenanceRefusal enforces it) — Defect 29 (P1, reproduced twice): a requirement inferred from viewings ('3 beds (inferred from 2 recent viewings)') reached her recap as something the buyer said — the writer refuses a search_criteria fact whose only cited source rows are browsing kinds (the prompt rule at extract-context.ts:2863 already says never; a code guard beside provenanceRefusal enforces it) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Defects 12/13: the return greetings — the signed-in opener stitched the last pill LABEL into a sentence and said 'Hi, I'm Myli' to a known person (that sentence exists only in AiSearchModal.legacy.tsx:3093; prod now carries NEXT_PUBLIC_MYLI_NEXT — re-measure on #23 before building); the signed-out second window claimed 'no searches on file', a false absence claim to a stranger — Defects 12/13: the return greetings — the signed-in opener stitched the last pill LABEL into a sentence and said 'Hi, I'm Myli' to a known person (that sentence exists only in AiSearchModal.legacy.tsx:3093; prod now carries NEXT_PUBLIC_MYLI_NEXT — re-measure on #23 before building); the signed-out second window claimed 'no searches on file', a false absence claim to a stranger <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Defect 18: her prose names a home that has no card (the first comps home) and she tells the buyer the product cannot filter finished basements when it can (a hard JSONB predicate) — Defect 18: her prose names a home that has no card (the first comps home) and she tells the buyer the product cannot filter finished basements when it can (a hard JSONB predicate) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Defect 20: the replayed anonymous history is stamped one second apart (a fake clock) and the session card counts both sides as turns (18 vs the web thread's turnCount 9) — Defect 20: the replayed anonymous history is stamped one second apart (a fake clock) and the session card counts both sides as turns (18 vs the web thread's turnCount 9) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Defect 32: two input surfaces on the identity turn — a new money question painted above the sign-in code box (M28 ONE-INPUT-SURFACE) — Defect 32: two input surfaces on the identity turn — a new money question painted above the sign-in code box (M28 ONE-INPUT-SURFACE) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Defect 33: the sign-in OTP texts (three Twilio SIDs on the bench line) file no hl_messages row — the one text every new lead receives is invisible to the feed (ALL_FEED_IS_EVERYTHING) — Defect 33: the sign-in OTP texts (three Twilio SIDs on the bench line) file no hl_messages row — the one text every new lead receives is invisible to the feed (ALL_FEED_IS_EVERYTHING) <sub>*Added 2026-09-09 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471a: absorbed by THE MYLI SSOT FOLD v2 — F1.1/F1.2 (the one write road + the projector; SMS ingestion next week); its content is carried in the plan and ticks with that slice's YAML receipt.*</sub>
- [ ] Revisit roads after the affordability number carry the signed copy (#257.386a): 'Homes in this range' · 'Walk through your must-haves' · 'Get pre-approved' · 'Browse homes' — never 'talk to a lender' (JOURNEY_REENTRY, one line) — Revisit roads after the affordability number carry the signed copy (#257.386a): 'Homes in this range' · 'Walk through your must-haves' · 'Get pre-approved' · 'Browse homes' — never 'talk to a lender' (JOURNEY_REENTRY, one line) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] The opener streams: a 10-13 s bare spinner on every first open today (the opener appends whole on the JSON road — RECEIPT-webchat-2026-09-09 §M1 names it as the next cut) — The opener streams: a 10-13 s bare spinner on every first open today (the opener appends whole on the JSON road — RECEIPT-webchat-2026-09-09 §M1 names it as the next cut) <sub>*Added 2026-09-09 via the task door. RETIRED 2026-09-09 via the task entrance (lee) — #257.386: STALE — measured on the live tree by grind-qa-bench-3, 2026-09-09: her opener STREAMS, ~1-5s to first token. The 10-13s bare-spinner claim predates the streaming opener and three lanes could have built against it. Evidence: .claude/reports/grinder/qa-bench/1788989900-receipt.md.*</sub>
- [ ] The brain map's #257.383 banner is stale: layer 3 is live behind MYLI_NEXT=1 (round 3 measured it) and the second-opinion receipt exists — redraw the banner, mark the block road BUILT and the regex minter / state table as the unreached fallback — The brain map's #257.383 banner is stale: layer 3 is live behind MYLI_NEXT=1 (round 3 measured it) and the second-opinion receipt exists — redraw the banner, mark the block road BUILT and the regex minter / state table as the unreached fallback <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Design C (morning report call #1): persist the figures she was handed and used on the turn record so the cross-turn recap keeps her own earlier numbers — a STRUCTURE_GATE interview first — Design C (morning report call #1): persist the figures she was handed and used on the turn record so the cross-turn recap keeps her own earlier numbers — a STRUCTURE_GATE interview first <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] The thinking lines: event-bound Haiku lines with a restate-only fallback (the desk's synthesis, build log §4, second opinion) vs Haiku on a cadence (#257.382b) — the ruling words so far are 'no dead space' + 'contextual' (#257.386c) — The thinking lines: event-bound Haiku lines with a restate-only fallback (the desk's synthesis, build log §4, second opinion) vs Haiku on a cadence (#257.382b) — the ruling words so far are 'no dead space' + 'contextual' (#257.386c) <sub>*Added 2026-09-09 via the task door. RETIRED 2026-09-10 via the task entrance (Lee: Zach 'not even sure if we are to use haiku with our experience'; the desk agrees) — #257.408: Haiku is retired from the thinking lines entirely. Status lines are telemetry, not storytelling (#257.393): a fixed map from execution events to sentences, one stable line per stage, no model between the event and the sentence. Both proposals put a model there — the author-without-a-referee shape that produced the eight-deep invented-activity run. Superseded by the event-bound DETERMINISTIC status-lines card.. (cleared blocked_by: zach: event-bound Haiku lines (context-aware, grounded, restate-only fallback, no timer) — the desk's synthesis — or Haiku on a cadence as #257.382b said; 'no dead space' + 'contextual' (#257.386c) are satisfied by the synthesis)*</sub>
- [ ] Docs: a run-log STATUS VOCABULARY page in docs-site (the two-count law: 'skipped' is a successful gate outcome, 'failed' is not, 'dropped'/'refused' are refusals), linked from guides/myli-os — r2 S8(a) — Docs: a run-log STATUS VOCABULARY page in docs-site (the two-count law: 'skipped' is a successful gate outcome, 'failed' is not, 'dropped'/'refused' are refusals), linked from guides/myli-os — r2 S8(a) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Q11 golden set for the room-extraction hop: 100 real teammate turns, parse failures must be &lt;=1 (the '>=99% returns JSON' claim is still unmeasured) — r2 S8(b) — Q11 golden set for the room-extraction hop: 100 real teammate turns, parse failures must be &lt;=1 (the '>=99% returns JSON' claim is still unmeasured) — r2 S8(b) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Flow-judge journey room-extraction.journey.json (teammate posts in the care-team room -> the run-log row -> the facts on the lead) — now walkable on the bench since S1 landed — r2 S8(c) — Flow-judge journey room-extraction.journey.json (teammate posts in the care-team room -> the run-log row -> the facts on the lead) — now walkable on the bench since S1 landed — r2 S8(c) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] FUB SHADOW INTAKE (#256.25/.25a): search-bar import + 10-most-active batch → neutered test-band shadow clone → full tape backfill → fire replay/random-real-house/re-engagement → grade; zero pollution; purge; run history outlives the purge — FUB SHADOW INTAKE (#256.25/.25a): search-bar import + 10-most-active batch → neutered test-band shadow clone → full tape backfill → fire replay/random-real-house/re-engagement → grade; zero pollution; purge; run history outlives the purge <sub>*Added 2026-09-03 via the task door. ⚠ WANTED CARD "qa-bench" (claims no floor) — landed on the Punch List; re-home when the card gains a floor. MOVED 2026-09-09 via the task entrance (desk) — from The Punch List to The Bench — three personas born for real: re-homed: add's pre-fix band-claim blind spot landed this on the Punch List even though qa-bench owns a real band (#257.386d addendum).*</sub>
- [ ] HouseLoop-lead shadows + the re-activation scenario (#256.25b) — HouseLoop-lead shadows + the re-activation scenario (#256.25b) <sub>*Added 2026-09-03 via the task door. ⚠ WANTED CARD "qa-bench" (claims no floor) — landed on the Punch List; re-home when the card gains a floor. MOVED 2026-09-09 via the task entrance (desk) — from The Punch List to The Bench — three personas born for real: re-homed: add's pre-fix band-claim blind spot landed this on the Punch List even though qa-bench owns a real band (#257.386d addendum).*</sub>
- [ ] The cockpit↔group-thread coordination wire (Landing move 7) — The cockpit↔group-thread coordination wire (Landing move 7) <sub>*Added 2026-09-03 via the task door. ⚠ WANTED CARD "qa-bench" (claims no floor) — landed on the Punch List; re-home when the card gains a floor. MOVED 2026-09-09 via the task entrance (desk) — from The Punch List to The Bench — three personas born for real: re-homed: add's pre-fix band-claim blind spot landed this on the Punch List even though qa-bench owns a real band (#257.386d addendum).*</sub>
- [ ] Repair the 15 misfiled hl_run_log rows (fn=room-extraction: hard failures filed as 'skipped', rendering green in the one-log) — a history UPDATE, backup first (Extraction P1 receipt §6.1; still 15 on the tape 9/09 evening) — Repair the 15 misfiled hl_run_log rows (fn=room-extraction: hard failures filed as 'skipped', rendering green in the one-log) — a history UPDATE, backup first (Extraction P1 receipt §6.1; still 15 on the tape 9/09 evening) <sub>*Added 2026-09-09 via the task door. ⚠ WANTED CARD "qa-bench" (claims no floor) — landed on the Punch List; re-home when the card gains a floor. MOVED 2026-09-09 via the task entrance (desk) — from The Punch List to The Bench — three personas born for real: re-homed: add's pre-fix band-claim blind spot landed this on the Punch List even though qa-bench owns a real band (#257.386d addendum).*</sub>
- [ ] Q11 measured as a RATE: the room extractor returns valid JSON on at least 99% of a 100-turn golden set (parse failures at most 1) — one live success is a sample, not a rate (Extraction P1 receipt §6.2) — Q11 measured as a RATE: the room extractor returns valid JSON on at least 99% of a 100-turn golden set (parse failures at most 1) — one live success is a sample, not a rate (Extraction P1 receipt §6.2) <sub>*Added 2026-09-09 via the task door. ⚠ WANTED CARD "qa-bench" (claims no floor) — landed on the Punch List; re-home when the card gains a floor. MOVED 2026-09-09 via the task entrance (desk) — from The Punch List to The Bench — three personas born for real: re-homed: add's pre-fix band-claim blind spot landed this on the Punch List even though qa-bench owns a real band (#257.386d addendum).*</sub>
- [ ] Docs: the run-log status vocabulary ('skipped' is a successful gate outcome, 'failed' is not) written into docs-site — today it lives only in a comment at one-log.ts:144 (Extraction P1 receipt §6.4) — Docs: the run-log status vocabulary ('skipped' is a successful gate outcome, 'failed' is not) written into docs-site — today it lives only in a comment at one-log.ts:144 (Extraction P1 receipt §6.4) <sub>*Added 2026-09-09 via the task door. ⚠ WANTED CARD "qa-bench" (claims no floor) — landed on the Punch List; re-home when the card gains a floor. MOVED 2026-09-09 via the task entrance (desk) — from The Punch List to The Bench — three personas born for real: re-homed: add's pre-fix band-claim blind spot landed this on the Punch List even though qa-bench owns a real band (#257.386d addendum).*</sub>
- [ ] THE MEMORY ARCHITECTURE — how Myli's memory actually works: stated vs inferred vs behavioral signal, confidence bands, facts that change over time, and total recall without blowing context — THE MEMORY ARCHITECTURE — how Myli's memory actually works: stated vs inferred vs behavioral signal, confidence bands, facts that change over time, and total recall without blowing context <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] THE CHATGPT BENCHMARK: record the affordability funnel on video, second by second, and judge HouseLoop against ChatGPT running the same funnel — THE CHATGPT BENCHMARK: record the affordability funnel on video, second by second, and judge HouseLoop against ChatGPT running the same funnel <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Her thinking lines narrate work that is not happening ('checking your income' before he has given an income) — retire the pre-written bank, lines come from real tool events only — Her thinking lines narrate work that is not happening ('checking your income' before he has given an income) — retire the pre-written bank, lines come from real tool events only <sub>*Added 2026-09-09 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471a: absorbed by THE MYLI SSOT FOLD v2 — F4.2 (the status FSM); its content is carried in the plan and ticks with that slice's YAML receipt.*</sub>
- [ ] The screen REPLACES itself when her answer or a tool card arrives — content swaps the whole view instead of appending below; it jumps and reflows — The screen REPLACES itself when her answer or a tool card arrives — content swaps the whole view instead of appending below; it jumps and reflows <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] She echoes the buyer's own number back as a flat statement with no personality — 'You're comfortable spending around $2,600 a month' instead of acknowledging it like a person — She echoes the buyer's own number back as a flat statement with no personality — 'You're comfortable spending around $2,600 a month' instead of acknowledging it like a person <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] TWO greetings that contradict each other on one open: 'Welcome back — you were working out what you can afford' then 'Hey Riley — what are we looking for today?' — the second discards the resume the first just offered (sibling of 967e6d96, different mechanism: two authors, not one phrasing) — TWO greetings that contradict each other on one open: 'Welcome back — you were working out what you can afford' then 'Hey Riley — what are we looking for today?' — the second discards the resume the first just offered (sibling of 967e6d96, different mechanism: two authors, not one phrasing) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Clean the frozen stranger-hellos: every past client with a pre-fix thread still carries 'Hi, I'm Myli' at position 0 (backup-first data pass, not a build lane's UPDATE) — Clean the frozen stranger-hellos: every past client with a pre-fix thread still carries 'Hi, I'm Myli' at position 0 (backup-first data pass, not a build lane's UPDATE) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] She RE-ASKS for the down payment 25s after being told $30,000 and acknowledging it — in the same breath as an answer that uses '$30k saved' (benchmark t=60.14) — She RE-ASKS for the down payment 25s after being told $30,000 and acknowledging it — in the same breath as an answer that uses '$30k saved' (benchmark t=60.14) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Question 4 asks for a credit score; ChatGPT asks 'what monthly payment would feel comfortable' — Zach's own stated standard. And the opener promises four steps without naming them — Question 4 asks for a credit score; ChatGPT asks 'what monthly payment would feel comfortable' — Zach's own stated standard. And the opener promises four steps without naming them <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] The sliders arrive PRE-FILLED ($8,000 income, $0 debts) under a primary-blue 'That's about right' — we put an invented number in the buyer's mouth — The sliders arrive PRE-FILLED ($8,000 income, $0 debts) under a primary-blue 'That's about right' — we put an invented number in the buyer's mouth <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Literal backticks render on screen in her prose, and '~93% LTV' is untrue ($30k on $315k = 90.5%) — NUMBER_UNTRUE reached the buyer — Literal backticks render on screen in her prose, and '~93% LTV' is untrue ($30k on $315k = 90.5%) — NUMBER_UNTRUE reached the buyer <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] A PILL'S LABEL NEVER WRAPS: 'That's about right' breaks to two lines at 390 and doubles the pill's height beside a one-line neighbour — cage it, do not just restyle it — A PILL'S LABEL NEVER WRAPS: 'That's about right' breaks to two lines at 390 and doubles the pill's height beside a one-line neighbour — cage it, do not just restyle it <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] The pill ROW wraps raggedly on mobile: four credit options land 2/1/1 at different widths, left-aligned — an accidental flex-wrap where four peers need a deliberate layout — The pill ROW wraps raggedly on mobile: four credit options land 2/1/1 at different widths, left-aligned — an accidental flex-wrap where four peers need a deliberate layout <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Commit-unit streaming: the second author (W6, the ack-echo/derived-count re-author) is still live — the double-paint is gone but her prose can still be re-authored — Commit-unit streaming: the second author (W6, the ack-echo/derived-count re-author) is still live — the double-paint is gone but her prose can still be re-authored <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] P0 RETURNING BUYER: her thread asks for monthly debts SEVEN times running, quotes TWO different incomes for the same person ($7,917 vs $12,250), delivers the answer three times, and offers an income slider PRE-FILLED at $8,000 to a buyer whose income is on file — P0 RETURNING BUYER: her thread asks for monthly debts SEVEN times running, quotes TWO different incomes for the same person ($7,917 vs $12,250), delivers the answer three times, and offers an income slider PRE-FILLED at $8,000 to a buyer whose income is on file <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] THE VIEW YANKS UP ~200px EVERY TURN: her question bubble is rewritten SMALLER on answer (433->270 chars) and the slider card is deleted, shortening the document and pulling scrollTop up (314->113, 1007->860, 1700->1499) — THE VIEW YANKS UP ~200px EVERY TURN: her question bubble is rewritten SMALLER on answer (433->270 chars) and the slider card is deleted, shortening the document and pulling scrollTop up (314->113, 1007->860, 1700->1499) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] The status-line REQUIRES gate is the right shape but too narrow: the general and funnel_clarifying banks are UNGATED — six invented activities after a chip tap, ending 'Reading what you asked' about a chip the buyer pressed — The status-line REQUIRES gate is the right shape but too narrow: the general and funnel_clarifying banks are UNGATED — six invented activities after a chip tap, ending 'Reading what you asked' about a chip the buyer pressed <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] THE SPENT-CONTROL DESIGN: what a slider and a pill become AFTER they are used — locked showing the choice, still live, or gone; and can a buyer change an answer by going back up the thread — THE SPENT-CONTROL DESIGN: what a slider and a pill become AFTER they are used — locked showing the choice, still live, or gone; and can a buyer change an answer by going back up the thread <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Myli affordability funnel: the flat echo (bbc777a9) — the 5 confirm-turns are one sentence mould, turn 5 only restates the buyer after a 19s wait, turn 3 promises the answer then asks another question. Copy desk rewrites in BENCHMARK-affordability-vs-chatgpt-2026-09-09.md R2.3 — Myli affordability funnel: the flat echo (bbc777a9) — the 5 confirm-turns are one sentence mould, turn 5 only restates the buyer after a 19s wait, turn 3 promises the answer then asks another question. Copy desk rewrites in BENCHMARK-affordability-vs-chatgpt-2026-09-09.md R2.3 <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Myli affordability funnel: raw markdown leaks to screen — literal backticks render around the Q2 example, and Q1/Q2 format their examples differently. Measured frame 031 of the 390px capture — Myli affordability funnel: raw markdown leaks to screen — literal backticks render around the Q2 example, and Q1/Q2 format their examples differently. Measured frame 031 of the 390px capture <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] The word 'queued' renders to the BUYER — the turn-queue's internal state is leaking into the conversation as user-facing copy (seen when several answers arrive fast) — The word 'queued' renders to the BUYER — the turn-queue's internal state is leaking into the conversation as user-facing copy (seen when several answers arrive fast) <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] THE ACK AND THE ANSWER DUPLICATE VERBATIM: the 1.1s ack call writes a sentence, then the main turn repeats it word for word below the thinking ('To figure out what you can actually afford, I need to know a bit more about your finances') — THE ACK AND THE ANSWER DUPLICATE VERBATIM: the 1.1s ack call writes a sentence, then the main turn repeats it word for word below the thinking ('To figure out what you can actually afford, I need to know a bit more about your finances') <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] Fact provenance FILL: hl_lead_facts.basis is NULL on 6,038 of 6,112 rows (98.8%) — the column exists and the one extractor is already caged; the hole is that nothing writes stated/derived/unknown. Backfill + write-path. Desk ruling #257.395. — Fact provenance FILL: hl_lead_facts.basis is NULL on 6,038 of 6,112 rows (98.8%) — the column exists and the one extractor is already caged; the hole is that nothing writes stated/derived/unknown. Backfill + write-path. Desk ruling #257.395. <sub>*Added 2026-09-09 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471a: absorbed by THE MYLI SSOT FOLD v2 — F5.2 (the assertion model, one evidence pointer); its content is carried in the plan and ticks with that slice's YAML receipt.*</sub>
- [ ] STATUS LINES bind to EXECUTION EVENTS, not to facts. Ruling #257.393: criterion changes from 'is this compatible with facts we know' to 'is this a projection of an execution event that actually occurred' (answer_received/answer_validated/calculation_started/...). ONE stable status per stage at phone width; the 8-paraphrase rotation is DELETED not gated. Keep the REQUIRES machinery, re-point it. Needs the event stream first. — STATUS LINES bind to EXECUTION EVENTS, not to facts. Ruling #257.393: criterion changes from 'is this compatible with facts we know' to 'is this a projection of an execution event that actually occurred' (answer_received/answer_validated/calculation_started/...). ONE stable status per stage at phone width; the 8-paraphrase rotation is DELETED not gated. Keep the REQUIRES machinery, re-point it. Needs the event stream first. <sub>*Added 2026-09-09 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471a: absorbed by THE MYLI SSOT FOLD v2 — F4.2 (the status FSM over real tool events); its content is carried in the plan and ticks with that slice's YAML receipt.*</sub>
- [ ] STATE VERSIONING per model call — freeze state_version; a generation started at v42 must not paint against v43. Named root cause of the re-ask defect (asked for down payment at t=60.1 while the message below it used the $30k). Ruling #257.393. — STATE VERSIONING per model call — freeze state_version; a generation started at v42 must not paint against v43. Named root cause of the re-ask defect (asked for down payment at t=60.1 while the message below it used the $30k). Ruling #257.393. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] THE RENDERER OWNS THE FUNNEL QUESTION, its numbering and its option set — the model may add personality above it but never authors 'Question 4 of 4'. Ruling #257.393; this is the architectural backing for the pill-geometry lock #257.388c. — THE RENDERER OWNS THE FUNNEL QUESTION, its numbering and its option set — the model may add personality above it but never authors 'Question 4 of 4'. Ruling #257.393; this is the architectural backing for the pill-geometry lock #257.388c. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] ONE SPEAKER row 2 of 4 — THE DASHBOARD OPENER still speaks outside the one agent loop. Ruling #257.394: parent bf3304cd ticks only when all four roads do. — ONE SPEAKER row 2 of 4 — THE DASHBOARD OPENER still speaks outside the one agent loop. Ruling #257.394: parent bf3304cd ticks only when all four roads do. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] ONE SPEAKER row 3 of 4 — THE CRM SMS/EMAIL ENGINE still speaks outside the one agent loop. Ruling #257.394. — ONE SPEAKER row 3 of 4 — THE CRM SMS/EMAIL ENGINE still speaks outside the one agent loop. Ruling #257.394. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] ONE SPEAKER row 4 of 4 — THE RETELL PHONE VOICE still speaks outside the one agent loop. Ruling #257.394. — ONE SPEAKER row 4 of 4 — THE RETELL PHONE VOICE still speaks outside the one agent loop. Ruling #257.394. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] THE CLOSER-PILL ROW Zach photographed is NOT converted to MyliPillRow and NOT measured — it still renders through the modal's own flex-wrap and can go ragged. It looks correct today ONLY because its labels happen to be long enough to force one per row: a defect standing behind a coincidence. Highest-value pill follow-up (lane-render-contract honest red #1, 2026-09-09). — THE CLOSER-PILL ROW Zach photographed is NOT converted to MyliPillRow and NOT measured — it still renders through the modal's own flex-wrap and can go ragged. It looks correct today ONLY because its labels happen to be long enough to force one per row: a defect standing behind a coincidence. Highest-value pill follow-up (lane-render-contract honest red #1, 2026-09-09). <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] FOURTEEN legacy flex-wrap pill rows remain outside MyliPillRow — the pill law (#257.388b) is enforced on the converted road only. Listed in .claude/reports/grinder/qa-bench/RECEIPT-render-contract-2026-09-09.md. Convert or card each; an unconverted row is outside the cage. — FOURTEEN legacy flex-wrap pill rows remain outside MyliPillRow — the pill law (#257.388b) is enforced on the converted road only. Listed in .claude/reports/grinder/qa-bench/RECEIPT-render-contract-2026-09-09.md. Convert or card each; an unconverted row is outside the cage. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] 17 SECONDS OF DEAD AIR after a chip tap is UNCHANGED — the status-line fix stopped the wait LYING about it (Reading what you asked now requires a typed sentence) but did not shorten it. Zach's standing rule: 'I just can't have any dead space.' Measured by lane-render-contract 2026-09-09 over a 24s sample. Needs a truthful execution-event line per #257.393, or real latency work. — 17 SECONDS OF DEAD AIR after a chip tap is UNCHANGED — the status-line fix stopped the wait LYING about it (Reading what you asked now requires a typed sentence) but did not shorten it. Zach's standing rule: 'I just can't have any dead space.' Measured by lane-render-contract 2026-09-09 over a 24s sample. Needs a truthful execution-event line per #257.393, or real latency work. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] DEAD CODE: the 'revise' path is emitted by BOTH Myli routes and consumed by NOBODY — no client registers onRevise. Proven by lane-render-contract 2026-09-09 while hunting a post-hoc rewrite that did not exist. Remove it, or it will be hunted again. — DEAD CODE: the 'revise' path is emitted by BOTH Myli routes and consumed by NOBODY — no client registers onRevise. Proven by lane-render-contract 2026-09-09 while hunting a post-hoc rewrite that did not exist. Remove it, or it will be hunted again. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] OWED AFTER PUSH #23: re-run the stranger-introduction consequence count. Tonight's arm read 15 -> 15 because nothing was pushed, so a flat number proves the arm RAN, not that the defect is closed in the world. Query: select count(*) from hl_ai_search_conversation where userId is not null and (messages->0->>'content') like 'Hi, I''m Myli. Tell me what you%'. Baseline 15 at 2026-09-09 22:01:04Z. — OWED AFTER PUSH #23: re-run the stranger-introduction consequence count. Tonight's arm read 15 -> 15 because nothing was pushed, so a flat number proves the arm RAN, not that the defect is closed in the world. Query: select count(*) from hl_ai_search_conversation where userId is not null and (messages->0->>'content') like 'Hi, I''m Myli. Tell me what you%'. Baseline 15 at 2026-09-09 22:01:04Z. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] DECIDE: the thread-store write gate keys on OPENING_STRANGER (one exact never-templated sentence). Tighter than keying on the relationship claim — if that opener is reworded, templated, translated or A/B'd the gate silently stops matching and the defect returns with every test green (cage-rot shape). Options: (a) keep the exact key and CAGE the string against drift, or (b) key on a predicate scoped to this seam ('claims a FIRST meeting', excluding welcome-backs). Routed to the round-2 judge 2026-09-09. — DECIDE: the thread-store write gate keys on OPENING_STRANGER (one exact never-templated sentence). Tighter than keying on the relationship claim — if that opener is reworded, templated, translated or A/B'd the gate silently stops matching and the defect returns with every test green (cage-rot shape). Options: (a) keep the exact key and CAGE the string against drift, or (b) key on a predicate scoped to this seam ('claims a FIRST meeting', excluding welcome-backs). Routed to the round-2 judge 2026-09-09. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] DECIDE (ZACH): the SPENT CONTROL residual. A tall card becoming a compact row still reclaims SOME height, so the ~200px yank shrinks rather than vanishes. If #257.390's 'never give back height' is literal, the spent row must hold the card's committed height. ALSO: the second opinion never isolated whether the scroll jump is INDEPENDENTLY fixable (reserve collapsed height / anchor to composer) — nobody may read 'the control stays' as the scroll fix. — DECIDE (ZACH): the SPENT CONTROL residual. A tall card becoming a compact row still reclaims SOME height, so the ~200px yank shrinks rather than vanishes. If #257.390's 'never give back height' is literal, the spent row must hold the card's committed height. ALSO: the second opinion never isolated whether the scroll jump is INDEPENDENTLY fixable (reserve collapsed height / anchor to composer) — nobody may read 'the control stays' as the scroll fix. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] NODE-IDENTITY ASSERTION: no arm in the suite asks whether the status region and a message share a node — nothing would have caught the original double-paint. Buildable now that the fix introduced the stable name data-hl='myli-status-region'; carry the benchmark's two node-identity arms with it (#257.390 is the same law at the layout altitude). — NODE-IDENTITY ASSERTION: no arm in the suite asks whether the status region and a message share a node — nothing would have caught the original double-paint. Buildable now that the fix introduced the stable name data-hl='myli-status-region'; carry the benchmark's two node-identity arms with it (#257.390 is the same law at the layout altitude). <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] VOCAB_TRUTH W-1: Myli states '65 active 2-3 bed homes in Overland Park within $365k' and FOUR independent readings say 41 / 69 / 45 / 72 — none is 65, and 2+ beds is broader than her 2-3 so the true comparable is &lt;=45. CONTROL: her other named number is exactly right (a 3-bed on W 108th at $339,900 matches 12736 W 108th Street to the dollar) — she is not generally unreliable, this count is untrue. Census first: is she restating a tool answer, querying different filters than her sentence claims, or composing a number with no tool call? Found by judge-qa-bench-r3 2026-09-09. — VOCAB_TRUTH W-1: Myli states '65 active 2-3 bed homes in Overland Park within $365k' and FOUR independent readings say 41 / 69 / 45 / 72 — none is 65, and 2+ beds is broader than her 2-3 so the true comparable is &lt;=45. CONTROL: her other named number is exactly right (a 3-bed on W 108th at $339,900 matches 12736 W 108th Street to the dollar) — she is not generally unreliable, this count is untrue. Census first: is she restating a tool answer, querying different filters than her sentence claims, or composing a number with no tool call? Found by judge-qa-bench-r3 2026-09-09. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] VOCAB_TRUTH W-2: one house, two prices, one screen. Her prose says Hadley Street 'for $359k'; the card beneath says $350,000. $359,000 is REAL — a superseded row in that home's own priceHistory (three same-date 'Listed for sale' rows: 350000/359000/369000). She reads the history array instead of listPrice. The card is right and the prose is wrong, which is the worse way round — the prose is what a buyer repeats on the phone. Found by judge-qa-bench-r3 2026-09-09. — VOCAB_TRUTH W-2: one house, two prices, one screen. Her prose says Hadley Street 'for $359k'; the card beneath says $350,000. $359,000 is REAL — a superseded row in that home's own priceHistory (three same-date 'Listed for sale' rows: 350000/359000/369000). She reads the history array instead of listPrice. The card is right and the prose is wrong, which is the worse way round — the prose is what a buyer repeats on the phone. Found by judge-qa-bench-r3 2026-09-09. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] DATA REMEDIATION: 15 pre-fix conversation rows across 4 people still RENDER Myli's stranger introduction at the top of restored threads. Clause E is CLOSED on live code (the thread born during the judge's walk is clean; the leak count held at 15 from 22:01Z to 23:12Z) — these are historical rows, not a code defect, and nobody has written the remediation. One row (7c271e7a, born 22:01:04Z, five minutes after the SessionProvider fix) resists the tidy story; likeliest a page loaded before the fix compiled, NOT proven. — DATA REMEDIATION: 15 pre-fix conversation rows across 4 people still RENDER Myli's stranger introduction at the top of restored threads. Clause E is CLOSED on live code (the thread born during the judge's walk is clean; the leak count held at 15 from 22:01Z to 23:12Z) — these are historical rows, not a code defect, and nobody has written the remediation. One row (7c271e7a, born 22:01:04Z, five minutes after the SessionProvider fix) resists the tidy story; likeliest a page loaded before the fix compiled, NOT proven. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] STALE TEST BLOCKING PUSH #23: contract.test.ts:948 ('the computation's record: the CRM route's body, built from the estimate') passes 322_000/379_000 and asserts they return unchanged; the payload returns 320000/380000. That is commit afb33be661 today ('the record speaks her number') rounding through spokenEstimate to the nearest $5,000 instead of a bare Math.round — because one arithmetic result was appearing on prod as FOUR different numbers ($135,000 spoken, $136K on the card, $136,363 twice). The test's expectations date to e2982f6d56 on 09-05, four days before the change. THE CODE IS RIGHT, THE TEST IS STALE. Owner: the audit-defects lane — the fix is two numbers, but only its owner knows whether the rounding is final. Named by lane-render-contract 2026-09-09. — STALE TEST BLOCKING PUSH #23: contract.test.ts:948 ('the computation's record: the CRM route's body, built from the estimate') passes 322_000/379_000 and asserts they return unchanged; the payload returns 320000/380000. That is commit afb33be661 today ('the record speaks her number') rounding through spokenEstimate to the nearest $5,000 instead of a bare Math.round — because one arithmetic result was appearing on prod as FOUR different numbers ($135,000 spoken, $136K on the card, $136,363 twice). The test's expectations date to e2982f6d56 on 09-05, four days before the change. THE CODE IS RIGHT, THE TEST IS STALE. Owner: the audit-defects lane — the fix is two numbers, but only its owner knows whether the rounding is final. Named by lane-render-contract 2026-09-09. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] ROOT FIX (outranks the write gate): guidedGreeting() at AiSearchModal.tsx:1781-1789 carries a SECOND stranger introduction on the identical 'authenticated && first ? named : STRANGER' two-way branch this item exists to end — the loading window falls into the branch meaning 'we have never met'. It must adopt contractOpeningFor so it stops composing a false introduction at all. The judge: 'The gate is the backstop; the composer is the defect. Fixing only the backstop leaves the wrong words on the SCREEN, which is where Zach reads them.' HONEST LIMIT: it feeds GUIDED_START as a GuidedTurn (role 'm') and clean() takes only user/assistant, so it is NOT proven to reach the store today — live on the screen, possibly unreachable through the record. — ROOT FIX (outranks the write gate): guidedGreeting() at AiSearchModal.tsx:1781-1789 carries a SECOND stranger introduction on the identical 'authenticated && first ? named : STRANGER' two-way branch this item exists to end — the loading window falls into the branch meaning 'we have never met'. It must adopt contractOpeningFor so it stops composing a false introduction at all. The judge: 'The gate is the backstop; the composer is the defect. Fixing only the backstop leaves the wrong words on the SCREEN, which is where Zach reads them.' HONEST LIMIT: it feeds GUIDED_START as a GuidedTurn (role 'm') and clean() takes only user/assistant, so it is NOT proven to reach the store today — live on the screen, possibly unreachable through the record. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] LOADED GUN BEHIND A FLAG: AiSearchModal.legacy.tsx:1511-1520 carries the SAME 'authenticated && first ? named : STRANGER' introduction branch verbatim, and it serves traffic the moment NEXT_PUBLIC_MYLI_NEXT flips. The live copy (AiSearchModal.tsx:1781-1790) is DEAD CODE — measured exhaustively by lane-thread-gate: GUIDED_START declared once and never referenced, role 'm' nowhere else, guided set three times all to null, so every read of guided.log is undefined. So the defect the judge ordered fixed cannot fire today, but its twin is one env var from painting 'Hi, I'm Myli' at a signed-in past client as a mystery regression. Fix the twin or prove it unreachable and card it as a flag-flip prerequisite. — LOADED GUN BEHIND A FLAG: AiSearchModal.legacy.tsx:1511-1520 carries the SAME 'authenticated && first ? named : STRANGER' introduction branch verbatim, and it serves traffic the moment NEXT_PUBLIC_MYLI_NEXT flips. The live copy (AiSearchModal.tsx:1781-1790) is DEAD CODE — measured exhaustively by lane-thread-gate: GUIDED_START declared once and never referenced, role 'm' nowhere else, guided set three times all to null, so every read of guided.log is undefined. So the defect the judge ordered fixed cannot fire today, but its twin is one env var from painting 'Hi, I'm Myli' at a signed-in past client as a mystery regression. Fix the twin or prove it unreachable and card it as a flag-flip prerequisite. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] DELETE the dead guided-chat mechanism: GUIDED_START / guidedGreeting() / the 'guided' state in AiSearchModal.tsx, proven unreachable in the active engine (retired for openContractChat per THE_UNHOBBLING). Same class as the dead 'revise' path — dead code that LOOKS live costs an afternoon every time someone hunts it. Deletion has a bigger blast radius than it looks; census before removing. — DELETE the dead guided-chat mechanism: GUIDED_START / guidedGreeting() / the 'guided' state in AiSearchModal.tsx, proven unreachable in the active engine (retired for openContractChat per THE_UNHOBBLING). Same class as the dead 'revise' path — dead code that LOOKS live costs an afternoon every time someone hunts it. Deletion has a bigger blast radius than it looks; census before removing. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] FLAG FALLBACK IS BACKWARDS: MyliModal.tsx:24-27 reads NEXT_PUBLIC_MYLI_NEXT === '1' ? next : legacy — so unset, empty, typo'd or missing in a NEW environment renders the LEGACY engine. The default-on-absence should be the engine we intend to serve. This is the standing shape behind every other legacy defect, and the estate has already been misled once about which engine production serves (the stale flag.ts comment). Found by judge-qa-bench-r3 2026-09-09 while verifying a dead-code claim. — FLAG FALLBACK IS BACKWARDS: MyliModal.tsx:24-27 reads NEXT_PUBLIC_MYLI_NEXT === '1' ? next : legacy — so unset, empty, typo'd or missing in a NEW environment renders the LEGACY engine. The default-on-absence should be the engine we intend to serve. This is the standing shape behind every other legacy defect, and the estate has already been misled once about which engine production serves (the stale flag.ts comment). Found by judge-qa-bench-r3 2026-09-09 while verifying a dead-code claim. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] CORRECTION to the legacy-twin card: AiSearchModal.legacy.tsx:1511-1520 is DEAD TOO, verified by hand — GUIDED_START declared :1520 referenced nowhere else, setGuided at :3219/:4712/:4780 all null, useState&lt;Guided|null>(null) at :1588. The ten lines are verbatim identical to the live file and so is the deadness. It is NOT a flag-flip prerequisite and NOT urgent: 'the gun is a prop'. It is a DELETION card in BOTH files, worth doing because this dead code has already cost TWO separate investigations in one night — it reads as live to every census that greps the pattern, and it will again. — CORRECTION to the legacy-twin card: AiSearchModal.legacy.tsx:1511-1520 is DEAD TOO, verified by hand — GUIDED_START declared :1520 referenced nowhere else, setGuided at :3219/:4712/:4780 all null, useState&lt;Guided|null>(null) at :1588. The ten lines are verbatim identical to the live file and so is the deadness. It is NOT a flag-flip prerequisite and NOT urgent: 'the gun is a prop'. It is a DELETION card in BOTH files, worth doing because this dead code has already cost TWO separate investigations in one night — it reads as live to every census that greps the pattern, and it will again. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] DECIDE (ZACH) — TWO OF YOUR OWN RULINGS CONTRADICT: #257.390 says never give back height; a44c1588 requires the status region to be DISMISSED. Dismissing something that occupies vertical space necessarily gives height back. Measured after the fix: six unrequested scroll decreases remain, and only ONE is a message node shrinking (the spent-control swap) — the other five reclaim height from OUTSIDE the message nodes, the transient regions unmounting. The yank is reduced, not eliminated: ~200px on 4/4 turns before, largest 125px now. TWO READINGS: (a) transient regions are EXEMPT because they were never committed layout (the render lane's reading), or (b) they must collapse into RESERVED space. Reported rather than chosen, because a lane may not pick between two of your rulings. — DECIDE (ZACH) — TWO OF YOUR OWN RULINGS CONTRADICT: #257.390 says never give back height; a44c1588 requires the status region to be DISMISSED. Dismissing something that occupies vertical space necessarily gives height back. Measured after the fix: six unrequested scroll decreases remain, and only ONE is a message node shrinking (the spent-control swap) — the other five reclaim height from OUTSIDE the message nodes, the transient regions unmounting. The yank is reduced, not eliminated: ~200px on 4/4 turns before, largest 125px now. TWO READINGS: (a) transient regions are EXEMPT because they were never committed layout (the render lane's reading), or (b) they must collapse into RESERVED space. Reported rather than chosen, because a lane may not pick between two of your rulings. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] DECIDE (ZACH) — the 'all four tiles or none' rail is not LITERALLY satisfied: three credit labels render one line, 'Building under 640' renders two. The boxes are identical (148px) and the text is centred, so the ROW is uniform — but literal compliance costs a SCHEMA change (labels arriving as two structured parts in registry.ts plus the chips block), not a layout change, because a component cannot impose a line break on arbitrary children without inventing a split rule. Accept what is on screen, or fund the two-part label. — DECIDE (ZACH) — the 'all four tiles or none' rail is not LITERALLY satisfied: three credit labels render one line, 'Building under 640' renders two. The boxes are identical (148px) and the text is centred, so the ROW is uniform — but literal compliance costs a SCHEMA change (labels arriving as two structured parts in registry.ts plus the chips block), not a layout change, because a component cannot impose a line break on arbitrary children without inventing a split rule. Accept what is on screen, or fund the two-part label. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] OBSERVED LIVE, NO MECHANISM YET: the credit chip answer FAILED TO SEND during a browser drive. The product showed its honest affordance (Didn't send / 'That one didn't come through.' / Ask again) so the failure surface is correct. It is NOT the send gate — a chip calls send() directly and never touches submitInput. Cost the drive its result-turn pill row, still unverified. Needs a mechanism hunt. — OBSERVED LIVE, NO MECHANISM YET: the credit chip answer FAILED TO SEND during a browser drive. The product showed its honest affordance (Didn't send / 'That one didn't come through.' / Ask again) so the failure surface is correct. It is NOT the send gate — a chip calls send() directly and never touches submitInput. Cost the drive its result-turn pill row, still unverified. Needs a mechanism hunt. <sub>*Added 2026-09-09 via the task door.*</sub>
- [ ] ⛔ SEVERE — AT 360px THE SECOND ROW OF CREDIT OPTIONS IS CLIPPED BY THE COMPOSER. 'Fair 640-699' is sliced through the middle and 'Building under 640' loses its second line: tiles at y=683 + ~66px run to ~749 behind a composer topping out ~700. At 390 the same row sits at y=629 and clears it. TWO OF FOUR OPTIONS ARE INVISIBLE WHEN THE QUESTION RENDERS, WITH NO CUE — and the hidden two are Fair and Building, the two a self-conscious buyer most needs. Found by lane-chatgpt-benchmark 2026-09-09 at 360px. — ⛔ SEVERE — AT 360px THE SECOND ROW OF CREDIT OPTIONS IS CLIPPED BY THE COMPOSER. 'Fair 640-699' is sliced through the middle and 'Building under 640' loses its second line: tiles at y=683 + ~66px run to ~749 behind a composer topping out ~700. At 390 the same row sits at y=629 and clears it. TWO OF FOUR OPTIONS ARE INVISIBLE WHEN THE QUESTION RENDERS, WITH NO CUE — and the hidden two are Fair and Building, the two a self-conscious buyer most needs. Found by lane-chatgpt-benchmark 2026-09-09 at 360px. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ PROVENANCE LAUNDERING, STRONGEST FORM: the income card arrives PRE-FILLED (text '8,000', range 8000, 'That's right' ENABLED) and one tap renders 'About $8,000 a month before taxes' AS THE BUYER'S OWN MESSAGE — the product composes a first-person sentence they never typed — then she replies 'You're working with about $8,000 a month before taxes.' ChatGPT's name for it: turning 'the software displayed $8,000' into 'the user told us $8,000'. GREEN ALONGSIDE: Q2 debts now arrives EMPTY where it shipped $0, so the empty state is proven buildable; Q1 income is the only survivor. — ⛔ PROVENANCE LAUNDERING, STRONGEST FORM: the income card arrives PRE-FILLED (text '8,000', range 8000, 'That's right' ENABLED) and one tap renders 'About $8,000 a month before taxes' AS THE BUYER'S OWN MESSAGE — the product composes a first-person sentence they never typed — then she replies 'You're working with about $8,000 a month before taxes.' ChatGPT's name for it: turning 'the software displayed $8,000' into 'the user told us $8,000'. GREEN ALONGSIDE: Q2 debts now arrives EMPTY where it shipped $0, so the empty state is proven buildable; Q1 income is the only survivor. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] THE CLOSER PILL ROW FAILS AT 360px (deterministic, reproduced on BOTH label sets): row gaps 52/52/52 at 390 become 52/52/68 at 360 — 'All metro homes around $X/mo' wraps after its width is squeezed 289->274. Confirms the row holds at 390 only because the labels are long enough to force one-per-row AND short enough to fit one line — a defect standing behind a coincidence, now measured. — THE CLOSER PILL ROW FAILS AT 360px (deterministic, reproduced on BOTH label sets): row gaps 52/52/52 at 390 become 52/52/68 at 360 — 'All metro homes around $X/mo' wraps after its width is squeezed 289->274. Confirms the row holds at 390 only because the labels are long enough to force one-per-row AND short enough to fit one line — a defect standing behind a coincidence, now measured. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] MAKE THE TWO-LINE CREDIT TILE THE DESIGN AT EVERY WIDTH. Benchmark answered the desk's open question: at 390 the 2x2 reads as an ACCIDENT (three labels one line, 'Building under 640' stacked — the odd one out looks like it ran out of room, the exact 'broken wrapping appearance' the consultation warned of); at 360, where all four stack, it reads as INTENTIONAL. The build is closest to 'designed' at the width nobody tuned for. This is also the consultation's prescription. Needs Zach's eye or his sign. — MAKE THE TWO-LINE CREDIT TILE THE DESIGN AT EVERY WIDTH. Benchmark answered the desk's open question: at 390 the 2x2 reads as an ACCIDENT (three labels one line, 'Building under 640' stacked — the odd one out looks like it ran out of room, the exact 'broken wrapping appearance' the consultation warned of); at 360, where all four stack, it reads as INTENTIONAL. The build is closest to 'designed' at the width nobody tuned for. This is also the consultation's prescription. Needs Zach's eye or his sign. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] RATCHET — two benchmark arms were INERT and are scored unproven, not passed: (1) the F4 label-length arm used '$2,402' and '$4,416', the SAME character count, so it never varied what it was meant to vary — the real test interpolates a place or count ('See all 1,233 in Overland Park at this payment'); (2) the text-scale arm left the pill at 18px->18px at root 22px, so scaling was never tested. An inert arm that reads green is the dead-needle class. — RATCHET — two benchmark arms were INERT and are scored unproven, not passed: (1) the F4 label-length arm used '$2,402' and '$4,416', the SAME character count, so it never varied what it was meant to vary — the real test interpolates a place or count ('See all 1,233 in Overland Park at this payment'); (2) the text-scale arm left the pill at 18px->18px at root 22px, so scaling was never tested. An inert arm that reads green is the dead-needle class. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⭐ THE RECALL CARD — the third state that fixes authorship AND #257.390 together. Replace empty-vs-prefilled with a state machine: unknown -> recalled_unconfirmed -> edited_unconfirmed -> confirmed. Render 'Monthly income · $8,000/month · From your previous answer · [Still $8,000] [Change]', and after the tap KEEP THE CARD MOUNTED as '✓ Confirmed just now · [Change]'. No disappearing control, no reclaimed height, no dead space, and NO extra tap — the tap already existed, its MEANING changes from vague acceptance to explicit reaffirmation. Second opinion 2026-09-09. — ⭐ THE RECALL CARD — the third state that fixes authorship AND #257.390 together. Replace empty-vs-prefilled with a state machine: unknown -> recalled_unconfirmed -> edited_unconfirmed -> confirmed. Render 'Monthly income · $8,000/month · From your previous answer · [Still $8,000] [Change]', and after the tap KEEP THE CARD MOUNTED as '✓ Confirmed just now · [Change]'. No disappearing control, no reclaimed height, no dead space, and NO extra tap — the tap already existed, its MEANING changes from vague acceptance to explicit reaffirmation. Second opinion 2026-09-09. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] STOP WRITING A FABRICATED user_message: the transcript must not contain semantic content beyond what the user knowingly committed. Tapping a chip and serialising it user-side is faithful; composing 'About $8,000 a month before taxes' from confirm(previous_value=8000) is a fabricated quotation. Replace with an ACTION RECEIPT ('✓ Confirmed $8,000/month'). ⛔ Changing only the bubble text while still writing the fabricated user_message into the event stream moves the laundering BELOW the UI — explicitly rejected. — STOP WRITING A FABRICATED user_message: the transcript must not contain semantic content beyond what the user knowingly committed. Tapping a chip and serialising it user-side is faithful; composing 'About $8,000 a month before taxes' from confirm(previous_value=8000) is a fabricated quotation. Replace with an ACTION RECEIPT ('✓ Confirmed $8,000/month'). ⛔ Changing only the bubble text while still writing the fabricated user_message into the event stream moves the laundering BELOW the UI — explicitly rejected. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] DATA LAYER: split Fact from Confirmation. Fact&#123;value, original_assertion_event, original_source, original_observed_at&#125; stays; Confirmation&#123;fact_id, event_id, actor, confirmed_at, confirmation_method&#125; is added. A confirmation makes a fact fresher and stronger — it does not rewrite history. Matters most when the recalled value was imported, inferred or agent-supplied: retain 'system supplied P -> user later confirmed P'. — DATA LAYER: split Fact from Confirmation. Fact&#123;value, original_assertion_event, original_source, original_observed_at&#125; stays; Confirmation&#123;fact_id, event_id, actor, confirmed_at, confirmation_method&#125; is added. A confirmation makes a fact fresher and stronger — it does not rewrite history. Matters most when the recalled value was imported, inferred or agent-supplied: retain 'system supplied P -> user later confirmed P'. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⭐⭐ NO NAKED DIAGNOSTIC NUMBERS ACROSS A LANE BOUNDARY. The desk made FOUR numeric errors in one night because prose turns typed results into naked tokens and lineage decays at every compression boundary — 'I must be more careful' is the wrong mitigation. Every externally meaningful number returns as a Measurement&#123;metric,value,population,aggregation,data_snapshot,source,query_fingerprint,implementation_version&#125; with a stable id. Prose may display 40; the reasoning layer refers to m_123. Any comparative sentence (wider/narrower/increased/mismatch/delta) must come from a code ComparisonResult that can return NON_COMPARABLE naming the offending dimension — never from two numbers in model context. — ⭐⭐ NO NAKED DIAGNOSTIC NUMBERS ACROSS A LANE BOUNDARY. The desk made FOUR numeric errors in one night because prose turns typed results into naked tokens and lineage decays at every compression boundary — 'I must be more careful' is the wrong mitigation. Every externally meaningful number returns as a Measurement&#123;metric,value,population,aggregation,data_snapshot,source,query_fingerprint,implementation_version&#125; with a stable id. Prose may display 40; the reasoning layer refers to m_123. Any comparative sentence (wider/narrower/increased/mismatch/delta) must come from a code ComparisonResult that can return NON_COMPARABLE naming the offending dimension — never from two numbers in model context. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔⛔ BLOCKING — THE STORE HOLDS FABRICATED BUYER TESTIMONY. 245 rows across 242 conversations where role='user' holds a sentence THE PRODUCT COMPOSED (first_seen 2026-09-04 17:23:45, last_seen 2026-09-09 23:47:51). CONTROL: the inverted predicate returns 7,151 real user lines, so the predicate discriminates. Verbatim: 'About $20,000 a month before taxes' / 'About $8,000 a month before taxes' / 'About $6,700 a month before taxes'. It launders downstream — the fact extractor, the memory pipeline, a human reading the thread, and Myli on her next turn all read the product's own suggestion back as the buyer's testimony. Clause 1 of bf3304cd: a store whose role='user' rows were written by the product is not a record. ⚠ 245 is a FLOOR — only ONE sentence template was probed. Found by judge-qa-bench-r3 2026-09-09 by probing the store after the desk relayed it as a 360px UI defect. — ⛔⛔ BLOCKING — THE STORE HOLDS FABRICATED BUYER TESTIMONY. 245 rows across 242 conversations where role='user' holds a sentence THE PRODUCT COMPOSED (first_seen 2026-09-04 17:23:45, last_seen 2026-09-09 23:47:51). CONTROL: the inverted predicate returns 7,151 real user lines, so the predicate discriminates. Verbatim: 'About $20,000 a month before taxes' / 'About $8,000 a month before taxes' / 'About $6,700 a month before taxes'. It launders downstream — the fact extractor, the memory pipeline, a human reading the thread, and Myli on her next turn all read the product's own suggestion back as the buyer's testimony. Clause 1 of bf3304cd: a store whose role='user' rows were written by the product is not a record. ⚠ 245 is a FLOOR — only ONE sentence template was probed. Found by judge-qa-bench-r3 2026-09-09 by probing the store after the desk relayed it as a 360px UI defect. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] DATA REMEDIATION (separate from the code fix, needs a desk decision + backup first per RULE #0 v2): the 245 existing fabricated role='user' rows. They are a record of what happened; backing them out is not automatic. Decide: annotate, re-attribute, or leave with a provenance marker. Do NOT delete before Zach or the desk signs, and back up to ~/hl-reversals/ first. — DATA REMEDIATION (separate from the code fix, needs a desk decision + backup first per RULE #0 v2): the 245 existing fabricated role='user' rows. They are a record of what happened; backing them out is not automatic. Decide: annotate, re-attribute, or leave with a provenance marker. Do NOT delete before Zach or the desk signs, and back up to ~/hl-reversals/ first. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] CENSUS: find EVERY place the product composes a role='user' row. The 245 came from ONE sentence template ('About $X a month before taxes'); the true count is unknown and is a floor. Report the number before fixing beyond template one. (judge S-2) — CENSUS: find EVERY place the product composes a role='user' row. The 245 came from ONE sentence template ('About $X a month before taxes'); the true count is unknown and is a floor. Report the number before fixing beyond template one. (judge S-2) <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] OPTION 2 — the full 'guided' mechanism removal, properly sized. A dead VALUE is not a dead MECHANISM: guided is always null at runtime, but there are 75 matches / ~50 call sites in AiSearchModal.tsx and 84 in AiSearchModal.legacy.tsx — an 11-field Guided type, four helpers (offeredPills, hintFor, stationPlaceholder, questionForGuided), a synced guidedRef, sessionStorage persistence via GUIDED_KEY, and LIVE render/composer branches (guided ? brainTurn : send — which transcript the composer posts to; chatTail = guided ? -1; an empty && !guided render fork). Needs its own walk, tsc and render verification. lane-thread-gate correctly refused to absorb this under DO NOT PUSH with no walk. — OPTION 2 — the full 'guided' mechanism removal, properly sized. A dead VALUE is not a dead MECHANISM: guided is always null at runtime, but there are 75 matches / ~50 call sites in AiSearchModal.tsx and 84 in AiSearchModal.legacy.tsx — an 11-field Guided type, four helpers (offeredPills, hintFor, stationPlaceholder, questionForGuided), a synced guidedRef, sessionStorage persistence via GUIDED_KEY, and LIVE render/composer branches (guided ? brainTurn : send — which transcript the composer posts to; chatTail = guided ? -1; an empty && !guided render fork). Needs its own walk, tsc and render verification. lane-thread-gate correctly refused to absorb this under DO NOT PUSH with no walk. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] THE 60-vs-65 GAP IS UNEXPLAINED and was handed on as an open question rather than given a second wrong mechanism. The judge's 'the missing 5 are forSale-but-not-raw-active' does NOT survive — that 41-vs-45 gap is the raw-SQL instrument again (Core Heartland matrix + canonical dedup), not a group split, and SEARCH_PREVIEW_LIMIT is 10 so it is not the preview either. Open. (judge S-5) — THE 60-vs-65 GAP IS UNEXPLAINED and was handed on as an open question rather than given a second wrong mechanism. The judge's 'the missing 5 are forSale-but-not-raw-active' does NOT survive — that 41-vs-45 gap is the raw-SQL instrument again (Core Heartland matrix + canonical dedup), not a group split, and SEARCH_PREVIEW_LIMIT is 10 so it is not the preview either. Open. (judge S-5) <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] REMEDIATION SEQUENCE for the 245+ synthetic user rows, as designed by the second opinion (needs Zach's sign + a backup first): (1) SNAPSHOT, then QUARANTINE the known rows — do not wait to find every template before protecting the known 245. (2) PRESERVE the row and DO NOT change its original role ('that field is part of what you're documenting as having been wrong'); add provenance_status=invalid, invalid_reason=synthetic_user_utterance, invalidated_at, correction_event_id. (3) Append the CORRECTED event separately: event_type=user_confirmation, actor=user, field, value, interaction=confirm_button, value_origin=previously_supplied_user_value, display_text_origin=product_generated. (4) DEPENDENCY INVALIDATION + RE-DERIVATION downstream — a fact with an independent genuine source SURVIVES; one sourced ONLY from a synthetic row does not. Where lineage is missing, rebuild the derived object for the affected conversation rather than guessing which sentence influenced which inference. (5) Any memory summary lacking per-source provenance is contaminated until recomputed. — REMEDIATION SEQUENCE for the 245+ synthetic user rows, as designed by the second opinion (needs Zach's sign + a backup first): (1) SNAPSHOT, then QUARANTINE the known rows — do not wait to find every template before protecting the known 245. (2) PRESERVE the row and DO NOT change its original role ('that field is part of what you're documenting as having been wrong'); add provenance_status=invalid, invalid_reason=synthetic_user_utterance, invalidated_at, correction_event_id. (3) Append the CORRECTED event separately: event_type=user_confirmation, actor=user, field, value, interaction=confirm_button, value_origin=previously_supplied_user_value, display_text_origin=product_generated. (4) DEPENDENCY INVALIDATION + RE-DERIVATION downstream — a fact with an independent genuine source SURVIVES; one sourced ONLY from a synthetic row does not. Where lineage is missing, rebuild the derived object for the affected conversation rather than guessing which sentence influenced which inference. (5) Any memory summary lacking per-source provenance is contaminated until recomputed. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ FORBIDDEN FIXES on the synthetic-row incident, named explicitly so nobody reaches for them: (a) UPDATE role='system' — every role value is false, and the thing is not a message at all but a user ACTION whose words the app synthesized; (b) hard delete — destroys incident evidence and breaks lineage; (c) editing the sentence — rewrites history and makes old ids point to new semantics; (d) a read filter alone — excellent CONTAINMENT, not remediation; (e) banning the pre-fill — 'you would be solving the wrong layer again': a remembered value offered as 'Previously you told me $8,000/month. Still right?' is legitimate. — ⛔ FORBIDDEN FIXES on the synthetic-row incident, named explicitly so nobody reaches for them: (a) UPDATE role='system' — every role value is false, and the thing is not a message at all but a user ACTION whose words the app synthesized; (b) hard delete — destroys incident evidence and breaks lineage; (c) editing the sentence — rewrites history and makes old ids point to new semantics; (d) a read filter alone — excellent CONTAINMENT, not remediation; (e) banning the pre-fill — 'you would be solving the wrong layer again': a remembered value offered as 'Previously you told me $8,000/month. Still right?' is legitimate. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] SCHEMA (long-term, not this week): role is insufficient to represent the event. Distinguish actor (who performed the action), content_origin (who or what generated the words), event_type (what happened), evidence_status (what downstream reasoning may infer). Compressing all four into role IS the architectural defect that produced the 245 rows. — SCHEMA (long-term, not this week): role is insufficient to represent the event. Distinguish actor (who performed the action), content_origin (who or what generated the words), event_type (what happened), evidence_status (what downstream reasoning may infer). Compressing all four into role IS the architectural defect that produced the 245 rows. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] TOOLING: make 'State effect' a required field on the intake tool (tools/master-plan/task.mjs add), values none|transient/local|external request|persistent|unknown — so #257.404 becomes a refusal rather than a habit. ⚠ Add it OPTIONAL-with-warning first: making it required immediately breaks every live lane's filing call mid-shift. A card labelled presentation-only carrying State effect: unknown is an open row in the PM projection. — TOOLING: make 'State effect' a required field on the intake tool (tools/master-plan/task.mjs add), values none|transient/local|external request|persistent|unknown — so #257.404 becomes a refusal rather than a habit. ⚠ Add it OPTIONAL-with-warning first: making it required immediately breaks every live lane's filing call mid-shift. A card labelled presentation-only carrying State effect: unknown is an open row in the PM projection. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] LOADED GUN BEHIND A FLAG — lib/myli/controls.ts (the MIX system) composes first-person buyer claims for the same four money facts PLUS CREDIT SCORE AND MONTHLY COMFORT. Dormant today (NEXT_PUBLIC_MYLI_CONTROLS unset) but it is WIDER than the template-one fix that shipped: two extra fact families are not covered. Fix or prove unreachable before that flag ever moves. — LOADED GUN BEHIND A FLAG — lib/myli/controls.ts (the MIX system) composes first-person buyer claims for the same four money facts PLUS CREDIT SCORE AND MONTHLY COMFORT. Dormant today (NEXT_PUBLIC_MYLI_CONTROLS unset) but it is WIDER than the template-one fix that shipped: two extra fact families are not covered. Fix or prove unreachable before that flag ever moves. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] LOADED GUN BEHIND A FLAG — MyliMoneyAnswer.legacy.tsx is the verbatim PRE-FIX twin of the composition that was just corrected. Dormant (legacy engine unreachable at NEXT_PUBLIC_MYLI_NEXT=1) but it will emit 'About $X a month before taxes' as buyer testimony the moment the flag moves. Pairs with the backwards flag-fallback card (MyliModal.tsx:24-27 defaults to LEGACY on absence), which makes this reachable in any new environment where the var is unset. — LOADED GUN BEHIND A FLAG — MyliMoneyAnswer.legacy.tsx is the verbatim PRE-FIX twin of the composition that was just corrected. Dormant (legacy engine unreachable at NEXT_PUBLIC_MYLI_NEXT=1) but it will emit 'About $X a month before taxes' as buyer testimony the moment the flag moves. Pairs with the backwards flag-fallback card (MyliModal.tsx:24-27 defaults to LEGACY on absence), which makes this reachable in any new environment where the var is unset. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] SIGNED DISCRIMINATOR for composed-testimony defects (#257.404 family): does the product add SEMANTIC content, or only JOINING? 'About' is a hedge the buyer did not choose; 'a month before taxes' is a unit and qualifier they did not state — SEMANTIC, therefore a fabricated quotation. Punctuation joining a name/email/phone the buyer typed into fields they controlled adds nothing they did not commit — a FAITHFUL SERIALIZATION, and a user bubble is correct. MyliIdentityAnswer.tsx is ruled correct as-is; converting it to a receipt would be the over-correction. Same reasoning carves out 'Not sure' label taps. — SIGNED DISCRIMINATOR for composed-testimony defects (#257.404 family): does the product add SEMANTIC content, or only JOINING? 'About' is a hedge the buyer did not choose; 'a month before taxes' is a unit and qualifier they did not state — SEMANTIC, therefore a fabricated quotation. Punctuation joining a name/email/phone the buyer typed into fields they controlled adds nothing they did not commit — a FAITHFUL SERIALIZATION, and a user bubble is correct. MyliIdentityAnswer.tsx is ruled correct as-is; converting it to a receipt would be the over-correction. Same reasoning carves out 'Not sure' label taps. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ DATA LOSS — THE RETRY PATH ERASES MYLI'S ANSWERS. Established by census (not reproduction) by lane-vocab-truth 2026-09-09: (1) grep req.signal/signal.aborted over route.ts returns NO HITS — the server never observes a client disconnect, so it completes the turn and calls persistConversation regardless; (2) persistConversation REPLACES the transcript (:95), it does not append; (3) the outbound body filters 'synthetic' only, NOT pending:'failed', so a failed message rides again; (4) 'Ask again' calls send(last.content) — a NEW message, not an in-place retry. CONSEQUENCE: a client give-up over a turn the server actually completed writes the buyer's line TWICE and REPLACES the row, erasing Myli's first answer from the canonical record. Second way the one store stops being a record, same night as the first. State effect: PERSISTENT. — ⛔ DATA LOSS — THE RETRY PATH ERASES MYLI'S ANSWERS. Established by census (not reproduction) by lane-vocab-truth 2026-09-09: (1) grep req.signal/signal.aborted over route.ts returns NO HITS — the server never observes a client disconnect, so it completes the turn and calls persistConversation regardless; (2) persistConversation REPLACES the transcript (:95), it does not append; (3) the outbound body filters 'synthetic' only, NOT pending:'failed', so a failed message rides again; (4) 'Ask again' calls send(last.content) — a NEW message, not an in-place retry. CONSEQUENCE: a client give-up over a turn the server actually completed writes the buyer's line TWICE and REPLACES the row, erasing Myli's first answer from the canonical record. Second way the one store stops being a record, same night as the first. State effect: PERSISTENT. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] DECIDE (DESK/ZACH): should the server observe req.signal and ABANDON the write when the client is gone, or COMPLETE and persist? Real argument both ways — the buyer's question WAS answered, so the answer is not untrue; but a turn the buyer never saw should arguably not silently become the record. ⚠ Also open: persistConversation REPLACING rather than appending may be DELIBERATE (it is how a live transcript stays authoritative against a stale client) — if load-bearing, the fix lives in what the client SENDS, not in how the server writes. — DECIDE (DESK/ZACH): should the server observe req.signal and ABANDON the write when the client is gone, or COMPLETE and persist? Real argument both ways — the buyer's question WAS answered, so the answer is not untrue; but a turn the buyer never saw should arguably not silently become the record. ⚠ Also open: persistConversation REPLACING rather than appending may be DELIBERATE (it is how a live transcript stays authoritative against a stale client) — if load-bearing, the fix lives in what the client SENDS, not in how the server writes. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] COUNT CORRECTION — the fabricated-testimony incident is 622 rows across 255 conversations, NOT 245 across 242. The original probe covered only the monthly_income template: monthly_income 245 · monthly_debts 191 · down_payment 181 · max_price 5. All four are the same producer (describeMoneyCommit), same code path, same pre-fix wording. First 2026-09-04 17:23:45Z, last 2026-09-09 23:47:51Z — all before the fix commit, consistent with nothing pushed. 245 was an incident lower bound exactly as the judge flagged; this is the producer-level census it asked for. — COUNT CORRECTION — the fabricated-testimony incident is 622 rows across 255 conversations, NOT 245 across 242. The original probe covered only the monthly_income template: monthly_income 245 · monthly_debts 191 · down_payment 181 · max_price 5. All four are the same producer (describeMoneyCommit), same code path, same pre-fix wording. First 2026-09-04 17:23:45Z, last 2026-09-09 23:47:51Z — all before the fix commit, consistent with nothing pushed. 245 was an incident lower bound exactly as the judge flagged; this is the producer-level census it asked for. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] COUNT DISCIPLINE — the fabricated-testimony figure is '>=622 rows; EXACT for the four templates of producer describeMoneyCommit; coverage complete for that producer, NOT system-wide; as of 2026-09-10 00:34Z'. It is NOT '622 rows'. The desk relayed the judge's explicitly-labelled FLOOR of 245 as a bare total and it hardened across four messages; the producer census made it 622, and the second opinion caught the desk about to repeat the error one level up. TWO more producer paths are known and unaudited (controls.ts MIX — WIDER, adds credit score + monthly comfort; MyliMoneyAnswer.legacy.tsx), so a system-wide census is OWED before any number is called the total. — COUNT DISCIPLINE — the fabricated-testimony figure is '>=622 rows; EXACT for the four templates of producer describeMoneyCommit; coverage complete for that producer, NOT system-wide; as of 2026-09-10 00:34Z'. It is NOT '622 rows'. The desk relayed the judge's explicitly-labelled FLOOR of 245 as a bare total and it hardened across four messages; the producer census made it 622, and the second opinion caught the desk about to repeat the error one level up. TWO more producer paths are known and unaudited (controls.ts MIX — WIDER, adds credit score + monthly comfort; MyliMoneyAnswer.legacy.tsx), so a system-wide census is OWED before any number is called the total. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] THE REMAINDER after the containment fix (named honestly by lane-thread-gate): a completed-but-undelivered assistant reply can still be overwritten by a later replace-write, because the client never received the aborted fetch's response body and so its array never learned about it. THE DESIGN (#257.405): assistant_message carries status=generated + presentation_state=pending|presented; persist the completed answer marked UNPRESENTED; on reconnect the client asks the server for the state of that turn instead of sending a new user message. ⭐ AND THE MYLI HALF: an answer with presented=false must be EXCLUDED FROM SHARED-DIALOGUE MEMORY, or she says 'like I mentioned earlier' about something the buyer never received — the fabricated-testimony error from the other side. Needs stable turn identity + idempotency UNIQUE(conversation_id, client_message_id) + reconciliation on reconnect. A build, not a filter. — THE REMAINDER after the containment fix (named honestly by lane-thread-gate): a completed-but-undelivered assistant reply can still be overwritten by a later replace-write, because the client never received the aborted fetch's response body and so its array never learned about it. THE DESIGN (#257.405): assistant_message carries status=generated + presentation_state=pending|presented; persist the completed answer marked UNPRESENTED; on reconnect the client asks the server for the state of that turn instead of sending a new user message. ⭐ AND THE MYLI HALF: an answer with presented=false must be EXCLUDED FROM SHARED-DIALOGUE MEMORY, or she says 'like I mentioned earlier' about something the buyer never received — the fabricated-testimony error from the other side. Needs stable turn identity + idempotency UNIQUE(conversation_id, client_message_id) + reconciliation on reconnect. A build, not a filter. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] RULED, DO NOT BUILD: route.ts must NOT observe req.signal to abandon the write on client disconnect (#257.405). It converts ordinary mobile/network behaviour into cancellation semantics, discards paid work, races the abort against the commit, and still sometimes writes — leaving client/server/provider/database each believing something different. Abort on an explicit Cancel/Stop, never on transport loss. Carded so the next lane meets the ruling instead of re-deriving the intuitive wrong answer. ALSO RULED: persistConversation's replace semantics are LOAD-BEARING (the 'start fresh' shrink and clarify-repost depend on the client array being authoritative) — leave them, and stop treating the blob as the source of truth rather than ripping it out. — RULED, DO NOT BUILD: route.ts must NOT observe req.signal to abandon the write on client disconnect (#257.405). It converts ordinary mobile/network behaviour into cancellation semantics, discards paid work, races the abort against the commit, and still sometimes writes — leaving client/server/provider/database each believing something different. Abort on an explicit Cancel/Stop, never on transport loss. Carded so the next lane meets the ruling instead of re-deriving the intuitive wrong answer. ALSO RULED: persistConversation's replace semantics are LOAD-BEARING (the 'start fresh' shrink and clarify-repost depend on the client array being authoritative) — leave them, and stop treating the blob as the source of truth rather than ripping it out. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⚠ THE WEB DATABASE HAS NO SCHEMA LOCK — the writer census named this as its OWN blind spot rather than passing: two REAL live tables (hl_home_claim, hl_short_links) were born by raw SQL and Prisma does not model them, so ARM 1 cannot see them. 'An UNDECLARED raw-SQL web table is invisible here, and that is this census's own named blind spot, not a green.' The CRM has a committed schema lock; the WEB database does not. Until one exists, any raw-SQL web table is outside every drift and census cage. Surfaced during the push #23 pre-flight battery 2026-09-10. — ⚠ THE WEB DATABASE HAS NO SCHEMA LOCK — the writer census named this as its OWN blind spot rather than passing: two REAL live tables (hl_home_claim, hl_short_links) were born by raw SQL and Prisma does not model them, so ARM 1 cannot see them. 'An UNDECLARED raw-SQL web table is invisible here, and that is this census's own named blind spot, not a green.' The CRM has a committed schema lock; the WEB database does not. Until one exists, any raw-SQL web table is outside every drift and census cage. Surfaced during the push #23 pre-flight battery 2026-09-10. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ PUSH #23 IS BLOCKED BY THE INNGEST-ARM CAGE — 166 unarmed drivers against a shrink-only floor of 165. The +1 is almost certainly apps/houseloop-crm/scripts/room-extraction.test.ts (the only candidate touched 2026-09-09); it reaches lib/inngest/client.ts transitively via lib/ai/ai-job-guard (flushAiFunctionCache) and has ZERO dispatch calls — no inngest.send, no dispatchNotification. It therefore FITS the cage's existing declared-exception pattern ('proof driver, never dispatches', 9 already signed). ⚠ THE DESK DELIBERATELY DID NOT SIGN THAT EXCEPTION AT 01:10 TO CLEAR A PUSH IT WANTED — BEDROCK's third clause: 'a verdict written to clear a red is how a registry rots… never an exemption written to get a push out.' ⛔ AND THE FLOOR MUST NOT BE RAISED TO 166: 'a floor raised to admit a known-false red banks it as permanent headroom, and the next TRUE red rides in underneath it.' OWNER: whoever owns the CRM extraction work. THE FIX IS ONE OF TWO — arm it, or sign the exception in daylight having verified the import chain dispatches nothing. — ⛔ PUSH #23 IS BLOCKED BY THE INNGEST-ARM CAGE — 166 unarmed drivers against a shrink-only floor of 165. The +1 is almost certainly apps/houseloop-crm/scripts/room-extraction.test.ts (the only candidate touched 2026-09-09); it reaches lib/inngest/client.ts transitively via lib/ai/ai-job-guard (flushAiFunctionCache) and has ZERO dispatch calls — no inngest.send, no dispatchNotification. It therefore FITS the cage's existing declared-exception pattern ('proof driver, never dispatches', 9 already signed). ⚠ THE DESK DELIBERATELY DID NOT SIGN THAT EXCEPTION AT 01:10 TO CLEAR A PUSH IT WANTED — BEDROCK's third clause: 'a verdict written to clear a red is how a registry rots… never an exemption written to get a push out.' ⛔ AND THE FLOOR MUST NOT BE RAISED TO 166: 'a floor raised to admit a known-false red banks it as permanent headroom, and the next TRUE red rides in underneath it.' OWNER: whoever owns the CRM extraction work. THE FIX IS ONE OF TWO — arm it, or sign the exception in daylight having verified the import chain dispatches nothing. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] CORRECTION to the push #23 blocker card: the desk's identification of apps/houseloop-crm/scripts/room-extraction.test.ts as the +1 unarmed driver is FALSE. Verified: it EXISTED at the floor commit 0b2f59062c and ALREADY imported flushAiFunctionCache there, so it was already counted in the 165. All seven CRM files touched since the floor existed at the floor. ⛔ THE +1 IS UNIDENTIFIED. The delta is therefore NOT a new driver file but almost certainly an existing driver that NEWLY REACHES lib/inngest/client.ts because a LIB file in its import chain gained an import — which the cage counts as 'reach the client 180'. To find it honestly: run the cage at the floor commit and diff the unarmed lists; do not guess again. ⛔ Do NOT raise the floor to 166 and do NOT sign an exception to clear it. — CORRECTION to the push #23 blocker card: the desk's identification of apps/houseloop-crm/scripts/room-extraction.test.ts as the +1 unarmed driver is FALSE. Verified: it EXISTED at the floor commit 0b2f59062c and ALREADY imported flushAiFunctionCache there, so it was already counted in the 165. All seven CRM files touched since the floor existed at the floor. ⛔ THE +1 IS UNIDENTIFIED. The delta is therefore NOT a new driver file but almost certainly an existing driver that NEWLY REACHES lib/inngest/client.ts because a LIB file in its import chain gained an import — which the cage counts as 'reach the client 180'. To find it honestly: run the cage at the floor commit and diff the unarmed lists; do not guess again. ⛔ Do NOT raise the floor to 166 and do NOT sign an exception to clear it. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ S-7 (BLOCKING) — CENSUS BY STRING, NOT BY PRODUCER, then fix every live emitter. Round 3 proved the write stop closed ONE producer while a SECOND live producer kept writing: conversation e38679ef, born 11 min after the fix, holds the old fabrication for monthly_income beside the new receipts for debts and down_payment — one thread, one session, one code version, its own internal control. describeMoneyCommit is NOT at fault. Candidates: lib/myli/controls.ts:234 (live MIX wiring) and MyliMoneyAnswer.legacy.tsx:96 — WHICH ONE IS THE BUILDER'S CENSUS, NOT THE JUDGE'S ASSERTION. Third instance tonight of: a census enumerated what it could see, the fix was scoped to those, the class was wider. — ⛔ S-7 (BLOCKING) — CENSUS BY STRING, NOT BY PRODUCER, then fix every live emitter. Round 3 proved the write stop closed ONE producer while a SECOND live producer kept writing: conversation e38679ef, born 11 min after the fix, holds the old fabrication for monthly_income beside the new receipts for debts and down_payment — one thread, one session, one code version, its own internal control. describeMoneyCommit is NOT at fault. Candidates: lib/myli/controls.ts:234 (live MIX wiring) and MyliMoneyAnswer.legacy.tsx:96 — WHICH ONE IS THE BUILDER'S CENSUS, NOT THE JUDGE'S ASSERTION. Third instance tonight of: a census enumerated what it could see, the fix was scoped to those, the class was wider. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] S-8 — THE ACTOR IS STILL WRONG ON THE FIXED ROAD. Receipt rows store role:'user'; the fix changed wording and added an actionReceipt flag, not the actor. Every consumer filtering on role — the fact extractor, the memory pipeline, a human, Myli's next turn — is unchanged. 'A flag respected only by its author' — the round-1 provisional shape again. ⚠ SEQUENCING: the CONSUMERS MOVE FIRST. The COMMITTED_SAID regex tails are load-bearing (deliberately preserved) and must not change until whatever reads them can handle the new actor. Do not flip role first. — S-8 — THE ACTOR IS STILL WRONG ON THE FIXED ROAD. Receipt rows store role:'user'; the fix changed wording and added an actionReceipt flag, not the actor. Every consumer filtering on role — the fact extractor, the memory pipeline, a human, Myli's next turn — is unchanged. 'A flag respected only by its author' — the round-1 provisional shape again. ⚠ SEQUENCING: the CONSUMERS MOVE FIRST. The COMMITTED_SAID regex tails are load-bearing (deliberately preserved) and must not change until whatever reads them can handle the new actor. Do not flip role first. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] SHARPENED DISCRIMINATOR (supersedes the desk's semantic-vs-joining test): does the product ADD A PROPOSITION THE BUYER DID NOT ASSERT? 'Semantic' is not decidable at the margin — is an em-dash semantic? is lowercasing? — but a proposition is. 'About' hedges precision; 'before taxes' defines meaning; a reformatted phone number asserts nothing new. RE-PRESENTING WHAT THEY ENTERED IS ALWAYS ALLOWED; ADDING A PROPOSITION NEVER IS. Same verdict on both files, decidable at the margin. — SHARPENED DISCRIMINATOR (supersedes the desk's semantic-vs-joining test): does the product ADD A PROPOSITION THE BUYER DID NOT ASSERT? 'Semantic' is not decidable at the margin — is an em-dash semantic? is lowercasing? — but a proposition is. 'About' hedges precision; 'before taxes' defines meaning; a reformatted phone number asserts nothing new. RE-PRESENTING WHAT THEY ENTERED IS ALWAYS ALLOWED; ADDING A PROPOSITION NEVER IS. Same verdict on both files, decidable at the margin. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ THE REAL S-8 GAP, proven by arm: turnsFromTranscript — the wire from the conversation store to the CRM's fact extractor — DROPS actionReceipt ENTIRELY. The type has no field for it. So every turn reaching extract-context.ts is INDISTINGUISHABLE FROM LITERAL SPEECH regardless of the flag, and the fact extractor writes facts from product-composed lines as if the buyer had said them. This is 'a flag respected only by its author' proven at the seam that matters most — the one that writes facts. Censused by lane-thread-gate 2026-09-10 across all four role='user' consumers with a disposition each. ⚠ SEQUENCING STANDS: consumers move FIRST; do not flip role; the COMMITTED_SAID tails are load-bearing. — ⛔ THE REAL S-8 GAP, proven by arm: turnsFromTranscript — the wire from the conversation store to the CRM's fact extractor — DROPS actionReceipt ENTIRELY. The type has no field for it. So every turn reaching extract-context.ts is INDISTINGUISHABLE FROM LITERAL SPEECH regardless of the flag, and the fact extractor writes facts from product-composed lines as if the buyer had said them. This is 'a flag respected only by its author' proven at the seam that matters most — the one that writes facts. Censused by lane-thread-gate 2026-09-10 across all four role='user' consumers with a disposition each. ⚠ SEQUENCING STANDS: consumers move FIRST; do not flip role; the COMMITTED_SAID tails are load-bearing. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] OWED AFTER TRAFFIC — the S-7c consequence assertion for the SECOND producer fix (controls.ts fromContract().said) is reported UNPROVEN, not green: B=0, no time has passed for real traffic since the commit. A flat count proves the arm RAN, never that the defect is closed in the world. Re-run once real traffic exists post-push. ⚠ Related open question worth settling with it: the lane argues NEXT_PUBLIC_MYLI_CONTROLS measures OFF because this machine's only dev server (PID 38974) started 12+ hours after .env.local's last edit — so the flag 'has been absent its whole life'. That is either sound or it is a story about ONE machine; another agent's machine may carry a different flag. — OWED AFTER TRAFFIC — the S-7c consequence assertion for the SECOND producer fix (controls.ts fromContract().said) is reported UNPROVEN, not green: B=0, no time has passed for real traffic since the commit. A flat count proves the arm RAN, never that the defect is closed in the world. Re-run once real traffic exists post-push. ⚠ Related open question worth settling with it: the lane argues NEXT_PUBLIC_MYLI_CONTROLS measures OFF because this machine's only dev server (PID 38974) started 12+ hours after .env.local's last edit — so the flag 'has been absent its whole life'. That is either sound or it is a story about ONE machine; another agent's machine may carry a different flag. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] REFERENCE — MyliMoneyAnswer.legacy.tsx is IMMUTABLE BY ITS OWN HEADER: 'VERBATIM ORIGIN ce4b5c8c10 · deleted when MYLI_NEXT ships · never edit'. lane-thread-gate applied a correct-looking wording fix there during round 4, read the header, and REVERTED IT BYTE-FOR-BYTE before reporting (git diff --stat empty against HEAD). It is DEAD on two independent grounds — the env flag and its own immutability declaration — and it is not to be edited; it is to be DELETED when MYLI_NEXT ships. Carded so the next lane meets the rule instead of re-making the same correct-looking change. — REFERENCE — MyliMoneyAnswer.legacy.tsx is IMMUTABLE BY ITS OWN HEADER: 'VERBATIM ORIGIN ce4b5c8c10 · deleted when MYLI_NEXT ships · never edit'. lane-thread-gate applied a correct-looking wording fix there during round 4, read the header, and REVERTED IT BYTE-FOR-BYTE before reporting (git diff --stat empty against HEAD). It is DEAD on two independent grounds — the env flag and its own immutability declaration — and it is not to be edited; it is to be DELETED when MYLI_NEXT ships. Carded so the next lane meets the rule instead of re-making the same correct-looking change. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] S-13 METHODOLOGY — every producer census carries a REACHABILITY COLUMN per producer: LIVE / FLAG-GATED (naming the flag and where it is set or absent, checked in the .env FILES and the running PROCESS) / DEAD (initializer checked, not just setters). Born from FOUR dormant mechanisms fixed in one night (guidedGreeting x2, the legacy twin, MIX). The diagnosis is not carelessness: 'this code emits the bad string' is cheap to establish and 'this code RUNS' is expensive, so every census stops at the first question. ⚠ AND THE INSTRUMENT TRAP: ps eww is NOT authoritative for Next env vars — Next loads .env.local into process.env itself (MYLI_NEXT is absent from ps, present in the file, effective). Never a file mtime. — S-13 METHODOLOGY — every producer census carries a REACHABILITY COLUMN per producer: LIVE / FLAG-GATED (naming the flag and where it is set or absent, checked in the .env FILES and the running PROCESS) / DEAD (initializer checked, not just setters). Born from FOUR dormant mechanisms fixed in one night (guidedGreeting x2, the legacy twin, MIX). The diagnosis is not carelessness: 'this code emits the bad string' is cheap to establish and 'this code RUNS' is expensive, so every census stops at the first question. ⚠ AND THE INSTRUMENT TRAP: ps eww is NOT authoritative for Next env vars — Next loads .env.local into process.env itself (MYLI_NEXT is absent from ps, present in the file, effective). Never a file mtime. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔⛔ THE PROVENANCE HOLE IS TWO BOUNDARIES DEEP, NOT ONE — and hl_messages cannot express it at all. Traced by lane-thread-gate 2026-09-10: (1) website-chat-turns.ts's WebsiteChatTurn type has no actionReceipt field, mirrored identically on the CRM side; (2) the CRM intake route maps role==='user' -> direction:'inbound' and calls logMessage (BEDROCK's ONE WRITER for hl_messages), so a product-composed receipt lands BYTE-IDENTICAL TO A REAL INBOUND TEXT FROM THE LEAD; (3) extract-context.ts's loadHistory reads WHERE direction='inbound' as the lead's own words. 'direction' is a lead-vs-system distinction and cannot express user-vs-receipt even if the wire carried the flag. THE FULL PATH: client Msg.actionReceipt -> outboundMessages (drops) -> hl_ai_search_conversation.messages (carries) -> turnsFromTranscript (drops, both type defs) -> CRM intake route -> hl_messages.direction -> loadHistory. ⛔ NOT FIXED, NOT ATTEMPTED — it touches the one-writer path and the CRM message store. — ⛔⛔ THE PROVENANCE HOLE IS TWO BOUNDARIES DEEP, NOT ONE — and hl_messages cannot express it at all. Traced by lane-thread-gate 2026-09-10: (1) website-chat-turns.ts's WebsiteChatTurn type has no actionReceipt field, mirrored identically on the CRM side; (2) the CRM intake route maps role==='user' -> direction:'inbound' and calls logMessage (BEDROCK's ONE WRITER for hl_messages), so a product-composed receipt lands BYTE-IDENTICAL TO A REAL INBOUND TEXT FROM THE LEAD; (3) extract-context.ts's loadHistory reads WHERE direction='inbound' as the lead's own words. 'direction' is a lead-vs-system distinction and cannot express user-vs-receipt even if the wire carried the flag. THE FULL PATH: client Msg.actionReceipt -> outboundMessages (drops) -> hl_ai_search_conversation.messages (carries) -> turnsFromTranscript (drops, both type defs) -> CRM intake route -> hl_messages.direction -> loadHistory. ⛔ NOT FIXED, NOT ATTEMPTED — it touches the one-writer path and the CRM message store. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] THE SHAPE FOR THE PROVENANCE FIX (second opinion 2026-09-10, do NOT build the boolean): provenance belongs in the EVENT IDENTITY, not as optional metadata. A human tap and a product-rendered sentence are DIFFERENT EVENT CLASSES: utterance&#123;author&#125; | user_action&#123;author:human,action,value&#125; | action_receipt&#123;author:product,causedByEventId,text&#125;. ⭐ The fact extractor should NEVER read the receipt sentence — it consumes the structured user_action (monthlyIncome=8000); the receipt is a rendering projection. Mechanisms: ONE canonical event type (consumers may READ less, never CREATE a smaller semantic copy — 'type ExtractorTurn = &#123;role,text&#125;' IS the provenance-destroying boundary) · the extractor accepts only FactEvidence = HumanUtterance | HumanStructuredAction behind one policy boundary toFactEvidence() · discriminated union + assertNever so a new kind breaks adapters at compile time · boundary validation that REJECTS missing provenance ('authorKind = payload.authorKind ?? human' is catastrophic; absence must FAIL CLOSED) · author_kind/event_kind NOT NULL with NO human default · and ONE SENTINEL CONTRACT TEST through the whole real path asserting the receipt is ABSENT from extraction evidence while the structured action is PRESENT. — THE SHAPE FOR THE PROVENANCE FIX (second opinion 2026-09-10, do NOT build the boolean): provenance belongs in the EVENT IDENTITY, not as optional metadata. A human tap and a product-rendered sentence are DIFFERENT EVENT CLASSES: utterance&#123;author&#125; | user_action&#123;author:human,action,value&#125; | action_receipt&#123;author:product,causedByEventId,text&#125;. ⭐ The fact extractor should NEVER read the receipt sentence — it consumes the structured user_action (monthlyIncome=8000); the receipt is a rendering projection. Mechanisms: ONE canonical event type (consumers may READ less, never CREATE a smaller semantic copy — 'type ExtractorTurn = &#123;role,text&#125;' IS the provenance-destroying boundary) · the extractor accepts only FactEvidence = HumanUtterance | HumanStructuredAction behind one policy boundary toFactEvidence() · discriminated union + assertNever so a new kind breaks adapters at compile time · boundary validation that REJECTS missing provenance ('authorKind = payload.authorKind ?? human' is catastrophic; absence must FAIL CLOSED) · author_kind/event_kind NOT NULL with NO human default · and ONE SENTINEL CONTRACT TEST through the whole real path asserting the receipt is ABSENT from extraction evidence while the structured action is PRESENT. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] METHODOLOGY (supersedes 'census by string'): CENSUS THE VIOLATED INVARIANT, NOT THE KNOWN MANIFESTATION — artifact searches are DETECTORS, not class definitions. A tree-wide search for one string establishes every producer OF THAT STRING; it does not establish every producer of product-authored content masquerading as user speech ('I make about $8,000 monthly' is the same disease and matches nothing we searched). WRITE THE TARGET CLASS BEFORE CHOOSING THE QUERY. Trace BOTH directions: sink -> instances -> producers, AND producer -> all emissions -> sinks. State coverage as: Observed / Frame / Guarantee (exact WITHIN FRAME, therefore >=N system-wide) / Unknown — and distinguish lower-bound, frame-exact and population-exact, with population-exact expensive to claim. ⭐ ORG RULE: a builder may certify 'producer P is clean'; it may NOT promote that to 'the defect is gone' without a class-level postcondition owned by a SEPARATE lane — a lane's context boundary becomes an accidental epistemic boundary. — METHODOLOGY (supersedes 'census by string'): CENSUS THE VIOLATED INVARIANT, NOT THE KNOWN MANIFESTATION — artifact searches are DETECTORS, not class definitions. A tree-wide search for one string establishes every producer OF THAT STRING; it does not establish every producer of product-authored content masquerading as user speech ('I make about $8,000 monthly' is the same disease and matches nothing we searched). WRITE THE TARGET CLASS BEFORE CHOOSING THE QUERY. Trace BOTH directions: sink -> instances -> producers, AND producer -> all emissions -> sinks. State coverage as: Observed / Frame / Guarantee (exact WITHIN FRAME, therefore >=N system-wide) / Unknown — and distinguish lower-bound, frame-exact and population-exact, with population-exact expensive to claim. ⭐ ORG RULE: a builder may certify 'producer P is clean'; it may NOT promote that to 'the defect is gone' without a class-level postcondition owned by a SEPARATE lane — a lane's context boundary becomes an accidental epistemic boundary. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔⛔ actor_role IS HARDCODED TO 'lead' FOR 100% OF INBOUND WEBSITE_CHAT ROWS — every row, any conversation, and it PREDATES all of the 2026-09-09 work. The CRM's own canonical 'who is this' column already asserts the buyer said it, unconditionally. This is the load-bearing fact under all of S-12: the provenance defect is OLDER and WIDER than the receipt wording. ⚠ CONSEQUENCE FOR THE FIX: threading a new discriminator through hl_messages.metadata while actor_role still says 'lead' leaves TWO columns disagreeing about authorship on the same row, and the next reader believes whichever they find first. Measured by lane-thread-gate 2026-09-10. — ⛔⛔ actor_role IS HARDCODED TO 'lead' FOR 100% OF INBOUND WEBSITE_CHAT ROWS — every row, any conversation, and it PREDATES all of the 2026-09-09 work. The CRM's own canonical 'who is this' column already asserts the buyer said it, unconditionally. This is the load-bearing fact under all of S-12: the provenance defect is OLDER and WIDER than the receipt wording. ⚠ CONSEQUENCE FOR THE FIX: threading a new discriminator through hl_messages.metadata while actor_role still says 'lead' leaves TWO columns disagreeing about authorship on the same row, and the next reader believes whichever they find first. Measured by lane-thread-gate 2026-09-10. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] DE-RISKING FACT (recorded, deliberately not acted on): hl_messages already has metadata jsonb NOT NULL, populated today for inbound website_chat rows with &#123;"surface":"site"&#125; — so a provenance marker can ride as an additive JSON key with NO DDL and no desk DDL signature. The eventual S-12 build is therefore cheaper than feared. ⚠ It is still ~7 files across BOTH apps including logMessage, BEDROCK's ONE WRITER for hl_messages — blast radius, not schema risk, is what makes it a daylight change. ⚠ AND THE NAMING RAIL: an OPTIONAL authorKind re-creates the hazard the consultation named — 'missing flag = human speech, exactly backward'. The boundary must FAIL CLOSED on absence: a row with no authorKind is UNKNOWN, not human. — DE-RISKING FACT (recorded, deliberately not acted on): hl_messages already has metadata jsonb NOT NULL, populated today for inbound website_chat rows with &#123;"surface":"site"&#125; — so a provenance marker can ride as an additive JSON key with NO DDL and no desk DDL signature. The eventual S-12 build is therefore cheaper than feared. ⚠ It is still ~7 files across BOTH apps including logMessage, BEDROCK's ONE WRITER for hl_messages — blast radius, not schema risk, is what makes it a daylight change. ⚠ AND THE NAMING RAIL: an OPTIONAL authorKind re-creates the hazard the consultation named — 'missing flag = human speech, exactly backward'. The boundary must FAIL CLOSED on absence: a row with no authorKind is UNKNOWN, not human. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ SECOND PUSH BLOCKER (not the inngest-arm one): node tools/harness/activity-harness-gate.mjs exits 1 — the 'ai_send_blocked' proof is STALE. Traced by grind-myli-os-2: of its five write-path files exactly ONE moved past its receipt — apps/houseloop-crm/lib/ai/authorized-send.ts at 5e92a8da65, the AI-containment send road. ROUTE TO WHOEVER MOVED authorized-send.ts; they re-run 'node tools/harness/run-proofs.mjs ai_send_blocked'. ai_touch_held's own receipt is green and fresh (pass:true, 01:08:28Z, commit 8df01391e9). The silence-card item touched none of those five files. — ⛔ SECOND PUSH BLOCKER (not the inngest-arm one): node tools/harness/activity-harness-gate.mjs exits 1 — the 'ai_send_blocked' proof is STALE. Traced by grind-myli-os-2: of its five write-path files exactly ONE moved past its receipt — apps/houseloop-crm/lib/ai/authorized-send.ts at 5e92a8da65, the AI-containment send road. ROUTE TO WHOEVER MOVED authorized-send.ts; they re-run 'node tools/harness/run-proofs.mjs ai_send_blocked'. ai_touch_held's own receipt is green and fresh (pass:true, 01:08:28Z, commit 8df01391e9). The silence-card item touched none of those five files. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] SIGNED VOCABULARY (desk, 2026-09-10) for ai_touch_held: label 'AI Touch Held' -> 'AI Outreach Held' (key UNCHANGED); icon PauseCircle -> a clock glyph, colour #64748B unchanged (PauseCircle is the icon of STALLED/SUSPENDED — the exact misreading the sentence fix exists to prevent) ⚠ the copy desk had NO SCREENSHOT and read the icon off the catalog row — VERIFY IN PIXELS before ticking. RULED: the 'nothing scheduled' case renders 'Nothing scheduled yet' — the composer's signed 'a blank beats a guess' governs UNKNOWNS, and a MEASURED ZERO is not an unknown. HELD: the sentence 'held off' -> 'held off on reaching out' does NOT ship until the width cage is re-run against the LONGEST actor string ('HouseLoop automation held off on reaching out'); mig 1318 measured only the short one. CARRIED: the Next-touch stamp's false precision needs a day-grain option on fmtBusinessShortStamp. — SIGNED VOCABULARY (desk, 2026-09-10) for ai_touch_held: label 'AI Touch Held' -> 'AI Outreach Held' (key UNCHANGED); icon PauseCircle -> a clock glyph, colour #64748B unchanged (PauseCircle is the icon of STALLED/SUSPENDED — the exact misreading the sentence fix exists to prevent) ⚠ the copy desk had NO SCREENSHOT and read the icon off the catalog row — VERIFY IN PIXELS before ticking. RULED: the 'nothing scheduled' case renders 'Nothing scheduled yet' — the composer's signed 'a blank beats a guess' governs UNKNOWNS, and a MEASURED ZERO is not an unknown. HELD: the sentence 'held off' -> 'held off on reaching out' does NOT ship until the width cage is re-run against the LONGEST actor string ('HouseLoop automation held off on reaching out'); mig 1318 measured only the short one. CARRIED: the Next-touch stamp's false precision needs a day-grain option on fmtBusinessShortStamp. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] OPEN QUESTION — the 360px credit-row clipping DOES NOT REPRODUCE and the desk cannot yet say why. Measured by lane-render-contract at 360x740: pane bottom 603 · composerTop 603 · canScrollMore 0 · lowest pill bottom 583 — twenty pixels of clearance INSIDE the pane, pane already at max scroll, all four tiles visible, confirmed by eye. And composerTop === pane.bottom settles the structure by measurement: the composer is a SIBLING BELOW the pane and cannot overlay pane content, so 'clipped by the composer' was never the mechanism (that was the desk's framing, relayed from a description of what the eye saw — symptom-layer anchoring). TWO EXPLANATIONS, NEITHER CLAIMED: (a) FIXED BY SIDE EFFECT — the tile change makes the set two 58px rows instead of four full-width rows, ~76px less vertical, far more than the 20px now measured; closes as fixed-by-side-effect and wants a RE-WALK not a re-fix. (b) DIFFERENT VIEWPORT — the original report puts the pane bottom near 700 where the lane measures 603; then the defect is real and the lane measured the wrong geometry. ⭐ RESOLVED BY TWO FACTS FROM THE JUDGE'S WALK: its viewport HEIGHT, and whether its build carried data-pill-layout='tile-2'. Do not reconstruct them from memory. — OPEN QUESTION — the 360px credit-row clipping DOES NOT REPRODUCE and the desk cannot yet say why. Measured by lane-render-contract at 360x740: pane bottom 603 · composerTop 603 · canScrollMore 0 · lowest pill bottom 583 — twenty pixels of clearance INSIDE the pane, pane already at max scroll, all four tiles visible, confirmed by eye. And composerTop === pane.bottom settles the structure by measurement: the composer is a SIBLING BELOW the pane and cannot overlay pane content, so 'clipped by the composer' was never the mechanism (that was the desk's framing, relayed from a description of what the eye saw — symptom-layer anchoring). TWO EXPLANATIONS, NEITHER CLAIMED: (a) FIXED BY SIDE EFFECT — the tile change makes the set two 58px rows instead of four full-width rows, ~76px less vertical, far more than the 20px now measured; closes as fixed-by-side-effect and wants a RE-WALK not a re-fix. (b) DIFFERENT VIEWPORT — the original report puts the pane bottom near 700 where the lane measures 603; then the defect is real and the lane measured the wrong geometry. ⭐ RESOLVED BY TWO FACTS FROM THE JUDGE'S WALK: its viewport HEIGHT, and whether its build carried data-pill-layout='tile-2'. Do not reconstruct them from memory. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] CORRECTION to the benchmark's 360 reading: it reported 'all four stack' — the fact that made 360 read INTENTIONAL and on which the judge scored VISUAL down 4/5 -> 3/5. Measured by lane-render-contract: THREE of four stack; 'Fair 640-699' holds one line at 133px. The conclusion survives (uniform boxes, centred text, reads as designed) but the count does not — and the corrected ratio explains the finding BETTER: one-of-four stacking at 390 reads accidental, three-of-four at 360 reads intentional. ALSO BUILT: the closer row at 360 is layout=column with data-column-wraps=true, the long money label ('All metro homes around $2,898/mo' at 274px) taking the DESIGNED two-line treatment at 55px while the other three stay content-sized and single-line (192.6 / 221.7 / 156.7px) — the accidental break is now a designed one and nothing equalises the column. — CORRECTION to the benchmark's 360 reading: it reported 'all four stack' — the fact that made 360 read INTENTIONAL and on which the judge scored VISUAL down 4/5 -> 3/5. Measured by lane-render-contract: THREE of four stack; 'Fair 640-699' holds one line at 133px. The conclusion survives (uniform boxes, centred text, reads as designed) but the count does not — and the corrected ratio explains the finding BETTER: one-of-four stacking at 390 reads accidental, three-of-four at 360 reads intentional. ALSO BUILT: the closer row at 360 is layout=column with data-column-wraps=true, the long money label ('All metro homes around $2,898/mo' at 274px) taking the DESIGNED two-line treatment at 55px while the other three stay content-sized and single-line (192.6 / 221.7 / 156.7px) — the accidental break is now a designed one and nothing equalises the column. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⭐ DECISIVE TIMESTAMP for the 360 clipping question: data-pill-layout='tile-2' first entered the tree at 6f1119324e, 2026-09-09 23:17:44Z (git log -S on MyliPillRow.tsx). judge-qa-bench-r3's walk ran 23:06:07Z -> ~23:12Z at 390x844, so it was PRE-TILE with NO chunk-staleness caveat available in either direction — the code did not exist yet, so it cannot have been in its loaded chunk. This is the one reading all evening where the hot-reload confound cannot apply. ⚠ The judge never drove 360 at all; the y=683/composer~700 figures are lane-chatgpt-benchmark's. IF that lane's Row 0 run also predates 23:17:44Z, hypothesis (a) fixed-by-side-effect gets much stronger with nobody re-driving. — ⭐ DECISIVE TIMESTAMP for the 360 clipping question: data-pill-layout='tile-2' first entered the tree at 6f1119324e, 2026-09-09 23:17:44Z (git log -S on MyliPillRow.tsx). judge-qa-bench-r3's walk ran 23:06:07Z -> ~23:12Z at 390x844, so it was PRE-TILE with NO chunk-staleness caveat available in either direction — the code did not exist yet, so it cannot have been in its loaded chunk. This is the one reading all evening where the hot-reload confound cannot apply. ⚠ The judge never drove 360 at all; the y=683/composer~700 figures are lane-chatgpt-benchmark's. IF that lane's Row 0 run also predates 23:17:44Z, hypothesis (a) fixed-by-side-effect gets much stronger with nobody re-driving. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ WITHDRAWN — 'the 360px credit row is CLIPPED BY THE COMPOSER' is NOT a defect and never was. RESOLVED 2026-09-10 by lane-chatgpt-benchmark against itself: its run drove the conversation at 390 and RESIZED to 360 MID-QUESTION, never scrolling after the reflow. A native-360 run auto-scrolls the new question into view, which is exactly what lane-render-contract measured (pane bottom 603 · composerTop 603 · canScrollMore 0 · lowest pill bottom 583, twenty px of clearance). The y=683 figure is where the tiles sat in a STALE SCROLL POSITION. ⚠ AND THE MECHANISM WAS NEVER MEASURED: 'composer topping out ~700' was EYEBALLED off a screenshot; composerTop === pane.bottom proves the composer is a SIBLING BELOW the pane and cannot overlay its content, so the named mechanism was structurally impossible. canScrollMore was never recorded, so that run's own data cannot say whether the pane could have scrolled. Timestamps kill the fixed-by-side-effect theory too: the 360 screenshot is 23:43:05Z, 25 min AFTER tile-2 landed at 23:17:44Z. ⭐ SURVIVING FROM THE SAME RUN, NOT WITHDRAWN: the closer-row wrap at 360 (width-driven reflow, gaps 52/52/68, both label sets — a 360-wide viewport wraps that label however you arrived at 360) and 'the 2x2 reads as an accident at 390' (about what the eye sees on one screen, independent of scroll). — ⛔ WITHDRAWN — 'the 360px credit row is CLIPPED BY THE COMPOSER' is NOT a defect and never was. RESOLVED 2026-09-10 by lane-chatgpt-benchmark against itself: its run drove the conversation at 390 and RESIZED to 360 MID-QUESTION, never scrolling after the reflow. A native-360 run auto-scrolls the new question into view, which is exactly what lane-render-contract measured (pane bottom 603 · composerTop 603 · canScrollMore 0 · lowest pill bottom 583, twenty px of clearance). The y=683 figure is where the tiles sat in a STALE SCROLL POSITION. ⚠ AND THE MECHANISM WAS NEVER MEASURED: 'composer topping out ~700' was EYEBALLED off a screenshot; composerTop === pane.bottom proves the composer is a SIBLING BELOW the pane and cannot overlay its content, so the named mechanism was structurally impossible. canScrollMore was never recorded, so that run's own data cannot say whether the pane could have scrolled. Timestamps kill the fixed-by-side-effect theory too: the 360 screenshot is 23:43:05Z, 25 min AFTER tile-2 landed at 23:17:44Z. ⭐ SURVIVING FROM THE SAME RUN, NOT WITHDRAWN: the closer-row wrap at 360 (width-driven reflow, gaps 52/52/68, both label sets — a 360-wide viewport wraps that label however you arrived at 360) and 'the 2x2 reads as an accident at 390' (about what the eye sees on one screen, independent of scroll). <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] METHODOLOGY — A SCREENSHOT SHOWS YOU WHAT; IT DOESN'T SHOW YOU WHY, AND THE MECHANISM IS ALWAYS THE CLAIM. Written by lane-chatgpt-benchmark about itself after withdrawing an eyeballed mechanism: it caught THREE instrument errors in one evening by asking whether a reading was about the product or the harness, then filed a fourth because the picture looked obvious. ⚠ This is the same structural bias a consultation named about the DESK the same night — a supervising layer working from screenshots and walk reports is systematically biased toward presentation-layer diagnoses, because a screenshot 'disproportionately exposes things that have pixels'. ⭐ AND THE COMPANION RULE, from the same exchange: A RESIZE MID-RUN IS NOT A NATIVE RUN. Resizing is a legitimate instrument for WIDTH-driven questions (reflow, wrapping) and an invalid one for SCROLL-state questions, because nothing scrolls after the reflow and a real user never resizes their phone mid-question. — METHODOLOGY — A SCREENSHOT SHOWS YOU WHAT; IT DOESN'T SHOW YOU WHY, AND THE MECHANISM IS ALWAYS THE CLAIM. Written by lane-chatgpt-benchmark about itself after withdrawing an eyeballed mechanism: it caught THREE instrument errors in one evening by asking whether a reading was about the product or the harness, then filed a fourth because the picture looked obvious. ⚠ This is the same structural bias a consultation named about the DESK the same night — a supervising layer working from screenshots and walk reports is systematically biased toward presentation-layer diagnoses, because a screenshot 'disproportionately exposes things that have pixels'. ⭐ AND THE COMPANION RULE, from the same exchange: A RESIZE MID-RUN IS NOT A NATIVE RUN. Resizing is a legitimate instrument for WIDTH-driven questions (reflow, wrapping) and an invalid one for SCROLL-state questions, because nothing scrolls after the reflow and a real user never resizes their phone mid-question. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔⛔ IT ALREADY CROSSED — FIFTEEN PRODUCT-COMPOSED SENTENCES ARE FILED ON REAL LEADS IN THE CRM AS LEAD TESTIMONY. hl_messages, direction='inbound', actor_role='lead': 'About $7,917 a month before taxes' (2026-09-09 07:53:14+00), 'About $8,000 a month before taxes' (09-09 04:09:45), 'About $16,750 a month before taxes' (09-08 11:51:41) — 15 rows, oldest 2026-09-04 18:05:23, newest 2026-09-09 07:53:14. CONTROL: outbound rows on the same channel carry actor_role='ai_agent' (507), so the column DISCRIMINATES — this is not 'everything is lead'. 15 against 622 in the web store means only the LEAD-ATTACHED subset crossed, which is the subset that matters: it is what a human and the fact extractor read as the lead's own words. IT IS THE SYSTEM OF RECORD. The census established the MECHANISM; these are the ROWS, and nobody had measured them until judge-qa-bench-r3 went looking 2026-09-10. — ⛔⛔ IT ALREADY CROSSED — FIFTEEN PRODUCT-COMPOSED SENTENCES ARE FILED ON REAL LEADS IN THE CRM AS LEAD TESTIMONY. hl_messages, direction='inbound', actor_role='lead': 'About $7,917 a month before taxes' (2026-09-09 07:53:14+00), 'About $8,000 a month before taxes' (09-09 04:09:45), 'About $16,750 a month before taxes' (09-08 11:51:41) — 15 rows, oldest 2026-09-04 18:05:23, newest 2026-09-09 07:53:14. CONTROL: outbound rows on the same channel carry actor_role='ai_agent' (507), so the column DISCRIMINATES — this is not 'everything is lead'. 15 against 622 in the web store means only the LEAD-ATTACHED subset crossed, which is the subset that matters: it is what a human and the fact extractor read as the lead's own words. IT IS THE SYSTEM OF RECORD. The census established the MECHANISM; these are the ROWS, and nobody had measured them until judge-qa-bench-r3 went looking 2026-09-10. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] S-17 — THE ACTOR FIX IS THE CHEAPEST IT WILL EVER BE, AND THE CENSUS MISSED WHY: actor_role is is_nullable YES with NO default and ALREADY holds four values in live use (lead 18,217 · system 1,894 · human 1,187 · ai_agent 909). THERE IS NO SCHEMA OBSTACLE to stamping the true actor — no migration, no DDL signature. The whole fix is the call site at route.ts:145 which HARDCODES actorRole:'lead'. Found by judge-qa-bench-r3 2026-09-10 while probing the crossing. — S-17 — THE ACTOR FIX IS THE CHEAPEST IT WILL EVER BE, AND THE CENSUS MISSED WHY: actor_role is is_nullable YES with NO default and ALREADY holds four values in live use (lead 18,217 · system 1,894 · human 1,187 · ai_agent 909). THERE IS NO SCHEMA OBSTACLE to stamping the true actor — no migration, no DDL signature. The whole fix is the call site at route.ts:145 which HARDCODES actorRole:'lead'. Found by judge-qa-bench-r3 2026-09-10 while probing the crossing. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] S-16 REMEDIATION (desk-signed, NOT a lane's call) — the 15 crossed hl_messages rows need their actor_role corrected. ⚠ RULE #0 v2: dump to ~/hl-reversals/ BEFORE any UPDATE · through the ONE WRITER (logMessage), never raw SQL · desk-signed. Distinct from the >=622 web-store rows, which stay untouched pending Zach. These 15 are the subset that reached the system of record. — S-16 REMEDIATION (desk-signed, NOT a lane's call) — the 15 crossed hl_messages rows need their actor_role corrected. ⚠ RULE #0 v2: dump to ~/hl-reversals/ BEFORE any UPDATE · through the ONE WRITER (logMessage), never raw SQL · desk-signed. Distinct from the >=622 web-store rows, which stay untouched pending Zach. These 15 are the subset that reached the system of record. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] S-18 (instrument, filed by the judge against itself) — A FEATURE'S NAME IS NOT ITS STORED VALUE, AND A ZERO FROM A GUESSED LITERAL IS A BROKEN QUERY, NEVER A FINDING. The judge probed channel='website_chat' (what the CODE calls the feature), got 0 rows, and was one step from filing a false 'this path never wrote' finding. The CRM files these under channel:'dashboard' (route.ts:153). Its third instrument error of the night and, by its own account, the worst — because it had just written S-13 against exactly this shape. Before any census keyed on a string literal: read the WRITER to learn the stored value. — S-18 (instrument, filed by the judge against itself) — A FEATURE'S NAME IS NOT ITS STORED VALUE, AND A ZERO FROM A GUESSED LITERAL IS A BROKEN QUERY, NEVER A FINDING. The judge probed channel='website_chat' (what the CODE calls the feature), got 0 rows, and was one step from filing a false 'this path never wrote' finding. The CRM files these under channel:'dashboard' (route.ts:153). Its third instrument error of the night and, by its own account, the worst — because it had just written S-13 against exactly this shape. Before any census keyed on a string literal: read the WRITER to learn the stored value. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] LEAD CLOSED (dead) — the 'Didn't send' failure is NOT a harness wait. lane-render-contract's driver waits on the CONTROL, not on text: waitForSelector on the chip's own selector, then waitTurnIdle() which is STATE-based (polls turnLive/sendBlocked, returns only after seeing a turn start AND finish), then reads the chip's innerText BEFORE clicking — positive proof the control was mounted and populated. grep for any text-settle helper across both drivers: zero hits. So the product's 'Didn't send' was its own copy over a REAL failed send. ⭐ AND THE TWO-HALVES READING IS WHAT THE EVIDENCE SUPPORTS: the trace named exactly two branches and the drives found one each — render-contract hit the branch that PAINTS a floor (transport rejection or watchdog); the stuck-turn run hit the ORPHANED TURN, which paints nothing at all. Same defect family, two terminal states, and data-floor is still uncaptured on the first. ⚠ LIMIT NAMED: rc-verify.mjs mtime 19:30:54 is later than that lane's receipt (~19:18), so this reads the driver AS IT STANDS NOW; a text-settle would have had to exist and then be removed — possible, unevidenced, asserted neither way. — LEAD CLOSED (dead) — the 'Didn't send' failure is NOT a harness wait. lane-render-contract's driver waits on the CONTROL, not on text: waitForSelector on the chip's own selector, then waitTurnIdle() which is STATE-based (polls turnLive/sendBlocked, returns only after seeing a turn start AND finish), then reads the chip's innerText BEFORE clicking — positive proof the control was mounted and populated. grep for any text-settle helper across both drivers: zero hits. So the product's 'Didn't send' was its own copy over a REAL failed send. ⭐ AND THE TWO-HALVES READING IS WHAT THE EVIDENCE SUPPORTS: the trace named exactly two branches and the drives found one each — render-contract hit the branch that PAINTS a floor (transport rejection or watchdog); the stuck-turn run hit the ORPHANED TURN, which paints nothing at all. Same defect family, two terminal states, and data-floor is still uncaptured on the first. ⚠ LIMIT NAMED: rc-verify.mjs mtime 19:30:54 is later than that lane's receipt (~19:18), so this reads the driver AS IT STANDS NOW; a text-settle would have had to exist and then be removed — possible, unevidenced, asserted neither way. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⭐ A COVERAGE TABLE THAT ONLY EVER COUNTS UP IS A SCOREBOARD, NOT A RATCHET. lane-chatgpt-benchmark moved Q1's slider-commit path OPEN -> DRIVEN (FAIL), 9 -> 10 of 82, and recorded a row moving BACKWARDS in confidence in the same pass (its 'clipped by the composer' withdrawn then positively disproven). THREE THINGS IT REFUSED TO LET THE TABLE CLAIM: (1) the twelve closer pills stay RENDERED not DRIVEN — 'measuring a ROW is not driving a PILL'; we know what the row looks like and still know nothing about where any of those twelve go; same for the credit tile row. (2) Q2's commit tap stays OPEN even though its worst property is fixed — the card arriving EMPTY instead of $0 is a real green, recorded as one, but nobody has tapped the button so the row does not move. (3) The retraction is recorded beside the advance. ⚠ THE STANDING NUMBER: 10 driven of 82 after a full shift. — ⭐ A COVERAGE TABLE THAT ONLY EVER COUNTS UP IS A SCOREBOARD, NOT A RATCHET. lane-chatgpt-benchmark moved Q1's slider-commit path OPEN -> DRIVEN (FAIL), 9 -> 10 of 82, and recorded a row moving BACKWARDS in confidence in the same pass (its 'clipped by the composer' withdrawn then positively disproven). THREE THINGS IT REFUSED TO LET THE TABLE CLAIM: (1) the twelve closer pills stay RENDERED not DRIVEN — 'measuring a ROW is not driving a PILL'; we know what the row looks like and still know nothing about where any of those twelve go; same for the credit tile row. (2) Q2's commit tap stays OPEN even though its worst property is fixed — the card arriving EMPTY instead of $0 is a real green, recorded as one, but nobody has tapped the button so the row does not move. (3) The retraction is recorded beside the advance. ⚠ THE STANDING NUMBER: 10 driven of 82 after a full shift. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] STATUS LINES — DETERMINISTIC, EVENT-BOUND, NO MODEL (ruling #257.408, supersedes the Haiku proposals). A fixed map from execution events (answer_received · answer_validated · affordability_calculation_started · affordability_calculation_completed · response_generation_started · …) to sentences; ONE stable line per stage at phone width; a line is shown because its event fired, never because its facts are known. The REQUIRES fact-gate machinery is KEPT and re-pointed at execution events. 'No dead space' is satisfied by truthful lines where work happens; genuine silence is the LATENCY card, not narration. Needs the orchestrator to emit the event stream first. — STATUS LINES — DETERMINISTIC, EVENT-BOUND, NO MODEL (ruling #257.408, supersedes the Haiku proposals). A fixed map from execution events (answer_received · answer_validated · affordability_calculation_started · affordability_calculation_completed · response_generation_started · …) to sentences; ONE stable line per stage at phone width; a line is shown because its event fired, never because its facts are known. The REQUIRES fact-gate machinery is KEPT and re-pointed at execution events. 'No dead space' is satisfied by truthful lines where work happens; genuine silence is the LATENCY card, not narration. Needs the orchestrator to emit the event stream first. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ ENVIRONMENT INCIDENT 2026-09-10 ~09:55 CT — ~/Library/Caches/ms-playwright/ was DELETED (chromium-1208, chromium_headless_shell-1208, ffmpeg-1011). Not disk pressure (109Gi free), not memory (65% free). Cause UNKNOWN — no guess filed. Being restored by npx playwright install. ⚠ THE PART THAT MATTERS: the system ffmpeg on this Mac is BROKEN (libvpx.12.dylib missing), so Playwright's bundled ffmpeg-1011 was the ONLY working frame extractor — without it 'video'd and watched by the judge' cannot happen. Zach's standing order depends on a binary in an unguarded cache dir. FIX: pin the browsers via PLAYWRIGHT_BROWSERS_PATH to a path inside the repo's tooling (or ~/hl-tooling) that no cache-clean touches, AND repair the system ffmpeg (brew reinstall libvpx ffmpeg) so there are two extractors, not one. — ⛔ ENVIRONMENT INCIDENT 2026-09-10 ~09:55 CT — ~/Library/Caches/ms-playwright/ was DELETED (chromium-1208, chromium_headless_shell-1208, ffmpeg-1011). Not disk pressure (109Gi free), not memory (65% free). Cause UNKNOWN — no guess filed. Being restored by npx playwright install. ⚠ THE PART THAT MATTERS: the system ffmpeg on this Mac is BROKEN (libvpx.12.dylib missing), so Playwright's bundled ffmpeg-1011 was the ONLY working frame extractor — without it 'video'd and watched by the judge' cannot happen. Zach's standing order depends on a binary in an unguarded cache dir. FIX: pin the browsers via PLAYWRIGHT_BROWSERS_PATH to a path inside the repo's tooling (or ~/hl-tooling) that no cache-clean touches, AND repair the system ffmpeg (brew reinstall libvpx ffmpeg) so there are two extractors, not one. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ THE AI TAB DROPS THE REASON — a SECOND PROJECTION loses substance for 28 catalog kinds. CenterLane.tsx:6790-6793 (MyliSubLines) renders only &#123;sentence&#125;+&#123;title&#125;; title and body are NULL on every ai_touch_held row and the reason lives only in meta.holdReason, which reaches the surface as the resolver's substance[] that this component never reads. Its own header states the failing assumption ('title is its substance' — true for the fact/memory rows it was written against). On the AI tab the hold renders as a bare grey 'Myli held off' — no reason, no timestamp — line 20 of 30 memory-trace rows under a chat card headed Sep 8 9:18 PM (the hold is Sep 9 2:12 PM); on the ALL tab the same row is a correct full card. One row, two meanings, same page. 28 kinds carry substance lines; all lose them here; ai_touch_held is where the loss becomes a LIE because control D refuses to WRITE an unexplained hold while this projection makes an unexplained hold the thing a human READS. WHY SIX GREEN ARMS MISSED IT: every arm asserts the composer's output and the All-tab render; none asserts what a second projection does with the row. BEDROCK layer 5: every surface renders through resolveCard. Found by judge-myli-os-2 2026-09-10 on :3020. — ⛔ THE AI TAB DROPS THE REASON — a SECOND PROJECTION loses substance for 28 catalog kinds. CenterLane.tsx:6790-6793 (MyliSubLines) renders only &#123;sentence&#125;+&#123;title&#125;; title and body are NULL on every ai_touch_held row and the reason lives only in meta.holdReason, which reaches the surface as the resolver's substance[] that this component never reads. Its own header states the failing assumption ('title is its substance' — true for the fact/memory rows it was written against). On the AI tab the hold renders as a bare grey 'Myli held off' — no reason, no timestamp — line 20 of 30 memory-trace rows under a chat card headed Sep 8 9:18 PM (the hold is Sep 9 2:12 PM); on the ALL tab the same row is a correct full card. One row, two meanings, same page. 28 kinds carry substance lines; all lose them here; ai_touch_held is where the loss becomes a LIE because control D refuses to WRITE an unexplained hold while this projection makes an unexplained hold the thing a human READS. WHY SIX GREEN ARMS MISSED IT: every arm asserts the composer's output and the All-tab render; none asserts what a second projection does with the row. BEDROCK layer 5: every surface renders through resolveCard. Found by judge-myli-os-2 2026-09-10 on :3020. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] RECORD — the desk's NINTH unverified mechanism in 24h, and the lane caught it by measuring first: the SRG DNS brief said 'a stale A record 69.46.46.21 is fighting the Railway CNAME — remove it.' FALSE. That A was Porkbun's ALIAS flattening the live Railway IP at query time (ipinfo: AS400940 Railway). The zone was already correct. The REAL cause: Railway requires a SEPARATE _railway-verify TXT ownership-proof record per custom domain, distinct from the traffic CNAME/ALIAS; both SRG domains had sat in VALIDATING_OWNERSHIP since being recreated 2026-09-09 18:51Z because those TXTs never existed. houseloop.ai (working) was the control — its TXT exists and matches. Yesterday's support-ticket draft missed it for the same reason (checked the traffic CNAME only). Fix: two additive TXT records; apex cert in ~2 min. Had the lane followed the brief it would have deleted a record that was doing nothing wrong. Backup: ~/hl-reversals/srg-dns-backup-2026-09-10.json. — RECORD — the desk's NINTH unverified mechanism in 24h, and the lane caught it by measuring first: the SRG DNS brief said 'a stale A record 69.46.46.21 is fighting the Railway CNAME — remove it.' FALSE. That A was Porkbun's ALIAS flattening the live Railway IP at query time (ipinfo: AS400940 Railway). The zone was already correct. The REAL cause: Railway requires a SEPARATE _railway-verify TXT ownership-proof record per custom domain, distinct from the traffic CNAME/ALIAS; both SRG domains had sat in VALIDATING_OWNERSHIP since being recreated 2026-09-09 18:51Z because those TXTs never existed. houseloop.ai (working) was the control — its TXT exists and matches. Yesterday's support-ticket draft missed it for the same reason (checked the traffic CNAME only). Fix: two additive TXT records; apex cert in ~2 min. Had the lane followed the brief it would have deleted a record that was doing nothing wrong. Backup: ~/hl-reversals/srg-dns-backup-2026-09-10.json. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] MORNING BRIEF GAP — '6 · The push' only measures prod-vs-origin freshness (both apps read '0h behind', which is true) and NEVER surfaces that local HEAD is ~2,466 commits ahead of origin, unpushed. It read 'caught up' on a morning when production was a full shift stale. FIX: add a local-HEAD-vs-origin probe to tools/pm/projection.mjs so the brief shows 'N commits unpushed since &lt;last push time>'. Flagged by lane-morning-brief 2026-09-10 rather than freelanced (a real code change). ALSO: the generator is tools/boards/desk-boards.mjs — tools/morning-briefing.mjs is a RETIRED WhatsApp script; the desk's brief named the wrong one (tenth unverified mechanism in 24h). — MORNING BRIEF GAP — '6 · The push' only measures prod-vs-origin freshness (both apps read '0h behind', which is true) and NEVER surfaces that local HEAD is ~2,466 commits ahead of origin, unpushed. It read 'caught up' on a morning when production was a full shift stale. FIX: add a local-HEAD-vs-origin probe to tools/pm/projection.mjs so the brief shows 'N commits unpushed since &lt;last push time>'. Flagged by lane-morning-brief 2026-09-10 rather than freelanced (a real code change). ALSO: the generator is tools/boards/desk-boards.mjs — tools/morning-briefing.mjs is a RETIRED WhatsApp script; the desk's brief named the wrong one (tenth unverified mechanism in 24h). <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ 'Browse Leawood Right Now now' — a visible nonsense label on the 4b pivot row. The place was captured as 'Leawood Right Now' from her own sentence ('what's available in Leawood right now') and a template's 'now' appended. The 'big back' class (#257 8/27: a clipped pill reading as a nonsense word), on the control a buyer is most likely to tap. Root: an interpolated label that trusts a captured span as a place name. Fix: the place token must be the RESOLVED place (city/neighborhood from the search params), never a substring of prose. Row 4b video 2026-09-10. — ⛔ 'Browse Leawood Right Now now' — a visible nonsense label on the 4b pivot row. The place was captured as 'Leawood Right Now' from her own sentence ('what's available in Leawood right now') and a template's 'now' appended. The 'big back' class (#257 8/27: a clipped pill reading as a nonsense word), on the control a buyer is most likely to tap. Root: an interpolated label that trusts a captured span as a place name. Fix: the place token must be the RESOLVED place (city/neighborhood from the search params), never a substring of prose. Row 4b video 2026-09-10. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] THE PIVOT ROW OFFERS SIX CONTROLS WHERE HER SENTENCE OFFERS TWO — two exact duplicate intent pairs (Finish affordability / Finish my price range; Leawood market / Browse Leawood Right Now now). Her prose is correct ('I can run your affordability number OR give you a Leawood snapshot — which first?'); the row does not match it. Under #257.393 the RENDERER owns the option set, so this is the renderer disagreeing with the one author: the row must be derived from the SAME fork the prose names, one control per intent. Row 4b video 2026-09-10 (the benchmark's F4, confirmed). — THE PIVOT ROW OFFERS SIX CONTROLS WHERE HER SENTENCE OFFERS TWO — two exact duplicate intent pairs (Finish affordability / Finish my price range; Leawood market / Browse Leawood Right Now now). Her prose is correct ('I can run your affordability number OR give you a Leawood snapshot — which first?'); the row does not match it. Under #257.393 the RENDERER owns the option set, so this is the renderer disagreeing with the one author: the row must be derived from the SAME fork the prose names, one control per intent. Row 4b video 2026-09-10 (the benchmark's F4, confirmed). <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⭐ THE ONE-AUTHOR PROBLEM IN A COSTUME: on 4a she opens 'Yes — and I actually already covered that one!' — she had NOT; the only prior mention is HER OWN ACK seven seconds earlier in the same turn. The main author reads the ack as prior conversation and credits the buyer with having heard it; to a stranger it reads as being told off for asking something they never asked. EVIDENCE that the ack call is still a second author polluting the main author's context. Per #257.393 first paint is a deterministic state projection, not a model ack — this is the same defect from a new angle. Row 4a video 2026-09-10. — ⭐ THE ONE-AUTHOR PROBLEM IN A COSTUME: on 4a she opens 'Yes — and I actually already covered that one!' — she had NOT; the only prior mention is HER OWN ACK seven seconds earlier in the same turn. The main author reads the ack as prior conversation and credits the buyer with having heard it; to a stranger it reads as being told off for asking something they never asked. EVIDENCE that the ack call is still a second author polluting the main author's context. Per #257.393 first paint is a deterministic state projection, not a model ack — this is the same defect from a new angle. Row 4a video 2026-09-10. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee: the desk's eleventh unverified mechanism in 24h, and the first that would have reversed a win) — #257.409: RE-DIAGNOSED by judge-qa-bench-r3 from the frames: the ack IS a rendered message bubble in the transcript (above the 'Worked for Ns' divider, her avatar beside it, visible in 4a/02-q2 and 4b/03-after-pivot). So 'I actually already covered that one' is TRUE. The defect is REPETITION, not false memory: the main author correctly saw the ack had answered and answered AGAIN in near-identical words. The desk's card would have sent a builder to BLIND the main author to the ack — which restores the two-author disease five rounds were spent curing. Superseded by the correct card..*</sub>
- [ ] RATCHET ROW 4e — STATE/PROSE DRIFT WHEN THE PIVOT ARRIVES BEFORE Q1 IS ANSWERED: her prose asked for INCOME while the card beneath asked for DEBTS. The renderer counted the pivot as answering Q1 and advanced; her prose correctly did not. A path needing NO instrument failure to reach (a buyer who asks before answering). ⚠ PARTIALLY EVIDENCED: run 2 overwrote run 1's timeline and screenshots (same output dir — the driver's own error, named); the video survives and the transcript is quoted from console. Drive it DELIBERATELY before treating it as fully evidenced. Also ROW 4f: the interpolated-label class needs its own pre-registered row. — RATCHET ROW 4e — STATE/PROSE DRIFT WHEN THE PIVOT ARRIVES BEFORE Q1 IS ANSWERED: her prose asked for INCOME while the card beneath asked for DEBTS. The renderer counted the pivot as answering Q1 and advanced; her prose correctly did not. A path needing NO instrument failure to reach (a buyer who asks before answering). ⚠ PARTIALLY EVIDENCED: run 2 overwrote run 1's timeline and screenshots (same output dir — the driver's own error, named); the video survives and the transcript is quoted from console. Drive it DELIBERATELY before treating it as fully evidenced. Also ROW 4f: the interpolated-label class needs its own pre-registered row. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⭐ COUNT CORRECTION + BLAST-RADIUS CORRECTION on the CRM crossing: it is 21 rows, not 15 (same closed window — newest still 2026-09-09 07:53:14; the 15 was an undercount, the population has NOT grown). AND BOTH AFFECTED LEADS ARE is_test=TRUE (Zach Test, Riley Pastclient-TEST) — NOBODY REAL IS MISQUOTED. This lowers the blast radius the round-5 verdict assumed; it does NOT lower the forward defect's severity (the write path was live for real leads and only luck kept them out). Measured by grind-qa-bench-r6 2026-09-10 10:13:47Z. — ⭐ COUNT CORRECTION + BLAST-RADIUS CORRECTION on the CRM crossing: it is 21 rows, not 15 (same closed window — newest still 2026-09-09 07:53:14; the 15 was an undercount, the population has NOT grown). AND BOTH AFFECTED LEADS ARE is_test=TRUE (Zach Test, Riley Pastclient-TEST) — NOBODY REAL IS MISQUOTED. This lowers the blast radius the round-5 verdict assumed; it does NOT lower the forward defect's severity (the write path was live for real leads and only luck kept them out). Measured by grind-qa-bench-r6 2026-09-10 10:13:47Z. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ROUND 7 — ALIGN THE WIRE TO THE THREE ENUMS. Round 6 shipped authorKind as a REQUIRED binary (fail-closed on the wire — the hazard that mattered is closed). But hl_chat_events uses actor_role · origin · content_kind, and the buyer's own tap is actor_role:lead / origin:ui_confirmation / content_kind:structured_answer — a binary must call the buyer's tap 'product' to exclude it. Align WebsiteChatTurn to the three enums and carry source_event_id from the ledger. Also: the correction DOOR on the one writer — logMessage is INSERT-only (ON CONFLICT DO NOTHING) with no path to correct an existing row's actor_role; the first REAL lead needing correction will need it, and it must not become a raw-SQL habit. — ROUND 7 — ALIGN THE WIRE TO THE THREE ENUMS. Round 6 shipped authorKind as a REQUIRED binary (fail-closed on the wire — the hazard that mattered is closed). But hl_chat_events uses actor_role · origin · content_kind, and the buyer's own tap is actor_role:lead / origin:ui_confirmation / content_kind:structured_answer — a binary must call the buyer's tap 'product' to exclude it. Align WebsiteChatTurn to the three enums and carry source_event_id from the ledger. Also: the correction DOOR on the one writer — logMessage is INSERT-only (ON CONFLICT DO NOTHING) with no path to correct an existing row's actor_role; the first REAL lead needing correction will need it, and it must not become a raw-SQL habit. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⚠ NAMED RATCHET WITH A DEADLINE: loadHistory's read is fail-closed on the WIRE but compatibility-open on HISTORY — COALESCE(author_kind,'human') admits pre-existing rows, because tightening today would delete 503 genuinely typed website-chat lines to catch 21 bad ones. Sequence: S-16 stamps the whole population -> the default tightens to 'unknown' -> the predicate goes total. 'A green run here does NOT mean history is clean' (the lane's own words). DEADLINE: the tighten lands in round 7 or this is a hole with a nice name. — ⚠ NAMED RATCHET WITH A DEADLINE: loadHistory's read is fail-closed on the WIRE but compatibility-open on HISTORY — COALESCE(author_kind,'human') admits pre-existing rows, because tightening today would delete 503 genuinely typed website-chat lines to catch 21 bad ones. Sequence: S-16 stamps the whole population -> the default tightens to 'unknown' -> the predicate goes total. 'A green run here does NOT mean history is clean' (the lane's own words). DEADLINE: the tighten lands in round 7 or this is a hole with a nice name. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] WHERE THE FLAG ACTUALLY DIED (five rounds assumed elsewhere): apps/houseloop-web/lib/myli/outbound-messages.ts:38 flattened every turn to &#123;role, content, hidden?&#125; BEFORE the request left the browser — the server never saw actionReceipt. FOUR boundaries dropped provenance, not one: outbound flatten -> wire type -> intake hardcode -> loadHistory predicate. And the hl_messages row has TWO writers of which only one carried the flag. Fixed in round 6; recorded so the next census starts at the browser, not the server. — WHERE THE FLAG ACTUALLY DIED (five rounds assumed elsewhere): apps/houseloop-web/lib/myli/outbound-messages.ts:38 flattened every turn to &#123;role, content, hidden?&#125; BEFORE the request left the browser — the server never saw actionReceipt. FOUR boundaries dropped provenance, not one: outbound flatten -> wire type -> intake hardcode -> loadHistory predicate. And the hl_messages row has TWO writers of which only one carried the flag. Fixed in round 6; recorded so the next census starts at the browser, not the server. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] REPETITION AFTER A SUBSTANTIVE ACK (Row 4a): the ack bubble answered the aside ('lenders count them when they're figuring what you can borrow'); the main turn then said 'I actually already covered that one!' — TRUE, it had — and answered AGAIN in near-identical words. 'Already covered' does not create the defect, it ANNOUNCES it, and reads as chiding because the next sentence proves the repeat was unnecessary. THE FIX IS THE OTHER HALF OF ONE-AUTHOR: when the ack has already answered, the main turn ADVANCES instead of re-answering. ⛔ DO NOT blind the main author to the ack — that restores the two-author disease. ⚠ ROOT DESIGN CALL, not a lane's: should an ack be allowed to answer substantively at all? Under #257.393 first paint is a deterministic state projection, and a substantive ack is a second author by definition. — REPETITION AFTER A SUBSTANTIVE ACK (Row 4a): the ack bubble answered the aside ('lenders count them when they're figuring what you can borrow'); the main turn then said 'I actually already covered that one!' — TRUE, it had — and answered AGAIN in near-identical words. 'Already covered' does not create the defect, it ANNOUNCES it, and reads as chiding because the next sentence proves the repeat was unnecessary. THE FIX IS THE OTHER HALF OF ONE-AUTHOR: when the ack has already answered, the main turn ADVANCES instead of re-answering. ⛔ DO NOT blind the main author to the ack — that restores the two-author disease. ⚠ ROOT DESIGN CALL, not a lane's: should an ack be allowed to answer substantively at all? Under #257.393 first paint is a deterministic state projection, and a substantive ack is a second author by definition. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔⛔ THE PRE-FILL LANDS ON A REFUSAL TURN (Row 4c). Immediately after 'I'd rather not say' -> 'Got it — you'd rather skip that for now', the NEXT card renders 'Saved for a down payment: $20,000' PRE-FILLED, slider positioned, to an ANONYMOUS STRANGER on a fresh profile who has just declined to give a number. One tap of 'That's right' files 'About $20,000 saved for a down payment' as their own first-person statement — the exact string class measured >=622 times in the web store and 21 in the CRM. THE FLOW HONOURS THE REFUSAL IN HER WORDS AND REVERSES IT IN THE CONTROL, one tap wide, on the turn where the buyer just said no. Does not withdraw the 4c green (F3 was 'a refusal not accepted', and it was accepted). What is NEW is not the pre-fill — it is the pre-fill on a REFUSAL turn, where it stops being a convenience and becomes a way to obtain a number the buyer withheld. A refusal must produce an EMPTY control (Q2's empty state proves it is buildable). Found by judge-qa-bench-r3 from the 4c video. — ⛔⛔ THE PRE-FILL LANDS ON A REFUSAL TURN (Row 4c). Immediately after 'I'd rather not say' -> 'Got it — you'd rather skip that for now', the NEXT card renders 'Saved for a down payment: $20,000' PRE-FILLED, slider positioned, to an ANONYMOUS STRANGER on a fresh profile who has just declined to give a number. One tap of 'That's right' files 'About $20,000 saved for a down payment' as their own first-person statement — the exact string class measured >=622 times in the web store and 21 in the CRM. THE FLOW HONOURS THE REFUSAL IN HER WORDS AND REVERSES IT IN THE CONTROL, one tap wide, on the turn where the buyer just said no. Does not withdraw the 4c green (F3 was 'a refusal not accepted', and it was accepted). What is NEW is not the pre-fill — it is the pre-fill on a REFUSAL turn, where it stops being a convenience and becomes a way to obtain a number the buyer withheld. A refusal must produce an EMPTY control (Q2's empty state proves it is buildable). Found by judge-qa-bench-r3 from the 4c video. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] OPEN QUESTION FOR THE DRIVER (one line, it holds the DOM): at 390 the pivot row's right-hand column is cut off and 'Leawood market' — half of the binary she just offered — reads as 'Leawood marke'. Does that row scroll horizontally (overflow-x:auto, like the sibling row measured earlier)? If yes: benign, a deliberate scroller with the next pill part-visible as the affordance. If no: a defect. A still cannot decide it. ALSO a provenance correction: 'Browse Leawood Right Now now' is REAL (driver DOM measurement, 265px) but NOT VISIBLE in the captured frame — it reads 'Browse Leaw…' with the nonsense past the right edge; this corrects its RANKING (the duplicate pair is what a buyer meets first; the nonsense label is what they meet if they scroll), not its existence. — OPEN QUESTION FOR THE DRIVER (one line, it holds the DOM): at 390 the pivot row's right-hand column is cut off and 'Leawood market' — half of the binary she just offered — reads as 'Leawood marke'. Does that row scroll horizontally (overflow-x:auto, like the sibling row measured earlier)? If yes: benign, a deliberate scroller with the next pill part-visible as the affordance. If no: a defect. A still cannot decide it. ALSO a provenance correction: 'Browse Leawood Right Now now' is REAL (driver DOM measurement, 265px) but NOT VISIBLE in the captured frame — it reads 'Browse Leaw…' with the nonsense past the right edge; this corrects its RANKING (the duplicate pair is what a buyer meets first; the nonsense label is what they meet if they scroll), not its existence. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] LEDGER — WIRE THE TWO ROADS THAT PASS NO LEDGER: lib/myli-legacy/nlp-route.ts (6 call sites) and the dashboard opener road write NO evidence to hl_chat_events; opts.ledger is optional on purpose so they keep working. The history guard is a RATCHET — a conversation with no committed evidence cannot be refused (CONTROL D). Wire them or retire the legacy route. The dashboard opener is already row 2 of the four-road parent (#257.394). Named by lane-chat-ledger 2026-09-10. — LEDGER — WIRE THE TWO ROADS THAT PASS NO LEDGER: lib/myli-legacy/nlp-route.ts (6 call sites) and the dashboard opener road write NO evidence to hl_chat_events; opts.ledger is optional on purpose so they keep working. The history guard is a RATCHET — a conversation with no committed evidence cannot be refused (CONTROL D). Wire them or retire the legacy route. The dashboard opener is already row 2 of the four-road parent (#257.394). Named by lane-chat-ledger 2026-09-10. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔⛔ ADOPTION FINDING, NOT A SAFETY FINDING (judge S-21, 2026-09-10): every inbound website-chat row in hl_messages belongs to a TEST lead — 527 of 527, no is_test=false row at all. 'No real lead was misquoted' is TRUE ONLY BECAUSE NO REAL BUYER HAS EVER USED THE WEB CHAT THAT REACHES THE CRM. Nothing protected them; nobody arrived. It goes false the first day a real buyer taps that card. THIS IS A PRODUCT FACT ZACH SHOULD KNOW: the surface the whole night was spent hardening has zero real-world traffic into the system of record. (The judge's 23 vs the earlier 21 is its widened pattern — instrument changed, data did not.) — ⛔⛔ ADOPTION FINDING, NOT A SAFETY FINDING (judge S-21, 2026-09-10): every inbound website-chat row in hl_messages belongs to a TEST lead — 527 of 527, no is_test=false row at all. 'No real lead was misquoted' is TRUE ONLY BECAUSE NO REAL BUYER HAS EVER USED THE WEB CHAT THAT REACHES THE CRM. Nothing protected them; nobody arrived. It goes false the first day a real buyer taps that card. THIS IS A PRODUCT FACT ZACH SHOULD KNOW: the surface the whole night was spent hardening has zero real-world traffic into the system of record. (The judge's 23 vs the earlier 21 is its widened pattern — instrument changed, data did not.) <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] S-20 (BLOCKING) — SIGNED IS NOT APPLIED. The desk signed the S-16 bounded UPDATE; the judge then measured actor_role='lead' -> 22 rows, 'system' -> 1: twenty-two of twenty-three still read lead. Either the script has not run or it ran on one row and stopped. The desk REPORTED 'signed' as though it implied 'corrected'. Apply it with the after-probe pasted, or mark it PENDING in every place it was reported. A round reporting a signed correction must not be read as a corrected one. — S-20 (BLOCKING) — SIGNED IS NOT APPLIED. The desk signed the S-16 bounded UPDATE; the judge then measured actor_role='lead' -> 22 rows, 'system' -> 1: twenty-two of twenty-three still read lead. Either the script has not run or it ran on one row and stopped. The desk REPORTED 'signed' as though it implied 'corrected'. Apply it with the after-probe pasted, or mark it PENDING in every place it was reported. A round reporting a signed correction must not be read as a corrected one. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] SEND FAILURE REPRODUCED ON A PLAIN FLOW (Row 2 school-rating, turn 3): buyer sends 'Up to about $550,000.' (verified landed), she acks in full, 'Worked for 15s', then the floor notice 'That one didn't come through.' + Ask again. Per AiSearchModal.tsx:6376 that exact wording means the SERVER returned data.floor (not timeout/network) or a reply-less response (line 5235 'no-answer'). Four-minute repro: .claude/reports/grinder/qa-bench/BENCHMARK-ROW2-school-rating-2026-09-10.md. Hunt the server-side reason by name (the floor value + the route's log line), not the symptom. — SEND FAILURE REPRODUCED ON A PLAIN FLOW (Row 2 school-rating, turn 3): buyer sends 'Up to about $550,000.' (verified landed), she acks in full, 'Worked for 15s', then the floor notice 'That one didn't come through.' + Ask again. Per AiSearchModal.tsx:6376 that exact wording means the SERVER returned data.floor (not timeout/network) or a reply-less response (line 5235 'no-answer'). Four-minute repro: .claude/reports/grinder/qa-bench/BENCHMARK-ROW2-school-rating-2026-09-10.md. Hunt the server-side reason by name (the floor value + the route's log line), not the symptom. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] home_search status lines name PLACES the buyer never said: after the school-rating pill tap and before any buyer input, 'Checking school ratings in Overland Park / Olathe / Leawood' at t=6.60/7.05/7.50s. The &#123;city&#125; merge field cannot be known from a turn with no inputs. Same disease the REQUIRES gate closed for affordability; still open for home_search. Measured: Row 2 receipt + video. — home_search status lines name PLACES the buyer never said: after the school-rating pill tap and before any buyer input, 'Checking school ratings in Overland Park / Olathe / Leawood' at t=6.60/7.05/7.50s. The &#123;city&#125; merge field cannot be known from a turn with no inputs. Same disease the REQUIRES gate closed for affordability; still open for home_search. Measured: Row 2 receipt + video. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (desk) — lane:chatgpt-benchmark withdrew F2: the opener is a deliberate three-city metro survey; her prose names Olathe and Leawood in the same turn — the status lines narrated real work.*</sub>
- [ ] Buyer said 'strong' schools; she converted it to 'GreatSchools rating of 8 or higher' and filtered the whole search by a definition the buyer never gave (pill-sets.ts:160-161 lock, one layer down). She names the source (good); she must ask, or state the threshold as HER choice the buyer can change. Row 2 receipt. — Buyer said 'strong' schools; she converted it to 'GreatSchools rating of 8 or higher' and filtered the whole search by a definition the buyer never gave (pill-sets.ts:160-161 lock, one layer down). She names the source (good); she must ask, or state the threshold as HER choice the buyer can change. Row 2 receipt. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] MyliPillRow.tsx:210-233 ROW branch: CSS grid + whitespace-nowrap, no overflow-x, no scroll container — a label wider than its cell neither wraps nor scrolls, it CLIPS (the file's own words: 'invisible and broken'). Code reading; DOM confirmation needs one drive reaching a live multi-column row (Row 2's failure state carried no [data-pill-layout] row). — MyliPillRow.tsx:210-233 ROW branch: CSS grid + whitespace-nowrap, no overflow-x, no scroll container — a label wider than its cell neither wraps nor scrolls, it CLIPS (the file's own words: 'invisible and broken'). Code reading; DOM confirmation needs one drive reaching a live multi-column row (Row 2's failure state carried no [data-pill-layout] row). <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] Scroll yank is WORSENING row by row: Row 1 −125 · Row 4 −167/−182 · Row 2 −318 at 390px (largest measured anywhere). Filed against #257.390; the trend is the finding. — Scroll yank is WORSENING row by row: Row 1 −125 · Row 4 −167/−182 · Row 2 −318 at 390px (largest measured anywhere). Filed against #257.390; the trend is the finding. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] RESPONSE CONTRACT (second opinion 9/10, adopted): the nlp route's reply becomes a discriminated union — answered | failed&#123;reason_code, retryable&#125; | named alternate outcome (action_completed…). No reply-less success. Client records INVALID_SERVER_RESPONSE when a success carries neither, never invents 'no-answer'. After lane-send-failure lands the reason code on the wire. — RESPONSE CONTRACT (second opinion 9/10, adopted): the nlp route's reply becomes a discriminated union — answered | failed&#123;reason_code, retryable&#125; | named alternate outcome (action_completed…). No reply-less success. Client records INVALID_SERVER_RESPONSE when a success carries neither, never invents 'no-answer'. After lane-send-failure lands the reason code on the wire. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F4.2 (the status FSM) / F6 (claims); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] STATUS-LINE PROVENANCE (Row 2 F2 + second opinion 9/10): a status/progress template may interpolate only fields whose SOURCE is permitted for that template (current_user_input · explicit_ui_selection · accepted_durable_fact · current_tool_result); with no authorized &#123;city&#125; render the generic line ('Checking school information…'). Close home_search the way REQUIRES closed affordability. Red-prove with the Row 2 pill tap at t&lt;7s. — STATUS-LINE PROVENANCE (Row 2 F2 + second opinion 9/10): a status/progress template may interpolate only fields whose SOURCE is permitted for that template (current_user_input · explicit_ui_selection · accepted_durable_fact · current_tool_result); with no authorized &#123;city&#125; render the generic line ('Checking school information…'). Close home_search the way REQUIRES closed affordability. Red-prove with the Row 2 pill tap at t&lt;7s. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F4.2 (the status FSM) / F6 (claims); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] TRAFFIC CLASS AT THE CONVERSATION BOUNDARY (#257.411): conversation carries traffic_class ∈ &#123;test, organic, unknown&#125; + origin + test_run_id, stamped ONCE at create/bind FROM lead.disposition or the QA-walker origin, immutable; turns inherit; no is_test on rows. Backfill legacy as unknown. DDL at the desk; schema lock regenerated in the same commit. — TRAFFIC CLASS AT THE CONVERSATION BOUNDARY (#257.411): conversation carries traffic_class ∈ &#123;test, organic, unknown&#125; + origin + test_run_id, stamped ONCE at create/bind FROM lead.disposition or the QA-walker origin, immutable; turns inherit; no is_test on rows. Backfill legacy as unknown. DDL at the desk; schema lock regenerated in the same commit. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] NON-VACUOUS CONTROL (second opinion 9/10 Q6): an Inngest nightly emits web_chat test_rows / non_test_rows / unclassified_rows + control_state ∈ &#123;CONTROL_VALID, VACUOUS_NO_NONTEST, VACUOUS_NO_TEST, UNCLASSIFIED_PRESENT&#125;; the judge layer refuses a PASS on any test/non-test discrimination claim when either population is 0. Today it must read test=527 non_test=0 control_state=VACUOUS_NO_NONTEST, visibly. — NON-VACUOUS CONTROL (second opinion 9/10 Q6): an Inngest nightly emits web_chat test_rows / non_test_rows / unclassified_rows + control_state ∈ &#123;CONTROL_VALID, VACUOUS_NO_NONTEST, VACUOUS_NO_TEST, UNCLASSIFIED_PRESENT&#125;; the judge layer refuses a PASS on any test/non-test discrimination claim when either population is 0. Today it must read test=527 non_test=0 control_state=VACUOUS_NO_NONTEST, visibly. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] GUARD REPAIR PATH (second opinion 9/10 Q3): generate → grounding guard → ONE constrained regeneration with the exact violations supplied → guard → deliver or named failure; hard latency budget on the repair; rejected candidate preserved in the log. Never strip sentences. Lands inside lane-send-failure if the cause is the guard; otherwise this card. — GUARD REPAIR PATH (second opinion 9/10 Q3): generate → grounding guard → ONE constrained regeneration with the exact violations supplied → guard → deliver or named failure; hard latency budget on the repair; rejected candidate preserved in the log. Never strip sentences. Lands inside lane-send-failure if the cause is the guard; otherwise this card. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F4.2 (the status FSM) / F6 (claims); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] hl_message_presentations DOES NOT EXIST in the CRM db (designed, not built — measured by lane-memory-journey 9/10). Leg 5 of the myli-memory journey (abort mid-response → the turn completes server-side → nothing in the visible projection) reports NOT_REACHED: presentation_state absent, a RED BY ABSENCE, until the table exists. Design + DDL at the desk; ChatGPT consult first (#257.410: architectural decisions go to the second opinion). — hl_message_presentations DOES NOT EXIST in the CRM db (designed, not built — measured by lane-memory-journey 9/10). Leg 5 of the myli-memory journey (abort mid-response → the turn completes server-side → nothing in the visible projection) reports NOT_REACHED: presentation_state absent, a RED BY ABSENCE, until the table exists. Design + DDL at the desk; ChatGPT consult first (#257.410: architectural decisions go to the second opinion). <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F1 (the spine); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] FOLLOW-UP to the myli-memory oracle: it binds to what the CRM route WRITES (metadata.origin/content_kind/actor_role/source_event_id/turn_id) — 'authorKind' named in two comments in files that do not define it (WebsiteChatAuthorKind) — retire those comments; the fact slot is finance/income (22 live rows), not monthly_income; update any brief/doc that says otherwise. — FOLLOW-UP to the myli-memory oracle: it binds to what the CRM route WRITES (metadata.origin/content_kind/actor_role/source_event_id/turn_id) — 'authorKind' named in two comments in files that do not define it (WebsiteChatAuthorKind) — retire those comments; the fact slot is finance/income (22 live rows), not monthly_income; update any brief/doc that says otherwise. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] ATTRIBUTE LAW slice 1 (#257.413, TODAY): hl_attribute_catalog + hl_listing_attribute + hl_listing_attribute_observation in the WEB db (migration by the lane, DDL signed+applied by the desk, web-schema-lock regenerated same commit); seed the catalog from typed columns + all 143 visionTags keys + the feature arrays (flooring/interior/exterior/fireplace/appliances/basement/parking/hoaIncludes) + schools + taxes; backfill from existing values (JSON untouched); the semantic resolver (term→catalog key, synonyms, UNRESOLVED_ATTRIBUTE); search_homes.attribute_filters compiled deterministically to parameterized SQL with evidence ids. Prove: 'hardwood floors + white cabinets + fireplace, tax ≤ $6k' returns rows whose attributes trace to observations. — ATTRIBUTE LAW slice 1 (#257.413, TODAY): hl_attribute_catalog + hl_listing_attribute + hl_listing_attribute_observation in the WEB db (migration by the lane, DDL signed+applied by the desk, web-schema-lock regenerated same commit); seed the catalog from typed columns + all 143 visionTags keys + the feature arrays (flooring/interior/exterior/fireplace/appliances/basement/parking/hoaIncludes) + schools + taxes; backfill from existing values (JSON untouched); the semantic resolver (term→catalog key, synonyms, UNRESOLVED_ATTRIBUTE); search_homes.attribute_filters compiled deterministically to parameterized SQL with evidence ids. Prove: 'hardwood floors + white cabinets + fireplace, tax ≤ $6k' returns rows whose attributes trace to observations. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ATTRIBUTE LAW slice 2: get_home_details returns canonical facts + evidence (&#123;key, value, source, confidence&#125;); home_schools / school_rating_count read hl_school + hl_listing_school lifted from schoolsGreatSchools (no runtime JSON); taxHistory lifted into hl_listing_tax_history(listing_id, tax_year, tax_amount, assessed_value, source_kind); HOA includes through the catalog (hasHoa/hoaMonthly/hoaFee stay as columns). — ATTRIBUTE LAW slice 2: get_home_details returns canonical facts + evidence (&#123;key, value, source, confidence&#125;); home_schools / school_rating_count read hl_school + hl_listing_school lifted from schoolsGreatSchools (no runtime JSON); taxHistory lifted into hl_listing_tax_history(listing_id, tax_year, tax_amount, assessed_value, source_kind); HOA includes through the catalog (hasHoa/hoaMonthly/hoaFee stay as columns). <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ATTRIBUTE LAW slice 3 — SUBDIVISION IS AN ENTITY: source census first (realieRawJson structured fields → MLS field → parcels → remarks as LOW confidence), hl_subdivision_candidate → hl_subdivision (canonical + centroid/boundary, canonicalized by name + GEOGRAPHY) + hl_subdivision_alias + hl_listing_subdivision; measured today (CORRECTED): the MLS subdivision is the `neighborhood` column, 11,313 of 11,781 active, 2,850 distinct raw strings, already read by resolve_place — a string, not an entity; the `subdivision` column (4 rows) is dead. resolve_place resolves subdivision aliases to a geo entity. — ATTRIBUTE LAW slice 3 — SUBDIVISION IS AN ENTITY: source census first (realieRawJson structured fields → MLS field → parcels → remarks as LOW confidence), hl_subdivision_candidate → hl_subdivision (canonical + centroid/boundary, canonicalized by name + GEOGRAPHY) + hl_subdivision_alias + hl_listing_subdivision; measured today: subdivision text on 4 of 11,781 active. resolve_place resolves subdivision aliases to a geo entity. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ATTRIBUTE LAW slice 4 — ONE GEO ENTITY, ONE MARKET ENGINE: hl_geo_entity(type city|zip|county|neighborhood|subdivision, geom); market-stat key converges on geo_entity_id + period + property_type + buckets; nightly/incremental subdivision snapshots beside city/zip; market_stats + market_analysis take geo_entity_id; never compute subdivision stats live from 355k solds per turn. sold_comps moves onto the attribute compiler so feature-matched CMAs work. — ATTRIBUTE LAW slice 4 — ONE GEO ENTITY, ONE MARKET ENGINE: hl_geo_entity(type city|zip|county|neighborhood|subdivision, geom); market-stat key converges on geo_entity_id + period + property_type + buckets; nightly/incremental subdivision snapshots beside city/zip; market_stats + market_analysis take geo_entity_id; never compute subdivision stats live from 355k solds per turn. sold_comps moves onto the attribute compiler so feature-matched CMAs work. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ATTRIBUTE LAW slice 5 — THE NIGHTLY RED NUMBER (Inngest or it doesn't exist): hl_source_field_inventory (every populated source path, disposition mapped|not_askable|opaque_payload|unreviewed with reason+owner) × hl_assistant_attribute_capability (attribute × detail|filter|aggregate × tool) → UNREACHABLE ASKABLE FACTS = cataloged-but-unwired + populated-but-uncataloged, three numbers under the headline, reported at zero. Plus the push cage: a Myli tool whose filters are not catalog-resolved is refused. — ATTRIBUTE LAW slice 5 — THE NIGHTLY RED NUMBER (Inngest or it doesn't exist): hl_source_field_inventory (every populated source path, disposition mapped|not_askable|opaque_payload|unreviewed with reason+owner) × hl_assistant_attribute_capability (attribute × detail|filter|aggregate × tool) → UNREACHABLE ASKABLE FACTS = cataloged-but-unwired + populated-but-uncataloged, three numbers under the headline, reported at zero. Plus the push cage: a Myli tool whose filters are not catalog-resolved is refused. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ATTRIBUTE LAW slice 6 — ROOM-AWARE VISION: hl_listing_photo + hl_vision_run + hl_photo_attribute_observation(room_type, attribute, value, confidence) → hl_listing_room_attribute rollup; room attribution CANNOT be recovered from the flat tags (destroyed at flattening) — first check whether per-photo captions/raw responses were retained; otherwise a NARROWER per-photo room+feature pass. MEASURE THE SPEND before it runs (~30-40 photos × 11.8k active) and put the number to Zach. — ATTRIBUTE LAW slice 6 — ROOM-AWARE VISION: hl_listing_photo + hl_vision_run + hl_photo_attribute_observation(room_type, attribute, value, confidence) → hl_listing_room_attribute rollup; room attribution CANNOT be recovered from the flat tags (destroyed at flattening) — first check whether per-photo captions/raw responses were retained; otherwise a NARROWER per-photo room+feature pass. MEASURE THE SPEND before it runs (~30-40 photos × 11.8k active) and put the number to Zach. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ATTRIBUTE LAW slice 4 - ONE GEO ENTITY, ONE MARKET ENGINE: hl_geo_entity(type city/zip/county/neighborhood/subdivision, geom); market-stat key converges on geo_entity_id + period + property_type + buckets; nightly/incremental subdivision snapshots beside city/zip; market_stats + market_analysis take geo_entity_id; never compute subdivision stats live from 355k solds per turn. sold_comps moves onto the attribute compiler so feature-matched CMAs work. — ATTRIBUTE LAW slice 4 - ONE GEO ENTITY, ONE MARKET ENGINE: hl_geo_entity(type city/zip/county/neighborhood/subdivision, geom); market-stat key converges on geo_entity_id + period + property_type + buckets; nightly/incremental subdivision snapshots beside city/zip; market_stats + market_analysis take geo_entity_id; never compute subdivision stats live from 355k solds per turn. sold_comps moves onto the attribute compiler so feature-matched CMAs work. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ROOM LAW slice A (#257.415): hl_listing_room (listing_hlid text FK, source, room_type normalized Master/Primary Bedroom/Primary Suite → primary_bedroom, room_name_raw, room_identity_basis mls_attested, level, width_ft, length_ft, area_sqft GENERATED; partial index on primary area) + backfill from Listing.rooms for ACTIVE AND SOLD (355k) + room-scoped fireplaceFeatures into the attribute catalog. DDL written by the lane, signed+applied by the desk, web lock regenerated same commit. — ROOM LAW slice A (#257.415): hl_listing_room (listing_hlid text FK, source, room_type normalized Master/Primary Bedroom/Primary Suite → primary_bedroom, room_name_raw, room_identity_basis mls_attested, level, width_ft, length_ft, area_sqft GENERATED; partial index on primary area) + backfill from Listing.rooms for ACTIVE AND SOLD (355k) + room-scoped fireplaceFeatures into the attribute catalog. DDL written by the lane, signed+applied by the desk, web lock regenerated same commit. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ROOM LAW slice B: get_primary_bedroom_benchmark(listing) — subject = one MLS room row (width×length); benchmark = MEDIAN area over UNIQUE properties, MLS-attested primary, same property type, 5-year lookback, subdivision → ZIP → city with MIN_N=10; payload exactly as the receipt; classification large = ≥1.20×median AND n≥10; guards G1–G15 in front of the renderer; red-first tests for the four sentence cases (found / no dims / thin subdivision / nothing reaches 10). — ROOM LAW slice B: get_primary_bedroom_benchmark(listing) — subject = one MLS room row (width×length); benchmark = MEDIAN area over UNIQUE properties, MLS-attested primary, same property type, 5-year lookback, subdivision → ZIP → city with MIN_N=10; payload exactly as the receipt; classification large = ≥1.20×median AND n≥10; guards G1–G15 in front of the renderer; red-first tests for the four sentence cases (found / no dims / thin subdivision / nothing reaches 10). <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ROOM LAW slice C: the Myli capability — 'does this have a big primary?', 'biggest primaries in Canyon Lakes', 'primaries over 300 sq ft', 'how does this bedroom compare nearby' — through get_home_details / search_homes attribute+room filters (zero new tools, #257.413); flow-judge acceptance before any vision work. Lands AFTER attribute slice 1 (same agent-voice.ts seam). — ROOM LAW slice C: the Myli capability — 'does this have a big primary?', 'biggest primaries in Canyon Lakes', 'primaries over 300 sq ft', 'how does this bedroom compare nearby' — through get_home_details / search_homes attribute+room filters (zero new tools, #257.413); flow-judge acceptance before any vision work. Lands AFTER attribute slice 1 (same agent-voice.ts seam). <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ROOM LAW slice D: persist Repliers imageInsights on every listing ingest (hl_listing_photo + hl_photo_room_classification: room_type, confidence, photo quality; hl_listing_room_quality_signal per room) and backfill 11,781 actives — measure Repliers rate limits first; expose Bedroom photos to Myli as BEDROOM photos, never primary. — ROOM LAW slice D: persist Repliers imageInsights on every listing ingest (hl_listing_photo + hl_photo_room_classification: room_type, confidence, photo quality; hl_listing_room_quality_signal per room) and backfill 11,781 actives — measure Repliers rate limits first; expose Bedroom photos to Myli as BEDROOM photos, never primary. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ROOM LAW slice E: the Repliers quality signal exposed with provenance wording ('Repliers' image analysis rates this kitchen below average, 2.73'); a HouseLoop percentile is spoken as HouseLoop's aggregate with its n; grounding rule forbids converting the vendor score into an objective judgment. — ROOM LAW slice E: the Repliers quality signal exposed with provenance wording ('Repliers' image analysis rates this kitchen below average, 2.73'); a HouseLoop percentile is spoken as HouseLoop's aggregate with its n; grounding rule forbids converting the vendor score into an objective judgment. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ROOM LAW slice F — THE ROUTED PAID VISION PASS (NOT RUN WITHOUT ZACH'S WORD): Repliers room class → the 4–6 photos of that room → a 15–30-key room-specific prompt, ONE batched call, photo ids retained; hl_photo_feature_observation + rollup (non-observation is not absence; categorical rollup only on agreement); est. 0.5–1.5× the ~$0.006/listing baseline (~$35–105 for all actives) — measure on 500 listings and put the number to Zach. — ROOM LAW slice F — THE ROUTED PAID VISION PASS (NOT RUN WITHOUT ZACH'S WORD): Repliers room class → the 4–6 photos of that room → a 15–30-key room-specific prompt, ONE batched call, photo ids retained; hl_photo_feature_observation + rollup (non-observation is not absence; categorical rollup only on agreement); est. 0.5–1.5× the ~$0.006/listing baseline (~$35–105 for all actives) — measure on 500 listings and put the number to Zach. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (desk) — #257.415 + slice-1 measurement: re-carded vision-only: the MLS already carries fireplace rooms (10 rooms, 208k rows) and 12,320 (room, feature) pairs; the paid pass covers only what MLS lacks.*</sub>
- [ ] THE GROUNDING CORPUS IS PER-TURN — Myli cannot repeat what she said last turn (lane-send-failure 9/10, observed live on other lanes' traffic): 'Of the three I just showed you: 10924 W 115th St at $250,000' STRIPPED by the ungrounded-number guard because those prices were grounded on a PREVIOUS turn's tool results; also '$2,125/month' and '26 listings'. The buyer experiences a memory failure. Design: a per-conversation evidence ledger the guard reads from (every delivered tool result, keyed by turn + source, with a refresh rule for prices) composed with hl_chat_events/hl_turns. ChatGPT consult in flight (turn 10). — THE GROUNDING CORPUS IS PER-TURN — Myli cannot repeat what she said last turn (lane-send-failure 9/10, observed live on other lanes' traffic): 'Of the three I just showed you: 10924 W 115th St at $250,000' STRIPPED by the ungrounded-number guard because those prices were grounded on a PREVIOUS turn's tool results; also '$2,125/month' and '26 listings'. The buyer experiences a memory failure. Design: a per-conversation evidence ledger the guard reads from (every delivered tool result, keyed by turn + source, with a refresh rule for prices) composed with hl_chat_events/hl_turns. ChatGPT consult in flight (turn 10). <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F1 (the spine); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] GUARD DESIGN: pre-paint vs strip on the streamed road — the W-2 false positive was fatal because a single finding went to the floor with no repair path (retry disabled on streaming; painted-prose law forbids revise-after-paint). The lane gave W-1/W-2 the signed strip-before-floor arm. Open question to settle from the turn-10 consult: buffer-judge-then-stream (latency cost?) vs the strip ladder. Ruling owed. — GUARD DESIGN: pre-paint vs strip on the streamed road — the W-2 false positive was fatal because a single finding went to the floor with no repair path (retry disabled on streaming; painted-prose law forbids revise-after-paint). The lane gave W-1/W-2 the signed strip-before-floor arm. Open question to settle from the turn-10 consult: buffer-judge-then-stream (latency cost?) vs the strip ladder. Ruling owed. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F4.2 (the status FSM) / F6 (claims); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] GUARD LAW (#257.416) slice 1 — PASS|CONVICT|ABSTAIN: every detector in lib/search/agent-voice-core.ts returns the tri-state; ambiguous entity (name-only address matching >1 home) or ambiguous claim typing (digits inside a street ordinal) → ABSTAIN with telemetry; claims typed against listing ids where the id is in hand. Red-first: the Street/Terrace plant must ABSTAIN not CONVICT; a real $495k-vs-$350k mismatch on one unambiguous home still CONVICTS. — GUARD LAW (#257.416) slice 1 — PASS|CONVICT|ABSTAIN: every detector in lib/search/agent-voice-core.ts returns the tri-state; ambiguous entity (name-only address matching >1 home) or ambiguous claim typing (digits inside a street ordinal) → ABSTAIN with telemetry; claims typed against listing ids where the id is in hand. Red-first: the Street/Terrace plant must ABSTAIN not CONVICT; a real $495k-vs-$350k mismatch on one unambiguous home still CONVICTS. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F6 (claims as references); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] GUARD LAW slice 2 — THE CONVERSATION EVIDENCE PROJECTION (the memory defect: she cannot repeat last turn's prices): hl_conversation_evidence in the WEB db projected from hl_chat_events trusted events (tool results, control submissions, typed claims of published units) — subject_id (stable listing id), predicate, value, unit, source_type, source_event_id, observed_at, available_from, presented_at, freshness_class, fresh_until, supersedes; prior ASSISTANT PROSE never projected; the ungrounded-number guard reads current ∪ prior trusted evidence for the conversation. Selftest: a price delivered on turn 2 grounds a recap on turn 5; a number Myli merely SAID on turn 4 does not ground turn 7. — GUARD LAW slice 2 — THE CONVERSATION EVIDENCE PROJECTION (the memory defect: she cannot repeat last turn's prices): hl_conversation_evidence in the WEB db projected from hl_chat_events trusted events (tool results, control submissions, typed claims of published units) — subject_id (stable listing id), predicate, value, unit, source_type, source_event_id, observed_at, available_from, presented_at, freshness_class, fresh_until, supersedes; prior ASSISTANT PROSE never projected; the ungrounded-number guard reads current ∪ prior trusted evidence for the conversation. Selftest: a price delivered on turn 2 grounds a recap on turn 5; a number Myli merely SAID on turn 4 does not ground turn 7. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F6 (claims as references); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] GUARD LAW slice 3 — TEMPORAL ADMISSIBILITY for list_price + listing_status only: past-tense claims admit old evidence forever; current-tense claims need evidence inside the MLS ingest freshness window (measure the ingest SLA; no invented 24h TTL); 'is it still…' forces a refresh by listing id appended as a new tool_result event, never a destructive update. — GUARD LAW slice 3 — TEMPORAL ADMISSIBILITY for list_price + listing_status only: past-tense claims admit old evidence forever; current-tense claims need evidence inside the MLS ingest freshness window (measure the ingest SLA; no invented 24h TTL); 'is it still…' forces a refresh by listing id appended as a new tool_result event, never a destructive update. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F6 (claims as references); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] GUARD LAW slice 4 — UNIT-GATED PUBLICATION replaces stream-level stripping: private buffer → sentence/card/bullet boundary (~0.2–1.0 s) → deterministic guards → publish; on CONVICT regenerate ONLY the unpublished unit once; drop only if proven drop-safe (no later anaphora, not the answer content, no dangling comparison); else named failure. Keep the strip arm as drop_safe=true last resort while this ships. Latency measured before/after on a 900-word answer. — GUARD LAW slice 4 — UNIT-GATED PUBLICATION replaces stream-level stripping: private buffer → sentence/card/bullet boundary (~0.2–1.0 s) → deterministic guards → publish; on CONVICT regenerate ONLY the unpublished unit once; drop only if proven drop-safe (no later anaphora, not the answer content, no dangling comparison); else named failure. Keep the strip arm as drop_safe=true last resort while this ships. Latency measured before/after on a 900-word answer. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F4.2 (the status FSM) / F6 (claims); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] RE-DERIVE THE PARKED FACTS (#257.417 step 5): after S-25's park, an Inngest-visible run re-extracts each quarantined slot from the ADMISSIBLE corpus only (buyer-authored inbound, UI submissions, confirmed_claim observations) into a NEW clean fact version with clean lineage; unrecoverable slots stay quarantined; contaminated rows are never reactivated. Run Log row is the proof. — RE-DERIVE THE PARKED FACTS (#257.417 step 5): after S-25's park, an Inngest-visible run re-extracts each quarantined slot from the ADMISSIBLE corpus only (buyer-authored inbound, UI submissions, confirmed_claim observations) into a NEW clean fact version with clean lineage; unrecoverable slots stay quarantined; contaminated rows are never reactivated. Run Log row is the proof. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] NIGHTLY unsupported_active_facts (#257.417 clause 3, Inngest or it doesn't exist): active facts with zero admissible value support (assistant-only lineage as a diagnostic subtype) + the quarantined census (real/test split), spoken at zero; the judge layer may not PASS a memory claim while it is nonzero. — NIGHTLY unsupported_active_facts (#257.417 clause 3, Inngest or it doesn't exist): active facts with zero admissible value support (assistant-only lineage as a diagnostic subtype) + the quarantined census (real/test split), spoken at zero; the judge layer may not PASS a memory claim while it is nonzero. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] THE EVIDENCE CHAIN DOES NOT RESOLVE (lane-memory-drive 9/10, verified in source): every hl_messages row's metadata.source_event_id is '&lt;conversationId>:&lt;index>' — a POSITION — while hl_chat_events keys events by uuid; the website's own comment says 'the ids are positions'. The memory architecture's chain (event → message → fact) terminates at a pointer nothing answers. Fix at the CRM wire (website-chat-turns.ts): carry the ledger event uuid as source_event_id; keep the position as a separate field. Then the oracle's join is real. — THE EVIDENCE CHAIN DOES NOT RESOLVE (lane-memory-drive 9/10, verified in source): every hl_messages row's metadata.source_event_id is '&lt;conversationId>:&lt;index>' — a POSITION — while hl_chat_events keys events by uuid; the website's own comment says 'the ids are positions'. The memory architecture's chain (event → message → fact) terminates at a pointer nothing answers. Fix at the CRM wire (website-chat-turns.ts): carry the ledger event uuid as source_event_id; keep the position as a separate field. Then the oracle's join is real. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] AN ENTRY PILL'S TEXT IS DRAWN IN A HUMAN SPEECH BUBBLE (lane-memory-drive 9/10, alive on :3001): 'Show me homes near me in the Kansas City metro' renders as a USER bubble; its canonical event is ui_selection and a tap is not an utterance. The founding defect of this item, on the client renderer. Route per #257.404 (symptom layer): the renderer must draw a ui_selection as a selection chip/echo, never as the buyer's words; the ledger row is already correct. — AN ENTRY PILL'S TEXT IS DRAWN IN A HUMAN SPEECH BUBBLE (lane-memory-drive 9/10, alive on :3001): 'Show me homes near me in the Kansas City metro' renders as a USER bubble; its canonical event is ui_selection and a tap is not an utterance. The founding defect of this item, on the client renderer. Route per #257.404 (symptom layer): the renderer must draw a ui_selection as a selection chip/echo, never as the buyer's words; the ledger row is already correct. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (desk) — #257.418: Zach: a tapped pill renders as the buyer's own sentence; provenance lives in the record.*</sub>
- [ ] myli-memory ORACLE follow-up: SYNTHETIC_TEXT_ATTRIBUTED_TO_USER overstates a broken join — add a distinct code EVIDENCE_CHAIN_UNRESOLVED (a fabrication and an unresolvable pointer must never share a colour); leg windows keyed on turn START not end (the desk's own instrument error, fixed in drive 3); leg 5 capture must span both conversations the journey makes; rebirth of the newly-registered persona is blocked by the home page's Homes carousel intercepting the birth click — verify with eyes whether that is a real overlay defect at 390 or the instrument, then card or close. — myli-memory ORACLE follow-up: SYNTHETIC_TEXT_ATTRIBUTED_TO_USER overstates a broken join — add a distinct code EVIDENCE_CHAIN_UNRESOLVED (a fabrication and an unresolvable pointer must never share a colour); leg windows keyed on turn START not end (the desk's own instrument error, fixed in drive 3); leg 5 capture must span both conversations the journey makes; rebirth of the newly-registered persona is blocked by the home page's Homes carousel intercepting the birth click — verify with eyes whether that is a real overlay defect at 390 or the instrument, then card or close. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] ONE MEMORY LAW audit (#257.420, Fable, audit+spec, NO code): census today's FUB→memory road — lib/fub-import/past-client-facts.ts, lib/inngest/functions/fub-lead-import.ts, staging-import.ts, classify-fub-event.ts, backfill-fub-comms.ts, the extractor's read window — against the law: which sources are treated as evidence, whether tags leak into any fact, whether outbound agent texts are treated as buyer testimony, whether yes/no replies bind to a proposal, how the past agent is resolved today, whether behavior becomes stated facts. Output: findings with file:line + the build spec by slice for Sonnet/Opus. — ONE MEMORY LAW audit (#257.420, Fable, audit+spec, NO code): census today's FUB→memory road — lib/fub-import/past-client-facts.ts, lib/inngest/functions/fub-lead-import.ts, staging-import.ts, classify-fub-event.ts, backfill-fub-comms.ts, the extractor's read window — against the law: which sources are treated as evidence, whether tags leak into any fact, whether outbound agent texts are treated as buyer testimony, whether yes/no replies bind to a proposal, how the past agent is resolved today, whether behavior becomes stated facts. Output: findings with file:line + the build spec by slice for Sonnet/Opus. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ONE MEMORY LAW slice 1 — normalized_event + observation + derivation tables (CRM db) with the two dimensions (origin × evidence_role), the three UNIQUE keys, normalization_run + person_checkpoint; the FUB adapter classifies each tape row ONCE (D3) into normalized_event; Inngest function with resume-by-person. DDL signed at the desk; schema lock same commit. — ONE MEMORY LAW slice 1 — normalized_event + observation + derivation tables (CRM db) with the two dimensions (origin × evidence_role), the three UNIQUE keys, normalization_run + person_checkpoint; the FUB adapter classifies each tape row ONCE (D3) into normalized_event; Inngest function with resume-by-person. DDL signed at the desk; schema lock same commit. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] ONE MEMORY LAW slice 2 — evidence spans + speaker attribution + extraction under #257.417: inbound message + the exact outbound proposal it answers + minimal context; yes/no binds to one proposal; agent notes as secondary testimony with basis/authority; thread/chunk compute boundary; idempotent by fingerprint. — ONE MEMORY LAW slice 2 — evidence spans + speaker attribution + extraction under #257.417: inbound message + the exact outbound proposal it answers + minimal context; yes/no binds to one proposal; agent notes as secondary testimony with basis/authority; thread/chunk compute boundary; idempotent by fingerprint. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] ONE MEMORY LAW slice 3 — behavior_observation from zillow_home_activity (1.08M) + observed_interest derivations (window, decay, weighted actions) that can never become a stated requirement; the speech contract per basis class enforced in the grounding guard (Myli never improvises the provenance verb). — ONE MEMORY LAW slice 3 — behavior_observation from zillow_home_activity (1.08M) + observed_interest derivations (window, decay, weighted actions) that can never become a stated requirement; the speech contract per basis class enforced in the grounding guard (Myli never improvises the provenance verb). <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] ONE MEMORY LAW slice 4 — person_agent_relationship (temporal, derived from tags + assignment history, input-hashed, superseded on change; tags participate here ONLY); confidence thresholds for speech (≥0.90 unqualified, 0.75–0.90 qualified); reconcile with the SRG who-repped-us engine (guides/srg-reconciliation-engine) — reuse, never a second resolver. — ONE MEMORY LAW slice 4 — person_agent_relationship (temporal, derived from tags + assignment history, input-hashed, superseded on change; tags participate here ONLY); confidence thresholds for speech (≥0.90 unqualified, 0.75–0.90 qualified); reconcile with the SRG who-repped-us engine (guides/srg-reconciliation-engine) — reuse, never a second resolver. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] ONE MEMORY LAW slice 5 — THE 40 HOSTILE PEOPLE acceptance (8 changed values · 6 yes/no · 6 conflicting notes · 6 behavior-only · 5 junk tags · 5 assignment changes · 4 contaminated) + the ten invariants incl. replay determinism and resume-after-kill; the nightly red-line metrics as an Inngest function; a memory claim may not PASS while any red line is nonzero. — ONE MEMORY LAW slice 5 — THE 40 HOSTILE PEOPLE acceptance (8 changed values · 6 yes/no · 6 conflicting notes · 6 behavior-only · 5 junk tags · 5 assignment changes · 4 contaminated) + the ten invariants incl. replay determinism and resume-after-kill; the nightly red-line metrics as an Inngest function; a memory claim may not PASS while any red line is nonzero. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] #257.418a — the web history/context builder sends a tap to the MODEL as the semantic event ('[Lead selected option: … (intent=…)]'), never as 'user: &lt;sentence>'; display_text_at_event frozen on the ledger row; extraction sees only the typed envelope; screen unchanged (buyer bubble). — #257.418a — the web history/context builder sends a tap to the MODEL as the semantic event ('[Lead selected option: … (intent=…)]'), never as 'user: &lt;sentence>'; display_text_at_event frozen on the ledger row; extraction sees only the typed envelope; screen unchanged (buyer bubble). <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] VALUE COHERENCE AT THE WRITE PATH (grind-qa-bench-r8 9/10, measured on b6962297): a fact value carrying two mutually exclusive figures ('$95,000/yr ($7,916/mo); disclosed as $16,750/mo — ambiguity noted') passes every lineage/content detector; a retroactive grounding sweep flags 8 facts incl. legitimate derivations, so it is not the instrument. Needed: a value-coherence refusal in canWriteFact (one normalized value per fact; an 'ambiguity noted' value is a REFUSAL + a clarify turn, never a stored fact). New mechanism → second opinion first (#257.384). — VALUE COHERENCE AT THE WRITE PATH (grind-qa-bench-r8 9/10, measured on b6962297): a fact value carrying two mutually exclusive figures ('$95,000/yr ($7,916/mo); disclosed as $16,750/mo — ambiguity noted') passes every lineage/content detector; a retroactive grounding sweep flags 8 facts incl. legitimate derivations, so it is not the instrument. Needed: a value-coherence refusal in canWriteFact (one normalized value per fact; an 'ambiguity noted' value is a REFUSAL + a clarify turn, never a stored fact). New mechanism → second opinion first (#257.384). <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] attribute_filters live turn: the source-of-truth probe returned 22 and she said 21 — unreconciled by the lane (a saved-search constraint the probe did not replicate). The compiler==control equality is exact at 1741; this is the turn-level parity the flow judge's NUMBER_UNTRUE detector must close: replicate the exact constraint set in the probe or make her count come from the same query. — attribute_filters live turn: the source-of-truth probe returned 22 and she said 21 — unreconciled by the lane (a saved-search constraint the probe did not replicate). The compiler==control equality is exact at 1741; this is the turn-level parity the flow judge's NUMBER_UNTRUE detector must close: replicate the exact constraint set in the probe or make her count come from the same query. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] THE ENTRY PILL'S ACT IS NOT LEDGERED (lane-memory-drive 9/10, re-judged on drive 3's real rows after #257.418): 'Show me homes near me in the Kansas City metro' is on screen as the buyer's bubble (correct per the ruling), the turn reached the model and answered, and hl_chat_events holds NO lead event of any origin behind it — zero events carry that sentence; the conversation opens with Myli answering first. The renderer obeys the ruling; the LEDGER is missing the user act the turn was built on. Find the road (the entry pill from the home page / opener) and append the lead·ui_selection event before the assistant's. Sibling of the ledger-less roads card. — THE ENTRY PILL'S ACT IS NOT LEDGERED (lane-memory-drive 9/10, re-judged on drive 3's real rows after #257.418): 'Show me homes near me in the Kansas City metro' is on screen as the buyer's bubble (correct per the ruling), the turn reached the model and answered, and hl_chat_events holds NO lead event of any origin behind it — zero events carry that sentence; the conversation opens with Myli answering first. The renderer obeys the ruling; the LEDGER is missing the user act the turn was built on. Find the road (the entry pill from the home page / opener) and append the lead·ui_selection event before the assistant's. Sibling of the ledger-less roads card. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F1 (the spine); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] ENTRY PILL NOT LEDGERED — MECHANISM (lane-evidence-corpus 9/10, measured, hypothesis refuted then corrected): route.ts:1609 gates the whole ledger block on conversationId; persist-conversation.ts:80 mints the id AFTER the turn; ReturningStrip.tsx:148 → askMyli :107 dispatches fresh:true (no id) so the gate is closed for the pill's first turn; a second road, the opener at route.ts:2426, never persists and its user line is a server-composed synthetic ask. NOT universal: of 30 conversations, 15 open with the lead and 15 with the assistant — a client already holding an id ledgers correctly. FIX: mint the conversation id in the route BEFORE the turn (~6 persistConversation call sites in a 3,800-line route, incl. the opener road that deliberately does not persist) — its own lane, red-first: a fresh:true pill turn must produce a lead·ui_selection event before the assistant's; the 15/15 split must go to 30/0. — ENTRY PILL NOT LEDGERED — MECHANISM (lane-evidence-corpus 9/10, measured, hypothesis refuted then corrected): route.ts:1609 gates the whole ledger block on conversationId; persist-conversation.ts:80 mints the id AFTER the turn; ReturningStrip.tsx:148 → askMyli :107 dispatches fresh:true (no id) so the gate is closed for the pill's first turn; a second road, the opener at route.ts:2426, never persists and its user line is a server-composed synthetic ask. NOT universal: of 30 conversations, 15 open with the lead and 15 with the assistant — a client already holding an id ledgers correctly. FIX: mint the conversation id in the route BEFORE the turn (~6 persistConversation call sites in a 3,800-line route, incl. the opener road that deliberately does not persist) — its own lane, red-first: a fresh:true pill turn must produce a lead·ui_selection event before the assistant's; the 15/15 split must go to 30/0. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F1 (the spine); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] FUB AUDIT LANDED (.claude/reports/myli-os/AUDIT-fub-into-the-one-memory-2026-09-10.md, Fable, HEAD c83776a166): ⛔ neither fact guard was on prod (origin 631471b5ef 9/09) → push #23 fired. Baseline on 60 FUB leads / 813 active facts: assistant-only 148 · behavior-only 207 (48 in governed slots) · note-only 21 · tag-born 4 · stage_changed cited 86 · whole-window citations 255 (31%) · basis NULL 773/813. The spec per slice (tiers: 1 Opus census · 2 Fable rule then Sonnet · 3 Sonnet · 4 Opus · 5 Sonnet+judge · 418a Sonnet) is in the doc. — FUB AUDIT LANDED (.claude/reports/myli-os/AUDIT-fub-into-the-one-memory-2026-09-10.md, Fable, HEAD c83776a166): ⛔ neither fact guard was on prod (origin 631471b5ef 9/09) → push #23 fired. Baseline on 60 FUB leads / 813 active facts: assistant-only 148 · behavior-only 207 (48 in governed slots) · note-only 21 · tag-born 4 · stage_changed cited 86 · whole-window citations 255 (31%) · basis NULL 773/813. The spec per slice (tiers: 1 Opus census · 2 Fable rule then Sonnet · 3 Sonnet · 4 Opus · 5 Sonnet+judge · 418a Sonnet) is in the doc. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] FUB finding 1 — SPEAKER ATTRIBUTION BROKEN AT FILING (D3 breach): 1,451 FUB agent texts + 102 emails + 223 notes are by_kind='system' in hl_activity_log while hl_staging_event says user/human on the SAME rows; logMessage resolves outbound actors by our phone line → hl_users (log-message.ts:691,717) and departed agents have no seat → 'system'; 592 agent texts read to the model as 'the system sent a text' (situation.ts:312); 6 of 21 active Agent-history facts name Zach because HIS outbound texts are mis-actored. ONE classifier on one tape: the staging verdict is the actor. — FUB finding 1 — SPEAKER ATTRIBUTION BROKEN AT FILING (D3 breach): 1,451 FUB agent texts + 102 emails + 223 notes are by_kind='system' in hl_activity_log while hl_staging_event says user/human on the SAME rows; logMessage resolves outbound actors by our phone line → hl_users (log-message.ts:691,717) and departed agents have no seat → 'system'; 592 agent texts read to the model as 'the system sent a text' (situation.ts:312); 6 of 21 active Agent-history facts name Zach because HIS outbound texts are mis-actored. ONE classifier on one tape: the staging verdict is the actor. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] FUB finding 2 — evidenceClassOf keys on KIND alone (extract-context.ts ~:680): agent notes = humanRecord on equal footing with buyer words; 348 pipeline-canary notes + 266 'Follow Up Boss'-authored Zillow view notes are humanRecord; outbound agent texts = assistantProse (right by accident, kills the proposal half of a span); stage/task/appointment rows unclassified and PASS S-24 outside the five governed slots. Replace with origin × evidence_role (#257.420). — FUB finding 2 — evidenceClassOf keys on KIND alone (extract-context.ts ~:680): agent notes = humanRecord on equal footing with buyer words; 348 pipeline-canary notes + 266 'Follow Up Boss'-authored Zillow view notes are humanRecord; outbound agent texts = assistantProse (right by accident, kills the proposal half of a span); stage/task/appointment rows unclassified and PASS S-24 outside the five governed slots. Replace with origin × evidence_role (#257.420). <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] FUB finding 3 — NO BINDING EXISTS: one [eN] citation per fact (extract-context.ts :99, :3729); no confirmed_claim / evidence span anywhere; 202 bare replies ('yes', 'correct') on the 60 leads; 84 facts cite one row among 35–301. Slice 2 builds the span (inbound + the exact proposal it answers). — FUB finding 3 — NO BINDING EXISTS: one [eN] citation per fact (extract-context.ts :99, :3729); no confirmed_claim / evidence span anywhere; 202 bare replies ('yes', 'correct') on the 60 leads; 84 facts cite one row among 35–301. Slice 2 builds the span (inbound + the exact proposal it answers). <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] FUB finding 4 — THE STARVATION IS BROWSING, NOT TEXTS: Kristen Hansan has 329 texts; the extractor's window holds 0 of them and 380 Zillow views; 44 of 60 leads exceed the window; 2,218 inbound texts never read by any pass. The read window must be per evidence class with behavior capped, or behavior in its own lane (slice 3). — FUB finding 4 — THE STARVATION IS BROWSING, NOT TEXTS: Kristen Hansan has 329 texts; the extractor's window holds 0 of them and 380 Zillow views; 44 of 60 leads exceed the window; 2,218 inbound texts never read by any pass. The read window must be per evidence class with behavior capped, or behavior in its own lane (slice 3). <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] FUB finding 5 — THREE PAST-AGENT RESOLVERS: the staging ladder (97.0%, rung+source, reads fub.assignment_history) · SRG per-deal our_agent · fub-structured-memory.ts's own regex (no chain, scalar). All 21 active Agent-history facts are LLM prose; 9 disagree with the ladder. fub.assignment_history.changed_at is NULL on all 22,449 rows — intervals must come from authored-row dates. Slice 4: ONE resolver (the staging ladder), person_agent_relationship with intervals, the other two retired. — FUB finding 5 — THREE PAST-AGENT RESOLVERS: the staging ladder (97.0%, rung+source, reads fub.assignment_history) · SRG per-deal our_agent · fub-structured-memory.ts's own regex (no chain, scalar). All 21 active Agent-history facts are LLM prose; 9 disagree with the ladder. fub.assignment_history.changed_at is NULL on all 22,449 rows — intervals must come from authored-row dates. Slice 4: ONE resolver (the staging ladder), person_agent_relationship with intervals, the other two retired. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] FUB finding 6 — hl_staging_event IS normalized_event minus origin/evidence_role/thread_id, plus a zillow_home_activity lane (995k rows already classified); website-chat-turns.ts:126 already holds an origin enum — slice 1 EXTENDS these, never twins them. Also: the real deterministic FUB→facts writer is lib/fub/fub-structured-memory.ts (past-client-facts.ts is vocabulary only); guides/srg-reconciliation-engine 301s to guides/seller-engine — CLAUDE.md + HOUSELOOP_DATA_DOCTRINE point at the old slug (fix). — FUB finding 6 — hl_staging_event IS normalized_event minus origin/evidence_role/thread_id, plus a zillow_home_activity lane (995k rows already classified); website-chat-turns.ts:126 already holds an origin enum — slice 1 EXTENDS these, never twins them. Also: the real deterministic FUB→facts writer is lib/fub/fub-structured-memory.ts (past-client-facts.ts is vocabulary only); guides/srg-reconciliation-engine 301s to guides/seller-engine — CLAUDE.md + HOUSELOOP_DATA_DOCTRINE point at the old slug (fix). <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] THE OWNER REPORT DOES NOT RENDER ON localhost:3001 (grind-qa-bench-r8 9/10, baseline identical with its arm neutralized): node tools/flow-judge/run.mjs myli-owner → MOVE_NOT_REACHED at 'loaded-report' (no text matching /If you sold at/i on the settled page) + two LEDGER_VACUOUS consequences; the abort is at a page-render move upstream of any chat turn, so the owner journey has not been exercised today. This is the HOME VALUE REPORT surface — measure on prod vs localhost, name the owner, fix, re-run the journey. — THE OWNER REPORT DOES NOT RENDER ON localhost:3001 (grind-qa-bench-r8 9/10, baseline identical with its arm neutralized): node tools/flow-judge/run.mjs myli-owner → MOVE_NOT_REACHED at 'loaded-report' (no text matching /If you sold at/i on the settled page) + two LEDGER_VACUOUS consequences; the abort is at a page-render move upstream of any chat turn, so the owner journey has not been exercised today. This is the HOME VALUE REPORT surface — measure on prod vs localhost, name the owner, fix, re-run the journey. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ROOM LAW slice F (RE-CARDED VISION-ONLY, 9/10 after slice 1 + room law A/B): MLS already gives room-scoped fireplaces (room.fireplace, 10 rooms) and 12,320 (room, feature) pairs (room.feature: kitchen 3,775 · primary_bedroom 2,552 · primary_bathroom 1,870). The ROUTED paid vision pass (NOT RUN WITHOUT ZACH'S WORD) covers only what MLS lacks: cabinet color / flooring / condition BY ROOM — Repliers room class → the 4–6 photos of that room → a 15–30-key room prompt, one batched call; est. 0.5–1.5× the ~$0.006/listing baseline; measure 500 first. — ROOM LAW slice F (RE-CARDED VISION-ONLY, 9/10 after slice 1 + room law A/B): MLS already gives room-scoped fireplaces (room.fireplace, 10 rooms) and 12,320 (room, feature) pairs (room.feature: kitchen 3,775 · primary_bedroom 2,552 · primary_bathroom 1,870). The ROUTED paid vision pass (NOT RUN WITHOUT ZACH'S WORD) covers only what MLS lacks: cabinet color / flooring / condition BY ROOM — Repliers room class → the 4–6 photos of that room → a 15–30-key room prompt, one batched call; est. 0.5–1.5× the ~$0.006/listing baseline; measure 500 first. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] SCHEMA GAP (lane-attribute-slice1 9/10): hl_listing_attribute.winner_observation_id has no index and its FK is ON DELETE SET NULL, so deleting one observation seq-scans the ~860k-row serving table — deleting 6,947 ran 4 min and was cancelled; 6,947 documented orphan observations remain. DESK: CREATE INDEX on winner_observation_id after push #23's battery (a live-DB change mid-battery reds the web schema-drift cage), regenerate the lock, then delete the orphans. — SCHEMA GAP (lane-attribute-slice1 9/10): hl_listing_attribute.winner_observation_id has no index and its FK is ON DELETE SET NULL, so deleting one observation seq-scans the ~860k-row serving table — deleting 6,947 ran 4 min and was cancelled; 6,947 documented orphan observations remain. DESK: CREATE INDEX on winner_observation_id after push #23's battery (a live-DB change mid-battery reds the web schema-drift cage), regenerate the lock, then delete the orphans. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] TWO ROADS WRITE THE SAME DERIVED SLOT UNDER TWO CASINGS (grind-qa-bench-r8 9/10, measured on lead 2d705a27): 'search_criteria/homes viewed' + 'homes saved' (lowercase, source=extraction, refused as ordinary facts) beside 'search_criteria/Homes viewed' (capital H, source=derived); isDerivedSlot matches exact strings so only the capitalised one is recognised as derived. ONE_ANSWER_TABLE shape — one slot key, one writer; find the lowercase writer (prod's unguarded extractor at 631471b5ef is the suspect) and retire it; the taxonomy's derivedSlots are the vocabulary. — TWO ROADS WRITE THE SAME DERIVED SLOT UNDER TWO CASINGS (grind-qa-bench-r8 9/10, measured on lead 2d705a27): 'search_criteria/homes viewed' + 'homes saved' (lowercase, source=extraction, refused as ordinary facts) beside 'search_criteria/Homes viewed' (capital H, source=derived); isDerivedSlot matches exact strings so only the capitalised one is recognised as derived. ONE_ANSWER_TABLE shape — one slot key, one writer; find the lowercase writer (prod's unguarded extractor at 631471b5ef is the suspect) and retire it; the taxonomy's derivedSlots are the vocabulary. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] BRAIN-READER CAGE: recognise the conversation ledger + its projection BY PATH CLASS (apps/houseloop-web/lib/myli/chat-ledger*.ts, conversation-evidence*.ts) — they are the RECORD and its INDEX, not prompt builders; today's 234→244 re-baseline carries the measured attribution, but every future ledger write would bump the floor again (a token cage answering a structural question, BEDROCK third clause). — BRAIN-READER CAGE: recognise the conversation ledger + its projection BY PATH CLASS (apps/houseloop-web/lib/myli/chat-ledger*.ts, conversation-evidence*.ts) — they are the RECORD and its INDEX, not prompt builders; today's 234→244 re-baseline carries the measured attribution, but every future ledger write would bump the floor again (a token cage answering a structural question, BEDROCK third clause). <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F1 (the spine); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] evidenceClassOf should CANONICALISE the incoming kind (grind-qa-bench-r8 9/10, filed not built): a row arriving under an alias (text_received, message_received, home_viewed, property_view — 44 alias tokens) falls through to unclassified and fails closed today; canonicalising would make an aliased buyer text count as the lead's words — a widening of what counts as testimony that deserves its own red-proof, never a ride-along on a refactor. — evidenceClassOf should CANONICALISE the incoming kind (grind-qa-bench-r8 9/10, filed not built): a row arriving under an alias (text_received, message_received, home_viewed, property_view — 44 alias tokens) falls through to unclassified and fails closed today; canonicalising would make an aliased buyer text count as the lead's words — a widening of what counts as testimony that deserves its own red-proof, never a ride-along on a refactor. <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (Lee) — #257.471: absorbed by THE MYLI SSOT FOLD — F5 (memory: one catalog, one evidence pointer, derived values, one extractor); its content is carried in the plan (THE-MYLI-SSOT-FOLD-PLAN-2026-09-10.md §1–§2) and ticks with that slice's receipt.*</sub>
- [ ] UNVERIFIABLE PROMOTED BOARDS (lane-boards-home-v2 9/10): 3 boards (seo-recon-2026-09-08, the-night-of-2026-09-07, website-polish-2026-09-07) were promoted from a SESSION SCRATCH DIR (…/scratchpad/desk/) whose source files no longer exist, so the drift cage cannot verify them forever. Rule: promote.mjs refuses a --source outside the tree (docs/wireframes/** or .claude/reports/**); re-source or retire the three. — UNVERIFIABLE PROMOTED BOARDS (lane-boards-home-v2 9/10): 3 boards (seo-recon-2026-09-08, the-night-of-2026-09-07, website-polish-2026-09-07) were promoted from a SESSION SCRATCH DIR (…/scratchpad/desk/) whose source files no longer exist, so the drift cage cannot verify them forever. Rule: promote.mjs refuses a --source outside the tree (docs/wireframes/** or .claude/reports/**); re-source or retire the three. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ARRIVAL-BOUNDARY +2 (push #23 run 12, measured 9/10): on lead 5d0d8d23 (Jalil, a FUB person imported 9/09 with 200+ historical texts), prod fired sms_received TWICE for ONE text (real_event_id 'fub:textMessagesCreated:296538' at 16:42, then 'fub:fub_text:296538' at 20:40 with source '' — the only fub_text: spelling in the whole table) and fired registration_completed (Zillow, 16:50) with NO activity card of that kind ever filed. Both fires removed with backup (~/hl-reversals/hl_trigger_fire-jalil-duplicate-and-orphan-2026-09-10.json). ROOT CAUSES to fix on the roads: (a) a second spelling of the same real_event_id defeats the (event_key, real_event_id) unique index — one spelling per source; (b) the Zillow registration road fires without filing its card. — ARRIVAL-BOUNDARY +2 (push #23 run 12, measured 9/10): on lead 5d0d8d23 (Jalil, a FUB person imported 9/09 with 200+ historical texts), prod fired sms_received TWICE for ONE text (real_event_id 'fub:textMessagesCreated:296538' at 16:42, then 'fub:fub_text:296538' at 20:40 with source '' — the only fub_text: spelling in the whole table) and fired registration_completed (Zillow, 16:50) with NO activity card of that kind ever filed. Both fires removed with backup (~/hl-reversals/hl_trigger_fire-jalil-duplicate-and-orphan-2026-09-10.json). ROOT CAUSES to fix on the roads: (a) a second spelling of the same real_event_id defeats the (event_key, real_event_id) unique index — one spelling per source; (b) the Zillow registration road fires without filing its card. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] PHONE-BORN LEADS' ARRIVAL CARDS NAME NO CAUSE (9/10, 7 leads since 9/08 on phone_analegacy / phone_company): lead_created.reason_evidence_id was NULL while the phone_call_received row sits 2 s later — the desk backfilled the cause for the 7 (backup ~/hl-reversals/hl_activity_log-lead_created-phone-cause-backfill-2026-09-10.json) but the mint fire still cannot be exempted because the fire's real_event_id is NULL. Fix the phone roads to stamp reason_evidence_id AND real_event_id at mint; then the '37 arrival cards name no resolvable cause' number falls. — PHONE-BORN LEADS' ARRIVAL CARDS NAME NO CAUSE (9/10, 7 leads since 9/08 on phone_analegacy / phone_company): lead_created.reason_evidence_id was NULL while the phone_call_received row sits 2 s later — the desk backfilled the cause for the 7 (backup ~/hl-reversals/hl_activity_log-lead_created-phone-cause-backfill-2026-09-10.json) but the mint fire still cannot be exempted because the fire's real_event_id is NULL. Fix the phone roads to stamp reason_evidence_id AND real_event_id at mint; then the '37 arrival cards name no resolvable cause' number falls. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ⛔ A LOCALHOST WALK RUNS PROD'S PIPELINE (judge-qa-bench-r8 9/10, measured: a local turn emitted myli/memory.process, Inngest ran PROD's unguarded extractor, run 01M25P3PDB5JYGQZYSEM1BZW87, and 67 s later 8 active facts landed on the persona — six citing only Myli's own sentences, re-minting two slots S-25 had parked). Local dev and prod share ONE Inngest app/event key, so every localhost drive writes to prod's memory through prod's code. Fix: a dev Inngest environment (separate event key + app) or an env-gated emitter; until then every walker must know its memory pass runs on prod (memory saved by the judge). Then S-30: push lands → PUT sync → prove one prod run refuses by name → re-park post-11:56Z rows under a new reversal → a zero that stays zero. — ⛔ A LOCALHOST WALK RUNS PROD'S PIPELINE (judge-qa-bench-r8 9/10, measured: a local turn emitted myli/memory.process, Inngest ran PROD's unguarded extractor, run 01M25P3PDB5JYGQZYSEM1BZW87, and 67 s later 8 active facts landed on the persona — six citing only Myli's own sentences, re-minting two slots S-25 had parked). Local dev and prod share ONE Inngest app/event key, so every localhost drive writes to prod's memory through prod's code. Fix: a dev Inngest environment (separate event key + app) or an env-gated emitter; until then every walker must know its memory pass runs on prod (memory saved by the judge). Then S-30: push lands → PUT sync → prove one prod run refuses by name → re-park post-11:56Z rows under a new reversal → a zero that stays zero. <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] THE BRIEF-ISSUANCE GATE (tools/desk/issue-brief.mjs): every desk brief's mechanism claims bound to evidence or demoted to hypotheses; 5→8 canaries red-proven; first live use 9/10 caught 2 real unverified claims (ordered by the second opinion after 11 unverified mechanisms in 24h) — THE BRIEF-ISSUANCE GATE (tools/desk/issue-brief.mjs): every desk brief's mechanism claims bound to evidence or demoted to hypotheses; 5→8 canaries red-proven; first live use 9/10 caught 2 real unverified claims (ordered by the second opinion after 11 unverified mechanisms in 24h) <sub>*Added 2026-09-10 via the task door. RETIRED 2026-09-10 via the task entrance (desk) — desk: duplicate add (the desk's key-capture attempt added it twice); c155cfd2 is the one.*</sub>
- [ ] WIREFRAMES ARE THREE BANDS OF CARDS — pending sign-off · signed off · drafts — with the drawing's own date (#257.386c-ii), and the PM day cards (#257.386c-i) — WIREFRAMES ARE THREE BANDS OF CARDS — pending sign-off · signed off · drafts — with the drawing's own date (#257.386c-ii), and the PM day cards (#257.386c-i) <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] THE BRIEF-ISSUANCE GATE (tools/desk/issue-brief.mjs): every desk brief's mechanism claims bound to evidence or demoted to hypotheses; 5→8 canaries red-proven; first live use 9/10 caught 2 real unverified claims — THE BRIEF-ISSUANCE GATE (tools/desk/issue-brief.mjs): every desk brief's mechanism claims bound to evidence or demoted to hypotheses; 5→8 canaries red-proven; first live use 9/10 caught 2 real unverified claims <sub>*Added 2026-09-10 via the task door.*</sub>
- [x] Signed-in home page: the Myli section / hero renders crushed after sign-in (Zach live 9/10) — Signed-in home page: the Myli section / hero renders crushed after sign-in (Zach live 9/10) <sub>*Added 2026-09-10 via the task door. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/website-ui/AUDIT-signed-in-home-2026-09-11.md. audit-signed-in-home measured anon/active-buyer/past-client at 390 and 1440 on HEAD and prod: the SAME MyliShowcaseCard in the same wrapper, nothing crushed; the 12:07–12:51 CT fold (353f66f2fe, on prod since ~17:00 CT) closed it; cage home-row-card.test.ts V5-9..9h green 161/161*</sub> `FRI 9/11`
- [ ] QA bench: eight test leads (Riley, Morgan, Taylor, Zach Test, the stage probes) sit on the 'website' source whose handoff_user_id is a REAL agent — the showing-partner guard refuses a test lead resolving a production partner (it hit the Gauntlet lead on the 'other' source at push #23 run 32/33; moved to test_showing_partner, backed up). The bench's personas need a test partner without leaving the website source: a per-lead test handoff or the website source's test-lead branch — the bench owner's call — QA bench: eight test leads (Riley, Morgan, Taylor, Zach Test, the stage probes) sit on the 'website' source whose handoff_user_id is a REAL agent — the showing-partner guard refuses a test lead resolving a production partner (it hit the Gauntlet lead on the 'other' source at push #23 run 32/33; moved to test_showing_partner, backed up). The bench's personas need a test partner without leaving the website source: a per-lead test handoff or the website source's test-lead branch — the bench owner's call <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] QA bench: the persona lease does not cover a persona born through the PRODUCT's registration road (a lane walking /register mints newly-registered as a side effect; no bench script consults the lock) — a chat-road lease for test-band identities, or per-run cloned personas (ChatGPT's preferred long-term shape) — QA bench: the persona lease does not cover a persona born through the PRODUCT's registration road (a lane walking /register mints newly-registered as a side effect; no bench script consults the lock) — a chat-road lease for test-band identities, or per-run cloned personas (ChatGPT's preferred long-term shape) <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] ONE_ANSWER_TABLE: two slot vocabularies disagree — the registry's RECITED_SLOT says "$20k down" IS a down-payment recital (pill gate) while on-record's SLOT_WORDS says it is slot-less (the stamp); one phrase, two verdicts — fold to one vocabulary with a red-proof on the live corpus — ONE_ANSWER_TABLE: two slot vocabularies disagree — the registry's RECITED_SLOT says "$20k down" IS a down-payment recital (pill gate) while on-record's SLOT_WORDS says it is slot-less (the stamp); one phrase, two verdicts — fold to one vocabulary with a red-proof on the live corpus <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] The CRM files the one-voice conversation TWICE from the debts question on (43 hl_messages rows for 24 turns): the CRM turn key is an ARRAY POSITION and three writers shape the transcript differently (persistConversation keeps hidden engine lines; the birth replay reads one moment; the modal's snapshot PUT strips hidden+opener) → key on the ledger's durable hl_chat_events.id, ONE writer of the projection — second opinion first (defect class) — The CRM files the one-voice conversation TWICE from the debts question on (43 hl_messages rows for 24 turns): the CRM turn key is an ARRAY POSITION and three writers shape the transcript differently (persistConversation keeps hidden engine lines; the birth replay reads one moment; the modal's snapshot PUT strips hidden+opener) → key on the ledger's durable hl_chat_events.id, ONE writer of the projection — second opinion first (defect class) <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] The phone step (H2) costs 7.7 s server-side — the CRM birth + the Twilio send ride the turn's critical path against a 1.0 s bar; the reply paints first, the birth runs behind it (Inngest), only the code's send is awaited — The phone step (H2) costs 7.7 s server-side — the CRM birth + the Twilio send ride the turn's critical path against a 1.0 s bar; the reply paints first, the birth runs behind it (Inngest), only the code's send is awaited <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] Fact evidence: the extraction road (website:dashboard-chat) and the derived road (crm:react-search, the ai_searched harness proof) fill source_activity_ids but leave evidence_activity_id NULL — two columns for one pointer; the cage reads the second, the writers fill the first (push #23 run 43, 4 Probe facts quarantined, reversal ~/hl-reversals/fact-evidence-probe-quarantine-BEFORE-20260910T184010Z.json) — one column, the writer sets it, the twin folds — Fact evidence: the extraction road (website:dashboard-chat) and the derived road (crm:react-search, the ai_searched harness proof) fill source_activity_ids but leave evidence_activity_id NULL — two columns for one pointer; the cage reads the second, the writers fill the first (push #23 run 43, 4 Probe facts quarantined, reversal ~/hl-reversals/fact-evidence-probe-quarantine-BEFORE-20260910T184010Z.json) — one column, the writer sets it, the twin folds <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] QA bench S-50 (desk-carded from r11): the confirmed-output fact path named in the desk's S-43 ruling does not exist (grep 0) — if confirmed output is to be a fact it is written by the ONE extractor only, or it is not written; decide, then build or close — QA bench S-50 (desk-carded from r11): the confirmed-output fact path named in the desk's S-43 ruling does not exist (grep 0) — if confirmed output is to be a fact it is written by the ONE extractor only, or it is not written; decide, then build or close <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] Activity log: conversation_id wants a COLUMN (a pointer at the chat ledger), not a key inside meta — the feed's conversation pull-forward (load-lead-feed.ts ~1054) filters it in JS today to stay under the costume floor; DDL at the desk with the STRUCTURE_GATE interview, writer stamps it, the pull-forward keys on the column — Activity log: conversation_id wants a COLUMN (a pointer at the chat ledger), not a key inside meta — the feed's conversation pull-forward (load-lead-feed.ts ~1054) filters it in JS today to stay under the costume floor; DDL at the desk with the STRUCTURE_GATE interview, writer stamps it, the pull-forward keys on the column <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] Review queue: a human's decision (approve / reject / edit) on a held Myli draft has NO CATALOG CARD — it lives on the intent row only (judge r1 finding 4) — a catalog kind through the one writer, harness-proven, so the lead's record shows who decided and when — Review queue: a human's decision (approve / reject / edit) on a held Myli draft has NO CATALOG CARD — it lives on the intent row only (judge r1 finding 4) — a catalog kind through the one writer, harness-proven, so the lead's record shows who decided and when <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] Mission Control's testBandOnly default hides REAL people until ?band=all — the day the real-person hold is on, a human opening Mission Control sees no real drafts (judge r1) — the default follows the switch: real-person mode ON → real people shown — Mission Control's testBandOnly default hides REAL people until ?band=all — the day the real-person hold is on, a human opening Mission Control sees no real drafts (judge r1) — the default follows the switch: real-person mode ON → real people shown <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] Review queue proof ⑧/⑨ (desk-edit-containment.proof.ts) assert the INTENT-gate refusal shape (status='blocked', MASTER_AI_DISABLED, comms-security.ts:1626) while the refusal lands at the WIRE whose recorder markIntentFailed (:1679) writes status='failed' and OVERWRITES decision_reason with the error text — ONE refusal shape for both rungs (the reason column keeps the rung's name; the error text goes beside it), then the two arms re-cut to it — Review queue proof ⑧/⑨ (desk-edit-containment.proof.ts) assert the INTENT-gate refusal shape (status='blocked', MASTER_AI_DISABLED, comms-security.ts:1626) while the refusal lands at the WIRE whose recorder markIntentFailed (:1679) writes status='failed' and OVERWRITES decision_reason with the error text — ONE refusal shape for both rungs (the reason column keeps the rung's name; the error text goes beside it), then the two arms re-cut to it <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] lib/myli-legacy/dynamic-pills.ts:95 still carries the banned lender hand-off sentence (#257.248d) — NOT dead: app/api/search/nlp/route.ts falls back to it whenever MYLI_NEXT != '1' (prod + dev both = 1 today) — retire the legacy fallback by making the route stop reading it (the flag's off-road is the loaded gun behind a flag), or give it the same registry-derived fix; a plant with MYLI_NEXT unset proves which — lib/myli-legacy/dynamic-pills.ts:95 still carries the banned lender hand-off sentence (#257.248d) — NOT dead: app/api/search/nlp/route.ts falls back to it whenever MYLI_NEXT != '1' (prod + dev both = 1 today) — retire the legacy fallback by making the route stop reading it (the flag's off-road is the loaded gun behind a flag), or give it the same registry-derived fix; a plant with MYLI_NEXT unset proves which <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] THE PAGE CONTEXT (#257.466 crux, Zach 9/10 5:35 pm): every Myli turn on a place/search page is handed THE PAGE as deterministic context — place id/name/type, the page's OWN rendered counts, the shelf + pill labels with their counts, the active filters — built server-side at render, stamped, sent with every turn as structured context; she may not state a number absent from it; the header greeting derives from the same object (second opinion asked 9/10 evening — build waits on the receipt) — THE PAGE CONTEXT (#257.466 crux, Zach 9/10 5:35 pm): every Myli turn on a place/search page is handed THE PAGE as deterministic context — place id/name/type, the page's OWN rendered counts, the shelf + pill labels with their counts, the active filters — built server-side at render, stamped, sent with every turn as structured context; she may not state a number absent from it; the header greeting derives from the same object (second opinion asked 9/10 evening — build waits on the receipt) <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] The chat opener painted a garbled status line — "Couldn't re the result: searching homes in Johnson County, searching homes in Overland Park, and 8 more" (#257.466 f2): a truncated/ungrammatical status shown as prose; status lines are a fixed vocabulary keyed to real tool events, buffered then revealed whole, never a broken sentence — The chat opener painted a garbled status line — "Couldn't re the result: searching homes in Johnson County, searching homes in Overland Park, and 8 more" (#257.466 f2): a truncated/ungrammatical status shown as prose; status lines are a fixed vocabulary keyed to real tool events, buffered then revealed whole, never a broken sentence <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] After a typed answer ("675 — top of my budget") the chat jumped to the BOTTOM of the screen instead of anchoring on her new reply (#257.466 f3) — the panel anchors the viewport on the top of her newest turn — After a typed answer ("675 — top of my budget") the chat jumped to the BOTTOM of the screen instead of anchoring on her new reply (#257.466 f3) — the panel anchors the viewport on the top of her newest turn <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] Her picks surfaced a COMING-SOON listing whose "photo" is a Google Street View of an empty lot (142nd St, #257.466 f4) — coming-soon listings are EXCLUDED from her recommendations; a street-view/satellite/lot image is not a photo and is filtered before a card renders — Her picks surfaced a COMING-SOON listing whose "photo" is a Google Street View of an empty lot (142nd St, #257.466 f4) — coming-soon listings are EXCLUDED from her recommendations; a street-view/satellite/lot image is not a photo and is filtered before a card renders <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] Home cards in chat are far too big — three cards hid her sentence and the pills beneath (#257.466 f5): her picks render as a horizontal SLIDE of three SMALL preview cards (the home page's preview card, HOME_RENDER_LAW's one card), swipeable left-right, her closing line and pills visible under it — Home cards in chat are far too big — three cards hid her sentence and the pills beneath (#257.466 f5): her picks render as a horizontal SLIDE of three SMALL preview cards (the home page's preview card, HOME_RENDER_LAW's one card), swipeable left-right, her closing line and pills visible under it <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] The pills did not match her question (#257.466 f6): she asked "a specific city or part of Johnson County · MAXIMUM bedroom count · a certain style" and the pills read "a specific part of Johnson County · MINIMUM bedroom count · certain style of home" — the pills are the question's own words (MYLI_CONVERSATION_STYLE rule 19), one vocabulary; after the picks the pills are: See all 1,257 on the main screen · What can I afford? · Nail down my must-haves · Keep browsing — The pills did not match her question (#257.466 f6): she asked "a specific city or part of Johnson County · MAXIMUM bedroom count · a certain style" and the pills read "a specific part of Johnson County · MINIMUM bedroom count · certain style of home" — the pills are the question's own words (MYLI_CONVERSATION_STYLE rule 19), one vocabulary; after the picks the pills are: See all 1,257 on the main screen · What can I afford? · Nail down my must-haves · Keep browsing <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] lib/myli/dynamic-pills.ts is a KILL-SWITCH-ONLY road today (judge-qa-bench-r14, measured: on MYLI_NEXT=1 + agent voice ON the typed turn never runs generateDynamicPills — route.ts:1231/:1513/:1649; pillsForTurn only at :1549/:3649, unreachable from the modal; the live pills come from bindActions over the registry) — a second, never-exercised pill engine: retire it or make it the one road; the desk decides, then the call sites stop reading whichever dies — lib/myli/dynamic-pills.ts is a KILL-SWITCH-ONLY road today (judge-qa-bench-r14, measured: on MYLI_NEXT=1 + agent voice ON the typed turn never runs generateDynamicPills — route.ts:1231/:1513/:1649; pillsForTurn only at :1549/:3649, unreachable from the modal; the live pills come from bindActions over the registry) — a second, never-exercised pill engine: retire it or make it the one road; the desk decides, then the call sites stop reading whichever dies <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] Her PROSE still says "before you talk to a lender" on 2 of 3 drives (judge r13/r14, #257.248d — the hand-off framing in her own sentence, not the pills): a JUDGE ASSERTION on the agent road's output (THE_UNHOBBLING rule 1: a defect is answered by a judge arm or a tool, never a prompt paragraph), red-proven on the captured wire — Her PROSE still says "before you talk to a lender" on 2 of 3 drives (judge r13/r14, #257.248d — the hand-off framing in her own sentence, not the pills): a JUDGE ASSERTION on the agent road's output (THE_UNHOBBLING rule 1: a defect is answered by a judge arm or a tool, never a prompt paragraph), red-proven on the captured wire <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] At 390 on a brand-new profile's FIRST typed turn ("how do I get pre-approved?") the chips answer a different question than the prose asks (r1 RADAR 6, reproduced by r14 — not stale state): the debt chips render under a yes/no ask — the pills are the question's own words (one vocabulary, rule 19) and the chip row is chosen from the question the model actually asked — At 390 on a brand-new profile's FIRST typed turn ("how do I get pre-approved?") the chips answer a different question than the prose asks (r1 RADAR 6, reproduced by r14 — not stale state): the debt chips render under a yes/no ask — the pills are the question's own words (one vocabulary, rule 19) and the chip row is chosen from the question the model actually asked <sub>*Added 2026-09-10 via the task door.*</sub>
- [x] THE MYLI SSOT AUDIT (#257.468): the state of the union — every table, registry, map, prompt fragment, composer, projection and writer that touches Myli (brain · output · pills · thinking/status · facts · memory · the conversation ledger · the CRM projection) with readers/writers by file:line, and every fact with two or more authorities (today's measured list in the ruling) — a Fable audit, no code; then the second opinion with the audit's numbers; then ONE plan: the target model, the folds in order, the cage per fold (the Bedrock shape) — THE MYLI SSOT AUDIT (#257.468): the state of the union — every table, registry, map, prompt fragment, composer, projection and writer that touches Myli (brain · output · pills · thinking/status · facts · memory · the conversation ledger · the CRM projection) with readers/writers by file:line, and every fact with two or more authorities (today's measured list in the ruling) — a Fable audit, no code; then the second opinion with the audit's numbers; then ONE plan: the target model, the folds in order, the cage per fold (the Bedrock shape) <sub>*Added 2026-09-10 via the task door. TICKED 2026-09-10 via the task entrance (desk) — receipt: .claude/reports/myli-os/AUDIT-myli-ssot-2026-09-10.md. 38 duplications, six diseases, nine questions; Fable, read-only*</sub> `THU 9/10`
- [ ] FLOW JUDGE: the myli-consumer journey aborts at move 0 — MOVE_NOT_REACHED, no element matches button[aria-label^="Ask Myli"]; the live home page serves NO aria-label containing Myli (lane-fold-claim-refs 9/11, verified independently) — the journey config's selector is stale against the current home page, so NUMBER_UNTRUE has not been evaluated on this flow; re-anchor the selector on the V5 card's real control and re-run — FLOW JUDGE: the myli-consumer journey aborts at move 0 — MOVE_NOT_REACHED, no element matches button[aria-label^="Ask Myli"]; the live home page serves NO aria-label containing Myli (lane-fold-claim-refs 9/11, verified independently) — the journey config's selector is stale against the current home page, so NUMBER_UNTRUE has not been evaluated on this flow; re-anchor the selector on the V5 card's real control and re-run <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] flow-judge: WALKER_THREAD_DEAD on the myli-consumer journey predates F3.3 (measured 9/11) — the walker's thread dies before the judge runs; find the mechanism (a Playwright context torn down? the walker's own timeout?) and fix at the root so a dead walker is a named RED, never a silent abort — flow-judge: WALKER_THREAD_DEAD on the myli-consumer journey predates F3.3 (measured 9/11) — the walker's thread dies before the judge runs; find the mechanism (a Playwright context torn down? the walker's own timeout?) and fix at the root so a dead walker is a named RED, never a silent abort <sub>*Added 2026-09-11 via the task door.*</sub>

### The financing line — ruled, and ruled wider — 10/30

- [x] ⛔ **BLOCKS** — Rule the financing line: Myli owns the LOGISTICS, hands off the ADVICE — Myli answers any money question — how much house, what a loan looks like, what a payment might run — always saying out loud that it is a rough estimate, and she offers to hand you to John Nelson at Fountain Mortgage for the real number. <sub>*RULED 8/24, and ruled WIDER than this item proposed: "Myli owns questions and guidance. Myli is HouseLoop's ChatGPT." She answers any financing question and can talk loan products, always with the rough-estimate disclosure, and offers to connect people to John Nelson at Fountain Mortgage for the real number*</sub> `MON 8/24`
- [x] Playbook actions surface in the feed — census every move, weld the silent writers — Zach found playbooks doing real work invisibly: 'started the playbook' then nothing, while Myli texted, pinged Trish, booked and sent agreements underneath. Every playbook move files its card on the lead, and the census becomes a standing gate. <sub>*GRINDER-LANDED r3 — receipt: .claude/reports/grinder/myli-os/1788066516-receipt-r3.md · verdict: .claude/reports/grinder/myli-os/verdict-r3-1788066516269.md*</sub> `SUN 8/30`
- [x] Build the warm nurture band — hot's message types at the bi-weekly pace, on the existing Nurture Warm stage — Zach ruled warm's voice (8/29): same as hot, just slower — a house, a tour, or the dashboard every two weeks. The stage already exists and has never received a lead; the band of moves is the missing piece, with demotion to cold after 30 silent days. <sub>*GRINDER-LANDED r1 — receipt: .claude/reports/grinder/myli-os/1788068534-receipt-warm-band.md · verdict: .claude/reports/grinder/myli-os/verdict-r1-1788068714572.md*</sub> `SUN 8/30`
- [x] Silence files a card — the watch moves get a quiet feed entry — When Myli deliberately holds off, the feed says so: 'Myli held off — next touch scheduled Sep 5.' Her restraint becomes visible as a decision, through a new catalog kind with its harness proof. <sub>*Ruling #74.2-0829; folds with the feed-weld work (session #82 slice 4). TICKED 2026-09-10 via the task entrance (auto:grinder) — receipt: .claude/reports/grinder/myli-os/verdict-r2-1789048114573.md. GRINDER-LANDED r2 — receipt: .claude/reports/grinder/myli-os/1789044461-receipt-r2.md*</sub> `THU 9/3`
- [x] Let the review queue see a real person: run Myli's Desk on real leads with a human approving every message — The desk works - held drafts, approve, edit, reject, and it really sends. It has just never seen a real client: all 612 drafts so far are test records. This is the entire distance between us and a competitor whose team reviews hundreds of drafts every morning. <sub>*Added 2026-08-30 via the task door. TICKED 2026-09-10 via the task entrance (Lee) — receipt: .claude/reports/grinder/myli-os/verdict-r2-1789075156875.md. BUILT over two rounds + the desk's batch: the real-person hold (a draft aimed at any non-test lead is ALWAYS held; the switch stays OFF for real people); the decision ledger on /ai/review-queue names sent / refused-by-name / discarded and survives refresh (judge r2 GREEN by its own hands at 640, 390, 1440); the edit-road proof runs again (fixture by shape, open-run predicate). Judge r2's three one-liners applied at the desk (doubled 'waiting for you'; the double period; the fixture pick). Residuals carded: the 390 shell column (mobile-crm-app), a catalog kind for the human's decision, Mission Control's testBandOnly default, proof arms ⑧/⑨ (the wire recorder overwrites decision_reason), the intent gate's ai_master read on dispatch (send-safety, unverified), the walker's cookie arm + fixture facility (order 4). Zach's switch: ai_test_mode off + a named_lead grant per person — untouched*</sub> `MON 8/31`
- [ ] Nothing ages or clears a held draft - 33 have sat five days — Drafts waiting for approval never expire and nothing reminds anyone they are there. A queue nobody drains gets drained in a hurry, which is how a bad message gets approved. Needs an age signal on the card, a morning nudge, and a rule for what happens to a draft nobody answered. <sub>*Added 2026-08-30 via the task door. · SUPERSEDED 2026-09-07 (board curation 2026-09-07, lane-myli-os-board (Fable curator, under Lee's desk)).*</sub> `MON 8/31`
- [ ] The approve button says the wrong thing on group iMessage drafts — Twelve of the fifty-six waiting drafts are group iMessages, and their button reads Approve instead of Approve and send - so it looks like it only files something when it actually sends a message to people. <sub>*Added 2026-08-30 via the task door. RETIRED 2026-09-09 via the task entrance (desk) — #257.379a: no group-iMessage drafts on Myli's road under Twilio.*</sub> `TUE 9/1`
- [x] ⭐ FIRST TASK MONDAY — FABLE 5 writes THE ROAD TO AUTONOMOUS MYLI roadmap (9 weeks): brief .claude/state/briefs/MONDAY-fable5-autonomous-myli-roadmap.md · board /boards/road-to-autonomous-myli — Fable 5 writes the real nine-week game plan to fully autonomous Myli — starting by checking whether those 49 approvals were a person or the AI judge. <sub>*Added 2026-08-30 via the task door. TICKED 2026-08-31 via the task door — receipt: .claude/reports/myli-os/AUTONOMOUS-ROADMAP.md. Delivered by the fable-roadmap lane (#90.3): step 0 answered (ZERO human approvals ever), nine-week shape, boarded findings.*</sub> `MON 8/31`
- [ ] The OUTBOX: one screen showing what Myli DID · what she WOULD have done · how the judge scored it (Zach's named missing build, #90) — A place to see every draft Myli made and how she was graded. <sub>*Added 2026-08-30 via the task door. · SUPERSEDED 2026-09-07 by MC-r5 (the 9/05 lock (board curation 2026-09-07, lane-myli-os-board (Fable curator, under Lee's desk)).*</sub> `MON 8/31`
- [ ] Myli's brain sees EVERY listing field (neighborhood/tax/schools + the whole Repliers record) — the law, not a fact sheet (#90) — Give Myli the whole listing record so she can answer tax and school questions. <sub>*Added 2026-08-30 via the task door.*</sub> `MON 8/31`
- [ ] AUTHOR a playbook: playbooks are born only in SQL migrations and the page is read-only — the first broken link to autonomy (#90) — Be able to create a new lead playbook and turn it on, without a developer. <sub>*Added 2026-08-30 via the task door. · SUPERSEDED 2026-09-07 (board curation 2026-09-07, lane-myli-os-board (Fable curator, under Lee's desk)).*</sub> `MON 8/31`
- [ ] Measure THE JUDGE: what it is, how often it runs, what it scores, and where a person can read the score (#90) — Find out how much the AI grader actually runs and make its scores visible. <sub>*Added 2026-08-30 via the task door. · SUPERSEDED 2026-09-07 by #257.276 (board curation 2026-09-07, lane-myli-os-board (Fable curator, under Lee's desk)).*</sub> `MON 8/31`
- [x] Held drafts never age or expire — 33 have sat 5 days; a queue nobody drains gets drained in a hurry (#90) — Old drafts should expire instead of piling up. <sub>*Added 2026-08-30 via the task door. CLOSED AS DUPLICATE 8/31 (monday-audit defect #1): same floor/band/day as the older twin "Nothing ages or clears a held draft" — no work performed; the twin stays open.*</sub> `MON 8/31`
- [ ] ONE playbook surface: settings page renders a 4-frame hardcoded GOAL_FRAMES constant while the engine runs the 6-row hl_playbooks table — point every surface at the TABLE before any editor is built (roadmap finding) — The playbook page and the playbook engine show two different sets. One truth first, then the editor. <sub>*Added 2026-08-31 via the task door.*</sub> `TUE 9/1`
- [x] ONE fact resolver: lead-conversation.ts has schools-no-tax, resolve-listing.ts has tax-no-schools — what Myli can quote depends on which tool fires. One derived resolver over the whole listing record (#90 the law) — Myli's answers change depending on which internal tool she happens to use. Give her one complete fact source. <sub>*Added 2026-08-31 via the task door. TICKED 2026-08-31 via the task door — receipt: .claude/reports/myli-facts-2026-08-31/RECEIPT.md. Census found a THIRD disagreeing sheet; all three now on listing-facts.ts (61-key sheet, honest absence, status-gated). Desk capstone: resolve-listing.test.ts + the 41-arm proof both re-run GREEN by the desk's own hands. Two truth defects fixed en route (status U ≠ under-contract; the dont-recite instruction split).*</sub> `MON 8/31`
- [x] ⭐ #91 THE COCKPIT RUN-LOOP: persona → Myli acts → judge grades → copywriter critiques → receipt files on the TEST LEAD'S CenterLane → repeat until green. The three scenarios: Zillow showing · new lead · reactivation — Myli runs full scenarios against test people inside the cockpit, gets graded, and every run shows up on that test lead's own record. <sub>*Added 2026-08-31 via the task door. TICKED 2026-08-31 via the task door — receipt: .claude/reports/first-cockpit-run-2026-08-31/RECEIPT.md. THE LOOP IS PROVEN END TO END on run 072fff12 — first real grades ever (6/6, exactly-two, every fail named+quoted), receipt on the CenterLane in order. The product's RED is the loop's OUTPUT, not its failure. Scenarios 2-3 pend the line reallocation + budget ruling.*</sub> `MON 8/31`
- [ ] #91 THE TWO GRADERS: a judge that approves each Myli message + a copywriter that says how it improves — both scores PERSISTED and visible in the cockpit (closes the no-score gap from #90.3) — Every Myli message gets a grade and a better-version note, written down where you can see them. <sub>*Added 2026-08-31 via the task door.*</sub> `TUE 9/1`
- [ ] Goal Frames need a surface: live (4 leads enrolled), driving the Director, and now INVISIBLE — their only window was the playbooks page they didn't belong on. A Goal Frames board is a new surface (Zach sign per UI doctrine) — The Director's goal frames are running live with no screen showing them. They need their own small board. <sub>*Added 2026-08-31 via the task door. · SUPERSEDED 2026-09-07 by #257.287 §4 + #257.258 (board curation 2026-09-07, lane-myli-os-board (Fable curator, under Lee's desk)).*</sub>
- [ ] Fact-sheet cage: no file outside listing-facts.ts may compose a listing fact sheet (census-style, shrink-only — named by the myli-facts receipt, not yet built) — A guard so nobody quietly builds a fourth fact list. <sub>*Added 2026-08-31 via the task door.*</sub>
- [x] ⏳ ZACH PURCHASE: a third lead-facing Twilio test number (provision + census as category='test') — unblocks the THIRD concurrent cockpit scenario (reactivation); allocator needs zero code change when it lands — Buy one more test phone number so all three scenarios can run at once. <sub>*Added 2026-08-31 via the task door. RETIRED 8/31 by ruling #93.1: no purchase — Zach says we have plenty of test Twilio lines; replaced by the census/reallocate task.*</sub> `MON 8/31`
- [ ] Split the strayTexts containment alarm (desk-APPROVED): classifyIsolation() = ESCAPE (real person reached, P0) vs CROSS-TALK (test lead got another scenario's message) — runner.ts:1331-1338, ships with its own red-proof (rig-line-allocator finding) — The test-run alarm currently cries wolf when two scenarios run at once. Split it so the real alarm stays sharp. <sub>*Added 2026-08-31 via the task door.*</sub> `TUE 9/1`
- [ ] ⭐ THE GIANT INBOX (#93.3): one inbox of leads, Myli's draft rendered IN each lead's iPad lane based on what happened, sign-off-to-send — the OUTBOX + Myli's Desk + CenterLane converged (design brief after FUA research + cockpit Lane D land) — One big inbox: each lead shows what Myli would do next, right on their record, and you tap to approve. <sub>*Added 2026-08-31 via the task door. · SUPERSEDED 2026-09-07 by MC-r5 (the 9/05 lock): Mission Control IS the one inbox (board curation 2026-09-07, lane-myli-os-board (Fable curator, under Lee's desk)).*</sub> `TUE 9/1`
- [ ] REAL PAYLOADS on the band (#93.4): persona activity = real FUB/Zillow webhook shapes replayed on test leads + real hl_pixel_event rows from real houseloop.ai browsing — the scenario engine rides the production tapes — Test people generate the same real signals a real Zillow lead does. <sub>*Added 2026-08-31 via the task door.*</sub> `TUE 9/1`
- [ ] FUA steal #3 — THE SLA CLOCK: an SLA row per lead (slaMinutes, nullable slaMet), bell at breach, escalation past it — STRUCTURE_GATE interview FIRST, then Inngest or it doesn't exist (the 157-of-205-no-outbound gap) — Every new lead gets a countdown clock and an alarm if nobody touches them in time. <sub>*Added 2026-08-31 via the task door.*</sub> `TUE 9/1`
- [ ] GIANT INBOX design inputs locked (#93.3 + FUA): the 8-part card grammar (WHY line, quoted trigger, dollar chip, pre-declared side-effects, one transport-named button) · the NEXT-UP queue cursor · counts that name their subset (fix the desk's silent caps + #99 in the same pass) · the cadence-advisor block rendered ON the card (what the model saw = what the human sees) — The design checklist for the big approve-inbox, with the best of the competitor folded in. <sub>*Added 2026-08-31 via the task door. · SUPERSEDED 2026-09-07 by MC-r5 (the 9/05 lock) (board curation 2026-09-07, lane-myli-os-board (Fable curator, under Lee's desk)).*</sub> `TUE 9/1`
- [ ] P1 (run 072fff12 F1): TWO VERDICTS, ONLY THE FLATTERING ONE VISIBLE — the cockpit shows the runner's GREEN 2/2 everywhere while the graders' RED 20-failures appears nowhere; render both verdicts side by side on the run ledger + the scored card — The test screen says the run went great while the judges say it failed. Show both. <sub>*Added 2026-08-31 via the task door.*</sub> `TUE 9/1`
- [ ] Myli contradicts listing status inside one thread (run 072fff12): pending/active flip-flopped between turns; stale where_they_stand line feeds her context naming only the 8/23 request — locate the writer, make the sentence derive live (composes with the one fact resolver) — Myli told the lead two opposite stories about the same house. Find and fix the stale sentence she reads. <sub>*Added 2026-08-31 via the task door.*</sub> `TUE 9/1`
- [ ] Rig blockers B2+B3 (run 072fff12): the send budget makes the 4-send Zillow scenario arithmetically unrunnable (rig-scoped exemption = a Zach/desk safety ruling, on Your Calls) + no conversation reset between runs (design the sanctioned reset, never a browser-triggered purge) — Two test-rig limits: the message cap blocks the richest scenario, and old test chatter poisons the next run. <sub>*Added 2026-08-31 via the task door.*</sub> `TUE 9/1`
- [ ] #94.6 THE TOOLBOX CONVERSION: Zach's 16 enumerated tools mapped onto the live tool-manifest; per-GOAL grants via hl_playbooks.tools (the empty column); pill flows become tool calls (masterplan-synthesis carries the map) — Everything Myli can do becomes a named tool that playbooks grant per goal. <sub>*Added 2026-08-31 via the task door.*</sub> `TUE 9/1`
- [ ] #94.7+.8 THE SCORE ENGINE: high-intent signals move the score, threshold crossings mint an activity kind + trigger a playbook; the score panel (score·velocity·MOMENTUM·tier·churn·response·next action) visible on every lead; grade the existing book; delete the fake field — A real lead score that reacts to behavior, shows its direction, and switches playbooks when it crosses a line. <sub>*Added 2026-08-31 via the task door.*</sub> `TUE 9/1`

### The A bricks — the frame stack — 7/7

- [x] The correction first: the feedback moves leave the coordination playbook and become their own — Closed August 23 by the coordination lane. The follow-up asks now live on their own feedback playbook, switched off and limited to test people. The next piece is the real start of this floor. <sub>*CLOSED 8/23 night by the coordination lane — the feedback asks now live on their own `showing_feedback` playbook, disabled and test-banded. The next brick (A0) is the real start of this floor*</sub>
- [x] Give Myli a stack of goals instead of a single one, with one door for changing it — Nothing changes in the database; the field has never been used — no conversation carries a goal today. <sub>*CLOSED 8/28: the run stack is DERIVED (runStackFor — status IN (open,suspended), most-recently-parked first), runs.ts is the ONE writer, migration 0912 applied. Gate: test:playbook-suspend 81/0/0, exit 0. Receipt: .claude/reports/state-stack/RECEIPT.md*</sub> `WED 8/26`
- [x] Record where Myli was when she got interrupted, and work it out fresh when she returns rather than trusting an old snapshot — When Myli gets interrupted part-way through a job she now writes down where she was, not a photograph of the whole situation, and works the rest out fresh when she picks it back up — so she can never act on a stale picture. <sub>*CLOSED 8/28: the suspend writes a POSITION, never a photograph — the whole block is re-derived at read time, and the interruption context reaches moveGoalFor. Gate: test:playbook-suspend 81/0/0, exit 0. Receipt: .claude/reports/state-stack/RECEIPT.md*</sub> `THU 8/27`
- [x] Let one conversation pause and another pick it back up, with both moves visible on the canvas — The desk signs the database change, and that change waits for the code that reads it to be live — never just committed. <sub>*GRINDER-LANDED r3 — receipt: .claude/reports/grinder/myli-os/1788003198-receipt-r3.md · verdict: .claude/reports/grinder/myli-os/verdict-r3-1788003292062.md (blocked_by cleared on tick-audit 8/31: HOLD_0925 — the paused and resumed card kinds are drafted and unsigned. The mechanism is proven (81/0/0); the half you can SEE waits on the desk signing those two kinds)*</sub> `THU 8/27`
- [x] Give Myli teeth in the chat: you ask, she drafts, you approve, it sends — She can act from a conversation now instead of only talking about acting. You give the order, she shows you exactly what she would send, you approve it, and it goes - walked end to end, on test people only. The daily brief landed in the same pass. <sub>*Lane receipt: .claude/reports/builder-commandcenter/RECEIPT.md. Checked-at mined from that receipt's commit. One migration was still held at the desk when the receipt was written.*</sub> `TUE 8/25`
- [x] Give each person their own Myli voice instead of one voice for everybody — How she speaks is now set per person rather than being one flat voice across the whole book, so the way she talks to a past client is not the way she talks to a brand-new inquiry. <sub>*Lane receipt: .claude/reports/builder-voices/RECEIPT.md. Checked-at mined from that receipt's commit.*</sub> `TUE 8/25`
- [x] Sign off the way Myli speaks in her own room, live — The room-shaped rule for what she is allowed to say where, agreed and switched on the same day rather than sitting as a drawing. <sub>*Lane receipt: .claude/reports/builder-myliroom/RECEIPT.md. Checked-at mined from that receipt's commit.*</sub> `TUE 8/25`

### The B bricks — tasks are her clock — 3/5

- [x] Make every task in the system get created through one door instead of six different ones — Tasks were being created six different ways, so no two of them behaved the same. Every task is now born through one door, and creating the same one twice can no longer double it up. <sub>*CLOSED 8/28: B0 the one task writer, migration 0921 applied, idempotency proven end to end and the old transitional dedupe replaced with a stronger one. Gate: test:task-writer 31/0/0, exit 0. Receipt: .claude/reports/state-stack/RECEIPT.md*</sub> `FRI 8/28`
- [x] When Myli finishes a playbook she sets herself a real follow-up task, visible in the system and assigned to her — When Myli finishes a job she books herself a real follow-up task you can see in the system with her name on it — and she actually reads it back when it comes due. <sub>*GRINDER-LANDED r5 — receipt: .claude/reports/grinder/myli-os/1788032600-receipt-r5.md · verdict: .claude/reports/grinder/myli-os/verdict-r5-1788032656113.md*</sub> `FRI 8/28`
- [ ] Give Myli a way to wake herself up: she reads her own due tasks and starts the right playbook — Myli reads her own tasks as they come due and starts the right job off the back of them. The task is the doorbell — it never tells her what to say. <sub>*the task is the doorbell, never the script. Built event-only; the schedule is a decision, not a deploy*</sub> `MON 8/31`
- [ ] Arm the task sweep on a schedule after one watched run, and answer the running-cost question honestly — You said: "Just make that work dude... I'm tired of all this cost." The desk watches one real run, arms the schedule off the back of it, and then tells you straight whether her tasks need the paid background service at all or can run inside the app. <sub>*BUILD ORDER 8/25 (E30) - one watched run satisfies the law, then the schedule is armed; the cost answer lands in the next brief.*</sub> `SUN 9/6`
- [x] Flip the task sweep onto a cron — You handed it over: "Just make that work dude." So the desk watches one real run, arms the schedule off the back of it, and answers your cost question straight in the next brief. The arming is now its own item. <sub>*ANSWERED 8/25 (E30) - delegated to the desk; arming plus the honest cost answer seeded as their own item.*</sub>

### The C bricks — cooling, warming, habit — 1/3

- [ ] Work out whether someone is cooling off by counting the record each time, never by storing a score — Whether a person is going cold, whether they cancel habitually, whether this was the first time — all of it gets counted from their real history every time it is asked, instead of a score stashed somewhere that quietly goes stale. <sub>*"is this person cooling down, do they do this habitually, was this the first time they canceled" — derived from the tape, never hand-maintained*</sub> `THU 9/3`
- [x] When someone goes quiet, how fast should Myli slow down? — You ruled it: gentler. Daily for three tries, then once a week, then once a month. So a buyer who stops answering on a Monday hears from her Tuesday, Wednesday and Thursday, then weekly, then monthly. <sub>*REMANDED and REWRITTEN 8/25 (E31) - Zach: "I don't know what that is." The weights stay unsigned until he blesses these speeds.*</sub>
- [ ] Publish what Myli works out about someone as signals the rest of the system can react to — What Myli figures out about a person — cooling off, warming up, a pattern — becomes a signal the rest of the system can react to. It is waiting on the layer that reacts to signals to exist first. <sub>*blocked on the automation-trigger layer; stated, not routed around*</sub> `SUN 9/6`

### Keeping the record straight — 1/1

- [x] Make the floor page and the architecture page use one naming scheme — Done — both pages now use one scheme end to end. The old one survives nowhere. <sub>*DONE — the floor page and the state-engine page now use ONE scheme end to end (E-1, A0/A1/A2, B0/B1/B2, C0/C1). The old scheme survives nowhere except this checkbox's own note.*</sub> `MON 8/24`

### The night shift, Tue→Wed — 2/2

- [x] The conversion doctrine mined from the real book — a goal for everything Myli says — Everything Myli says now has a point to it, drawn from what actually worked in our own book of business. `WED 8/26`
- [x] Your AI master switch is real now — off means off, every channel — The AI off switch genuinely turns her off now. Off means off, on every channel at once, not just the one you were looking at. `WED 8/26`

### Her voice, made honest (8/26) — 5/5

- [x] Fair housing: the three-move decline live on every road, hostile-probe verified — When someone asks the kind of question fair housing law will not let us answer, Myli declines the same clean way every time, on every way in — proven by deliberately trying to trip her up. <sub>*desk-ported §8b; rubric §6 17→21/22*</sub> `WED 8/26`
- [x] All three improvised address-refusal roads closed — she answers about homes we carry, honestly absent on ones we don't, and an outage is never an absence — There were three ways to ask about a specific address where Myli would improvise an answer. All three now run the same check: she speaks about homes we actually carry, says plainly when we do not have one, and never passes a broken lookup off as 'we don't have it'. <sub>*nlp modal + flows road + dashboard-chat; the shared matcher, two rails*</sub> `WED 8/26`
- [x] The honest close made deterministic: enforced wants DESCRIBED, dropped wants DISCLOSED, one grammar with the page — When Myli hands back results she now spells out which of your wishes she actually applied and which she had to drop, in the same words the screen uses. <sub>*mylitruth rounds 2-4; no-zero suite 33→73 assertions*</sub> `WED 8/26`
- [x] Style rule 24 minted and swept: she never speaks screen directions — Myli stopped giving directions to the screen. She was telling people to use the filters below while the filters were sitting above her. <sub>*from Zach's 4:04 shot ("use the filters below" while Filters sat above her)*</sub> `WED 8/26`
- [x] Her line is personable and NAMES the search — "Here's your Overland Park search…" (Zach's dinner ruling, executed live) — Myli's line now sounds like a person and names what you are actually looking at — "Here's your Overland Park search" — and she no longer claims you were just looking at something you never opened. <sub>*MyliLine organic mode + the arrival marker (no more false "we were just looking at this")*</sub> `WED 8/26`

### THE MYLI SSOT FOLD — the weekend (Zach #257.468/#257.471): one authority per semantic question — 27/57

- [x] F0.1 THE AUTHORITY MAP as law — .claude/rules/MYLI_AUTHORITY_MAP.md (the table in the plan §1, ENFORCED BY naming each fold's cage); the law-cage index reports it — F0.1 THE AUTHORITY MAP as law — .claude/rules/MYLI_AUTHORITY_MAP.md (the table in the plan §1, ENFORCED BY naming each fold's cage); the law-cage index reports it <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-10 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F0-freeze-2026-09-10.md. the law, ENFORCED BY the authority cage*</sub> `THU 9/10`
- [x] F0.2 THE MORATORIUM CAGE — tools/vocab/myli-authority-cage.mjs --cage: a forbidden import/write (the CI import-ban list, seeded today, grows per fold) is RED; shrink-only floor; red-proven — F0.2 THE MORATORIUM CAGE — tools/vocab/myli-authority-cage.mjs --cage: a forbidden import/write (the CI import-ban list, seeded today, grows per fold) is RED; shrink-only floor; red-proven <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-10 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F0-freeze-2026-09-10.md. selftest GREEN; wired; floor 83; the vector = F0.2b*</sub> `THU 9/10`
- [x] F1.1 THE ONE WRITE ROAD into the ledger — appendTurn()/appendEvent() over hl_chat_events/hl_turns with global ULID ids; every writer (web chat, the owner room, the one-voice funnel, the CRM's SMS/text turns) calls it; CAGE: no other file INSERTs into the ledger (grep cage + the gateway's DB role is the only writer) — v2: WEBCHAT writers only (the chat route, the owner room as a webchat entry, the one-voice funnel); SMS/text next week; the function is recordEvent() — F1.1 THE ONE WRITE ROAD into the ledger — appendTurn()/appendEvent() over hl_chat_events/hl_turns with global ULID ids; every writer (web chat, the owner room, the one-voice funnel, the CRM's SMS/text turns) calls it; CAGE: no other file INSERTs into the ledger (grep cage + the gateway's DB role is the only writer) — v2: WEBCHAT writers only (the chat route, the owner room as a webchat entry, the one-voice funnel); SMS/text next week; the function is recordEvent() <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F1-spine-2026-09-10.md. appendChatEvent() writes the outbox row in the event's transaction; the legacy receivers refuse a top-level source_event_id; live round trip 5 events → 5 outbox → 5 projected (302c04728b, 953e32ac76)*</sub> `FRI 9/11`
- [x] F1.2 THE CRM PROJECTOR — hl_messages.source_event_id UNIQUE NOT NULL (desk DDL, interview + lock), one projector fed by an outbox; persistConversation / the birth replay / the snapshot PUT retired as writers; CAGE: the UNIQUE + the projector's DB role is the only writer of projected rows — v2: the outbox row is written in the SAME WEB transaction as the event; the projector is at-least-once, idempotent on source_event_id, keeps the source sequence, carries a gap detector; the UNIQUE is PARTIAL (the webchat projection) and the global NOT NULL is next week — F1.2 THE CRM PROJECTOR — hl_messages.source_event_id UNIQUE NOT NULL (desk DDL, interview + lock), one projector fed by an outbox; persistConversation / the birth replay / the snapshot PUT retired as writers; CAGE: the UNIQUE + the projector's DB role is the only writer of projected rows — v2: the outbox row is written in the SAME WEB transaction as the event; the projector is at-least-once, idempotent on source_event_id, keeps the source sequence, carries a gap detector; the UNIQUE is PARTIAL (the webchat projection) and the global NOT NULL is next week <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F1-spine-2026-09-10.md. the WEB drain (conversation-ordered, GAP quarantines one conversation) + the ONE CRM receiver /api/internal/chat-events + the projector through logMessage with source_event_id; fold-invariants.mjs exit 0; the Inngest dispatcher's RUN is still unproven (dev :3001 sync 500 — the desk restarts the server)*</sub> `FRI 9/11`
- [x] F1.3 THE BACKFILL — every in-flight conversation mapped to event ids once, the doubles reconciled (the 43-for-24 case), the array-position key frozen; receipt: before/after on 20 conversations, shadow diff = 0 — v2: the proof is SQL invariants over the WHOLE scoped population, never twenty conversations — F1.3 THE BACKFILL — every in-flight conversation mapped to event ids once, the doubles reconciled (the 43-for-24 case), the array-position key frozen; receipt: before/after on 20 conversations, shadow diff = 0 — v2: the proof is SQL invariants over the WHOLE scoped population, never twenty conversations <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F1.3-backfill-2026-09-10.md. 15,863 events through appendLegacyEvents() (origin legacy_import); 4403/4403 conversations; the 4-hour naive-timestamp slip caught, reversed, replayed; 8 partials await the desk's ruling (wall 1)*</sub> `FRI 9/11`
- [ ] F1.4 FOUR SURFACES READ THE LEDGER — the web transcript, the CRM lead record, the review queue, Mission Control; never a second transcript shape; receipt: one conversation renders identically on all four — F1.4 FOUR SURFACES READ THE LEDGER — the web transcript, the CRM lead record, the review queue, Mission Control; never a second transcript shape; receipt: one conversation renders identically on all four <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven.*</sub>
- [ ] F2.1 KILL THE SECOND PILL ENGINE — lib/myli/dynamic-pills.ts, lib/myli-legacy/, the MYLI_NEXT + MYLI_AGENT_VOICE roads: call sites stop reading, then the files go; CAGE: CI import ban on the paths — F2.1 KILL THE SECOND PILL ENGINE — lib/myli/dynamic-pills.ts, lib/myli-legacy/, the MYLI_NEXT + MYLI_AGENT_VOICE roads: call sites stop reading, then the files go; CAGE: CI import ban on the paths <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven.*</sub>
- [x] F2.2 FLOW CONFIG = CAPABILITY IDS ONLY — the flow schema physically has no description/what/promise field; a generated CapabilityId enum + the model manifest generated from the registry; an unknown id fails to compile and the validator rejects it at runtime — F2.2 FLOW CONFIG = CAPABILITY IDS ONLY — the flow schema physically has no description/what/promise field; a generated CapabilityId enum + the model manifest generated from the registry; an unknown id fails to compile and the validator rejects it at runtime <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F2.2-bundle-2026-09-10.md. generator + contract-bundle.v1.json + active pointer + shims (unwired) + the --check cage; selftest plant 35→36 detected; TOOLS read by a bracket-depth structural scanner (the one-word export is F2.3's first line)*</sub> `FRI 9/11`
- [ ] F2.3 PILLS = registry label + the eligibility resolver (personasFor/rung) only; receipt: the wire capture shows only registry words on three surfaces — F2.3 PILLS = registry label + the eligibility resolver (personasFor/rung) only; receipt: the wire capture shows only registry words on three surfaces <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven.*</sub>
- [x] F3.1 THE ONE CONTEXT ASSEMBLER — buildTurnContext(input): Promise&lt;TurnContextV1>, branded; the LLM runner's signature takes ONLY it (no systemPrompt/extraContext/buyerFacts/flowDescription params); CAGE: an ad-hoc object does not compile — F3.1 THE ONE CONTEXT ASSEMBLER — buildTurnContext(input): Promise&lt;TurnContextV1>, branded; the LLM runner's signature takes ONLY it (no systemPrompt/extraContext/buyerFacts/flowDescription params); CAGE: an ad-hoc object does not compile <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F3.1-assembler-2026-09-10.md.*</sub> `FRI 9/11`
- [x] F3.2 THE PAGE SNAPSHOT — buildPlacePageModel(): ONE producer feeds the React render, the TEMPLATED header greeting and the serialized PageContext (place · filters · inventory · shelves · pills, each with a queryFingerprint; pageContextId + revision per render/navigation; sent by id per turn, expanded server-side); CAGE (judge arm): rendered DOM count == PageContext.inventory.forSale == greeting count on /, a county, a city — v2: PageContext carries inventorySnapshotIds + query fingerprints + capability ids, never a copied count; the judge arm reads the snapshot it cites — F3.2 THE PAGE SNAPSHOT — buildPlacePageModel(): ONE producer feeds the React render, the TEMPLATED header greeting and the serialized PageContext (place · filters · inventory · shelves · pills, each with a queryFingerprint; pageContextId + revision per render/navigation; sent by id per turn, expanded server-side); CAGE (judge arm): rendered DOM count == PageContext.inventory.forSale == greeting count on /, a county, a city — v2: PageContext carries inventorySnapshotIds + query fingerprints + capability ids, never a copied count; the judge arm reads the snapshot it cites <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F3.2-page-snapshot-2026-09-10.md. buildPlacePageModel() + the signed page token; PAGE_DIVERGENCE live; page-context-parity cage born at 0 per page type; / declares headerGreeting null (no rendered count) — accepted as the honest arm*</sub> `FRI 9/11`
- [x] F3.3 MIGRATE THE SURFACES IN ORDER — website chat → owner dashboard → CRM/SMS through the one assembler; the strip composer's own derivation (F64's family) deleted; receipt: the Johnson County walk re-driven — her header names the county with the page's number and the chat opener knows the page — F3.3 MIGRATE THE SURFACES IN ORDER — website chat → owner dashboard → CRM/SMS through the one assembler; the strip composer's own derivation (F64's family) deleted; receipt: the Johnson County walk re-driven — her header names the county with the page's number and the chat opener knows the page <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F3.3-prompt-bundle-2026-09-11.md. 38 prose blocks registered, promptBundleVersion pb1:dcf12514ff53 live on both surfaces; prompt-size byte-identical; arm 2 (the model window = the ledger projection) BLOCKED on the conversation binding (44% of live turns run conversation=none; dashboard-opener passes an empty id) → carded as the precondition slice*</sub> `FRI 9/11`
- [x] F4.1 ONE INVENTORY SNAPSHOT — typed SearchScope + one resolver over 5B's listingWhereFor/facetActiveCount; shelves, headers, pills and chat consume the snapshot, never query helpers; CAGE: every count on a place page resolves to one snapshot id (SHOW_ALL_PARITY stays) — v2: the invariant is 'same query fingerprint + same revision ⇒ same snapshot everywhere' (a county page legitimately has several scopes) — F4.1 ONE INVENTORY SNAPSHOT — typed SearchScope + one resolver over 5B's listingWhereFor/facetActiveCount; shelves, headers, pills and chat consume the snapshot, never query helpers; CAGE: every count on a place page resolves to one snapshot id (SHOW_ALL_PARITY stays) — v2: the invariant is 'same query fingerprint + same revision ⇒ same snapshot everywhere' (a county page legitimately has several scopes) <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F4.1-inventory-snapshot-2026-09-10.md. resolveInventorySnapshot() at lib/market/inventory-snapshot.ts (the manifest's path); the chat's resolve_place 495→465 = the city page's fingerprint; no live number moved; coming-soon RULED at the desk as the C7 v1.1 delta (#257.466 (4) binds her picks, the pages keep the 6/17 default and say so)*</sub> `FRI 9/11`
- [x] F4.2 THE STATUS FSM — accepted · assembling_context · resolving_search · running_tool · composing · completed · retryable_failure · hard_failure; the API schema carries a state enum and NO statusText; the client owns the copy from the same snapshot; the arrival modal's build lines (#257.453) are this FSM's copy; CAGE: a free-text status reaching the stream is RED — F4.2 THE STATUS FSM — accepted · assembling_context · resolving_search · running_tool · composing · completed · retryable_failure · hard_failure; the API schema carries a state enum and NO statusText; the client owns the copy from the same snapshot; the arrival modal's build lines (#257.453) are this FSM's copy; CAGE: a free-text status reaching the stream is RED <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F4.2-status-fsm-2026-09-11.md. turn-status.ts + the status frame (enum + ids only) + status-copy.ts + the status-fsm cage red-proven by mutating the law; the 'Couldn't re the result' composer does not exist in the tree (the model rephrased a handed sentence) — cured structurally; the two C9 questions ruled at the desk*</sub> `FRI 9/11`
- [ ] F4.3 THE COUNTY WALK'S FOUR — the chat viewport anchors on her newest turn (#257.466 f3); her picks render as the small preview-card slide (f5); coming-soon excluded from picks (f4); pills = the question's own words (f6); receipt: the county walk at 390 — v2: PRODUCT POLISH, not an SSOT prerequisite → NEXT WEEK (plan §7) — F4.3 THE COUNTY WALK'S FOUR — the chat viewport anchors on her newest turn (#257.466 f3); her picks render as the small preview-card slide (f5); coming-soon excluded from picks (f4); pills = the question's own words (f6); receipt: the county walk at 390 — v2: PRODUCT POLISH, not an SSOT prerequisite → NEXT WEEK (plan §7) <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven.*</sub>
- [x] F5.1 ONE FACT CATALOG — a generated FactSlotId; RECITED_SLOT and SLOT_WORDS fold into it; the prompt, the extractor, the resolver and the CRM projection speak it; CAGE: no arbitrary slot string compiles — F5.1 ONE FACT CATALOG — a generated FactSlotId; RECITED_SLOT and SLOT_WORDS fold into it; the prompt, the extractor, the resolver and the CRM projection speak it; CAGE: no arbitrary slot string compiles <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F5.1-fact-catalog-2026-09-11.md. tools/myli/fact-catalog.json (72 slots/12 domains/4 folds/6 orphans) → three generated consumers; the write boundary at upsertFact (assertFact is a C8 name, built by F5.2); casing twins fold at the writer; the census detector tightened (generated files are not authoring sites); two C8 rulings at the desk*</sub> `FRI 9/11`
- [ ] F5.2 THE ASSERTION MODEL on hl_lead_facts — evidence_event_id NOT NULL for asserted facts (ONE pointer; source_activity_ids dropped by desk DDL after the backfill), status, supersedes, extractor_version; ONE assertFact() writer holding the table permission; CAGE: the fact-evidence floor → 0 + the DB role — v2: additive DDL; evidence_event_id NOT NULL only for facts written THROUGH THE FOLDED WRITER (a scoped CHECK); older origins normalized next week, never fabricated — F5.2 THE ASSERTION MODEL on hl_lead_facts — evidence_event_id NOT NULL for asserted facts (ONE pointer; source_activity_ids dropped by desk DDL after the backfill), status, supersedes, extractor_version; ONE assertFact() writer holding the table permission; CAGE: the fact-evidence floor → 0 + the DB role — v2: additive DDL; evidence_event_id NOT NULL only for facts written THROUGH THE FOLDED WRITER (a scoped CHECK); older origins normalized next week, never fabricated <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven.*</sub>
- [ ] F5.3 DERIVED VALUES ARE A SPECIES — hl_derived_value (kind · value · calculator_version · input_fact_assertion_ids · snapshot hash · valid_until · status); the affordability estimate moves here; a withdrawn/parked input invalidates the derivation structurally; CAGE: a derived value with a dead input is RED on the nightly — v2: hl_derived_value_inputs as a child table with real FKs, not an id array — F5.3 DERIVED VALUES ARE A SPECIES — hl_derived_value (kind · value · calculator_version · input_fact_assertion_ids · snapshot hash · valid_until · status); the affordability estimate moves here; a withdrawn/parked input invalidates the derivation structurally; CAGE: a derived value with a dead input is RED on the nightly — v2: hl_derived_value_inputs as a child table with real FKs, not an id array <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven.*</sub>
- [ ] F5.4 ONE EXTRACTOR — extract-context + on-record fold into one that proposes catalog ids + evidence; a deterministic evidence gate admits; an assistant sentence is never evidence for a person fact; receipt: the r10/r11 corpus re-driven with zero own-voice facts — F5.4 ONE EXTRACTOR — extract-context + on-record fold into one that proposes catalog ids + evidence; a deterministic evidence gate admits; an assistant sentence is never evidence for a person fact; receipt: the r10/r11 corpus re-driven with zero own-voice facts <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven.*</sub>
- [x] F6.1 CLAIMS ARE REFERENCES — VALUE_REF / COUNT_REF / PRICE_REF / ADDRESS_REF / PAYMENT_REF: the model emits references, the renderer resolves from the turn's authorities, the grounding guard stays as the seat belt; CAGE (judge arm): a bare number in her prose resolving to no reference is RED — v2: the DANGEROUS classes only this weekend (counts, money, prices, payments, percentages/affordability); the RENDERER is the sole committer of user-visible output; addresses and every other noun next week — F6.1 CLAIMS ARE REFERENCES — VALUE_REF / COUNT_REF / PRICE_REF / ADDRESS_REF / PAYMENT_REF: the model emits references, the renderer resolves from the turn's authorities, the grounding guard stays as the seat belt; CAGE (judge arm): a bare number in her prose resolving to no reference is RED — v2: the DANGEROUS classes only this weekend (counts, money, prices, payments, percentages/affordability); the RENDERER is the sole committer of user-visible output; addresses and every other noun next week <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F6.1-claim-refs-2026-09-11.md. claim-refs.ts + turn-renderer.ts (the sole committer at the stream gate and the return); counts bind to their own scope's snapshot, prices to the card's hlid; the citation half deferred (charter floor 6860, zero headroom) — binding:'resolved' until F9.1; manifest path turn-renderer.ts ratified*</sub> `FRI 9/11`
- [x] F7.1 PROJECTIONS STAY STUPID — the CRM lead record, the review queue, the owner dashboard's history and the web transcript are views of the ledger + resolvers; no metadata->> business decision anywhere; CAGE: the costume floor on Myli tables → 0 — v2: Monday's two projections only (the web transcript + the CRM transcript the journeys touch); every other CRM surface next week — F7.1 PROJECTIONS STAY STUPID — the CRM lead record, the review queue, the owner dashboard's history and the web transcript are views of the ledger + resolvers; no metadata->> business decision anywhere; CAGE: the costume floor on Myli tables → 0 — v2: Monday's two projections only (the web transcript + the CRM transcript the journeys touch); every other CRM surface next week <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F7.1-projections-2026-09-11.md. the web transcript reads the ledger (the blob had 11 divergent conversations, one repeating turns 1-6 verbatim); the CRM transcript was already a resolveCard projection; detector C on projection-composer at floor 4 (2 need the conversation_id column DDL, 2 are the agent dock); the drain's missing origin filter carded P0*</sub> `FRI 9/11`
- [x] F8.1 DESTROY THE ESCAPE HATCHES — drop the deprecated columns/tables after the backfills (source_activity_ids, the array key, the presentations design), remove every old-composer flag, the old RPCs, the direct DB permissions; CI forbids the imports; every authority gets a NEGATIVE CONTROL journey; receipt: what died by name + what is NOT yet dead by name, on this board — v2: death by PERMISSION and IMPORT this weekend (the deployed code cannot call the old writers; DB roles/constraints refuse the folded write paths); death by SCHEMA next week — historical columns stay through the rollback window — F8.1 DESTROY THE ESCAPE HATCHES — drop the deprecated columns/tables after the backfills (source_activity_ids, the array key, the presentations design), remove every old-composer flag, the old RPCs, the direct DB permissions; CI forbids the imports; every authority gets a NEGATIVE CONTROL journey; receipt: what died by name + what is NOT yet dead by name, on this board — v2: death by PERMISSION and IMPORT this weekend (the deployed code cannot call the old writers; DB roles/constraints refuse the folded write paths); death by SCHEMA next week — historical columns stay through the rollback window <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F8.1-escape-hatches-2026-09-11.md. ssot-reachability cage born at the measured 3 (2 via persist-conversation.ts — F1's fold, 1 via dynamic-pills.ts — F2's); arm 2 refused on measurement (persist-conversation appends assistant.answered INSIDE it); death by permission blocked on least-privilege roles; geo-block.ts is alive (a generator slices it) — the two signed docs corrected*</sub> `FRI 9/11`
- [ ] F9.1 SUNDAY — the four journeys pass^2 by Fable judges: anonymous buyer → affordability → return → the CRM transcript · the owner dashboard (Riley) · the county page → chat → picks · the one-voice funnel A–J; plus a thirty-minute sense roam filing nothing new — v2: SATURDAY EVENING DEPLOY (migrate): the complete SSOT webchat stack behind the ingress switch, synthetic journeys through ssot_v1, read-only shadow comparison, never shadow writes; SUNDAY: the invariants + the four journeys pass×2 + the sense roam → the projection watermark → route ALL webchat to ssot_v1; rollback = flip the ingress, never restore a writer — F9.1 SUNDAY — the four journeys pass^2 by Fable judges: anonymous buyer → affordability → return → the CRM transcript · the owner dashboard (Riley) · the county page → chat → picks · the one-voice funnel A–J; plus a thirty-minute sense roam filing nothing new — v2: SATURDAY EVENING DEPLOY (migrate): the complete SSOT webchat stack behind the ingress switch, synthetic journeys through ssot_v1, read-only shadow comparison, never shadow writes; SUNDAY: the invariants + the four journeys pass×2 + the sense roam → the projection watermark → route ALL webchat to ssot_v1; rollback = flip the ingress, never restore a writer <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven.*</sub>
- [ ] F9.2 ZACH'S MONDAY WALK — Myli talking perfectly with perfect memory on webchat — F9.2 ZACH'S MONDAY WALK — Myli talking perfectly with perfect memory on webchat <sub>*Minted 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN (#257.471); ticks only with the slice's named receipt + its cage red-proven.*</sub>
- [x] F0.3 THE AUTHORITY MANIFEST — tools/myli/authority-manifest.json: per authority the only package that may construct/write it, the protected DB objects, the exported public API, the forbidden legacy imports, the owning lane; CI derives the import-graph cage from it — F0.3 THE AUTHORITY MANIFEST — tools/myli/authority-manifest.json: per authority the only package that may construct/write it, the protected DB objects, the exported public API, the forbidden legacy imports, the owning lane; CI derives the import-graph cage from it <sub>*Added 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN v2 (#257.471a, the plan review adopted); ticks only with the YAML receipt + its cage red-proven. TICKED 2026-09-10 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F0-freeze-2026-09-10.md. ten authorities + the renamed switch*</sub> `THU 9/10`
- [x] F0.5 THE CONTRACTS, FROZEN (serial, the desk, before any builder): hl_chat_events = THE ledger, hl_turns = the envelope, ONE write function recordEvent(); the canonical id = the existing durable web event id (no ULID migration); TurnContextV1 · PageContextV1 · InventorySnapshotV1 + SearchScope · FactSlotId + the assertion shape · StatusState · the claim references · resolveActor()'s contract · the outbox row — a lane implements behind a contract, never redefines one; CAGE: generate && git diff --exit-code — F0.5 THE CONTRACTS, FROZEN (serial, the desk, before any builder): hl_chat_events = THE ledger, hl_turns = the envelope, ONE write function recordEvent(); the canonical id = the existing durable web event id (no ULID migration); TurnContextV1 · PageContextV1 · InventorySnapshotV1 + SearchScope · FactSlotId + the assertion shape · StatusState · the claim references · resolveActor()'s contract · the outbox row — a lane implements behind a contract, never redefines one; CAGE: generate && git diff --exit-code <sub>*Added 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN v2 (#257.471a, the plan review adopted); ticks only with the YAML receipt + its cage red-proven. TICKED 2026-09-10 via the task entrance (desk) — receipt: .claude/reports/myli-os/THE-MYLI-CONTRACTS-V1-2026-09-10.md. C1–C10 frozen + the v1.1 hardening deltas from the second opinion*</sub> `THU 9/10`
- [x] F1.4 IDENTITY — resolveActor() / linkActorToPerson() is the ONE binding for 'who is this returning human'; the anonymous → sign-in → return journey proves it (a returning visitor's history loads from the ledger by that binding) — F1.4 IDENTITY — resolveActor() / linkActorToPerson() is the ONE binding for 'who is this returning human'; the anonymous → sign-in → return journey proves it (a returning visitor's history loads from the ledger by that binding) <sub>*Added 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN v2 (#257.471a, the plan review adopted); ticks only with the YAML receipt + its cage red-proven. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F1.4-identity-2026-09-11.md. lib/myli/actor.ts + actor-contract.ts: resolveActor/resolveActorBinding/linkActorToPerson/linkActorFromTurn; the link is an append-only identity.linked ledger event carrying its evidence (no DDL); the journey proved 6 rows byte-identical, count 6→12 by append; the outbox drain's leadIdFor folded onto the binding; actor-binding cage born at 36; manifest row corrected at the desk; C3 v1.1 delta (a lead-bearing actor) written*</sub> `FRI 9/11`
- [ ] F1.5 THE ONE CUTOVER SWITCH at the outermost webchat ingress — legacy_responder ↔ ssot_v1, owner = the desk, delete-by Tue 2026-09-15; BOTH responders run over the canonical ledger/projector; no per-feature flags; serving plane reversible, data plane forward-only (#257.455a: the emergency rollback exception) — F1.5 THE ONE CUTOVER SWITCH at the outermost webchat ingress — legacy_responder ↔ ssot_v1, owner = the desk, delete-by Tue 2026-09-15; BOTH responders run over the canonical ledger/projector; no per-feature flags; serving plane reversible, data plane forward-only (#257.455a: the emergency rollback exception) <sub>*Added 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN v2 (#257.471a, the plan review adopted); ticks only with the YAML receipt + its cage red-proven.*</sub>
- [ ] F9.0 FRIDAY EVENING DEPLOY (expand): additive DDL + the gateway + the outbox/projector + the cages + compatibility adapters — webchat already WRITING canonically, the responder unchanged; proof: the SQL invariants over the whole scoped population (unmapped 0 · duplicate source ids 0 · gaps 0 · hash mismatches 0) — F9.0 FRIDAY EVENING DEPLOY (expand): additive DDL + the gateway + the outbox/projector + the cages + compatibility adapters — webchat already WRITING canonically, the responder unchanged; proof: the SQL invariants over the whole scoped population (unmapped 0 · duplicate source ids 0 · gaps 0 · hash mismatches 0) <sub>*Added 2026-09-10 by the desk from THE MYLI SSOT FOLD PLAN v2 (#257.471a, the plan review adopted); ticks only with the YAML receipt + its cage red-proven.*</sub>
- [ ] F2.4 likeness.assets — Myli's headshot/portrait/pose/aura have ONE URL each from one module; every surface incl. the SRG page reads them; the authority cage grows a forbidden_literals arm; the SRG copy retired (#257.475) — F2.4 likeness.assets — Myli's headshot/portrait/pose/aura have ONE URL each from one module; every surface incl. the SRG page reads them; the authority cage grows a forbidden_literals arm; the SRG copy retired (#257.475) <sub>*Added 2026-09-10 via the task door.*</sub>
- [x] F0.6 the audit's findings to ChatGPT; plan v3 hardened; manifest/contract edits at the desk before wave A (#257.476) — F0.6 the audit's findings to ChatGPT; plan v3 hardened; manifest/contract edits at the desk before wave A (#257.476) <sub>*Added 2026-09-10 via the task door. TICKED 2026-09-10 via the task entrance (desk) — receipt: .claude/reports/myli-os/SECOND-OPINION-myli-ssot-audit-hardening-2026-09-10.md. the four-heading receipt; plan v3 §9 + contracts v1.1 landed at the desk*</sub> `THU 9/10`
- [x] F0.2b THE VECTOR FLOORS — tools/vocab/myli-ssot-census.mjs: per-disease shrink-only floors born at the audit's numbers (identity openers 29 · tool maps 7 · flow maps 5 · money banks 4 · state representations 4 · count query shapes 8 · count producers 22 · extraction roads 10 · memory bypass sites 16), a mutation test each; the authority floor becomes per-authority (plan v3 §9.2) — F0.2b THE VECTOR FLOORS — tools/vocab/myli-ssot-census.mjs: per-disease shrink-only floors born at the audit's numbers (identity openers 29 · tool maps 7 · flow maps 5 · money banks 4 · state representations 4 · count query shapes 8 · count producers 22 · extraction roads 10 · memory bypass sites 16), a mutation test each; the authority floor becomes per-authority (plan v3 §9.2) <sub>*Added 2026-09-10 via the task door. TICKED 2026-09-10 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F0.2b-vector-floors-2026-09-10.md. nine floors 26/22/13/29/14/8/43/11/21 (audit numbers reconciled per counter); one new extraction road found (simulator/runner.ts:714)*</sub> `THU 9/10`
- [x] F2.1b KILL-NOW-FREE (Fri AM): rename the dashboard MYLI_STATES → DashboardExperienceState · quarantine hl_fub_person_facts + ban its import · drop ROOM_BUDGETS from prompt assembly · delete /api/myli/converse + /api/myli/flow-answer · correct the two lock verdicts (D13 D24 D32 D33 D38; plan v3 §9.4) — F2.1b KILL-NOW-FREE (Fri AM): rename the dashboard MYLI_STATES → DashboardExperienceState · quarantine hl_fub_person_facts + ban its import · drop ROOM_BUDGETS from prompt assembly · delete /api/myli/converse + /api/myli/flow-answer · correct the two lock verdicts (D13 D24 D32 D33 D38; plan v3 §9.4) <sub>*Added 2026-09-10 via the task door. TICKED 2026-09-10 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F2.1b-kill-now-2026-09-10.md. D13 D32 D33 D38 landed (3fab88a4c4 c69c4e1763 011f4a8b12 ae37ddcaa9); D24 quarantined by the app-SQL ban at zero in the manifest; DROP deferred until the three tools/ readers retire; verdict diffs blessed at the desk*</sub> `THU 9/10`
- [ ] F6.1b THE K ADVERSARY JOURNEY — fact active → estimate → fact parked → old prose still says k → new reply: no k, no old estimate, assertion absent, derived value stale (ASSERTION_REF + DERIVED_REF live; plan v3 §9.5 wall 2) — F6.1b THE K ADVERSARY JOURNEY — fact active → estimate → fact parked → old prose still says k → new reply: no k, no old estimate, assertion absent, derived value stale (ASSERTION_REF + DERIVED_REF live; plan v3 §9.5 wall 2) <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] F9.1b THE THREE WALLS — ONE CONVERSATION (4402/4402, unreconciled 0, new array keys 0, history writers 1) · CONTEXT PURITY (the ssot_v1 dependency graph reaches memory only via resolveAdmissibleMemory) · SNAPSHOT EXCLUSIVITY (the same snapshot id on DOM/strip/greeting/reply) — receipts by SQL; anything else does not flip Sunday (plan v3 §9.5) — F9.1b THE THREE WALLS — ONE CONVERSATION (4402/4402, unreconciled 0, new array keys 0, history writers 1) · CONTEXT PURITY (the ssot_v1 dependency graph reaches memory only via resolveAdmissibleMemory) · SNAPSHOT EXCLUSIVITY (the same snapshot id on DOM/strip/greeting/reply) — receipts by SQL; anything else does not flip Sunday (plan v3 §9.5) <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] F4.4 MARKET KNOWLEDGE — the market.knowledge authority (#257.489): ONE resolver over the data we hold (hl_market_stat × every grain, the SEO page content + FAQs, Listing.subdivision + builder, the geo tail, place boundaries, new-construction counts) + the lookup_market_knowledge(query) tool + PLACE_REF in C10; the persona (tour guide × best agent × Tony Robbins EQ × Miner/Elliott sales craft) in the ONE prompt bundle; acceptance = the two-Wilshire journey ('wilshire something off 159th, newer construction?' → Wilshire Farms / Wilshire by the Lake, 13 new builds) with every count a ref — F4.4 MARKET KNOWLEDGE — the market.knowledge authority (#257.489): ONE resolver over the data we hold (hl_market_stat × every grain, the SEO page content + FAQs, Listing.subdivision + builder, the geo tail, place boundaries, new-construction counts) + the lookup_market_knowledge(query) tool + PLACE_REF in C10; the persona (tour guide × best agent × Tony Robbins EQ × Miner/Elliott sales craft) in the ONE prompt bundle; acceptance = the two-Wilshire journey ('wilshire something off 159th, newer construction?' → Wilshire Farms / Wilshire by the Lake, 13 new builds) with every count a ref <sub>*Added 2026-09-10 via the task door.*</sub>
- [ ] F1.2b the ledger's in-transaction ON CONFLICT branch has NO test (a mutation enqueuing an outbox row on a replay stayed green because appendChatEvent answers replays from the idempotency lookup above the transaction) — a DB-level plant with a SECOND pg client racing the same idempotency key; carried by F1.3's lane — F1.2b the ledger's in-transaction ON CONFLICT branch has NO test (a mutation enqueuing an outbox row on a replay stayed green because appendChatEvent answers replays from the idempotency lookup above the transaction) — a DB-level plant with a SECOND pg client racing the same idempotency key; carried by F1.3's lane <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] context-purity floor RE-SCOPED (desk ruling on F3.1 receipt §5): the tool layer is the SANCTIONED road for listing truth (#256.18), so a DB reach THROUGH a tool module is not a wall-2 breach — the cage records tool-layer reaches as an informational count and counts only reaches on the runner's own path; floor re-seeded DOWNWARD to the measured remainder; the header states the boundary and why; selftest gains a control (a prisma import inside the tool dir is SILENT) and keeps the plant (a prisma import on the runner's path is RED) — context-purity floor RE-SCOPED (desk ruling on F3.1 receipt §5): the tool layer is the SANCTIONED road for listing truth (#256.18), so a DB reach THROUGH a tool module is not a wall-2 breach — the cage records tool-layer reaches as an informational count and counts only reaches on the runner's own path; floor re-seeded DOWNWARD to the measured remainder; the header states the boundary and why; selftest gains a control (a prisma import inside the tool dir is SILENT) and keeps the plant (a prisma import on the runner's path is RED) <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] F3.x THE TOOL BOUNDARY: extract execTool()'s ~2000-line dispatch out of lib/search/agent-voice.ts into its own module so wall 2's cage has a STRUCTURAL boundary (the desk ruling on 34362218: tool-layer DB reaches are the sanctioned #256.18 road, not context breaches — but the boundary today is two regions of ONE file, so the cage cannot draw it without a brace-span parser); lands AFTER F3.2 (same file); then the context-purity floor re-seeds downward to the runner's own reaches — F3.x THE TOOL BOUNDARY: extract execTool()'s ~2000-line dispatch out of lib/search/agent-voice.ts into its own module so wall 2's cage has a STRUCTURAL boundary (the desk ruling on 34362218: tool-layer DB reaches are the sanctioned #256.18 road, not context breaches — but the boundary today is two regions of ONE file, so the cage cannot draw it without a brace-span parser); lands AFTER F3.2 (same file); then the context-purity floor re-seeds downward to the runner's own reaches <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] CUTOVER: retire the route's pageContext STRING (nlp/route.ts ~:1178/:1261) — its last two consumers, the state token's page binding (state-token.ts ~:102) and the homeInHand regex (route.ts ~:1689), move onto the signed page token from F3.2; until then the string is kept deliberately independent so the PAGE_DIVERGENCE detector stays a real instrument — CUTOVER: retire the route's pageContext STRING (nlp/route.ts ~:1178/:1261) — its last two consumers, the state token's page binding (state-token.ts ~:102) and the homeInHand regex (route.ts ~:1689), move onto the signed page token from F3.2; until then the string is kept deliberately independent so the PAGE_DIVERGENCE detector stays a real instrument <sub>*Added 2026-09-11 via the task door.*</sub>
- [x] F6.1 first move (from F4.1 §6b): the chat's search_homes tool quoted its OWN searchListingsPaginated total (1,872) over a county page citing 1,950 — instrument runParams() to log the tool's params per turn, then the renderer's COUNT_REF refuses a count that cites no snapshot id and a label whose status set does not match the cited scope (the C7 v1.1 delta) — F6.1 first move (from F4.1 §6b): the chat's search_homes tool quoted its OWN searchListingsPaginated total (1,872) over a county page citing 1,950 — instrument runParams() to log the tool's params per turn, then the renderer's COUNT_REF refuses a count that cites no snapshot id and a label whose status set does not match the cited scope (the C7 v1.1 delta) <sub>*Added 2026-09-11 via the task door. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F6.1-claim-refs-2026-09-11.md. runParams instrumented; search_homes' total resolves to a real snapshot with its own fingerprint; the pre-broadening label weld fixed (fellBackToBroadened)*</sub> `FRI 9/11`
- [ ] Home page count is ACTIVE-ONLY by accident (F4.1 §3): the old body hand-built an envelope that never reached parseSearchParams, so / fell to the status default while every county/city page speaks the 6/17 default (forSale,comingSoon: 7,102 vs 7,824) — DECIDE which set the home page states and label it so; no number moves until ruled — Home page count is ACTIVE-ONLY by accident (F4.1 §3): the old body hand-built an envelope that never reached parseSearchParams, so / fell to the status default while every county/city page speaks the 6/17 default (forSale,comingSoon: 7,102 vs 7,824) — DECIDE which set the home page states and label it so; no number moves until ruled <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] F4.2b RETIRE lib/search/tool-status.ts (delete-by 2026-09-15, the C9 v1.1 delta): the per-tool status line derives from the bundle's TOOLS + the cited snapshot's SearchScope through the client's slot template; the 'snapshot' frame precedes any status frame that cites it; generate-contract-bundle.mjs:126 statusToolNames moves onto TOOLS; WIRE_FREE_TEXT floor 2 → 0; the '…and N more' construction dies (the other half of #257.466 finding 2) — F4.2b RETIRE lib/search/tool-status.ts (delete-by 2026-09-15, the C9 v1.1 delta): the per-tool status line derives from the bundle's TOOLS + the cited snapshot's SearchScope through the client's slot template; the 'snapshot' frame precedes any status frame that cites it; generate-contract-bundle.mjs:126 statusToolNames moves onto TOOLS; WIRE_FREE_TEXT floor 2 → 0; the '…and N more' construction dies (the other half of #257.466 finding 2) <sub>*Added 2026-09-11 via the task door.*</sub>
- [x] F6.1c the owner-dashboard road commits through THE RENDERER: app/api/myli/dashboard-chat/route.ts:479 is its own runner and its own committer (the claim-refs cage's committer floor is 2 because of it + the legacy responder); route its stream gate and return through lib/myli/turn-renderer.ts with the same refs; floor 2 → 1 (→ 0 when legacy dies 9/15) — F6.1c the owner-dashboard road commits through THE RENDERER: app/api/myli/dashboard-chat/route.ts:479 is its own runner and its own committer (the claim-refs cage's committer floor is 2 because of it + the legacy responder); route its stream gate and return through lib/myli/turn-renderer.ts with the same refs; floor 2 → 1 (→ 0 when legacy dies 9/15) <sub>*Added 2026-09-11 via the task door. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F3.3-prompt-bundle-2026-09-11.md. the owner-dashboard road commits through the renderer; claim-refs committer census 2→1 (re-seeded by the desk); the legacy responder is the survivor (dies 9/15)*</sub> `FRI 9/11`
- [ ] F5.2 inherits the sized fold-map job (F5.1 §7): 53 hl_lead_facts rows across 4 casing twins, ALL source='extraction' sitting on slots only the deterministic writer may own — NOT a plain UPDATE SET slot (folding the casing would supersede a real count with prose); DESK RULING: PARK them (status='parked', a backup file under ~/hl-reversals first, RULE #0), never delete; the 6 orphan rows ride the same DML; the new boundary already stops the 54th — F5.2 inherits the sized fold-map job (F5.1 §7): 53 hl_lead_facts rows across 4 casing twins, ALL source='extraction' sitting on slots only the deterministic writer may own — NOT a plain UPDATE SET slot (folding the casing would supersede a real count with prose); DESK RULING: PARK them (status='parked', a backup file under ~/hl-reversals first, RULE #0), never delete; the 6 orphan rows ride the same DML; the new boundary already stops the 54th <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] fact-slot-literals floor shrink: LeadSummarySeat.tsx and LockLeftBand.tsx hand-copy the same five slots LEAD_SUMMARY_SLOTS already exports (the exact defect that constant was created to fix; F5.1 §census) — import the constant, ~20 lines, floor falls — fact-slot-literals floor shrink: LeadSummarySeat.tsx and LockLeftBand.tsx hand-copy the same five slots LEAD_SUMMARY_SLOTS already exports (the exact defect that constant was created to fix; F5.1 §census) — import the constant, ~20 lines, floor falls <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] Two CRM suites red before Friday's push, not caused by the fold (F5.1 measured): test:fact-home 19/1 (a fourth resolveFactHome call site exists) and test:remember-fact 29/3 (tool wiring + actor attribution) — measure each at its root, fix or re-baseline with the reason; the web contract.test.ts 636/1 affordability arm (322000/379000 vs 320000/380000, afb33be661 9/09) is already carded — Two CRM suites red before Friday's push, not caused by the fold (F5.1 measured): test:fact-home 19/1 (a fourth resolveFactHome call site exists) and test:remember-fact 29/3 (tool wiring + actor attribution) — measure each at its root, fix or re-baseline with the reason; the web contract.test.ts 636/1 affordability arm (322000/379000 vs 320000/380000, afb33be661 9/09) is already carded <sub>*Added 2026-09-11 via the task door.*</sub>
- [x] ⛔ FRIDAY BLOCKER — drainChatOutbox() has NO origin filter (F7.1 §2): the ~200 F1.3-backfilled conversations (origin legacy_import) sit PENDING in hl_chat_outbox and the first deploy would project them into the CRM a SECOND time (the CRM already holds them as the 709 array-position rows); DESK RULING: legacy_import events never project — the drain skips origin='legacy_import' and marks those rows drained_as='skipped_legacy_import' (no delete), with a red-first test (a pending legacy_import row is skipped by name; a pending ssot_v1 row drains) and a count of the rows it would skip printed at deploy — ⛔ FRIDAY BLOCKER — drainChatOutbox() has NO origin filter (F7.1 §2): the ~200 F1.3-backfilled conversations (origin legacy_import) sit PENDING in hl_chat_outbox and the first deploy would project them into the CRM a SECOND time (the CRM already holds them as the 709 array-position rows); DESK RULING: legacy_import events never project — the drain skips origin='legacy_import' and marks those rows drained_as='skipped_legacy_import' (no delete), with a red-first test (a pending legacy_import row is skipped by name; a pending ssot_v1 row drains) and a count of the rows it would skip printed at deploy <sub>*Added 2026-09-11 via the task door. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-drain-origin-filter-2026-09-11.md. the drain filters origin=legacy_import before the attribution gate and marks projected_at+last_error=SKIPPED_LEGACY_IMPORT; 1,449 pending rows / 224 conversations measured — Friday's first run must print that skip count; 11/11 red-first*</sub> `FRI 9/11`
- [x] F1.6 SPLIT persist-conversation.ts (F8.1 §arm 2 measured): it is the ssot road's turn-completion writer — assistant.answered is appended INSIDE it (:261) plus turn.completed via markTurn, and it is also the identity stamper, the CRM filer, the history guard and the BLOB writer, shared byte-for-byte with the legacy road at seven call sites. Move the ledger appends onto the ssot road's own writer (the renderer's commit → appendChatEvent; the route already appends user.said), the identity stamp onto resolveActor/linkActorToPerson (C3), the CRM filing onto the outbox (already), and leave the blob write + the rest to the legacy road only; ssot-reachability falls 3→1 (dynamic-pills stays F2.4's) — F1.6 SPLIT persist-conversation.ts (F8.1 §arm 2 measured): it is the ssot road's turn-completion writer — assistant.answered is appended INSIDE it (:261) plus turn.completed via markTurn, and it is also the identity stamper, the CRM filer, the history guard and the BLOB writer, shared byte-for-byte with the legacy road at seven call sites. Move the ledger appends onto the ssot road's own writer (the renderer's commit → appendChatEvent; the route already appends user.said), the identity stamp onto resolveActor/linkActorToPerson (C3), the CRM filing onto the outbox (already), and leave the blob write + the rest to the legacy road only; ssot-reachability falls 3→1 (dynamic-pills stays F2.4's) <sub>*Added 2026-09-11 via the task door. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-F1.6-persist-split-2026-09-11.md. turn-writer.ts owns the ledger appends + identity stamp + CRM filing for ssot_v1; the blob is no longer authored on the ssot road (live: blob 0, ledger 5); the direct CRM intake KEPT (it carries applyConversationMove + requestMemoryProcess — the closing move carded); C3 does not exist yet (F1.4 is load-bearing); floors re-seeded by the desk*</sub> `FRI 9/11`
- [ ] PREREQUISITE the plan never named (F8.1 §2): both databases have exactly ONE login role — postgres, a superuser, the role the app connects as — so 'death by permission' (REVOKE the folded write paths) binds to nothing. DESK DDL, next week: mint least-privilege roles (app_web, app_crm; grants per table from the receipt's per-write-path table), move the app connection strings onto them (Railway env, a deploy), THEN the REVOKE/CHECK set. Until then the reachability cage is the only wall — PREREQUISITE the plan never named (F8.1 §2): both databases have exactly ONE login role — postgres, a superuser, the role the app connects as — so 'death by permission' (REVOKE the folded write paths) binds to nothing. DESK DDL, next week: mint least-privilege roles (app_web, app_crm; grants per table from the receipt's per-write-path table), move the app connection strings onto them (Railway env, a deploy), THEN the REVOKE/CHECK set. Until then the reachability cage is the only wall <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] C2 closing move (F1.6 §1): the direct CRM intake on the ssot road stays because it runs applyConversationMove (the run chain + per-tool cards) and requestMemoryProcess (the memory pass) and the started card, which chat-projector.ts (logMessage only) does not — move those three onto /api/internal/chat-events so the direct write drops out with nothing to catch; until then the array-position write is a declared debt, never to be briefed as de-duplication again — C2 closing move (F1.6 §1): the direct CRM intake on the ssot road stays because it runs applyConversationMove (the run chain + per-tool cards) and requestMemoryProcess (the memory pass) and the started card, which chat-projector.ts (logMessage only) does not — move those three onto /api/internal/chat-events so the direct write drops out with nothing to catch; until then the array-position write is a declared debt, never to be briefed as de-duplication again <sub>*Added 2026-09-11 via the task door.*</sub>
- [x] Dashboard regression left by F1.6 (declared): lib/dashboard/anonymous-greeting.ts:91 reads the transcript blob for lastChatSubject, which is null on ssot conversations now that the blob is not authored (the chats count is unaffected) — repoint the read at transcriptsFromLedger (F7.1's projection), three lines, with a plant (an ssot conversation yields its subject) and a control (a legacy conversation still does) — Dashboard regression left by F1.6 (declared): lib/dashboard/anonymous-greeting.ts:91 reads the transcript blob for lastChatSubject, which is null on ssot conversations now that the blob is not authored (the chats count is unaffected) — repoint the read at transcriptsFromLedger (F7.1's projection), three lines, with a plant (an ssot conversation yields its subject) and a control (a legacy conversation still does) <sub>*Added 2026-09-11 via the task door. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-greeting-ledger-read-2026-09-11.md. lastChatSubject derives from transcriptFromLedger; plant + control green; live greeting returns the subject; two more blob readers found (website-chat-log.ts, dashboard-chat/route.ts:435) — measured at the desk*</sub> `FRI 9/11`
- [x] THE BIRTH REPLAY reads the dead blob (desk measured 9/11 09:50 EDT): replayVisitorConversations in lib/myli/website-chat-log.ts (callers lib/crm-intake.ts:21, lib/myli/thread-store.ts:27) replays anonymous turns onto the new lead from hl_ai_search_conversation.messages — which F1.6 no longer authors on ssot_v1, so an ssot conversation replays NOTHING when its visitor becomes a lead; the C2 road for that moment is the outbox's pending NO_LEAD rows attributing through F1.4's link — prove it on the anonymous→sign-in→return journey, then the birth replay dies for ssot_v1 (a 'dies' cell in the authority map) — THE BIRTH REPLAY reads the dead blob (desk measured 9/11 09:50 EDT): replayVisitorConversations in lib/myli/website-chat-log.ts (callers lib/crm-intake.ts:21, lib/myli/thread-store.ts:27) replays anonymous turns onto the new lead from hl_ai_search_conversation.messages — which F1.6 no longer authors on ssot_v1, so an ssot conversation replays NOTHING when its visitor becomes a lead; the C2 road for that moment is the outbox's pending NO_LEAD rows attributing through F1.4's link — prove it on the anonymous→sign-in→return journey, then the birth replay dies for ssot_v1 (a 'dies' cell in the authority map) <sub>*Added 2026-09-11 via the task door. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/grinder/myli-ssot-fold/RECEIPT-birth-replay-retire-2026-09-11.md. replayVisitorConversations skips any conversation whose ledger carries a live act (conversationIsSsot in chat-ledger.ts, ONE shared predicate); legacy replays unchanged; the plant used a populated blob so the pre-existing empty-turns guard could not make it vacuous*</sub> `FRI 9/11`
- [ ] F1.4b THE LEAD-BEARING ACTOR (C3 v1.1 delta): ActorRef gains kind 'lead' (leadId, personId null) for a guest with only an email; resolveActor returns it from the CRM filing's answer; the guest-email stamp F1.6 named as departure 3 RETIRES; the actor-binding floor (36) falls by the sites that derived a lead id by hand (contract/session.ts, visitor-lead.ts, four dashboard loaders, five app/api/myli routes — named in the cage's own run) — F1.4b THE LEAD-BEARING ACTOR (C3 v1.1 delta): ActorRef gains kind 'lead' (leadId, personId null) for a guest with only an email; resolveActor returns it from the CRM filing's answer; the guest-email stamp F1.6 named as departure 3 RETIRES; the actor-binding floor (36) falls by the sites that derived a lead id by hand (contract/session.ts, visitor-lead.ts, four dashboard loaders, five app/api/myli routes — named in the cage's own run) <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] DESK DDL (with the roles, next week): hl_chat_actor_link (visitor_key text PRIMARY KEY, person_id text NOT NULL, evidence_event_id uuid NOT NULL REFERENCES hl_chat_events(id), linked_at timestamptz NOT NULL DEFAULT now()), insert-only — the cross-conversation identity index; STRUCTURE_GATE interview + web-schema-lock regen in the same pass; linkActorToPerson's body becomes one INSERT — DESK DDL (with the roles, next week): hl_chat_actor_link (visitor_key text PRIMARY KEY, person_id text NOT NULL, evidence_event_id uuid NOT NULL REFERENCES hl_chat_events(id), linked_at timestamptz NOT NULL DEFAULT now()), insert-only — the cross-conversation identity index; STRUCTURE_GATE interview + web-schema-lock regen in the same pass; linkActorToPerson's body becomes one INSERT <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] ⛔ F3.4 THE CONVERSATION BINDING ON BOTH SURFACES (the precondition F3.3 arm 2 measured): 11 of 25 live [turn-context] lines ran conversation=none / history=0 — EVERY owner_dashboard turn, because lib/dashboard/dashboard-opener.ts passes conversationId hard-coded empty, and some webchat turns too; C5 forbids a fallback so the ledger window would ship blank. Bind every turn to its conversation on both surfaces (the dashboard's conversation id resolved through the actor binding F1.4 + the person's dashboard thread), prove history>0 on a live dashboard turn, THEN arm 2 (the model window = transcriptFromLedger) lands; this is also C11's precondition — ⛔ F3.4 THE CONVERSATION BINDING ON BOTH SURFACES (the precondition F3.3 arm 2 measured): 11 of 25 live [turn-context] lines ran conversation=none / history=0 — EVERY owner_dashboard turn, because lib/dashboard/dashboard-opener.ts passes conversationId hard-coded empty, and some webchat turns too; C5 forbids a fallback so the ledger window would ship blank. Bind every turn to its conversation on both surfaces (the dashboard's conversation id resolved through the actor binding F1.4 + the person's dashboard thread), prove history>0 on a live dashboard turn, THEN arm 2 (the model window = transcriptFromLedger) lands; this is also C11's precondition <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] prompt-bundle static floor 7 → 2: delete the retired buildContractSystem composer (F3.3 measured it legacy-only; buildStateTurn is the reachable composition) — census its importers from the repo root incl. tools/, then git rm with its test/SCOPE edits in one commit; the desk re-seeds downward — prompt-bundle static floor 7 → 2: delete the retired buildContractSystem composer (F3.3 measured it legacy-only; buildStateTurn is the reachable composition) — census its importers from the repo root incl. tools/, then git rm with its test/SCOPE edits in one commit; the desk re-seeds downward <sub>*Added 2026-09-11 via the task door.*</sub>

## The desk's running notes

*Verbatim from the desk board, absorbed here when the board retired into the schedule tree. Receipts, not plans — the checkboxes above are the plan.*

**Floor 42 — MYLI'S STATE STACK** (*"the playbook is just a consequence of the state Myli's in"* — [full punch list](/master-plan/floor-42)):
- [x] the architecture designed brick by brick + Zach's dictation captured. ⛔ **NOTHING IS BUILT** — no code, no DDL, no data, no send
- [ ] ⛔ **THE FINANCING LINE blocks this floor** (see the shelf below) · - [ ] E-1 the feedback-playbook split · - [ ] A0/A1/A2 the frame stack, the hard line, the run-level pivot · - [ ] B0/B1/B2 the one task door → run-close mints her task → the wake sweep · - [ ] C0/C1 cooling and habit as derived reads

## ⭐ What only Zach can decide on this floor

- **The financing line — advice handed off, logistics owned?** — everything in the flagship's financing pivot waits on this, and it draws a licensed-advice boundary
- **The cooling and habit weights** — the numbers that decide when she backs off
- **The task sweep's cron** — build is the desk's, the schedule is yours

---
*Generated 2026-08-28 by `tools/master-plan/build-floors.mjs` — edit `tools/master-plan/floors.json`, never this page. Tick a box by setting that item's `"done": true`.*
