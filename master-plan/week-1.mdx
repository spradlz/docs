---
title: "WEEK 1 — The Foundation (Floors 1–7)"
description: "22 open items across 7 floors. The Foundation."
---

> 📖 **Week-1 Foundation reading — [THE FUB BOOK](/fub/fub-pipeline-board)** (9 chapters, top to
> bottom: why FUB exists → the six roads → the tapes → the fleet → the sentries → the registry →
> the operator's manual → the D3 end state). Its arrival-map tables are **generated from the live
> registry** — the book cannot drift from the system it documents. The floors below are the work;
> the book is the understanding.

Everything else stands on this. Week 1 makes the FUB signal pipeline **boring**: all 28 signed signals proven live end to end, the settings page showing THE 28 instead of rule soup, sentries that page when a signal dies, messages filing the moment they're captured (killing the unbounded bell forever), the signal registry as data, and Camp David writing the one tape directly. When Floor 7 gates, the foundation Zach called "a 747 held together with duct tape" is a certified pipeline with receipts.

**ZACH GATE at Floor 7:** the foundation top-out — he reads the gauntlet receipts and the restore-drill proof, and signs the foundation as certified.

| Floor | What it is | Open |
|---|---|---|
| **1** | The 28 Signals Verified | 9 |
| **2** | The Triggers Menu | 1 |
| **3** | The Sentries + Canary | 2 |
| **4** | File-at-Capture — CLOSED 8/19 | 0 |
| **5** | The Signal Registry | 1 |
| **6** | Kill the Two-Hop | 1 |
| **7** | Foundation Close-out | 10 |

## Floor 1 — The 28 Signals Verified

1. ✅ **[FUB-8]** THE 28-SIGNAL LIVE-FIRE GAUNTLET — **CLOSED 8/22 night**: the zillow_inapp chain-of-custody proven L1-L6 on TWO independent nonces (send→tape 67s/69s; full chain on the new captain road 2m22s; render walked on prod with the Zillow venue mark; receipt `.claude/reports/fub8-inapp-custody/RECEIPT.md` cross-citing `.claude/reports/fub-gauntlet/RECEIPT-R3.md`) + the -TEST mailbox ask answered by REUSE (test-conner@/test-jasmeet@ existed licensed since 8/14; live-fired 6.0s send→bell with lead resolution exercised). Original email-road proof 8/19 unchanged below — — **email road LIVE-FIRE PROVEN 8/19** (3 real sends into all 3 bundle classes, capture in 5-7s, mailbox provenance stamped, stranger-design held: 0 cards 0 bells, desk-gated on the nonce rows; receipt `.claude/reports/email-livefire/RECEIPT.md`). Remaining for ✅: the zillow_inapp chain-of-custody re-verification (Zach's carve-out) + a minted -TEST mailbox so lead-resolution exercises live in future walks · [board](https://crm.houseloop.ai/boards/fub-pipeline)
2. ✅ **[FUB-9]** THE COMMAND-CENTER PAGE — **LIVE 8/19** at [houseloop.mintlify.io/fub/fub-pipeline-board](https://houseloop.mintlify.io/fub/fub-pipeline-board) (publish proof `published:true, buildStatus:success`, commit `50fae0a37e`): the 28-signal arrival map grouped by the five roads (webhooks · Chrome scrape · note-parsing · native Gmail · the retired email arm), every row from the LIVE `hl_event_mapping` (42 rules) + signals lock, the sentry board w/ FACT columns + thresholds from `lib/health/probes.ts`, the two-databases truth, and the honest proven-vs-🔨 state. Existing page EDITED per DOCS_SECTION_DISCIPLINE, master-scraper-ssot pointed-at never restated · receipt `.claude/reports/fub-command-center/RECEIPT.md` · [board](https://crm.houseloop.ai/boards/fub-pipeline)
3. ✅ **[FUB-22]** ⭐⭐⭐ THE 90-DAY MVP END-TO-END AUDIT — **DELIVERED 8/19** (`.claude/reports/mvp-audit-90/AUDIT.md`, desk-gated): per-road 90-day table on the honest denominator (70 mapped leads, never the 49,246 unpoured tape people), every number one inline live query. Verdicts: calls/saved-searches/native-Gmail CLEAN · notes road HEALED (the "3,077 hole" = 2,999 correct FUB-28 dedups, real June residue 43) · home-activity near-clean (the "254 hole" = 244 in-flight today, 10 genuinely stuck). NEW DEFECTS FILED: ⛔ G1 the webhook ARRIVAL LOG dead 41 days (both fub.webhook_event_log + fub.sync_log stop 2026-07-09 19:36Z → task #165) · the no-street-number filing refusal (46/254 lost vs 0/12,693 kept, class 199 → #166) · two notify=true insight signals 0 bells in 90d + 8 uncarded outbound emails + 6 uncarded texts (→ #167). Honest limits named: tape→card only (G1 blinds FUB→tape), bell adjudication needs the run-log trail · [board](https://crm.houseloop.ai/boards/fub-pipeline)
4. ✅ **[FUB-23]** ⭐⭐⭐ THE BOOK — **THE JOIN COMPLETED AT THE DESK 8/21** (the lane's refused tick now earned): ① the FUB book is Week-1 Foundation reading — linked at the top of this page + the weekly spine; ② the per-section detail view exists across ALL 14 sections (build-master-plan.mjs v2 per-project pages: PRD lede + docs/artifacts + wireframes + item table; PRD coverage verified **14/14**, generator run clean, 461 items); ③ **SLICE-8 SHIPPED**: `tools/docs/build-fub-book.mjs` generates the book's ch.2 road tables + ch.6 registry stats from the LIVE `hl_signal_channel` ⋈ `hl_event_mapping` between explicit markers — drift fatal both directions (a live road with no marker, a marker with no road), `--check` reports page-vs-registry drift; first run already caught the hand-typed counts stale ("42 active" vs live 40 map + 8 drops). HONEST SCOPE correction to the PRD's "ch.2-6" wording: chapters 3-5's facts live in `probes.ts`/pm2/receipts, NOT the registry — generating them from these two tables would fabricate provenance, so they stay authored (stated in ch.6 + the tool header). Original chapter work: the command center reads top-to-bottom (Zach 8/18 night verbatim: "read like a book top to bottom broken down into weekly sections… a working table of contents… click a sub section → the human overview, the PRD, the artifacts, the checklist items; then a MASTER CHECKLIST, infinitely scrollable, 1-500+") — **THE FUB HALF DELIVERED 8/19** (book-23 lane, publish-proven live:true/buildStatus:success; receipt `.claude/reports/book-23/RECEIPT.md`): the FUB section now reads as 9 chapters across 3 pages in nav order with a how-to-read table + prev/next; ch.1 (why FUB exists — D2 pipe-vs-source) written NEW (the purpose material sat at line 1,636 of the manual); ⛔ SAFETY FIX: the manual's FIVE disagreeing process rosters consolidated to ONE (three called the unsafe-to-restart watchdog "self-heals autonomously — never touch it"; two declared the RUNNING scraper dead off the frozen CD heartbeat — a doc-following human would have armed the hazard); all 8/19 corrections folded by citation. THE WEEKLY-SPINE HALF pre-exists (build-master-plan.mjs + the week pages). **REMAINING for ✅ (the lane correctly refused the tick): the JOIN** — link the FUB book into the weekly spine as Week-1 reading + the per-section detail view (human overview + PRD + artifacts + items) across ALL sections, not just FUB · slice-8 generation (ch.2-6 from hl_signal_channel ⋈ hl_event_mapping — the structural cure for roster drift, now buildable) · [board](https://crm.houseloop.ai/boards/fub-pipeline)
5. ✅ **[TASK-157]** The 28-signal arrival map — **LIVE 8/19** on the command-center page (delivered by [FUB-9]): every signed signal → its road in → its tape table → its catalog kind, derived from the live registry, [one findable page](https://houseloop.mintlify.io/fub/fub-pipeline-board)
6. ✅ **[TASK-163]** Board + gauntlet — **CLOSED 8/22 night**: run 3 of the FUB signal gauntlet re-fired live after the #193 actor fix, the #197 sixth-road promotion and the #241 mint gate shipped, on FUB person 50556 "Gauntlet Fub-TEST" with both `-TEST` belts passed before the first write and both databases asserted in-session (tape and CRM resolve to DIFFERENT servers — checked explicitly, because both are named "railway" and a mirror diff against one database is a vacuous instrument). The in-app road fired at 21:37:58.699Z and ran GREEN end to end — injection → capture → enqueue → tape → card with a named actor → bell `{sms,email}` delivered in 141.6s, all four assertions proven at the rows. ⚠ **The green did not close a defect it found**: two inbound messages produced ONE notification and the un-announced one was stamped `notified_at` anyway, closing its ledger so no sweep would ever retry it — filed, then measured by captain-g4 (the suppressor is the freshness gate, not a collapse) and FIXED by the builder-surgery lane, which is why this ticks now · receipts `.claude/reports/fub-gauntlet/GAUNTLET.md` + `.claude/reports/fub-gauntlet/RECEIPT-R3.md` · [board](https://crm.houseloop.ai/boards/fub-pipeline)
7. ✅ **[TASK-84]** BlueBubbles false-success — **THE FEATURE FIX SHIPPED + WITNESSED 8/23 (builder-bbremedy)**: the rank ladder consolidated to ONE (lib/comms/delivery-rank.ts — it existed in triplicate), the webhook observer reads error/isDelivered as INPUT never gate, and the event-only bb-delivery-sweep backstop deployed (registration proven by the function LIST 145→146; witnessed run 01M0Q39XP565E9CWQ65Z4BWNYB) — whose FIRST production run caught a real false-success and corrected it (scanned 2, corrected 1). test:bb-stamp 15→32/32, plant reds exactly the 3 remedy assertions. BB had carried error:22 on real sends since 5/24 with no reader. NO cron until Zach signs. Receipt: .claude/reports/builder-bbremedy/RECEIPT.md · [board](https://crm.houseloop.ai/boards/fub-pipeline)
191. ✅ **[FUB-24]** GAUNTLET F1 — FIXED 8/18 (filing-4): the tape held TWO spellings; "Virtually Toured" (the variant) was missing from the exact-match gate while the full spelling filed fine — variant added + welded by tests, red-first · [receipt](https://crm.houseloop.ai/boards/fub-pipeline)
192. ✅ **[FUB-25]** GAUNTLET F4 — FIXED 8/18 (filing-4): regex missed $-M prices ($1.3M — the priciest leads fell to note_added) + no-parenthetical bodies; corpus-proven 2,943→3,027 of 3,028 (the 1 miss = the gauntlet's own synthetic, loudly quarantined) · [receipt](https://crm.houseloop.ai/boards/fub-pipeline)
193. ✅ **[FUB-26]** GAUNTLET F5 — FIXED 8/18 (filing-4): silent skip now impossible — any behavior-shaped note yielding no card emits a loud review-lane error naming the note id; corpus 16,601/16,610 (9 = 7 real empty-URL notes + 2 synthetics) · [receipt](https://crm.houseloop.ai/boards/fub-pipeline)
194. ✅ **[FUB-27]** GAUNTLET F6 — FIXED 8/18 (filing-4): both arms now share ONE factory + ONE dedup sourceId, actor identical either arrival order — one fact, one card; 4 historical twin pairs await desk absorption (precedent #65/#74) · [receipt](https://crm.houseloop.ai/boards/fub-pipeline)
196. ✅ **[FUB-29]** ⭐⭐ INBOUND EMAIL HOLE — **HEALED 8/18 night, end to end**: 8/8 mailboxes connected to the native Gmail-watch (tyler@ retired by Zach's order) → live capture proven same night → the mailbox backfill ran THROUGH the real writers as Inngest fn `email-inbound-backfill`: dry-run plate desk-verified (run 01M0BC56G1: 9,272 scanned / 0 errors / 0 truncated) → execute (run 01M0BDN6C8, 29min: **7,789 recovered**, bells 2,315→2,315 exact, synced_mailbox stamped) → idempotence run 01M0BFF01Y **inserted 0**. The 598 dry-vs-execute delta = cross-mailbox Message-ID collapse (dedup working). Incident on record: the first dispatch was a FAKE-GREEN (PUT raced the deploy; event accepted, nothing ran) — the Inngest amendment held: no run id, no claim · receipt `.claude/reports/backfill-49/RECEIPT.md` · [design](https://crm.houseloop.ai/boards/fub-pipeline)

## Floor 2 — The Triggers Menu

8. ✅ **[FUB-7]** THE TRIGGERS MENU — the FUB settings page shows THE 28, never the rule soup — **shipped 8/18 (5a688ad53c) + desk-walked on the deployed page** (28 rows, banned tokens fold-only, Property Shared honestly OFF; receipt `.claude/reports/triggers-ui/WALK-RECEIPT.md`) · [board](https://crm.houseloop.ai/boards/fub-pipeline)

## Floor 3 — The Sentries + Canary

9. ✅ **[FUB-15]** SLICE 4 — per-signal sentries derived from the registry — **THRESHOLDS ARE DATA 8/19** (registry-build lane, mig 0786 + probes repoint; receipt `.claude/reports/registry-build/RECEIPT.md`): the sentry windows now READ from `hl_signal_channel.thresholds` by channel_key (probe key is many-to-many with road — the lane proved it can't address the numbers), with the exact prior constants seeded (21/21 parity proven against a rolled-back live migration, discriminating by plant) and absent/malformed rows falling back WHOLE to the compiled defaults. **Red-first: 12 data-flips changed live verdicts with ZERO code change** (incl. the 8/19 Jovanna 330m sentinel RED→GREEN→RED). Deployed proof: the 11:30:04Z scan tick wrote `thresholdsSource: hl_signal_channel` on all 3 FUB probes (11:20 pre-deploy tick NULL — before/after in the same table, desk re-read). Earlier 8/18 first slice (inbound-email + saved-searches + home-activity probes) unchanged underneath. Honest notes: note_parse has no PARSE sentry (arrival covered; parse-to-nothing raises the review-lane error, no probe counts it — #159 remainder) · relay hop honestly NOT-CHECKED where CAMP_DAVID_DATABASE_URL unset · [board](https://crm.houseloop.ai/boards/fub-pipeline)
10. ✅ **[TASK-161]** Photos unmeasured — RESOLVED 8/18-8/20 (task #161): hl-photos-redfin restarted the structural way (ecosystem-managed w/ the Camp David DATABASE_URL asserted, #225 cure); measurement owner assigned, fleet reboot-survivable

## Floor 4 — File-at-Capture — CLOSED 8/19

11. ✅ **[FUB-12]** SLICE 1b — FILE AT CAPTURE — **LIVE-PROVEN 8/19**: the consumer now carries the recovered row IN the POST (read from Camp David, the capture source); `fub-body-recovered` files from the payload immediately and the confirmed-stamp became a verify-tape reconciliation belt (mirror confirmed before `notified_at`, throw-retry + sweep re-request as backstop; bare-ids POSTs unchanged). Harness 20/20 red-first (tape-withheld bare-ids = the old relay-bounded world files NOTHING; same event + payload files in 506ms; land-tape+replay stays exactly 1; adverse order green) · **prod e2e on the deployed build: POST → card in 5.4s with the tape withheld, verify-tape stamped after the tape landed, bells 2316→2316** · consumer restarted + pm2-save dump-verified · proof `lib/activity/inapp-filing-capture.test.ts` · receipt `.claude/reports/floor4-capture/RECEIPT.md` · [board](https://crm.houseloop.ai/boards/fub-pipeline)
12. ✅ **[TASK-158]** The unbounded bell — **KILLED 8/19** by [FUB-12]: card + bell are now bounded by capture + one POST (measured 5.4s live, 506ms in-process), never by the relay cursor (8m43s measured lag) or unrelated later traffic triggering a backfill
195. ✅ **[FUB-28]** GAUNTLET double-arrival — **TWO-ARM CURE SHIPPED 8/19** (scraper ↔ automation-notes, the arms behind the 40 measured twins): `backfill-fub-behavior` now runs a DB-verified twin claim before filing (alreadyFiledAs precedent — a (lead, kind, zpid, UTC-day) already carded by the OTHER FUB arm skips OUT LOUD, never a skipLog flag); the old in-memory dedup only saw one run's events, so cross-run twins minted whenever the scraper's row landed after the note already carded (or vice versa). Harness-proven both directions, incl. the discriminating assertion that ONLY the claim (not per-table idempotence) stood between the event and a twin. **Honest remainder: the pixel arm (34/7d) files separately with its own rows — its fold into the claim is a follow-on** · proof `lib/activity/inapp-filing-capture.test.ts` §⑤ · receipt `.claude/reports/floor4-capture/RECEIPT.md`

## Floor 5 — The Signal Registry

13. ✅ **[FUB-13]** SLICE 2 — hl_signal_channel: the registry AS DATA — **LIVE 8/19** (mig 0786, desk-signed diet + table verdict; commits 3ada047ddb/71b088d62d, 14/14 cages ×2; receipt `.claude/reports/registry-build/RECEIPT.md`): one row per ROAD (webhook · chrome_scrape · note_parse · native_gmail · email_scrape-retired), every seed fact READ live (pm2 describe, the write sites file:line), exactly the 14 signed columns. `hl_event_mapping.channel_key` FK added + 42/43 rules backfilled — ⭐ the lane proved raw_type ALONE cannot decide the road (the 4 textMessagesCreated rules split on messageType; a naive derive would have filed our highest-intent traffic under the wrong road). DESK RULING: `_global` stays NULL permanently (the master kill rule genuinely spans all roads — a fake 'all' road would break the grain; the migration's DO-block guards that ONLY _global may be NULL). FUB-19 can now GENERATE the command-center road tables from `hl_signal_channel ⋈ hl_event_mapping`. Refinements noted, not taken: tape_table as text[] · ON UPDATE CASCADE absent per design (renames are migrations anyway) · [board](https://crm.houseloop.ai/boards/fub-pipeline)

## Floor 6 — Kill the Two-Hop

14. ✅ **[FUB-14]** SLICE 3 — KILL THE TWO-HOP — **KILLED AT THE DESK 8/22 evening**: census found only TWO families still rode the relay (zillow_insights + saved_searches — every other family already wrote the tape directly); both got the per-person direct mirror sync (syncPersonTableToMirror, the home-activity precedent — ids preserved for relay-era key continuity, anti-wipe guard, loud logging); 5 direct syncs verified live across both families BEFORE the stop; **fub-relay STOPPED + pm2-saved (quarantined, never deleted — one `pm2 start fub-relay` reverses)**. Ledger: `.claude/reports/fub-captain/QUARANTINE.md` (incl. the accepted wake-ping residue + the fossil heartbeat + the pending SMS scope-cut). Honest remainder recorded there, NOT here: the local capture DB stays alive for the flagged dashboard readers — its death is a later, separately-sequenced kill · [board](https://crm.houseloop.ai/boards/fub-pipeline)

## Floor 7 — Foundation Close-out

15. ✅ **[FUB-10]** SLICE 0 — STOP THE BLEEDING — **STOPPED, measured, 8/22**: the mint floodgates closed (9 action-needed triggers only; registration/pre-approvals/person-created OFF — Zach's law as data, dumped before-state ~/hl-reversals/mint-set-0822/) · replays never mint AND never notify (D4 weld 347ae1d702; 54 refusals held in one day; the phone-noise leak monitor-verified sealed) · the trashed-twin gate holds (a human's no is final) · outbound records can't mint (the inbound-only seat-line gate, red-first) · **the twin-minting scraper cured at the writer** (both-generation dedup seed + stable no-timestamp identity; acceptance watch: 1 new scraped row in 83 min vs ~26 expected under the disease) · the population itself purged to the law (62 active, every lead explains itself). The bleeding is stopped at every measured hole · [board](https://crm.houseloop.ai/boards/fub-pipeline)
16. ✅ **[FUB-11]** SLICE 1 — ZACH'S DECISIONS LAND AS DATA — **LIVE 8/22, every ruling he made today is rows, not code**: the mint set = hl_event_mapping.creates_lead toggles (settings-page controlled, "nothing hardcoded" — his words); the arrival grammar + payload-only + the fidelity ladder = hl_settings rows (his wording changes are one UPDATE, proven twice today); sentry/canary thresholds + writer identities = hl_signal_channel.thresholds; the signal set itself = the signed signals-lock (re-signed twice today for his rental retirement + the mint law, each with the reason in the commit); the rental rule's retirement + re-point = mapping-row data. Receipts throughout the day card · [board](https://crm.houseloop.ai/boards/fub-pipeline)
17. ✅ **[FUB-16]** SLICE 5 — QUEUE HYGIENE — **DONE 8/22**: ONE consolidated job table (fub.job_queue, the renamed+widened body_recovery_queue — ONE_ANSWER_TABLE honored, job_kind a column; the rename's 9-hour trigger-function outage found, cured, and autopsied IN the DDL file as the lesson) · claimed_by/claimed_at stamps for interchangeable workers · FOR UPDATE SKIP LOCKED claims · the queue drains clean (verified tonight: 0 stuck inbox_sweep jobs, tick 40/40 green, capture 27 min fresh) · the rescue's 982-row cleanup + id-preserving re-run closed the erosion · [board](https://crm.houseloop.ai/boards/fub-pipeline)
18. ✅ **[FUB-17]** SLICE 6 — the restore drill — **BUILT + DRILLED 8/22 · DEPLOYED TO CAMP DAVID BY THE DESK same evening** (`scp → ~/houseloop-backups/restore-drill.sh`, DEPLOYED confirmed). Honest remainder recorded here, not hidden: the WEB + FUB-mirror dumps still want the same drill treatment (CRM drilled only). `tools/houseloop-backups/restore-drill.sh` restores the newest CRM dump into a scratch DB (`hl_crm_drill_<ts>`), counts five sentinel tables against floors, prints PASS/FAIL + timing, and DROPS the scratch DB on every exit path. **Inaugural drill: the 8/22 dump restored in 46s, 5/5 sentinels over floor, zero residue.** The stronger receipt predates it — the 8/18 dump was restored for real as `hl_crm_0818_snapshot` (2,480 MB, still on Camp David) and its **155 `hl_leads` rows are what the 8/22 triage ran against**. All three failure paths proven RED before the PASS was believed (name guard planted with `houseloop_crm` → refused before `createdb`; a real 14-day-old dump → `348h old` FAIL; `hl_leads` floor planted at 999,999 → `actual 229 — SHORT`). ⚠ `pg_restore --list` cannot prove completeness (the TOC is at the front of the file) — only a restore can. Runbook: [the operator's manual](/fub/master-scraper-ssot). **Remaining for ✅:** the desk deploys the script to `~/houseloop-backups/` and the WEB + FUB-mirror dumps get the same treatment (drilled: CRM only) · [board](https://crm.houseloop.ai/boards/fub-pipeline)
19. ✅ **[FUB-18]** SLICE 7 — staging → import per Bedrock D3 — **THE PERSON STAGING LAYER IS LIVE AND WITNESSED 8/22**: `hl_staging_person` (mig **0820**, signed verdict `canonical` Lee 8/22) holds **one pre-derived row per FUB person — 49,250, the whole tape population** — poured by the registered Inngest function **`fub-staging-person-derive`**, witnessed in `hl_run_log` at runs **`01M0NZ888P6MS3S82RT2629YAT`** (first full pour) and **`01M0P07RSNXVWTETZVKTGYZD6Q`** (the duplicate-lead fix). 43 of the tape's 68 columns staged, the other 25 not-staged **each with a recorded reason**; every derived group carries a verdict from one closed set (`resolved` / `absent_on_tape` / `unresolvable`) plus the SOURCE that produced it, with CHECKs making a NULL-without-a-verdict unrepresentable — **proven RED on planted violations against a passing control** (a resolved agent with no rung; a house seat wearing rung ②; rung ⑤ wearing a real source; a venue reading "Follow Up Boss"). ⭐ **THE ACTOR LADDER** resolves once per person through `lib/integrations/fub-agent-ladder.ts` — the ONE home the house-name set finally has — and stamps `agent_rung` + `agent_source`: **② 10,069 · ③ 6,057 · ④ 4,617 · ⑤ house-seat floor 28,494 · no evidence at all 13**. Scored against the **156,346 witnessed outbound rows at 97.02% exact / 2.98% wrong person**, graded on the POURED rows rather than a re-run of the resolver, with the shipped message-grain encoding reproduced first as a calibration control. Maddison Mettman (fub 13394) resolves to the joint account **"Lane and Noelle" at rung ③** — her person record holds only a house seat, her `Agent:` tag names the house, her two `Team Exit -` tags disagree so the tag rung correctly refuses, and the chain is the only source that speaks (⚠ which required reading **both ends of every hop** — every `to_agent` in her chain is a house seat, so the PRD's literal `to_agent`-only rung ③ could not have answered her; corrected in the PRD). ⛔ **IT MINTS NOTHING** — no leads, no cards, no notifications, no writes outside `hl_staging_person`; the import/pour is a separate gated step that READS this table. ⛔ **The SRG estate stays untouchable, reads included**: past-client membership is **NOT DERIVABLE HERE** and carries its own blocker token (`relationship_class_not_derivable_here_membership_arrives_at_import`) distinct from the not-built-yet blockers beside it — Zach confirmed Madison Mettman IS a past client, and staging filing her `relationship_class` as unresolved is **correct behaviour, not a miss**. ⭐ **Two open decisions travel as DATA, not schema** — `trigger_window_spec` (poured at **2026-07-01**, the record's answer per Zach's *"from july 1st on is fair game"*) and `source_raw` beside a versioned `source_key` — so re-running under a different ruling is a visible re-derivation, never a rewrite of history. **⚠ REMAINING for the SLICE (staging → *import*) — this tick is the PERSON STAGING half only:** the identity-fill / household / relationship-class jobs, the readable surface, repointing the pour to READ staging, and the ratchet that reds any import reading `fub.*` directly for a derived answer. **Desk items filed from this build:** the named-DNC set needs a machine-readable home (it is four prose entries, three of them surnames — `hl_fub_person_facts.is_dnc` is an AI extraction and was correctly refused), and the `TAG_NOISE_NAMES` repoint on the live staging-EVENT path. 📎 Receipt: [the staging & import PRD](/fub/staging-import-prd) (per-field map + the annotated reach table) · [board](https://crm.houseloop.ai/boards/fub-pipeline)
20. ✅ **[FUB-19]** SLICE 8 — the docs GENERATED from the registry — **DELIVERED 8/22: the generator ran and CAUGHT REAL DRIFT.** `tools/docs/build-fub-book.mjs --check` went red against the live registry; regenerating corrected the book's road tables to the day's signed changes — the mint law took `fub_person_created` / `Registration` / `Pre Approval` out of the mints-a-lead column, and the retired rental rule now shows `Rental Inquiry ⛔ disabled → property_inquired`. Registry stats moved 40 recognition rules (38 enabled → **37**) and 31 → **30** distinct catalog kinds. `tools/master-plan/build-master-plan.mjs` re-ran clean with **zero diff** (461 items, 14 project pages, nav current) — the master plan had not drifted. **Remaining for ✅:** chapters 3-5 stay authored on purpose (their facts live in `probes.ts`/pm2/receipts, not the registry — generating them would fabricate provenance), and nothing yet re-runs `--check` on a cadence, so the book can drift again the moment a toggle moves · [board](https://crm.houseloop.ai/boards/fub-pipeline)
21. ✅ **[FUB-20]** THE PLAN SIGNATURE — **SIGNED BY ZACH 8/21 night, verbatim: "fub 20 signed"** — slices 10-19 ARMED; the captain lane (Phase B, item 199) is executing the pipeline rebuild the slices ride on · [board](https://crm.houseloop.ai/boards/fub-pipeline)
22. ✅ **[TASK-159]** FUB overhaul slices — **THE UMBRELLA RECONCILES 8/23 (auditor-ticks, read on this page)**: every slice this item covers is ticked with its own receipt — [FUB-10] slice 0 · [FUB-11] slice 1 · [FUB-12] slice 1b · [FUB-13] slice 2 · [FUB-14] slice 3 · [FUB-15] slice 4 · [FUB-16] slice 5 · [FUB-17] slice 6 · [FUB-18] slice 7 · [FUB-19] slice 8 · [FUB-20] the plan signature. This tick is the RECONCILIATION only and inherits, never erases, the honest remainders the slices themselves record: the restore drill covers CRM only (WEB + FUB-mirror dumps undrilled, [FUB-17]) · [FUB-18] is the PERSON-STAGING half of staging→import, not the import · the generated book has no cadence `--check`, so it can drift the moment a toggle moves ([FUB-19]). Nothing new was proven here — the umbrella closes because its children are closed · [board](https://crm.houseloop.ai/boards/fub-pipeline)
23. ✅ **[TASK-150]** pm2 log rotation — DONE (task #150): pm2-logrotate live on Camp David; the 590MB orphan-log deletion decision recorded separately (#171 done)
24. ✅ **[TASK-139]** Retry-loop client — ROOT-CAUSED + FIXED + deployed as a prod hotfix (task #139): the stale cap literal made the 422 handler deaf; cap exists ONCE, explicit creates refuse honestly; 1,134-error/day bleed stopped

## 📚 The shelf — deep-dive pages this week builds from

- [The Bedrock program — the five layers, one-door, the no-lies harness](/master-plan/bedrock)
- [Notifications — the per-seat rules estate](/master-plan/notifications)
- [Activity cards — the catalog render grammar](/master-plan/activity-cards)
- [Group text — the seat-model transport](/master-plan/comms-group-text)

---
_№ numbers match [THE MASTER CHECKLIST](/master-plan/master-checklist) exactly. Bracket tags name the item's home board (see the checklist legend) — they are never floor numbers._

---

# ⭐ THE E2E VERIFICATION FLOORS — Floors 29–34 (Zach 8/21: "the CRM as built functions as intended")

**The rule: a box ticks ONLY on an AUDITOR receipt** — a fresh-context auditor walks it on screen,
screenshots it, and asserts the database row. A builder's own report never ticks its own box.
V-numbers are permanent; new items append; nothing renumbers. Master index:
[E2E Verification PRD](/master-plan/e2e-verification).

## Floor 29 — Activity cards render right (the iPad view)

- **1.** ✅ **SIGNED BY ZACH 8/21** — the activity catalog (verbatim "I SIGN OFF ON THE ACTIVITY CATALOG", reconfirmed at the floor-numbering fix). Receipt: the signed catalog + prod shot `.claude/reports/legacy-catalog-261/shots/train18-isaac-wilde-chevron-675fix-prod.jpg`.
- **2.** ✅ **AUDITOR PASS 8/21** — teal band (#0F766E, one continuous wash) + round real-logo chevron (gold/blue/red match to the digit) on 232 legacy cards, prod DOM-measured. Receipt: `.claude/reports/foundation-audit/AUDITOR-V2-V4.md`.
- **3.** ✅ **AUDITOR PASS 8/21** — the 4 discriminating rows (DB names the LEAD as actor) all wear the chevron; class-wide initials detector over 570 cards = 0. Receipt: `.claude/reports/foundation-audit/AUDITOR-V2-V4.md`.
- **4.** ✅ **AUDITOR PASS 8/21** — full scroll to the oldest card (Apr 2), 570 legacy cards, zero blank/undefined/`{{token}}`/invalid-date. Two honest observations flagged (pill 630 vs 590 rendered → item 5's question; twin saved-search tape rows). Receipt: `.claude/reports/foundation-audit/AUDITOR-V2-V4.md`.

## Floor 30 — Everything runs through the activity card

- **5.** ✅ **COMPLETE 8/21 (third audit, v4, pinned to one build)** — the count is finally ONE answer everywhere: Isaac serves 590 rows in an id-for-id IDENTICAL sequence at limit=40 AND limit=200 (16 pages, 15 boundaries, zero loss — the 590-vs-589 split dead), pill == 590; Conner 144 served / pill 137 rendered ON SCREEN ("137 All"), served ≥ pill is the CORRECT state (the pill counts collapsed cards a human sees). Three cures stacked: the pill's 3 visibility copies → one pipeline (Train 22) · the counter's 3 hand-copied orderings → one `feedOrderBy` declaration with the (occurred_at, id) tiebreak (Train 27) · the trashed-union owner-gate (#287, Train 27 — 0 rows from the trashed twin, foreign = the sanctioned household member only). ⭐ v4's find: the desk brief's "both tie rows served once" assertion was ITSELF the defect written as an expectation — the two rows are a #74/#188 double-file twin (one fact, byte-identical body, two source_ids) and composeFeed correctly renders ONE; the true fix-bar is the DETERMINISTIC SURVIVOR across page sizes, which holds. Receipts: `BUILDER-ITEM5-287.md` + `AUDITOR-V2.md` §AUDITOR-V4.
- **6.** ✅ **PREMISE DEAD 8/21 (desk-verified at source)** — no double-render exists: the feed reads ONLY hl_activity_log (zero hl_stage_transitions refs in the route); the "duplicate" was the household union correctly rendering TWO people's moves (Eddy + Jesus Lozano, 4 distinct rows). Guard test shipped (10/10, red-proven): no compose file may ever read hl_stage_transitions. Receipt: `.claude/reports/foundation-audit/BUILDER-273.md`. NEW FIND filed for Zach: two people's stage cards read byte-identical on a household feed (grammar names stages, never the person — 294 pairs exposed).
- **7.** ✅ **AUDITOR PASS 8/21, walked end to end** — ONE note seam (`create-lead-note.ts`: both routes call ONE function owning the INSERT + the filing; a writer that discards its own row id cannot file). Live walk on prod: add-lead modal note → `hl_notes` via the polymorphic pair → `note_added` card keyed on the note's own id → bell DELIVERED to Zach (actor excluded per D4) → renders "Myli created a note". Counts moved exactly (+1/+1/+2). #272 closed as dated-then-cured. Fixture trashed, zero residue. Receipt: `.claude/reports/foundation-audit/AUDITOR-V2.md`.
- **8.** ✅ **COMPLETE 8/21 (v5, the full mint arc) — FLOOR 30 DONE** — Item Eight-TEST walked end to end on prod: anonymous tour door → lead MINTED → `registration_completed` card + 4 bells (recipient ≠ actor, D4 held) → returning case correctly did NOT re-mint → trashed via the real disposition route (#202's orchestrator witnessed: `lead_trashed` filed by Myli 19:21:22Z, zero bells on the trash) → all three phone-line guards green before AND after, zero -TEST litter, baseline exact. Two findings filed, not hidden: the nine signed `registered_*` DOOR kinds have NO WRITER (every registration collapses onto the generic kind — the "which door" half of the two-layer model never fires) · the returning-registration arm is UNREACHABLE from the website road (the site never emits a second registration event for an existing user — the Train-21 sibling arm waits on a touchpoint that never comes). Residue: 2 scheduled appointments survive the trash (no cancel route exists — product gap filed). Receipts: `AUDITOR-V2.md` §AUDITOR-V5 + 12 shots.
- **9.** ✅ **PREMISE DEAD 8/21 (desk-verified)** — the "6h-off" pair are two REAL rows 6h01m apart (06:30Z vs 12:31Z; the minute differs — no offset moves a minute); both render correct CT. Receipt: `.claude/reports/foundation-audit/BUILDER-273.md`.
- **10.** ✅ **COMPLETE 8/21 w/ deploy witness** — notes ownership has ONE home: has_notes + backfill cursor + the second reader (backfill:804, the census miss) ALL repointed and DEPLOYED (Trains 21+22, deploy 687a9e6e SUCCESS); writer retired; mig 0800 applied WITH its hl_schema_migrations ledger row (15:00:59Z). ⚠ The honest history: the first drop outran its reader's deploy → 35-min leads-table outage → restored from the dump → the boot runner re-applied the drop correctly on Train 21's boot. Law banked: DDL waits for its readers' DEPLOY, never their commit; a zero-consumer error belt is fake (:804 threw silently under 99 "success" runs until the loud belts shipped). Receipts: `BUILDER-ITEM10-PREP.md` §EXEC+§FINDING-2, `AUDITOR-V2.md`, mig 0800's corrected header.

## Floor 31 — Leads table works + filters like a human

- **11.** ✅ **AUDITOR PASS 8/21** — 4/4 sorts sequence-identical to the DB under the app's own ORDER BY (collation-aware, not byte-compare); persistence proven on a bare URL. Receipt: `.claude/reports/foundation-audit/AUDITOR-FLOOR31.md`.
- **12.** ✅ **COMPLETE 8/21 (v3 N=1 + v4 N≥2)** — every active filter is a removable chip with a real, specific label ("Last Communication is empty") and its own aria remove button; "N filters active" == chip count at 1 AND 2; removal is EXACT (2→1 lands byte-equal to the surviving criterion's standalone count 68; 1→0 returns to baseline 75 on the bare route). v4's positive-control discipline: its first criteria pair read 0 rows at both N — indistinguishable from breakage — thrown out and re-run with nonzero distinct sets (11/68/11-AND). Bonus reading: the criteria-only path lands on the bare route cleanly; #288's polarity split is confined to the ?sf=-present case. Receipts: `AUDITOR-V2.md` §AUDITOR-V4 + shot v4-item12-two-chips.jpg.
- **13.** ✅ **COMPLETE 8/21 (r5, auditor-v3)** — THE STICKY POISON IS DEAD: bare /leads renders the full table with zero params hours after a pill was applied (the r3 one-click-empty reproduction, now clean); pill criterion carries a URL trace, reload honest, Clear-all state honest through reload. All controls previously driven r4 (Stage 47/47 · Agents 63+7 · Sources 55/55). Residual defects filed, not hidden: #288 (post-Clear-all the saved-filter TAB still reads active over an unfiltered table — ruled: Clear-all drops sf) · #268/#270 (AI clones in the Agents list, fix shipped Train 24, walk owed on item 15). Receipts: `AUDITOR-V2.md` §AUDITOR-V3 + shots.
- **14.** ✅ **COMPLETE 8/21 (v4, 3/3 screen == SQL)** — expected counts derived from the app's OWN gate expression (buildLeadsCountQuery executed live with the page's own default flags — the count/list contract, not a shortcut): combo A 9==9 · B 0==0 · C 9==9, with combo C carrying 3 chips / "3 filters active" (item 12's proof extends to N=3). ⭐ Method law from the near-miss: combo A read 11 people two hours earlier — live drift (real leads got contacted), NOT a defect; a screen count and a SQL count compared across any gap on a live table are two different populations. Cure: SAME-WINDOW comparison, timestamps in the receipt (19:05:51→19:06:06Z, exact agreement). Receipt: `AUDITOR-V2.md` §AUDITOR-V4, commit bbfcc2f68a.
- **15.** ✅ **COMPLETE 8/21 (Train 24 fix + v4 screen pass)** — EIGHT distinct human pickers walked on prod (Agents filter · add-lead assignee · bulk-assign · task assignee · the TC band's four), every one exactly 4 humans, 0 duplicates, 0 AI names, 0 emailless entries (the clone tell); the task picker's feeder network-proven (/api/options, the filtered endpoint); typeahead checked at empty-focus so no member could hide behind a query. The AI is correctly SEGREGATED, not absent — Myli lives under her own labelled "AI AGENT" control, nowhere in the human pickers. Before/after: the same roster served 7 entries with two duplicate name-pairs + Myli this morning. Root cause + cure: `humanSeatsOnly` (type-keyed, NULL-safe) at the shared source. Cosmetic residue noted in #268: sibling TC pickers disagree on sort order (Zach-first vs alphabetical). Receipts: `BUILDER-ITEM15.md` + `AUDITOR-V2.md` §AUDITOR-V4 + 4 shots.
- **16.** ✅ **COMPLETE 8/21 (auditor-v6, all four faces on screen + desk-verified)** — rows NO LONGER move under a live selection: the table freezes with a held selection and surfaces changes as a banner instead of re-sorting under your cursor. (a) arrivals → "N new leads" banner, rows frozen, selection intact; (b) non-arrival re-order sits until wake; (c) click applies + clears; **(d) the zero-arrival branch witnessed live on prod**: `· order changed — update` on screen (20:13:46→56Z) with arrivals PROVEN 0 two ways (row count 82→82 AND `hl_leads` created-in-window = 0, desk re-queried), selection held through the burst, click → applied + banner gone + selection kept. The wake for (d) was the auditor's own #293 finding applied: a note bell (adds no row) wakes the bells-only realtime channel — exactly how a zero-arrival re-order surfaces in production. Cleanup DB-verified at the desk: fixture name restored, the one probe note soft-deleted, one declared bell on a -TEST fixture. Receipts: `AUDITOR-V2.md` §AUDITOR-V6 + shot v6-item16-banner-order-changed-zero-arrivals.png.
- **17.** ✅ The filter-parity guard PROVEN RED-PROVABLE (auditor-v6 8/21, desk re-ran 319/0 + zero plant residue): baseline GREEN → TWO independent plants RED (a registry field with no condToSql branch → 4 reds quoting the builder's own "result set WIDER than asked" warning — the 7/24 bug class made unbuildable; the literal list/count parity break → §7 red printing both diverging WHERE clauses) → restored byte-identical (md5), FINAL 319/0 = baseline. Plants were SHELL writes on purpose (an editor write auto-commits to the shared branch); two self-caught false-green instrument failures on the record (wrong-cwd empty diff discriminated by a planted byte; a single-line grep truncating multi-line WHERE values into "identical strings reported different"). Receipt: AUDITOR-V2.md §AUDITOR-V6.
- **197.** ✅ **COMPLETE 8/22 00:29Z — THE LEADS-TABLE COLUMN SET, walked 7/7 PASS on the deployed build (auditor-v7, sha-witnessed, 5 shots)**: five default columns · the Action Needed PAYLOAD cell (Zach's three lines: imperative → composed sentence → "It's been X with no response" + stamp, "Mark complete ☐" inline right on line 1, layout v2 after his live screenshot iterations) · 7 signed imperatives incl. "Make first contact:" (zero blank lit rows) · zero bare clocks BOTH columns (created_at OUT of the All-Comm clock; mint kept in the ranked default) · the lone-URL rule (Isaac's Raytown URL = one truncated line) · leadDisplayHandle (the UUID row now reads "(913) 270-1783 wrote"; 0 UUIDs page-wide) · "Marked complete · Undo" residue SURVIVES hard reload (11→65 on clear, back to 11 on undo, prod left as found) · Zach's sentences live from the resolver ("marked an action needed item complete"/"reopened…") · the table at ~1.2s after the 10s P0 (index 0805 + the lateral fix). Trains 37b71a0671 + d443d5d34b, deploy 2dcacd94. Receipts: BUILDER-277B*.md + AUDITOR-V2.md §ITEM 197 FINAL + shots/v7-item197-final-*.png. Morning residue: seller_demand double-verb · "reopened" wording glance · the imperative catalog COLUMN migration. — **PROD WALK 8/21 night (auditor-v7, sha-pinned to train 37b71a0671): five columns render, both dead labels absent page-wide, clear→sink exact (0→55 of 82, bench==prod), second-undo fix confirmed live, Zach's stored layout carries all five. TWO WALK DEFECTS desk-ruled into the payload rebuild: undo unreachable after reload (cure: "Marked complete · Undo" residue state) + a nameless lead's UUID rendering as the actor name (cure: ONE leadDisplayHandle fallback — phone → email → "Unknown caller" — shared by name cell + composer). Seat note recorded: audit walks run as myli@ (the only authenticated prod browser); Myli-stamped fixture actions in walk windows are the auditor.** — Zach's signed rulings: Piece 1 default sort DONE (lit rows float; 117/0 live-DB tests w/ plant, deployed `ad1cfad0` SUCCESS 8/21, live-verified rows 2+6 of 74) · Piece 2 live-verify DONE · OPEN: Piece 3 row-level clear (`action_cleared`/`action_uncleared` via logActivity, red-first re-light proof) · Piece 4 THE FIVE COLUMNS default-visible (last inbound / last outbound / all communication / action needed / website activity — every column reads the EXPORTED leads-query expressions; the `last_comm` denorm READERS die here, #253 executes via this piece; denorm BODY stays, its DROP is a separate desk event) · Piece 5 `last_convo` dies (Communication group = Last Inbound / Last Outbound / TTFC). FINDING riding this lane: nameless inbound-call leads render BLANK name cells (rows 1/2/4 of the default table, e.g. lead `8c207ed3` — needs a name-cell fallback ruling, likely the phone number). **ZACH RULED 8/21 night (verbatim anchors): the Action Needed cell carries the PAYLOAD — the un-responded triggering signal itself ("tour request, text received, phone call received, property inquiry, contact request, in app message") with substance + time, never a bare checkmark; the control is a small top-right checkbox labeled "Mark complete" (label-law win over "clear action"); catalog sentences move to "marked an action needed item complete" grammar. ALSO: the All Communication cell renders the Zillow mark + actor sentence + message snippet + date/time for in-app messages — the sentence source widens to the three-estate union so the sentence always describes the event the clock reports (Zach hit the defensive bare-clock residual live). Both in the builder lane.**

## Floor 32 — Notifications work properly

- **18.** ✅ TIER RULE LIVE AS DATA (built builder-276, applied+verified at the desk 8/21, mig 0803 ledgered): `hl_notify_classes.notify_tier` (SMS default per Zach's action-needed rule; seat rules ALWAYS win) + `email_default` (seeded VERBATIM from the retired PREF_DEFAULTS — zero email movement at fold time, so the SMS 13-quiets/3-gains notice to Zach stays the complete delta). THE FOLD: dispatch's hand-kept 43-entry PREF_DEFAULTS code map is DEAD with a tombstone naming its own two lifetime drifts (a missing voicemail_received twin; `port_updated` misspelled `port_update` for thirteen months, silently defeating the comment above it); resolution = ONE exported pure function `forcePref ?? rulePref ?? classRow`. Applied live: 63 classes / 22 action_needed / 20 email_default / ALL FOUR PAGERS protected on both axes (the #169 lesson as data); DO-block refuse-on-drift passed; `test:notify-tier` 30/30 desk-re-run AGAINST THE POST-MIGRATION DB incl. seat-beats-tier both directions + the off-diagonal a one-column tier cannot express + sever-the-fold-goes-RED. Table choice re-signed off the catalog after the builder's coverage finding (BEDROCK layer 3/4). Receipt: BUILDER-ITEM18-FOLD.md. Zach's redline stays open per-row on the 13 SMS quiets. **FEED THE REDLINE (auditor-v7 8/21 night, from the #289 Myli-parity walk): `ai_fact_captured` has belled Zach's phone 177× by SMS (every Myli fact-capture → an SMS; D4 actor-exclusion itself HOLDS 0/177). Desk recommendation for the morning redline: demote ai_fact_captured to bell-tier (no enterprise CRM pages a phone for an AI note) — but it's a config flip on the notify estate, so it waits for the redline session per the 0801 lesson (a flag is a recorded decision; change it with the record open).**
- **19.** ✅ **COMPLETE 8/21 (ruling + build + post-deploy walk, desk-verified)** — the bell column is OFF /settings/notifications per the desk ruling (the hl_notification row IS the delivery record; bell = the floor, per Zach's Myli spec): auditor-v6 walked the deployed build (deploy witnessed via deploy-status.sh, refused to walk stale) — 8 header cells / 8 per row measured from the CSS GRID itself (a word-count would have false-redded on the section copy's 41 "bell" mentions), zero render errors, the honest section copy verbatim, a REAL row save round-tripping `PUT /api/notification-preferences` 200 + server-verified after reload; bell deliberately retained in the PUT (route-read, not inferred from a truncated capture). ⚠ Declared side effect, desk-closed affirmatively: the auditor flipped the account-wide email master for 3.5s mid-walk (checkbox-index assumption on a DIV grid), reverted + server-verified; desk re-derived live: master `{sms:true,email:true}` AND **0 notifications minted in the exposure window** — nothing was owed, nothing suppressed. Receipts: `AUDITOR-V2.md` §AUDITOR-V6 + shot v6-item19-bell-off.png.
- **20.** ✅ **COMPLETE 8/21 (auditor-v7 flip walk, desk re-measured)** — the 🔔 writes the store it displays: flipping "Seller Inquired" OFF fired exactly ONE write (`PATCH /api/activity-catalog/seller_inquired {"notify_class":null}`), the CATALOG moved (`notify true→false`), the MAPPING did not (`hl_event_mapping.updated_at` byte-identical before/after — never written), reload persisted it (not an optimistic patch), restored + reload-proven + desk re-queried (`notify=t, class='seller_inquiry'`). The a012000622 wrong-store defect is NOT reproducible on this control. ⚠ Incident declared: the flip is ASYMMETRIC (OFF = one click; ON refuses by design — the class picker IS the on-switch, and OFF destroys `notify_class`, the value the restore needs) — v7's first run left the bell OFF ~2m50s before the picker-restore; exposure window measured 0 cards / 0 notifications owed. Any future bell walk must capture `notify_class` in its BEFORE read. ⚠ CORRECTED FINDING + a desk incident owned: v7 flagged `lead_reassigned` (`notify=true, notify_class=null`) as a fake-on bell off the UI comment's derived-notify invariant, and the desk normalized it OFF — **wrongly**: mig 0801 (desk-signed 19:08Z same day) had armed exactly that state DELIBERATELY (class-NULL → `notifyClassForKind` falls back to the KEY; same train removed the duplicate hand-call). The desk reverted within ~20 min (`notify=t` verified; exposure window measured 0 reassigns, nothing owed). Lessons banked: the AutomationTriggersBoard invariant comment is STALE vs the key-fallback (fix its text in item 21's lane); a catalog flag is a RECORDED DECISION — read the migration ledger before "normalizing" any value; v7's 10-cards→0-notifications evidence measured the PRE-weld era and proved nothing about the new road. `lead_reassigned` still renders in none of the 67 notify rows — the board-visibility gap is real and rides item 21 (#271). Receipts: `AUDITOR-V2.md` §ITEM 20 — v7 + 5 shots.
- **21.** ✅ **PROVEN LIVE 8/22 (bell-builder + harness)** — reassign-to-self refuses the bell, red-first'd against prod. The finding that matters: the cure (f30fe2ae77/97239cfc0f + mig 0801) was shipped but had NEVER EXECUTED ONCE (`lead_reassigned` bells all-time: 0) — a guard nobody planted; the lane built the missing harness proof, planted it red, proved it green live. Receipt: `BELL-BUILDER-21-22.md`.
- **22.** ✅ **PROVEN LIVE 8/22 (same lane)** — no bell routes to an AI seat: the structural exclusion (d117cc6db3) red-first'd + proven live; zero transport leaks in the session (0 assign/reassign notifications created; fixture restored). Receipt: `BELL-BUILDER-21-22.md`.
- **23.** ☐ Every kill toggle proven AT THE WIRE with a red proof (finish the F3 census). **AUDITED 8/23 (auditor-ticks — STAYS OPEN, and the census the box points at is not the one that answers it):** the F3 census is `.claude/reports/toggle-census-f3/CENSUS.md` (~84 controls: **18 FAKE · 8 DARK · 13 PARTIAL · ~45 REAL**, its own headline "only 9 of 86 toggles have a named covering test") — `builder-g4b/RECEIPT.md` §6 enumerates only THREE toggles and adds no new wire proofs, `captain-g4` §4b adds one bell-level red/green. Independent live enumeration found `hl_settings` carrying `ai_master`/`ai_comms`/`ai_clones`/`ai_conversations`/`ai_closed_loop`/`ai_behavioral_triggers`/`notification_channel_master` plus **18 `hl_ai_function` rows**, `hl_agent_clone_config.enabled` and `hl_leads.ai_mode_enabled` — **none of which appear in either receipt's census.** Genuinely wire-proven: `ai_comms` ×3 channels + fail-CLOSED on read error · `ai_master` · `ai_clones` at both gates · `ai_conversations.enabled` · suppressions · the intent/step ladder. **Remainder: five named kill switches have NO wire refusal proof — `ai_conversations.voice_enabled` (only the fold projection is asserted, a screen claim not a refusal) · `ai_closed_loop.enabled` (source-assertions only, its three real gates undriven) · `hl_agent_clone_config.enabled` (only column-exists) · `hl_leads.ai_mode_enabled` (DARK: its UI has zero imports while the column still drives `agent-lane.ts:27`) · "AI team on duty", which has no implementation at all (zero `on_duty` hits repo-wide).** Correction to the census: it recorded `ai_comms` ABSENT/unarmed; live it reads `{"text":true,"email":true,"voice":true}` — since seeded.
- **24.** ☐ Dead notify columns dropped w/ reversal dump (writerless, still scored by coverage.ts). **DESK STATE 8/21 night (found in the ledger, not redone): mig 0802 applied 19:19Z dropped 3 of 5 (`hl_activity_log.notified_at` · `hl_leads.notify_anchor_at` · `hl_activity_catalog.existing_notify`), reversal at `~/hl-reversals/notify-cols-24/`; `notify_owner` KEPT by name (the only record of a human classification, ONE_ANSWER_TABLE); the `hl_lead_source_events.notify`/`existing_notify` pair drops ONLY after #185's writer-removal has its DEPLOY WITNESS (the DDL-waits-for-its-reader's-deploy law) — that witness + the final pair drop are what's left of this box.** **AUDITED 8/23 (auditor-ticks — STAYS OPEN, and it is NOT ready to execute).** Live census confirms mig 0802: all three named columns are GONE; `hl_lead_source_events.notify` + `.existing_notify` both still present; the reversal is on disk (3 CSVs, stamped Aug 21 15:19). ⚠ Correction to the desk state above: the KEPT `notify_owner` is on **`hl_event_mapping`**, not `hl_leads`/`hl_activity_catalog` (neither has such a column — mig 0802's own header says so). **The deploy witness EXISTS:** #185's writer-removal is `3af48ba3fb` (removes `ConfigureTriggerEventModal.tsx`, the repo's only `existing_notify` writer, +3 files, 1,002 deletions), it is an ancestor of `origin/houseloop-app`, and the Railway deployments API returns `SUCCESS 2026-08-23T04:02:31Z commit e16994f372ff` — the migration ledger was deliberately NOT used as the witness (`checksum` is NULL on every row and 0823/0824 applied 56s after their commit, far faster than a Docker build, so ledger rows cannot distinguish boot-runner from desk psql). **Remainder: the blocker is not the writer but TWO LIVE READERS the law's own wording points at — `lib/triggers/automation-triggers.ts:227` selects `e.notify FROM hl_lead_source_events` (feeding `mirrorNotify` at :440, and `4a07eb67df` made that catch LOUD, so a drop visibly blanks the Automation Triggers board) and `lib/notifications/coverage.ts:100` selects on `WHERE notify = TRUE`. Those two must be cut and DEPLOYED before the pair drop; `existing_notify` alone is genuinely readerless.**
- **25.** ☐ App-push (4th channel) designed + DDL signed (HOLD_ drafts exist). **AUDITED 8/23 (auditor-ticks — STAYS OPEN; the box is self-refuting as written, `HOLD_` IS the unsigned state).** The design is genuinely desk-grade, not a sketch: `.claude/reports/notify-config/APP-PUSH-DESIGN.md` (162 lines) names the dispatch seam beside the existing two (`dispatch.ts` ~:642 SMS / ~:743 email, same `master.<ch> && pref.<ch>` shape), refuses to reuse `smsAllowedForClass` with an argued reason, specifies the 404/410 pruning trap, and states its own status as "nothing built". The DDL FITS the live shape — verified live: `hl_notification_rule` (147 rows) is column-per-channel (`bell`/`sms`/`email` booleans) with its only CHECK on `lead_scope`, so **there is no channel enum to widen** and `app boolean NOT NULL DEFAULT false` slots in as a fourth sibling; `hl_push_subscription` does not exist (`to_regclass` NULL); both drafts are `HOLD_`-prefixed, which the boot runner's `^\d+_` pattern structurally cannot pick up, so they cannot self-apply. **Remainder: nothing is signed and nothing is built — zero hits for `vapid|web-push|pushManager` across the app, no `web-push` dep, no service worker, no VAPID keys in `.env` (the only live artifact is the honest `APP (coming)` disabled cell at `settings/notifications/page.tsx:959`) — and by the drafts' OWN binding condition the `app` column may not land without its dispatch reader in the same change, or it becomes exactly the fake toggle (`hl_workflows.notify`, 0/75 on, zero readers) the notification page was built to end.**
- **200.** ✅ **COMPLETE 8/22 01:17Z — RUN WITNESSED per the law** (Inngest run 01M0KGQGMX9FBM3AAX7J034X1C, status success, Run Log row, on deployed build 7404d19e, fired through the REAL writer): 15 facts on Conner Kelley-TEST → EXACTLY ONE debounced run → ONE notification **"Myli updated her working memory on Conner Kelley-TEST — 15 new facts"** (desk re-queried both rows). The re-read assertion proves the burst is read from the log, not the debounced payload (factsTotal=15, not 1). Per-fact bell dead by VOCABULARY (catalog notify→false, mig w/ ruling quoted); ai_context_built retired to feed-card-only; ai_facts carries the working-memory copy + sms/email opt-in eligibility (bell default). The design detail that made it cheap: 97.6% of fact gaps are `<10s` (machine bursts), so 1,080 facts → ~105 notifications and the window constant doesn't matter. First-fixture abort honesty on the record (a junk-conversation fixture wrote 0 facts and the harness REFUSED rather than filing a false NO-RUN). **UPGRADED 8/22 per Zach's overnight ruling ("the bell rings when myli FINISHES — not a clock; we need a new mechanic"): the debounce is DEAD, replaced by a COMPLETION EVENT the writer emits when the build finishes, carrying a DB-clock watermark so the digest reads THIS build's facts as an exact set. Live-fire on prod: run 01M0MM57MF5FM0R0P4HBN55B9D (desk re-verified), 9 facts → ONE run → ONE card, fired-to-rung in TWENTY-THREE SECONDS (the debounce would have idled 2 minutes). Copy per his refinement: per-fact cards keep "captured a fact"; the completion reads "updated her working memory"; "dossier" swept to zero on every user-facing surface.** Receipts: BUILDER-277B receipts + the digest suite 25/0. **THE MYLI FACT DIGEST (Zach-signed 8/21 night, verbatim anchors: "i need myli to run a timer or something and capture all of her atomic facts then give me one dossier [notification] … myli updated john smith dossier, John is now looking in overland park and is going to be purchasing the home for his daughter … in vertical order like bullet points, the fact family — purchasing for: daughter, financing: cash … just one notification … i dont want atomic facts as an individual notification, that goes for the bell as well … it should always go through the bell but i should have the ability in the notification settings to turn it on for text or email as well and also the in-app message that is saved on my homescreen").** THE SPEC: ① facts still CARD per-fact on the feed (the record is untouched); the NOTIFICATION becomes a per-lead DIGEST — a timer/debounce window batches a burst of Myli's captured facts into ONE notification. **COPY (Zach 8/21, verbatim: "instead of dossier, i like the word working memory — myli updated her working memory with blah blah blah, new facts captured, then a bulleted list"):** headline **"Myli updated her working memory"** on `<lead>`, body = the one-line narrative ("John is now looking in Overland Park and purchasing for his daughter") + **"New facts captured:"** + the bullets `"<fact family>: <value>"` in capture order. "Working memory" is the product word for Myli's per-lead knowledge everywhere this surface speaks — never "dossier" in user-facing copy. ② Default channel BELL always; per-seat settings expose SMS/email opt-in for the digest class + the homescreen in-app surface. ③ The per-fact bell dies ONLY in the same train the digest ships (copy never ships ahead of behavior — killing per-fact first = silence). ④ Build shape: REUSE the saved-search digest precedent (the deterministic Inngest digest — highest-volume consumer email); a new notify class for the digest goes through the desk's schema gate; D4 evaluated at the one point; actor exclusion as ever. ⑤ Current state measured 8/21: ai_facts seat rules already bell-only (sms=f email=f both seats — the 177 SMS were historical); the digest replaces the per-fact BELL flood. Builder lane after the payload cell.**
- **201.** 🔨 **THE MINT LAW + THE SEVEN'S RECOVERY (Zach's 8/22 morning walk — the whole arc, receipts in BUILDER-277B*/recovery-lane reports):** HIS RULING (verbatim anchors): leads mint ONLY on a qualifying action (in-app / text / tour-property request) fresher than the gate, since July 15; trash means trash. SHIPPED: the three-prong mint gate (02c4db1d88, proven live — Cao Yiming's re-mint REFUSED on prod, 230→230; supersedes the 8/14 "trash is not a blocklist" arm, supersession recorded in-file; the human-pull-in consent bypass + tombstone-mints rulings preserved with assertions). CLEANED: 10 unqualified overnight mints trashed (dumped, ~/hl-reversals/stale-mints-0822/; Rene Purtee HELD — PCSOI estate). THE SEVEN (James Wisner · Jessica Moore · Lamiya Abdulkarim · Taylor Davis · Ashlynn · Cindy Jackson · James Ross): cause CLOSED — the 7/08 pre-tombstone test-lead cleanup swept 6 by an except-keepers list; the tombstone hole closed 8/6; the 8/18 re-import restored MORE than was lost (all 15 July tours present, rendered, named; both duplicate + blank-card findings RETRACTED under proper instruments; only possible loss = 2 weeks of native activity, measured ≈zero). CLOSED 8/22 (recovery-lane receipt: `.claude/reports/recovery-seven/RECEIPT.md`): the restamp APPLIED ONCE + verified (true 6/22-7/05 dates live, provenance meta on each card) · James Ross answered (the Zach-approved 7/10 CleanSlate demo; recovery verified 10/10 against its own dump) · deleter pinned NOT ARMED (hand-run lanes; the one raw path gets its tombstone belt in the builder lane) · the resurrection loop KILLED at the bypass (trashed-as-existing lookup) + the 19-arm mint gauntlet deployed · three findings honestly WITHDRAWN (blank cards / duplicate class / the 24,200 actor gap — all one instrument error: asserting raw columns where a resolver computes; the 23.7k drain stands as a plated, reversible consistency pass) · REMAINING from the arc: the 17-card chrome pass + exclusions (Zach's permission pending) · the via-clause build · fub.notes actor provenance (needs per-row work, correctly left). **PLUS SHIPPED 8/22 ~12:35Z — PORTAL TRUTH (a0a19f8ded, D2 made render-real): cards claim a portal only on EVIDENCE — event meta → the lead's import-time source fold (Zach's locked 7/04 six-bucket map, its 4th consumer; PORTAL_BUCKETS={Zillow, Realtor.com} beside the fold) → honest NO CLAIM + the carrier fact. Census: 66,154 keep the Zillow mark (now evidenced, was a 97.2% fallback guess), 2,119 unprovable go honest, +1ms measured on the hot route. The near-miss on the record: the desk's "one-line flip" order would have blanked 66,842 cards (no render-time data path for the source layer) — the lane REFUSED ON EVIDENCE, threaded the lead's already-reconciled bucket, census-gated, then flipped. Open: the Google-bucket product call (claims nothing until Zach rules) · the 417 pipe-as-portal DATA defect (own item).** **AUDITED 8/23 (auditor-ticks — 🔨 HOLDS; only remainder (iii) blocks, and the board sentence for (i) and (ii) is STALE and should be rewritten rather than ticked as written).** **(i) 17-card chrome pass — effectively CLOSED, and BOTH clauses of "permission pending" are false:** permission was GRANTED 2026-08-22T12:50:55Z (Zach verbatim: *"Yes to the 4 write approvals, whatever you need to do to cleanup"*) and the pass EXECUTED ~12:58Z — 9 strips via `amendActivityBody` + 8 cards deleted by id (of the 17 in `~/hl-reversals/chrome-cards-0822/cards-before.json`, exactly 9 survive / 8 gone) plus 71 tape exclusions, then superseded by a 282-card sweep (THEFT 50 / POLLUTION 232, 18 victims) → 282→0. **builder-mask does NOT cover it — different set, different operation:** `.claude/reports/inapp-header-17/RECEIPT.md` corrected 5 rows / 1 already-correct / 11 refused, changing only `kind`+`actor`, stripping no body and deleting no card; by `source_id` **8 of the chrome 17 overlap and 9 are untouched** (Timothy Wilkinson ×6, Kwizera, Zairanett, Monica Zinn). ⚠ **Flag for the desk: mask's rung-② stamped `Raul Lene` as actor on the Timothy Wilkinson STOLEN cards — it made stolen cards look MORE authentic, and those are the chrome DELETE set.** Residue: 16 panel notices parked as a defect, and the 282→0 figure lives only in `lee-handoff.md:79-80` with no `reports/` receipt dir — the one number not independently re-readable. **(ii) The via-clause is BUILT and shipped 8/22** — `Lead created in HouseLoop via the {integration} integration` (`registration-labels.ts:309` via `arrivalCard()` :355/:363), eyebrow `Registered via {phrase} on {portal}` :243 with the no-via-over-false-via fallback :230 gated on `road === 'live_signal'`, wired at `render-activity.ts:1762/:1784`, painted at `ActivityHeroCard.tsx:350-364`, asserted in `arrival-grammar.test.ts` + `render-activity.card.test.ts:216`; commits `d5d350a90c` / `9947b26c83`, pushed. (The board line was written at `a6dad1127a` 08:19 — **1h13m before the code landed.** Stale comment to fix: `origin-and-carrier.ts:33-35` still says "NOT YET WIRED INTO THE RENDER".) **(iii) ⛔ THE ONE THAT BLOCKS — fub.notes actor provenance, now with a number: of 154,323 tape rows every one carries a `user_name`, but only 102,967 (66.7%) resolve to a PERSON; the remaining 51,356 (33.3%) are not people — 47,336 system labels (`Follow Up Boss` 30,702 · `Imports User` 16,634) and 3,891 playbook/automation labels. Separately 12,372 rows have a NULL `user_id`, of which 129 name a real human with no id to resolve against (126 `Zach Spradling`, 3 `Ben Froeschl`) — that 129 is the genuinely per-row slice.**
- **199.** ☐ **⭐⭐ THE CAMP DAVID ONE-TRUTH REBUILD (Zach-ordered 8/21 night, verbatim: "there should be one goddamn runlog of camp david and one ssot of truth that anybody fucking reads … there should not be 50 legacy fucking ways that somebody can read something, everything needs to be fucking quarantined and fucking new fucking process that is robust as shit and simple as hell").** The measured disease that earned it, all in ONE night: the scraper SSOT contradicts itself on whether fub-webhook-scraper should run (roster: ✅ up · another section: "correctly stopped") · the health tool reads GREEN over a 37h-dark arm because a replacement road (sync-lead-record.ts, 106k Railway-only rows since 7/20) keeps sibling tapes warm · the arrival-map registry names a road that hasn't been the live one for a month · the heartbeat the SSOT calls "unconditional every 30s" did NOT move while the worker was provably alive and processing (measured 8/21 23:0x) · the consumer enqueues rescrapes for a claimant retired by order. THE WORK (composes FUB-14 slice-3 + item 32's sentry model): ① ONE run log for every Camp David process (each writes its own liveness column — no supervisor testimony) ② ONE fact-measured roster in ONE doc; every other roster/section/tool claim becomes a pointer or dies ③ quarantine the legacy readers (the dead heartbeat sentries, the deprecated queues, the stale MCP claims) ④ the per-family tape-silence + expected-writer sentries (auditor-v7's adopted spec) ⑤ simple > clever: the robust rebuild is slice-3's one-tape architecture, not another patch. INTERIM 8/21: fub-webhook-scraper + fub-home-refresh resurrected + pm2 save'd (worker verified by its OWN LOG, not the lying heartbeat); saved-search tape movement pending verification.** **AUDITED 8/23 (auditor-ticks — STAYS OPEN; ③⑤ advanced, ①②④ fail on live measurement).** **① FAIL — there is no ONE run log.** `hl_run_log` carries only the Railway arms (`fub-inapp-detector`, `fub-canary-webhook`, `fub-inapp-poll`, `system-health-scan`); the Camp David pm2 daemons write separate per-daemon tables, and by `captain-manual.mdx`'s OWN roster **two of five have no liveness column at all** — `fub-body-recovery` = "its queue drains; job rows move" and `fub-chrome-guard` = "curl localhost:18800", i.e. inference, the exact supervisor testimony ① bans. **And the fossil is confirmed and NOT quarantined:** CAPTURE `fub.webhook_scraper_heartbeat.last_beat` = 2026-08-21 20:19 vs the TAPE copy beating 47s ago — ~28h dead, same table name, two stores; `QUARANTINE.md` §2 declares a DIFFERENT table fossil (`fub.scraper_heartbeat`) and `master-scraper-ssot.mdx:328` still teaches the capture copy as truth. **② FAIL, and it went backwards:** the self-contradiction is live and un-struck (`master-scraper-ssot.mdx:154` "fub-webhook-scraper ✅ up" vs `:328` "its proc is correctly STOPPED", the second sitting inside the "READ THIS FIRST / these signals LIE" block) — and instead of the other rosters becoming pointers, a THIRD was born (`captain-manual.mdx`, 8/22, authoritative for "processes, queues, roads" while chapter 8 "remains the law" for the rest). **Rosters went 2 → 3.** **③ PARTIAL** — real quarantine work landed with reversals (relay stopped, the 27-file `/Users/lee` graveyard, `fub-webhook-sink` roster entry deleted), but the capture fossil is still readable-as-truth and `tools/fub-scraper/scraper-health.mjs:71` still reads `FUB_RAILWAY_DATABASE_URL` — a SECOND env name for the same tape host, outside the two-name `FUB_STORES` registry. **④ FAIL — armed on 2 of 5 live families:** writers are declared only on `chrome_scrape` (text_messages, saved_searches, zillow_home_activity) and `webhook` (text_messages); **`native_gmail`, `note_parse` and `tag_parse` declare NONE — and the probe cannot say so**, because `writerLiveness` returns `unknown` only when the list is EMPTY, so with 4 rules present it reports green *"all 4 declared writers beating"* and never names the three unarmed families (a want-list satisfied by its easiest members). **⑤ OK** — relay quarantined, cargo on per-person `syncPersonTableToMirror`; that is slice-3's one-tape shape. **Remainder: ①②④ — still no single run log, the SSOT still contradicts itself while a third roster was added, and the writer sentry reports green over three families it never armed.**
- **198.** ☐ **THE 800-LINE DELIVERABILITY HARDENING (filed 8/21 night from a live incident):** Zach's iPhone junk-filtered 913-800-4470 ON ITS OWN — the thread vanished to the Junk folder, every notification silenced while Twilio read `delivered` (measured: 25/25 delivered that day incl. his Zillow-walk tour + both in-app messages + a desk test he never saw; he recovered the thread himself). The false trail this burned: "zero notifications" read as a dispatch defect and "the thread got deleted" read as an intrusion — both were the filter. THE WORK: ① ~~verify A2P registration~~ **DESK-MEASURED 8/22 00:45Z (direct Twilio API, sheet creds): brand APPROVED + identity VERIFIED · campaign VERIFIED (MIXED, zero errors) · +19138004470 IS attached to the registered A2P service (with 11 sibling lines). The registration was NEVER the gap — iOS junk-filtered a fully-registered line on content pattern alone.** Remaining: fix the a2p MCP server's stale creds (401s while direct curl works) ② reduce the junk-signature of notification bodies (emoji+link density) where cheap ③ standing advice recorded: the notification number is saved as a CONTACT on team phones ("HouseLoop Alerts") — iOS never filters known senders. Lesson banked for every future "no notification" report: check Twilio delivery status FIRST — `delivered` + unseen = device-side filtering, not our pipeline. **AUDITED 8/23 (auditor-ticks — STAYS OPEN; ① and ③ are genuinely closed, ② was never built).** **(i) The a2p MCP 401 is CONFIRMED — but the diagnosis in this box is wrong and would have sent someone to rotate a healthy credential.** `mcp__twilio__twilio_a2p_brand_status` returns **401 code 20003 "invalid username"**, while the SAME `.env` `TWILIO_ZACH_*` creds returned **200** on both `/Accounts/{sid}.json` and `/v1/a2p/BrandRegistrations` (brand APPROVED, identity VERIFIED) by direct curl this session. **The creds are not stale — the wiring is:** `tools/mcp/twilio-server.mjs:36-37` reads `process.env.TWILIO_ZACH_ACCOUNT_SID` and **never loads `.env`**, while `.mcp.json:193-196` passes `${TWILIO_ZACH_ACCOUNT_SID}` from a shell env where it is UNSET — an empty SID is precisely error 20003. Noted, not fixed. **(ii) ⛔ Remainder: the body de-junking rule does not exist — repo-wide grep for `normal-bodies|junk-signature|de-junk` hits TWO files, both board text (this page's order and the week-2 checklist); zero code, zero commit, zero report, and `lib/notifications/` carries no emoji or link rule.** The 14-day corpus on +19138004470 does show density falling (emoji **0 every single day**; links 3-5/day through 8/14 → **0** from 8/17), but that drop is dated **8/15–8/17, BEFORE the 8/21-night incident that ordered ②**, and the low-density rows are gauntlet strings ("burn 1", "play message 3") rather than shipped copy — with **zero sends on 8/22–8/23**, no post-change production corpus exists to measure at all.

## Floor 33 — FUB forward history flows in

- **26.** ✅ **RE-PROVEN LIVE 8/21 night (auditor-v7, real Zillow test-buyer send)** — in-app message → tape 19s → card 5m15s end-to-end, right actor (`by_name='Boss Test Lead 205'`), right lead, RIGHT HOUSE on the card (`meta.house.address` = the listing composed on), bell 39ms after the card, notify per catalog. The filing beat is a measured CONSTANT (~296s, identical to the second across 3 independent samples) not a lag. Zach's burying concern does NOT reproduce — his 29s-apart clustered messages both carded + both belled (5/5 in-app rows today); FUB's bell showing only the newest thread item is a FUB-UI display property, not a capture defect. All 6 bells on the lead read `delivered` in OUR table — corroborating the junk-filter cause. 14/14 walk cards render with catalog grammar, zero raw sentences. Receipt: `AUDITOR-V2.md` §ZILLOW VERIFY + shots.
- **27.** ☐ Views / saved searches / insights → cards (browsing path verified 8/21 — auditor tick). **SPLIT VERDICT 8/21 night (auditor-v7): views ✅ (5.8s road) + saved home ✅ render with house+grammar — but ⛔ THE SAVED-SEARCH ARM IS DARK: Zach's live saved search NEVER reached the tape, the arm's newest capture is 37+ hours old, and NO SENTRY covers it (the quiet-arm-no-owner shape — its siblings home-activity/timeline are live). Box stays open on the saved-search capture fix + a sentry on that arm (rides item 32's sentry model). **AUTOPSY COMPLETE 8/21 ~23:00 (auditor-v7, read-only): saved searches are a BYPRODUCT of the person-timeline scrape (middle lane; FUB killed its webhook — scrape is the sole carrier BY DESIGN; no chevron issue, the train18 "chevron" was the FUB brand logo). BOTH claimant processes died 8/19-20: fub-webhook-scraper STOPPED deliberately (↺0), fub-parallel-scraper unregistered from pm2 — while the CRM's sync-lead-record.ts road silently replaced the visible tapes (106,010 Railway-only rows since 7/20), masking the outage; the health tool reads GREEN off the replacement road's freshness with `saved_searches 0/2h` in its own breakdown (the fooled-instrument shape). FIX RULED: resurrect the middle-lane claimant (the twice-hardened reader — fresh-page + read-during-scroll — stays the carrier); restart-or-hold is ZACH-GATED on the unexplained deliberate stop. SENTRY SPEC (auditor's, adopted): per-family TAPE SILENCE primary + expected-WRITER assertion secondary, thresholds as data on hl_signal_channel. SEPARATE FINDING: the arrival-map registry is WRONG about zillow_home_activity (says chrome_scrape; really the CRM road since 7/20) — the FUB book generator faithfully reproduces the stale registry; registry correction rides FUB-14 slice-3, not a midnight edit.** ALSO FILED from the same pass: 195 all-time (19/30d) in-app messages on 9 named real leads captured by the 8/16 dom_only bulk sweep but NEVER FILED — the live timeline_api arm files 100%; the residue needs an idempotent file-the-backlog pass through the real writers (Inngest).** **AUDITED 8/23 (auditor-ticks) — BOTH NAMED TICK CONDITIONS ARE NOW MET; the box stays open on the backlog rider alone.** ① **The saved-search arm is ALIVE on BOTH stores, verified live:** capture (Camp David `bossloop`) 7,331 rows, newest `scraped_at` 19 SECONDS old at read; tape (Railway `yamabiko`) 7,372 rows, newest 19s old; **1,180 / 1,188 rows captured in the last 24h.** Zach's 8/21-era search is on the tape (id 44411, person 50557, `created_date` 2026-08-21) — the arm that was 37h dark is carrying again. ② **A SENTRY NOW COVERS IT, as data, exactly to the adopted spec:** `hl_signal_channel.chrome_scrape.thresholds` carries `roads.saved_searches {yellowH:6, redH:12}` (tape-silence primary) AND a `writers.saved_searches` expected-WRITER assertion (`fub-webhook-scraper`, `livenessRef fub.webhook_scraper_heartbeat`, red at 120min) — with the outage written into the row's own note; `probes.ts:440` consumes it via `FUB_SCRAPE_ROADS` and `writerLiveness()` reads the declared writers. ⚠ Carried to item 199: the `fub.webhook_scraper_heartbeat` on the CAPTURE store is a **FOSSIL** (newest beat 8/21 20:19, ~28h dead) while the TAPE copy beats every 47s — same table name, two stores, and the fossil is still readable as truth. **Remainder — NOTHING has run the backlog file-pass, and it now carries a number instead of a phrase: on the leads that actually exist today, 37 in-app tape rows across 13 named leads (Ashlynn 7 · Julie Ferguson 5 · Nickolas Loring 4 · Marco McCormack 4 · Jo Davis 4 · Timothy R Wilkinson 3 · Lamiya Abdulkarim 2 · Juno Reyes 2 · Sachind Jamesh 2 · Monica Zinn · Alberto Lopez · Jessica Moore · Marion Minor, spanning 2022-12-17 → 2026-07-15) have NO card, and ZERO of the 37 appear in `hl_tape_exclusion` — so none of them is a deliberate omission.** (The 195 all-time figure counts people who were never minted; 37 is the residue on the 216-lead live estate.)
- **28.** ☐ Emails + texts → cards, named actors, no twins (337 historical twins deleted w/ dump under the delegation). **DESK-VERIFIED 8/21 night: the twin class is EMPTY live — 0 pairs at ±2s across all of hl_activity_log (two independent probes incl. the design's own example pair, now a single NAMED card). The history: pairs re-keyed to the `fub.text_messages` canonical 8/11 (cascade-key-migration.csv), final victims DELETED 8/21 06:54 w/ full dump — reversals at `~/hl-reversals/twins-337/`. Remaining for ✅: the auditor's walk proving emails + texts land as cards with NAMED actors (the no-twins half is measured done).** **AUDITED 8/23 (auditor-ticks — STAYS OPEN; the WALK half is now DONE, and the NO-TWINS half is OVERTURNED).** ① **The walk is satisfied both ways.** TEXTS: `walker-fub/RECEIPT.md` §3/§4 extracted every rendered actor sentence per lead — Cheryl Brown 29 cards all naming Cheryl ↔ her own agent with matching numbers; Monica Zinn's card reads *"Zach Spradling sent a Zillow in-app message to Monica Zinn"* with the Zillow venue mark (📎 `shots/s3-monica-zach-card.png`); Jake Smith's in-app card verified against its row (📎 `shots/s4-jake-inapp-cards.png`). EMAIL: the live-fire is `.claude/reports/fub8-inapp-custody/RECEIPT.md` (not `email-livefire/`, which is a stranger walk that files 0 cards by design) — real send 21:17:53Z → captured → lead resolved → card `c8ff689a…` `email_received`, `by_name='Conner Kelley-TEST'` → bell, **6.0s end to end**, and the card **rendered on prod with the RESOLVER's sentence read off the live DOM** (📎 `shots/L5-email-card-rendered.png`) — the correct assertion target. ② ⛔ **THE NO-TWINS CLAIM IS FALSE AS WRITTEN — see item 33 for the proof: 937 twin groups / 1,013 surplus cards / 45 of 216 leads, on the strict definition (same lead + same body + same `occurred_at` second, distinct source_id).** Kristen Hansan's 106 messages each render twice because her tape holds two generations (FUB-real + synthetic ≥900.1M, 106/106 bodies shared) and the feed's dedup key is `source_table:source_id`, which differs per generation. Twin pair delta is **0.000000s** — a ±2s probe should have caught it, so the probe that returned "0 pairs" was keyed on a predicate a cross-generation pair cannot trip. ③ **A second, smaller actor gap stands: 8 live `email_received` cards (8/9–8/15, real inbound mail) carry `by_kind` NULL, `actor_type` NULL and no `by_lead_id`/`by_user_id` — nothing can name them at render.** **Remainder: the twin re-measurement + the cure (one fact, one card, across BOTH generations), and the 8 anchorless email cards.**
- **29.** ✅ Tag road LIVE 8/23 04:01Z (#266): peopleTagsCreated → fub/tags.created → fubTagSync → cards through the one writer; desk-fired proof arrival processed → run success → 2 cards (by lead, true timestamps) → 2 bell rows; SMS leg correctly held by the lifeline policy. Receipt: .claude/reports/builder-tags/RECEIPT.md
- **30.** ✅ 403 LOUD 8/23 (#267): the revocation branch was structurally unreachable (express Response shadowed the global — a method compared to a number); fixed, reads the response, names the revocation + lead, alerts, never retries; 15/15 red-proven. Same receipt.
- **31.** ☐ Mirror self-loop repointed (#206) + relay landmine defused (#216). **AUDITED 8/23 (auditor-ticks — SPLIT: one claim holds, one does not, so the box stays open).** **#206 ✅ HOLDS in the source:** the two ends resolve through `resolveStoreUrl` to different named stores — READ end `fub-webhook-scraper.mjs:75` + `fub-home-activity-refresh.mjs:39` → `capture` = `bossloop @ 192.168.1.211`; WRITE end `mirrorPool()` → `tape` = `railway @ yamabiko` — and both daemons now BOOT-ASSERT identity via `pg_control_system().system_identifier` (`fub-webhook-scraper.mjs:595`, `fub-home-activity-refresh.mjs:122`), so the name-only ambiguity that caused #206 can no longer pass silently. (Honest boundary: proven for the REPO source; the pm2 roster runs `/Users/lee/*.mjs` copies on the box. Stale prose: `fub-scraper-lib.mjs:2150`'s param is still named `cdPool` and its comment still describes the un-repointed world.) **#216 ⛔ DOES NOT HOLD — quarantined is not defused. Remainder: the box still runs the unguarded 2026-07-09 `fub-relay.mjs` — `ecosystem.config.cjs:131-135` says so verbatim ("the box is still running the 2026-07-09 copy, which is why nothing has failed yet") and the file is absent from the deploy FILES list, so the repo's cure (`fub-relay.mjs:49` requiring `FUB_RELAY_SOURCE_URL` with no fallback, `:221-228` exiting on a source==target match) has never shipped; QUARANTINE.md §1 documents the reversal as `pm2 start fub-relay`, and pm2 restores the env captured at first registration — so the self-sync landmine RE-ARMS on exactly the one command the record tells an operator to run.** (`deploy.sh:303-311`'s `--only $PROCS` closes the deploy path, not the manual one.)
- **32.** ✅ **RECONCILED + CURED 8/23 (desk)**: both measured remainders died the same day. (1) The "no page ever left the building" premise was the AUDITOR'S INSTRUMENT — `alerted` is a top-level payload key, not under result; the pager has paged **30 times** (builder-pager receipt, two independent witnesses). (2) The latch gap is CURED: the cooling re-page shipped AND WAS ARMED by Zach's sign (15 probes @ 24h; a still-red ticket re-pages daily with the CURRENT detail naming the dead road), notified_at now stamps only who was actually told, and the fub_stage_mirror fossil closed. Held as signed design: tickets latch per probe_key (a signal needing its own page gets its own PROBE — the estate's thrice-used pattern); failed_inngest stays cooling-excluded until its predicate can decay. Original audit findings preserved below — A road-death **is inside** the pager predicate: `FUB_SCRAPE_ROADS` (`probes.ts:439`) is looped at `:964` inside `probeFubFeedFreshness`, each road measured against its OWN `hl_signal_channel` window (`resolveHourRule(channels, f.channel, ['roads', f.table])`) with the probe taking the worst verdict — so `saved_searches` going dark reddens `fub_feed_freshness`. The cadence is real and registered: `systemHealthScan`, Inngest `id: 'system-health-scan'`, cron `*/10 * * * *`, **1,008 run-log rows in 7 days, latest 8/23 04:10Z**; a ticket opens via `INSERT INTO hl_system_health_issue … ON CONFLICT DO NOTHING` keyed on probe_key; the page is `alertZach()` → `deliver({channel:'sms', log:false})` from the systems-health number — a deterministic SYSTEM alert, never the AI gate. **Remainder — two measured gaps: (1) no page has ever demonstrably left the building — `alerted:true` appears in 0 of 6,501 recorded scans over 60 days, and the last mile is proven only by `lib/health/pager-road.test.ts`, which is honest about being an INJECTED alert sink ("never a delivery"); (2) all nine roads share ONE ticket key and the pager is a LATCH (fires only on a newly-opened ticket), so while `fub_feed_freshness` holds an open ticket for any cause a road death pages nobody — `failed_inngest` has held an open ticket since 2026-07-06. `fub_feed_freshness` is not latched today, so a road death would page — that is luck of timing, not a property.**

## Floor 34 — FUB past history pulls in correct

- **33.** ☐ Pull 3 named legacy leads fresh: tape rows == cards, 10-card spot-check vs FUB's screen. **WALKED 8/23 00:37–00:48Z (auditor-ticks, own receipt `.claude/reports/auditor-ticks/item33/`) — STAYS OPEN: CAPTURE PASSES, THE CARD LAYER DOES NOT.** Three named legacy leads (**Kristen Hansan** fub 44536 · **Msale Olongo** 45774 · **Xochil .** 40643) re-scraped fresh through the desk road — `fub.rehydration_queue`, `trigger_event='auditor-ticks-item33'`, priority high, ids 269891-269893 (dumped: `queue-before.csv` / `queue-inserts.csv` / `queue-after.csv`). **The road works:** claimed in 38s, all three `done` in 160s/264s/330s, zero errors. ① **CAPTURE IS LOSSLESS — proven against FUB's OWN API, not a receipt:** every text FUB reports exists on the tape, **`FUB_not_tape = 0` on all three** (FUB 107/148/249 vs tape 216/239/253); every extra tape row is the synthetic scraped generation (ids ≥ 900.1M — 109/91/4, **zero** in the real-id range); and the fresh rescrape wrote `texts_synced=0` across all three, which is the CORRECT answer when nothing is missing. ② ⛔ **BUT `tape rows == cards` FAILS on 2 of 3:** Kristen 216 tape → 216 cards (exact), **Msale 239 → 236, Xochil 253 → 124.** Zero phantom cards anywhere (`card_without_tape = 0` on all three). Xochil's **129 cardless rows are all inbound SMS from her OWN primary number** (9132634669 — 11 texts from the same number DID file), **126 distinct real message bodies**, spanning 2024-11-11 → 2025-10-14 **interleaved with** the filed ones (so no backfill horizon explains it), **none in `hl_tape_exclusion`**, and **129/129 confirmed present in FUB's own API** — they are real messages FUB still holds and we render nothing for. ③ ⛔⛔ **THE SPOT-CHECK OVERTURNS A DESK CLAIM — THE TWIN CLASS IS NOT EMPTY.** Kristen's tape carries TWO generations of the same conversation (107 FUB-real + 109 synthetic, **106 of 106 bodies shared**), both generations were filed, and the feed **cannot** collapse them: the read path's dedup key is `DISTINCT ON (COALESCE(a.source_table||':'||a.source_id, a.id::text))` (`app/api/leads/[id]/activity/route.ts:471`) and the twins carry **different source_ids** (e.g. `187677` and `944536104`, same lead, same body, **delta 0.000000s**), so DISTINCT ON keeps both. Estate-wide on the strict definition (same lead + same body + same `occurred_at` second, distinct source_id): **937 twin groups · 1,013 surplus cards · 45 of 216 leads.** `archived_from` is a PROVENANCE label (`'follow_up_boss'`), not a hide flag — both twins carry it. **This contradicts item 28's "the twin class is EMPTY live — 0 pairs at ±2s": a ±2s probe should have seen a 0.000000s pair, so the probe was keyed on something a cross-generation pair cannot trip (a same-`source_id` predicate can never duplicate by construction) — a detector that is not a superset of what it guards.** Honest boundary: this is measured against the production DEDUP KEY read out of the live query, not a browser walk — the mechanism is asserted, the pixels are not.
- **34.** ✅ **DONE 8/21 (builder-277b run 01M0K6301YPZ1ZQDKWB7C0REPS, success 22:12:43Z) — board state was STALE; independently re-verified 8/23 (builder-classify receipt): emails lane named **74,268** rows (the recorded 74,286 was a transposition of its own prediction — three AFTER cells drifted; tick against the live 74,268, 18-row gap unexplained), 5/5 non-circular spot-check vs the tape, quarantine clean 0/0/0, both guards armed (guard 2 covers 3,453/3,453 on-mirror stolen ids — the full-superset scope is what holds; a then-scoped guard would now leak 326), texts lane proven held both ways. FLAGGED open: the 3,453-vs-3,127 mirror delta (likely a staged∩mirror measurement artifact — needs a relay-cursor read; contained by guard 2 either way) + the prod walk of one named card (riding auditor-ticks).** — original state: Actor naming at import ships (78,105 rows get real names — #245 built; ship + staging re-run + ledger restamp in that order). **DESK STATE 8/21 night (builder map `BUILDER-277B-ITEM34-MAP.md`): #245 IS shipped+deployed (git-verified) — the receipt's "NOT SHIPPED" was stale; the real missing step is the CLASSIFY RE-RUN (staging carries zero #245 verdicts). ⛔ NEW P1 FOUND: the 8/10 staging hold's containment premise is FALSE — all 3,127 remaining stolen text ids are already IN staging and 4 reached the ledger (quarantine ordered); the hold was prose in the wrong file with no code guard. DESK RULING: quarantine the 4 → hold becomes CODE (classify preflight refuses the texts lane; merge-import structurally excludes the stolen set) → emails lane LIFTED (74,286 of the target; contamination is texts-only) and runs classify→dump→restamp→verify via Inngest with the run witnessed; fub.text_messages lane STAYS HELD until the remaining 3,127 leave the mirror by the same granted process as the 10,712 (routing owed).** **⛔ THE OWED NAMED-ACTOR WALK RAN 8/23 (auditor-ticks, own headed Chrome on prod, sha-free read-only) AND IT FAILED — THE RESTAMP IS REAL IN THE LEDGER AND INVISIBLE ON THE EMAILS TAB.** The 8/21 train is findable as data: 87 email cards carry `meta.actor_restamp = {reason:"item34 #245 actor archaeology … fub-staging-classify run 01M0K6301YPZ1ZQDKWB7C0REPS", ordered_by:"lee-desk:item34-stage-c:2026-08-21"}`, each now `by_kind='user'` · `actor_type='human'` · a real `by_name` — actors **Carrie Markway 40 · Lana Mount 15 · Jeremy David 14 · Jorge Blanco 10 · Donna Mason 8** (⚠ NOT the spot-check five the desk named — Kathy Shen / Dean Mauldin / Tevin Jacutin / Priscilla Ssemambo were restamped by **lane-203 on 8/20** and the r1-restamp lane, a different operation). **Walked on prod, two independent leads, two independent actors, same result: the card renders "HouseLoop sent an email".** Marcus Rodriguez (`a9fbd25c…`, actor Carrie Markway) — four consecutive cards under the FOLLOW UP BOSS LEGACY ACTIVITY band all read *"HouseLoop sent an email"* with the house avatar 📎 `.claude/reports/auditor-ticks/shots/item34-email-card-carrie-markway-prod.png`; Brent Morris (`29f0adc8…`, actor Jeremy David) renders *"HouseLoop sent an email"* on "Confirmed: 134 Bellaire Avenue" 📎 `…/item34-email-card-jeremy-david-prod.png`. **In BOTH cases the human's name appears on the page ONLY inside the quoted email body (a signature block / a calendar-invite organizer line) — an `innerText` grep for the actor returns TRUE and is a FALSE GREEN; my first probe returned exactly that before I pulled the card block.** ⭐ **THE SEAM, captured live: the Emails tab is a SECOND READER that never reads the ledger's actor.** The tab is fed by **`/api/email/thread?lead_id=…`** (`/api/leads/[id]/activity` is called only with `limit=1`), and that payload has **no actor-name field at all** — its 21 keys carry `from_label: null` and `sender_identity: null`, with only `sender_is_user: true`, so the UI has nothing to print but the house. The ledger row for the same email carries `by_name='Carrie Markway'`. The composer is not at fault and its unit tests are green on this very lead (`render-activity.sentence.test.ts:234/240/247` assert "An agent…" / "Tevin Jacutin…" / "HouseLoop sent an email to Marcus Rodriguez") — **the tests pass on a composer this surface never calls.** **Remainder: item 34's walk cannot close until `/api/email/thread` carries the ledger's actor (or the tab reads the one door), and the desk's spot-check five need re-pointing at the actors the #245 train actually restamped.**
- **35.** 🟡 **RULED AT THE DESK 8/21 (enterprise delegation — was Zach-gated)**: ① the 74 tag-vs-body conflicts — the BODY wins per-message (measured: the tag names the LAST-ASSIGNED agent, wrong-as-author 74,286/74,286; SFDC field history names the actual actor; reverses the earlier hold, context changed by measurement). ② the 6,564 tag-only rows stay HONESTLY NAMELESS (no enterprise CRM guesses actors; a 4% misattribution of a NAME is worse than absence — the actor canon refuses guesses). Execution (ledger restamp after staging re-run) rides item 34's ship order.
- **36.** 🟡 Full-book import (#90): **REHEARSAL HALF ✅ DONE 8/23 (builder-rehearse, read-only, receipt `.claude/reports/builder-rehearse/RECEIPT.md`) — EXECUTION HALF STAYS OPEN on Zach's explicit go, cohort-by-cohort.** THE COVERAGE PROOF, full population, live queries: **48,618 of 49,250 people (98.72%) importable-clean; all 49,250 carry a named landing place and the classification sums exactly — zero unexplained** (388 review · 144 no-contact-handle · 49 already-imported · 46 trashed twins · 3 agent-absent · 2 DNC held). Events: **83.25% of 995,173 staged events carry a catalog kind**, every remaining row carrying a named reason — **no catch-all kind, D3 holds**. Landing matrix by rung + source in the receipt (rung ③ carries 92% of review load; **16,237 people — 33% — have no source on the tape** and land with an honest blank per rule 5). Sampled 500-person cohort agreed with the full population within 0.23pp, so the sampling is honest. **⛔ SIX THINGS FILED, NONE BUILT (read-only lane) — the first two GATE cohort #1:** ① **124,961 events (12.56%) drop for ONE missing mapping family** — five FUB types on the `fub.events` lane have no mapping row (Viewed Property 115,892 · Viewed Page 5,780 · Saved Property 3,270 · +3 small); four already have live catalog kinds, mapped only on the `fub.notes` lane. Cure is DATA (`hl_event_mapping`, D5) and takes coverage **83.25% → 95.81%**. ⚠ Its review reason **names the wrong cause** (`source_brand_unmapped` — RealScout is 123,166 of them), disproved by the control: the 1,687 `<unspecified>`-brand rows raise no brand complaint and drop identically, so minting a RealScout source row would clear the flag and fix nothing. ② **THE TWIN GENERATION IS STILL LIVE IN STAGING** though the PRD marks rule 2 "must be LIVE BEFORE any cohort rehearsal": **58,918 surplus text rows, 19,759 disagreeing with their twin on DIRECTION** (the PRD's independent count is 19,764 of 58,854 — two measurements five apart). ⭐ The discriminator was found: `payload->>'messageType'` (`sms`=API / `group_sms`=scraped) separates **19,752 of 19,759 (99.96%)** — so the preference is a staging predicate, no mirror reach-back. ⚠ **Do NOT drop `group_sms` wholesale: 36,761 of 95,596 are the ONLY copy of their fact.** ③ **`readiness` is structurally unsatisfiable** — all 49,250 read `blocked` because a blocker that by signed ruling can never clear on this side of the conveyor (`relationship_class…arrives_at_import`) is stamped unconditionally; it cannot be the cohort go/no-go signal. ④ **`lead_link_status` goes stale and already has** — RaShaund Jackson staged `no_lead`, an active lead minted on the same fub id 1h54m later; the whole estate staged in an 11-second window, so import must re-resolve the link rather than trust the snapshot or it mints duplicates. ⑤ **11,908 events carry `external_person_id='0'`** — personless AND contentless, yet staged `map`; no review reason anywhere names `person_id=0`. ⑥ Rule 8's staging half is clean (994,456 of 995,173 stamped `is_backfill`; the 717 exceptions all recent, none bell-heavy) but the **import-time replay path is not provable read-only** and is NOT closed by this receipt. **⭐ ZACH RULED 8/21 (verbatim: "i will never fully backfill all 49k at once i will backfill in cohorts" + "verify that things work for 100 leads and that myli can handle those leads first, then bigger cohorts… but you should rehearse for the full 49k so they all have a place to fit"): the REHEARSAL covers the FULL 49k (prove every record has a landing place — mapping/kind/actor coverage across the whole book, write:false); EXECUTION is cohort-by-cohort — first cohort ~100 leads verifying the pipeline AND that Myli handles them, then bigger cohorts, each on his explicit go. Never whole-book, never unattended.**
