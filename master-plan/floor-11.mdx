---
title: "Floor 11 — The catalog becomes vocabulary"
description: "Retiring the old trigger-events table so the activity catalog is the single vocabulary — the way Bedrock signed it. Four phases, in dependency order, and the order changed once alr"
---

**[← the punch list](/master-plan/punch-list)** · **[the 12-week schedule](/master-plan/schedule)** · [week 2 — The Cascade](/master-plan/week-2) · 🟡 WORKING — **3/8** punch-list items done

## What this floor is

> Retiring the old trigger-events table so the activity catalog is the single vocabulary — the way Bedrock signed it. Four phases, in dependency order, and the order changed once already when a phase turned out to depend on another.

Phase 1 poured the facts additively — 49 wiring statuses, 16 clean activity words, 30 automation flags — every count guarded by an assertion, so a wrong count writes nothing. Zero catalog values were overwritten.

The lesson it bought is worth keeping: a structural test that says 'the target word is NULL' is **necessary and never sufficient**, because a word poured onto an umbrella kind becomes a claim about every member underneath it.

Phase 2 shipped partially and neither foreign key was promoted. It also uncovered a live defect: two settings surfaces selected registration events by the SHAPE of their name rather than by the catalog, so the website integration page had been showing one of eight real registration events. That exact bug had been diagnosed and fixed once before, on a sibling count, and never swept for its twins.

## The record behind it

- **The census and phase rulings** — `.claude/reports/builder-bed8prep/RECEIPT.md`
- **Phase 1 — the additive pour** — `.claude/reports/builder-bed8-phase1/RECEIPT.md`
- **Phase 2 — the partial repoint** — `.claude/reports/builder-bed8-phase2/RECEIPT.md`
- [Week 2 - the current week, its days and its decision shelf](/master-plan/week-2)
- **THE SCHEMA VERDICT — signed all six phases 8/25; its Phase 5 is this floor** — `.claude/artifacts/review-2026-08-25/schema-plan.html`

## The punch list — what completes this floor

- [x] Census the whole retirement: readers, writers, phases — *41 readers, 5 writers, 22 files; four phases ruled*
- [x] Phase 1 — the additive pour, every count assertion-guarded — *49 + 16 + 30 poured; zero values overwritten; zero unaccounted facts*
- [ ] Phase 3 — cut the 41 readers, palette work first — *Not started, and the surface GREW: 43 files read the old table today, up from the 41 the census counted.* `WED 8/26`
- [ ] Phase 2 — promote the two foreign keys — *HALF DONE 8/27 night: 0718 FK APPLIED at the desk (catalog FK validated, old constraint dropped, pre-image filed). 0719 still HOLD_ — 7 rows outside catalog + runtime.ts:452/464/492 exact-match (Blocker 2); b5's post-0902 census at .claude/reports/b5-floor11/phase3-reader-safety-post0902.txt.* `WED 8/26`
- [ ] Phase 4 — drop the autoseat and the table — *Structurally blocked behind the readers above — the table cannot be dropped while 43 files still read it.* `WED 8/26`
- [ ] Shed the reaction columns from the catalog — *Untouched — the catalog still carries notify/notify_class/automation as its own columns, and the schema doc still documents them as the OWNER columns, against the layer law that says reactions never live on the vocabulary.* `WED 8/26`
- [ ] The no-lies gates — *Not started. Four gates WERE built 8/25 — but they are a DIFFERENT four (the Structure Gate set), and three of those are not wired into the push cage yet, so they are instruments rather than gates.* `WED 8/26`
- [x] Execute the schema plan you signed — eighteen database changes in one day — *Lane receipt: .claude/reports/builder-schemaphases/RECEIPT.md. Checked-at mined from that receipt's commit.* `TUE 8/25`

## The desk's running notes

*Verbatim from the desk board, absorbed here when the board retired into the schedule tree. Receipts, not plans — the checkboxes above are the plan.*

**Floor 11 — the catalog becomes vocabulary** (DDL-class, desk diets):
- [ ] BED-8 retire hl_trigger_events — **CENSUS DONE + PHASE-0 RULED 8/23 night** (builder-bed8prep receipt §6): 41 readers/5 writers/22 files at SQL-string level; the autoseat runs catalog→tev (FK repoint must precede its drop); 4 phases ruled — arm-aware rekey onto registered_* (closes the armed saved_search_registration Bradley-shape), catalog gains wiring_status, is_active delta = 0 true disagreements (pour + grain death), category coercion dies with per-reader diffs. ⏳ One Zach sign on the shelf: mint pre_approved + prequalified as kinds (Phase 4 alone blocks on it)
- [x] **PHASE 1 (the additive pour) DONE 8/23 night** (builder-bed8-phase1, mig 0839, ledger 1009): catalog gained `wiring_status` (nullable diet, zero readers until Phase 3) and took the three pours — **49** wiring statuses verbatim · **16** clean activity words · **30** automation flags. Every count a RAISE EXCEPTION assertion, so a wrong count writes nothing. **Zero-loss table has 0 unaccounted facts** across status/activity/is_active. Diffed against pre-image CSVs: 0 catalog values overwritten (all NULL→value), 0 mirror values changed by the autoseat side-effect (30 `updated_at` moved, as predicted). Drift vs census, all benign: 17 named activity keys → 16 pours (`registration_completed` already carried the identical word) · 32 → 30 automation (`ai_searched` has no tev twin, so no fact to pour). Desk flag RULED same night — **THE SHELF HOLDS**: `manual_entry_registration` is structurally a clean pour but `lead_created` is the UMBRELLA New Lead kind (its card carries the entry road's reason as DATA per the regProvenance rollup), so one road's word on the umbrella would misdescribe every non-manual lead. **The structural test ("target word is NULL") is necessary, never sufficient — a word poured onto an umbrella kind becomes a claim about all its members.** The word stays on `registered_manual_entry`, the kind that names that road; this is why the migration enumerates its 16 keys by hand rather than deriving them from the predicate, which would have swept the umbrella in
- [ ] **PHASE 2 (the FK repoint) — PARTIAL 8/23 night, NEITHER FK PROMOTED** (builder-bed8-phase2, mig 0840, ledger 1010). What shipped: `manual_enrollment` stopped pretending to be a trigger — twin-hunt found `hl_workflows.started_by` already existed (mig 0003, NOT NULL, its own comment declaring `state_entry|signal|manual|scheduled`), so **no diet, no new column**; 'AI: Standing By' is now `trigger_type=NULL, started_by='manual'`, still paused, readers shipped with it. Proven DISCRIMINATINGLY on the live page: the folder header still reads "Manual enrollment" while trigger_type is NULL — the old reader would have said "No trigger", so the label can only be coming from the new fact. ⚠ `started_by` had **zero readers** and 76/77 rows carry its DEFAULT — a column nothing reads is a default, not a decision, so 'state_entry' is deliberately unrendered. Blocker A decoupled (the `LIKE 'ai\_%'` prefix → an explicit set derived from `TOOL_TO_CAPABILITY`, the same map that computes the lookup key), proven set-identical live and under two rolled-back counterfactuals. **Both HOLDs stay HOLD, and both for the SAME disease their headers do not name — a fact with no catalog home wearing an alias that makes it look like a spelling.** 🛑 STOP 1 → the ai_create_saved_search shelf item above. 🛑 STOP 2: **ruling ①'s arm-aware rekey is unimplementable** — the `registered_*` seats ALREADY EXIST AND ARE ALL ACTIVE (11 rows on the same sources), so the map RELOCATES the collisions onto them: **14, up from 13**. But the 11 legacy twins are already dead twice over — `FLAT_KEY` rewrites 8 of the 9 keys at dispatchTrigger's FIRST line (so **step 1's "registration-key flattening" already shipped** as the v4 flat model, mig 0381, making Blocker 2 largely moot), and every twin is `is_active=false` / `workflow_active=false` / unbound. ⇒ the cure is a **DELETE of 11 dead seat rows**, i.e. exactly the product decision Blocker 1 reserves. Also: the rekey does **NOT** close the armed-seat Bradley risk — `create.ts:768`'s rung reads `hl_trigger_events` by type and never reads a seat, so it needs its own ruling before Phase 4. ⚠ Third find, unnamed in either HOLD: **21 ACTIVE palette triggers are not catalog keys** and the save paths write `trigger_type` raw → after the swap, picking any of them 500s on save (a WRITER hazard; step 4 scoped the blast radius to readers only). **Desk ruled it a PRECONDITION and re-sequenced: Phase 3's palette work now runs AHEAD of Phase 2's swap** — the phases were ordered by dependency and this is a new one. **STOP 2 RESOLVED same night — desk amended ruling ① on the measurement and authorized the DELETE (RULE #0 v2, build scope): mig 0841 (ledger 1011) removed the 11 dead legacy seats.** 109→98 rows · 22→11 outside catalog · **collisions 14→3** · armed on-count 19→19 (asserted invariant: if deleting a "dead" row moved what is armed, it was not dead) · every deleted row held the IDENTICAL workflow_id as its twin so no binding was lost — only a tag_id on 8, preserved with its resolved NAME. ⚠ `manual_entry_registration` does **not** inherit the group's FLAT_KEY proof (not a FLAT_KEY source) and was re-measured alone rather than waved through. **All 3 FUB pairs HELD, and the instrument had to change:** no FUB key is FLAT_KEY'd, so "is it emitted" was substituted — and the 41-day/17,610-row fire ledger showed **0 fires for two keys that have LIVE EMITTERS** (`insight-engine.ts:207`, `fub-lead-import.ts:107`). **An empty ledger cannot judge a writer that exists**; a ledger-only read would have deleted two live doors. The seller pair additionally has **no winner seat at all**. ⭐ **AND THE DELETE UNCOVERED A LIVE DEFECT:** two settings surfaces selected registration seats by the SUFFIX spelling `event_key ~ '_registration$'` — which only ever matched the DEAD rows — so the website integration page had been showing **1 of 8** real registration events and went to 0, and the global Lead Flow board lost its whole New Lead Registration group. **This exact bug was diagnosed and fixed ONCE (QA item 62, on the sibling count, comment and all) and never swept for its twins.** Swept now with that identical predicate: website page 0→**8 live**, global board 0→**11**, both verified live. *A correction must be swept for its own twins; selecting by a name shape instead of the catalog is the same disease as the `LIKE 'ai\_%'` prefix — three instances of it surfaced in this one lane*
- [ ] Phase 3 cut the 41 readers · [ ] Phase 4 drop the autoseat + table (blocks on ruling ③)
- [ ] BED-12 catalog sheds reaction columns · [ ] BED-31 the no-lies gates

---
*Generated 2026-08-25 by `tools/master-plan/build-floors.mjs` — edit `tools/master-plan/floors.json`, never this page. Tick a box by setting that item's `"done": true`.*
