---
title: "Floor 40 — CRM Foolproof"
description: "Before we layer more AI on top, the CRM underneath has to be foolproof: saved searches, the AI tools, appointments, leads arriving from Follow Up Boss, and Zillow in-app messages. "
---

**[← the punch list](/master-plan/punch-list)** · **[the 12-week schedule](/master-plan/schedule)** · [week 2 — The Cascade](/master-plan/week-2) · 🟡 WORKING — **23/23** punch-list items done

## What this floor is

> Before we layer more AI on top, the CRM underneath has to be foolproof: saved searches, the AI tools, appointments, leads arriving from Follow Up Boss, and Zillow in-app messages. A verification pass walked all five on 8/23. Two work. Three are degraded. This floor is the eight defects it found, ranked, each routed to a fix or to your desk.

This floor exists because of your own sequencing ruling on 8/23: *"I don't want to pivot too much into the AI programming until the CRM everything works as intended: the saved-search ability, the AI tools, the appointments, getting leads from FUB, the in-app messages from Zillow. The CRM has to be foolproof, THEN we layer in the AI — we can do both at the same time, spawn subagents."*

The verification was read-only: no browsers, no writes, no code shipped. Its verdicts: **Leads from FUB — WORKS**. **Zillow in-app — WORKS, with a live tape defect.** **Saved searches, AI tools, appointments — DEGRADED.** The eight defects below are its ranked output, and the ranking is the build order.

## The record behind it

- **The verification receipt (5 roads, 8 ranked defects)** — `.claude/reports/builder-crmproof/RECEIPT.md`
- **The /sell page CTA recommendation** — `.claude/reports/builder-sellerpage/RECOMMENDATION.md`
- [AI Containment Law — the comms master and its open decision](/command-center/ai-containment-law)

## The punch list — what completes this floor

### The verification itself — 1/1

- [x] Walk all five roads read-only and rank what breaks — We drove all five ways a seller can reach us without changing a single thing, and ranked what broke. Two worked, three limped, and eight problems were written down in the order they hurt. <sub>*8/23 — 5 roads, 2 WORK / 3 DEGRADED, 8 defects ranked, zero writes*</sub>

### The seller door — collapsed to one CTA, shipped — 6/6

*A walk of /sell found two competing calls-to-action that open with the identical question, and neither has ever captured a lead: 20 page views from 7 unique visitors across the page's whole tracked life, and 0 success rows all time in a table holding 271,632 page views. The instrument is alive; the door is not.*

- [x] Put a real number for their own home in the valuation and demand report — First thing Wednesday, and your words: "It needs to BE the home valuation and demand report and pull that stuff in." An address goes in, we go out and gather what the home is worth and how much people are searching for homes like it, and the report comes back with a number for THEIR house. A report of city statistics never gets to call itself a valuation again. <sub>*BUILD ORDER 8/25 (E29), FIRST THING WEDNESDAY - address in, outside home data plus the search-demand read, assembled into a per-home number.*</sub> `WED 8/26`
- [x] Approve collapsing /sell to a single CTA — the Home Valuation and Demand Report — The seller page now asks for exactly one thing — the home valuation and demand report — instead of scattering a visitor across competing offers. Approved and live the same day. <sub>*APPROVED and SHIPPED 8/24 — /sell is live as a pure single CTA, redlined by Zach the same day*</sub> `MON 8/24`
- [x] Point all three plan-card buttons at the report modal with the plan pre-selected, and fold the cash-offer button into the same door — Every button on the seller page, the cash-offer one included, now opens that same report request with whichever plan the visitor clicked already chosen for them. <sub>*Waits on the approval above*</sub>
- [x] Close the old seller Get Started door on the website — turn off its two entry points, its two automations and its confirmation email, and retire the two dead seller seats — The old name lives on forever as a registered alias, so no history breaks and nothing has to move. <sub>*The spelling lives on forever as a registered alias, so no history breaks and nothing migrates*</sub>
- [x] Decide whether the report modal pulls the real per-home valuation, or the copy stops saying 'valuation' — You ruled it: "It needs to BE the home valuation and demand report and pull that stuff in." So the report carries a real number for their home, and no city-wide summary gets to call itself a valuation. Building it is the first thing Wednesday, seeded right below. <sub>*ANSWERED 8/25 (E29) - real per-home number required; build order seeded at the top of Wednesday.*</sub>
- [x] Fix the 22 blank-title, blank-body seller-inquiry cards — Twenty-two seller inquiries were showing up as blank cards with no name and no sentence on them. Every one of them now names who did what. <sub>*Phase 6 territory — every card names an actor and says a specific sentence*</sub>

### The P1s — a road that lies to Myli or escapes the gate — 3/3

- [x] Stop every home Myli finds coming back with a blank status — Done and live. Every home she finds now says Coming Soon or Active instead of coming back blank, and the test carries a control arm proving the old code really did return nothing. The box was simply never ticked. <sub>*DONE and ON PROD — `lead-conversation.ts` falls back to Coming Soon/Active, with a harness carrying a PRE-FIX control arm that asserts the old code really did return null. Commit `0c29271d81`, an ancestor of origin. The box was simply never ticked.*</sub> `MON 8/24`
- [x] Decide whether Myli's email tool answers to the AI off-switch, the way her texting already does — You ruled it tonight: "The AI switch off? The AI can't do anything - email, text, voice. AI switch off = she can't act." So her email answers to that switch exactly like her texting. Doing the wiring is now its own item on Wednesday. <sub>*ANSWERED 8/25 (E22) - the master is real and absolute; the wire-up is seeded as its own item.*</sub> `WED 8/26`
- [x] Make Myli’s email answer to the AI off-switch, the same way her texting already does — Your ruling: "The AI switch off? The AI can't do anything - email, text, voice." Today her email leaves marked as if the system sent it, so the off-switch never sees it. This makes every way she can reach a person stop when you flip that one switch. <sub>*DONE 8/27 night (commit 40fe09ba3e): /api/email/send stamps composedBy:'ai' for origin:'ai' sends — was hardcoded 'system' for all, so an AI-composed email was un-killable. Verified the arm feeds authorizeWireSend. All 11 AI emails in 30 days were test-band; nothing real silenced.*</sub> `WED 8/26`

### The P2s — a road that files the wrong fact — 6/6

- [x] Stop the Zillow in-app capture recording a message we received as one we sent — The version that filed a message on the wrong person is fixed. The version that flips the direction has no guard at all — the guard’s own comment says direction is deliberately not consulted — and we reproduced the bad rows on August 23. <sub>*BUILT 8/27 night, NOT YET DEPLOYED: the direction-twin guard is in fub-scraper-lib (a chrome-scraped DOM row may not contradict a provenance row's direction; 4 historical twins measured as the target). Deploy = Camp David pull + scraper restart — held for the morning window (the scraper was healthy mid-cycle; a night restart wasn't justified). Proof on deploy: the [GUARD] line / zero new inverted twins.*</sub> `WED 8/26`
- [x] Add a Google Calendar sync step to the website tour road so a website tour reaches the agent's calendar — Done and live. A real calendar step now runs on the website tour road, proven by writing to a real Google Calendar and reading it back from Google rather than from our own records. The one honest gap: nobody has yet watched a real website tour fire it on the live site. <sub>*DONE and ON PROD — a real `sync-google-calendar` step on the tour road (commit `01b040308c`), proven by a harness that writes to a real Google Calendar and verifies by reading it back from Google rather than from our own column. ⚠ The one honest residual the harness names itself: nobody has yet watched a real website tour fire the step on prod.*</sub> `MON 8/24`
- [x] Confirm a real scheduled saved-search email recorded which search it came from, and only then call that item done — Done, with the exact witness the item asked for: the real scheduled morning run (not a hand fire) sent 4 digest emails and every one recorded which saved search it came from. <sub>*COLLECTED 8/27 night, AS WRITTEN: the 13:00Z 2026-08-26 CRON run (hl_run_log saved-search-daily-alert, trigger=inngest, success, started 13:00:21Z) produced 4 saved_search_digest_email rows at 13:00:30Z, ALL with saved_search_id populated (e.g. e290dbf1). The earlier PAUSED note was stale — the scheduled witness now exists and it records which search it came from.*</sub> `WED 8/26`
- [x] **🖊 DESK** — Decide whether the home photo a saved-search card finds is allowed to count as that card's main picture — Your call, unmade. The card still finds a photo and the frame still drops it. <sub>*⭐ Desk/your call, unmade — the card still resolves a photo and the frame still drops it.*</sub> `WED 8/26`
- [x] Fire a -TEST appointment through the real CRM API door — A rehearsal booking now runs end to end through the real machinery, with a real calendar event created and cleaned up inside the proof — though it goes through the shared writer rather than the outside road. <sub>*The appointment road IS genuinely exercised now — a real booking end to end from a drawn playbook move, real calendar event created and deleted inside the proof — but through the shared WRITER, not the HTTP API door this box names. Your call whether that satisfies it.*</sub> `WED 8/26`
- [x] Put voicemails on Action Needed, with a player you can press — A voicemail used to be something you could only find if you went looking. It now appears where the work is, and you can play it right there instead of hunting for the recording. <sub>*Lane receipt: .claude/reports/builder-callvis/RECEIPT.md. Checked-at mined from that receipt's commit.*</sub> `TUE 8/25`

### The P3s and the standing data question — 2/2

- [x] Settle which of the two saved-search lists is the real one — No longer needed. The table plan you signed retires this list instead of merging it — the website’s list is the truth, so the clash no longer needs a winner. <sub>*Phase 6 of the Schema Verdict you signed retires this table instead of pouring it — the website table is the truth, so the collision no longer needs a winner.*</sub> `MON 8/24`
- [x] Reconcile the four phone-assistant abilities that are marked built against what actually exists — Four abilities are declared built. One of them only exists under a different name, and the setting that is supposed to turn them on is read by nothing. <sub>*capture_caller / set_callback / take_message / flag_spam are declared built:true; one handler exists under a different name (log_callback) and `tools_enabled` has zero in-repo consumers*</sub>

### The mobile web walk, closed same-day (8/26) — 5/5

- [x] Zach's 3:21 phone findings fixed AND deployed: bars pinned always, the honest arrival line, the exact/nearby count split, the reworded stale-pin notice, the new headshot at real size — Everything Zach caught on his phone that afternoon is fixed and live: the bars stay pinned, the arrival line tells the truth, exact and nearby homes are counted separately, the stale-pin notice reads plainly, and the headshot is the new one at its real size. <sub>*pushed + Railway-confirmed + desk prod-walked the same evening*</sub> `WED 8/26`
- [x] The counting verb is FIT on every surface — header, filter sheet, map pill, her prompts — One word for one idea: every surface now counts the homes that FIT — the header, the filter sheet, the map pill, and Myli's own sentences. "Matching homes" is gone from the results screen. <sub>*the F4 pair + the desk sweep; "matching homes" is dead on the results surface*</sub> `WED 8/26`
- [x] /picks — her picks on their own page: ack dance, her line, always two pills; the curated rail stays — Myli's picks have their own page now, with her line, the little acknowledgment moment, and always two things to tap next. The count on it is read off the real homes, never made up. <sub>*dashboard lane, walked at 390; the count is read, never written*</sub> `WED 8/26`
- [x] /new-today — the clickable 24-hour count lands on her list (one query, two readings) — The count of homes that showed up in the last day is now clickable, and it lands on the very list it counted — one question asked once, so the number and the page can never disagree. <sub>*desk-built from the dying lane's halves; back-home affordance; the remainder named*</sub> `WED 8/26`
- [x] The half-body Myli on mobile, one art source for both bands; the wrap disease root-caused (the shared header sacrificed the sentence and kept the decoration) — Myli shows up half-body on a phone, drawn from one source of art in both places she appears. The text that kept wrapping badly turned out to be a shared header that protected the decoration and squeezed the sentence. <sub>*dashboard lane*</sub> `WED 8/26`

## The desk's running notes

*Verbatim from the desk board, absorbed here when the board retired into the schedule tree. Receipts, not plans — the checkboxes above are the plan.*

**Floor 40 — CRM FOOLPROOF** (Zach's sequence ruling: *"The CRM has to be foolproof, THEN we layer in the AI"*):
- [x] the verification pass itself (builder-crmproof, 8/23, read-only): 5 roads — **FUB leads WORK · Zillow in-app WORKS with a live tape defect · saved searches, AI tools and appointments DEGRADED**; 8 defects ranked and routed
- [ ] the two P1s: `search_homes` returns `status:null` on 100% of homes (13 of 17 in the live query were Coming Soon) · **Myli's `send_email` escapes the AI comms master** → ⭐ ZACH
- [ ] the P2s: the in-app twin at capture · website tours reach no calendar (10/10 since 8/14) · the 8/24 13:00Z digest witness · the saved-search card photo (desk) · fire a -TEST appointment through the real API door
- [ ] the seller door: collapse `/sell` to ONE CTA → ⭐ ZACH · [full punch list](/master-plan/floor-40)

## ⭐ What only Zach can decide on this floor

- **Does Myli's send_email go behind the AI comms master?** — her text tool is gated, her email tool is not; this is the fake-switch class the containment law exists to end
- **6/27 or 6/28 — which saved-search ruling wins?** — the pour is blocked until one of your two rulings is the answer
- **Collapse /sell to one CTA?** — five buttons, two roads, one question, zero leads ever

---
*Generated 2026-08-28 by `tools/master-plan/build-floors.mjs` — edit `tools/master-plan/floors.json`, never this page. Tick a box by setting that item's `"done": true`.*
