---
title: "Week 2 — THE EXECUTION PRD (Days 1-3, automode contract)"
description: "The standing work order for Week 2's opening: the import→rehearsal→re-pour chain, the email-reply coverage measurement, feature-work resumption, and the Floor 11 burn-down — sequenced so the loop runs non-stop. Zach's 8/23 rulings verbatim inside."
---

# Week 2 — The Execution PRD (Days 1-3)

**Zach's order (8/23 ~02:45, verbatim intent):** *"I need all of week 2 lined up, ready to go
tomorrow… a full-on PRD at least for the first two or three days, so you can work non-stop and get
through days one and two."* This page is that document. Same operating model as
[the Week-1 PRD](/master-plan/week-1-prd): one desk + up to two lanes, cages at every push, a box
ticks only on an auditor receipt or a desk-verified premise-death, deploy-witness before any
reader-repoint tick. [week-2](/master-plan/week-2) is the scoreboard this PRD drives.

## The rulings this PRD is built on (Zach, 8/22-23 night, all on the record)

- **The re-pour is a TEST, 5 edge-case past clients only:** a simple lead · a couple · one with
  multiple houses · a couple where the spouse has NO contact info (name-only). Never a bulk pour.
- **The rehearsal is ~5 leads imported CORRECTLY, not 100** — correctness over count; plus ~5 past
  clients through the same road.
- **The dotloop backlink is best-effort from persisted keys only** — *"we're not going back through
  13,000 contracts… it's already been resolved."* Unconfirmable phantoms stay held.
- **No email bodies, ever** (the 6/23 compose-incident ban stands). The ask is the REPLY SIGNAL
  only: *"I only need to know that an email was responded to."*
- **Spouses without contact info mint as NAME-ONLY connected persons** (the FUB model), identity =
  name + household edge + deal provenance.
- **Normal human message bodies in anything carrier-visible; markers in timestamps/metadata;
  fixtures wear realistic names.**

---

# DAY 1 — the import chain + the reply-signal measurement

## D1-A · The email-reply coverage MEASUREMENT (before any build)

The premise to verify, from the record: lead email replies already land via the **native Gmail
watch** (8 mailboxes, live since 8/18; live-fired 8/22 at 6.0s send→bell with lead resolution).
- Measure 30 days of real inbound lead email against `email_received` cards: the honest coverage %.
- Enumerate the misses (if any) and classify: FUB-proxied reply addresses · unconnected mailbox ·
  resolution failure · the BED-127 class (which is OUTBOUND sync, not inbound — don't conflate).
- **The gap-closer, if needed, is the FUB API metadata road** (emailsCreated webhook / `/v1/emails`
  — who/when/direction, NO body): a body-less `email_received` card + reply-signal. A FUB-inbox UI
  scraper is built ONLY if the measurement proves the API road cannot see a class of reply — and
  that build goes to the desk first.
- Acceptance: a one-page coverage report with the number, the miss classes, and the chosen closer.

## D1-B · The IMPORT half of FUB-18 (staged person → product lead, one writer)

The staging estate is poured (49,250 rows, 97.02% actor score). The import job:
- An Inngest function that takes a staged person (verdicts resolved, review clear) → mints/updates
  the product lead THROUGH the one writer path (the same mint gates: trashed-twin, inbound-only,
  the 9-trigger law) → stamps `imported_at` + `imported_lead_id` on the staging row (the pointer,
  never the answer) → `resolved_by='srg_staging_fold:person_poured'` semantics preserved.
- `created_at` = the HouseLoop pull date (rule 6); the actor ladder's answer rides as the lead's
  legacy agent context; `is_backfill` semantics on any history filing (cards yes, bells never).
- Red-first: a staged fixture imports clean; a trashed-twin refuses; a review-flagged row refuses;
  re-run imports 0 (idempotent).
- ⚠ Import is EVENT/MANUAL-dispatched only in Week 2 — no cron until the rehearsal signs.

## D1-C · THE REHEARSAL — 5 leads, perfect

- Desk picks 5 staged persons spanning the rungs (a comm-agent rung-2, a chain rung-3, a tag rung-4,
  a house-floor rung-5, one with a review flag to prove the refusal).
- Import through D1-B → walk EVERY one on prod: the lead page, the arrival card grammar (eyebrow,
  three lines, integration badge), the Action-Needed state, the agent context, zero bells fired.
- A miss on ANY of the 5 stops the line — fix, re-run (idempotent), re-walk. **Correctness over
  count** is the acceptance bar, per the ruling.
- **⭐ THE CLEANUP IS PART OF THE REHEARSAL (Zach 8/23, verbatim: "once they're imported, they are
  deleted out and removed. The same with the past clients")**: after the walk signs, the 5 test
  imports are REMOVED cleanly (dumped first, soft-delete through the sanctioned path, staging rows
  un-stamped so a future real import isn't blocked by the rehearsal's pointer). A rehearsal leaves
  ZERO residue. The 100-lead cohort is EVENTUAL, on his explicit go — never tonight's number.

## D1-D · THE RE-POUR — the 5 edge-case past clients (the household product test)

Through the BED-35 shape (the past-client staging adapter — the estate's own resolved records, the
SRG estate itself untouched as source-of-truth):
- The 5: one simple · one couple (both with contact info) · one multi-house client · one couple
  with a NAME-ONLY spouse · one more desk-picked from the 549 key-persisted set.
- Each pours to: the lead(s) + the connected-person spouse (name-only where ruled) + the
  `hl_connected_people` edge (the LIVE lifecycle table — writers must target the partial unique
  index `WHERE ended_at IS NULL`) + `household_primary_id` where both ends exist.
- Walk all 5 on prod: the Connections section renders the spouse; the household folds correctly on
  the leads table; the name-only spouse shows with no fabricated contact info.
- **This is also the BB delivery-remedy test bed** (D2-C): the poured couple with real numbers is
  the send target for the delivery measurement.
- **Same cleanup law**: after the walk + the BB measurement sign, the 5 re-poured past clients are
  deleted out and removed (dumped first) — the SRG estate stays the only permanent record until the
  real cohort pours on Zach's go.

# DAY 2 — feature work resumes + the notification estate closes

## D2-A · Floor 10 / BED-37 — feature work RESUMES on bedrock

- The desk re-opens the TC loop, playbooks, and Myli-campaign specs (unchanged specs per the item)
  against the proven foundation. Day-2 scope: the SPEC REFRESH + the first build slice of whichever
  Zach ranks first in the morning (default: the TC loop, its walk is Floor 12's BED-64).

## D2-B · Floor 11 opening burn — the catalog becomes vocabulary

1. **BED-8** retire `hl_trigger_events`: the page reads the catalog; ⚠ MUST unwire the autoseat
   trigger first (`hl_catalog_autoseat()` — recovered verbatim in migration 0822; it seats a
   trigger-events row on every catalog insert). TypeScript and Postgres agree on what a legal kind is.
2. **BED-12** the catalog sheds its reaction columns (notify / notify_class / existing_notify /
   automation → their Phase 2-3 homes). ⚠ BED-13's direction/channel death lands on a FRESH
   migration number (0490 is taken by a hold — the runner silently skips a restored original).
3. **BED-31** the no-lies gates: one-writer guard + no new kind without a harness walk.
- Each is DDL-class: desk-signed diets, schema-lock in the same push, deploy-witness before any
  reader tick.

## D2-C · The BB delivery remedy (task-84's real fix)

- Measure the async error-22 delay with ONE live send to an OWNED test line (the poured D1-D couple
  or the Twilio test lines) — the number that picks re-read vs webhook.
- Then the two halves TOGETHER: the rank-guard write (replacing `WHERE delivery_status IS NULL`)
  + the post-response observer (timed guid re-read or the BB webhook consumer reading error/
  isDelivered — ⛔ isDelivered is never the GATE, only an input). `test:bb-stamp` extends to cover
  the late-error arm.

## D2-D · The pre-paid tick audit (desk pass, cheap)

BED-46 · BED-47 · BED-119 · BED-68 were substantially delivered by the 8/22 night's captain work
(the phantom quarantine, the twin cure + absorption, the seller census). Audit each against its
item text; tick what the receipts cover; name the remainder on the item for what they don't.

# DAY 3 — the QA burn-down + E2E floor 35

## D3-A · Floor 11 QA sweep (bounded fixes, one lane, in this order)

BED-85 count chip reads page size · BED-93 AI cockpit two-populations card · BED-100 website tab's
three numbers · BED-102 tasks badge counts activity rows · BED-109 signature-request cards drop the
actor sentence · TASK-149 hydration #418 · TASK-135 trashed-lead digest leak · TASK-153 dead notes
column · TASK-128 ask-act pills · TASK-137 star-table FK hazard. Each: measure → red-first → fix →
walk. Anything structural STOPS at a proposal.

## D3-B · The saved-search detail suite (BED-121/122/123/125 + BED-127)

Last-sent/opened blank after immediate send · clickable live-match count · delete affordance (the
lifecycle precedent: an ending, never an erasure) · the SEND flow (channel + sender identity +
per-home message) · the native OUTBOUND email sync gap (an email from zach@ Gmail didn't pull in —
the outbound twin of D1-A's inbound measurement).

## D3-C · E2E Floor 35 — household proven end to end

On Conner + Jasmeet (-TEST): linked as a household · activity by one files correctly seat-scoped on
both · the leads-table household view groups them · Myli's household working memory (#81). The D1-D
re-pour receipts feed this directly.

---

## Standing rails for every Day-1-3 lane

The SRG estate is enriched, never rewritten; no surname matching; no email-dedup · backup-first
before every write batch; zero deletes on estates (archive/absorb/end) · `is_backfill` never bells ·
suppressions are LOUD · the tape is verbatim · one writer per concern; a settled decision is STORED
(actor_source) · normal bodies, realistic fixture names · git status --porcelain before "committed" ·
cages from the repo root, run LAST · registration proven by the function LIST, runs proven in
hl_run_log · deployed ≠ exercised — every new road gets a birth-certificate job.

## Carry-over items woven in (from the 8/22 night queue)

The detector ELECTION (in flight 8/23 early: beat relocation + bridge exercise + cutover) · G4
completion (in flight: the wire-contract fix, the two honest refusals, RED-3, the toggle census) ·
G5 full-registry verify + G6 cattle drill close (desk passes) · the best-effort dotloop backlink +
cheap-evidence phantom retirement (fold into D1-D's adapter work) · the desk small-pass batch
(named-DNC table · TAG_NOISE_NAMES repoint · HOLD_-files cage gap · Zairanett enrich-absorb ·
Marcos Ramirez correction · Kwizera duplicate group · the 2 held identity questions).

---

# DAYS 4-7 — THE MYLI TOOLBOX WEEK (appended 8/23, builder-mylitools lane)

**Zach's order (8/23, verbatim):** *"All the Myli toolboxes, the Myli tools that she needs, the rest
of the week, need to get done."* **The north star (Signed Addendum 4):** the FLAGSHIP closed-loop
showing-coordination scenario — Zillow tour request → Myli engages → texts the showing partner →
coordinates + schedules → TC single-showing document filled/sent/verified/signed → Google Calendar
invite (us + partner + client) → verify everything → follow up with the partner AND the client —
**runnable END TO END on the -TEST rig by week's close.** That E2E is the definition of done for
this whole block. Standing laws: TEST LEADS ONLY until Zach flips each playbook · no cohort ·
Fable on playbooks/workflows lanes · every send through the containment gate + a drawn move.

## The census verdict this order is built on (measured 8/23, receipts in `.claude/reports/builder-mylitools/RECEIPT.md`)

The conversation runtime already carries **20 wired tools** (`lead-conversation.ts` execTool,
`ALL_TOOLS`) covering: text/email the lead, search/lookup/details (the NLP brain), send_home,
create_saved_search, schedule/update/cancel/disposition appointment, start_showing_coordination,
send_document (incl. the single-showing form), create_task, schedule_followup, add_note,
update_contact, update_stage, manage_tags, build_context. The showing-coordination estate
(`lib/coordination/`) already carries the partner ask, the negotiation, the doc hook, and the
agent-feedback capture. The measured GAPS, in build order:

1. **The ledger starves from the tools** — `writeLedgerSlot` has feeders from facts + registration
   only; `tour_time_confirmed`, `next_showing_time`, `last_home_feedback` have ZERO writers, so
   gates-to-gates cannot see a real booking (Addendum 2 §3 put this wiring IN SCOPE).
2. **"Tune the search" has no tool** — create exists; no update. The route-level PATCH edits
   name/frequency only, never criteria; the feedback path mints a NEW search instead of tuning.
3. **The partner post-showing ask is built but never fires** — `askAgentForFeedback` has zero
   callers; the client feedback ANSWER never lands in the ledger.
4. **No move→tool grant seam** — `hl_playbooks.tool_grants` (mig 0461) has zero readers; playbook
   moves compose with READ-ONLY tools only; every mutating tool is reachable solely from the
   conversation dispatch. The spec's §1.5 grain ruling implies moves carry their tool ordering.
5. **No document-verify read** — Myli cannot truthfully answer "is it signed?"; the e-sign chase
   stays KILLED until it exists as drawn moves (AI containment law).
6. **AI search** — one brain serves the site bar AND Myli's search_homes (`/api/search/nlp`);
   Zach's order: *"AI searches need to work properly. The whole deal needs to be locked in."*

## D4-A · The tool→ledger feeders (THIS lane builds it — Day 4, first)

- `feedLedgerFromBooking` / `feedLedgerFromDisposition` in `lib/ai/convo-state.ts` (the ONE ledger
  writer keeps its single-writer discipline; executors call feeders, never mergeLedger).
- Wired: `execScheduleAppointment` + `execUpdateAppointment` → `tour_time_confirmed` +
  `next_showing_time` · `execDispositionAppointment` → `last_home_feedback` (showed outcomes) ·
  `handleAgentFeedback` (mediation) → `last_home_feedback`.
- **Red-first:** `npm run test:booking-ledger` — pre-change run captured RED (slots absent after the
  write road runs); post-change every arm green, idempotent re-run, goal_frame never clobbered.
- **Acceptance:** after a booked tour on the -TEST lead, `hl_lead_convo_state.ledger` shows the
  tour slots; after a dispositioned showing, the feedback slot; the frame ladder can resolve
  TOUR_TIME/HOME_FEEDBACK from real events. Walk: the state-engine page renders the slots.

## D4-B · `update_saved_search` — the "tune the search" tool (THIS lane builds it)

- One-door `updateSavedSearchForLead` (`lib/leads/update-saved-search.ts`): resolves the lead's
  search (by id or most-recent active), re-validates criteria through the SAME validators as create
  (`toCriteriaAndUrl` — the write-validator + the status-value guard), updates criteria+URL+name+
  frequency, logs the EXISTING catalog kind `saved_search_edited` (twin-hunt done: no new kind).
- Tool registration: `ALL_TOOLS` + dispatchTool case + the create_saved_search CAPABILITY gains the
  tool (→ the existing `ai_create_saved_search` door; no new trigger key, no new capability row).
- **Red-first:** `npm run test:update-saved-search` — refuses an off-vocabulary criteria patch;
  tunes price/beds on a real row; activity row lands kind `saved_search_edited`, actor ai_agent.
- **Acceptance:** Myli can narrow "under 400k, 3+ beds" onto the lead's existing search in one tool
  call — no duplicate search minted; the CRM Searches card shows the edit.

## D5-A · The post-showing BOTH-MOUTHS wiring (flagship steps 7-8)

- The TRIGGER for `askAgentForFeedback`: a dispositioned SHOWING (outcome=showed/completed) with an
  active coordination → the partner chase fires; plus the tour+24h TTL backstop.
- The CLIENT ask already exists as the post-appointment floor (`schedulePostAppointmentFollowup` —
  "how'd it go?"); the missing half is the ANSWER capture: the fact extractor maps the reply into
  `last_home_feedback` (extend `FACT_LEDGER_MAP` — a `search_criteria`/feedback domain row).
- Every send = a drawn move / the existing floor road through the gate chain. NOTHING new touches a
  wire outside the rig.
- **Acceptance:** on the -TEST rig, one dispositioned showing produces the partner ask AND the
  client check-in; both replies land (coordination.feedback + ledger.last_home_feedback); receipts.

## D5-B · Document VERIFY + the drawn chase (flagship step 4's back half)

- A READ-ONLY `check_document_status` tool (coordination row + the e-sign provider's status) so
  Myli answers "is it signed?" truthfully — read tool, joins COMPOSE_READONLY_TOOLS.
- The document CHASE returns ONLY as drawn playbook moves (the containment law killed the hidden
  chase; it stays dead until it is visible). Move set proposal → desk sign → seed.
- **Acceptance:** harness proves status truth (sent/viewed/signed) against a real -TEST envelope;
  an undrawn chase attempt refuses with its own token.

## D6-A · The move→tool grant seam (STRUCTURAL — desk-signed before build)

- The 0461 `tool_grants` column gets its READER: a playbook move of kind `action` may invoke a
  GRANTED tool through the one execTool choke (containment stamps + tool-fire ledger intact);
  ungranted = refuse with its own token, trace row written. This is what makes the flagship's
  moves able to book/fill/send rather than only talk.
- DDL-class + engine surgery → the desk signs the diet first; this lane STOPS at the proposal.
- **Acceptance:** red-first — ungranted refuses; a granted action move creates a task on the -TEST
  lead through the real seam; `hl_playbook_turn_trace` carries the tool fire.

## D6-B · AI SEARCH lock-in (Zach's verbatim week order)

- The estate is ONE brain: `apps/houseloop-web/app/api/search/nlp` (+ `nlp-map.ts`,
  `repliers-nlp.ts`) serving the site bar AND `execSearchHomes`. Lock-in = a FIXED EVAL SUITE
  (~25 queries: place-proximity, feature/vision, price/beds/status combos, the status-value traps,
  the known regressions) run against prod; every miss triaged → fixed → re-run.
- **Acceptance:** the suite green two consecutive runs; the search console surfaces (site bar +
  Myli's tool road) return the SAME answer for the same query; the suite lands as a repeatable
  script, not a one-off walk.

## D7 · THE FLAGSHIP E2E (with the simulator sibling lane)

- The Addendum-4 scenario end to end on the simulator: AI-plays-Julie + the test-Trish arm, time-
  compressed, Myli's full thinking visible (playbook · move · because · waiting-for).
- **Acceptance = the eight numbered steps, each verified:** engage → partner text → coordinated +
  scheduled → single-showing doc filled/sent/VERIFIED signed → calendar invite with all three
  guests → task/appointment/doc all verified done → partner follow-up → client feedback captured
  in the ledger. Receipts on every step. This green is the week's definition of done; live
  widening remains Zach's flip, per playbook, in the AI Control Center.
