---
title: "Floor 1 — The 28 Signals Verified"
description: "The live-fire gauntlet: all 28 signed signals driven end to end, each one verdicted out loud, and the defects it shook out fixed the same week."
---

**[← the punch list](/master-plan/punch-list)** · **[the 12-week schedule](/master-plan/schedule)** · [week 1 — The Foundation](/master-plan/week-1) · ✅ DONE — **7/14** punch-list items done

## What this floor is

> The live-fire gauntlet: all 28 signed signals driven end to end, each one verdicted out loud, and the defects it shook out fixed the same week.

## The record behind it

- **The gauntlet receipt — all 28 verdicted** — `.claude/reports/fub-gauntlet/RECEIPT-R2.md`
- **The four filing fixes the gauntlet found** — `.claude/reports/filing-4`

## The punch list — what completes this floor

### The gauntlet and what it shook out — 7/14

- [x] Fire all 28 signals for real and give every one of them a verdict — The whole signal list driven end to end in one sitting: 8 fired live in front of us, 18 confirmed against real traffic that had already arrived, 2 refused for reasons we wrote down, and 2 with nothing to fire. Nothing was marked working because it looked like it ought to work. <sub>*Receipt: .claude/reports/fub-gauntlet/RECEIPT-R2.md. Checked-at mined from that receipt’s own commit.*</sub>
- [x] Catch the second spelling that was quietly dropping a signal — The record held two spellings of the same event and only the longer one was recognised, so the short one fell on the floor every time. Found by the gauntlet, fixed, and welded shut by a test that fails if it ever comes back. <sub>*Receipt: .claude/reports/filing-4. Checked-at mined from that receipt’s commit.*</sub>
- [x] Stop million-dollar prices falling out of the note reader — The reader understood a price written out in full but not one written in shorthand, so our priciest leads were being filed as plain notes. It now reads 3,027 of 3,028 real notes correctly, and the single miss is a test message we planted ourselves. <sub>*Receipt: .claude/reports/filing-4. Checked-at mined from that receipt’s commit.*</sub>
- [x] Make a note that produces no card shout instead of skipping quietly — If a note looked like something had happened but produced nothing, it used to disappear without a sound. It now raises a loud error naming the note, so silence can never be mistaken for success again. <sub>*Receipt: .claude/reports/filing-4. Checked-at mined from that receipt’s commit.*</sub>
- [x] Make both arms of the same event produce one card instead of two — Two different roads can carry the same fact. They now share one card maker and one identity, so whichever one arrives first you get a single card with the right person’s name on it. <sub>*Receipt: .claude/reports/filing-4. Checked-at mined from that receipt’s commit.*</sub>
- [ ] Mint the FUB SCRAPER standing card — scraper defects have no home (kinds.json routes scraper -> null) — Mint the FUB SCRAPER standing card — scraper defects have no home (kinds.json routes scraper -> null) <sub>*Added 2026-09-09 via the task door.*</sub>
- [x] THE MINT TOGGLE HAS THREE HOMES (measured 2026-09-10 night, Keren Aguilar): the FUB mint gate reads hl_event_mapping.creates_lead; the Trigger Events UI writes hl_lead_source_events.creates_lead (source × event, /api/lead-flow); the activity-catalog configure modal (#563) writes hl_activity_catalog.creates_lead (NULL on every row) — Zach saw ON in the UI while the gate read OFF. ONE_ANSWER_TABLE: the gate must read the row the UI writes (or the UI writes the gate's row); pick one home, migrate the other two to reads, cage the twin — THE MINT TOGGLE HAS THREE HOMES (measured 2026-09-10 night, Keren Aguilar): the FUB mint gate reads hl_event_mapping.creates_lead; the Trigger Events UI writes hl_lead_source_events.creates_lead (source × event, /api/lead-flow); the activity-catalog configure modal (#563) writes hl_activity_catalog.creates_lead (NULL on every row) — Zach saw ON in the UI while the gate read OFF. ONE_ANSWER_TABLE: the gate must read the row the UI writes (or the UI writes the gate's row); pick one home, migrate the other two to reads, cage the twin <sub>*Added 2026-09-11 via the task door. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/the-foundation/RECEIPT-fub-intake-fold-2026-09-10.md. the toggle-home cage born green (7/7) + the catalog modal read-only; the disease measured latent; 1330 + 1331 signed and applied*</sub> `FRI 9/11`
- [x] A FUB-minted lead carries NO card for the signal that minted it (measured tonight: the Zillow tour-request relay minted lead_created + task_created + 'Dashboard provisioned', but no tour_requested card with the address/date/time/relay text) — so the needs-response queue shows 'Contact &lt;name> · Zillow' with no payload; the signal's card must file on the minted lead in the same beat as the mint, and a cage proves every minted lead carries its arriving signal's card — A FUB-minted lead carries NO card for the signal that minted it (measured tonight: the Zillow tour-request relay minted lead_created + task_created + 'Dashboard provisioned', but no tour_requested card with the address/date/time/relay text) — so the needs-response queue shows 'Contact &lt;name> · Zillow' with no payload; the signal's card must file on the minted lead in the same beat as the mint, and a cage proves every minted lead carries its arriving signal's card <sub>*Added 2026-09-11 via the task door. TICKED 2026-09-11 via the task entrance (desk) — receipt: .claude/reports/the-foundation/RECEIPT-fub-intake-fold-2026-09-10.md. ROOT CAUSE: two classifiers (classifyFubEvent vs resolveMapping) disagreed on one signal — the card step now resolves through the mapping (the same entry the gate uses); provenance source_mapping_id wired; fleet defect 38/94 FUB-minted leads carry no signal card; the replay of 1836987 + the watermark happen AT DEPLOY (Friday)*</sub> `FRI 9/11`
- [ ] THE NAMELESS-CALLER CARD IS PARSED WRONG (Zach's 8:15 PM screenshot, 2026-09-10): an inbound call to the sign number minted 'New contact (636) 875-2553'; the needs-response card reads 'Call back (636) 875-2553 / (636) 875-2553 called +16368752553 called +19133548600 (Inbound Call (sign #))' — the number three times, 'called … called' doubled, the source in nested parentheses, the body wrapping mid-number. The phone_call_received card grammar: actor = the caller (name, else the number ONCE), object = OUR line by its source NAME (sign number), no raw E.164; one line; a control for a named caller — THE NAMELESS-CALLER CARD IS PARSED WRONG (Zach's 8:15 PM screenshot, 2026-09-10): an inbound call to the sign number minted 'New contact (636) 875-2553'; the needs-response card reads 'Call back (636) 875-2553 / (636) 875-2553 called +16368752553 called +19133548600 (Inbound Call (sign #))' — the number three times, 'called … called' doubled, the source in nested parentheses, the body wrapping mid-number. The phone_call_received card grammar: actor = the caller (name, else the number ONCE), object = OUR line by its source NAME (sign number), no raw E.164; one line; a control for a named caller <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] THE NEEDS-RESPONSE CARD FOR A CALL says nothing about a VOICEMAIL and cannot play it (Zach 2026-09-10 ~20:20 CT): the phone_call_received card must say 'left a voicemail (0:42)' vs 'called, no voicemail', carry the transcript line when one exists, and render an inline player (the recording URL Twilio already stores on the call row) inside the queue column — never a click-through to the lead — THE NEEDS-RESPONSE CARD FOR A CALL says nothing about a VOICEMAIL and cannot play it (Zach 2026-09-10 ~20:20 CT): the phone_call_received card must say 'left a voicemail (0:42)' vs 'called, no voicemail', carry the transcript line when one exists, and render an inline player (the recording URL Twilio already stores on the call row) inside the queue column — never a click-through to the lead <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] THE ROUTER CALLS THE ONE CLASSIFIER (next week, after the Sunday cutover — the FUB intake second opinion's order 6 + #257.494 DEPARTED): fub-webhook-router and the in-app detector stop deciding trigger/webhookType themselves and hand the tape row to the staging classifier (fub-staging-classify → hl_staging_event, whose review_status/review_reasons ARE the review queue, last classified 2026-08-23); unknown signals land there as review_status='review' within 60 s; the freshness cage: tape max(received) − classification max(classified_at) &lt; 60 s or RED; fub-source-sweep/restamp become repair consumers of the classified result (order 9) — THE ROUTER CALLS THE ONE CLASSIFIER (next week, after the Sunday cutover — the FUB intake second opinion's order 6 + #257.494 DEPARTED): fub-webhook-router and the in-app detector stop deciding trigger/webhookType themselves and hand the tape row to the staging classifier (fub-staging-classify → hl_staging_event, whose review_status/review_reasons ARE the review queue, last classified 2026-08-23); unknown signals land there as review_status='review' within 60 s; the freshness cage: tape max(received) − classification max(classified_at) &lt; 60 s or RED; fub-source-sweep/restamp become repair consumers of the classified result (order 9) <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] THE ATOMIC MINT (the FUB intake second opinion G, NOT built tonight): the mint + the arriving signal's card commit in ONE CRM transaction with the MINT_WITHOUT_SOURCE_ACTIVITY rollback — requires deferring logActivity's on-insert side effects (notifications, realtime, enrichment; 2,667 lines / 275 callers) to after commit: a signed re-architecture of the one writer, not a lane edit — THE ATOMIC MINT (the FUB intake second opinion G, NOT built tonight): the mint + the arriving signal's card commit in ONE CRM transaction with the MINT_WITHOUT_SOURCE_ACTIVITY rollback — requires deferring logActivity's on-insert side effects (notifications, realtime, enrichment; 2,667 lines / 275 callers) to after commit: a signed re-architecture of the one writer, not a lane edit <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] THE INTAKE RECEIPT + THE 60-SECOND SENTINEL (the FUB intake second opinion E, step 5, not started): every intake road writes its run-log row inside its own try/finally (decision · refusal_code · lead_id · signal_activity_id · source_event_key fub:&lt;object>:&lt;id> · received/completed) — typed decision/refusal columns need the desk's interview (never payload->> filters); fub-intake-sentinel.ts every minute reds at +60 s with the reason, delivered on the existing founder notify road; registered in serve() + the run-log registry — THE INTAKE RECEIPT + THE 60-SECOND SENTINEL (the FUB intake second opinion E, step 5, not started): every intake road writes its run-log row inside its own try/finally (decision · refusal_code · lead_id · signal_activity_id · source_event_key fub:&lt;object>:&lt;id> · received/completed) — typed decision/refusal columns need the desk's interview (never payload->> filters); fub-intake-sentinel.ts every minute reds at +60 s with the reason, delivered on the existing founder notify road; registered in serve() + the run-log registry <sub>*Added 2026-09-11 via the task door.*</sub>
- [ ] HARNESS PROOFS for the two kinds minted by 1331 (portal_registration_received · fub_person_created): tools/harness/run-proofs.mjs — fire the real signal on the bench, assert the log row, the render, the rule evaluation; the activity-harness cage refuses Friday's push without them — HARNESS PROOFS for the two kinds minted by 1331 (portal_registration_received · fub_person_created): tools/harness/run-proofs.mjs — fire the real signal on the bench, assert the log row, the render, the rule evaluation; the activity-harness cage refuses Friday's push without them <sub>*Added 2026-09-11 via the task door.*</sub>

---
*Generated 2026-08-28 by `tools/master-plan/build-floors.mjs` — edit `tools/master-plan/floors.json`, never this page. Tick a box by setting that item's `"done": true`.*
