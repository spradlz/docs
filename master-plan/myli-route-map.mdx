---
title: "The Myli Route Map"
description: "Every place a user can talk to Myli — the before-page, the context she carries, the pills, the branches, and the per-surface typed-question law. GENERATED from lib/myli/route-map.ts, never hand-edited."
---

**[← back to the Master Plan roll-up](/master-plan)** · major build #11

<Warning>
**This page is GENERATED.** It renders from `apps/houseloop-web/lib/myli/route-map.ts` via
`node tools/myli-route-map/build-route-map.mjs`, so the page can never disagree with the record.
Change a pill by editing the RECORD and re-running the generator. A hand-edit here is a second
copy of the map, and second copies drift invisibly — which is the whole reason this estate exists.
</Warning>

## What this is

Zach, 2026-08-28: *"a single data map of every place a user can click on the website to talk to
Myli — the before-page they came from, the context, the pills, the choices, then all of the
off-the-wall things somebody could type and how she handles that in that specific page… a place
you can just look at it and update one pill from that branch."*

And on the shape, 2026-08-29: *"this would this be like a canvas map builder, i'm not sure we need
that, i just need something that maps it out or keeps track of it… its probably code."* So there is
no canvas builder. There is a declaration, and this page.

<Note>
**What a green map does NOT mean.** This page proves the map and the code AGREE about what is
offered where. It says nothing about whether a pill WORKS — a pill wired to a handler with no arm
for it is declared, resolves, and is dead on the screen. Only tapping it finds that, and that is
parity direction 3: the flow judge's TAP CENSUS (`node tools/flow-judge/tap-census.mjs`) drives
every declared pill on the live product — control first, then the tap, then the promised
consequence — and a pill it cannot drive is a NAMED row on its shrink-only floor, never a skip.
</Note>

## The census

| | count |
|---|---|
| Conversation entries found | **21** |
| …mapped (Zach's four surfaces) | 16 |
| …discovered but NOT-MAPPED | 5 |
| Pills declared | **58** |
| Flows they can reach | 9 |
| Modal acts · dashboard acts | 17 · 10 |

### Surface coverage

A surface with no declared pill and no declared typing lane reports **NOT-MAPPED**, never "parity
holds" — an empty map passes every parity check ever written, so absence has to have its own colour.

| surface | route | entries | pills | verdict |
|---|---|---|---|---|
| The home page | `/` | 6 | 17 | **COVERED** |
| A listing page | `/home/[slug]/[hlid]` | 2 | 6 | **COVERED** |
| The results page | `/listings` | 2 | 4 | **COVERED** |
| The client dashboard | `/my-houseloop` | 4 | 26 | **COVERED** |
| The owner page (home value report) | `/my-houseloop` | 2 | 5 | **COVERED** |
| Site chrome (header / CTAs, any page) | `*` | 0 | 0 | ⚠ NOT-MAPPED |

## The entries

### The home page — `/`

#### Ask Myli (the hero button)

<sub>`home-hero-ask` · rendered from `components/home/HomeExperience.tsx:134`</sub>

**Before this tap:** Arrival — the first screen of the site, signed in or not.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | — | — |

`askMyli(undefined)` — the thread opens empty. Nothing travels.

**The pills:** none — this entry is a door, not an offer.

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — There is no home on this entry's envelope, so any house fact she stated would be invented. The honest ask is the answer.
- *Worked example* — they type **"How much is the HOA a month?"** → The honest ask — "great question, which house?" — because no home travels with this entry (Zach's own example, 8/28).

<sub>**Owes the flow judge:** `PER_SURFACE_TYPED_LAW`</sub>

#### The hero search bar

<sub>`home-hero-search` · rendered from `components/home/HomeExperience.tsx:138`</sub>

**Before this tap:** Arrival. They typed instead of tapping.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | — | — |

`askMyli(q)` — the words travel, nothing else. Same brain as a spoken turn.

**The pills:** none — this entry is a door, not an offer.

**They can type here.** Placeholder: *"the hero's own search placeholder"* — A descriptive or unresolvable query is handed to the AI brain — one engine, two surfaces.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — There is no home on this entry's envelope, so any house fact she stated would be invented. The honest ask is the answer.
- *Worked example* — they type **"How much is the HOA a month?"** → The honest ask — "great question, which house?" — because no home travels with this entry (Zach's own example, 8/28).

<sub>**Owes the flow judge:** `PER_SURFACE_TYPED_LAW` · `FUNNEL_REASK_MID_THREAD`</sub>

#### The hero's five

<sub>`home-hero-pills` · rendered from `components/home/HomeExperience.tsx:95-102 (pickPill)`</sub>

**Before this tap:** Arrival. The first thing on the page after the slogan.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | — | — |

A criteria pill carries its OWN search (params) and never opens a thread; a tool pill opens her dialog with its label as the first turn.

**The pills:**

| pill | does | where it lands | retires |
|---|---|---|---|
| **3+ beds under $600k**<br /><sub>`hero-beds3-under-600k`</sub> | quick-pull `beds3-under-600k` | Acks ("Perfect — pulling up 3+ bed homes under $600k right now.") and lands on /listings with the params. Zero questions — the tap-cost law (#23). | — |
| **Big yard**<br /><sub>`hero-big-backyard`</sub> | quick-pull `big-backyard` | Ack, then results. Same zero-question road. | — |
| **School rating**<br /><sub>`hero-school-rating`</sub> | flow `school-rating` | Opens her school-rating flow: the BUYER sets the threshold against the third-party metric — we never characterize (Zach lock 8/26, which is why the words are never “great schools”). | — |
| **Updated kitchens**<br /><sub>`hero-updated-kitchens`</sub> | quick-pull `updated-kitchens` | Ack, then results on `renovated_kitchen`. (It was “Modern kitchens” and went nowhere — no such facet existed.) | — |
| **What can I afford?**<br /><sub>`hero-afford`</sub> | flow `affordability` | Opens her affordability flow — a short dance, never the full funnel. | — |

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — There is no home on this entry's envelope, so any house fact she stated would be invented. The honest ask is the answer.
- *Worked example* — they type **"How much is the HOA a month?"** → The honest ask — "great question, which house?" — because no home travels with this entry (Zach's own example, 8/28).

<sub>**Owes the flow judge:** `TAP_TO_CONSEQUENCE`</sub>

#### Meet Myli (the anonymous mid-page band)

<sub>`home-meet-myli` · rendered from `components/home/HomeExperience.tsx:158`</sub>

**Before this tap:** Arrival, never signed in. The band OCCUPIES the slot a returning client's card would.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | — | — |

`askMyli(undefined)` — an empty thread, same as the hero button.

**The pills:** none — this entry is a door, not an offer.

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — There is no home on this entry's envelope, so any house fact she stated would be invented. The honest ask is the answer.
- *Worked example* — they type **"How much is the HOA a month?"** → The honest ask — "great question, which house?" — because no home travels with this entry (Zach's own example, 8/28).

<sub>**Owes the flow judge:** `PER_SURFACE_TYPED_LAW`</sub>

#### "What would you like to do next?" — the returning ask bar

<sub>`home-returning-askbar` · rendered from `components/home/ReturningAskBar.tsx:17-27`</sub>

**Before this tap:** A returning, signed-in client landing on the home page.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | — | — |

Dispatches `hl:ai-search-open` with `&#123; q &#125;` only. ⚠ `q` is a field name nothing else on the site uses — one of the informal detail shapes this map exists to make visible.

**The pills:**

| pill | does | where it lands | retires |
|---|---|---|---|
| **How is my search going?**<br /><sub>`askbar-how-is-my-search`</sub> | sends words: "How is my search going?" | Her words, in her chat, about their own saved search. | — |
| **Any new homes for me?**<br /><sub>`askbar-any-new-homes`</sub> | sends words: "Any new homes for me?" | Her words about what has landed since they last looked. | — |
| **What can I afford?**<br /><sub>`askbar-what-can-i-afford`</sub> | sends words: "What can I afford?" | ⚠ DECLARED AS IT SHIPS, NOT AS IT SHOULD: this sends WORDS, while the same sentence on the hero (`hero-afford`) runs the affordability FLOW. One concept, two roads — visible here for the first time, and exactly the class of thing the map was built to surface. Not changed by this lane (declaration only). | — |
| **What will closing costs run me?**<br /><sub>`askbar-closing-costs`</sub> | sends words: "What will closing costs run me?" | The grounded general road (rung 3) — no home needed to answer it. | — |

**They can type here.** Placeholder: *"Ask Myli anything…"* — Submits the typed string as `&#123; q &#125;` on the open event.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — There is no home on this entry's envelope, so any house fact she stated would be invented. The honest ask is the answer.
- *Worked example* — they type **"How much is the HOA a month?"** → The honest ask — "great question, which house?" — because no home travels with this entry (Zach's own example, 8/28).

<sub>**Owes the flow judge:** `PER_SURFACE_TYPED_LAW` · `TAP_TO_CONSEQUENCE`</sub>

#### The returning 2×2 (her band)

<sub>`home-returning-strip` · rendered from `components/home/ReturningStrip.tsx:80-99, :501-507`</sub>

**Before this tap:** A returning or first-sign-in client. WHICH set renders depends on the state; pills with nothing behind them are dropped and refilled from the bench so the grid stays four.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | ✅ | — |

The strip knows whether each pill has anything real behind it — that is the `person` half. No home, no flow.

**The pills:**

| pill | does | where it lands | retires |
|---|---|---|---|
| **What can I afford?**<br /><sub>`strip-what-can-i-afford`</sub> | sends words: "What can I afford?" | Opens her chat with the question as the first turn (`p.action.ask`). | — |
| **Homes I've viewed**<br /><sub>`strip-homes-ive-viewed`</sub> | personal pull `viewed` (destination built at the tap) | A per-person destination built from their view history — no params to spell, which is why it is not a hero quick-pull. | — |
| **Show me price drops**<br /><sub>`strip-price-cuts`</sub> | personal pull `price-cuts` (destination built at the tap) | Built from their saved search's own criteria at load. | — |
| **School rating**<br /><sub>`strip-school-rating`</sub> | sends words: "School rating" | Her rating dialog, via the words road. | — |
| **Show me homes near me**<br /><sub>`strip-first-signin-homes-near-me`</sub> | personal pull `near-me` (destination built at the tap) | FIRST-SIGN-IN SET. A geolocated pull — the destination is built at the tap. | — |
| **Steps to buying a home**<br /><sub>`strip-first-signin-steps-to-buying`</sub> | flow `steps-to-buying` | FIRST-SIGN-IN SET. Her steps flow — one of only two flows with real material today. | — |
| **Show me homes near me**<br /><sub>`strip-bench-homes-near-me`</sub> | personal pull `near-me` (destination built at the tap) | THE BENCH. Stands in when a returning pill has nothing real behind it, so the grid stays four without inventing copy (lock #13). | — |
| **Steps to buying a home**<br /><sub>`strip-bench-steps-to-buying`</sub> | flow `steps-to-buying` | THE BENCH, second substitute. | — |

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — There is no home on this entry's envelope, so any house fact she stated would be invented. The honest ask is the answer.
- *Worked example* — they type **"How much is the HOA a month?"** → The honest ask — "great question, which house?" — because no home travels with this entry (Zach's own example, 8/28).

<sub>**Owes the flow judge:** `TAP_TO_CONSEQUENCE`</sub>

### A listing page — `/home/[slug]/[hlid]`

#### Her bar on the house (the three pills)

<sub>`listing-myli-bar` · rendered from `components/listing/MyliListingBar.tsx:108-112, :264-271`</sub>

**Before this tap:** Standing on one specific home — arrived from results, a saved search email, a share link, or search.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| ✅ | — | ✅ | — | — |

⭐ THE ONE ROAD THAT CARRIES THE HOUSE. `tourListing()` builds hlid · street · list price · HOA MONTHLY (null when unknown, never 0) · the dues AS STATED with their period · the annual tax bill · status · city/state/zip · LIVE days on market. The `done` ledger travels with chained flows.

**The pills:**

| pill | does | where it lands | retires |
|---|---|---|---|
| **What should I offer?**<br /><sub>`listing-offer`</sub> | flow `offer-strategy` | Her offer flow on THIS home, reading the real closing statistics for the area. (It used to send its own label at the search brain, which answered “Got it — searching now” and then declined the question — ruling #82.) | `offer_strategy` |
| **What's the monthly payment?**<br /><sub>`listing-payment`</sub> | flow `monthly-payment` | Her payment flow, computed from THIS listing's price and dues. The period conversion happens in code — a walk once had her say “$450 a month” about $450 a YEAR, inflating the payment by ~$412 on the first line a buyer checks. | `monthly_payment` |
| **Request a tour**<br /><sub>`listing-tour`</sub> | flow `request-tour` | Her booking dance in the thread — the conversational version of the page's own blue CTA, ending at the SAME `/api/tour-requests`. ⭐⭐ TWO DIFFERENT DEAD CONTROLS HAVE WORN THE SAME STRANGER SENTENCE HERE ("I tapped request a tour and nothing happened"), ON TWO DIFFERENT ROADS, WITH TWO DIFFERENT MECHANISMS. Keeping them apart IS this note's job: a reader who collapses them is sent to the wrong file the next time either one returns. ── (1) ZACH'S P1, 8/28 walk, verbatim: "when I went to request a tour from inside a specific house it did not load, nothing happened." THE DIAGNOSIS ON RECORD FOR HIS REPORT IS THE ERASURE, AND IT IS NOT THIS PILL — it is the "Request a tour" pill her payment and offer CLOSES draw, INSIDE a running flow. A pill inside a flow is dispatched ONLY through `actFor` in the terminal branch, and `flow:request-tour` was missing from the `FlowAct` union — so the close-drawn pill carried no act at all, `actFor` matched nothing, and the turn came back `say:'' pills:[]`: not a no-op, it EMPTIED her chat at the payoff moment. Cured 2026-08-28 in three welded places (the act joined the union; the three close pills gained it — in the scripted flows module, since RETIRED and deleted on 2026-09-03 under the contract cutover (#256.50a/.59c), so the act now lives on the registry capability; `AiSearchModal` performs it), and the cure was verified LIVE ON PROD 2026-09-01 by judge-rm-tour. The standing ratchet is `TAP_TO_CONSEQUENCE` in `tools/flow-judge/journeys/myli-consumer.journey.json`, which carries his words and its own absence-control. ⚠ Note for anyone re-deriving this: the `actFor(paymentClose(…))` CALL is dated 8/27 and cured a different dead pill ("Discuss offer strategy"); it is a precondition of this road, never the cure of his P1. ── (2) A SECOND, DISTINCT DEAD CONTROL, FOUND 2026-09-01 — this time THIS pill, and ONLY inside the CRM agent frame. `?hlAgentEmbed=1` mounted `MyliListingBar`'s four dispatchers while gating the only host that answers them, so the tap fired `hl:ai-search-open` carrying the right flow and the right home into nobody, and her sheet closed into an unchanged page. Reproduced on PROD at both matrix configs with its control (the identical tap without the frame opens her tour turn), and repaired by mounting the launcher ungated; an unheard tap now also leaves a mark a walk can read (`&lt;html data-hl-myli-unheard>`) instead of dying in silence. STATUS: fixed-in-tree, UNPROVEN-IN-PROD until the desk pushes and the embed road is re-walked. ── ⚠⚠ WHICH ROAD ZACH ACTUALLY TAPPED IS NOT PROVEN, AND THIS ENTRY WILL NOT PRETEND IT IS. Defect (2) has only ever existed inside the agent frame; his walk was the consumer road, where it never existed — which is why (1) is the better-supported attribution and is written first. But nobody has his URL. So this entry claims NO closure of his report beyond what was measured: two roads that each produce his sentence were each found and each cured, one on 8/28 and one on 9/01. | `tour` |
| **Watch it**<br /><sub>`listing-watch`</sub> | sends words: "Watch it" | On a PENDING home: her watch offer for this house — she confirms she'll say when it comes back or closes (the registry's watch_home capability answers). | — |
| **What happened?**<br /><sub>`listing-what-happened`</sub> | sends words: "What happened with it?" | On a SOLD / closed home: what it went for and when, from the record (the registry's what_did_it_go_for capability answers). | — |
| **Similar homes**<br /><sub>`listing-similar`</sub> | sends words: "Similar homes" | On any gone home: homes like this one that are still available (the registry's similar capability answers). | — |

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `home-fact` · `honest-absence` · `grounded-general` · `named-address`
- ⛔ *Never here:* `funnel` — A thread carrying a home may never ask which home, which city, or for a school rating. That re-ask is the amnesia defect verbatim (DESIGN.md §0).
- *Worked example* — they type **"How much is the HOA a month?"** → The figure, from this listing's own dues with its period converted in code — never a number a model produced.

**The decrement (3 → 2 → 1):** retires `offer_strategy` · `monthly_payment` · `tour`. Zach 8/28: the rotation goes 3 → 2 → 1 as each completes. The mechanism is the done-intents ledger (`FlowState.done`), which the closes already read. ⭐ LIVE 2026-09-01: the bar reads it too — `MyliListingBar` listens for `hl:myli-intents-done` and drops the pill whose `retires` key THIS home has completed, so the row decrements in place instead of re-offering the payment a buyer just watched her work out. ⭐⭐ WHERE THE ATTRIBUTION LIVES (r2, and this sentence is the one a later reader will believe, so it names the real holder): each completed intent records ITS OWN HOUSE at the moment it completes, in `FlowState.doneOn` — written beside the boolean at the three flow sites that write `done`, and carried with the conversation state. It is NOT derived by diffing the session ledger: that earlier mechanism asked 'what is newly true in this session?' when the question is 'what did this flow, on this house, just finish?', which are the same question only for the FIRST house of a session — so the button retired on house A and on no house after it, and a reload moved a retirement to whichever house was last discussed. ⚠ THE HONEST LIMITS, NAMED: the per-tab memory itself is unpersisted (`lib/myli/intent-ledger`), but `doneOn` rides the persisted conversation, so a restored thread re-announces with its attribution intact rather than re-crediting the open house; and an intent with NO recorded home (a thread older than the field, or an intent that is not about a house) retires nothing anywhere — a blank beats a guess.

<sub>**Owes the flow judge:** `TAP_TO_CONSEQUENCE` · `PILL_DECREMENT` · `PER_SURFACE_TYPED_LAW`</sub>

#### Ask me anything (the sheet's open lane)

<sub>`listing-sheet-open-lane` · rendered from `components/listing/MyliListingBar.tsx:319`</sub>

**Before this tap:** Inside her sheet on a specific home, having chosen not to tap a pill.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | — | — |

⛔ THE AMNESIA, DECLARED WHERE IT HAPPENS. `askMyli(null, &#123; about: fullAddress &#125;)` — `about` is only read when there is ALSO an `ask`, so opening with no words carries NO HOME AT ALL, and the first typed line reaches the funnel brain, whose entire schema is area/beds/baths/budget. She then asks a buyer standing in a house what city they are looking in. Slice 1 replaces `about` with the home object.

**The pills:** none — this entry is a door, not an offer.

**They can type here.** Placeholder: *"her ask-anything bar in the sheet"* — The typed road, and today the one road on this surface that drops the house at the door.

**If they type something off the wall:**

- *Reachable here:* `home-fact` · `honest-absence` · `grounded-general` · `named-address`
- ⛔ *Never here:* `funnel` — A thread carrying a home may never ask which home, which city, or for a school rating. That re-ask is the amnesia defect verbatim (DESIGN.md §0).
- *Worked example* — they type **"How much is the HOA a month?"** → The figure, from this listing's own dues with its period converted in code — never a number a model produced.

<sub>**Owes the flow judge:** `HOUSE_THREAD_AMNESIA` · `TYPED_HOUSE_FACT_UNANSWERED` · `FUNNEL_REASK_MID_THREAD` · `PER_SURFACE_TYPED_LAW`</sub>

### The results page — `/listings`

#### Her line above the grid

<sub>`results-myli-line` · rendered from `components/listings/MyliLine.tsx:304`</sub>

**Before this tap:** A results page — from a search, a pill, a saved search, or the map.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | ✅ | ✅ |

Carries the results URL (`searchUrl`) so every road out of it stays inside this search.

**The pills:** none — this entry is a door, not an offer.

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — A search is not a house. A fact about one of the results requires the buyer to name which one (rung 4).
- *Worked example* — they type **"Is this a good deal?"** → A market read on THIS search's area, carried on the entry's `searchUrl` — never a fresh 'where are you looking?'.

<sub>**Owes the flow judge:** `PER_SURFACE_TYPED_LAW`</sub>

#### The dock sheet (her face on the results page)

<sub>`results-dock-sheet` · rendered from `components/listings/MyliSearchSheet.tsx:81-152`</sub>

**Before this tap:** They tapped her face on the results page. “Never a scroll-to-top” — the point is that tapping her starts a conversation instead of moving the page under the reader.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | ✅ | ✅ |

`&#123; entry: 'myli-dock-sheet', searchUrl &#125;` — so she answers inside the search rather than asking where they are looking. `hasSavedSearch` and `isOwnSearch` decide the second pill's words.

**The pills:**

| pill | does | where it lands | retires |
|---|---|---|---|
| **How's this market?**<br /><sub>`dock-market-snapshot`</sub> | flow `market-snapshot` | Runs her market tool on THIS search's area — it names the program and hands over the area, rather than sending words at the search brain (ruling #82). | — |
| **Narrow this search**<br /><sub>`dock-refine`</sub> | sends words: "Narrow this search" | STATE: they have a saved search AND this is one of theirs. There is no refine TOOL yet — the pill sends its own label as the buyer's turn, carrying the search URL. The quick-reply shape, and the receipt says so. | — |
| **Narrow it down**<br /><sub>`dock-narrow`</sub> | sends words: "Narrow it down" | STATE: they have a saved search but this is somebody else's search (the browse fork). Same road, honest wording — it may not say “this search” about one that is not theirs. | — |
| **Save this search**<br /><sub>`dock-create-saved-search`</sub> | dispatches `hl:save-search-open` | STATE: no saved search yet. Opens the SITE'S OWN save-search control, not a chat about saving — a pill named for an intent has to serve that intent (style rule 23). | `saved_search` |

**They can type here.** Placeholder: *"Chat with me about something else…"* — The something-else invitation is the BAR'S PLACEHOLDER, never a clause in her sentence (#69). `go(null)` opens her lane carrying the search.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — A search is not a house. A fact about one of the results requires the buyer to name which one (rung 4).
- *Worked example* — they type **"Is this a good deal?"** → A market read on THIS search's area, carried on the entry's `searchUrl` — never a fresh 'where are you looking?'.

<sub>**Owes the flow judge:** `TAP_TO_CONSEQUENCE` · `PER_SURFACE_TYPED_LAW`</sub>

### The client dashboard — `/my-houseloop`

#### The composer in her card

<sub>`dashboard-chat` · rendered from `components/dashboard/MyliDashboard.tsx:4149-4182 (POST at :4023)`</sub>

**Before this tap:** Their own dashboard. She has already spoken an opener composed from their state.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | — | — |

⚠ THE BODY IS `&#123; message &#125;` AND NOTHING ELSE. The route adds `&#123; state, opener &#125;` server-side. There is an envelope and it has no room for a home — the third amnesia (DESIGN.md §1b(b)), and slice 3a's target.

**The pills:** none — this entry is a door, not an offer.

**They can type here.** Placeholder: *"Ask Myli anything…"* — Enter sends, Shift+Enter newlines. The composer is drawn only on a readiness answer — maintenance speaks in her voice where the bar would be, and a suppressed account draws nothing rather than inviting a tap it must refuse.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — There is no home on this entry's envelope, so any house fact she stated would be invented. The honest ask is the answer.
- *Worked example* — they type **"How much is the HOA a month?"** → The honest ask — "great question, which house?" — because no home travels with this entry (Zach's own example, 8/28).

<sub>**Owes the flow judge:** `INVITE_THEN_REFUSE` · `COLD_REFUSAL_VOICE` · `PER_SURFACE_TYPED_LAW`</sub>

#### Her standing offer (the state-driven row)

<sub>`dashboard-standing-pills` · rendered from `lib/myli/state-engine.ts:1346-1830 → lib/dashboard/load-client-dashboard.ts:1685-1869 → MyliDashboard.tsx:1718`</sub>

**Before this tap:** Their own dashboard, in whichever state the engine resolved them into — a tour coming up, a home toured and unjudged, saves with no tour, pre-approved, browsing with nothing saved, or (2026-08-30) a HOMEOWNER with no search at all, who gets her value and the browsing-or-moving fork instead of a buyer's ladder.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | ✅ | — |

The pills arrive as SERVER DATA (`myli.pills`), composed by the state engine and translated act-by-act by `toPill`. ⚠ That translator has explicit arms and NO catch-all: an unmapped act ships as a SILENTLY ABSENT pill — a dead affordance that leaves no trace, which is precisely why direction 3 exists.

**The pills:**

| pill | does | where it lands | retires |
|---|---|---|---|
| **Show me the home**<br /><sub>`dash-show-the-home`</sub> | navigates to that stop's own home page (`/home/[slug]/[hlid]`, built per person); fallback `/listings` when no stop href exists | STATE tourUpcoming with exactly ONE stop (n === 1). Navigates to the tour's first stop's OWN page (`first.href`, state-engine.ts:1644); the match set (`matchesHref`) only when the tour carries no stop href. | — |
| **Show me the &#123;n&#125; homes**<br /><sub>`dash-show-the-n-homes`</sub> | navigates to that stop's own home page (`/home/[slug]/[hlid]`, built per person); fallback `/listings` when no stop href exists | STATE tourUpcoming with TWO OR MORE stops (n >= 2) — the plural arm of the same ternary as `dash-show-the-home` (state-engine.ts:1644). It navigates to the tour's FIRST stop's own page (`first.href`), never the match set; `matchesHref` is reached only when the tour carries no stop href at all. Word-identical to `dash-show-me-the-homes` and a completely different road, which is the whole reason it is its own row. | — |
| **Add another home**<br /><sub>`dash-add-another-home`</sub> | navigates to `/listings` | STATE tourUpcoming. Their matches, to add a stop. | — |
| **Shortlist it**<br /><sub>`dash-shortlist-it`</sub> | dashboard act `verdictHome` | STATE touredAwaitingFeedback, no appointment id. Files the verdict and (when not already saved) saves the home. | `save` |
| **Not a fit**<br /><sub>`dash-not-a-fit`</sub> | dashboard act `verdictHome` | STATE touredAwaitingFeedback. Files the negative verdict; when a must-have is missing she offers to add one. | — |
| **Tell me more**<br /><sub>`dash-tell-me-more`</sub> | dashboard act `noteHome` | STATE touredAwaitingFeedback, BOTH arms. It answers a different want — “I'd rather write it than pick from a list” — and it now REACHES that want: the act opens the ＋ Note composer on this home's own card (the `AttachedStrip` on the toured rail → /api/client-notes), which is the ONE client-note composer in the product. ⭐ TASK #167 IS CLOSED (desk-signed 2026-09-01, cure B). It used to declare `navigate-stop` and land on the home's public `/home/&lt;slug>/&lt;HLID>` page, which renders no note affordance — the want offered and unanswerable at the destination. ⚠ THE `href` IS STILL ON THE PILL and that is not vestigial: a surface with no composer (the CRM mirror, whose home-act handlers are blanked) keeps the honest navigation instead of dropping the pill, the same contract `modalSearch` carries. ⭐ AND THE MAP NAMES THAT ROAD RATHER THAN LEAVING IT UNSPOKEN (r4, order 8b): the href is `stop.href ?? input.matchesHref` — THIS STOP'S OWN `/home/&lt;slug>/&lt;HLID>` PAGE, falling back to the client's match set only when the tour carries no stop href. So on the dashboard the pill is an ACT (the composer opens, nothing navigates) and on the composer-less mirror it is a STOP ROAD to the very home the note would have been about. Two renditions, one destination, both said out loud — because a `state-act` declaration names no road at all, and a real road the record does not name is exactly what the pill-href-road cage exists to catch. ⭐ THE CENSUS FOUND THIS ONE: my first cut of the map declared the two verdict pills and missed the third that renders beside them in both arms, which is exactly the class of omission direction 1 exists to catch (and it caught its author). | — |
| **I'm just browsing**<br /><sub>`dash-owner-just-browsing`</sub> | navigates to `/listings` | STATE ownerFork — a homeowner with no search and no saves. Lands on `matchesHref` (state-engine.ts:2141), which for a client with no saved search IS /listings; the declared base route is the honest floor and the variance is per-person. ⚠ THE PRECEDENT THIS ONCE CITED WAS REVERSED (r3): `dash-show-the-home` now declares `navigate-stop`, because its road is a stop's own `/home/` page — a different fact entirely. This row stands on its OWN reason: `matchesHref` is a `/listings` road for every client, narrowed per person by their saved search, so `navigate /listings` is a true base and never a fallback wearing a road's clothes. She takes the browsing answer at face value and does not convert it into a search behind their back. | — |
| **I'm thinking about a move**<br /><sub>`dash-owner-thinking-about-a-move`</sub> | navigates to `/listings` | STATE ownerFork, the PRIMARY answer. Lands on /listings?ai=first-search — the same base route with her first-search dance armed, so the declared intent to move is where the buyer journey is born rather than a stage flip nobody can see. | — |
| **Request a tour**<br /><sub>`dash-set-up-a-tour`</sub> | dashboard act `tourHome` | STATE savedNoTour. Talk-then-navigate: she speaks, then the home page opens with the tour widget armed. ⭐ THE MAP NAMES THE ROAD (r4, order 8b): the href is `home.href` — THE SAVED HOME'S OWN `/home/&lt;slug>/&lt;HLID>` PAGE, never the results map. That distinction is the whole reason this pill exists: the killed "Schedule a tour" pill's sin was an href pointing at the RESULTS MAP, which dropped a buyer on a page where they had scheduled nothing and still had to pick a house. This one names a house they already picked. The declaration is `state-act tourHome` because the ACT is what does the work; the href is the honest second rendition for a surface that cannot run the act, and a road the record does not name is what the pill-href-road cage refuses. | `tour` |
| **Keep it under that**<br /><sub>`dash-keep-it-under-that`</sub> | dashboard act `profile` | STATE preApprovedActive with a known amount. Opens the must-haves editor. | — |
| **See my letter**<br /><sub>`dash-see-my-letter`</sub> | navigates to `/api/my/pre-approval-letter` | STATE preApprovedActive. Only rendered when a letter actually exists. | — |
| **Yes, all &#123;n&#125;**<br /><sub>`dash-save-all`</sub> | dashboard act `saveHomes` | STATE viewedUnsavedOffer. Saves every viewed-and-unsaved home, with a toggle-restore when a save comes back already-saved. | `save` |
| **Just the one on &#123;street&#125;**<br /><sub>`dash-save-the-one-on`</sub> | dashboard act `saveHomes` | STATE viewedUnsavedOffer. The single-home version of the same act. | `save` |
| **Add a must-have**<br /><sub>`dash-add-a-must-have`</sub> | dashboard act `profile` | STATE browsingNoSaves. Opens the must-haves editor. | — |
| **Show me the &#123;n&#125; homes**<br /><sub>`dash-show-me-the-homes`</sub> | navigates to `/listings` | THE `showHomesPill` ROAD ONLY (state-engine.ts:1329) — the ONE builder four states reuse rather than each spelling its own count sentence. Its href is `input.matchesHref`, the client's own match set, which is why `/listings` is the honest declared base. ⚠ IT DOES NOT COVER THE TOUR'S PLURAL ARM: `state-engine.ts:1644` prints these same words for a tourUpcoming client with two or more stops and goes to `first.href` — a `/home/` page. That arm is `dash-show-the-n-homes`, declared `navigate-stop` above. | — |
| **Calculate your affordability**<br /><sub>`dash-calculate-your-affordability`</sub> | dashboard act `openFlow` | STATE tourUpcoming (pill 3 of the 84.16 four) + appended as the journey chip on any buyer row under 3 pills while the affordability step is open (84.15). Opens the affordability flow IN the one Myli modal (84.18 — never the bare calculator). Web only; the mirror drops it (D13). | — |
| **Chat with me about something else**<br /><sub>`dash-chat-with-me-about-something-else`</sub> | dashboard act `somethingElse` | STATE tourUpcoming, pill 4 of the 84.16 four — Zach's long label, verbatim, on this row only. Opens the something-else beat: his verbatim turn + the suggestion pills + the live composer. | — |
| **Something else**<br /><sub>`dash-something-else`</sub> | dashboard act `somethingElse` | THE CLOSED-LOOP ESCAPE (85.1): appended to every non-empty standing row that lacks one, and authored on the verdict + ownerFork rows. Same destination as the long label above. Web only (D13 on the mirror). | — |
| **What can I afford?**<br /><sub>`dash-something-else-afford`</sub> | dashboard act `openFlow` | Suggestion pill INSIDE the something-else beat (the one sanctioned menu). Opens the affordability flow in the modal. Label = the locked pill-sets vocabulary. | — |
| **Steps to buying a home**<br /><sub>`dash-something-else-steps`</sub> | dashboard act `openFlow` | Suggestion pill inside the something-else beat → the steps-to-buying flow in the modal. | — |
| **School rating**<br /><sub>`dash-something-else-schools`</sub> | dashboard act `openFlow` | Suggestion pill inside the something-else beat → the school-rating flow in the modal. | — |
| **Overland Park**<br /><sub>`dash-anon-overland-park`</sub> | navigates to `/listings` | ANONYMOUS. `areaHref('Overland Park','KS')` — the Explore Areas tile's own road. | — |
| **Leawood**<br /><sub>`dash-anon-leawood`</sub> | navigates to `/listings` | ANONYMOUS. `areaHref('Leawood','KS')`. | — |
| **Another area**<br /><sub>`dash-anon-somewhere-else`</sub> | navigates to `/listings` | ANONYMOUS. Still an ANSWER to the location question — opens the guided funnel's station 0 (“Where are you looking?”) in the one modal, in place, via the first-search arm. | — |

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — There is no home on this entry's envelope, so any house fact she stated would be invented. The honest ask is the answer.
- *Worked example* — they type **"How much is the HOA a month?"** → The honest ask — "great question, which house?" — because no home travels with this entry (Zach's own example, 8/28).

<sub>**Owes the flow judge:** `TAP_TO_CONSEQUENCE`</sub>

#### The tour-feedback pill (her question IS the label)

<sub>`dashboard-tour-feedback` · rendered from `components/dashboard/MyliDashboard.tsx:1308-1323`</sub>

**Before this tap:** They toured a home and have not said what they thought. The pill reads “What did you think of &#123;address&#125;?”

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | ✅ | — | — | — |

`&#123; flow: 'tour-feedback', appointmentId, street, nonce &#125;`. ⚠ THE STREET TRAVELS AS A STRING, NOT A HOME: she knows which address she is asking about and nothing else about it — no price, no dues, no status. A follow-up house question on this thread has no facts behind it.

**The pills:**

| pill | does | where it lands | retires |
|---|---|---|---|
| **What did you think of &#123;address&#125;?**<br /><sub>`dash-tour-feedback`</sub> | dashboard act `tourFeedback` | ⛔ RETIRED FROM THE ROW (#95, desk order 2026-08-30): the question-as-pill is dead — the toured row now renders the three verdict answers + the escape on BOTH arms, and the verdict files in one tap. Flow G's dance + its per-appointment reason writer stay built; this entry records the retired entrance so a revival is a decision, not a drift. | — |

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — There is no home on this entry's envelope, so any house fact she stated would be invented. The honest ask is the answer.
- *Worked example* — they type **"How much is the HOA a month?"** → The honest ask — "great question, which house?" — because no home travels with this entry (Zach's own example, 8/28).

<sub>**Owes the flow judge:** `TAP_TO_CONSEQUENCE` · `HOUSE_THREAD_AMNESIA`</sub>

#### Build my first search

<sub>`dashboard-first-search` · rendered from `components/dashboard/MyliDashboard.tsx:1774`</sub>

**Before this tap:** Their dashboard with no saved search yet.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| — | — | — | — | — |

`&#123; firstSearch: true &#125;` — opens the in-page modal instead of navigating, because a second Myli on one screen is a defect Zach named on 8/13. Falls back to navigation if the dispatch throws.

**The pills:**

| pill | does | where it lands | retires |
|---|---|---|---|
| **Build my first search**<br /><sub>`dash-first-search`</sub> | modal act `first-search` | The build-your-first-search funnel, in the modal. | `saved_search` |

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `grounded-general` · `named-address` · `funnel`
- ⛔ *Never here:* `home-fact` — There is no home on this entry's envelope, so any house fact she stated would be invented. The honest ask is the answer.
- *Worked example* — they type **"How much is the HOA a month?"** → The honest ask — "great question, which house?" — because no home travels with this entry (Zach's own example, 8/28).

<sub>**Owes the flow judge:** `TAP_TO_CONSEQUENCE`</sub>

### The owner page (home value report) — `/my-houseloop`

#### Myli's greeting on the value report

<sub>`owner-myli-greeting` · rendered from `components/dashboard/OwnerDashboard.tsx:608`</sub>

**Before this tap:** The owner's own home value report — they signed in (or tapped their texted link) and hold a resolved home claim. Her greeting is the chat bar: tap a pill and the thread expands DOWN in place, the page staying behind it.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| ✅ | — | — | ✅ | — |

THE THREE-LEG ENVELOPE the mint ruling ordered (Zach 8/28 ~19:40: 'a myli who knows everything about your house, and the equity and what has sold nearby'), all handed to her as props computed ONCE on the server (OwnerDashboard.tsx:608-655) from the SAME variables the page's own instruments render, so her numbers and the page's cannot disagree: (1) THE HOUSE — the claim's own address/HLID (`homeName`), its value picture (estimate/low/high, the value month, gain since purchase, appreciation, the monthly move, at-all-time-high); (2) THE EQUITY — `loanBalance`, the SAME balance the net sheet reads; (3) WHAT SOLD NEARBY — the `nearby` window (lib/dashboard/sold-nearby.ts) plus `yourSqft`, the fact that turns a neighbour's price into a sentence about THEIR house. Her opening story (`chatLines`) is composed server-side from those values. `recordSale` is ALWAYS null today (no record-sale source exists; MarketStat holds median/mean only), so the record arm has never rendered. ⚠ TYPING IS NOT LIVE ANYWHERE ON THIS SURFACE: where a composer would be, a chip says 'I can't take typing in this thread yet — tap an option to keep going' — one sentence, every thread, which is why freeText is null and the ONE_ANSWER_ABOUT_WHETHER_SHE_CAN_HEAR_YOU assertion exists.

**The pills:**

| pill | does | where it lands | retires |
|---|---|---|---|
| **What would I pocket if I sold?**<br /><sub>`owner-pocket-if-sold`</sub> | opens the owner chat's calc thread in place | Opens the in-chat net-sheet calculator thread in place (first slot on the row — the highest-intent control on the owner's screen leads it, both viewports). The thread runs the SAME netSheet() the page's own playable sheet runs, so the two cannot answer one question two ways; its one closing TURN pill is the drawing's 'Add my loan balance' (CALC_CLOSING_PILLS in the component) → requestLoanBalance(), which scrolls to and opens the page's ONE balance receiver. The same words also serve as the sold thread's answer pill and the chat thread's onward pill — turn pills, owned by the component (the map declares where conversations start; the threads own their turns). Words: CHAT_ONWARD_CALC, welded by route-map.test.ts. | — |
| **Show me the record sale**<br /><sub>`owner-record-sale`</sub> | opens the owner chat's chat thread in place | THE ARM THAT HAS NEVER RENDERED, declared so the map does not read as if the slot were only its fallback: it mounts only when `recordSale` is non-null, and OwnerDashboard passes null for every owner — there is no record-sale source (MarketStat stores median and mean, never a max) and Heartland §12.3b bans the per-address sold price the lock drew. The slot's second pill below is the deterministic fallback that actually renders (the lock's own WF 956-961 rule: the surface drops rather than lies). NOT retired — retired means killed by decision; this is unbuilt by absence of a source, and the day one lands the declaration is already here. | — |
| **What’s sold near me**<br /><sub>`owner-sold-nearby`</sub> | opens the owner chat's sold thread in place | The neighbourhood slot's live rendition, three branches at the one render slot: with real nearby sales held, it opens the sold thread in place — she compares the middle price and size against THEIR house and ends on the calculator question; with no nearby window but a city on file, it is an &lt;a> straight to the city's sold list, carrying the owner's identity on the URL (`nearHome` + `nearHomeId`, the authority half, built beside the dashboard's own roads); with neither, an aria-disabled span titled 'No sales area on file' rather than a dead link. The words are ONE exported string spoken at four sites (owner-sold-thread.ts's own note): this pill, the owner's echo bubble, the story feed's door (the entry below), and the chat thread's onward row. | — |
| **Homes for sale near me**<br /><sub>`owner-browse-near-me`</sub> | navigates to `/listings` | An &lt;a> to `/listings?city=&lt;their home's own city>` (the dashboard's browseHref) — the declared base route is the honest floor and the per-person city is the variance, the dash-owner-just-browsing precedent verbatim. The lock's half-mile radius is not buildable (the search envelope never implements `radius`), so the road goes somewhere real and wider, named as a leave in the component. With no city on file it renders the aria-disabled span 'No search area on file'. Words: CHAT_ONWARD_BROWSE, welded by route-map.test.ts. | — |
| **Chat with me**<br /><sub>`owner-chat-with-me`</sub> | opens the owner chat's chat thread in place | The soft pill. Opens the chat thread in place: her composed story (value delta, all-time high, the monthly move — every number a prop the page also renders), then the onward TURN row DERIVED from what this owner can actually reach (the calculator always; the sold thread or the sold-list road when one exists; browse when a city exists) — an option with no destination is never rendered dead, the r5 O34 ruling. | — |

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `home-fact` · `honest-absence` · `grounded-general` · `named-address`
- ⛔ *Never here:* `funnel` — A thread carrying a home may never ask which home, which city, or for a school rating. That re-ask is the amnesia defect verbatim (DESIGN.md §0).
- *Worked example* — they type **"How much is the HOA a month?"** → The figure, from this listing's own dues with its period converted in code — never a number a model produced.

<sub>**Owes the flow judge:** `EVERY_GREETING_PILL_VISIBLE_OR_AFFORDED` · `ONE_ANSWER_ABOUT_WHETHER_SHE_CAN_HEAR_YOU` · `UNSIGNED_ASK` · `UNOWNED_WE` · `REPEATED_ASK`</sub>

#### The story feed's door into the sold thread

<sub>`owner-feed-sold-door` · rendered from `components/dashboard/SoldThreadCta.tsx:24`</sub>

**Before this tap:** The same report, ~3,000px down its ONE document, in the story feed — too far from her greeting for a shared click handler.

**What she carries (the envelope):**

| home | flow | done-intents | person | search |
|---|---|---|---|---|
| ✅ | — | — | ✅ | — |

NOT A PILL — a control declared so the map shows every way this conversation opens (the results-map-listener discipline: a seam the map omitted would read as if only the pills existed). `SoldThreadCta` dispatches `hl:owner-sold-thread` and scrolls to the chat anchor; `OwnerMyliChat` is the ONE listener (the one-door-in rule, judge order O2 — a second control opening a lookalike thread is the defect it exists to prevent). Its words are the SAME exported string as the owner-sold-nearby pill (owner-sold-thread.ts:28), deliberately: one act, one sentence, however many doors. ⚠ The pill row resolves its words through the one reader; this door and the echo bubbles speak the constant — the one-edit-reaches-all authority question is the label-collision floor's open O5, at the desk.

**The pills:** none — this entry is a door, not an offer.

**They cannot type here** — pills only.

**If they type something off the wall:**

- *Reachable here:* `home-fact` · `honest-absence` · `grounded-general` · `named-address`
- ⛔ *Never here:* `funnel` — A thread carrying a home may never ask which home, which city, or for a school rating. That re-ask is the amnesia defect verbatim (DESIGN.md §0).
- *Worked example* — they type **"How much is the HOA a month?"** → The figure, from this listing's own dues with its period converted in code — never a number a model produced.

<sub>**Owes the flow judge:** `NEARBY_COUNT_AGREES_WITH_ITS_DESTINATION_OR_NAMES_THE_DIFFERENCE` · `MIDDLE_PRICE_TRUE_ACROSS_THE_HOP`</sub>

## Discovered and NOT-MAPPED

An entry we have not mapped is **named here**, never omitted — the `STATE_NOT_REACHED` discipline
applied to a map. An absent entry is indistinguishable from one nobody ever found.

### ⚠ The header's Ask-Myli button (desktop)

<sub>`site-ai-search-top-button` · Site chrome (header / CTAs, any page) · `components/site/AiSearchTopButton.tsx:28`</sub>

**Why it is not mapped:** Outside Zach's four surfaces. It also carries no detail, so its envelope is empty on EVERY page it appears on — including a listing page, where the amnesia is worst. Mapping it means deciding what a page-agnostic entry should carry, which is a design question this slice does not own.

**What it carries:** ⚠ `new Event(...)` — a BARE event with NO DETAIL AT ALL. Whatever page they are on is discarded at the door.

### ⚠ The AI-search CTA (server-rendered pages)

<sub>`site-ai-search-cta` · Site chrome (header / CTAs, any page) · `components/search/AiSearchCta.tsx:16`</sub>

**Why it is not mapped:** Outside Zach's four surfaces; same empty-detail question as the header button.

**What it carries:** ⚠ Also a bare `new Event(...)`, no detail.

### ⚠ Her picks page

<sub>`home-picks-page` · The home page · `components/home/PicksPage.tsx:58`</sub>

**Why it is not mapped:** A sub-page of the home surface, not the home screen Zach walks. Its envelope is a nonce, so it inherits the home screen's law with nothing added — cheap to map next, and named here so it cannot be forgotten.

**What it carries:** `&#123; nonce &#125;` only — a fresh thread, no context.

### ⚠ The new-today page

<sub>`home-new-today-page` · The home page · `components/home/NewTodayPage.tsx:49`</sub>

**Why it is not mapped:** Same class as the picks page.

**What it carries:** `&#123; nonce &#125;` only.

### ⚠ The map's own Myli listener

<sub>`results-map-listener` · The results page · `components/listings/ListingsMap.tsx:905`</sub>

**Why it is not mapped:** A LISTENER, not an entry — it decides who OWNS the modal when an entry fires, which is a different fact from where a conversation starts. Declared here because the double-modal defect lives at exactly this seam and a map that omitted it would read as if only one listener existed.

**What it carries:** ⚠ A THIRD LISTENER for one event (with the launcher and the home experience). Two mounted launchers once put TWO full-viewport modals at z=100 on one screen, both rendering the flow — a recorded walk finding.

## How this map is kept honest

| direction | the question | the instrument | today |
|---|---|---|---|
| **1 — CODE → MAP** | is anything offered that nobody declared? | `node tools/myli-route-map/undeclared-pill.mjs --cage` — shrink-only floor | **at 0** for the declared scope |
| **2 — MAP → CODE** | does everything declared actually exist? | `npx tsx lib/myli/route-map.test.ts` + the compiler | **green** |
| **3 — DECLARED → EXERCISED** | does the pill actually DO the thing? | the TAP CENSUS: `node tools/flow-judge/tap-census.mjs` — every declared pill driven (control → tap → promised consequence), shrink-only floor `tools/flow-judge/tap-census.floor.json` | **live** (slice 4, 2026-08-31) |

Directions 1 and 2 would both be **green on a pill that does nothing** — the declaration is present
and the target exists. Only direction 3 finds that, and it is the one that would have caught the P1
(Zach, 8/28: *"Request a tour" inside a specific house did nothing*). The census reports every
declared pill with a named status (CONSEQUENCE_OK · TAP_DEAD · INCONCLUSIVE_CONTROL ·
PILL_NOT_ON_SCREEN · STATE_NOT_REACHED · NOT_DRIVEN); a pill no standing persona can reach lives
ON the floor, by name, with its reason — visible, never absent.

### What is editable, and what ships through the gates

A **wording** change will be a live edit once slice M-b moves pill copy into `hl_templates` keyed by
pill id (with the code literal as the fallback, so a deleted row can never blank a button). A
**branch** change is a code change and ships through the gates — behavior editable from a page with
no gate is the hidden-middleware shape `WORKFLOW_CANVAS_IS_TRUTH` forbids.

