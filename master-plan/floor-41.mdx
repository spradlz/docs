---
title: "Floor 41 — The Mobile App MVP — the group thread and user profiles"
description: "Your own workflow, made into the product: you, Ana and Trish work out of a group text; Myli sits in that thread, extracts the facts, updates the lead and sets the next follow-up. E"
---

**[← the punch list](/master-plan/punch-list)** · **[the 12-week schedule](/master-plan/schedule)** · [week 2 — The Cascade](/master-plan/week-2) · 🟡 WORKING — **13/34** punch-list items done

## What this floor is

> Your own workflow, made into the product: you, Ana and Trish work out of a group text; Myli sits in that thread, extracts the facts, updates the lead and sets the next follow-up. Every teammate gets a user detail page that looks exactly like a lead detail page — their appointments and tasks on the left, their whole conversation inbox in the middle, Myli on the right.

Three bands, and they are ordered by dependency, not by appetite. **Band A is the drawings — and they are LOCKED as of 2026-08-24**, Zach verbatim: *"yes the mobile app group thread drawing is locked."* The port now builds from `docs/wireframes/mobile-groupthread-LOCKED-2026-08-24.html` node-by-node, because the wireframe is the truth and a build from prose is how the TC bands went wrong in July. **Band B is the foundation** — seats, the in-app channel, the user page, Myli in the room. **Band C is the group-text capture** — making the real iMessage group we proved on 8/23 file correctly into the CRM.

The architecture underneath was already signed and is already partly built: the seat model (one fact, a seat per person it touched, so the thread, the lead record and every user profile read the same rows), the /m shell, the partner-compose road, and AI containment — which means the in-app channel arrives deny-by-default for AI and reaches a person only through the one gate.

What this floor newly creates: the **user-profile estate**, the **Negotiator role** (your wife writes the offers — that role does not exist today), the **in-app message channel**, and **Myli DM threads** per user.

## The record behind it

- **Zach's vision, captured near-verbatim — the source document** — `.claude/reports/mobile-mvp/ZACH_GROUP_TEXT_VISION_SOURCE.md` — where this and any spec disagree, the source document wins
- [The Mobile MVP spec](/guides/mobile-mvp-spec)
- **The foundation design — 7 slices, zero new tables** — `.claude/reports/mobile-foundation/DESIGN.md`
- **TEST B — the live group-iMessage proof** — `.claude/reports/builder-testb/RECEIPT.md`
- **The transport research — why iMessage and not Twilio group-MMS** — `.claude/reports/grouptext-transport/FINDINGS.md`
- **Zach's rulings, dated and near-verbatim — including the 8/24 lock** — `.claude/reports/myli-state/ZACH_STATE_DICTATION_2026-08-23.md` — the §THE MOBILE GROUP-THREAD WIREFRAME: LOCKED entry is the receipt for the lock
- **The user detail page — the three-lane build, 63 assertions, 4 walk shots (8/25)** — `.claude/reports/builder-userdetail/RECEIPT.md` — the receipt behind the four ticked slice-3 boxes
- **⛔ The group-bind re-measurement — why a test-line fan-out receipt would be a false green** — `.claude/reports/builder-groupsms/RECEIPT.md` — all three owned lines return Twilio 400 / 50407; the log is under that lane logs/

## 📐 The signed drawings — the wireframe IS the spec

**Mobile group thread — the six screens — LOCKED 2026-08-24** — repo: `docs/wireframes/mobile-groupthread-LOCKED-2026-08-24.html`

![Mobile group thread — the six screens — LOCKED 2026-08-24](/images/wireframes/groupthread-DRAFT-2026-08-23--phones.png)

**The user detail page — the sixth screen** — repo: `docs/wireframes/mobile-groupthread-LOCKED-2026-08-24.html`

![The user detail page — the sixth screen](/images/wireframes/groupthread-DRAFT-2026-08-23--userpage.png)

**Mobile inbox — LOCKED 2026-07-26 (the family these screens must match)** — repo: `docs/wireframes/mobile-inbox-LOCKED-2026-07-26.html`

![Mobile inbox — LOCKED 2026-07-26 (the family these screens must match)](/images/wireframes/mobile-inbox-LOCKED-2026-07-26--desktop.png)

**Mobile comms — LOCKED 2026-07-25** — repo: `docs/wireframes/mobile-comms-LOCKED-2026-07-25.html`

![Mobile comms — LOCKED 2026-07-25](/images/wireframes/mobile-comms-LOCKED-2026-07-25--desktop.png)

## The punch list — what completes this floor

### The drawings — LOCKED 2026-08-24, the port builds from the file — 6/10

- [x] Draw the six screens and put them in front of Zach — *8/23 — his verdict: "This thing's freaking god-tier level amazing… There's nothing I would change on it", with one addition and one fork answered*
- [x] Add the avatar-tap affordance: every round circle in the thread header routes to the mobile lead-detail view — *inside the lock — the 8/23 redline is carried in the locked file* `MON 8/24`
- [x] Add the lead SEARCH entry to screen ① — search a lead, get their most recent conversation, tap the circle for the profile — *inside the lock — the 8/23 redline is carried in the locked file* `MON 8/24`
- [x] Add the fork text to the drawing: one standing team thread AND a thread per client, both over one seat estate — *inside the lock — BOTH thread models are drawn: one standing team thread AND a thread per client* `MON 8/24`
- [ ] Apply the held disposition: rename 'Threads' to 'Inbox' across all 7 sites — *The RULING landed (#48 → the word is Inbox) and the app chrome is applied, but the locked wireframe still says "Threads" in 8 places — and the item says the drawing comes first.* `WED 8/26`
- [ ] Fill in Trish's real user record — surname, cell, email — in place of the wireframe placeholders — *Untouched — the drawing still carries a MADE-UP phone number for a real person, and its own note still lists the placeholders to replace.* `WED 8/26`
- [ ] Re-shoot both PNGs after the redline pass and put the pair in front of Zach — *Not done — both PNGs are still dated Aug 23 with DRAFT filenames, so the pictures on the page still predate your redlines.* `WED 8/26`
- [x] ⛔ **BLOCKS** — Say 'locked' on the redlined wireframe — *LOCKED 2026-08-24, verbatim: "yes the mobile app group thread drawing is locked"* `MON 8/24`
- [ ] Derive the spec node by node from the locked HTML and strip the DRAFT banner — *⚠ I UNDER-REPORTED THIS ONE EARLIER AND THE CORRECTION IS THE POINT: the HARD half is DONE — a real 134-row node-by-node mapping table derived from the locked HTML across six screens (`.claude/reports/builder-mobilethread/MAPPING-TABLE.md`, commit `77ed6befe0`). What is actually left is a two-line text edit stripping the DRAFT banner and renaming two PNGs. My earlier "not touched" was wrong.* `WED 8/26`
- [x] Rebuild the mobile app’s navigation, name the speakers, and un-bury Send — *Lane receipts: .claude/reports/builder-mobileia/RECEIPT.md (the navigation) with .claude/reports/builder-mobileport/RECEIPT.md and .claude/reports/builder-mobileapp/RECEIPT.md the same day. Checked-at mined from the navigation receipt's commit.* `TUE 8/25`

### The foundation slices — seats, the channel, the user page — 6/14

*Seven slices, each a query or a transport over the one below it. The finding that shaped the design: the seat model shipped its EMAIL slice and stopped — so this is zero new tables, all the way up.*

- [x] Slice 1 — the room and its members: seat every person on a thread, replay-safe, leaving is a timestamp not a delete — *8/23 GREEN, red-first: before the unique index one person held 3 seats on one thread — listed twice in the roster and notified twice for every message*
- [x] Step 2 — teach the one send door to deliver inside the app, and ban the raw sender by name in the same change that adds it — *8/23 GREEN — an AI-composed send with no drawn move is refused and writes no message row at all; containment refuses before membership*
- [x] Step 3 — build the teammate profile page and mount the three existing lanes on it — *DONE 8/25 — commit `de7a1d0ef0`. One lane became three: `UserCommsLane.tsx` (middle) + `UserMyliLane.tsx` (right) + the ask bar. 63 assertions in `lib/ai/user-ask-guard.test.ts`, 4 walk shots in `.claude/reports/builder-userdetail/shots/`. Zach walked it and signed the MVP (redline U7). Receipt: `.claude/reports/builder-userdetail/RECEIPT.md` 🟦 BUILT BUT NOT ON PROD — commit `de7a1d0ef0` is not an ancestor of origin, so this is real, finished work that cannot be verified live until the push lands. It is not missing.* `TUE 8/25`
- [ ] Lift the lead-detail grid into a named shell so the user page and the lead page share it — *Confirmed not done, and WORSE than first measured: the user page and the lead page each hand-roll their OWN three-column grid with DIFFERENT column ratios, so they will drift apart on the next edit to either one.* `WED 8/26`
- [x] Render a teammate's texts lane on their user page, from their seats — *DONE 8/25 — `lib/comms/user-inbox.ts`: the subject-scoped thread read, rooms gated on the SUBJECT's seat plus the subject's lead conversations. Each empty state says WHICH measurement is empty rather than collapsing three different claims into one blank. Receipt: `.claude/reports/builder-userdetail/RECEIPT.md` 🟦 BUILT BUT NOT ON PROD — commit `de7a1d0ef0` is not an ancestor of origin, so this is real, finished work that cannot be verified live until the push lands. It is not missing.* `TUE 8/25`
- [x] Render a teammate's appointments lane on their user page — *DONE — it landed 8/24 in the LEFT lane (`UserLane.tsx`, "Upcoming appointments"), not today. ⚠ The drawn Appointments DOCK TAB was deliberately NOT built: the left lane already renders them, and a fourth tab would put one fact in two places on one screen. Desk ruling wanted on whether the tab is still wanted.* `TUE 8/25`
- [x] Mount Myli on the right rail of the user page by reusing the existing DM surface — *DONE 8/25 — Myli is on the right rail (`UserMyliLane.tsx`). ⚠ NOT by reusing the existing DM surface, and the deviation is the finding: `/api/leads/[id]/myli-chat` is hard-bound to a leadId (`composeAiMessage` → `loadLeadContext(leadId)`), and `/api/internal/dashboard-chat` is the CLIENT road — an agent asking "do I have appointments this week?" would have filed on some client's record as if the client said it. Built instead as a read-only user-scoped context packet through the shared model router, carrying the AI-Systems compute gate. Its toggle (`ask_myli`, mig 0870) was proven to gate at the wire, not cosmetically. 🟦 BUILT BUT NOT ON PROD — commit `de7a1d0ef0` is not an ancestor of origin, so this is real, finished work that cannot be verified live until the push lands. It is not missing.* `TUE 8/25`
- [ ] Slice 4 arm G, red-first: a non-test app recipient must refuse and name the clause that refused it `WED 8/26`
- [ ] Step 4, test F — an in-app message from Myli with no drawn step behind it is refused, not sent `WED 8/26`
- [ ] Re-point the drawn partner-ping move at the app channel `WED 8/26`
- [ ] Run Zach's own test end to end: Myli asks Trish for availability in the room, Trish replies, and it lands in one thread and on all three user profiles and the lead — *this is the acceptance bar he stated himself — the whole floor is judged here* `WED 8/26`
- [ ] Slice 5 — the Myli DM: a private two-seat thread per user — *"each user — lender, TC, everyone — has their own ability to text Myli directly"* `THU 8/27`
- [ ] Slice 6 — push notifications, all eight steps, with the '(coming)' label coming off in the same change that ships the reader — *the dark machinery from 8/23 is necessary and a long way from sufficient*
- [ ] Slice 7 — the extraction listener: it can read the room and it cannot speak — *deliberately last — the only arm that writes facts nobody typed*

### The group-text capture — making the real group file correctly — 1/10

*TEST B settled the product question on 8/23: Myli spoke into a group, an app-less line received it, and Zach's reply reached that line. It is a real shared group. What it also proved is the danger: on the first send everything we can query read green — ok:true, participants(2), error:0 — and the member received nothing.*

- [x] Prove a real shared group forms and a reply reaches an app-less member — *8/23 — Zach's "Hey, got the message" at 23:11:11 reached the app-less line; the group is reused, not re-minted (112 → 114 chats on send #1, zero new on send #2)*
- [ ] Key the thread estate on the bare chat identifier, never the returned GUID — *the service prefix flips and would split one thread's history in two* `WED 8/26`
- [ ] Resolve and persist the seats from the chat identifier at capture — *the inbound names the sender and never the membership; dedupe, because the prefix match returns two rows* `WED 8/26`
- [ ] Fix the echo path: write the real membership instead of the literal "0", set the lead from the filing predicate instead of null, stop re-stamping the actor as an AI agent, and stop defaulting every group member to 'lead' — *four wrong fields on every group send today — which is why a group send renders on no lead's feed* `WED 8/26`
- [ ] Build a per-member delivery assertion that can tell a delivered member from a silently excluded one — *nothing we can query today distinguishes them — this is the requirement the danger model bought* `WED 8/26`
- [ ] Add the one-fact-many-seats dedup so a group containing a number we own stops writing the same utterance twice — *Zach's one reply landed as two message rows 2.8 seconds apart, with different recipients recorded* `WED 8/26`
- [ ] Give the thread estate a group-iMessage kind, take the chat id into its external id, and fold the old participant table into the one seat table — *one thread estate, two transports — partners on iMessage, leads on the registered long code, transport-agnostic from the first commit* `WED 8/26`
- [ ] Make the group-send allowlist fail closed instead of falling back to a hard-coded pair of old test lines — *deleting the settings row today silently swaps in a different allowlist* `WED 8/26`
- [ ] Detach the group-SMS test lines from fixture lead records — *a fixture lead holds Myli's own line and another holds Zach's cell; it produced a notification naming the wrong person during the test* `WED 8/26`
- [ ] Fix Text Message Forwarding on Myli's iPhone — *needs physical access; until it is fixed the rig has no self-service real-handset sender and every group run needs Zach's thumbs*

## The desk's running notes

*Verbatim from the desk board, absorbed here when the board retired into the schedule tree. Receipts, not plans — the checkboxes above are the plan.*

**Floor 41 — THE MOBILE APP MVP** (the group thread + user profiles — [full punch list](/master-plan/floor-41)):
- [x] **Band A drawings + Zach's redline ruling** (8/23): *"There's nothing I would change on it"* + avatars-are-the-road + the fork answered BOTH
- [x] **Band B slices 1-2 GREEN** (mobile-foundation): thread seats (member role, replay-safe, red-first — one person had held 3 seats on one thread) · the in-app channel behind `deliver()` (an AI send with no drawn move is refused and **writes no message row at all**)
- [x] **Band C: TEST B PASSED LIVE** (8/23) — a real shared group forms and Zach's reply reached the app-less Twilio line. ⛔ And it measured the danger: on send #1 every signal read green (`ok:true`, participants(2), `error:0`) **and the member received nothing**
- [x] **THE DRAWING IS LOCKED (8/24)** — Zach verbatim: *"yes the mobile app group thread drawing is locked."* Pinned as `mobile-groupthread-LOCKED-2026-08-24.html`; the avatar-tap, lead-search and both-thread-models redlines are inside the lock. ⚠ The rendered PNGs still carry the DRAFT filenames — re-shooting the pair is still open. · - [ ] slice 3 user detail page · - [ ] slice 4 + **Zach's own end-to-end test** · - [ ] the group-capture fixes (bare chat id, seats at capture, the echo path's four wrong fields, the per-member delivery assertion)

## ⭐ What only Zach can decide on this floor

- **Are you and Ana in the thread by your REAL CELL NUMBERS, or app-only?** — if app-only, Trish's phone shows a 1:1 with Myli's number no matter what we build — a legitimate product, but not what 'one group text thread' means from her side, and nobody should ship it under that name
- **Which number is Trish's?** — the shared showing-partner config names Trisha against a number that is Myli's own line, while the real Trish sits on a different number — two numbers under one name, and it blocks Slice 4
- **Does Trish see the whole lead, or only the room?** — a seat-holder can read the room; whether she can read every fact she holds a seat on is an authorization question the design refuses to invent an answer to
- **Did the pure-iMessage first message ever appear on your phone?** — we cannot see it from our side — it decides whether an iMessage-only send is ever safe

---
*Generated 2026-08-25 by `tools/master-plan/build-floors.mjs` — edit `tools/master-plan/floors.json`, never this page. Tick a box by setting that item's `"done": true`.*
