---
title: "⭐ Caleb Master (WIP) — Past-Client Reconciliation & the Seller Report"
description: "The live master sheet for Caleb's lane: the END GOAL, what is DONE with receipts, what is in flight today, and the two keys Zach holds. Rewritten 2026-07-21 — the Jul-7 version is git history."
---

<Info>
**Last updated 2026-07-21 (morning).** The END GOAL + the G0-G5 game plan live at the TOP of the
[SRG Reconciliation Engine master](/guides/srg-reconciliation-engine) — that guide is the source of truth;
this page is the fast lane-status view.
</Info>

## 🎯 The end goal (Zach LOCKED 2026-07-20)
Every past client completely reconciled → ONE perfect, guarded HouseLoop ID card per home → **the Seller
Report (Home Value Report)** rendered three ways from the same truth (CRM enriched cards + seller dashboard,
and the Owner's View on their own `/home` page) → closed by the **monthly loop**: the HVR email with a
personal agent message + a text with the clickable report.

## ✅ DONE (receipts in the guide + `.claude/reports/`)

| Piece | State |
|---|---|
| Identity backbone | 1 HLID per rooftop · home URLs 100% (sellable book) · fork/twin sweeps done |
| Agents | **2,106/2,106** — MLS-ID law (ANASPRAD/SPRADLINGGP; LA2 = individual; departed = lost) |
| Disposition | **2,106/2,106, zero flags, contradictions 0** — per-home, golden-gated 8/8, rooftop-life anchored |
| Rooftop MLS lives | every past-client rooftop's FULL sale history banked (`hl_mls_property_history`) |
| Client-facts table | live VIEW `hl_srg_client_transactions` — SISU receipts 100%, MLS 95.8% · [the artifact](https://claude.ai/code/artifact/dcc92d3d-7322-45fb-8af2-13b09b1b6c0c) |
| Full-book FUB re-scrape | **COMPLETE 2026-07-20**: 1,668/1,686 fresh + 18 deleted-in-FUB (history final) · mirror verified hot |
| Rooftop guard (Inngest) | **DEPLOYED + first run SUCCESS** — 1,921 rooftops, 22 keepers repointed, run `01KY16D3…` in the Run Log |
| Status-tool lies | crash-heuristic DELETED from `fub_backfill_status` · vocabulary ban in the SSOT + tool descriptions |
| Docs | the 15-day render-crash class (percent-in-heading) found + fixed on 3 top pages; generator auto-sanitizes |

## 🟡 IN FLIGHT (today)
1. **DOSSIER REBUILD, raw-only** — the gate is OPEN (fresh mirror). Harvest v2 reads EVERYTHING (emails
   included, uncapped) → stage2 under the no-dogfood contract → persist across the book.
2. **Card-hydration sweep** — verify every past-client `/home` page renders current (photos · pill · price
   history · value present), data-checked book-wide + eye-sampled, fixes routed through the real pipelines.

## 🔑 The two keys Zach holds
1. **Guard sign-off** — eyeball run `01KY16D3NAPMSFD9VCE3GWAR7F` (Run Log / Inngest) → typed OK → the daily
   6am cron flips on and rooftops are guarded hands-free.
2. **R3 value refresh green light** — re-anchor web values to CURRENT Zestimates book-wide + kill every
   CRM-copy value read. The last step between "reconciled" and "reconciled AND current."

## 🗺️ Then the road (G2 → G5, in order, gates in the guide)
HVR design (wireframe → Zach locks pixels) → CRM surfaces (Past-Client Report cards + Seller Dashboard) →
Owner's View (owner mode on `/home`) → the monthly loop live (email + personal context block + SMS, Inngest,
one clean observed month before full-book send).
