---
title: "Chapter 9 — Where this is going (FUB staging & import, the PRD)"
description: "The mirror is the tape; a complete pre-derived staging layer is the truth the CRM imports from. Import is a dumb copy — all thinking happens in staging, visibly and re-runnably. Signed direction: Zach 2026-08-17 (ledger 315)."
---

> **← [Chapter 8 · The operator's manual](/fub/master-scraper-ssot)** · you are on the last chapter ·
> **[back to chapter 1](/fub/fub-pipeline-board)**
>
> Chapters 1-8 describe the pipeline as it stands. This one describes the shape it is being built
> toward — signed direction, not yet the running system.

> **THE LAW (Zach, 2026-08-17, verbatim):** *"we should not be deriving anything without it already
> derived and structured properly in the fub mirror… the fub mirror should be a staging table for
> the houseloop crm."* This extends Bedrock **Decision D3** (raw tape → staging → import) from
> events to PEOPLE: nothing is figured out at import time. Ever.

## The three layers, one sentence each

| layer | what it is | may it be edited? |
|---|---|---|
| **THE TAPE** — `fub.*` | The verbatim mirror of Follow Up Boss: people, events, notes, calls, emails, team members. | **Never.** It is evidence, not an answer. |
| **THE STAGING LAYER** — the subject of this PRD | One PRE-DERIVED row per FUB person carrying every answer the CRM needs, computed by re-runnable jobs. | Regenerated by its jobs, never hand-edited. |
| **THE IMPORT** | A dumb copy from staging into the CRM through the sanctioned writers (the pour for past clients; any future lead import). | It reads staging ONLY — never the raw tape, never derives. |

## Why (the disease this kills)

Every import that "thinks" at import time re-litigates the same questions — identity, agent
history, household, DNC — and two imports can answer them differently. That is the circles Zach
named on the resolver (two months of re-resolving). The cure is structural: **all derivation runs
BEFORE import, lands in staging where a human can read it, and the import copies what staging
says.** A wrong answer is fixed by fixing the staging job and re-running it — visibly — not by a
smarter import.

## The staging person row (the target shape)

One row per FUB person. Every column is DERIVED by a named, re-runnable job, with provenance:

| column group | carries | derived from (the job) |
|---|---|---|
| **identity** | resolved person verdict (`household-resolved` / `identity-confirmed` / `wrong-human` / `unresolved`), the evidence citation | the triangulation method (ledger 304): dotloop client parties → SISU → `fub.notes` → the contact census |
| **contact census** | EVERY email + phone on the record, each tagged person-vs-household (an email names a person; a phone names a household) | `fub.people` parse + the household artifact rules (ledger 311) |
| **agent relationship history** | the parsed history from tags — any other agent's tag supersedes the Ana Spradling / Ben Freshel distribution tags; team-exit marks the departure; current agent = Zach | the tag parser (Zach's 8/15 rules; ledger 314 — this is a COLUMN here, never its own twin table) |
| **household** | spouse/partner edges to other FUB persons, with the source (note, dotloop party, shared closing) | the same triangulation evidence |
| **relationship class** | past_client / lead / vendor / agent-peer, with the closing evidence for past clients | SISU spine + the book |
| **compliance** | the explicit-DNC flag (ONLY the named set: Harrisons · Burdett · Radke · Suzana Lewman), never derived from tags/stages | the named DNC ruling (ledgers 299/313) |
| **readiness** | import/pour readiness verdict + what blocks it, per person | the pour matrix |

**Standing laws that bind this table:** one answer table (a fact lives here once — the era/source
is a column); the tape is never edited; staging is derived and disposable; the import is
idempotent through the sanctioned writers; `is_backfill` never rings a bell.

## What already exists (honest status, 2026-08-17)

- **Events: DONE and LIVE.** `hl_event_mapping` + the FUB staging adapter translate tape events
  into catalog kinds — a live lead's Zillow views and tour requests render as HouseLoop cards
  today through this exact path.
- **Identity: DERIVED for the whole held set.** The 31 triangulation verdicts (ledger 311) are the
  first real rows of the identity column — 22 household-resolved, 9 identity-confirmed, 0 wrong
  humans, every verdict with its dotloop/SISU/notes citation.
- **The pour (past-client import): BUILT and pilot-proven** — stage-at-mint, hlid forwarding,
  honest dry-run accounting. It currently derives some answers at run time; under this PRD those
  derivations MOVE into staging and the pour reads them.
- **NOT built yet:** the staging person TABLE itself (the shape above), the tag parser job, the
  contact-census job, and the readiness projection reading staging instead of computing.

## The order of work

1. **DDL for the staging person table** (staging shelf, desk-gated) — columns above, provenance on
   every derived group.
2. **The jobs**, one per column group, each a registered Inngest function with a run-log receipt
   (Inngest or it doesn't exist): identity-fill (seeds from the 31 verdicts + extends book-wide),
   contact-census, tag→agent-history parser, household edges, readiness.
3. **A readable surface** — Zach can open a FUB person and SEE the staged answers before anything
   imports (the agent-history dropdown/column he asked for reads THIS).
4. **Repoint the pour** to read staging for identity/household/agent-history instead of deriving.
5. **The ratchet:** any import that reads `fub.*` directly for a DERIVED answer goes red.

---

# ⭐ THE BUILD-READY SPEC — added 2026-08-22 (FUB-18 prep)

Everything above is the SHAPE, signed 2026-08-17. This section is what the next lane can execute
from: the three requirements a staged lead must satisfy, and the rules — every one of them earned
on a real lead during the 8/21–8/22 inventory run — that the staging build has to honor or it
rebuilds the same diseases at 49k scale.

## THE REQUIREMENTS — dictated by Zach, 2026-08-22

**This spec is the cohort rehearsal's law: the 49k book imports through THIS or not at all.**
R1–R4 are Zach's own words, dictated live on 2026-08-22. R5–R6 are desk requirements that compose
with them.

### R1 — COMPLETE SCHEMA, NO HOLES (Zach, verbatim, 2026-08-22)

> *"the schema has to match. There cannot be holes in anything."*

Every staged row lands with **every destination field either resolved or explicitly recorded as
absent — never silently null.** A NULL that means "we could not derive this" and a NULL that means
"this person genuinely has none" are different facts, and a column that cannot tell them apart is a
hole. Absence is a recorded verdict with a reason, the same way a derived value is a recorded
verdict with a basis.

The acceptance test is Zach's own from 8/21 01:36Z, which is this requirement asked as a question:
*"WHAT IS THE TABLE THAT GETS POPULATED WHEN WE PULL A LEAD INTO HOUSELOOP CRM FROM FUB… so if this
happens again its just a simple delete lead, and restore lead and it comes back perfect."* **Trash a
staged lead, re-import it, and it comes back identical.** If anything was derived at import time
rather than read from staging, it will not.

### R2 — EVERY MESSAGE CARRIES ITS ACTOR; DERIVATION IS LICENSED (Zach, verbatim, 2026-08-22)

> *"the actor who was the agent that sent the message. That can be derived. It doesn't have to be
> perfect. It can be derived from the tags that we find, team exit / agent name, or by reading some
> of the messages to see who sent the message and then inferring that to the other messages."*

⭐ **This licenses IMPERFECT derivation for staging attribution — on one condition: the provenance
rides along.** Every derived rung stamps WHAT it derived from. Unstamped inference stays banned; an
actor with no `actor_source` is a hole under R1, not an answer.

**THE STAGING ACTOR LADDER** — tried in order, first hit wins, the rung recorded on the row:

| rung | `actor_source` | how it decides | confidence |
|:-:|---|---|---|
| ① | `witnessed` | the tape names the sender (`user_name` on the row) | the tape's own claim |
| ② | `assignment_derived` | `fub.assignment_history` — who held this person AT THE MESSAGE'S TIME | high |
| ③ | `tag_derived` | the agent-relationship tags (any other agent's tag supersedes the Ana Spradling / Ben Freshel distribution tags; team-exit marks the departure) | medium |
| ④ | `content_inferred` | a signed message body naming its own sender, propagated to its thread neighbours | medium-low, and it says so |
| ⑤ | `degraded` | nothing decided it — recorded as unresolved WITH the reason | none, and never dressed up |

Four things the ladder must not do:

- **Never re-derive at render time.** The staged actor is the answer; surfaces read it (the actor
  canon, one door).
- **Rung ② is time-aware or it is wrong.** "Who is assigned NOW" attributes a 2024 message to the
  current owner. The join is against the assignment interval containing the message's `occurred_at`.
- **Rung ④ propagates within a THREAD, never across a person's whole history.** One signed message
  licenses its neighbours, not every message that person ever received.
- **The machine-note actor rule outranks all five rungs** (rung ZERO, below) — a robot note never
  reaches the ladder to be dressed in a human name.

⚠ **Rung ① is the tape's claim, not the truth.** The recipient-fabrication revert (rule 7 below) is
the standing proof that a confident override of the tape can be pure, tested, green and wrong. Rung
① beats rungs ②-④ because the tape is evidence and they are inference — not because it is infallible.

### R3 — FULL TIMESTAMP PROVENANCE: THREE CLOCKS (Zach, verbatim, 2026-08-22)

> *"there has to be a timestamp when it got filled in, when it got pulled into BossLoop, when the
> lead got pulled in via the integration."*

Every staged fact carries **three first-class clocks**, never one collapsed field:

| clock | column | what it answers |
|---|---|---|
| **the event's own time** | `occurred_at` | when the thing actually happened, in the world |
| **the staging fill** | `staged_at` / `filled_at` | when WE derived this row — so a re-run is visible and a stale derivation is findable |
| **the lead's pull moment** | `lead_pulled_at` | when this person became a HouseLoop lead through the integration |

The third clock is already law: **`created_at` is the HouseLoop pull date** (rule 6 below) — Renita
Ford's 7/14 was correct and the 50-lead restamp was killed over exactly this. Keeping the three
apart is what makes a wrong date a *fixable derivation* instead of an argument.

### R4 — ALL OF IT DOCUMENTED: the mapping IS the contract (Zach, 2026-08-22)

The PRD carries the **complete mapping — tape table → destination table → per-field source → the
derivation ladder for every derived field.** The perfect-schema contract is not a promise about the
build; it is this document. A field that is not in the mapping is not allowed to be filled.

**Status, honestly: the TABLE level is below; the PER-FIELD level is the next lane's first
deliverable and is NOT done.** Writing a field map I have not verified against the live tape would
be the exact fabrication R1 exists to prevent.

| tape (`fub.*`) | destination | per-field source | derived? |
|---|---|---|---|
| `fub.people` | the staged person row | identity, contact census, tags | ✅ identity (triangulation) · contact census · agent history |
| `fub.text_messages` | messages | body, direction, timestamps | ✅ **actor** (the R2 ladder) · ⚠ **generation** (rule 2 — API wins) |
| `fub.emails` | messages | body, direction, addresses | ✅ actor (R2 ladder) · seats |
| `fub.calls` | calls | duration, outcome, recording | ✅ actor (R2 ladder) |
| `fub.notes` | notes | body, author | ✅ **rung ZERO: machine-note grammar** (rule 3) before the ladder |
| `fub.events` | the activity log | catalog kind, venue, payload | ✅ `hl_event_mapping` (already live) · venue (rule 4) |
| `fub.assignment_history` | *(not a destination)* | — | the EVIDENCE behind actor rung ② |

**The per-field map must state, for every column: its tape source, its ladder (if derived), its
absence-verdict (R1), and its three clocks (R3).** That is the shape the next lane fills in.

### R5 — The staged HISTORY reads as CATALOG CARDS, not raw tape *(desk)*

Every historical event a staged lead carries is already resolved to a catalog kind with its actor,
its venue and its sentence — it is a card before it is imported, and the import files it through
`logActivity` rather than inventing a second writer. A staged history that still needs the render
layer to work out what happened has not finished staging.

### R6 — The item that TRIGGERED the pull is marked and timestamped *(Zach-signed, rule 1 below)*

Not the import job, not "backfill": the actual co-temporal event that caused this person to become a
lead, named honestly and carrying its own timestamp.

<Warning>
**One provenance correction kept on the record.** An earlier desk brief attributed three phrases to
Zach — *PERFECT DATABASE SCHEMA* / *JUST LIKE THE ACTIVITY CATALOG* / *MARKED AND TIMESTAMPED*. Those
exact phrases appear nowhere on disk as his words; they were a desk paraphrase. **The requirements
above are different** — R1–R4 are his dictation of 2026-08-22 and are quoted verbatim. The
underlying law he signed on 2026-08-17 still governs: *"we should not be deriving anything without it
already derived and structured properly in the fub mirror… the fub mirror should be a staging table
for the houseloop crm."*
</Warning>

## The rules the build must honor

Each rule is here because a real lead was wrong without it. **A rule with no verbatim quote is a
desk ruling and says so** — do not upgrade it in a later retelling.

### 1. THE TRUE-TRIGGER RULE — signed by Zach 2026-08-22 (verbatim)

Zach, ratifying the desk's proposal in his own message (19:56Z), with James Wisner's card as the
worked example:

> *"the pull card's reason must be the TRUE co-temporal trigger — even when that trigger is a
> machine inference — honestly labeled. So Wisner's card would read: ▎ Lead created in HouseLoop via
> the Follow Up Boss integration ▎ Action: pulled in on a FUB automation note — "Buyer has started
> searching again after being inactive…", June 30 — this is the correct fix."*

**A machine-inference reason is SELF-INDICTING by design and is never dressed up as a human action.**
Live in `apps/houseloop-crm/lib/leads/arrival-trigger.ts`; staging inherits it rather than
re-deriving it.

**THE REASON-RESOLUTION LADDER** (desk wording, ruled 2026-08-22 downstream of the signature above):

| rung | what it is | renders as |
|---|---|---|
| ① | a co-temporal **qualifying** action (a kind whose `creates_lead` is true) | the action, with its venue |
| ② | a co-temporal **non-qualifying** source event with SUBSTANCE | its honest family — "a FUB automation note", "a Zillow insight" |
| ③ | manual | a person pulled this lead in |
| ④ | **unexplained = a filed defect** | drawn, never hidden — `unrecorded`, in warning colour |

Four mechanics the staging build must carry, not re-invent:

- **The window is MEASURED, not chosen:** 3 days back, 10 minutes of forward grace. 33 of 50 stamped
  lags land within 89 minutes, then hours, then 2.4 days — and the next one is 30 days. The gap is a
  factor of twelve, which is what makes 3 days a boundary rather than a preference.
- **Rung ② prefers SUBSTANCE over NEAREST.** Nearest-wins on Wisner picks a contentless
  `virtually_toured` and renders "took a virtual tour"; substance picks the insight and renders the
  card Zach actually signed.
- **Candidates are source-side only** (an `archived_from` stamp or a `fub.*` source table). Without
  that fence, rows written three minutes AFTER the pull get cited as its cause.
- **The qualifying set is read live from `hl_event_mapping.creates_lead`** — never a list in code
  (Bedrock D5: the toggles are DATA).

The defect this cures, measured 8/22: the earlier correction pass searched only qualifying kinds and
reached backwards, so Wisner's 2026-06-30 pull cited an `sms_received` from 2026-04-25 — **65 days
stale**. Nine real leads carried that shape, up to 351 days.

### 2. THE FILING-SIDE GENERATION PREFERENCE — desk ruling 2026-08-22 ⚠ GATES the cohort rehearsal

When one `(person, body, timestamp)` fact exists in both generations, **the API row files and the
scraped twin never does.** The scraped generation disagrees with the API generation on message
DIRECTION in **19,764 of 58,854 twin pairs**; on the API generation the actor is trustworthy.

⛔ **This must be LIVE BEFORE any cohort rehearsal.** Importing 49k through a reader that believes
111k lying rows replays the disease at scale — the cleanup is not a substitute for the rule, because
the next pull mints the twins again.

### 3. THE MACHINE-NOTE ACTOR RULE — ordered by Zach 2026-08-22 (verbatim), shipped

Zach, on finding his own name on a Zillow sync note:

> *"Zach Spradling created a note, wants to view a property, wants to be contacted by text.
> **I DIDNT DO THAT.**"*

(and on 8/21: *"it says zach spradling created a note… THAT IS A FUCKING ZILLOW SYNC NOTE FROM FUB"*)

**The discriminator is BODY GRAMMAR** — `lib/integrations/machine-note-grammar.ts`, validated on the
154,199-body corpus. Two cheaper discriminators were tested and REJECTED: `system_id`, and
`user_id IS NULL` (which is *inverted* — 2.5% vs 8.0%). 36 rows re-actored; the rule sits at rung
ZERO of `fub-authorship.ts`, above `tape_author_trusted`.

⚠ **Scale for staging: 1,155 machine notes are latent on the tape across 36 agent names** (largest
single name: 526). Staging must apply this rule at derivation time, or the pour re-dresses a
thousand robot notes in human clothes. Note the arithmetic drift in the record — the day card queues
"38 reclassify", the builder shipped **36**; a fixture trap silently rejected 17 rows before it was
fixed. Cite 36.

### 4. THE VENUE RIDES WITH THE VERB — signed by Zach 2026-08-22 (verbatim, iterated live)

The action sentence names **where the action happened**, never the pipe: *"made a general inquiry on
Zillow."* This is Bedrock **D2 in a single field** — Zillow is a SOURCE, Follow Up Boss is a PIPE.
Passing the carrier here would put *"made a general inquiry on Follow Up Boss"* on a card, which
asserts that somebody browsed our CRM vendor.

The hero card Zach signed, in his words (20:04Z): *"bottom left badge should be follow up boss icon
and the right of it should be the source… zachary bell, imported via follow up boss integration, no
fub signal [up top], we keep the fub signal at the bottom left, and the rest of the card stays the
same."* And on timeline events (19:58Z): *"the fub logo rides the EVENT."*

The three grammar lines live as DATA in `hl_settings.arrival_grammar`, drafted by Zach at 13:24Z:

```
Lead created in HouseLoop [via the Follow Up Boss integration]
Lead source: <bucket>
Action: <signal> — <payload>
```

Staging supplies the venue and the payload as COLUMNS. It never hands the renderer a carrier name.
⚠ Open, not shipped: the venue-logo law on timeline event cards (parked as its own measured pass —
`avatarFor` precedence touches every legacy card).

### 5. THE SOURCE COMES FROM FUB'S SOURCE FIELD — Zach 2026-08-22 (verbatim)

> *"you get the source from the source field in follow up boss, follow up boss is the integration."*

The lead source is a staged column read from the tape's source field, never inferred from a feed
name or a tag. ⚠ **The Google bucket is still an open product call** (Zach, 12:50Z: the Google bucket
arrives through either integration and FUB carries `source Google PPC` / `Google AdWords` tags) —
staging must carry the raw source verbatim so the bucketing decision stays reversible.

### 6. `created_at` IS THE HOUSELOOP PULL DATE — law 2026-08-22

Zach: *"don't we record it when they got pulled in"* → *"Renita Ford's actual pulling date was July
14th."* Her 7/14 date was CORRECT and the planned 50-lead restamp was **KILLED**. A staged lead's
CRM `created_at` is the moment HouseLoop pulled it; the FUB-side dates live in their own staged
columns and never overwrite it.

⚠ If you find a doc still listing "restamp-50" as live work, it is stale — that job is dead.

### 7. THE RECIPIENT-FABRICATION REVERT — retired 2026-08-22, falsified by Zach against FUB's screen

The `new_era_assigned_agent` override rewrote a message's recipient to the assigned agent. Zach
disproved it on Zairanett Hernandez: **FUB's own screen shows the message went to the person FUB
named.** 70 rows were reverted to FUB's claim.

⚠ **The lesson staging must internalize:** the rule was pure, fixture-tested and GREEN — because its
test asserted that the override FIRED. A derivation whose test asserts its own mechanism ran can be
green and wrong forever. Every staged derived column needs an assertion against the SOURCE, not
against the deriver. (The `RecipientSource` enum keeps the retired member so historical staging rows
stay readable.)

### 8. `is_backfill` NEVER RINGS A BELL — Bedrock D4, and a live violation is still open

Three independent gates enforce it on the seat-filing backfill, and replays (`road=catch_up`) never
notify. ⛔ **Not settled:** an agent-alert text fired to Ana on 2026-07-27 announcing a tour request
made 2025-12-18 — **221 days late** — because a FUB history replayed at import. The backfill fired a
real send and the replay path is presumably still armed. **A staging→import pass at cohort scale is
exactly the event that would fire it again.** Prove this closed before the first cohort import, not
after.

### 9. THE MINT LAW — 9 action-needed triggers, signed by Zach 2026-08-22 (verbatim)

> *"Property inquiry, Phone call, **I need all leads that need a freaking action taken.** A seller
> inquiry is different from a high intent seller signal and if somebody inquires to sell their home
> I need that, a text, an inbound email, an in app message, any type of tour request, contact
> request, should mint a lead."*

ON (9): `tour_request` · `in_app_message` · `text_message` · `property_inquiry` · `phone_call` ·
`seller_inquiry` · `contact_request` · `email_received` · `general_inquired`.
OFF (4): `registration` · `pre_approval` · `fub_zhl_preapproval_disguised` · **`fub_person_created`**
(the random-import cause). A seller INQUIRY mints; a high-intent-seller SIGNAL never does. It is
`hl_event_mapping` DATA controlled from the automation-triggers surface — **nothing hardcoded**.

⚠ Open, ordered but unconfirmed: calls/texts/emails must mint **INBOUND-only**.

## What staging must NOT reintroduce — the six named diseases

The 8/21–8/22 inventory run reduced the whole episode to six causes. Every one of them is a
staging-layer failure mode, which is why this PRD exists:

1. **Contaminated stamps** — a backfill dressing re-imports in campaign clothes.
2. **False-legacy masking, in BOTH directions** — native rows masked as legacy, legacy rows unmasked.
3. **The twin generation** — the scraper's direction-blind second copies (rule 2 above).
4. **Fabricated participants** — the assigned-agent recipient rule; FUB auto-notes wearing human
   actors (rules 3 and 7).
5. **Unwired truth** — grammar and reasons built with zero render callers. A staged column nothing
   reads is not progress.
6. **Mint floodgates** — cured by rule 9.

## The revised order of work

The five steps above still stand. These come first, because each one is a gate on the next:

1. **Rule 2 (filing-side preference) goes live.** It gates the cohort rehearsal; nothing that
   imports at scale may run before it.
2. **DDL for the staging person table**, now carrying the trigger columns R3 needs: the true-trigger
   event id, its rung (①-④), its kind, its venue, and its timestamp — with provenance.
3. **The derivation jobs** as registered Inngest functions (Inngest or it doesn't exist), each one
   applying the rules above at DERIVATION time: machine-note actor, venue, source-field, generation
   preference.
4. **Prove rule 8** (`is_backfill` fires nothing) red-first against a replay, before any cohort pass.
5. **The readable surface**, then the pour repoint, then the ratchet — unchanged from above.

## Open, and deliberately not decided here

- **The qualifying-window date disagrees across two live records:** the day card says the executed
  population law was "pull-card **July 1**+ with a real action reason, OR Zach/Ana communication";
  Lee's handoff says the qualifying window starts **July 7** (superseding an earlier 7/15). One of
  them is wrong and it is not this PRD's call to pick. Resolve at the desk before it is written into
  a staging predicate.
- **The Google source bucket** (rule 5) — a product decision, still Zach's.
- **Inbound-only minting** for calls/texts/emails (rule 9) — ordered, not confirmed live.

---

*Nothing in this PRD imports anyone. The 48k bulk import is dead (ledger 314); this layer exists
so that whatever IS imported — the past-client pour, a hand-picked beta lead — copies answers that
were already derived, structured, and visible.*
