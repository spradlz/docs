---
title: "FUB — How It Works"
description: "The index for everything Follow Up Boss: how lead data reaches HouseLoop (webhooks + scraper), where it lands, and the sections that document each piece."
---

<Note>
**This is the FUB index.** Everything Follow Up Boss lives under this one **FUB** group in the sidebar →
read down the sections on the left. Nothing FUB is documented anywhere else. Public mirror of the private GT
SSOT `GT_HOUSELOOP_FUB_EVENTS_AND_REQUEST_CARD_SSOT.md`.
</Note>

## The one-source map (memorize this)

There is **one source** (FUB, fed by Zillow), pulled by **two pipes** (webhooks + scraper), into **one store**
(`fub.*` on Railway), read by **many surfaces**.

```
   Zillow (Preferred · Rentals · RealScout)
                  │  inquiries · tours · views · saves · ML intent tags
                  ▼
            FOLLOW UP BOSS              ← the hub. FUB owns the signal first.
                  │
        ┌─────────┴─────────┐
   19 WEBHOOKS          SCRAPER (Chrome)
   real-time signals    only what the API hides:
   + clean property     · message/email/call bodies · saved searches · deep browse list
        └─────────┬─────────┘
                  ▼
        fub.* on Railway              ← THE ONE STORE (relay: Camp David → Railway)
                  │
   ┌──────────────┼───────────────────────────┐
   ▼              ▼                            ▼
 Telegram     HouseLoop CRM               FUB's own UI
 + SMS alert  (FUB Activity panel,        (followupboss.com)
              lead import, request cards)
```

## The principle (LOCKED)

**Every FUB signal becomes one of OUR OWN events** — minted via `logActivity()` into the one run-log
`hl_lead_activity` (no new store, no fork). `fub.*` is the raw landing zone; our event store is the truth. Once
a signal is our event it's our data, it renders on our cards, and it can trigger our follow-up.

## The sections (read down the sidebar)

| Section | What it covers |
| --- | --- |
| **FUB Scraper** | How data physically arrives: the 19 live webhooks, the fetch-back, and the scraper (real Chrome) that recovers only what the API hides — masked bodies + the deep browse list + saved searches. The full runbook (pm2 procs, watchdog, health checks). |
| **Data & Signals Matrix** | Every FUB signal → one of our own events. The coarse-type problem, the three request types, Behavior-Based Follow-Up, the Zillow playbook catalog. |
| **Lead Import** | How a FUB lead becomes a HouseLoop lead — the import engine, real-time sync, and how tour/contact requests cross into the HouseLoop feed. |
| **Myli Context** | Myli's brain on a FUB lead — the Lead Context model (facts + dossier) and the Watermark Law. |
| **Request Cards** | The one canonical card for a property inquiry / tour request / saved search. |
| **Follow-Up Widget** | The embeddable FUB follow-up widget. |
