---
title: "🔏 The Data Model (locked)"
description: "The signed column model for every canonical table — keepers, deaths, folds — derived from product code with a file receipt per verdict. Locked by Lee under Zach's 7/31 delegation. Read-only: regenerated from the verdict registry, never hand-edited."
---

> 🔏 **LOCKED BY LEE 2026-07-31 (overnight), under Zach's end-to-end delegation** ("you finish the column
> diet end to end... it's going to be in locked tables on the Mintlify developer docs and I'm not going
> to visit it again"). Every verdict DERIVED FROM PRODUCT CODE by parallel sweeps — what the product
> reads/writes survives; what nothing reads dies; receipts live in `tools/schema/column-verdicts.json`
> (carried inside THE ONE LOCK). Governed by [THE DATA HYGIENE LAW](/schema-lock/target-model).

<Info>
**How to read:** ✅ KEEP = the signed surviving set · ❌ DIES = sentenced (drops batch behind Zach's typed
Rule-0, snapshots first) · 🔀 FOLDS = the fact moves to the named destination · ➕ COMING = approved,
awaiting its build. Colors match the Table Sitting board.
</Info>

## `hl_activity_catalog`


**THE NUMBER: ~782 live columns → 540 signed keepers across 47 tables.**

*20 -&gt; 13. The catalog's own dead flags: creates_lead/notify/notify_class live elsewhere.*

**✅ KEEP:** `key` · `label` · `section` · `source` · `lens` · `payload_schema` · `emitted` · `sentence` · `color` · `icon` · `request` · `hero` · `card_layout`

**❌ DIES:**

- `label_present` — 0/148
- `creates_lead` — 0 true — the real gate is hl_lead_source_events.creates_lead
- `notify` — 0 true
- `notify_class` — 0 filled — the live one is on hl_trigger_events
- `status` — 1 distinct
- `signed_at` — zero readers
- `created_at` — zero readers

## `hl_ai_identity_delegation`

*9 -&gt; 7. SECURITY FLAG: allowed_playbook_keys is the per-playbook scope the gate NEVER CHECKS — enforce it or remove the promise.*

**✅ KEEP:** `id` · `ai_actor_user_id` · `presented_user_id` · `channel` · `enabled` · `valid_from` · `valid_until`

**❌ DIES:**

- `allowed_playbook_keys` — zero refs, absent from the schema test — a scope that doesn't scope
- `created_at` — zero readers

## `hl_appointments`

*27 -&gt; 22.*

**✅ KEEP:** `id` · `lead_id` · `title` · `description` · `location` · `type` · `starts_at` · `ends_at` · `status` · `is_all_day` · `created_by` · `created_at` · `updated_at` · `assigned_user_id` · `guest_emails` · `outcome` · `outcome_notes` · `outcome_at` · `outcome_by` · `calendar_event_id` · `calendar_provider` · `created_by_uuid`

**❌ DIES:**

- `recruiter_id` — PlacedAgent leftover, 0/12
- `owner_user_id` — 0/12 zero refs
- `updated_by` — 0/12
- `rescheduled_to_id` — zero refs repo-wide
- `data` — all rows '&#123;&#125;'

## `hl_audit_log`

*8 -&gt; 7.*

**✅ KEEP:** `id` · `actor_id` · `action` · `target_t` · `target_id` · `diff` · `at`

**❌ DIES:**

- `request_id` — typed, never rendered, 0/204 populated

## `hl_calls`

*19 -&gt; 14.*

**✅ KEEP:** `id` · `lead_id` · `user_id` · `phone` · `direction` · `outcome` · `duration_seconds` · `from_number` · `to_number` · `recording_url` · `transcript` · `summary` · `occurred_at` · `data`

**❌ DIES:**

- `recruiter_id` — 0/587 PlacedAgent legacy
- `created_at` — occurred_at is the time axis
- `our_identity_user_id` — byte-identical to user_id (same $2), zero reads
- `conversation_key` — 508/508 equal 'call:'||lead_id — pure derivation; the thread ORDER repoints
- `archived_from` — derived from data-&gt;&gt;'source'

```json
{
 "note": "Email split: leads-table email_count reads hl_messages channel='email' which has ZERO rows while 124 send-log rows sit beside it \u2014 the Email count is structurally 0 (defect list; resolves at the Phase-B email fold)"
}
```

## `hl_communication_intent`

*21 -&gt; 21 — 0 rows but fully wired; the four write-only stamps are the audit trail BY DESIGN (a security gate must be reconstructable).*

**✅ KEEP:** `id` · `lead_id` · `channel` · `body` · `proposed_by` · `status` · `created_at` · `authorized_at` · `blocked_at` · `playbook_key` · `playbook_step` · `intent_key` · `expires_at` · `authorized_by` · `wire_ref` · `sent_at` · `failed_at` · `error` · `target` · `updated_at` · `reason`

## `hl_contract_template`

*15 -&gt; 13. field_map DEAD (real maps are the JSON files per SIGNATURE_ANCHORING); the 5 dotloop columns are build-ahead, not dead.*

**✅ KEEP:** `id` · `key` · `name` · `form_kind` · `active` · `pdf_b64` · `created_at` · `updated_at` · `dotloop_document_id` · `dotloop_folder_id` · `category_key` · `page_count` · `imported_at`

**❌ DIES:**

- `field_map` — zero references — contract-forms.ts loads the JSON files
- `pdf_path` — zero references — pdf_b64 is the bytes source

## `hl_custom_fields`

*13 -&gt; 10.*

**✅ KEEP:** `id` · `key` · `label` · `field_type` · `applies_to` · `is_required` · `choices` · `description` · `default_value` · `sort_order`

**❌ DIES:**

- `is_recurring` — in the SELECT + interface, NO WRITER EXISTS, no consumer
- `created_at` — zero renderers
- `updated_at` — zero renderers

## `hl_email_campaigns`

*18 -&gt; 13. open/click counts are rendered-but-never-written — the page repoints to a SUM over hl_email_send_log in the same commit or the Open Rate tile dies.*

**✅ KEEP:** `id` · `name` · `type` · `status` · `list_id` · `template_id` · `subject` · `notes` · `from_email` · `scheduled_at` · `sent_at` · `send_count` · `created_at`

**❌ DIES:**

- `open_count` — no writer anywhere — permanently 0; derive from send_log
- `click_count` — same
- `list_rules` — written never consumed; audience comes from list_id
- `created_by` — zero readers
- `updated_at` — typed, never rendered

## `hl_email_event`

*11 -&gt; 7. THE BIG DEFECT: template_key is 2/2637 stamped, so Email Analytics reads '(unlabeled) 2635' and the lead-detail Website Activity Email tab is BLANK (its ILIKE filter matches 0 of 25 candidates). Fix = stamp at send time / join on the right id — a copy that is 99.9% null rescues nothing.*

**✅ KEEP:** `id` · `resend_email_id` · `event_type` · `to_email` · `lead_id` · `link` · `occurred_at`

**❌ DIES:**

- `saved_search_id` — 0/2637 filled — the answering surface reads hl_email_send_log's copy
- `template_key` — 2/2637 — see the defect; the fix is upstream, not this column
- `raw` — no query selects it
- `created_at` — occurred_at is the time axis everywhere

## `hl_inboxes`

*13 -&gt; 10.*

**✅ KEEP:** `id` · `name` · `kind` · `owner_user_id` · `manager_kind` · `manager_user_id` · `ai_channels` · `spam_mode` · `prompt_layer` · `is_active`

**❌ DIES:**

- `ring_chain` — duplicate of hl_routing_groups.ring_targets — the voice webhook rings the group; two UIs edit who-rings, only one is wired; all 8 rows '[]' so the subtitle is permanently blank
- `created_at` — write-only
- `updated_at` — write-only

```json
{
 "DEFECTS": "/inbox/company always redirects away (name lookup 'Company' vs actual 'HouseLoop Company') \u00b7 nav-by-editable-name pattern breaks on rename",
 "DIET_WATCH": "spam_mode + prompt_layer are settings round-trips no engine reads (the spam gate reads hl_calls.outcome) \u2014 kept pending a product decision; manager_user_id identical to owner on all populated rows \u2014 becomes a duplicate if no AI manager ever differs"
}
```

## `hl_lead_activity`

*23 -&gt; 18. actor trio = the cutover rename set; 4 dead + source duplicate.*

**✅ KEEP:** `id` · `lead_id` · `kind` · `occurred_at` · `direction` · `title` · `body` · `photo` · `meta` · `integration` · `source_name` · `source_table` · `source_id` · `created_at` · `input_tokens` · `output_tokens` · `actor` · `actor_role` · `actor_kind`

**❌ DIES:**

- `platform` — 1/17,891 rows, zero readers
- `channel` — 32 rows, deliberately NULL per 0479, zero readers
- `archived_from` — write-only (the archive chain never renders — DEFECT flagged: the teal-divider law was never wired)
- `notified_at` — 17,552 rows all from one migration UPDATE, zero readers
- `source` — LEGACY SENTENCED by its own writer doc — integration + source_name survive

```json
{
 "rename": "actor/actor_role/actor_kind -> the user columns at cutover (KEEP till then \u2014 live readers)"
}
```

## `hl_lead_activity_star`

*4 -&gt; 2.*

**✅ KEEP:** `user_id` · `activity_id`

**❌ DIES:**

- `lead_id` — stamp duplicating the JOIN sitting beside it
- `created_at` — zero readers

## `hl_lead_facts`

*10 -&gt; 7.*

**✅ KEEP:** `id` · `lead_id` · `domain` · `slot` · `value` · `confidence` · `updated_at`

**❌ DIES:**

- `evidence` — 292/293 written, zero readers
- `source` — 1 distinct hardcoded literal
- `created_at` — zero readers

## `hl_lead_relationships`

*6 -&gt; 4 (0 rows, fully wired — the spouse/referral build lands on this).*

**✅ KEEP:** `lead_id` · `related_lead_id` · `relationship` · `created_at`

**❌ DIES:**

- `id` — surrogate; unique key is the pair
- `created_by` — written, zero readers

## `hl_lead_source_events`

*21 -&gt; 17.*

**✅ KEEP:** `id` · `lead_source_id` · `event_key` · `tag_id` · `is_active` · `workflow_id` · `creates_lead` · `workflow_active` · `existing_workflow_id` · `existing_workflow_active` · `notify` · `existing_notify` · `is_quarantined` · `default_stage_id` · `default_ai_agent_user_id` · `default_ai_role_id` · `default_assigned_user_id`

**❌ DIES:**

- `label` — derived+force-synced from hl_trigger_events.label (one bare reader joins the catalog in the drop commit)
- `sort_order` — same derivation
- `created_at` — zero readers
- `updated_at` — zero readers

## `hl_lead_sources`

*38 -&gt; ~24. One name, one AI slot, external handoff becomes a user, dead word dies, token plumbing flagged to the vault pattern.*

**✅ KEEP:** `id` · `key` · `name` · `color` · `is_active` · `sort_order` · `created_at` · `updated_at` · `source_type` · `is_selectable` · `integration_id` · `use_integration_defaults` · `default_stage_id` · `default_pipeline_id` · `default_tag_ids` · `default_assigned_user_id` · `handoff_user_id` · `auto_handoff_on_appointment` · `default_message_sender` · `clone_default_on` · `inbox_id` · `mailbox` · `is_quarantined`

**❌ DIES:**

- `platform` — the dead word — already sentenced, never backfill, drop at cutover
- `brand_display_name` — second name for one thing — name wins
- `default_ai_agent_user_id` — AI pile -&gt; ONE ai user FK (clones are users)
- `default_ai_role_id` — role lives on the user row
- `default_responder` — collapses into the one AI slot
- `handoff_external_name` — an external partner is a PERSON -&gt; a user row (lender/showing_partner pattern), pointed at by handoff_user_id
- `handoff_external_email` — same
- `handoff_external_phone` — same

## `hl_lead_tags`

*5 -&gt; 3.*

**✅ KEEP:** `lead_id` · `tag_id` · `applied_at`

**❌ DIES:**

- `id` — surrogate PK never joined — the natural key (lead_id,tag_id) is the unique index AND the run-log sourceId
- `applied_by` — only the test harness reads it; all 430 live rows one value

## `hl_leads`

*FINAL — DERIVED FROM THE PRODUCT CODE (two-agent sweep 7/31 PM: list surface + detail + mobile + engines, every verdict carries a file receipt). 54 columns -&gt; 31 keepers. Zach's method: 'look at the actual code, the UI — what would you build?'*

**✅ KEEP:** `houseloop_lead_id` · `first_name` · `last_name` · `email` · `phone` · `address` · `created_at` · `updated_at` · `deleted_at` · `created_by` · `updated_by` · `source_id` · `assigned_user_id` · `lead_score` · `import_batch_id` · `fub_person_id` · `ai_mode_enabled` · `assigned_ai_agent_id` · `escalation_stage` · `ack_due_at` · `clone_ack_due_at` · `last_human_ack_at` · `notify_anchor_at` · `active_workflow_id`

**❌ DIES:**

- `name` — duplicate of first+last — LeadLane renders repoint to the formatter, then dies
- `source` — text fallback only (COALESCE in leads-query) — backfill source_id then die
- `platform` — dead word — the Integration row repoints to the integration column, then dies
- `channel` — allowlisted, ZERO readers anywhere — dead
- `pipeline_id + stage_id` — ORPHANED both surfaces (all 5 stage-move paths write memberships) — die; replaced by signed buyer_stage_id + seller_stage_id
- `myli_state_id` — dead prop (MyliLane never destructures it) — die
- `owner_user_id` — one engine reader (notification fan-out create.ts:233) — repoint to assigned_user_id, die
- `relationship_owner_user_id` — the Owner picker was dropped — dead, die
- `recruiter_id` — ZERO references (every grep hit is another table) — die
- `status` — written 'active' at creation, never read — die
- `current_workflow_run_id` — ZERO references since mig 0003 — fully dead; Zach's playbook rename lands on active_workflow_id instead
- `timezone` — dead since 7/24 BUSINESS_TZ hardcode (+ stale comment in CenterLane to clean)
- `is_past_client` — unmounted toggle — dies into the ONE relationship column
- `ai_responder` — the clone-vs-Myli picker — collapses into assigned_ai_agent_id pointing at the clone-user or Myli (UI rebuild in the same pass)
- `ai_clone_id` — indirect gating only — same collapse
- `ai_agent_user_id` — legacy backend fallback (runtime.ts) — repoint to assigned_ai_agent_id, die
- `ai_role_id` — role lives on the user row (clone model) — fold at cutover with the dropdown rebuild
- `data` — ZERO UI readers (proven) — dies after hl_lead_custom_value beam + the SISU export repoint
- `id` — ONE-ID law — retires at cutover PK surgery; effective now: never exposed
- `last_communication_at` — ZACH'S DERIVE-DON'T-STAMP LAW: 'that should be a filter from the run log — a query, not a hardcoded stamp; that's how shit drifts.' The list-sort derives from MAX(hl_lead_activity.occurred_at) comm kinds; if scale ever demands a cache it is a DECLARED regenerable one, never a hand-stamp.

**🔀 FOLDS:**

- `relationship_bucket` → LIVE UI (ReferralBand) -&gt; the ONE relationship column — Band rebuilds on it in the same fold
- `referral_rank` → LIVE UI -&gt; derives from relationships edges; Band keeps the A/B/C/D control on the derived value
- `relationship_dossier` → LIVE textarea -&gt; hl_lead_facts; Band repoints
- `loan_status + loan_lender_company_id + loan_officer_user_id + loan_status_at` → backend-writer only -&gt; per-TRANSACTION loan state (signed)
- `responder_mode` → escalation_stage — ONE failover-state column (it duplicates the ladder state; written by the same sla-sweep/handoff engine; the reasoning-copy reader repoints in the fold)
- `imessage_enabled` → the PHONE ENTRY's imessage attribute in the contact list (signed contact-JSON model) — 'can this number receive blue-bubble texts' is a fact about the NUMBER, not the lead; a lead with two numbers can differ per number. The composer checkbox repoints to the phone attribute; the message itself records transport (the signed hl_messages column) per send.

**➕ COMING:** `relationship` · `buyer_stage_id + seller_stage_id` · `integration` · `lender_user_id + showing_partner_user_id + tc_user_id`

```json
{
 "keep_notes": {
  "active_workflow_id": "the LIVE pointer (MyliWatchingBadge) \u2014 renames to active_playbook_id (playbook survives)",
  "created_by": "write-only audit, Zach-approved for manual entry",
  "notify_anchor_at": "notification-dedup engine (backend)",
  "escalation_+_ack_family": "the SLA/failover engine + the MyliLane cockpit \u2014 load-bearing",
  "houseloop_lead_id": "becomes THE lead_id per the ONE-ID law (today zero readers \u2014 the /lead/<n> display beam wires it) \u2014 RENAMED 0553: the brand wears the id (houseloop_lead_id beside fub_person_id, future chime_id)"
 }
}
```

## `hl_messages`

*30 -&gt; 16 (code-derived). THE CRITICAL FIND: the 0522 split-brain — metadata keys promoted to columns, readers never moved: 1,275 agent receipts leak into lead Texts, Myli chat 33/1,308, template-sends 0/700, and the status webhook still writes the blob while the column froze. Reader fix = 4 call sites + 1 webhook, LEE EXECUTING TONIGHT.*

**✅ KEEP:** `id` · `lead_id` · `direction` · `channel` · `source` · `body` · `metadata` · `created_at` · `external_id` · `recipient_role` · `template_key` · `image_url` · `actor` · `actor_role` · `status` · `delivery_status`

**❌ DIES:**

- `conversation_id` — 0/2576, dead since mig 0075
- `conversation_mode` — same
- `subject` — 0/2576 — email never lands here
- `workflow_run_id` — 0/2576 zero refs
- `llm_call_id` — same
- `decision_audit_id` — same
- `our_identity_user_id` — write-only; send identity re-resolved live
- `conversation_key` — duplicate filter of recipient_role (the view's predicate is the survivor; leads-query repoints)
- `delivery_status_at` — write-only, frozen at the 0522 backfill instant
- `archived_from` — derived from source='fub' (1473/1478 agree)
- `transport` — EXACT 1:1 derivation of channel (sms-&gt;twilio, imessage-&gt;bluebubbles), zero readers — the signed column turns out DERIVED; record the supersession honestly

```json
{
 "keep_notes": {
  "actor + actor_role": "RENAME set (user_name/user_type at cutover)",
  "status/delivery_status": "ONE survives \u2014 identical on all overlapping rows; consolidation rides the reader fix",
  "template_key + image_url": "zero readers TODAY because of the split-brain \u2014 readers repoint tonight"
 }
}
```

## `hl_myli_states`

*15 -&gt; 5 — a 1-row lookup wearing a state machine's clothes. RENAMES hl_ai_states (signed).*

**✅ KEEP:** `id` · `is_default` · `position` · `label` · `color`

**❌ DIES:**

- `key` — n=1, zero readers
- `description` — same
- `created_at` — same
- `updated_at` — same
- `priority` — 1 distinct
- `is_interruptible` — never exercised
- `is_waiting` — same
- `is_terminal` — same
- `allowed_tools` — 0 non-empty
- `default_workflow_id` — 0 set

## `hl_notes`

*14 -&gt; 9 (code-derived, receipts in the sweep). THE JUDGMENT CALL: lead_id vs entity_type/entity_id — polymorphism is theoretical (1234/1234 rows = 'lead') and lead_id carries the ON DELETE CASCADE integrity, so LEAD_ID SURVIVES: the 6 entity_* readers repoint onto it, the 171 NULL lead_id rows backfill from entity_id first, then entity_type+entity_id die. One path, and it's the one with referential integrity.*

**✅ KEEP:** `id` · `subject` · `body` · `author_user_id` · `source` · `created_at` · `lead_id` · `archived_from`

**❌ DIES:**

- `entity_type + entity_id` — theoretical polymorphism — readers repoint to lead_id (backfill 171 NULLs first)
- `is_html` — zero readers
- `updated_at` — stamped, never selected
- `created_by` — 4/1234 filled, zero readers — mig-0060 boilerplate; author_user_id is the truth
- `updated_by` — 0/1234 filled, zero readers

```json
{
 "INVESTIGATE": {
  "archived_from (chain)": "the whole FUB archive-flag chain (notes -> activity.archived_from) is WRITE-ONLY per the sweep \u2014 but the FUB law says archived rows render teal under a divider. Either the divider reads it somewhere the sweep missed or the archive rendering was never wired \u2014 that's a DEFECT to fix, not a column to kill. Verify before any verdict."
 }
}
```

## `hl_notification`

*15 -&gt; 11 (code-derived). seen_at was one concept stamped twice with a never-usable index; trigger_event was a write-only taxonomy; channels_sent written never read. event_key + notification_class BOTH stay (the class is the pref-lookup key that survived the 7/29 rename incident).*

**✅ KEEP:** `id` · `recipient_user_id` · `event_key` · `title` · `body` · `lead_id` · `entity_type` · `entity_id` · `payload` · `read_at` · `created_at` · `notification_class`

**❌ DIES:**

- `seen_at` — identical to read_at on all 1,753 rows, zero readers — plus DROP the dead index hl_notification_recipient_unseen_idx (predicates on seen_at, unusable by every unread query)
- `trigger_event` — zero readers anywhere — write-only taxonomy, FK'd and indexed for nothing
- `channels_sent` — written by dispatch, never selected

```json
{
 "DEFECT_FLAGGED": "loop-engine's tc_date_approaching dedup reads payload->>'milestone_key' which appears in ZERO live rows \u2014 the 20h dedup has never once matched; when TC date notifications go live they will spam. Fix the writer to stamp milestone_key (goes on the morning defect list)."
}
```

## `hl_notify_classes`

*7 -&gt; 5.*

**✅ KEEP:** `class_key` · `heading_tpl` · `subline_tpl` · `with_message` · `grp`

**❌ DIES:**

- `is_system` — all 55 rows true — discriminates nothing
- `updated_at` — written never read

## `hl_oauth_connections`

*20 -&gt; 16. recruiter_id's drop REWRITES the partial unique index the Gmail OAuth upsert depends on — its own migration, sequenced.*

**✅ KEEP:** `id` · `provider` · `account_email` · `access_token` · `refresh_token` · `expires_at` · `watch_history_id` · `watch_expiry` · `is_active` · `updated_at` · `user_id` · `sync_enabled` · `domain_filter_enabled` · `domain_filter_domains` · `is_default` · `signature`

**❌ DIES:**

- `channel_id` — zero refs, all NULL
- `recruiter_id` — all NULL; only no-op predicates — BUT the partial unique index rewrite gates the drop
- `scopes` — written+returned, zero UI consumers
- `created_at` — returned as gmail_connected_at, rendered by nothing

## `hl_phone_numbers`

*18 -&gt; 14. PLUS the night's nastiest infra finds: the UNIQUE constraint sits on the FORMATTED display string (re-import with different formatting = duplicate row) while e164 — the column EVERY lookup matches — has NO INDEX (every inbound call/text seq-scans); and assigned_user_id at 0/54 means Myli's warm-hat greeting is unreachable on every line (latent defect).*

**✅ KEEP:** `id` · `e164` · `label` · `category` · `platform` · `is_active` · `sort_order` · `created_at` · `routing_group_id` · `answer_mode` · `inbox_id` · `lead_source_id` · `assigned_user_id` · `forwards_to`

**❌ DIES:**

- `number` — duplicate of e164 (identical last-10 on all 54; display formats from e164)
- `creates_event` — selected+typed, never consumed — superseded by the lead_source_id seat
- `workflow_id` — joined for a name no UI renders — superseded by the seat's workflow
- `updated_at` — write-only stamp

```json
{
 "DEFECTS": "e164 misnamed+unnormalized (53/54 bare-10) \u00b7 unique on wrong column \u00b7 e164 unindexed \u00b7 warm-hat unreachable (assigned_user_id never set \u2014 column kept, the FEATURE needs wiring or a decision)"
}
```

## `hl_pipeline_section`

*VERDICT OVERRIDDEN BY LEE: the sweep read zero product readers and called the table dead — but it is SIGNED BUILD-AHEAD (Zach 7/31, mig 0531): the sections screen + Under-Contract redirect are on the morning ladder (Benji's own rabbit-hole entry). SEQUENCED, not speculative. If the screen is not built within the week, the hygiene law reclassifies it.*

**✅ KEEP:** `id` · `key` · `label` · `color` · `icon` · `sort_order` · `managed_by` · `pipeline_id` · `created_at` · `updated_at` · `side` · `chapter_key`

## `hl_pipelines`

*7 -&gt; 6.*

**✅ KEEP:** `id` · `name` · `is_default` · `sort_order` · `scope` · `created_at`

**❌ DIES:**

- `updated_at` — no reader

## `hl_pixel_event`

*9 -&gt; 8 + THREE BANNED DISPLAY READERS to repoint (pixel/events feed, requests status WRITE, saved-searches sub-tab).*

**✅ KEEP:** `id` · `visitor_id` · `event_type` · `occurred_at` · `lead_id` · `mls_number` · `property_payload` · `event_payload`

**❌ DIES:**

- `created_at` — occurred_at is the ordering key; not even a proxy (390,499 rows differ)

```json
{
 "VIOLATIONS": "3 display readers of the raw tier (target-model reader-ban) \u2014 morning repoint list; the Accept/Decline status WRITE into event_payload is product state in the raw tier"
}
```

## `hl_pixel_visitor`

*10 -&gt; 7.*

**✅ KEEP:** `visitor_id` · `lead_id` · `email` · `is_bot` · `bot_name` · `identified_at` · `created_at`

**❌ DIES:**

- `first_seen` — identical to created_at on 100% of rows
- `updated_at` — zero readers
- `user_agent` — 215,963 rows write-only — is_bot/bot_name are its derived outputs; the raw string stores an answered question

## `hl_run_log`

*12 -&gt; 11. The by-bytes king: created_at identical to started_at on 0-of-1,173,572 rows — indexed + huge, so a real migration not a casual ALTER.*

**✅ KEEP:** `id` · `app` · `source` · `fn` · `status` · `trigger` · `run_id` · `error` · `payload` · `started_at` · `finished_at`

**❌ DIES:**

- `created_at` — exact duplicate of started_at (measured: 0 rows differ), zero readers

## `hl_scheduled_actions`

*22 -&gt; 19.*

**✅ KEEP:** `id` · `lead_id` · `action_type` · `channel` · `payload` · `scheduled_for` · `actor_type` · `actor_user_id` · `status` · `created_at` · `updated_at` · `sent_at` · `cancelled_at` · `scheduled_by_name` · `attempts` · `last_error` · `requires_review` · `inngest_event_id`

**❌ DIES:**

- `inngest_run_id` — written never read — inngest_event_id is the linked one
- `workflow_run_id` — no SELECT or WHERE anywhere
- `result` — written never read

## `hl_settings`

*3 -&gt; 2.*

**✅ KEEP:** `key` · `value`

**❌ DIES:**

- `updated_at` — ~15 upserts write it, zero reads

## `hl_stages`

*14 -&gt; 10 + section_id sequenced (see hl_pipeline_section).*

**✅ KEEP:** `id` · `pipeline_id` · `name` · `position` · `color` · `icon` · `is_terminal` · `is_active` · `chapter_key` · `loop_status_key`

**❌ DIES:**

- `chapter_sort` — zero refs outside migrations
- `created_at` — no reader
- `updated_at` — no reader

```json
{
 "sequenced": {
  "section_id": "BUILD-AHEAD (0531, signed) \u2014 reader ships with the sections screen on the morning ladder"
 }
}
```

## `hl_tags`

*6 -&gt; 5. label is NOT a duplicate (name=slug, label=display, 150/172 differ, COALESCE reads).*

**✅ KEEP:** `id` · `name` · `label` · `color` · `is_locked`

**❌ DIES:**

- `created_at` — no SELECT reads it

## `hl_tasks`

*19 -&gt; 15.*

**✅ KEEP:** `id` · `lead_id` · `name` · `description` · `type` · `priority` · `assigned_user_id` · `created_by` · `created_at` · `updated_at` · `status` · `due_at` · `completed_at` · `calendar_event_id` · `calendar_provider`

**❌ DIES:**

- `owner_user_id` — 0/12 zero refs (all 5 INSERT sites use the same 9-col list)
- `created_by_uuid` — same
- `updated_by` — same
- `source_workflow_run_id` — same

## `hl_tc_loop_document`

*15 -&gt; 15 — the healthiest table in the family; every column live.*

**✅ KEEP:** `id` · `transaction_id` · `contract_fill_id` · `esign_provider` · `esign_doc_id` · `signed_at` · `file_url` · `contract_key` · `reminders_enabled` · `last_reminder_at` · `status` · `folder_id` · `name` · `created_at` · `updated_at`

## `hl_tc_loop_document_category`

*SAME OVERRIDE: signed build-ahead (0531) for the 58-form dotloop import + folders — the importer is Benji's next build. Sequenced; week deadline per the hygiene law.*

**✅ KEEP:** `id` · `key` · `label` · `folder_label` · `sort_order` · `side` · `created_at` · `updated_at`

## `hl_tc_loop_event`

*13 -&gt; 11 (renames hl_tc_loop_milestone). completed_by write-only — nothing can show WHO completed a milestone.*

**✅ KEEP:** `id` · `title` · `event_date` · `completed` · `sort` · `milestone_key` · `transaction_id` · `advances_to_stage_id` · `template_id` · `completed_at` · `created_at`

**❌ DIES:**

- `omitted` — seeded false, no component reads it
- `completed_by` — write-only — not in the SELECT

## `hl_tc_loop_participant`

*7 -&gt; 7 — all live.*

**✅ KEEP:** `id` · `transaction_id` · `full_name` · `email` · `phone` · `role` · `created_at`

## `hl_tc_loop_task`

*26 -&gt; 9 — THE HEAVIEST DIET. Write-once display-only table; no task endpoint exists; computeDue has zero callers so the 10 snapshot rule-columns have never executed. Don't polish until a writer exists.*

**✅ KEEP:** `id` · `transaction_id` · `label` · `assignee_role` · `sort` · `due_date` · `completed` · `template_id` · `created_at`

**❌ DIES:**

- `day_unit + business_day_def + weekend_roll + holiday_roll + skip_holidays + anchor_type + anchor_on + is_conditional + task_type + default_offset_days` — template snapshots, single-valued across all 487 rows, duplicate hl_tc_task_template via template_id — and the only consumer has zero callers
- `anchor_task_id` — 0/487 no reader
- `completed_at` — never written or read
- `completed_by` — same
- `is_overridden` — 0/487 no consumer
- `is_active` — the seed filter reads the TEMPLATE's flag, not this
- `phase` — DROP per the lock

**🔀 FOLDS:**

- `anchor` → milestone_key (sequenced rename)

## `hl_templates`

*21 -&gt; 14 (code-derived). PLUS three live bugs for the morning list: three *_text_hook blocks unpickable on text nodes (NULL channel defaults to email) · /api/email/send-template 422s for ~200 of 216 templates (only 1 of 40 category values mapped) · opening a system template in the editor silently rewrites visibility to team.*

**✅ KEEP:** `id` · `kind` · `name` · `subject` · `body` · `llm_compose_prompt` · `is_active` · `created_at` · `key` · `category` · `visibility` · `folder` · `channel` · `image_url`

**❌ DIES:**

- `body_source` — every hit is an INSERT — zero readers
- `tokens` — nothing parses it; self-inconsistent array/object
- `created_by` — joined for a name ZERO components render; 0/216 filled by the one live writer path
- `updated_at` — selected into the DTO, rendered nowhere
- `lifecycle` — editor echo written straight back; lifecycleMeta has zero call sites
- `scope` — constant 'crm' on all 216 rows
- `template_group` — 0/216 — groupForTemplate derives the same 4-value answer from folder+category; the derivation branch is the only one that has ever run

## `hl_transactions`

*35 -&gt; 29. loop_kind dead (confirmed vs the model's does-not-survive ruling); status quintet = the sequenced two-axes set.*

**✅ KEEP:** `id` · `lead_id` · `property_address` · `property_price` · `closing_date` · `notes` · `created_at` · `updated_at` · `list_price` · `cover_image_url` · `commission_type` · `commission_value` · `zillow_paid` · `agent_net` · `srg_net` · `transaction_type` · `website_hlid` · `stage_id` · `recorded_at` · `voided_at` · `void_reason` · `tc_assignee_user_id` · `created_by_user_id` · `milestone` · `tc_stage_id` · `transaction_status_id` · `loop_status_key` · `loop_status_at` · `loop_status_by_user_id`

**❌ DIES:**

- `loop_kind` — 8/8 populated, ZERO selects — write-only (matches the signed does-not-survive ruling)
- `lost_reason` — superseded by loop_status_key archive exits
- `lost_at_stage_id` — same
- `recorded_by_user_id` — dropped at the UI boundary — hl_audit writeAudit is the actor record
- `voided_by_user_id` — same

**➕ COMING:** `loop_status_reason`

## `hl_trigger_fire`

*9 -&gt; 8.*

**✅ KEEP:** `event_key` · `platform` · `fired_at` · `lead_id` · `source` · `matched` · `routed_by` · `id`

**❌ DIES:**

- `fub_person_id` — 4,700 written, zero readers — survivor hl_leads.fub_person_id

## `hl_users`

*Zach 7/31 PM: full_name splits into first_name + last_name (matching hl_leads — one name model everywhere); full_name becomes DERIVED for display, never stored as a second truth. Plus the data-key registry: 13 keys measured, 1 declared; the profile trio (title/card_phone/card_email) promotes to real columns with the user-profiles build; notification_prefs vs notification_channels = two keys one concept (collapse); license_state key vs hl_user_license table = two homes one fact (collapse).*

**❌ DIES:**

- `full_name` — becomes derived (first || ' ' || last) — display only, never stored

**➕ COMING:** `first_name + last_name`

## `hl_workflow_runs`

*21 -&gt; 14 (playbook runs). actor stamps write-only; state-machine leftovers 0/8.*

**✅ KEEP:** `id` · `workflow_id` · `lead_id` · `status` · `triggered_by` · `current_node_id` · `outcome` · `error` · `is_dry_run` · `started_at` · `ended_at` · `trigger_payload` · `updated_at`

**❌ DIES:**

- `actor_type` — write-only
- `actor_user_id` — write-only
- `state_id` — 0/8 zero refs
- `resolution_signal_id` — same
- `resolution_signal_key` — same
- `paused_at` — same
- `resumed_at` — same

## `hl_workflows`

*39 -&gt; 24 (playbook vocabulary at cutover). 15 dead incl. 7 copy-only carriers the duplicate faithfully copies and nothing reads.*

**✅ KEEP:** `id` · `key` · `name` · `description` · `status` · `trigger_type` · `folder` · `is_system` · `is_locked` · `lead_stage` · `created_by` · `created_at` · `updated_at` · `workflow_config` · `platform` · `trigger_config` · `sender_default` · `reenrollment_policy` · `cooldown_days` · `bh_override`

**❌ DIES:**

- `associated_state_id` — zero refs
- `is_default_for_state` — zero refs
- `variant_label` — zero refs
- `version` — zero refs
- `started_by` — zero refs
- `signal_id` — zero refs
- `myli_prompt_title + myli_prompt + schedule_config + preconditions + tool_access + quiet_hours + completion + target_type + kind` — copy-only carriers — cloned row to row, never read
