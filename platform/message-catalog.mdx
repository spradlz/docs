---
title: "Messages & Notifications (Catalog)"
description: "The one map of everything HouseLoop composes, renders, and sends — agent notifications, email + text templates, AI context blocks, merge fields, and the workflow two-beat. Section landing + current build state; atomic guides below."
---

<Note>
**This is the section landing for Messages & Notifications** — the umbrella over every message the CRM sends
and every piece of data that fills one. Each atomic guide (below) is its own page; this page is the map + the
current build state so nothing gets lost. Counterpart to the [Activity Card Catalog](/platform/activity-card-catalog)
(what a lead's activity *looks like*) — this answers what HouseLoop *sends*, and whether it's a real named
template that's *called* vs resolved ad-hoc.
</Note>

**📨 Live audit canvas** (every notification + workflow message rendered from the live `hl_templates`, with
named-template / AI-voice / missing status): [claude.ai/code/artifact/e5582966-451a-4744-a717-8f80005b923c](https://claude.ai/code/artifact/e5582966-451a-4744-a717-8f80005b923c)
**Ground truth (SSOT):** `GT/GT - ACTIVE/GT_HOUSELOOP_MESSAGE_CATALOG_PRD.md`.

## The atomic guides in this section

| Guide | What it is | State |
|---|---|---|
| [System Notifications](/platform/system-notifications) | Agent/user alerts (email + text) — who's notified, dispatch, prefs | ✅ live |
| [System Display Messages](/platform/system-display-messages) | On-screen messaging (toasts / banners / empty-states) | ⚠️ ad-hoc (no unified system) |
| [Email Templates](/platform/email-templates-system) | The email template system — shells, full-HTML, USED IN, SENT | ✅ live |
| [Text Templates](/platform/text-templates-system) | SMS/iMessage templates — parity with email (USED IN + SENT) | ✅ live |
| [AI Context Blocks](/platform/ai-context-blocks) | `{{ai.*}}` Myli-voice sections injected into templates (data, not a template) | ✅ live |
| [Merge Fields](/platform/merge-fields) | The `{{token}}` vocabulary every message draws from (data, not a template) | ✅ live |

*Templates vs data: **Email/Text Templates** are the actual things that render and send. **AI Context Blocks**
and **Merge Fields** are DATA injected INTO a template — kept in this section, but they are not templates.*

## The two-beat standard (locked)

Every lead-facing workflow fires the same shape:

- **Beat 1 — branded SYSTEM email, instant** (`updates@houseloop.ai`, a real named template): *"Got your
  request — a HouseLoop agent will reach out shortly."*
- **Beat 2 — Myli, ~2 min later** — a **Myli personal email** (`{{ai.*}}` voice) **+ a Myli text**, dropping the
  lead into a Myli workflow she can converse on.
- **System Text is intentionally omitted** — every text goes out as Myli (ONE NUMBER law).
- **`{{ai.*}}` / context-block copy is the accepted Myli-voice layer** — real catalog rows, not vibe code.

## Current build state (so nothing gets lost)

**Rendering + tracking — done:**
- Notifications: **30 events, all real named `hl_templates` rows** (email full-HTML since mig 0300, text plain). No gaps.
- Email + Text template pages both show **USED IN** (workflow / agent-notification / not-used, via `resolveTemplateUsage`) **and SENT** counts.
- **Text-send tracking shipped** — outbound texts stamp `template_key` into `hl_messages.metadata`; `/api/template-sends` unions email + text; who/when is in Run Log → Communications.

**Workflow two-beat — gaps queued** (17 active + 5 draft; all 32 template refs resolve — gaps are missing *beats*, not broken copy):
- `sys_coming_soon` + `sys_coming_soon_contact` — **fire but send nothing** (highest priority).
- Seller flows — missing branded Beat 1 + a Myli text.
- `sys_tour_request_ack` / `sys_contact_request_ack` — missing the Myli email (Beat 2).
- `sys_reg_tour_request` / `sys_reg_contact_request` — missing branded Beat 1.
- Drafts (`sys_prop_*`, `myli_saved_search`) — build-or-delete.
- Queued enhancement: `{{stage.from}}`/`{{stage.to}}` stage-transition merge namespace.

**Docs consolidation — in progress:** this section (landing + 6 atomic guides) is being stood up now. The old
scattered pages (`notification-templates`, and the notification/template content in `guides/*`) fold into these
guides; after review we collapse this section into its final sub-sections.

## Related (kept separate — NOT part of this section)

- [Trigger Events](/crm/pipeline-workflow-system-events) · [Lead Sources](/guides/lead-sources) ·
  [Workflows](/guides/workflows) — *what fires* a message, not the message itself.
