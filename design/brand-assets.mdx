---
title: "Brand Assets — the logo lockup, the banners, the colours"
description: "The HouseLoop asset library: the logo lockup as the website header renders it, ready-to-upload LinkedIn banners at LinkedIn's own dimensions, the two brand colours, and the typeface. Download and use these rather than re-drawing them."
---

## The one sentence

**Everything on this page is lifted from what the website already renders — never re-drawn.** If you need a HouseLoop mark, a wordmark or a banner, take it from here. If what you need is not here, ask before you draw it: a hand-made lookalike is the thing this page exists to prevent.

<Note>
The mark and wordmark are real components in the app — `components/ui/HouseLoopLogo.tsx` and `components/ui/HouseLoopWordmark.tsx`. The images below are renders of those components, so they cannot drift from the site.
</Note>

---

## LinkedIn banners

Sized to LinkedIn's own specifications. Download, upload, done — no cropping or resizing needed.

<Note>
**Every LinkedIn file comes as both PNG and JPG** — swap `.png` for `.jpg` in any filename below. LinkedIn accepts both; it does **not** accept SVG, so the `.svg` files on this page are masters for design work, never uploads. Prefer the PNG when a slot allows it: it is lossless, and LinkedIn re-compresses whatever you give it, so starting from JPG compresses twice.
</Note>

**The wordmark is the hero and the tagline sits under it.** The blue mark sits to the left of the letters, exactly as the website header arranges them.

### Personal profile banner — 1584 × 396

<Frame caption="houseloop-linkedin-banner-1584x396-white.png — THE DEFAULT">
  <img src="/images/brand/houseloop-linkedin-banner-1584x396-white.png" alt="HouseLoop LinkedIn banner on white: the blue HouseLoop mark left of a large navy House plus blue Loop wordmark, with The Smart Way Home and a brand-blue period underneath" />
</Frame>

<Frame caption="houseloop-linkedin-banner-1584x396-navy.png — the dark alternate">
  <img src="/images/brand/houseloop-linkedin-banner-1584x396-navy.png" alt="The same HouseLoop banner on a navy ground, with House in white and Loop still in brand blue" />
</Frame>

**Which one.** The white is the default: it carries the website header's exact treatment — navy `House`, brand-blue `Loop`, blue mark. Use the navy where the banner sits against other dark material or you want more presence in a feed.

<Warning>
**LinkedIn puts your profile photo over the lower-left of this image.** The composition is centred, so the photo lands clear of the words on desktop — but check it on your own profile after uploading, because the overlap shifts with photo size and on mobile LinkedIn crops the banner in from both edges.
</Warning>

### Company page cover — 1128 × 191

<Frame caption="houseloop-linkedin-cover-1128x191-white.png">
  <img src="/images/brand/houseloop-linkedin-cover-1128x191-white.png" alt="HouseLoop LinkedIn company page cover on white, with the lockup and tagline set to the right of centre" />
</Frame>

**This is a different layout, not the profile banner scaled down.** At 191 pixels tall a centred stack leaves both the lockup and the words too small to read, and LinkedIn drops the company logo tile onto the lower left of this image. So the composition sits right of centre and **the left third is deliberately empty.** Do not "fix" that empty space.

---

## The logo tile — 300 × 300

The LinkedIn **company logo** slot, and the right file for any square avatar: Google Business Profile, a favicon source, a social account.

<Frame caption="houseloop-logo-300x300-blue.png — THE DEFAULT">
  <img src="/images/brand/houseloop-logo-300x300-blue.png" alt="The HouseLoop mark in white on a brand-blue square" width="150" />
</Frame>

<CardGroup cols={2}>
  <Card title="houseloop-logo-300x300-navy.png">
    <img src="/images/brand/houseloop-logo-300x300-navy.png" alt="The HouseLoop mark in white on a navy square" width="120" />
    For dark material, or where a blue tile would sit next to other blue.
  </Card>
  <Card title="houseloop-logo-300x300-white.png">
    <img src="/images/brand/houseloop-logo-300x300-white.png" alt="The HouseLoop mark in brand blue on a white square" width="120" />
    Only inside something that already draws a border or a card.
  </Card>
</CardGroup>

**The tile is the MARK ALONE — never the lockup.** LinkedIn renders this at roughly 48px in a feed, where a wordmark is an unreadable smudge. The mark survives; the letters do not.

<Warning>
**Do not make the white tile your default on LinkedIn.** Checked at 48px against a white feed: the blue and navy tiles hold their edge and read instantly, and the white one dissolves — there is no boundary between the tile and the page, so it reads as a floating scribble rather than a logo. The white tile is for placements that already supply a border.
</Warning>

<Note>
**The mark is centred on its INK, not on its box.** The drawing sits half a unit above the centre of its own 32-unit viewBox, so dropped into a square untouched it rides visibly high. Every tile here corrects for that, computed rather than nudged by eye. `houseloop-mark.svg` is the scalable master with no ground — it carries no text, so unlike the banners it needs no font and scales anywhere.
</Note>

---

## The lockup

The mark is a roofline over a loop, and it always sits **to the left** of the letters on a shared baseline. The wordmark is `House` in bold beside `Loop` in regular weight — the weight change is what separates the two words, never a space.

| Ground | Mark | `House` | `Loop` |
|---|---|---|---|
| **Light** | brand blue | navy | brand blue |
| **Dark, at display size** | brand blue | white | **brand blue** |
| **Dark, at UI size** | white | white | white, regular weight |

<Warning>
**The dark ground has two different treatments, and the discriminator is SIZE, not taste.**

At UI size — the site's own navy bands, where the mark is about 24px — the whole lockup goes white. Brand blue on navy is too dim to read that small, and `PageHeader.tsx` already made that call.

At display size — these banners, where the wordmark is 122px — the same blue carries easily, so the two-colour wordmark survives and `Loop` keeps its brand blue. **The rule was always about legibility at size; it was never a rule that blue may not touch navy.**

If you are unsure which you are making, you are making the UI size. Go all white.
</Warning>

---

## The tagline

> ## The Smart Way Home<span style={{ color: "#2D6DFF" }}>.</span>

**The period is brand blue and it is part of the wordmark, not punctuation.** Set it in the same size as the words and change only its colour. A tagline typeset without the blue period is not the tagline.

On the banners it is set in medium weight, a step lighter than the wordmark's bold — so the two read as a hierarchy rather than as two competing headlines.

---

## Colours

Only two colours carry the brand. Both are already tokens in the app — `app/globals.css`.

| Token | Hex | What it is |
|---|---|---|
| `--hl-brand` | `#2D6DFF` | Brand blue. The mark, the `Loop`, the period, the primary button. |
| `--hl-navy` | `#0A1128` | Navy. Dark grounds, the footer, body type on white. |

<Warning>
**No green.** Green means the SMS channel or a real success state, nowhere else — see [Design Standards → Overview](/design/overview). And never paste a raw hex into app code: use the token.
</Warning>

---

## Typeface

**DM Sans**, the site's own face (`app/layout.tsx`, via `next/font/google`). The banners use 700 for the wordmark's `House`, 400 for `Loop`, and 500 for the tagline. Free from [Google Fonts](https://fonts.google.com/specimen/DM+Sans).

---

## How these were made, and how to remake them

The source is a single HTML file — `docs-site/images/brand/banner-source.html` — rendered through real Chrome at LinkedIn's exact pixel dimensions. Every value in it is lifted from the app: the mark's own SVG path, the wordmark's weights, the two colour tokens, and DM Sans loaded as a webfont.

<Note>
**Chrome, not a command-line converter, and the reason is worth knowing.** `rsvg-convert` and ImageMagick were both available and both rejected: DM Sans is not installed as a system font on the build machine, so either would have quietly substituted a different face and shipped a wordmark that is not the brand's. Rendering through a browser that loads the real webfont is the only way the letters are certain to be right.
</Note>

To change a banner, edit the source, re-render at the same dimensions, and replace the file under `docs-site/images/brand/`. Keep the filenames — they carry their dimensions, which is what stops someone uploading a profile banner into a company cover slot.
